---
phase: 29.5-merchant-scope-role-model-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - docs/contract-spec.md
  - scripts/seed_demo.py
  - tests/conftest.py
autonomous: true
requirements: [MER-01]
must_haves:
  truths:
    - "MER-01 is visible in REQUIREMENTS.md traceability before implementation starts."
    - "Seed and shared fixtures expose at least two same-tenant merchants with support and manager users bound to merchant ids."
    - "Admin remains unbound; legacy merchant users remain bound for compatibility tests."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Active MER-01 requirement traceability"
      contains: "MER-01"
    - path: "docs/contract-spec.md"
      provides: "Normative role-to-merchant-scope contract delta"
      contains: "merchant_bound_roles = {\"support\", \"manager\", \"merchant\"}"
    - path: "tests/conftest.py"
      provides: "Two-merchant fixture base for merchant isolation tests"
      contains: "second_merchant"
  key_links:
    - from: "scripts/seed_demo.py"
      to: "tests/conftest.py"
      via: "demo and test identities share merchant-bound support/manager semantics"
      pattern: "support.*merchant_id|manager.*merchant_id"
---

<objective>
Prepare Phase 29.5 implementation by reconciling requirement traceability, contract text, seed data, and shared fixtures.

Purpose: Later code plans should fail against real merchant-bound fixture data instead of old support/manager wildcard assumptions.
Output: MER-01 appears in planning traceability, contract text states the target semantics, and demo/test identity data has two merchants with bound support/manager users.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-SPEC.md
@.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-CONTEXT.md
@.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-VALIDATION.md
@docs/contract-spec.md
@scripts/seed_demo.py
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MER-01 traceability and reconcile contract delta</name>
  <files>.planning/REQUIREMENTS.md, docs/contract-spec.md</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/29.5-merchant-scope-role-model-alignment/29.5-SPEC.md, docs/contract-spec.md</read_first>
  <action>
    Add active requirement `MER-01` to `.planning/REQUIREMENTS.md` if it is missing. Wording must preserve: `support` / `manager` / legacy `merchant` are merchant-bound for business data; `admin` is the only platform-wide human business-data role; tenant public policy remains shared; AgentRun / Approval / ActionDraft / Memory / Replay / DB hardening follow-ups are routed to named later phases.

    In `docs/contract-spec.md`, reconcile old contract text with Phase 29.5 rather than duplicating sections. Ensure the contract states:
    - `merchant_bound_roles = {"support", "manager", "merchant"}`.
    - `platform_admin_roles = {"admin"}`.
    - Unknown roles default deny-all for business merchant scope.
    - Non-admin human wildcard `server_merchant_scope` is rejected, not silently narrowed.
    - `server_tool_permissions` never widens merchant scope.
    - Tenant public policy retrieval is separate from business merchant scope and is not denied solely because business `merchant_scope=[]`.
    - Business-data AgentRun/trace/replay visibility is owner/admin-only until later target merchant binding phases.
  </action>
  <acceptance_criteria>
    - `rg -n "MER-01" .planning/REQUIREMENTS.md` prints an active requirement line and traceability line.
    - `rg -n 'merchant_bound_roles = \\{"support", "manager", "merchant"\\}|platform_admin_roles = \\{"admin"\\}|server_tool_permissions|tenant public policy|owner/admin' docs/contract-spec.md` finds all target clauses.
  </acceptance_criteria>
  <verify>rg -n "MER-01" .planning/REQUIREMENTS.md && rg -n 'merchant_bound_roles = \\{"support", "manager", "merchant"\\}|platform_admin_roles = \\{"admin"\\}|server_tool_permissions|tenant public policy|owner/admin' docs/contract-spec.md</verify>
  <done>Planning and contract sources state the same MER-01 target semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Bind demo and fixture users to merchants</name>
  <files>scripts/seed_demo.py, tests/conftest.py</files>
  <read_first>scripts/seed_demo.py, tests/conftest.py, .planning/phases/29.5-merchant-scope-role-model-alignment/29.5-CONTEXT.md</read_first>
  <action>
    Update `scripts/seed_demo.py` so the demo tenant has at least two merchants with support and manager users bound to merchant ids. Keep admin users role `admin` with `merchant_id=None`. Keep at least one legacy role `merchant` user bound to a merchant id.

    Update `tests/conftest.py` before production code changes. Shared fixtures must include:
    - Primary demo merchant with `cs_zhang` role `support`, `approval_manager` role `manager`, and `merchant_wang` role `merchant` bound to that merchant.
    - A second same-tenant merchant with `cs_other_merchant` role `support` and `manager_other_merchant` role `manager` bound to that second merchant.
    - Same-tenant second merchant resources returned as `second_merchant`, `second_order`, `second_refund_case`, and `second_ticket`.
    - `admin_user` role `admin` with no merchant binding.
    - `other_support` in a different tenant, bound to that tenant's merchant.
  </action>
  <acceptance_criteria>
    - `rg -n '"second_merchant"|"second_order"|"second_refund_case"|"second_ticket"|cs_other_merchant|manager_other_merchant' tests/conftest.py` finds all fixture keys/users.
    - `rg -n 'role="support"|role="manager"|role="merchant"|role="admin"' tests/conftest.py scripts/seed_demo.py` shows the required role set is present.
    - Manual inspection confirms admin user creation does not set `merchant_id`.
  </acceptance_criteria>
  <verify>rg -n '"second_merchant"|"second_order"|"second_refund_case"|"second_ticket"|cs_other_merchant|manager_other_merchant' tests/conftest.py && rg -n 'role="support"|role="manager"|role="merchant"|role="admin"' tests/conftest.py scripts/seed_demo.py</verify>
  <done>Seed and shared fixture identities expose same-tenant merchant isolation cases without old unbound support/manager noise.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "MER-01" .planning/REQUIREMENTS.md`
- [ ] `rg -n '"second_merchant"|"second_order"|"second_refund_case"|"second_ticket"|cs_other_merchant|manager_other_merchant' tests/conftest.py`
- [ ] `git diff --check`
</verification>

<success_criteria>
- MER-01 traceability exists.
- Contract text has no old supervisor/manager tenant-wide contradiction for Phase 29.5.
- Fixtures and seed data are ready for later code/test rewrites.
</success_criteria>

<output>
After completion, create `.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-01-SUMMARY.md`.
</output>
