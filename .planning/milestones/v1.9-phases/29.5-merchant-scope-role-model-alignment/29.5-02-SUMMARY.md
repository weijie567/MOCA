---
phase: 29.5-merchant-scope-role-model-alignment
plan: 02
subsystem: auth
tags: [trusted-context, merchant-scope, permissions, fastapi]

requires:
  - phase: 29.5-01
    provides: Seed, fixture, contract, and MER-01 alignment for merchant-bound users
provides:
  - TrustedContextFactory role-to-merchant-scope matrix for support, manager, merchant, admin, and unknown roles
  - Non-admin server_merchant_scope no-widening enforcement
  - Route-level require_merchant_access helper for later business API migration
affects: [Phase 29.5 Plan 03, Phase 29.5 Plan 04, Phase 29.5 Plan 05, Phase 30 BusinessFactService]

tech-stack:
  added: []
  patterns: [deny-first merchant scope derivation, FastAPI HTTPException helper]

key-files:
  created: []
  modified:
    - src/platform/trusted_context.py
    - src/auth/permissions.py
    - tests/platform/test_trusted_context_factory.py
    - tests/platform/test_merchant_scope.py
    - .planning/LOCAL-VALIDATION-ISSUES.md

key-decisions:
  - "support, manager, and legacy merchant derive merchant_scope from user.merchant_id."
  - "admin is the only human role that derives wildcard business merchant scope."
  - "Non-admin server_merchant_scope can only narrow to the already-derived merchant id; wildcard or added ids raise ValueError."
  - "require_merchant_access is a route helper only; routes still own tenant-first 404 behavior."

patterns-established:
  - "TrustedContextFactory role matrix constants: MERCHANT_BOUND_ROLES and PLATFORM_ADMIN_ROLES."
  - "FastAPI business resource helper raises 403 using the existing FORBIDDEN error shape."

requirements-completed: [MER-01]

duration: 5min
completed: 2026-06-27
---

# Phase 29.5 Plan 02: Trusted Scope Foundation Summary

**TrustedContextFactory now derives merchant-bound business scopes and route code has a shared deny-first merchant access helper.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-06-27T09:57:58Z
- **Completed:** 2026-06-27T10:02:37Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Replaced support/manager wildcard merchant scope expectations with merchant-bound role matrix tests.
- Implemented TrustedContextFactory merchant scope derivation for support, manager, legacy merchant, admin, and unknown roles.
- Added no-widening enforcement for non-admin server_merchant_scope overrides.
- Added `require_merchant_access(...)` in `src/auth/permissions.py` with focused tests for admin allow, same-merchant allow, missing merchant deny, mismatch deny, and unknown role deny.

## Task Commits

1. **Task 1 RED: Factory merchant scope tests** - `8b4307c` (test)
2. **Task 1 GREEN: TrustedContextFactory role matrix** - `c5ec2ea` (feat)
3. **Task 2 RED: Merchant access helper tests** - `ccb6f5f` (test)
4. **Task 2 GREEN: Merchant access helper** - `fb016e3` (feat)

## Files Created/Modified

- `src/platform/trusted_context.py` - Added role constants, merchant-bound scope derivation, unknown-role deny-all, and server override no-widening checks.
- `src/auth/permissions.py` - Added `require_merchant_access(...)` and shared role constants for route-level business access checks.
- `tests/platform/test_trusted_context_factory.py` - Rewrote factory expectations for Phase 29.5 role matrix and server override behavior.
- `tests/platform/test_merchant_scope.py` - Added focused helper tests.
- `.planning/LOCAL-VALIDATION-ISSUES.md` - Recorded the existing architecture boundary red test that belongs to Plan 05 scope.

## Decisions Made

- Non-admin wildcard `server_merchant_scope` raises `ValueError`; it is not silently narrowed.
- Non-admin explicit other-merchant server overrides also raise `ValueError`.
- The helper compares merchant ids as strings so UUID and string call sites can share it.
- The helper intentionally does not perform tenant lookup or DB access; API routes remain responsible for tenant-first 404.

## Deviations from Plan

### Auto-fixed Issues

None - implementation stayed within Plan 02 source/test scope.

### Verification Deviation

**[Scope Boundary] Task 1 broader verify includes a pre-existing Plan 05 architecture red test**
- **Found during:** Task 1 verification
- **Issue:** `uv run pytest tests/platform/test_trusted_context_factory.py tests/platform/test_trusted_context.py tests/architecture/test_trusted_context_boundaries.py -q` fails because `src/agent/nodes/investigate.py` still directly constructs `ToolCallContext`.
- **Fix:** Not fixed in Plan 02 to avoid pulling Plan 05 investigate work into wave 2. Recorded in `.planning/LOCAL-VALIDATION-ISSUES.md`.
- **Files modified:** `.planning/LOCAL-VALIDATION-ISSUES.md`
- **Verification:** Plan-level verification passed with `uv run pytest tests/platform/test_trusted_context_factory.py tests/platform/test_merchant_scope.py -q` (`42 passed`).
- **Committed in:** Plan completion metadata commit.

---

**Total deviations:** 0 auto-fixed, 1 verification scope-boundary note.
**Impact on plan:** Factory and helper deliverables are complete. The remaining architecture boundary failure is already in Plan 05 scope and does not change Plan 02 behavior.

## Issues Encountered

- Task 1 broader verify surfaced the existing `investigate.py` direct `ToolCallContext` construction. It is recorded for Plan 05.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plan 03 can import `require_merchant_access(...)` for orders/refunds/tickets once wave 3 begins. Plan 04/05 can rely on `TrustedContextFactory` producing deny-all `[]` for missing merchant-bound users and wildcard only for admin.

---
*Phase: 29.5-merchant-scope-role-model-alignment*
*Completed: 2026-06-27*

## Self-Check: PASSED

- Plan-level verification passed: `uv run pytest tests/platform/test_trusted_context_factory.py tests/platform/test_merchant_scope.py -q`
- `git diff --check` passed before summary creation.
- Task 1 acceptance criteria passed.
- Task 2 acceptance criteria passed.
