---
phase: 34-approval-and-actiondraft-boundary-hardening
plan: 34-03
type: execute
wave: 3
depends_on:
  - 34-01
  - 34-02
files_modified:
  - src/approvals/schemas.py
  - src/approvals/repository.py
  - src/approvals/service.py
  - src/approvals/policy.py
  - src/agent/nodes/approval_gate.py
  - src/api/routers/approvals.py
  - src/api/schemas/approvals.py
  - tests/test_approval_api.py
  - tests/approvals/test_service_transitions.py
  - tests/platform/test_merchant_scope.py
  - tests/tools/test_merchant_scope_static.py
autonomous: true
requirements:
  - APF-15
  - APF-16
user_setup: []
must_haves:
  truths:
    - "ApprovalService persists Phase 34 binding fields when creating requests and returns them in trusted resume payloads."
    - "Manager list/get/decide is restored only for approvals whose target merchant is inside the actor's trusted merchant scope."
    - "Approval resume injects `tool:create_coupon_grant_draft` for approved decisions without widening non-admin merchant scope."
  artifacts:
    - path: "src/approvals/service.py"
      provides: "binding-aware approval create/decision/resume behavior"
      contains: "target_merchant_id"
    - path: "src/api/routers/approvals.py"
      provides: "same-merchant manager approval access and safe resume config"
      contains: "ACTION_DRAFT_PERMISSION"
    - path: "tests/test_approval_api.py"
      provides: "manager same-merchant allow and cross-merchant/missing-target deny tests"
      contains: "manager"
  key_links:
    - from: "src/api/routers/approvals.py"
      to: "src/platform/trusted_context.py"
      via: "TrustedContextFactory.create_from_request without wildcard server_merchant_scope"
      pattern: "create_from_request"
    - from: "src/approvals/service.py"
      to: "src/approvals/repository.py"
      via: "create_request_with_single_level persists Phase 34 binding fields"
      pattern: "target_merchant_id"
---

<objective>
Harden approval creation, trusted resume, and manager-scoped approval API access around persisted Phase 34 bindings.

Purpose: satisfy APF-15/APF-16 and decisions D-07, D-10..D-15, D-20..D-22, D-28..D-31 after risk_gate produces structured approval plans.
Output: ApprovalService/repository/API binding propagation, same-merchant manager restore, no-wildcard resume tests, and approval_gate display-only enforcement.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-approval-and-actiondraft-boundary-hardening/34-CONTEXT.md
@.planning/phases/34-approval-and-actiondraft-boundary-hardening/34-PATTERNS.md
@.planning/phases/34-approval-and-actiondraft-boundary-hardening/34-VALIDATION.md
@.planning/todos/deferred/2026-06-27-merchant-scope-approval-action.md
@docs/contract-spec.md
@src/approvals/schemas.py
@src/approvals/service.py
@src/approvals/repository.py
@src/approvals/policy.py
@src/agent/nodes/approval_gate.py
@src/api/routers/approvals.py
@src/platform/trusted_context.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist and return approval binding fields through ApprovalService</name>
  <files>src/approvals/schemas.py, src/approvals/repository.py, src/approvals/service.py, src/agent/nodes/approval_gate.py, tests/approvals/test_service_transitions.py, tests/test_approval_api.py</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/34-approval-and-actiondraft-boundary-hardening/34-CONTEXT.md
    - .planning/phases/34-approval-and-actiondraft-boundary-hardening/34-PATTERNS.md
    - docs/contract-spec.md sections 15.3-15.5
    - src/approvals/schemas.py
    - src/approvals/repository.py
    - src/approvals/service.py
    - src/agent/nodes/approval_gate.py
    - tests/approvals/test_service_transitions.py
    - tests/test_approval_api.py
  </read_first>
  <behavior>
    - Test `ApprovalService.create_request` persists target merchant, business fact refs, verified evidence refs, claim verification summary, risk decision, approval idempotency key, action hash, and snapshot refs.
    - Test `TrustedApprovalResultV1` from accept/approve contains the exact persisted binding fields from the active revision.
    - Test edit returns risk reroute and does not authorize direct draft creation with the old binding.
  </behavior>
  <action>
    Extend `ApprovalRepository.create_request_with_single_level(...)` to accept and persist `target_merchant_id`, `target_merchant_ref`, `business_fact_refs`, `verified_evidence_refs`, `claim_verification_ref`, `claim_verification_summary`, `risk_decision_ref`, `risk_decision`, and `approval_idempotency_key`.

    Update `ApprovalService.create_request(...)` to copy those fields from `ApprovalRequestCreateCommand`, including `approval_idempotency_key`; set `verified_evidence_refs` from `command.verified_evidence_refs` when present, otherwise from existing `command.evidence_refs` for compatibility. Do not reconstruct these refs from `proposed_action["reason"]`, memory, raw tool data, final response text, or LLM output.

    Update `ApprovalService._decision_result(...)` and returned `ApprovalDecisionResult.resume_payload` so `TrustedApprovalResultV1` includes the persisted `target_merchant_id`, `target_merchant_ref`, `business_fact_refs`, `verified_evidence_refs`, `claim_verification_ref`, `claim_verification_summary`, `risk_decision_ref`, and `risk_decision` for the exact active revision.

    Update `src/agent/nodes/approval_gate.py` interrupt payload to pass through only the structured `approval_plan` and safe binding refs from state: `target_merchant_id`, `target_merchant_ref`, `business_fact_refs`, `verified_evidence_refs`, `claim_verification_ref`, `claim_verification_summary`, `risk_decision_ref`, `risk_decision`, and `approval_idempotency_key`. Prefer `approval_plan["approval_idempotency_key"]` and fail closed if it is missing when an approval plan requires approval. Keep the comment that proposed action is display-only and ApprovalService owns persistence.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/approvals/test_service_transitions.py tests/test_approval_api.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/approvals/schemas.py src/approvals/repository.py src/approvals/service.py src/agent/nodes/approval_gate.py tests/approvals/test_service_transitions.py tests/test_approval_api.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "target_merchant_id|business_fact_refs|verified_evidence_refs|risk_decision_ref|approval_idempotency_key" src/approvals/repository.py src/approvals/service.py src/agent/nodes/approval_gate.py` returns matches.
    - `rg -n "requested_by.*merchant|merchant_id.*requested_by" src/approvals src/api/routers/approvals.py` returns no matches.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/approvals/test_service_transitions.py tests/test_approval_api.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>Approval requests and trusted resume payloads carry exact persisted Phase 34 binding fields, and approval_gate remains display/interrupt only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restore manager approval scope without wildcard resume</name>
  <files>src/approvals/policy.py, src/api/routers/approvals.py, src/api/schemas/approvals.py, tests/test_approval_api.py, tests/platform/test_merchant_scope.py, tests/tools/test_merchant_scope_static.py</files>
  <read_first>
    - AGENTS.md
    - .planning/todos/deferred/2026-06-27-merchant-scope-approval-action.md
    - .planning/phases/34-approval-and-actiondraft-boundary-hardening/34-CONTEXT.md
    - docs/contract-spec.md section 8.0.1
    - src/platform/trusted_context.py
    - src/approvals/policy.py
    - src/api/routers/approvals.py
    - src/api/schemas/approvals.py
    - tests/test_approval_api.py
    - tests/platform/test_merchant_scope.py
    - tests/tools/test_merchant_scope_static.py
  </read_first>
  <behavior>
    - Test manager with matching `user.merchant_id` can list/get/decide a pending approval whose `target_merchant_id` matches.
    - Test manager cannot list/get/decide cross-merchant approvals and receives 403 for same-tenant out-of-scope resources.
    - Test manager cannot list/get/decide approvals with missing `target_merchant_id`.
    - Test approval resume calls `TrustedContextFactory.create_from_request(..., server_tool_permissions=["tool:create_coupon_grant_draft"])` without `server_merchant_scope` for non-admin actors.
  </behavior>
  <action>
    Change approval reviewer roles from admin-only to explicit `{"admin", "manager"}` in `src/approvals/policy.py` and `src/api/routers/approvals.py`, but gate manager access with persisted target merchant binding.

    Add a helper in `src/api/routers/approvals.py` named `_assert_approval_scope(user: User, approval: ApprovalRequest) -> None` with exact behavior:
    - If `user.role == "admin"`, allow.
    - If `user.role == "manager"` and `approval.target_merchant_id` is non-null and equals `user.merchant_id` as strings, allow.
    - If `user.role == "manager"` and target merchant is missing or different, raise `HTTPException(status_code=403, detail={"code": "FORBIDDEN", "message": "Approval target merchant is outside actor scope"})`.
    - All other roles keep the existing 403.

    Apply `_assert_approval_scope(...)` to list/get/decide/info/recoverable-resume paths before returning approval details or calling `ApprovalService.decide`. For list, filter the loaded approvals so managers only receive same-merchant approvals and exclude missing-target approvals; admin still receives all tenant pending approvals.

    Update API response schema and `_to_response(...)` to include `target_merchant_id`, `business_fact_refs`, `verified_evidence_refs`, `claim_verification_ref`, `claim_verification_summary`, `risk_decision_ref`, and safe `risk_decision`.

    Keep `_resume_graph_config(...)` using `TrustedContextFactory.create_from_request` with `server_tool_permissions=permissions` only. Do not pass `server_merchant_scope` for human approval resume. Preserve D-14: `ACTION_DRAFT_PERMISSION` may be injected by server code but must not widen merchant scope.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_approval_api.py tests/platform/test_merchant_scope.py tests/tools/test_merchant_scope_static.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/approvals/policy.py src/api/routers/approvals.py src/api/schemas/approvals.py tests/test_approval_api.py tests/platform/test_merchant_scope.py tests/tools/test_merchant_scope_static.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "APPROVAL_ROLES = \\{\"admin\", \"manager\"\\}|_assert_approval_scope|target_merchant_id" src/api/routers/approvals.py src/approvals/policy.py` returns matches.
    - `rg -n "server_merchant_scope" src/api/routers/approvals.py` returns no matches.
    - `rg -n "requested_by.*merchant|merchant_id.*requested_by" src/api/routers/approvals.py tests/test_approval_api.py` returns no matches.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_approval_api.py tests/platform/test_merchant_scope.py tests/tools/test_merchant_scope_static.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>Manager approval access is restored only through explicit target merchant binding, and resume injects tool permission without wildcard merchant scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approval API actor -> ApprovalService | Authenticated human reviewer commands enter service transition logic. |
| ApprovalService result -> graph resume | Trusted resume payload crosses from API/service into LangGraph. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03-01 | Elevation of Privilege | `src/api/routers/approvals.py` | mitigate | Same-merchant manager access checks persisted `target_merchant_id`; missing/cross-merchant target fails closed. |
| T-34-03-02 | Spoofing | `_resume_graph_config` | mitigate | Use `TrustedContextFactory.create_from_request` with server tool permission only; no non-admin wildcard `server_merchant_scope`. |
| T-34-03-03 | Tampering | `ApprovalService._decision_result` | mitigate | Trusted resume payload includes exact persisted active-revision binding refs and hashes. |
</threat_model>

<verification>
```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_approval_api.py tests/approvals/test_service_transitions.py tests/platform/test_merchant_scope.py tests/tools/test_merchant_scope_static.py -q --tb=short
UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/approvals/schemas.py src/approvals/repository.py src/approvals/service.py src/approvals/policy.py src/agent/nodes/approval_gate.py src/api/routers/approvals.py src/api/schemas/approvals.py tests/test_approval_api.py tests/approvals/test_service_transitions.py tests/platform/test_merchant_scope.py tests/tools/test_merchant_scope_static.py
```
</verification>

<success_criteria>
- ApprovalService and API propagate Phase 34 binding fields without prompt/raw reconstruction.
- Manager approval list/get/decide is same-merchant only; missing target merchant fails closed.
- Non-admin human approval resume never receives wildcard `server_merchant_scope`.
</success_criteria>

<output>
After completion, create `.planning/phases/34-approval-and-actiondraft-boundary-hardening/34-03-SUMMARY.md`.
</output>
