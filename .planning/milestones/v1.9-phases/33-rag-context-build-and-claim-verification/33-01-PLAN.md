---
phase: 33-rag-context-build-and-claim-verification
plan: 33-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/knowledge/schemas.py
  - src/knowledge/service.py
  - src/agent/rag_context/schemas.py
  - src/agent/rag_context/claims.py
  - src/agent/state.py
  - src/agent/nodes/receive_request.py
  - tests/knowledge/test_verified_evidence_package.py
  - tests/knowledge/test_claim_verification_bundle.py
  - tests/knowledge/test_tenant_scope.py
  - tests/agent/rag_context/test_material_claims.py
  - tests/agent/test_nodes/test_receive_request.py
autonomous: true
requirements:
  - APF-13
  - APF-14
user_setup: []
must_haves:
  truths:
    - "KnowledgeService exposes strict public contracts for VerifiedEvidencePackageV1, MaterialClaimV1, and ClaimVerificationBundleV1."
    - "AgentState declares and resets Phase 33 RAG and claim fields every turn."
    - "KnowledgeService owns build_verified_context and verify_claims boundaries used by later graph nodes."
  artifacts:
    - path: "src/knowledge/schemas.py"
      provides: "KnowledgeService-owned public DTOs and exact status/route literals"
    - path: "src/knowledge/service.py"
      provides: "build_verified_context and verify_claims public methods"
    - path: "src/agent/state.py"
      provides: "Phase 33 AgentState fields"
    - path: "src/agent/nodes/receive_request.py"
      provides: "per-turn reset for package/bundle fields"
  key_links:
    - from: "src/knowledge/service.py"
      to: "src/agent/rag_context/builder.py"
      via: "PolicyKnowledgeService.build_verified_context"
      pattern: "build_verified_context.*ContextBuilder"
    - from: "src/knowledge/service.py"
      to: "src/agent/rag_context/verifier.py"
      via: "PolicyKnowledgeService.verify_claims"
      pattern: "verify_claims.*MaterialClaimVerifier"
---

<objective>
Create the Phase 33 contract, AgentState lifecycle, and KnowledgeService public boundary.

Purpose: satisfy APF-13/APF-14 foundation and decisions D-01, D-02, D-07, D-08, D-10..D-15, D-18..D-23, D-29, and D-30 before graph nodes consume the new fields.
Output: strict DTOs, service methods, state/reset fields, and focused contract tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-rag-context-build-and-claim-verification/33-CONTEXT.md
@.planning/phases/33-rag-context-build-and-claim-verification/33-RESEARCH.md
@.planning/phases/33-rag-context-build-and-claim-verification/33-PATTERNS.md
@.planning/phases/33-rag-context-build-and-claim-verification/33-VALIDATION.md
@docs/contract-spec.md
@docs/target-agent-platform-architecture-plan.md
@docs/eval-test-plan.md
@src/knowledge/schemas.py
@src/knowledge/service.py
@src/agent/rag_context/schemas.py
@src/agent/rag_context/claims.py
@src/agent/rag_context/builder.py
@src/agent/rag_context/verifier.py
@src/agent/state.py
@src/agent/nodes/receive_request.py
@tests/knowledge/test_phase22_evidence_validation.py
@tests/knowledge/test_service.py
@tests/knowledge/test_tenant_scope.py
@tests/agent/rag_context/test_material_claims.py
@tests/agent/test_nodes/test_receive_request.py

<interfaces>
Contract literals to implement exactly:
```python
RAG_CONTEXT_STATUSES = (
    "not_required", "verified", "partial", "no_evidence", "unauthorized",
    "stale", "conflict", "invalid_hash", "invalid_scope", "build_error",
)
CLAIM_TYPES = ("policy", "business_fact", "action_recommendation")
CLAIM_BUNDLE_ROUTES = ("continue", "final_response", "manual_review")
```

Existing service method to preserve:
```python
class PolicyKnowledgeService:
    async def search(self, request: KnowledgeSearchRequest, context: KnowledgeContext) -> KnowledgeSearchResult: ...
    async def get_verified_evidence_details(...) -> VerifiedEvidenceDetailsResult: ...
```
</interfaces>

No schema push is planned. Keep package and bundle payloads in Pydantic state and existing JSONB trace/step surfaces; if execution adds DB columns, tables, migrations, or CHECK-constrained event types, stop and add a blocking migration task before implementation continues.
</context>

<must_haves>
- APF-13: `VerifiedEvidencePackageV1.status` accepts exactly `not_required`, `verified`, `partial`, `no_evidence`, `unauthorized`, `stale`, `conflict`, `invalid_hash`, `invalid_scope`, and `build_error`.
- APF-13: `VerifiedEvidencePackageV1` contains `package_id`, `evidence_items`, `citation_map`, `evidence_map`, prompt/verifier/replay/debug projections, rejected/stale/conflict refs, reason codes, `policy_version`, and `retrieval_config_version`.
- APF-14: `MaterialClaimV1` uses `claim_type` values `policy`, `business_fact`, and `action_recommendation`; compatibility helpers may accept existing `authority_class` inputs but must emit the canonical target fields.
- APF-14: `ClaimVerificationBundleV1` contains claim-level results, blocked claims, safe support refs, reason codes, and verifier policy version.
- State lifecycle: `verified_evidence_package`, `citation_map`, `evidence_map`, `material_claims`, `claim_verification_bundle`, `blocked_claims`, and `safe_support_refs` reset on a new turn.
- Scope separation: tenant public policy evidence remains separate from merchant-scoped business fact/action authority; business fact and action recommendation claims require `BusinessFactRefV1` / `BusinessFactResultV1`.
</must_haves>

<tasks>

<task id="33-01-01" type="auto" tdd="true">
  <name>Task 33-01-01: Add strict KnowledgeService-owned DTOs</name>
  <files>src/knowledge/schemas.py, src/agent/rag_context/schemas.py, src/agent/rag_context/claims.py, tests/knowledge/test_verified_evidence_package.py, tests/knowledge/test_claim_verification_bundle.py, tests/agent/rag_context/test_material_claims.py</files>
  <read_first>
- docs/contract-spec.md
- .planning/phases/33-rag-context-build-and-claim-verification/33-CONTEXT.md
- .planning/phases/33-rag-context-build-and-claim-verification/33-RESEARCH.md
- .planning/phases/33-rag-context-build-and-claim-verification/33-PATTERNS.md
- src/knowledge/schemas.py
- src/agent/rag_context/schemas.py
- src/agent/rag_context/claims.py
- src/tools/contracts.py
- tests/knowledge/test_verified_evidence_package.py
- tests/knowledge/test_claim_verification_bundle.py
- tests/agent/rag_context/test_material_claims.py
  </read_first>
  <action>
Add strict Pydantic DTOs in `src/knowledge/schemas.py`: `EvidenceItemV1`, `VerifiedEvidencePackageV1`, `MaterialClaimV1`, `ClaimVerificationResultV1`, and `ClaimVerificationBundleV1`.

Use `ConfigDict(extra="forbid")` on every new public DTO. Use schema version literals exactly `evidence_item.v1`, `verified_evidence_package.v1`, `material_claim.v1`, `claim_verification_result.v1`, and `claim_verification_bundle.v1`. Use the exact status, claim type, support status, semantic review status, overall status, and bundle route literals from `docs/contract-spec.md` section 8.3.

Keep `EvidenceRefV1` and `BusinessFactRefV1` as the canonical ref types. Do not define reduced variants for replay, snapshot, or tool results. In `src/agent/rag_context/schemas.py` and `src/agent/rag_context/claims.py`, add compatibility adapters so existing `MaterialClaim` / `authority_class` payloads can be normalized into `MaterialClaimV1` with this mapping: `policy_claim -> policy`, `business_fact_claim -> business_fact`, `action_recommendation_claim -> action_recommendation`; `source_node` maps to `generated_from_step`.

Create `tests/knowledge/test_verified_evidence_package.py` and `tests/knowledge/test_claim_verification_bundle.py`. Update `tests/agent/rag_context/test_material_claims.py` so tests assert the canonical `MaterialClaimV1` field names and the compatibility mapping above.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/knowledge/test_verified_evidence_package.py tests/knowledge/test_claim_verification_bundle.py tests/agent/rag_context/test_material_claims.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/knowledge/schemas.py src/agent/rag_context/schemas.py src/agent/rag_context/claims.py tests/knowledge/test_verified_evidence_package.py tests/knowledge/test_claim_verification_bundle.py tests/agent/rag_context/test_material_claims.py</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "class VerifiedEvidencePackageV1|class MaterialClaimV1|class ClaimVerificationBundleV1|extra=.forbid|ConfigDict\\(extra=.forbid" src/knowledge/schemas.py` finds the target DTOs and strict config.
- `rg -n "not_required|invalid_hash|invalid_scope|build_error" src/knowledge/schemas.py tests/knowledge/test_verified_evidence_package.py` finds the exact `rag_context_status` literals.
- `rg -n "claim_type|generated_from_step|policy_claim.*policy|action_recommendation_claim.*action_recommendation" src/agent/rag_context/claims.py tests/agent/rag_context/test_material_claims.py` finds the compatibility mapping.
  </acceptance_criteria>
  <done>Strict target DTOs exist, compatibility claim mapping is tested, and schema/material-claim tests pass.</done>
</task>

<task id="33-01-02" type="auto" tdd="true">
  <name>Task 33-01-02: Declare and reset Phase 33 AgentState fields</name>
  <files>src/agent/state.py, src/agent/nodes/receive_request.py, tests/agent/test_nodes/test_receive_request.py</files>
  <read_first>
- docs/contract-spec.md
- .planning/phases/33-rag-context-build-and-claim-verification/33-CONTEXT.md
- src/agent/state.py
- src/agent/nodes/receive_request.py
- tests/agent/test_nodes/test_receive_request.py
  </read_first>
  <action>
Update `src/agent/state.py` so `AgentState` declares these target fields with clear types: `rag_context_status`, `verified_evidence_package`, `citation_map`, `evidence_map`, `material_claims`, `claim_verification_bundle`, `blocked_claims`, and `safe_support_refs`.

Update `src/agent/nodes/receive_request.py` so every new field resets on a new turn. Reset values must be: `rag_context_status: None`, `verified_evidence_package: None`, `citation_map: {}`, `evidence_map: {}`, `material_claims: []`, `claim_verification_bundle: None`, `blocked_claims: []`, and `safe_support_refs: []`.

Preserve existing compatibility resets for `rag_context_bundle`, `rag_verification`, `verifier_status`, `verification_route`, `verifier_reason_codes`, `verifier_safe_citation_refs`, and `verifier_metrics`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_receive_request.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/state.py src/agent/nodes/receive_request.py tests/agent/test_nodes/test_receive_request.py</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "verified_evidence_package|citation_map|evidence_map|material_claims|claim_verification_bundle|blocked_claims|safe_support_refs" src/agent/state.py src/agent/nodes/receive_request.py tests/agent/test_nodes/test_receive_request.py` finds declarations, resets, and tests.
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_receive_request.py -q --tb=short` passes and includes a test that stale package/bundle values are cleared.
  </acceptance_criteria>
  <done>AgentState fields are declared and every new Phase 33 field resets to the specified value on new turns.</done>
</task>

<task id="33-01-03" type="auto" tdd="true">
  <name>Task 33-01-03: Add KnowledgeService build and verify public methods</name>
  <files>src/knowledge/service.py, src/knowledge/schemas.py, tests/knowledge/test_verified_evidence_package.py, tests/knowledge/test_claim_verification_bundle.py, tests/knowledge/test_tenant_scope.py</files>
  <read_first>
- docs/contract-spec.md
- docs/target-agent-platform-architecture-plan.md
- .planning/phases/33-rag-context-build-and-claim-verification/33-CONTEXT.md
- .planning/phases/33-rag-context-build-and-claim-verification/33-PATTERNS.md
- src/knowledge/service.py
- src/knowledge/schemas.py
- src/agent/rag_context/builder.py
- src/agent/rag_context/verifier.py
- tests/knowledge/test_verified_evidence_package.py
- tests/knowledge/test_claim_verification_bundle.py
- tests/knowledge/test_tenant_scope.py
  </read_first>
  <action>
Add `PolicyKnowledgeService.build_verified_context(...)` and `PolicyKnowledgeService.verify_claims(...)`.

`build_verified_context` must accept candidate `EvidenceRefV1` refs, `business_fact_refs`, `KnowledgeContext`, and an evidence policy/risk hint mapping. It must call the existing `ContextBuilder` / canonical evidence validation path, construct `VerifiedEvidencePackageV1`, and compute status from validation results using this precedence: no evidence required -> `not_required`; all required candidates valid -> `verified`; text hash mismatch -> `invalid_hash`; tenant/scope/doc type failure -> `invalid_scope`; unauthorized/permission reason -> `unauthorized`; latest version/effective date/freshness failure -> `stale`; conflict reason -> `conflict`; some valid and some rejected -> `partial`; none valid -> `no_evidence`; caught expected build failure -> `build_error`.

`verify_claims` must accept `material_claims`, `verified_evidence_package`, `business_context`, and `proposed_action`. It must normalize claims to `MaterialClaimV1`, call the existing `MaterialClaimVerifier` for claim-level checks, and aggregate `ClaimVerificationBundleV1` with route `continue` only when no claim is blocked and no verifier error applies. It must use route `final_response` for unsupported user-visible/action claims, malformed inputs, invalid scope/hash, missing required business facts, timeout, or provider/budget errors. It may use route `manual_review` for ambiguous/manual-review reason codes. Semantic review results must not override identity, scope, hash, effective-date, or business fact authority failures.

Add or update tests in `tests/knowledge/test_tenant_scope.py` proving tenant public policy evidence validation does not create merchant-scoped business fact authority, and tests in `tests/knowledge/test_claim_verification_bundle.py` proving business fact and action recommendation claims require merchant-scoped `BusinessFactRefV1` / `BusinessFactResultV1` authority.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/knowledge/test_verified_evidence_package.py tests/knowledge/test_claim_verification_bundle.py tests/knowledge/test_tenant_scope.py tests/agent/rag_context/test_verifier.py tests/agent/rag_context/test_authority_boundaries.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/knowledge/service.py src/knowledge/schemas.py tests/knowledge/test_verified_evidence_package.py tests/knowledge/test_claim_verification_bundle.py tests/knowledge/test_tenant_scope.py</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "async def build_verified_context|async def verify_claims" src/knowledge/service.py` finds both public methods.
- `rg -n "ContextBuilder|MaterialClaimVerifier|VerifiedEvidencePackageV1|ClaimVerificationBundleV1" src/knowledge/service.py` finds service-boundary delegation and target DTO construction.
- `rg -n "text_hash_mismatch|latest_version_invalid|invalid_hash|invalid_scope|business_fact_ref_required|semantic_review_status" tests/knowledge/test_verified_evidence_package.py tests/knowledge/test_claim_verification_bundle.py` finds tests for hard-gate mappings.
- `rg -n "tenant public policy|merchant-scoped|BusinessFactRefV1|BusinessFactResultV1|business fact authority" tests/knowledge/test_tenant_scope.py tests/knowledge/test_claim_verification_bundle.py` finds explicit tenant-policy vs merchant-business-scope coverage.
  </acceptance_criteria>
  <done>KnowledgeService exposes and tests build_verified_context and verify_claims with package/bundle hard-gate mappings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| graph state -> KnowledgeService | Candidate refs and claims may be stale, malformed, or LLM-influenced before strict DTO validation. |
| KnowledgeService -> prompt/verifier/replay projections | Package internals must be separated so raw/debug data cannot enter ordinary surfaces. |
| old checkpoint -> new turn AgentState | Stale package/bundle fields must not cross turns. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-33-01-01 | Tampering / Elevation of privilege | `VerifiedEvidencePackageV1.status` | mitigate | strict status literals and tests for invalid hash/scope/stale/unauthorized/build-error status mapping. |
| T-33-01-02 | Spoofing / Elevation of privilege | `ClaimVerificationBundleV1` / business claims | mitigate | require `BusinessFactRefV1` on business fact claims; tests reject RAG, memory, model knowledge, prompt summaries, user text, and raw rows as business authority. |
| T-33-01-03 | Information disclosure | package/bundle projections | mitigate | DTOs keep prompt/verifier/replay/debug projections separate; tests assert prompt-safe projections exclude raw source/OCR/debug/verifier internals. |
| T-33-01-04 | Tampering | semantic review | mitigate | `verify_claims` aggregates hard gate failures before semantic status and tests that semantic success cannot override invalid scope/hash/business authority. |
| T-33-01-05 | Tampering / Repudiation | `receive_request` state reset | mitigate | reset tests prove stale package, citation maps, material claims, bundles, blocked claims, and safe refs are cleared per turn. |
</threat_model>

<verification>
Plan-local gate:
```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/knowledge/test_verified_evidence_package.py tests/knowledge/test_claim_verification_bundle.py tests/knowledge/test_tenant_scope.py tests/agent/rag_context/test_material_claims.py tests/agent/rag_context/test_verifier.py tests/agent/rag_context/test_authority_boundaries.py tests/agent/test_nodes/test_receive_request.py -q --tb=short
uv run ruff check src/knowledge/schemas.py src/knowledge/service.py src/agent/rag_context/schemas.py src/agent/rag_context/claims.py src/agent/state.py src/agent/nodes/receive_request.py tests/knowledge/test_verified_evidence_package.py tests/knowledge/test_claim_verification_bundle.py tests/knowledge/test_tenant_scope.py tests/agent/rag_context/test_material_claims.py tests/agent/test_nodes/test_receive_request.py
git diff --check
```
</verification>

<success_criteria>
- `src/knowledge/schemas.py` exports the target Phase 33 DTOs with strict Pydantic config and exact literals.
- `src/knowledge/service.py` exposes `build_verified_context` and `verify_claims` without adding new retrieval algorithms, new external search backends, or DB schema changes.
- Tests explicitly preserve tenant public policy vs merchant-scoped business fact/action authority separation.
- `AgentState` and `receive_request` own the new field lifecycle and reset stale package/bundle fields.
- Focused tests and ruff commands in the verification block pass through MOCA project entrypoints.
</success_criteria>

<notes>
- Contract/spec delta: none planned. `docs/contract-spec.md` already contains the target Phase 33 contracts.
- Explicit deferrals preserved: retrieval-quality expansion, new search backends, approval/action binding, broad replay/eval hardening, tenant-over-global policy fallback, policy source UI, and real external execution remain outside Phase 33.
</notes>

<output>
After completion, create `.planning/phases/33-rag-context-build-and-claim-verification/33-01-SUMMARY.md`.
</output>
