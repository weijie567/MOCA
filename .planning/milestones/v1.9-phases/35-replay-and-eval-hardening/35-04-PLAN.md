---
phase: 35-replay-and-eval-hardening
plan: 35-04
type: execute
wave: 4
depends_on:
  - 35-01
  - 35-02
  - 35-03
  - 35-05
files_modified:
  - eval/replay/dev-contract-manifest.v1.json
  - src/replay/phase35_eval_manifest.py
  - tests/eval/test_phase35_replay_eval_gates.py
  - tests/architecture/test_phase35_replay_eval_boundaries.py
autonomous: true
requirements:
  - APF-18
user_setup: []
must_haves:
  truths:
    - "Dev-contract replay/eval gates are deterministic Phase 35 blockers."
    - "Forbidden behavior datasets include scope leaks, raw payload exposure, unsupported claim/action paths, no-evidence action recommendation, stale/wrong-scope business facts, invalid-scope evidence, and approval payload hash mismatch."
    - "Static gates prevent replay-by-rerun, parallel event envelopes, physical microservice deployment, and real external execution creep."
  artifacts:
    - path: "eval/replay/dev-contract-manifest.v1.json"
      provides: "Blocking Phase 35 dev-contract manifest"
      contains: "phase35_replay_dev_contract_manifest.v1"
    - path: "src/replay/phase35_eval_manifest.py"
      provides: "Manifest validator for replay eval gates"
      contains: "validate_dev_contract_manifest"
    - path: "tests/eval/test_phase35_replay_eval_gates.py"
      provides: "Dev-contract manifest and forbidden behavior validation"
      contains: "approval_payload_hash_mismatch"
    - path: "tests/architecture/test_phase35_replay_eval_boundaries.py"
      provides: "Static no-scope-creep boundaries"
      contains: "replay_by_rerun"
  key_links:
    - from: "eval/replay/dev-contract-manifest.v1.json"
      to: "eval/replay/phase35-coverage-matrix.v1.json"
      via: "coverage matrix path and hash"
      pattern: "coverage_matrix_path"
    - from: "tests/architecture/test_phase35_replay_eval_boundaries.py"
      to: "src/replay/service.py"
      via: "replay must build from stored events"
      pattern: "get_replay"
---

<objective>
Create deterministic dev-contract eval gates and static forbidden-behavior guards for Phase 35.

Purpose: implement D-09, D-10, D-13, D-14, and D-18 after coverage, permission, and replay timeline tests exist. This plan keeps release sample volume and production telemetry outside the blocking gate while making deterministic safety regressions block Phase 35.
Output: dev-contract manifest, strict validator, eval tests, and architecture static guards.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-replay-and-eval-hardening/35-CONTEXT.md
@.planning/phases/35-replay-and-eval-hardening/35-RESEARCH.md
@.planning/phases/35-replay-and-eval-hardening/35-PATTERNS.md
@docs/contract-spec.md
@docs/eval-test-plan.md
@eval/replay/phase35-coverage-matrix.v1.json
@src/replay/phase35_matrix.py
@tests/replay/test_phase35_coverage_matrix.py
@tests/replay/test_phase35_trace_replay_permissions.py
@tests/replay/test_phase35_terminal_timelines.py
@tests/replay/test_phase35_redaction_negatives.py
@tests/agent/test_nodes/test_rag_context_build.py
@tests/agent/test_nodes/test_claim_verify.py
@tests/agent/test_nodes/test_assess_risk_and_approval.py
@tests/actions/test_phase34_action_draft_bindings.py
@tests/architecture/test_phase34_approval_action_boundaries.py

<interfaces>
Existing coverage matrix validator:
```python
def load_phase35_matrix(path: Path = Path("eval/replay/phase35-coverage-matrix.v1.json")) -> Phase35CoverageMatrix: ...
def validate_phase35_matrix(matrix: Phase35CoverageMatrix) -> list[str]: ...
```

Dev-contract manifest target:
```python
class Phase35DevContractManifest(BaseModel):
    schema_version: Literal["phase35_replay_dev_contract_manifest.v1"]
    gate_level: Literal["dev-contract"]
    blocking: Literal["phase_exit"]
    forbidden_behaviors: list[ForbiddenBehaviorCase]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the blocking dev-contract manifest</name>
  <files>eval/replay/dev-contract-manifest.v1.json, src/replay/phase35_eval_manifest.py, tests/eval/test_phase35_replay_eval_gates.py</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/35-replay-and-eval-hardening/35-CONTEXT.md
    - .planning/phases/35-replay-and-eval-hardening/35-PATTERNS.md
    - docs/eval-test-plan.md sections 20.0, 20.1, 20.4
    - eval/replay/phase35-coverage-matrix.v1.json
    - src/replay/phase35_matrix.py
    - eval/intent/coverage-manifest.v1.json
    - tests/agent/test_intent_manifest.py
  </read_first>
  <behavior>
    - Test dev-contract manifest is schema-valid and `blocking="phase_exit"`.
    - Test manifest references the Phase 35 coverage matrix and validates its SHA-256 hash.
    - Test manifest includes all deterministic forbidden behaviors from D-13 and D-18.
    - Test release and monitoring gates are represented as separate manifest-format checks, not as sample-volume or production-telemetry blockers.
  </behavior>
  <action>
    Create `eval/replay/dev-contract-manifest.v1.json` with these exact top-level fields: `schema_version`, `phase`, `gate_level`, `blocking`, `failure_impact`, `coverage_matrix_path`, `coverage_matrix_hash`, `required_gate_categories`, `forbidden_behaviors`, `required_test_commands`, and `non_blocking_gate_refs`.

    Use these exact values: `schema_version="phase35_replay_dev_contract_manifest.v1"`, `phase="35-replay-and-eval-hardening"`, `gate_level="dev-contract"`, `blocking="phase_exit"`, `failure_impact="block_phase_35_verification"`, and `coverage_matrix_path="eval/replay/phase35-coverage-matrix.v1.json"`. Set `coverage_matrix_hash` to the SHA-256 digest of the matrix file with prefix `sha256:`.

    Set `required_gate_categories` to include exactly these strings per D-10: `schema_validity`, `platform_event_coverage`, `event_order`, `terminal_replay_timelines`, `redaction`, `owner_admin_only_permissions`, `cross_tenant_cross_merchant_negatives`, `forbidden_behavior`, and `release_monitoring_manifest_format`.

    Add `forbidden_behaviors` cases with exact `case_id` values: `raw_prompt_leak`, `raw_tool_payload_leak`, `pii_leak`, `action_raw_payload_leak`, `owner_admin_only_bypass`, `cross_tenant_replay_access`, `cross_merchant_replay_access`, `unsupported_claim_to_action_bound_path`, `no_evidence_to_deterministic_action_recommendation`, `unsafe_action_path`, `stale_business_fact_ref_accepted`, `wrong_scope_business_fact_ref_accepted`, `invalid_scope_evidence_accepted`, and `approval_payload_hash_mismatch_creates_action_draft`.

    Create `src/replay/phase35_eval_manifest.py` with strict Pydantic models and functions `load_dev_contract_manifest(path: Path = Path("eval/replay/dev-contract-manifest.v1.json"))`, `compute_file_sha256(path: Path) -> str`, and `validate_dev_contract_manifest(manifest: Phase35DevContractManifest) -> list[str]`. The validator must check hash equality, required categories, required forbidden behavior case ids, command strings using MOCA-approved entrypoints, and `non_blocking_gate_refs` entries for `eval/replay/release-gate.v1.json` and `eval/replay/monitoring-gate.v1.json`. Because this plan depends on 35-05, the validator may also check that those referenced non-blocking manifest files exist.

    Create `tests/eval/test_phase35_replay_eval_gates.py` to validate this manifest and to assert all required Phase 35 test commands use `UV_CACHE_DIR=/tmp/uv-cache uv run pytest ...` or another approved project-scoped entrypoint.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_phase35_replay_eval_gates.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/replay/phase35_eval_manifest.py tests/eval/test_phase35_replay_eval_gates.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "phase35_replay_dev_contract_manifest.v1|block_phase_35_verification|release_monitoring_manifest_format" eval/replay/dev-contract-manifest.v1.json src/replay/phase35_eval_manifest.py` returns matches.
    - `rg -n "raw_prompt_leak|unsupported_claim_to_action_bound_path|approval_payload_hash_mismatch_creates_action_draft" eval/replay/dev-contract-manifest.v1.json tests/eval/test_phase35_replay_eval_gates.py` returns matches.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_phase35_replay_eval_gates.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>Phase 35 has a strict blocking dev-contract manifest covering deterministic replay/eval safety gates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add static forbidden-scope boundary guards</name>
  <files>tests/architecture/test_phase35_replay_eval_boundaries.py</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/35-replay-and-eval-hardening/35-CONTEXT.md
    - .planning/phases/35-replay-and-eval-hardening/35-PATTERNS.md
    - docs/contract-spec.md sections 0.2, 16.1-16.4, 17.2, 17.6, 17.7
    - tests/architecture/test_phase33_rag_claim_boundaries.py
    - tests/architecture/test_phase34_approval_action_boundaries.py
    - src/replay/service.py
    - src/replay/decision_events.py
    - src/api/routers/traces.py
    - src/api/routers/agent_runs.py
  </read_first>
  <behavior>
    - Static tests forbid replay-by-rerunning LLMs, tools, RAG, or actions.
    - Static tests forbid parallel replay event envelope construction outside `src/replay/decision_events.py` and `src/replay/service.py`.
    - Static tests forbid real external execution, outbox, reconciliation, compensation, and physical microservice deployment surfaces in Phase 35 changes.
    - Static tests prove the dev-contract manifest references existing focused tests for D-13/D-18 forbidden behavior.
  </behavior>
  <action>
    Create `tests/architecture/test_phase35_replay_eval_boundaries.py` with source/AST checks modeled on Phase 33 and Phase 34 architecture guards.

    Add tests that scan production `src/` and assert:
    - `ReplayService.get_replay` and replay API code do not call graph invocation, LLM clients, tool runtime invocation, RAG search/build services, or action draft creation to reconstruct replay output. Scope this replay-by-rerun string check to replay-owned code and the trace/replay API path, especially `src/replay/service.py` and `src/api/routers/traces.py`; do not widen it to unrelated production modules where these strings are legitimate runtime behavior. Use exact forbidden source strings: `invoke_graph`, `with_structured_output`, `ToolRuntime.invoke`, `PolicyKnowledgeService.search`, `build_verified_context`, `verify_claims`, `create_coupon_grant_draft`, and `create_draft`.
    - Production code does not define a second event envelope class with names matching `DecisionEventEnvelopeV2`, `ReplayDecisionEnvelope`, `ParallelReplayEnvelope`, or `ServiceEventEnvelope` outside the existing replay-owned modules.
    - Production code under `src/` does not introduce real execution surface names: `action_executions`, `action_outbox_events`, `action_reconciliation_jobs`, `action_compensation_records`, `ExternalExecutionWorker`, `OutboxDispatcher`, `ReconciliationWorker`, or `CompensationWorker`.
    - Production code and docs changed by Phase 35 do not introduce physical microservice deployment files or instructions such as `docker-compose.replay-service.yml`, `k8s/replay`, or `service_mesh`.

    Add manifest coverage checks that read `eval/replay/dev-contract-manifest.v1.json` and assert each D-13/D-18 forbidden behavior has at least one concrete test path under `tests/replay/`, `tests/eval/`, `tests/architecture/`, `tests/agent/`, or `tests/actions/`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_phase35_replay_eval_boundaries.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check tests/architecture/test_phase35_replay_eval_boundaries.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "replay_by_rerun|invoke_graph|with_structured_output|action_outbox_events|DecisionEventEnvelopeV2|service_mesh" tests/architecture/test_phase35_replay_eval_boundaries.py` returns matches.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_phase35_replay_eval_boundaries.py -q --tb=short` exits 0.
    - `rg -n "action_outbox_events|action_reconciliation_jobs|ExternalExecutionWorker|ReconciliationWorker" src/ | rg -v "tests|test_|docs"` returns no production matches introduced by Phase 35.
  </acceptance_criteria>
  <done>Static architecture gates enforce Phase 35 replay/eval boundaries and deterministic forbidden behavior coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval manifests -> phase verification | Manifest contents become blocking evidence for deterministic Phase 35 safety gates. |
| replay API -> service internals | Replay views must come from stored events, not live graph/model/tool/RAG/action execution. |
| Phase 35 scope -> deferred execution/deployment work | Eval hardening must not introduce external execution or physical microservice deployment surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04-01 | Tampering | `eval/replay/dev-contract-manifest.v1.json` | mitigate | Validate schema, matrix hash, required categories, forbidden cases, and command entrypoints through `phase35_eval_manifest.py`. |
| T-35-04-02 | Elevation of Privilege | replay/eval forbidden behavior | mitigate | Dev-contract manifest and architecture tests include owner/admin bypass, cross-tenant/cross-merchant access, and unsafe action-path negatives. |
| T-35-04-03 | Spoofing | replay reconstruction | mitigate | Static tests forbid replay-by-rerunning graph, LLM, tool, RAG, or action services. |
</threat_model>

<verification>
Run after both tasks:

```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_phase35_replay_eval_gates.py tests/architecture/test_phase35_replay_eval_boundaries.py -q --tb=short
UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/replay/phase35_eval_manifest.py tests/eval/test_phase35_replay_eval_gates.py tests/architecture/test_phase35_replay_eval_boundaries.py
```
</verification>

<success_criteria>
- Dev-contract replay/eval gates are deterministic and blocking for Phase 35.
- Required D-13/D-18 forbidden behavior cases are represented by manifest entries and test paths.
- Static guards prevent replay-by-rerun, parallel envelopes, real external execution, and physical microservice deployment scope creep.
</success_criteria>

<output>
After completion, create `.planning/phases/35-replay-and-eval-hardening/35-04-SUMMARY.md`.
</output>
