---
phase: 35-replay-and-eval-hardening
plan: 35-03
type: execute
wave: 2
depends_on:
  - 35-01
files_modified:
  - tests/replay/test_phase35_terminal_timelines.py
  - tests/replay/test_phase35_operation_identity.py
  - tests/replay/test_phase35_redaction_negatives.py
  - src/replay/lifecycle.py
  - src/replay/service.py
  - src/replay/validators.py
autonomous: true
requirements:
  - APF-17
  - APF-18
user_setup: []
must_haves:
  truths:
    - "Golden replay tests cover normal completed, interrupted approval-required, resumed, rejected, responded/needs-info, expired, error, and cancelled timelines."
    - "Replay timelines assert sequence/order, terminal or current status semantics, and operation identity where operation ids exist."
    - "Replay artifacts reject or omit raw prompt, raw tool payload, ticket/order/refund PII, raw action payload, secrets, and unsafe debug payloads."
  artifacts:
    - path: "tests/replay/test_phase35_terminal_timelines.py"
      provides: "P0 terminal/current replay timeline golden tests"
      contains: "responded_needs_info"
    - path: "tests/replay/test_phase35_operation_identity.py"
      provides: "Operation identity and sequence/order tests"
      contains: "operation_id"
    - path: "tests/replay/test_phase35_redaction_negatives.py"
      provides: "P0 raw payload exposure negative tests"
      contains: "raw_action_payload"
  key_links:
    - from: "tests/replay/test_phase35_terminal_timelines.py"
      to: "src/replay/lifecycle.py"
      via: "RunLifecycleService status events"
      pattern: "mark_completed|mark_interrupted|mark_resumed|mark_rejected|mark_expired|mark_error|mark_cancelled"
    - from: "tests/replay/test_phase35_redaction_negatives.py"
      to: "src/replay/service.py"
      via: "ReplayService projection must stay redacted"
      pattern: "project_event|get_replay"
---

<objective>
Turn replay lifecycle behavior into P0 golden timelines and raw-exposure negatives.

Purpose: implement D-02, D-04, D-10, D-14, D-15, and D-16. Replay remains audit replay from stored events; this plan must not rerun LLMs, tools, RAG, or external actions.
Output: golden terminal timeline tests, operation identity tests, and redaction/exposure negative tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-replay-and-eval-hardening/35-CONTEXT.md
@.planning/phases/35-replay-and-eval-hardening/35-RESEARCH.md
@.planning/phases/35-replay-and-eval-hardening/35-PATTERNS.md
@docs/contract-spec.md
@docs/eval-test-plan.md
@src/replay/lifecycle.py
@src/replay/service.py
@src/replay/validators.py
@src/replay/pairing.py
@tests/replay/test_lifecycle_finalizer.py
@tests/replay/test_operation_pairing.py
@tests/replay/test_replay_redaction_retention.py
@tests/replay/test_replay_service.py

<interfaces>
Replay lifecycle service surface:
```python
class RunLifecycleService:
    async def mark_running(...)
    async def mark_interrupted(...)
    async def mark_resumed(...)
    async def mark_completed(...)
    async def mark_rejected(...)
    async def mark_expired(...)
    async def mark_error(...)
    async def mark_cancelled(...)
```

Replay response contract:
```python
async def ReplayService.get_replay(run_id: uuid.UUID | str) -> dict[str, Any]:
    ...
    return {"schema_version": "replay_response.v3", "final_status": "...", "timeline": [...]}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add P0 terminal replay timeline goldens</name>
  <files>tests/replay/test_phase35_terminal_timelines.py, src/replay/lifecycle.py, src/replay/service.py</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/35-replay-and-eval-hardening/35-CONTEXT.md
    - .planning/phases/35-replay-and-eval-hardening/35-PATTERNS.md
    - docs/contract-spec.md sections 17.1, 17.6, 17.7
    - docs/eval-test-plan.md sections 20.4, 21.8
    - src/replay/lifecycle.py
    - src/replay/service.py
    - tests/replay/test_lifecycle_finalizer.py
    - tests/replay/test_replay_service.py
  </read_first>
  <behavior>
    - Golden test normal completed replay has running and completed status events and a non-empty ordered timeline.
    - Golden test interrupted approval-required replay has current `interrupted` status and approval refs without completed status.
    - Golden test resumed replay continues sequence after interruption and reaches completed status.
    - Golden tests rejected, responded/needs-info, expired, error, and cancelled replay paths preserve partial timelines and correct status semantics.
  </behavior>
  <action>
    Create `tests/replay/test_phase35_terminal_timelines.py` with inline factory helpers that seed `AgentRun` rows and append events through `RunLifecycleService` and `ReplayService`. Do not build timelines by rerunning graph nodes, LLMs, tools, RAG, or external actions per D-04.

    Add one test case for each D-15 timeline name with exact scenario identifiers: `normal_completed`, `interrupted_approval_required`, `resumed_completed`, `rejected`, `responded_needs_info`, `expired`, `error`, and `cancelled`.

    For every case assert:
    - `timeline` is non-empty.
    - `sequence` values are strictly increasing and start at 1.
    - Every event has `schema_version == "replay_event.v3"`.
    - `final_status` is `completed`, `interrupted`, `rejected`, `expired`, `error`, or `cancelled` as appropriate.
    - `responded_needs_info` contains an `approval_decided` or safe approval decision event with `decision_type="respond"` or equivalent safe status metadata and does not contain `run_status_changed` with `status="completed"`.
    - `resumed_completed` contains an approval decision/resume event before post-resume action/status events, and sequence continues after resume.
    - `error` includes a safe error code and no raw stack trace.
    - `cancelled` contains cancellation actor/source metadata and no action execution event.

    Modify `src/replay/lifecycle.py` or `src/replay/service.py` only if these golden tests expose a concrete gap in existing lifecycle/replay projection. If a fix is needed, preserve existing event names and do not add event types.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay/test_phase35_terminal_timelines.py tests/replay/test_lifecycle_finalizer.py tests/replay/test_replay_service.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check tests/replay/test_phase35_terminal_timelines.py src/replay/lifecycle.py src/replay/service.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "normal_completed|interrupted_approval_required|resumed_completed|responded_needs_info|cancelled" tests/replay/test_phase35_terminal_timelines.py` returns matches.
    - `rg -n "run_status_changed.*completed|action_execution_completed|action_execution_started" tests/replay/test_phase35_terminal_timelines.py` returns only assertions that forbid unsafe completed/execution states where expected.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay/test_phase35_terminal_timelines.py tests/replay/test_lifecycle_finalizer.py tests/replay/test_replay_service.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>P0 replay terminal/current timeline goldens cover every required status without replay reruns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add operation identity and redaction negatives</name>
  <files>tests/replay/test_phase35_operation_identity.py, tests/replay/test_phase35_redaction_negatives.py, src/replay/service.py, src/replay/validators.py</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/35-replay-and-eval-hardening/35-CONTEXT.md
    - .planning/phases/35-replay-and-eval-hardening/35-PATTERNS.md
    - docs/contract-spec.md sections 17.2, 17.6, 17.7
    - docs/eval-test-plan.md sections 20.1, 21.8
    - src/replay/pairing.py
    - src/replay/service.py
    - src/replay/validators.py
    - tests/replay/test_operation_pairing.py
    - tests/replay/test_replay_redaction_retention.py
    - tests/replay/test_tool_policy_events.py
  </read_first>
  <behavior>
    - Test started/terminal operation pairs share `operation_id`.
    - Test retry events use a new operation id plus `parent_operation_id` and incremented attempt.
    - Test replay append/projection rejects or omits raw prompt, raw tool payload, ticket/order/refund PII, raw action payload, secrets, and unsafe debug payloads.
  </behavior>
  <action>
    Create `tests/replay/test_phase35_operation_identity.py` using the existing `src/replay/pairing.py` and `ReplayService.append_event` patterns. Assert `tool_call_started` / `tool_call_completed`, `rag_retrieval_started` / `rag_retrieval_completed`, `llm_call_started` / `llm_call_failed`, and `memory_write_started` / `memory_write_completed` started-terminal pairs share `operation_id`, use positive `attempt`, and project stable `pairing_status`.

    Add a retry case where the retry terminal event uses a different `operation_id`, `parent_operation_id` points at the failed prior operation, and `attempt == 2`. Assert sequence remains monotonic across the retry.

    Create `tests/replay/test_phase35_redaction_negatives.py` with representative unsafe strings/keys for D-16: `raw_prompt`, `raw_tool_payload`, `ticket_pii`, `order_pii`, `refund_pii`, `raw_action_payload`, `secret`, `credential`, `unsafe_debug_payload`, `buyer_name`, and `api_key`. Test both append-time validation and `ReplayService.get_replay(...)` projection. Unsafe values must not appear in `json.dumps(replay_response, sort_keys=True)`.

    If validators need new forbidden key aliases, add them only to `FORBIDDEN_REDACTED_PAYLOAD_KEYS` in `src/replay/validators.py` and add matching tests. Do not add new replay event types in this plan.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay/test_phase35_operation_identity.py tests/replay/test_phase35_redaction_negatives.py tests/replay/test_operation_pairing.py tests/replay/test_replay_redaction_retention.py tests/replay/test_tool_policy_events.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check tests/replay/test_phase35_operation_identity.py tests/replay/test_phase35_redaction_negatives.py src/replay/service.py src/replay/validators.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "parent_operation_id|attempt == 2|pairing_status|operation_id" tests/replay/test_phase35_operation_identity.py` returns matches.
    - `rg -n "raw_prompt|raw_tool_payload|ticket_pii|order_pii|refund_pii|raw_action_payload|unsafe_debug_payload" tests/replay/test_phase35_redaction_negatives.py` returns matches.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay/test_phase35_operation_identity.py tests/replay/test_phase35_redaction_negatives.py tests/replay/test_operation_pairing.py tests/replay/test_replay_redaction_retention.py tests/replay/test_tool_policy_events.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>Operation identity and raw-exposure negatives are blocking dev-contract replay tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted events -> replay API | Stored audit events become replay timeline output without rerunning model/tool/RAG/action behavior. |
| operation events -> pairing validator | Operation identity links started, terminal, and retry attempts for audit interpretation. |
| event payloads -> replay projection | Redacted payloads and refs cross into API-facing replay artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-03-01 | Repudiation | replay timelines | mitigate | Golden tests assert monotonic sequence, terminal/current status, and partial timeline preservation for every P0 status. |
| T-35-03-02 | Tampering | operation identity | mitigate | Operation tests assert started/terminal sharing, retry parent linkage, and positive attempts. |
| T-35-03-03 | Information Disclosure | replay payload projection | mitigate | Redaction negatives cover raw prompt, raw tool payload, PII, raw action payload, secrets, and debug payload strings at append and projection time. |
</threat_model>

<verification>
Run after both tasks:

```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay/test_phase35_terminal_timelines.py tests/replay/test_phase35_operation_identity.py tests/replay/test_phase35_redaction_negatives.py tests/replay/test_lifecycle_finalizer.py tests/replay/test_operation_pairing.py tests/replay/test_replay_redaction_retention.py tests/replay/test_replay_service.py tests/replay/test_tool_policy_events.py -q --tb=short
UV_CACHE_DIR=/tmp/uv-cache uv run ruff check tests/replay/test_phase35_terminal_timelines.py tests/replay/test_phase35_operation_identity.py tests/replay/test_phase35_redaction_negatives.py src/replay/lifecycle.py src/replay/service.py src/replay/validators.py
```
</verification>

<success_criteria>
- All D-15 terminal/current replay timelines have golden tests.
- Replay ordering and operation identity are tested for normal and retry paths.
- D-16 raw exposure negatives are blocked at append/projection time.
- Replay remains audit replay and does not rerun LLMs, tools, RAG, or actions.
</success_criteria>

<output>
After completion, create `.planning/phases/35-replay-and-eval-hardening/35-03-SUMMARY.md`.
</output>
