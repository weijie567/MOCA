---
phase: 35-replay-and-eval-hardening
plan: 35-02
type: execute
wave: 2
depends_on:
  - 35-01
files_modified:
  - src/replay/proof_projection.py
  - tests/replay/test_phase35_trace_replay_permissions.py
  - tests/test_trace_api.py
  - tests/test_agent_runs_api.py
  - tests/test_approval_api.py
  - tests/agent/test_trace.py
autonomous: true
requirements:
  - APF-17
user_setup: []
must_haves:
  truths:
    - "Trace, replay, and business-data AgentRun visibility remains owner/admin-only in Phase 35."
    - "Support, manager, merchant, supervisor, and approval_manager users cannot read another user's business-data run, trace, or replay."
    - "Replay-safe proof fields expose future same-merchant proof status/source without becoming an authorization shortcut."
  artifacts:
    - path: "src/replay/proof_projection.py"
      provides: "Replay-safe future authorization proof projection"
      contains: "replay_authorization_proof.v1"
    - path: "tests/replay/test_phase35_trace_replay_permissions.py"
      provides: "Owner/admin-only trace and replay permission negatives"
      contains: "requested_by"
    - path: "tests/test_agent_runs_api.py"
      provides: "AgentRun visibility guard remains owner/admin-only"
      contains: "ADMIN_RUN_VISIBILITY_ROLES"
  key_links:
    - from: "src/replay/proof_projection.py"
      to: "src/agent/merchant_context.py"
      via: "target merchant proof status/source projection"
      pattern: "project_target_merchant_context"
    - from: "tests/replay/test_phase35_trace_replay_permissions.py"
      to: "src/api/routers/traces.py"
      via: "static and API authorization guard checks"
      pattern: "ADMIN_RUN_VISIBILITY_ROLES"
---

<objective>
Harden trace/replay proof fields while keeping Phase 35 authorization closed to owner/admin only.

Purpose: implement D-05, D-06, D-07, D-08, and D-17. This plan creates inspectable proof status/source for future authorization without opening same-merchant run/trace/replay visibility.
Output: replay-safe proof projection helper and permission/proof tests across replay, trace, AgentRun, and trace summary surfaces.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-replay-and-eval-hardening/35-CONTEXT.md
@.planning/phases/35-replay-and-eval-hardening/35-RESEARCH.md
@.planning/phases/35-replay-and-eval-hardening/35-PATTERNS.md
@docs/contract-spec.md
@docs/eval-test-plan.md
@src/api/routers/traces.py
@src/api/routers/agent_runs.py
@src/agent/merchant_context.py
@src/tools/contracts.py
@src/business/schemas.py
@tests/replay/test_replay_api.py
@tests/test_trace_api.py
@tests/test_agent_runs_api.py
@tests/test_approval_api.py
@tests/replay/test_tool_policy_events.py
@tests/replay/test_memory_foundation_alignment.py
@tests/agent/test_reviewed_memory_context_retrieve.py
@tests/agent/test_trace.py

<interfaces>
Existing owner/admin guard:
```python
ADMIN_RUN_VISIBILITY_ROLES = {"admin"}
if run.user_id != user.id and user.role not in ADMIN_RUN_VISIBILITY_ROLES:
    raise HTTPException(status_code=403, detail={"code": "FORBIDDEN", "message": "Cannot view this run"})
```

Existing target merchant projection:
```python
def project_target_merchant_context(state: Mapping[str, Any]) -> dict[str, Any]:
    ...
    return {
        "schema_version": "target_merchant_context.v1",
        "status": "...",
        "source": "...",
        "reason_codes": [...],
    }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add replay-safe proof status projection</name>
  <files>src/replay/proof_projection.py, tests/agent/test_trace.py</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/35-replay-and-eval-hardening/35-CONTEXT.md
    - .planning/phases/35-replay-and-eval-hardening/35-PATTERNS.md
    - docs/contract-spec.md sections 8.0.1, 17.6, 17.7
    - src/agent/merchant_context.py
    - src/tools/contracts.py
    - src/business/schemas.py
    - tests/agent/test_trace.py
  </read_first>
  <behavior>
    - Test proof projection reports `schema_version="replay_authorization_proof.v1"`.
    - Test valid scoped business fact refs/results produce `proof_status="resolved"` and `proof_source="business_fact_refs"` or `business_fact_results`.
    - Test unknown, mixed, denied, invalid, and cross-merchant proof inputs produce fail-closed status values.
    - Test proof projection serializes no raw order id, ticket id, refund id, raw user query, raw tool payload, or raw action payload.
  </behavior>
  <action>
    Create `src/replay/proof_projection.py` with `project_replay_authorization_proof(state: Mapping[str, Any]) -> dict[str, Any]`.

    The returned dict must use these exact keys: `schema_version`, `proof_status`, `proof_source`, `target_merchant_proof`, `business_fact_ref_count`, `business_fact_result_count`, `scope_check_results`, and `reason_codes`.

    Use `schema_version="replay_authorization_proof.v1"`. Allowed `proof_status` values are exactly `resolved`, `unknown`, `mixed`, `denied`, `invalid`, `cross_merchant`, and `not_applicable`. Allowed `proof_source` values are exactly `business_fact_refs`, `business_fact_results`, `target_merchant_context`, `combined`, and `none`.

    For D-06, represent equivalent target merchant proof as a safe `target_merchant_proof` object with `status`, `source`, and `reason_codes` copied from `project_target_merchant_context(state)`. Do not expose raw `target_merchant_id`, raw order/refund/ticket ids, raw user query text, or raw tool/action payloads in this projection.

    Treat `BusinessFactRefV1` / `BusinessFactResultV1` dictionaries as scoped proof only when strict Pydantic validation succeeds, tenant ids are consistent when present, resource identifiers are non-empty, `BusinessFactResultV1.scope_check_result == "allowed"`, and result statuses are `ok` or `partial`. Return `denied` for `permission_denied` or `scope_check_result="denied"`, `unknown` for missing proof on a business-scoped path, `mixed` for inconsistent resource tenants or mixed allowed/denied proof, `invalid` for validation errors, and `cross_merchant` when explicit safe target proof conflicts with scoped business fact proof.

    Extend `tests/agent/test_trace.py` with focused unit tests for the projection. Keep authorization behavior out of this helper; it is evidence for later phases only per D-08. Do not wire `project_replay_authorization_proof` into API authorization guards, and do not expand trace/replay API output unless the output is already a replay-safe trace summary projection explicitly covered by tests in this plan.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_trace.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/replay/proof_projection.py tests/agent/test_trace.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "replay_authorization_proof.v1|project_replay_authorization_proof|proof_status|proof_source|target_merchant_proof" src/replay/proof_projection.py tests/agent/test_trace.py` returns matches.
    - `rg -n "target_merchant_id|raw_prompt|raw_tool|raw_payload|raw_action_payload|user_query" src/replay/proof_projection.py` returns no matches except in denylist/test comments if any.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_trace.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>Replay-safe proof projection exists with fail-closed statuses and no authorization behavior change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove owner/admin-only visibility remains closed</name>
  <files>tests/replay/test_phase35_trace_replay_permissions.py, tests/test_trace_api.py, tests/test_agent_runs_api.py, tests/test_approval_api.py</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/35-replay-and-eval-hardening/35-CONTEXT.md
    - docs/contract-spec.md section 8.0.1
    - src/api/routers/traces.py
    - src/api/routers/agent_runs.py
    - src/replay/proof_projection.py
    - tests/replay/test_replay_api.py
    - tests/test_trace_api.py
    - tests/test_agent_runs_api.py
    - tests/test_approval_api.py
    - tests/replay/test_tool_policy_events.py
    - tests/replay/test_memory_foundation_alignment.py
    - tests/agent/test_reviewed_memory_context_retrieve.py
  </read_first>
  <behavior>
    - API tests keep owner access and admin access allowed.
    - API tests keep cross-tenant replay/trace/run access at 404.
    - API tests keep same-tenant non-owner support, manager, merchant, supervisor, and approval_manager access at 403.
    - Static tests prove authorization guards do not use `target_merchant_context`, replay proof projection, or `requested_by.user.merchant_id` shortcuts.
    - Static/source tests account for roadmap success criterion 4 surfaces: run listing, trace detail, tool result records, approval views, memory, and replay artifacts.
  </behavior>
  <action>
    Create `tests/replay/test_phase35_trace_replay_permissions.py` with Phase 35-specific static and API regression tests.

    Add static tests that assert:
    - `src.api.routers.traces.ADMIN_RUN_VISIBILITY_ROLES == {"admin"}`.
    - `src.api.routers.agent_runs.ADMIN_RUN_VISIBILITY_ROLES == {"admin"}`.
    - The source for `get_run_trace`, `get_run_replay`, and AgentRun status/evidence/stream visibility guards contains `run.user_id != user.id` and `user.role not in ADMIN_RUN_VISIBILITY_ROLES`.
    - Those guard sources do not contain `target_merchant_context`, `project_replay_authorization_proof`, `proof_status`, `requested_by.*merchant`, or `merchant_id.*requested_by`.

    Add or extend API tests so non-owner `support`, `manager`, legacy `merchant`, `supervisor`, and `approval_manager` users still receive 403 for same-tenant business-data trace, replay, AgentRun status, AgentRun evidence, and AgentRun stream access. Keep cross-tenant access at 404. Include a same-merchant manager case with valid proof projection and assert it still receives 403 in Phase 35 per D-05, D-07, D-08, and D-17.

    Extend existing `tests/test_trace_api.py`, `tests/test_agent_runs_api.py`, and `tests/test_approval_api.py` only where they already own trace/run/approval visibility cases. For tool result records and memory surfaces that are not directly changed by Phase 35, add static coverage rows in `tests/replay/test_phase35_trace_replay_permissions.py` that name the existing regression sources `tests/replay/test_tool_policy_events.py`, `tests/replay/test_memory_foundation_alignment.py`, and `tests/agent/test_reviewed_memory_context_retrieve.py`, and assert those files still exist. Record that Phase 35 adds no authorization widening for approval views, tool result records, or memory. Do not open manager same-merchant visibility and do not add any authorization branch that consumes replay proof fields.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay/test_phase35_trace_replay_permissions.py tests/test_trace_api.py tests/test_agent_runs_api.py tests/test_approval_api.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check tests/replay/test_phase35_trace_replay_permissions.py tests/test_trace_api.py tests/test_agent_runs_api.py tests/test_approval_api.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "ADMIN_RUN_VISIBILITY_ROLES|requested_by.*merchant|merchant_id.*requested_by|target_merchant_context|project_replay_authorization_proof" tests/replay/test_phase35_trace_replay_permissions.py` returns matches.
    - `rg -n "approval_views|tool_result_records|memory|test_tool_policy_events|test_memory_foundation_alignment|test_reviewed_memory_context_retrieve" tests/replay/test_phase35_trace_replay_permissions.py` returns matches.
    - `rg -n "project_replay_authorization_proof|proof_status|requested_by.*merchant|merchant_id.*requested_by" src/api/routers/traces.py src/api/routers/agent_runs.py` returns no matches in authorization guard code.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay/test_phase35_trace_replay_permissions.py tests/test_trace_api.py tests/test_agent_runs_api.py tests/test_approval_api.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>Trace, replay, and AgentRun visibility remains owner/admin-only, with future proof fields tested as non-authorizing evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proof projection -> authorization guard | Proof fields are inspectable evidence only and must not authorize same-merchant access in Phase 35. |
| API caller -> trace/replay/AgentRun data | Business-data run details cross from backend to authenticated callers only through owner/admin guards. |
| business fact refs -> replay projection | Scoped proof must be summarized without raw business payload leakage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-02-01 | Elevation of Privilege | `src/api/routers/traces.py` / `src/api/routers/agent_runs.py` | mitigate | Static and API tests keep guards owner/admin-only and forbid target merchant or requested_by merchant shortcuts. |
| T-35-02-02 | Information Disclosure | `src/replay/proof_projection.py` | mitigate | Project counts/status/source/reason codes only; tests reject raw merchant/order/ticket/refund ids and raw payload strings. |
| T-35-02-03 | Spoofing | replay proof status | mitigate | Strictly validate `BusinessFactRefV1` / `BusinessFactResultV1` dictionaries and fail closed on invalid, mixed, denied, or cross-merchant proof. |
</threat_model>

<verification>
Run after both tasks:

```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay/test_phase35_trace_replay_permissions.py tests/test_trace_api.py tests/test_agent_runs_api.py tests/agent/test_trace.py -q --tb=short
UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/replay/proof_projection.py tests/replay/test_phase35_trace_replay_permissions.py tests/test_trace_api.py tests/test_agent_runs_api.py tests/agent/test_trace.py
```
</verification>

<success_criteria>
- Owner/admin-only business-data run/trace/replay visibility remains closed.
- Replay-safe proof fields exist for future same-merchant authorization work without changing Phase 35 authorization.
- `requested_by.user.merchant_id` and `target_merchant_context` are test-proven non-authorizing shortcuts.
</success_criteria>

<output>
After completion, create `.planning/phases/35-replay-and-eval-hardening/35-02-SUMMARY.md`.
</output>
