---
phase: 35-replay-and-eval-hardening
plan: 35-01
type: execute
wave: 1
depends_on: []
files_modified:
  - eval/replay/phase35-coverage-matrix.v1.json
  - src/replay/phase35_matrix.py
  - tests/replay/test_phase35_coverage_matrix.py
autonomous: true
requirements:
  - APF-17
  - APF-18
user_setup: []
must_haves:
  truths:
    - "Phase 35 has a machine-checkable coverage matrix that maps every required platform boundary to replay events, trace projection, eval gate level, forbidden behavior, and acceptance tests."
    - "The matrix is a blocking dev-contract artifact, not a documentation-only checklist."
    - "Phase 35 does not add replay event types in this plan; existing registered events plus payload/projection tests are the contract strategy."
  artifacts:
    - path: "eval/replay/phase35-coverage-matrix.v1.json"
      provides: "Phase 35 replay/eval coverage matrix"
      contains: "business_fact_read_scope_freshness"
    - path: "src/replay/phase35_matrix.py"
      provides: "Strict validator for the Phase 35 coverage matrix"
      contains: "REQUIRED_BOUNDARIES"
    - path: "tests/replay/test_phase35_coverage_matrix.py"
      provides: "Blocking matrix contract tests"
      contains: "test_phase35_matrix_covers_required_platform_boundaries"
  key_links:
    - from: "eval/replay/phase35-coverage-matrix.v1.json"
      to: "src/replay/validators.py"
      via: "registered existing replay event names"
      pattern: "REPLAY_EVENT_TYPES"
    - from: "tests/replay/test_phase35_coverage_matrix.py"
      to: ".planning/ROADMAP.md"
      via: "Phase 35 six-plan split guard"
      pattern: "35-06-PLAN.md"
---

<objective>
Create the deterministic Phase 35 coverage matrix and validator before replay/eval behavior changes.

Purpose: implement D-01, D-02, D-03, D-04, D-09, D-10, D-20, and D-21 by making coverage drift visible and blocking. Phase 35 uses existing replay event types plus payload/projection contract tests; it does not add new event types in this plan.
Output: `eval/replay/phase35-coverage-matrix.v1.json`, a strict validator module, and blocking pytest coverage.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-replay-and-eval-hardening/35-CONTEXT.md
@.planning/phases/35-replay-and-eval-hardening/35-RESEARCH.md
@.planning/phases/35-replay-and-eval-hardening/35-PATTERNS.md
@docs/contract-spec.md
@docs/eval-test-plan.md
@docs/target-agent-platform-architecture-plan.md
@eval/intent/coverage-manifest.v1.json
@eval/intent/m6-statistical-gate.v1.json
@src/replay/validators.py

<interfaces>
Existing replay registry contract:
```python
REPLAY_EVENT_TYPES: set[str]
EVENT_RETENTION_CLASSIFICATION: dict[str, str]
def validate_event_type(event_type: str) -> None: ...
def retention_for_event_type(event_type: str) -> str: ...
```

Phase 35 matrix validator target:
```python
REQUIRED_BOUNDARIES = {
    "trusted_context_projection",
    "intent_policy",
    "slot_policy",
    "memory_load_policy",
    "memory_write_policy",
    "tool_visibility",
    "tool_runtime_auth",
    "business_fact_read_scope_freshness",
    "rag_validation",
    "claim_verification",
    "risk_decision",
    "approval_lifecycle",
    "action_draft",
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the Phase 35 coverage matrix contract</name>
  <files>eval/replay/phase35-coverage-matrix.v1.json, src/replay/phase35_matrix.py, tests/replay/test_phase35_coverage_matrix.py</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/35-replay-and-eval-hardening/35-CONTEXT.md
    - .planning/phases/35-replay-and-eval-hardening/35-RESEARCH.md
    - .planning/phases/35-replay-and-eval-hardening/35-PATTERNS.md
    - docs/contract-spec.md sections 0.2, 8.0.1, 17.2, 17.6, 17.7
    - docs/eval-test-plan.md sections 20.0, 20.1, 20.4, 21.8
    - docs/target-agent-platform-architecture-plan.md section 14
    - src/replay/validators.py
    - eval/intent/coverage-manifest.v1.json
    - tests/agent/test_intent_manifest.py
  </read_first>
  <behavior>
    - Test the matrix has exactly the required platform boundary rows listed in D-01 and APF-17.
    - Test each row includes existing replay event names, trace projection name, eval gate level, forbidden behaviors, acceptance test paths, and concrete decision assertion contracts.
    - Test each row declares `event_strategy="existing_event_plus_payload_contract"` so Phase 35 does not silently add event types.
  </behavior>
  <action>
    Create `eval/replay/phase35-coverage-matrix.v1.json` with `schema_version="phase35_replay_coverage_matrix.v1"`, `phase="35-replay-and-eval-hardening"`, `blocking_gate="dev-contract"`, `event_type_policy="no_new_event_types_in_phase35_matrix"`, and `gate_levels=["dev-contract","release","monitoring"]`.

    Add `rows` for these exact `boundary` values per D-01 and APF-17: `trusted_context_projection`, `intent_policy`, `slot_policy`, `memory_load_policy`, `memory_write_policy`, `tool_visibility`, `tool_runtime_auth`, `business_fact_read_scope_freshness`, `rag_validation`, `claim_verification`, `risk_decision`, `approval_lifecycle`, and `action_draft`.

    For every row include these exact fields: `boundary`, `owner`, `replay_events`, `trace_projection`, `eval_gate_level`, `forbidden_behaviors`, `acceptance_tests`, `decision_assertions`, `event_strategy`, and `notes`. Set `event_strategy` to `existing_event_plus_payload_contract` for all rows. Use only existing event names registered in `src/replay/validators.py`, including `node_completed`, `node_failed`, `run_status_changed`, `memory_write_started`, `memory_write_completed`, `memory_write_failed`, `approval_requested`, `approval_decided`, `approval_expired`, `approval_resumed`, `action_draft_created`, `tool_policy_visibility_recorded`, and `tool_policy_runtime_auth_recorded`.

    Map the boundary rows to concrete acceptance test paths that will be created or extended in Phase 35: `tests/replay/test_phase35_coverage_matrix.py`, `tests/replay/test_phase35_trace_replay_permissions.py`, `tests/replay/test_phase35_terminal_timelines.py`, `tests/replay/test_phase35_operation_identity.py`, `tests/replay/test_phase35_redaction_negatives.py`, `tests/eval/test_phase35_replay_eval_gates.py`, `tests/eval/test_phase35_release_monitoring_manifests.py`, and `tests/architecture/test_phase35_replay_eval_boundaries.py`.

    Each `decision_assertions` item must include `assertion_id`, `test_path`, `assertion_type`, and `asserts`. Allowed `assertion_type` values are exactly `replay_event_payload`, `trace_projection`, `api_projection`, and `eval_manifest`. For the APF-17 left-half boundaries that rely on generic replay events, include these exact assertion ids so the matrix cannot be paper-only: `trusted_context_projection_replay_context`, `intent_policy_effective_route_trace`, `slot_policy_inheritance_trace`, `memory_load_scope_trace`, `business_fact_scope_freshness_proof`, and `risk_decision_action_path_trace`. These assertions must point to focused Phase 35 tests that inspect replay-safe payloads, trace projections, API projections, or eval manifests; do not satisfy them by pointing only to the matrix self-test.

    Create `src/replay/phase35_matrix.py` with strict Pydantic models using `ConfigDict(extra="forbid")`, a `REQUIRED_BOUNDARIES` set, `load_phase35_matrix(path: Path = Path("eval/replay/phase35-coverage-matrix.v1.json"))`, and `validate_phase35_matrix(matrix: Phase35CoverageMatrix) -> list[str]`. The validator must return errors for missing boundaries, unknown gate levels, empty `replay_events`, event names absent from `REPLAY_EVENT_TYPES`, empty `forbidden_behaviors`, empty `acceptance_tests`, empty `decision_assertions`, decision assertions whose `test_path` is absent from that row's `acceptance_tests`, required left-half assertion ids missing from their matching boundary rows, required left-half assertions that point only to `tests/replay/test_phase35_coverage_matrix.py`, unscoped pytest entrypoints, and any row whose `event_strategy` is not `existing_event_plus_payload_contract`.

    If execution discovers a truly required new replay event type, stop this task and split that change into a synchronized event-type task touching `src/replay/validators.py`, `src/db/models.py`, and a migration/check constraint. Do not add event types ad hoc in this plan per D-03.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay/test_phase35_coverage_matrix.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/replay/phase35_matrix.py tests/replay/test_phase35_coverage_matrix.py</automated>
  </verify>
  <acceptance_criteria>
    - `eval/replay/phase35-coverage-matrix.v1.json` contains `"schema_version": "phase35_replay_coverage_matrix.v1"`.
    - `rg -n '"business_fact_read_scope_freshness"|"tool_runtime_auth"|"claim_verification"|"action_draft"' eval/replay/phase35-coverage-matrix.v1.json` returns matches.
    - `rg -n "existing_event_plus_payload_contract|no_new_event_types_in_phase35_matrix" eval/replay/phase35-coverage-matrix.v1.json src/replay/phase35_matrix.py` returns matches.
    - `rg -n "class Phase35CoverageMatrix|REQUIRED_BOUNDARIES|validate_phase35_matrix" src/replay/phase35_matrix.py` returns matches.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay/test_phase35_coverage_matrix.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>The matrix exists, validates strictly, covers all required platform boundaries, and explicitly uses existing replay events plus payload/projection tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard matrix drift and six-plan Phase 35 shape</name>
  <files>tests/replay/test_phase35_coverage_matrix.py</files>
  <read_first>
    - AGENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/35-replay-and-eval-hardening/35-CONTEXT.md
    - eval/replay/phase35-coverage-matrix.v1.json
    - src/replay/phase35_matrix.py
    - src/replay/validators.py
  </read_first>
  <behavior>
    - Test every matrix replay event is registered in `src/replay/validators.py`.
    - Test every matrix row has at least one dev-contract acceptance test path.
    - Test `.planning/ROADMAP.md` lists exactly six Phase 35 plan files and no single broad Phase 35 entry.
    - Test Phase 35 plan files and matrix strings use only MOCA-approved pytest entrypoints.
  </behavior>
  <action>
    Extend `tests/replay/test_phase35_coverage_matrix.py` with static tests that call `load_phase35_matrix()` and assert:
    - `validate_phase35_matrix(...) == []`.
    - For each row, `set(row.replay_events) <= REPLAY_EVENT_TYPES`.
    - `row.event_strategy == "existing_event_plus_payload_contract"`.
    - At least one row maps to each gate level `dev-contract`, `release`, and `monitoring`, with deterministic safety blockers mapped to `dev-contract` per D-10 and D-13.
    - `.planning/ROADMAP.md` matches a `**Plans:** <current>/6 plans complete` pattern with denominator six, regardless of current execution progress, and lists all six files `35-01-PLAN.md` through `35-06-PLAN.md`.
    - No Phase 35 plan or matrix file contains an unscoped pytest entrypoint; allowed commands must include `uv run pytest`, `UV_CACHE_DIR=/tmp/uv-cache uv run pytest`, `.venv/bin/pytest`, or `.venv/bin/python -m pytest`.

    Keep this as a test-only task. Do not edit replay event registries, ORM constraints, or migrations in this plan.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay/test_phase35_coverage_matrix.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check tests/replay/test_phase35_coverage_matrix.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "35-06-PLAN.md|PLAN_PROGRESS_RE|existing_event_plus_payload_contract|REPLAY_EVENT_TYPES|decision_assertions" tests/replay/test_phase35_coverage_matrix.py` returns matches.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay/test_phase35_coverage_matrix.py -q --tb=short` exits 0.
    - `tests/replay/test_phase35_coverage_matrix.py` includes an approved-entrypoint scan for Phase 35 plan and matrix files.
  </acceptance_criteria>
  <done>Matrix drift, registered event usage, MOCA command discipline, and the required six-plan split are test-guarded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifact -> phase execution | The matrix becomes a blocking contract input for later executor tasks. |
| replay event registry -> matrix rows | Matrix rows may only reference registered events unless a synchronized registry/model/migration change is planned. |
| eval command strings -> validation evidence | MOCA validation commands must use repository-scoped `uv run` or `.venv` entrypoints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01-01 | Tampering | `eval/replay/phase35-coverage-matrix.v1.json` | mitigate | Validate required rows, gate levels, registered event names, and test paths through `src/replay/phase35_matrix.py`. |
| T-35-01-02 | Repudiation | coverage matrix acceptance paths | mitigate | Require exact pytest-backed acceptance paths for every boundary row. |
| T-35-01-03 | Information Disclosure | replay coverage strategy | mitigate | Keep event strategy on existing redacted event payloads and forbid raw prompt/tool/PII/action payload coverage claims without tests. |
</threat_model>

<verification>
Run after both tasks:

```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay/test_phase35_coverage_matrix.py -q --tb=short
UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/replay/phase35_matrix.py tests/replay/test_phase35_coverage_matrix.py
```
</verification>

<success_criteria>
- APF-17 and APF-18 have a machine-checkable coverage matrix foundation.
- The matrix covers all required platform boundaries from D-01 and the Phase 35 goal.
- The six-plan Phase 35 roadmap shape is test-guarded.
- No new replay event type is introduced by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/35-replay-and-eval-hardening/35-01-SUMMARY.md`.
</output>
