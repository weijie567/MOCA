---
phase: 27-trustedcontextfactory-and-projections
plan: 27-01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/platform/test_trusted_context.py
  - tests/platform/test_trusted_context_factory.py
  - tests/platform/test_merchant_scope.py
  - tests/platform/test_context_projections.py
  - tests/agent/test_intent_policy_registry.py
  - tests/architecture/test_trusted_context_boundaries.py
  - tests/test_search_integration.py
  - tests/test_agent_runs_api.py
  - tests/agent/test_nodes/test_investigate.py
  - tests/agent/test_tools/test_unified_tool_manager.py
  - tests/knowledge/test_tenant_scope.py
autonomous: true
requirements:
  - APF-03
  - APF-04
requirements_addressed:
  - APF-03
  - APF-04
must_haves:
  truths:
    - "Wave 0 RED tests encode the exact TrustedContext and MerchantScopeV1 contracts before production implementation."
    - "Wave 0 RED tests prove projections cannot widen identity or scope and keep projection-local metadata out of canonical TrustedContext."
    - "Wave 0 RED tests explicitly cover request_id, effective_at, channel, policy_version, model_version, tool_version, and artifact refs as projection-local or metadata only."
    - "Wave 0 RED tests cover the current search, agent-run, graph-node, knowledge-tool, registry, and boundary seams without editing production code."
  artifacts:
    - path: "tests/platform/test_trusted_context.py"
      provides: "TrustedContext exact field-set, schema_version, and extra-field rejection coverage"
    - path: "tests/platform/test_merchant_scope.py"
      provides: "MerchantScopeV1 deny-all, wildcard, all-provided-dimensions, and invalid-value coverage"
    - path: "tests/platform/test_context_projections.py"
      provides: "Tool, knowledge, memory, approval, replay, intent, and AgentState identity projection coverage"
    - path: "tests/architecture/test_trusted_context_boundaries.py"
      provides: "Static boundary checks for canonical owner, projector isolation, and seam migration targets"
  key_links:
    - from: "tests/platform/test_trusted_context.py"
      to: "src/platform/trusted_context.py"
      via: "imports TrustedContext and MerchantScopeV1"
      pattern: "from src\\.platform\\.trusted_context import"
    - from: "tests/platform/test_context_projections.py"
      to: "src/platform/context_projections.py"
      via: "imports projection helpers"
      pattern: "project_to_.*context"
    - from: "tests/architecture/test_trusted_context_boundaries.py"
      to: "src/agent/context/projectors.py"
      via: "AST import/source scan"
      pattern: "projectors.*TrustedContext"
---

<objective>
Create Wave 0 RED tests for APF-03 and APF-04 before any production implementation.

Purpose: Phase 27 must be contract-led. This plan records the expected failures for the canonical trusted context, merchant scope, projection, registry, and current seam migrations so later plans cannot silently narrow the source decisions.
Output: focused RED pytest coverage and static boundary tests only; no production source files are modified by this plan.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-trustedcontextfactory-and-projections/27-CONTEXT.md
@.planning/phases/27-trustedcontextfactory-and-projections/27-RESEARCH.md
@.planning/phases/27-trustedcontextfactory-and-projections/27-PATTERNS.md
@.planning/phases/27-trustedcontextfactory-and-projections/27-VALIDATION.md
@.planning/phases/26-architecture-contract-baseline/26-01-SUMMARY.md
@docs/contract-spec.md
@src/tools/contracts.py
@src/knowledge/schemas.py
@src/api/routers/search.py
@src/api/routers/agent.py
@src/api/routers/agent_runs.py
@src/agent/nodes/investigate.py
@src/agent/nodes/action_draft.py
@src/tools/executors/knowledge.py
@src/agent/intent_policy.py
@src/agent/context/projectors.py

<interfaces>
Contract anchors the tests must encode:

```python
# docs/contract-spec.md §8.0
class MerchantScopeV1(BaseModel):
    schema_version: Literal["merchant_scope.v1"] = "merchant_scope.v1"
    merchant_ids: list[str]
    categories: list[str] | None = None
    risk_levels: list[str] | None = None
    match_rule: Literal["all_provided_dimensions"] = "all_provided_dimensions"
```

```python
# docs/contract-spec.md §8.0 / §10
TrustedContext fields:
schema_version, tenant_id, user_id, role, permissions, merchant_scope,
session_id, thread_id, run_id, trace_id, locale

AgentState identity projection fields:
tenant_id, user_id, role, session_id, thread_id, run_id, trace_id
```
</interfaces>

Scope guardrails:
- This plan must not edit `src/` files.
- Do not add skips, xfails, placeholder assertions, or credentials/live-provider dependencies.
- Deferred Phase 28-35 features from `27-CONTEXT.md` remain out of scope.
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | phase_goal | Shared trusted identity/scope/run context and projections for tool, knowledge, memory, approval, replay, and intent policy | 27-01, 27-02, 27-03 | COVERED | Wave 0 tests, platform contracts, then seam migration
REQ | APF-03 | TrustedContextFactory from trusted API/auth/run boundaries without LLM or user-payload overrides | 27-01, 27-02, 27-03 | COVERED | RED tests first, implementation second, route seams third
REQ | APF-04 | Service-safe projections without widening canonical identity/scope fields | 27-01, 27-02, 27-03 | COVERED | Projection tests, platform helpers, seam consumers
RESEARCH | resolved-open-question-knowledge | Preserve current KnowledgeContext merchant_scope list projection through a central adapter | 27-01, 27-02, 27-03 | COVERED | Tests here; adapter in 27-02; executor route in 27-03
RESEARCH | resolved-open-question-agent-state | Canonical AgentState helper returns run_id/session_id/trace_id; current_run_id only via explicit legacy adapter | 27-01, 27-02, 27-03 | COVERED | Tests here; helper in 27-02; graph compatibility in 27-03
RESEARCH | current-seams | Search, agent routes, investigate/action_draft, and KnowledgeToolExecutor directly construct contexts today | 27-01, 27-03 | COVERED | RED seam assertions here; migrations in 27-03
CONTEXT | D-01..D-04 | Exact canonical fields, trusted source inputs, projection-local exclusion, MerchantScopeV1 semantics | 27-01, 27-02 | COVERED | Task 1 tests and 27-02 implementation
CONTEXT | D-05..D-11 | Tool/knowledge/memory/approval/replay/intent projections and local metadata boundaries | 27-01, 27-02, 27-03 | COVERED | Task 1 tests; implementation in 27-02; route/node use in 27-03
CONTEXT | D-12..D-17 | Shared module, minimal integration, AgentState not authority, compatibility, read-only registries | 27-01, 27-02, 27-03 | COVERED | Boundary/registry tests here; contracts in 27-02; seams in 27-03
CONTEXT | D-18..D-21 | Contract, projection-local, focused integration, and import/boundary checks | 27-01, 27-03 | COVERED | Wave 0 RED tests here; focused gates in 27-03
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED platform contract, projection, registry, and boundary tests</name>
  <files>
tests/platform/test_trusted_context.py
tests/platform/test_trusted_context_factory.py
tests/platform/test_merchant_scope.py
tests/platform/test_context_projections.py
tests/agent/test_intent_policy_registry.py
tests/architecture/test_trusted_context_boundaries.py
  </files>
  <read_first>
AGENTS.md
.planning/phases/27-trustedcontextfactory-and-projections/27-CONTEXT.md
.planning/phases/27-trustedcontextfactory-and-projections/27-RESEARCH.md
.planning/phases/27-trustedcontextfactory-and-projections/27-PATTERNS.md
.planning/phases/27-trustedcontextfactory-and-projections/27-VALIDATION.md
docs/contract-spec.md sections 8.0, 8.3, 10, 12.5, and 17.2
src/tools/contracts.py
src/knowledge/schemas.py
src/agent/intent_policy.py
tests/agent/test_tools/test_tool_contracts.py
tests/architecture/test_tool_boundaries.py
tests/agent/test_intent_golden_contract.py
  </read_first>
  <behavior>
    - Test exact `TrustedContext` fields from D-01 and `contract-spec.md` §8.0: `schema_version`, `tenant_id`, `user_id`, `role`, `permissions`, `merchant_scope`, `session_id`, `thread_id`, `run_id`, `trace_id`, `locale`.
    - Test exact `MerchantScopeV1` schema from §8.0: `schema_version: Literal["merchant_scope.v1"]`, required `merchant_ids: list[str]`, optional `categories` and `risk_levels`, and `match_rule: Literal["all_provided_dimensions"]`.
    - Test deny-all empty scope, explicit wildcard-only `"*"`, all-provided-dimensions matching, invalid-value denial, and no model/user widening per D-04.
    - Test canonical `TrustedContext` rejects or ignores projection-local and metadata-only fields: `request_id`, `tool_call_id`, `caller_node`, `deadline_at`, `attempt`, `max_attempts`, `idempotency_key`, `approval_ref`, `safety_snapshot_ref`, `policy_snapshot_ref`, `effective_at`, `channel`, `policy_version`, `model_version`, `tool_version`, and artifact refs.
    - Test projection helpers for ToolCallContext, KnowledgeContext, MemoryContext, ApprovalContext, ReplayContext, IntentPolicyContext, canonical AgentState identity, and explicitly named legacy AgentState identity compatibility.
    - Test projection-local metadata appears only where the target projection supports it: existing `ToolCallContext` keeps `policy_snapshot_ref` and `safety_snapshot_ref` without accepting unsupported version fields, while memory/approval/replay/intent projection models keep `policy_version`, `model_version`, `tool_version`, and artifact refs out of canonical TrustedContext.
    - Test canonical `project_to_agent_state_identity` returns `tenant_id`, `user_id`, `role`, `session_id`, `thread_id`, `run_id`, and `trace_id`; it must not return `current_run_id`, `permissions`, or `merchant_scope`.
    - Test `project_to_legacy_agent_state_identity` is the only helper allowed to expose `current_run_id`, and it must not carry `permissions` or `merchant_scope`.
    - Test `IntentPolicyRegistry` and `SlotPolicyRegistry` expose read-only views over existing intent/slot policy constants without changing routing helper results.
    - Test boundary rules: canonical trusted-context classes only under `src/platform/trusted_context.py`, prompt projectors do not import trusted context authority, and listed migration seams stop directly constructing service contexts after 27-03.
  </behavior>
  <action>
Create RED tests only. The platform tests should import the planned production symbols and fail because those symbols do not exist yet. Keep tests syntactically valid and deterministic.

Required tests:
- `tests/platform/test_trusted_context.py`: `test_trusted_context_exact_field_set`, `test_trusted_context_schema_version_is_v1`, `test_trusted_context_rejects_projection_local_fields`.
- `tests/platform/test_trusted_context_factory.py`: `test_factory_uses_authenticated_user_verified_scopes_and_server_ids`, `test_factory_rejects_user_payload_and_llm_override_kwargs`, `test_factory_preserves_agent_runs_permission_intersection`.
- `tests/platform/test_merchant_scope.py`: `test_merchant_scope_schema_matches_contract_spec`, `test_merchant_scope_denies_empty_scope`, `test_merchant_scope_requires_explicit_wildcard`, `test_merchant_scope_requires_all_provided_dimensions`, `test_merchant_scope_rejects_invalid_values`.
- `tests/platform/test_context_projections.py`: `test_tool_projection_preserves_tool_context_v2_and_local_fields`, `test_knowledge_projection_keeps_effective_at_local_and_scope_list_compatible`, `test_memory_approval_replay_intent_projections_do_not_widen_identity_scope`, `test_intent_policy_context_channel_is_projection_local`, `test_agent_state_identity_projection_uses_canonical_run_id`, `test_legacy_agent_state_identity_adapter_is_explicit`.
- `tests/agent/test_intent_policy_registry.py`: `test_intent_policy_registry_mirrors_existing_constants`, `test_slot_policy_registry_mirrors_required_slot_policy`, `test_registries_are_read_only`.
- `tests/architecture/test_trusted_context_boundaries.py`: `test_only_platform_module_defines_trusted_context_models`, `test_prompt_projectors_do_not_import_trusted_context_authority`, `test_current_seams_use_projection_helpers_not_direct_trusted_context_constructors`.

Do not edit production files in this task.
  </action>
  <acceptance_criteria>
    - `rg -n "test_trusted_context_exact_field_set|test_merchant_scope_schema_matches_contract_spec|test_agent_state_identity_projection_uses_canonical_run_id|test_only_platform_module_defines_trusted_context_models" tests/platform tests/agent/test_intent_policy_registry.py tests/architecture/test_trusted_context_boundaries.py` finds the named tests.
    - `rg -n "schema_version.*merchant_scope\\.v1|match_rule.*all_provided_dimensions|current_run_id|run_id|session_id|trace_id" tests/platform/test_merchant_scope.py tests/platform/test_context_projections.py` finds explicit contract coverage.
    - `rg -n "policy_version|model_version|tool_version|artifact_ref|artifact_refs|safety_snapshot_ref|policy_snapshot_ref" tests/platform/test_trusted_context.py tests/platform/test_context_projections.py` finds explicit metadata-locality coverage.
    - `rg -n "xfail|skip\\(" tests/platform tests/agent/test_intent_policy_registry.py tests/architecture/test_trusted_context_boundaries.py` returns no matches introduced by this task.
    - Pytest collection errors, if any, are missing planned production imports only, not syntax errors.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set +e; uv run pytest tests/platform/test_trusted_context.py tests/platform/test_trusted_context_factory.py tests/platform/test_merchant_scope.py tests/platform/test_context_projections.py tests/agent/test_intent_policy_registry.py tests/architecture/test_trusted_context_boundaries.py -q; status=$?; test "$status" -ne 0'</automated>
    <automated>uv run ruff check tests/platform tests/agent/test_intent_policy_registry.py tests/architecture/test_trusted_context_boundaries.py</automated>
  </verify>
  <done>Platform, projection, registry, and boundary tests exist as Wave 0 RED coverage and no production files are changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RED seam integration assertions for current context construction paths</name>
  <files>
tests/test_search_integration.py
tests/test_agent_runs_api.py
tests/agent/test_nodes/test_investigate.py
tests/agent/test_tools/test_unified_tool_manager.py
tests/knowledge/test_tenant_scope.py
  </files>
  <read_first>
.planning/phases/27-trustedcontextfactory-and-projections/27-VALIDATION.md
src/api/routers/search.py
src/api/routers/agent.py
src/api/routers/agent_runs.py
src/agent/nodes/investigate.py
src/agent/nodes/action_draft.py
src/tools/executors/knowledge.py
tests/test_search_integration.py
tests/test_agent_runs_api.py
tests/agent/test_nodes/test_investigate.py
tests/agent/test_tools/test_unified_tool_manager.py
tests/knowledge/test_tenant_scope.py
  </read_first>
  <behavior>
    - Search API tests prove KnowledgeContext identity/scope comes from factory projections and ignores or rejects request-local identity/scope override attempts.
    - Agent-run tests prove graph config contains canonical `trusted_context` with schema `trusted_context.v1` and preserves legacy compatibility keys only as compatibility outputs.
    - Investigate tests prove tool calls consume trusted context from graph config and do not read permissions or merchant scope from AgentState.
    - Unified tool manager and knowledge tenant-scope tests prove the central knowledge projection preserves current list merchant_scope behavior and deny-before-query authorization.
  </behavior>
  <action>
Update existing integration tests with RED assertions for the current context-construction seams identified in research. These assertions should fail against the current production code because the platform factory/projection modules are not implemented yet.

Add focused assertions to:
- `tests/test_search_integration.py` for `/api/v1/search` factory-derived KnowledgeContext and no request/body identity override.
- `tests/test_agent_runs_api.py` for `trusted_context` graph config with `schema_version == "trusted_context.v1"`, canonical `run_id`, `session_id`, and `trace_id`, plus preserved permission/merchant-scope compatibility behavior.
- `tests/agent/test_nodes/test_investigate.py` for trusted config consumption and no AgentState permission/scope authority.
- `tests/agent/test_tools/test_unified_tool_manager.py` for `project_to_tool_context` compatibility with existing tool invocation.
- `tests/knowledge/test_tenant_scope.py` for factory-projected list merchant_scope preserving deny-before-query behavior.

Do not edit production files in this task.
  </action>
  <acceptance_criteria>
    - `rg -n "trusted_context|trusted_context\\.v1|project_to_tool_context|project_to_knowledge_context|TrustedContextFactory" tests/test_search_integration.py tests/test_agent_runs_api.py tests/agent/test_nodes/test_investigate.py tests/agent/test_tools/test_unified_tool_manager.py tests/knowledge/test_tenant_scope.py` finds RED seam assertions.
    - `rg -n "request.*tenant_id|request.*merchant_scope|payload.*permissions|AgentState.*merchant_scope|current_run_id" tests/test_search_integration.py tests/test_agent_runs_api.py tests/agent/test_nodes/test_investigate.py` finds negative override or legacy-compatibility coverage.
    - `rg -n "xfail|skip\\(" tests/test_search_integration.py tests/test_agent_runs_api.py tests/agent/test_nodes/test_investigate.py tests/agent/test_tools/test_unified_tool_manager.py tests/knowledge/test_tenant_scope.py` returns no matches introduced by this task.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set +e; uv run pytest tests/test_search_integration.py tests/test_agent_runs_api.py tests/agent/test_nodes/test_investigate.py tests/agent/test_tools/test_unified_tool_manager.py tests/knowledge/test_tenant_scope.py -q; status=$?; test "$status" -ne 0'</automated>
    <automated>uv run ruff check tests/test_search_integration.py tests/test_agent_runs_api.py tests/agent/test_nodes/test_investigate.py tests/agent/test_tools/test_unified_tool_manager.py tests/knowledge/test_tenant_scope.py</automated>
  </verify>
  <done>Current seam integration tests encode RED expectations for factory/projection consumption while keeping this plan test-only.</done>
</task>

</tasks>

<dependency_graph>
This plan has no dependencies and is Wave 0. It creates tests that plans 27-02 and 27-03 must turn green. Same-plan task scopes do not exceed 10 files, and the plan touches 11 files total.
</dependency_graph>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user/model payload -> tests | RED tests must encode spoofing and widening attempts as forbidden behavior. |
| graph checkpoint state -> service context | Tests must prove AgentState is never permission or merchant-scope authority. |
| prompt projectors -> trusted context | Static tests must keep prompt-safe projection separate from identity/scope authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | Spoofing / Elevation of Privilege | Wave 0 factory tests | mitigate | Negative tests cover request-body, LLM, and checkpoint override attempts before implementation. |
| T-27-01-02 | Tampering | Wave 0 projection tests | mitigate | Tests assert projection-local metadata does not enter canonical `TrustedContext`. |
| T-27-01-03 | Information Disclosure | Knowledge seam tests | mitigate | Tests assert unauthorized merchant filters deny before adapter query using trusted list projection. |
| T-27-01-04 | Tampering | Static boundary tests | mitigate | AST/source tests reject duplicate trusted context classes and prompt-projector authority imports. |
</threat_model>

<verification>
Wave 0 verification expects RED pytest failures from missing production implementation and green lint:

```bash
bash -lc 'set +e; uv run pytest tests/platform/test_trusted_context.py tests/platform/test_trusted_context_factory.py tests/platform/test_merchant_scope.py tests/platform/test_context_projections.py tests/agent/test_intent_policy_registry.py tests/architecture/test_trusted_context_boundaries.py -q; status=$?; test "$status" -ne 0'
uv run ruff check tests/platform tests/agent/test_intent_policy_registry.py tests/architecture/test_trusted_context_boundaries.py
```

After 27-02 and 27-03 complete, the same pytest commands must pass without the RED wrapper.
</verification>

<success_criteria>
- No production files are modified by 27-01.
- RED tests encode exact `MerchantScopeV1` and canonical AgentState identity projection behavior from resolved research and `contract-spec.md`.
- RED tests cover D-18, D-19, D-20, and D-21 before implementation.
- Later plans can run the same tests as green gates.
</success_criteria>

<output>
After completion, create `.planning/phases/27-trustedcontextfactory-and-projections/27-01-SUMMARY.md` with APF-03/APF-04 RED coverage, commands run, and confirmation that no production files changed.
</output>
