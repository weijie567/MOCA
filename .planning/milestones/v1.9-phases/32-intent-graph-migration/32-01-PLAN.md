---
phase: 32-intent-graph-migration
plan: 32-01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/agent/graph_vocabulary.py
  - tests/agent/test_graph_vocabulary.py
  - tests/agent/test_graph.py
autonomous: true
requirements:
  - APF-11
requirements_addressed:
  - APF-11
user_setup: []
must_haves:
  truths:
    - "Legacy graph node/router names remain the runtime/debug names while target canonical names are available for contract/eval assertions."
    - "`slot_resolution_gate` is represented by the typed vocabulary helper as a compatibility semantic projection, not only by later trace metadata."
    - "`rag_context_build` and `claim_verify` are present only as deferred/non-runnable Phase 33 target vocabulary entries."
    - "Unknown graph names pass through safely as `unknown_passthrough` without inventing authority or route behavior."
  artifacts:
    - path: "src/agent/graph_vocabulary.py"
      provides: "Typed target graph vocabulary, legacy alias mapping, deferred/non-runnable target entries, and trace-step projection helper"
    - path: "tests/agent/test_graph_vocabulary.py"
      provides: "Unit coverage for node aliases, router aliases, unknown passthrough, and deferred Phase 33 target entries"
    - path: "tests/agent/test_graph.py"
      provides: "Graph compilation compatibility assertions for legacy runtime node names and target vocabulary projection"
  key_links:
    - from: "src/agent/graph_vocabulary.py"
      to: "tests/agent/test_graph_vocabulary.py"
      via: "target_graph_name/project_trace_step_for_contract assertions"
      pattern: "contextual_intent_resolve|slot_resolution_gate|route_after_contextual_intent|deferred_non_runnable"
    - from: "tests/agent/test_graph.py"
      to: "src/agent/graph.py"
      via: "compiled graph keeps legacy node keys"
      pattern: "classify_intent|session_memory_load|long_term_memory_retrieve"
---

<objective>
建立 Phase 32 的 target graph vocabulary 基础层，先把 legacy runtime 名称和 target canonical 名称的关系变成 typed helper 与测试契约。

Purpose: 满足 APF-11 的 target vocabulary migration，同时按 D-01/D-02 保留现有 LangGraph runtime 名称，按 D-03 把 `rag_context_build` / `claim_verify` 限定为 Phase 33 deferred/non-runnable target entries。
Output: `src/agent/graph_vocabulary.py`、focused vocabulary tests、legacy graph compilation assertions。
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-intent-graph-migration/32-CONTEXT.md
@.planning/phases/32-intent-graph-migration/32-RESEARCH.md
@.planning/phases/32-intent-graph-migration/32-PATTERNS.md
@.planning/phases/32-intent-graph-migration/32-VALIDATION.md
@docs/contract-spec.md
@docs/target-agent-platform-architecture-plan.md
@docs/eval-test-plan.md
@.planning/phases/31-memory-platform-boundary/31-04-SUMMARY.md
@.planning/phases/31-memory-platform-boundary/31-05-SUMMARY.md
@src/agent/graph.py
@src/agent/trace.py
@tests/agent/test_graph.py

<interfaces>
Existing graph/runtime contracts to preserve:
```python
# src/agent/graph.py
def build_graph(checkpointer: AsyncPostgresSaver):
    builder.add_node("classify_intent", classify_intent, retry_policy=_llm_retry)
    builder.add_node("session_memory_load", session_memory_load)
    builder.add_node("long_term_memory_retrieve", long_term_memory_retrieve)
    builder.add_conditional_edges("classify_intent", route_after_intent, {...})
    builder.add_conditional_edges("extract_slots", route_after_slots, {...})
```

Existing trace summary behavior to extend later without replacing legacy names:
```python
# src/agent/trace.py
nodes_executed = [str(step.get("node") or "unknown") for step in trace_steps]
```
</interfaces>

Scope guardrails:
- Do not rename registered nodes in `src/agent/graph.py`.
- Do not add runnable `rag_context_build` or `claim_verify` nodes.
- Do not implement APF-13/APF-14 verified evidence or claim verification behavior.
- Do not change AgentRun, trace, replay, or API visibility in this plan; Plan 32-04 owns those projections.
</context>

<source_audit>
| Source | ID | Requirement / Decision | Responsible Plan | Status |
| --- | --- | --- | --- | --- |
| GOAL | Phase 32 | Migrate toward target canonical safety/session/intent/slot/memory graph vocabulary while preserving compatibility | 32-01..32-05 | COVERED |
| ROADMAP-SC | SC-01 | Legacy names map to target canonical node/router names in trace/eval projections | 32-01, 32-04, 32-05 | COVERED |
| ROADMAP-SC | SC-02 | IntentPolicyRegistry and SlotPolicyRegistry own effective route/slot decisions | 32-02, 32-03, 32-05 | COVERED |
| ROADMAP-SC | SC-03 | Router totality tests cover safety, low confidence, direct response, slots, and memory paths | 32-02, 32-03, 32-05 | COVERED |
| ROADMAP-SC | SC-04 | Target merchant context is recorded safely without tenant-wide manager/supervisor visibility | 32-04, 32-05 | COVERED |
| REQ | APF-11 | Target vocabulary mapping for `safety_pre_route`, `session_context_load`, `contextual_intent_resolve`, `slot_resolution_gate`, `memory_context_load`, `rag_context_build`, `claim_verify` | 32-01, 32-04, 32-05 | COVERED |
| REQ | APF-12 | Intent and slot policy registries drive effective decisions; LLM output is candidate-only | 32-02, 32-03, 32-05 | COVERED |
| CONTEXT | D-01..D-04 | Compatibility-first graph vocabulary, real target names, Phase 33 entries non-runnable, split plans | 32-01, 32-05 | COVERED |
| CONTEXT | D-05..D-07 | Intent registry owns effective policy; contextual intent trace; approval-like chat fail-closed | 32-02, 32-05 | COVERED |
| CONTEXT | D-08..D-10 | Slot registry owns required/inherited slots; uncertain inheritance clarifies; router totality | 32-03, 32-05 | COVERED |
| CONTEXT | D-11..D-14 | Trace/eval target names, legacy edge keys, target merchant context status, no auth broadening | 32-03, 32-04, 32-05 | COVERED |
| CONTEXT | D-15..D-19 | RED tests, registry tests, route totality, merchant evidence tests, approved MOCA test entrypoint | 32-01..32-05 | COVERED |
| RESEARCH | R-01 | Use typed graph vocabulary helper before runtime graph edits | 32-01 | COVERED |
| RESEARCH | R-02 | Move intent and slot consumers to registry APIs | 32-02, 32-03 | COVERED |
| RESEARCH | R-03 | Preserve legacy `trace_steps[].node`; add target projection beside it | 32-01, 32-04 | COVERED |
| RESEARCH | R-04 | Keep `rag_context_build` / `claim_verify` deferred/non-runnable in Phase 32 | 32-01, 32-05 | COVERED |
| RESEARCH | R-05 | Target merchant context is evidence/status, not authorization grant | 32-04, 32-05 | COVERED |
| VALIDATION | V-01 | Graph alias/router tests use MOCA pytest entrypoint | 32-01, 32-05 | COVERED |
| VALIDATION | V-02 | Intent registry focused tests | 32-02, 32-05 | COVERED |
| VALIDATION | V-03 | Slot policy and memory-context tests | 32-03, 32-05 | COVERED |
| VALIDATION | V-04 | API/trace/replay visibility tests | 32-04, 32-05 | COVERED |
| VALIDATION | V-05 | Static scan for direct policy constants after registry migration | 32-02, 32-03, 32-05 | COVERED |
| VALIDATION | V-06 | Static scan for no fake runnable Phase 33 nodes | 32-01, 32-05 | COVERED |
| VALIDATION | V-07 | Static scan for invalid direct pytest validation conclusions in Phase 32 artifacts | 32-05 | COVERED |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create typed graph vocabulary helper and RED projection tests</name>
  <files>src/agent/graph_vocabulary.py, tests/agent/test_graph_vocabulary.py</files>
  <read_first>
- AGENTS.md
- CLAUDE.md
- .planning/phases/32-intent-graph-migration/32-CONTEXT.md
- .planning/phases/32-intent-graph-migration/32-RESEARCH.md
- .planning/phases/32-intent-graph-migration/32-PATTERNS.md
- .planning/phases/31-memory-platform-boundary/31-04-SUMMARY.md
- .planning/phases/31-memory-platform-boundary/31-05-SUMMARY.md
- docs/contract-spec.md section 9
- docs/target-agent-platform-architecture-plan.md section 6
- src/agent/graph.py
- src/agent/trace.py
- tests/agent/test_graph.py
  </read_first>
  <action>
Implement D-01, D-02, D-03, D-15, APF-11, and research R-01/R-04.

Create `src/agent/graph_vocabulary.py` with:
- `TargetGraphKind = Literal["node", "router"]`
- `TargetGraphStatus = Literal["runtime", "compatibility_alias", "deferred_non_runnable", "unknown_passthrough"]`
- frozen dataclass `GraphVocabularyEntry` with fields `legacy_name: str`, `target_name: str`, `kind: TargetGraphKind`, `status: TargetGraphStatus`, `runnable: bool`, `reason_codes: tuple[str, ...] = ()`
- immutable maps for at least these aliases:
  - node `classify_intent` -> `contextual_intent_resolve`, status `compatibility_alias`, runnable `True`
  - node `intent_classification` -> `contextual_intent_resolve`, status `compatibility_alias`, runnable `True`
  - node `classify_intent:pre_route` -> `safety_pre_route`, status `compatibility_alias`, runnable `True`; this is trace/eval projection metadata derived from classifier pre-route trace/metrics, not a registered graph node rename.
  - node `session_memory_load` -> `session_context_load`, status `compatibility_alias`, runnable `True`
  - node `session_context_load` -> `session_context_load`, status `runtime`, runnable `True`
  - node `long_term_memory_retrieve` -> `memory_context_load`, status `compatibility_alias`, runnable `True`
  - node `reviewed_memory_context_retrieve` -> `memory_context_load`, status `runtime`, runnable `True`
  - node `extract_slots` -> `slot_resolution_gate`, status `compatibility_alias`, runnable `True`, reason code `SLOT_RESOLUTION_GATE_PROJECTED_FROM_EXTRACT_SLOTS`
  - node `slot_resolution_gate` -> `slot_resolution_gate`, status `compatibility_alias`, runnable `True`, reason code `SLOT_RESOLUTION_GATE_PROJECTED_FROM_EXTRACT_SLOTS`
  - node `rag_context_build` -> `rag_context_build`, status `deferred_non_runnable`, runnable `False`, reason code `PHASE_33_APF_13_OWNED`
  - node `claim_verify` -> `claim_verify`, status `deferred_non_runnable`, runnable `False`, reason code `PHASE_33_APF_14_OWNED`
  - router `route_after_intent` -> `route_after_contextual_intent`, status `compatibility_alias`, runnable `True`
  - router `route_after_slots` -> `route_after_slot_resolution`, status `compatibility_alias`, runnable `True`
- functions:
  - `graph_vocabulary_entry(name: str, *, kind: TargetGraphKind | None = None) -> GraphVocabularyEntry | None`
  - `target_graph_name(name: str, *, kind: TargetGraphKind | None = None) -> str`
  - `is_deferred_non_runnable_target(name: str, *, kind: TargetGraphKind | None = None) -> bool`
  - `project_trace_step_for_contract(step: Mapping[str, Any]) -> dict[str, Any]`

`project_trace_step_for_contract` must copy and preserve all original step keys, preserve the original `node` field, and add `implementation_node`, `target_node`, `target_graph_status`, and `target_graph_runnable`. Unknown names must return `target_node == implementation_node`, status `unknown_passthrough`, and runnable `True`. Production projection call sites should pass `kind="node"` or `kind="router"` when the kind is known; `kind=None` is retained for diagnostics and backwards-compatible lookup only.

Write RED tests first in `tests/agent/test_graph_vocabulary.py` for every alias above, target-name identity lookup for `contextual_intent_resolve`, `session_context_load`, `memory_context_load`, `slot_resolution_gate`, and `route_after_slot_resolution`, unknown passthrough, preservation of extra trace-step keys, and `rag_context_build` / `claim_verify` as non-runnable. Then implement the helper until tests pass.
  </action>
  <acceptance_criteria>
- `test -f src/agent/graph_vocabulary.py` succeeds.
- `rg -n "GraphVocabularyEntry|TargetGraphStatus|unknown_passthrough|deferred_non_runnable|PHASE_33_APF_13_OWNED|PHASE_33_APF_14_OWNED" src/agent/graph_vocabulary.py` finds the typed vocabulary and Phase 33 ownership reason codes.
- `rg -n "classify_intent.*contextual_intent_resolve|session_memory_load.*session_context_load|long_term_memory_retrieve.*memory_context_load|extract_slots.*slot_resolution_gate|route_after_slots.*route_after_slot_resolution" src/agent/graph_vocabulary.py` finds concrete aliases.
- `rg -n "project_trace_step_for_contract|implementation_node|target_node|target_graph_runnable" tests/agent/test_graph_vocabulary.py` finds projection assertions.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_graph_vocabulary.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/graph_vocabulary.py tests/agent/test_graph_vocabulary.py</automated>
  </verify>
  <done>Target graph vocabulary helper exists, maps required APF-11 names, and marks Phase 33 target entries as deferred/non-runnable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin graph compatibility against target vocabulary</name>
  <files>tests/agent/test_graph.py, tests/agent/test_graph_vocabulary.py</files>
  <read_first>
- src/agent/graph.py
- src/agent/graph_vocabulary.py
- tests/agent/test_graph.py
- tests/agent/test_graph_vocabulary.py
- .planning/phases/32-intent-graph-migration/32-CONTEXT.md
- .planning/phases/32-intent-graph-migration/32-VALIDATION.md
  </read_first>
  <action>
Implement D-01, D-02, D-03, D-12, D-15, D-17, and APF-11.

Extend `tests/agent/test_graph.py` with assertions that current graph compilation still uses legacy runtime/debug node keys including `classify_intent`, `session_memory_load`, `long_term_memory_retrieve`, and existing legacy edge keys from `route_after_intent` / `route_after_slots`.

In the same test area, assert target vocabulary projection maps those legacy keys to target names using `src.agent.graph_vocabulary.target_graph_name(...)`. Tests must prove compatibility, not require runtime graph renames.

Add a negative assertion that `src/agent/graph.py` does not register runnable `rag_context_build` or `claim_verify` nodes in Phase 32. Prefer checking the compiled graph node keys where available; if the test also inspects source, use `inspect.getsource(src.agent.graph.build_graph)` and assert those strings do not appear in `builder.add_node(...)` registrations.
  </action>
  <acceptance_criteria>
- `rg -n "target_graph_name|contextual_intent_resolve|session_context_load|slot_resolution_gate|memory_context_load|route_after_contextual_intent|route_after_slot_resolution" tests/agent/test_graph.py tests/agent/test_graph_vocabulary.py` finds target-name assertions.
- `rg -n "classify_intent|session_memory_load|long_term_memory_retrieve|route_after_slots" tests/agent/test_graph.py` finds legacy compatibility assertions.
- `rg -n "rag_context_build|claim_verify" tests/agent/test_graph.py tests/agent/test_graph_vocabulary.py` finds Phase 33 deferred/non-runnable assertions.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_graph_vocabulary.py tests/agent/test_graph.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check tests/agent/test_graph.py tests/agent/test_graph_vocabulary.py</automated>
  </verify>
  <done>Graph tests prove legacy runtime compatibility and target canonical mapping without adding runnable Phase 33 nodes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| legacy runtime graph -> target contract projection | Legacy node/router names become contract/eval target names without changing runtime execution. |
| Phase 32 vocabulary -> Phase 33 RAG/claim ownership | Target names may be listed but must not become successful runnable RAG/claim behavior. |
| trace step input -> contract projection | Trace projection helper must not drop original node names required for audit/debug. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-32-01-01 | Tampering / Repudiation | `src/agent/graph_vocabulary.py` | mitigate | Use immutable typed entries and tests for every required alias so target projection cannot drift silently. |
| T-32-01-02 | Spoofing / Elevation of Privilege | deferred `rag_context_build` / `claim_verify` entries | mitigate | Entries are `deferred_non_runnable`, `runnable=False`, and source tests assert graph does not register them. |
| T-32-01-03 | Repudiation | trace projection helper | mitigate | Projection preserves `node` and adds `implementation_node` beside `target_node` rather than replacing legacy audit fields. |
</threat_model>

<verification>
Run focused vocabulary/graph tests with the MOCA-approved entrypoint. If local validation reveals an environment or fixture failure, append the handled issue to `.planning/LOCAL-VALIDATION-ISSUES.md` in Chinese before completing the summary.
</verification>

<success_criteria>
- `src/agent/graph_vocabulary.py` exists and is the single source for Phase 32 target graph aliases.
- Runtime graph names and legacy edge keys stay compatible.
- `rag_context_build` and `claim_verify` are non-runnable target vocabulary entries only.
- APF-11 has executable tests for node aliases, router aliases, `slot_resolution_gate`, trace-step projection helper behavior, and unknown passthrough.
</success_criteria>

<output>
After completion, create `.planning/phases/32-intent-graph-migration/32-01-SUMMARY.md`.
</output>
