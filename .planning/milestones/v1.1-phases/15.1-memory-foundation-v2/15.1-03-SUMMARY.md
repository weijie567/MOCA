---
phase: 15.1-memory-foundation-v2
plan: 03
subsystem: memory
tags: [working-state, prompt-safety, authority-boundaries, pydantic]

requires:
  - phase: 15.1-02
    provides: ToolResultPromptSummary prompt-safe tool result projection
provides:
  - WorkingStateV1 prompt-safe current-run projection
  - project_working_state AgentState projector
  - Approval/action authority boundary regression coverage
affects: [phase-15.1, prompt-assembly, approval-boundaries, action-boundaries]

tech-stack:
  added: []
  patterns:
    - Strict Pydantic v2 projection models with ConfigDict(extra="forbid")
    - Allowlisted current-run working state separate from AgentState checkpoint state
    - Boundary tests that assert forbidden authority/raw bodies are absent from serialized prompt state

key-files:
  created:
    - src/agent/working_state.py
    - tests/agent/test_working_state.py
  modified:
    - tests/architecture/test_approval_boundaries.py
    - tests/architecture/test_action_draft_boundaries.py

key-decisions:
  - "AgentState remains the runtime/checkpoint state; WorkingStateV1 is a prompt-safe projection only."
  - "WorkingStateV1 exposes compact refs and summaries, not raw business context, evidence bodies, tool data, trace/debug blobs, or LLM outputs."
  - "Approval/action authority objects remain excluded from prompt state; only compact safe draft artifact metadata may be projected."

patterns-established:
  - "project_working_state() is the single projector from AgentState to WorkingStateV1."
  - "Recent tool results are accepted only through ToolResultPromptSummary-compatible fields."
  - "Architecture boundary tests check model_dump_json(), not only object construction."

requirements-completed:
  - MEMORY-FOUNDATION-02

duration: inline
completed: 2026-06-17
---

# Phase 15.1 Plan 03: WorkingStateV1 Summary

**Prompt-safe current-run working state projection with explicit approval/action authority exclusions**

## Performance

- **Duration:** inline after executor handoff stall
- **Completed:** 2026-06-17T06:52:48Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments

- Added `WorkingStateV1`, `WorkingToolResultRef`, and `WorkingDraftArtifact` as strict Pydantic models.
- Added `project_working_state()` to project only allowlisted current-run fields from `AgentState`.
- Added regression tests proving raw tool payloads, business context bodies, policy text, trace/debug blobs, LLM outputs, approval bodies, action payloads, safety snapshots, and hashes do not serialize into WorkingStateV1.
- Preserved compact prompt-safe refs for slots, unresolved questions, business/evidence refs, recent tool summaries, pending confirmation, and safe draft artifact metadata.

## Task Commits

1. **Task 1: Add WorkingStateV1 projection tests** - `86ab2a7` (test)
2. **Task 2: Implement strict WorkingStateV1 projection** - `971ed46` (feat)
3. **Task 3: Add boundary regression assertions for approval/action authority exclusion** - `fb2e4b6` (test)

## Files Created/Modified

- `src/agent/working_state.py` - Prompt-safe WorkingStateV1 models and projector.
- `tests/agent/test_working_state.py` - Allowlist/denylist projection tests.
- `tests/architecture/test_approval_boundaries.py` - Approval authority body exclusion test.
- `tests/architecture/test_action_draft_boundaries.py` - Action draft payload/hash/snapshot exclusion test.

## Decisions Made

- Kept `WorkingStateV1` independent from prompt assembly for this plan; Plan 05 owns ContextAssembler and live prompt-node wiring.
- Allowed `raw_result_ref` as an opaque ref in recent tool results while excluding raw result bodies and nested payloads.
- Exposed only `draft_id`, `action_type`, `status`, and `summary` in `draft_artifact`.

## Deviations from Plan

### Execution Handoff Recovery

- The spawned executor completed the RED test commit and wrote the implementation file, then stopped returning progress without committing the implementation or creating SUMMARY.md.
- Orchestrator closed the stalled agent, verified the working tree state, completed Task 2/3 inline, and committed the remaining work atomically.
- No scope change was introduced.

## Issues Encountered

- `gsd-sdk query roadmap.update-plan-progress` did not match decimal phase plan checkboxes for `15.1-03`; tracking was updated directly in GSD docs.

## Authentication Gates

None.

## Known Stubs

None.

## User Setup Required

None.

## Verification

- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_working_state.py -q --tb=short` - PASS, 3 tests.
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_working_state.py tests/architecture/test_approval_boundaries.py tests/architecture/test_action_draft_boundaries.py -q --tb=short` - PASS, 18 tests.
- `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/working_state.py tests/agent/test_working_state.py tests/architecture/test_approval_boundaries.py tests/architecture/test_action_draft_boundaries.py` - PASS.

## Next Phase Readiness

Ready for `15.1-04-PLAN.md`. Thread rolling summaries can consume conversation messages/tool summaries while WorkingStateV1 remains available for later ContextAssembler projection.

## Self-Check: PASSED

- Verified created/modified files exist on disk.
- Verified task commits exist in git history: `86ab2a7`, `971ed46`, `fb2e4b6`.
- Verified relevant tests and ruff pass.

---
*Phase: 15.1-memory-foundation-v2*
*Completed: 2026-06-17*
