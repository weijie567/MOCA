---
phase: 14-demo-action-executor-boundary
plan: 07
type: execute
wave: 6
depends_on:
  - "14-06"
files_modified:
  - src/actions/service.py
  - src/agent/nodes/action_draft.py
  - tests/agent/test_tools/test_create_coupon_grant_draft.py
  - tests/test_execute_action.py
autonomous: true
gap_closure: true
requirements:
  - DEMO-01
  - DEMO-02
must_haves:
  truths:
    - "ActionService rejects draft creation when payload canonical hash or payload action_type differs from the supplied binding."
    - "ActionService rejects every no-approval draft request in current Phase 14 code because snapshot existence alone is not durable auto_allowed evidence."
    - "The action_draft graph node cannot grant itself tool:create_coupon_grant_draft."
  artifacts:
    - path: "src/actions/service.py"
      provides: "Canonical payload/hash/action_type validation and no-approval binding guard before draft persistence"
      contains: "compute_action_payload_hash"
    - path: "src/agent/nodes/action_draft.py"
      provides: "Trusted permission passthrough to UnifiedToolManager without self-escalation"
      excludes: "permissions.append"
    - path: "tests/agent/test_tools/test_create_coupon_grant_draft.py"
      provides: "DB-backed regressions for payload mismatch, fail-closed omitted approval paths, ActionDraftV2Data projection, and bounded idempotency keys"
      contains: "ACTION_BINDING_MISMATCH"
    - path: "tests/test_execute_action.py"
      provides: "Graph-node permission regression proving PERMISSION_REQUIRED creates no draft update"
      contains: "PERMISSION_REQUIRED"
  key_links:
    - from: "src/actions/service.py"
      to: "src/approvals/snapshot_service.py"
      via: "compute_action_payload_hash(payload)"
      pattern: "compute_action_payload_hash\\("
    - from: "src/actions/service.py"
      to: "src.db.models.ActionSafetySnapshot"
      via: "no-approval path treats snapshot existence as insufficient and fails closed before persistence"
      pattern: "AUTO_ALLOWED_BINDING_REQUIRED"
    - from: "src/agent/nodes/action_draft.py"
      to: "src/tools/manager.py"
      via: "ToolCallContext.permissions uses only configurable permissions"
      pattern: "permissions=list\\(configurable.get\\(\"permissions\"\\) or \\[\\]\\)"
---

<objective>
Close Phase 14 verification gaps without replanning baseline Plans 14-01 through 14-06.

Purpose: enforce the durable draft-only demo boundary with exact payload/hash binding, fail-closed no-approval authorization, and a non-self-escalating write-tool boundary.
Output: focused source fixes and regression tests for the three blocking gaps in `14-VERIFICATION.md`.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-demo-action-executor-boundary/14-CONTEXT.md
@.planning/phases/14-demo-action-executor-boundary/14-RESEARCH.md
@.planning/phases/14-demo-action-executor-boundary/14-PATTERNS.md
@.planning/phases/14-demo-action-executor-boundary/14-VALIDATION.md
@.planning/phases/14-demo-action-executor-boundary/14-VERIFICATION.md
@.planning/phases/14-demo-action-executor-boundary/14-01-SUMMARY.md
@.planning/phases/14-demo-action-executor-boundary/14-02-SUMMARY.md
@.planning/phases/14-demo-action-executor-boundary/14-03-SUMMARY.md
@.planning/phases/14-demo-action-executor-boundary/14-04-SUMMARY.md
@.planning/phases/14-demo-action-executor-boundary/14-05-SUMMARY.md
@.planning/phases/14-demo-action-executor-boundary/14-06-SUMMARY.md
@docs/phase-13-17-architecture-plan.md
@docs/contract-spec.md
@src/actions/service.py
@src/agent/nodes/action_draft.py
@src/tools/manager.py
@tests/agent/test_tools/test_create_coupon_grant_draft.py
@tests/test_execute_action.py

<interfaces>
From `src/approvals/snapshot_service.py`:
```python
def compute_action_payload_hash(proposed_action: dict[str, Any]) -> str
```

From `src/actions/schemas.py`:
```python
class ActionDraftV2Data(BaseModel):
    schema_version: Literal["action_draft.v2"]
    tenant_id: str
    run_id: str
    draft_id: str
    proposed_action: dict[str, Any]
    action_payload_hash: str
    approval_ref: str | None = None
    approval_revision_ref: str | None
    safety_snapshot_ref: str
    safety_snapshot_hash: str
    target_id: str
    idempotency_key: str
    status: str
    execution_mode: Literal["demo"]
    draft_version: int = 1
    lifecycle_status: str = "active"
    retention_policy: str = "phase14_demo_draft"
    draft_outcome: DraftOutcomeV1
    created_at: str | None = None
```

From `src/tools/manager.py`:
```python
if descriptor.required_permission not in ctx.permissions:
    return result("permission_denied", "Required tool permission is missing", code="PERMISSION_REQUIRED")
```
</interfaces>
</context>

<architecture_alignment>
## Architecture Alignment

| Required Item | Phase 14 Gap-Closure Alignment |
| --- | --- |
| Canonical owner package | `src/actions/service.py` remains owner for durable demo draft creation, idempotency, and payload/hash validation. `src/agent/nodes/action_draft.py` remains graph-node caller only. `src/tools/manager.py` remains permission enforcement owner and is not bypassed by the node. |
| Schema owner | No new schema or migration in this plan. Phase 13 continues to own `ActionSafetySnapshot` and `CanonicalHashProfile v1`; Phase 14 only verifies action draft payloads against Phase 13 hashes and persists `ActionDraft.payload` as contract `proposed_action`. Because current `ActionSafetySnapshot`/`ApprovalRequest` rows have no durable auto_allowed marker, no-approval draft creation fails closed. |
| Old-path quarantine/deletion | `execute_action` shim quarantine from 14-03 is preserved. No new production imports from `src.agent.nodes.execute_action`; no new `action_result.status == "success"` dependency; no external execution path. |
| Forbidden new imports/references | Do not import external action adapters, outbox/reconciliation/compensation modules, or Phase 15 ReplayEventV3 services. Do not add `action_execution_*` events. Do not add code that appends `tool:create_coupon_grant_draft` inside `action_draft`. |
| Boundary tests | Add regressions for payload/hash mismatch, action_type mismatch, pending/high-risk omitted approval rejection, approved-but-omitted approval rejection, bare snapshot rejection, bounded idempotency key material, `ActionDraftV2Data` projection, and missing permission returning `PERMISSION_REQUIRED`. |
| Phase 13/14/15/16/17 non-overlap | Phase 13 snapshot/hash production remains unchanged. Phase 14 closes draft-only authorization and permission gaps. Phase 15 replay/lifecycle/read-switch and frontend timeline label cleanup remain deferred. Phase 16 memory remains untouched. Phase 17 external execution/outbox/reconciliation/compensation remains untouched. |
</architecture_alignment>

<source_audit>
## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | - | Enforce durable draft-only demo boundary with exact approval/snapshot binding | 14-07 | COVERED | Directly closes all three blocking verification gaps. |
| REQ | DEMO-01 | Demo mode creates durable draft and `draft_outcome` only, with no execution row or external side effect | 14-01..14-06, 14-07 | COVERED | 14-07 preserves no-execution behavior and proves blocked drafts create no `ActionDraft`. |
| REQ | DEMO-02 | Demo wording and hash/revision guards cannot claim or authorize real execution | 14-07 | COVERED | Payload/hash/action_type mismatch and every omitted-approval path are blocked before persistence until a durable auto_allowed binding exists. |
| RESEARCH | CanonicalHashProfile v1 | Use existing canonical hash helper; do not create a second hash profile | 14-07 | COVERED | Task 1 imports `compute_action_payload_hash` from Phase 13 snapshot service. |
| RESEARCH | Permission boundary | Write tools are node-only and must go through `UnifiedToolManager` permissions | 14-07 | COVERED | Task 3 removes node self-grant and verifies `PERMISSION_REQUIRED`. |
| RESEARCH | No Phase 15/17 scope | Do not implement replay read-switch or external execution/outbox/reconciliation | 14-07 | COVERED | Plan modifies no replay/external files and keeps negative boundaries. |
| CONTEXT | D-01, D-03, D-04, D-05, D-24, D-25, D-26, D-27 | Draft schema, outcome, no execution, trace safety | 14-01..14-06, 14-07 | COVERED | Baseline covered; 14-07 preserves and adds no-draft-on-error regressions. |
| CONTEXT | D-02, D-11, D-12, D-13, D-14, D-15, D-16, D-17 | Service-owned binding, idempotency, target, auto marker, tenant uniqueness | 14-02, 14-07 | COVERED | 14-07 closes missing canonical payload binding and makes the D-16 `auto_allowed` marker non-authorizing in current code unless durable evidence exists. |
| CONTEXT | D-06, D-07, D-08, D-09, D-10 | Draft_outcome wording and backend success semantics | 14-04, 14-06, 14-07 | COVERED | 14-07 does not alter wording; error paths create no draft success surface. |
| CONTEXT | D-18, D-19, D-20, D-21, D-22, D-23 | Canonical `action_draft`, tool name, shim quarantine, intent taxonomy | 14-03, 14-07 | COVERED | 14-07 preserves tool name and canonical node while removing permission self-escalation. |
| CONTEXT | D-28 | Spec/phase boundary explicit in coverage artifacts | 14-06, 14-07 | COVERED | 14-07 keeps Phase 15/17 non-overlap explicit and does not revise normative spec. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind persisted payload to canonical proposed_action hash</name>
  <files>src/actions/service.py, tests/agent/test_tools/test_create_coupon_grant_draft.py</files>
  <read_first>
    - .planning/phases/14-demo-action-executor-boundary/14-VERIFICATION.md
    - .planning/phases/14-demo-action-executor-boundary/14-CONTEXT.md
    - docs/contract-spec.md Sections 15.3 and 16.1-16.4
    - src/actions/service.py
    - src/approvals/snapshot_service.py
    - src/actions/schemas.py
    - tests/agent/test_tools/test_create_coupon_grant_draft.py
  </read_first>
  <behavior>
    - Given an approved request and the exact approved `action_payload_hash`, changing `payload["target_id"]` before draft creation returns `ACTION_BINDING_MISMATCH` and creates zero `ActionDraft` rows for the run.
    - Given an approved request and its exact `payload`, passing a top-level `action_type` that differs from `payload["action_type"]` returns `ACTION_BINDING_MISMATCH` and creates zero `ActionDraft` rows for the run.
    - Successful draft creation returns `data["action_draft"]` that validates with `ActionDraftV2Data`, including `proposed_action`, `approval_ref`, `draft_outcome`, and `created_at`.
    - `_build_idempotency_key` returns the D-12 raw key shape when the key is <=256 chars and returns deterministic bounded `key_sha256:<digest>` material when the raw key would exceed 256 chars.
  </behavior>
  <action>
    Add tests first in `tests/agent/test_tools/test_create_coupon_grant_draft.py`:
    - Add helper `_draft_payload(request: ApprovalRequest, **overrides) -> dict[str, Any]` returning `dict(request.proposed_action)` with overrides applied. Update existing successful direct service calls in this file to pass full `proposed_action.v1` payloads, not tiny `{"target_id": ...}` payloads, per D-02 and contract Section 15.3.
    - Add `test_create_coupon_grant_draft_rejects_payload_hash_mismatch_without_draft`: create an approved request, call `create_coupon_grant_draft` with `_binding_kwargs(request)` and `_draft_payload(request, target_id="RF-TAMPERED")`, assert `result["status"] == "error"`, `result["error"]["error_code"] == "ACTION_BINDING_MISMATCH"`, `retryable is False`, and `select(ActionDraft).where(ActionDraft.run_id == request.run_id)` returns no rows.
    - Add `test_create_coupon_grant_draft_rejects_action_type_mismatch_without_draft`: create an approved request, call with `action_type="manual_review"` and `_draft_payload(request)` whose `action_type` remains `"issue_coupon"`, assert the same `ACTION_BINDING_MISMATCH` and no draft rows.
    - Add `test_create_coupon_grant_draft_returns_complete_action_draft_v2_projection`: validate `ActionDraftV2Data.model_validate(result["data"]["action_draft"])`; assert `proposed_action == payload`, `approval_ref == str(request.id)`, `draft_outcome.status == "not_executed_demo"`, and `created_at` is present.
    - Add `test_build_idempotency_key_preserves_raw_shape_until_256_chars_and_bounds_long_keys`: import `_build_idempotency_key`; assert the ordinary D-12 key equals `{tenant_id}:{run_id}:approval_revision_1:issue_coupon:RF-APPROVAL-1:{request.action_payload_hash}`; assert a 400-character target produces a key with `len(key) <= 256`, contains `key_sha256:`, is deterministic across repeated calls, and does not contain the long target string.

    Implement in `src/actions/service.py` after tests fail:
    - Import `compute_action_payload_hash` from `src.approvals.snapshot_service`, `CanonicalHashError` if needed, and `ActionDraftV2Data` from `src.actions.schemas`.
    - Before `_validate_action_binding` and before `draft_store.create_or_get`, compute `computed_payload_hash = compute_action_payload_hash(payload)`. If canonical hash computation fails, if `computed_payload_hash != action_payload_hash`, or if `str(payload.get("action_type") or "") != action_type`, return `_tool_error("ACTION_BINDING_MISMATCH", "Action draft payload does not match approved safety binding", retryable=False)`.
    - Keep `TARGET_ID_REQUIRED` before key construction. The full payload must still provide `target_id`.
    - Change `_action_draft_data(draft)` to include `proposed_action=draft.payload`, `approval_ref=str(draft.approval_request_id) if present else None`, `draft_outcome=_draft_outcome_from_draft(draft)`, and `created_at=draft.created_at.isoformat() if present else None`; return `ActionDraftV2Data.model_validate(data).model_dump(mode="json")` so WR-01 is closed.
    - Change `_build_idempotency_key` to build the raw D-12 key first. If `len(raw_key) <= 256`, return it unchanged. If longer, compute `digest = hashlib.sha256(raw_key.encode("utf-8")).hexdigest()` and return `f"{tenant_id}:{run_id}:{revision_marker}:key_sha256:{digest}"`. This closes WR-02 while keeping the exact raw key material in the digest.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_tools/test_create_coupon_grant_draft.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/actions/service.py tests/agent/test_tools/test_create_coupon_grant_draft.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "compute_action_payload_hash" src/actions/service.py` returns at least one match.
    - `rg -n "ACTION_BINDING_MISMATCH" tests/agent/test_tools/test_create_coupon_grant_draft.py` returns payload/hash and action_type mismatch tests.
    - `rg -n "ActionDraftV2Data.model_validate" src/actions/service.py tests/agent/test_tools/test_create_coupon_grant_draft.py` returns matches.
    - `rg -n "key_sha256" src/actions/service.py tests/agent/test_tools/test_create_coupon_grant_draft.py` returns matches.
    - The pytest command in `<verify>` exits 0.
  </acceptance_criteria>
  <done>Persisted `ActionDraft.payload` is the same canonical `proposed_action.v1` body whose hash was approved or snapshotted, and long idempotency keys cannot exceed `ActionDraft.idempotency_key String(256)`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail closed on no-approval draft authorization bypass</name>
  <files>src/actions/service.py, tests/agent/test_tools/test_create_coupon_grant_draft.py</files>
  <read_first>
    - .planning/phases/14-demo-action-executor-boundary/14-VERIFICATION.md
    - .planning/phases/14-demo-action-executor-boundary/14-CONTEXT.md
    - docs/phase-13-17-architecture-plan.md Phase 14 gate tests
    - docs/contract-spec.md Sections 15.3, 16.2, and 18.2 cross-table consistency
    - src/actions/service.py
    - src/db/models.py
    - tests/agent/test_tools/test_create_coupon_grant_draft.py
  </read_first>
  <behavior>
    - Any `create_coupon_grant_draft` call with `approval_request_id is None` returns `_tool_error("AUTO_ALLOWED_BINDING_REQUIRED", "No-approval action draft requires a durable auto-allowed binding", retryable=False)` in current Phase 14 code.
    - A pending/high-risk `ApprovalRequest` snapshot cannot create a draft by omitting `approval_request_id`, and no `ActionDraft` row is created.
    - An approved request is rejected when `approval_request_id` is omitted and accepted only when the explicit approved `approval_request_id` is supplied.
    - A bare `ActionSafetySnapshot` with no matching `ApprovalRequest` is rejected because snapshot existence alone is not durable `auto_allowed` evidence.
  </behavior>
  <action>
    Add tests first in `tests/agent/test_tools/test_create_coupon_grant_draft.py`:
    - Replace the existing pending-request success expectation in `test_create_coupon_grant_draft_auto_allowed_key_is_service_owned` with a fail-closed assertion, or split it into a new `test_create_coupon_grant_draft_rejects_pending_high_risk_snapshot_when_approval_id_omitted`. Use a pending/high-risk approval request, `_draft_payload(request)`, and `_binding_kwargs(request, approval_request_id=None)`. Assert error code `AUTO_ALLOWED_BINDING_REQUIRED`, `retryable is False`, and no `ActionDraft` rows exist for `request.run_id`.
    - Add `test_create_coupon_grant_draft_requires_explicit_approval_id_when_matching_approved_request_exists`: create and approve a request, omit `approval_request_id`, pass `_draft_payload(request)`, assert `AUTO_ALLOWED_BINDING_REQUIRED` and no draft. Then call the same payload with `_binding_kwargs(request)` including `approval_request_id` and assert success.
    - Add `test_create_coupon_grant_draft_rejects_bare_snapshot_without_auto_allowed_binding`: create a run, persist an `ActionSafetySnapshot` directly through `persist_action_safety_snapshot` with a full `proposed_action.v1`, call `create_coupon_grant_draft` with `approval_request_id=None`, and assert `AUTO_ALLOWED_BINDING_REQUIRED`, `retryable is False`, and no `ActionDraft` row for that run. This proves a bare snapshot is not durable `auto_allowed` evidence.
    - Add or reuse a small assertion helper for omitted-approval rejection cases that queries `select(ActionDraft).where(ActionDraft.run_id == run_id)` and asserts no rows; use it in the pending/high-risk, approved-but-omitted, and bare-snapshot rejection tests.

    Implement in `src/actions/service.py`:
    - In `_validate_action_binding`, after the snapshot row is found, handle `approval_request_id is None` by immediately returning `_tool_error("AUTO_ALLOWED_BINDING_REQUIRED", "No-approval action draft requires a durable auto-allowed binding", retryable=False)`.
    - Do not query for an absent `ApprovalRequest` as positive evidence, do not infer authorization from `ActionSafetySnapshot` existence, and do not return `_ValidatedActionBinding(revision_marker="auto_allowed", approval_revision_ref="auto_allowed")` anywhere in current Phase 14 code.
    - Keep the explicit approved approval path unchanged: when `approval_request_id` is supplied, require `schema_version == "approval_request.v2"`, `status == "approved"`, non-legacy row, and exact `action_payload_hash`/`safety_snapshot_ref`/`safety_snapshot_hash` matches.
    - Do not create new tables, mutate `ActionSafetySnapshot`, or implement Phase 15 replay/Phase 17 execution.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_tools/test_create_coupon_grant_draft.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/actions/service.py tests/agent/test_tools/test_create_coupon_grant_draft.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "AUTO_ALLOWED_BINDING_REQUIRED" src/actions/service.py tests/agent/test_tools/test_create_coupon_grant_draft.py` returns matches.
    - `rg -n "_tool_error\\(\"AUTO_ALLOWED_BINDING_REQUIRED\"" src/actions/service.py` returns the `approval_request_id is None` fail-closed branch.
    - `rg -n "revision_marker=\"auto_allowed\"|approval_revision_ref=\"auto_allowed\"" src/actions/service.py` returns no matches.
    - `rg -n "approval_request_id=None|approval_request_id = None" tests/agent/test_tools/test_create_coupon_grant_draft.py` includes pending/high-risk, approved-but-omitted, and bare-snapshot rejection tests.
    - `rg -n "select\\(ActionDraft\\).*run_id|ActionDraft.run_id" tests/agent/test_tools/test_create_coupon_grant_draft.py` shows all omitted-approval rejection paths assert no draft row.
    - The pytest command in `<verify>` exits 0.
  </acceptance_criteria>
  <done>No snapshot can authorize draft creation by omitting `approval_request_id` in current Phase 14 code; explicit approved bindings still succeed only through their approved approval request id.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove action_draft write-tool permission self-escalation</name>
  <files>src/agent/nodes/action_draft.py, tests/test_execute_action.py</files>
  <read_first>
    - .planning/phases/14-demo-action-executor-boundary/14-VERIFICATION.md
    - .planning/phases/14-demo-action-executor-boundary/14-CONTEXT.md
    - docs/phase-13-17-architecture-plan.md Non-Negotiable Architecture Tests
    - src/agent/nodes/action_draft.py
    - src/tools/manager.py
    - src/tools/catalog.py
    - tests/test_execute_action.py
  </read_first>
  <behavior>
    - When `configurable["permissions"]` omits `tool:create_coupon_grant_draft`, `action_draft` returns an error action_result with `PERMISSION_REQUIRED`, creates no `action_draft` update, creates no `draft_outcome`, and does not call the action executor/service.
    - Existing approved success tests pass only when the trusted config includes `permissions=["tool:create_coupon_grant_draft"]`.
  </behavior>
  <action>
    Add tests first in `tests/test_execute_action.py`:
    - Add `test_action_draft_without_write_tool_permission_returns_permission_required_without_draft`. Build `_approved_state()`, pass `{"configurable": {"session": object(), "permissions": [], "action_tool_manager": UnifiedToolManager(executors=[fake_action_executor])}}`, where the fake executor has `has_tool("create_coupon_grant_draft") == True` and records if `execute` is called. Assert `result["action_result"]["status"] == "error"`, `result["action_result"]["error"]["error_code"] == "PERMISSION_REQUIRED"`, `"action_draft" not in result`, `"draft_outcome" not in result`, `result["trace_steps"][-1]["status"] == "error"`, and fake executor call count is zero.
    - Update existing success tests in this file to pass trusted config permissions explicitly: `{"configurable": {"session": object(), "permissions": ["tool:create_coupon_grant_draft"]}}`.

    Implement in `src/agent/nodes/action_draft.py`:
    - Replace the current permission mutation block with a trusted passthrough only: `permissions = list(configurable.get("permissions") or [])`.
    - Remove the conditional append of `tool:create_coupon_grant_draft`. Do not add an alternate hardcoded grant under any name.
    - Continue passing `caller_node="action_draft"` and the unchanged `ToolCallContext` fields so `UnifiedToolManager.invoke` remains the single permission enforcement boundary.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_execute_action.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/nodes/action_draft.py tests/test_execute_action.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "permissions\\.append|tool:create_coupon_grant_draft.*append|append\\(f?\\\"tool:" src/agent/nodes/action_draft.py` returns no matches.
    - `rg -n "permissions = list\\(configurable.get\\(\"permissions\"\\) or \\[\\]\\)" src/agent/nodes/action_draft.py` returns one match.
    - `rg -n "PERMISSION_REQUIRED" tests/test_execute_action.py` returns the missing-permission regression.
    - The pytest command in `<verify>` exits 0.
  </acceptance_criteria>
  <done>The graph node can no longer self-authorize the write tool; missing trusted permission fails through `UnifiedToolManager` before any draft service call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| graph state/config -> action_draft node | Runnable config carries trusted session, permissions, merchant scope, trace IDs; state may contain LLM-derived proposed action material and trusted Phase 13 binding fields. |
| action_draft node -> UnifiedToolManager | Node asks for node-only write tool; manager must enforce caller allowlist, side-effect guard, permission, idempotency key, and safety snapshot requirements. |
| ActionToolExecutor -> ActionService | Tool args cross into durable draft persistence; payload, action_type, approval id, and hash tuple must be validated before DB writes. |
| ActionService -> database | Service writes `action_drafts` and events only after exact binding and authorization checks pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-14-07-01 | Tampering | `ActionService.create_coupon_grant_draft` | mitigate | Compute `compute_action_payload_hash(payload)` and reject `ACTION_BINDING_MISMATCH` when it differs from `action_payload_hash` before `draft_store.create_or_get`. |
| T-14-07-02 | Tampering / Elevation of Privilege | `ActionService._validate_action_binding` no-approval branch | mitigate | Return `AUTO_ALLOWED_BINDING_REQUIRED` whenever `approval_request_id is None`; do not treat snapshot existence or absence of an approval row as durable `auto_allowed` evidence. |
| T-14-07-03 | Elevation of Privilege | `src/agent/nodes/action_draft.py` permissions | mitigate | Remove self-grant; pass only `configurable["permissions"]` into `ToolCallContext`; regression asserts `PERMISSION_REQUIRED` and no executor call. |
| T-14-07-04 | Denial of Service | `_build_idempotency_key` vs `ActionDraft.idempotency_key String(256)` | mitigate | Preserve raw key up to 256 chars and use deterministic `key_sha256:<digest>` bounded material for longer raw keys. |
| T-14-07-05 | Information Disclosure | action draft trace/read surfaces | accept | This plan does not add trace fields; Phase 14 baseline already excludes raw `ActionDraft.payload` from events and `/trace`, and tests remain in 14-05/14-06. |
</threat_model>

<verification>
Run focused regressions first:

```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_tools/test_create_coupon_grant_draft.py tests/test_execute_action.py -q --tb=short
UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/actions/service.py src/agent/nodes/action_draft.py tests/agent/test_tools/test_create_coupon_grant_draft.py tests/test_execute_action.py
```

Then run the Phase 14 focused gate and final project gates:

```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_execute_action.py tests/test_approval_integration.py tests/test_trace_api.py tests/agent/test_graph.py tests/agent/test_nodes/test_final_response.py tests/agent/test_tools/test_create_coupon_grant_draft.py tests/architecture/test_action_draft_boundaries.py tests/actions/test_action_draft_v2.py tests/agent/test_events.py tests/agent/test_nodes/test_receive_request.py -q --tb=short
UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q --tb=short
UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests
```
</verification>

<success_criteria>
- Payload/hash mismatch returns `ACTION_BINDING_MISMATCH` and no `ActionDraft` row is created.
- Top-level `action_type` mismatch against `payload["action_type"]` returns `ACTION_BINDING_MISMATCH` and no `ActionDraft` row is created.
- Pending/high-risk approval snapshots cannot create drafts when `approval_request_id` is omitted.
- Approved approval-bound snapshots cannot create drafts when `approval_request_id` is omitted, and still succeed when the explicit approved `approval_request_id` is supplied.
- Bare `ActionSafetySnapshot` rows cannot create drafts when `approval_request_id` is omitted because snapshot existence alone is not durable `auto_allowed` evidence.
- All omitted-approval rejection paths create zero `ActionDraft` rows.
- `action_draft` missing `tool:create_coupon_grant_draft` returns `PERMISSION_REQUIRED`, creates no draft update, and does not invoke the action executor.
- `ActionDraftV2Data` projection warning is closed or the projection is renamed; this plan chooses to close it by validating the returned projection.
- Idempotency key warning is closed by deterministic bounded key material.
- No Phase 15 ReplayEventV3/read-switch or Phase 17 external execution/outbox/reconciliation/compensation work is added.
</success_criteria>

<output>
After completion, create `.planning/phases/14-demo-action-executor-boundary/14-07-SUMMARY.md`.
</output>
