---
phase: 09-business-tool-facade
plan: 09
type: execute
wave: 3
depends_on: ["09-06", "09-07", "09-08"]
files_modified:
  - src/api/routers/agent_runs.py
  - src/agent/nodes/retrieve_policy_evidence.py
  - src/knowledge/service.py
  - tests/test_agent_runs_api.py
  - tests/agent/test_nodes/test_retrieve_policy_evidence.py
  - tests/knowledge/test_service.py
  - tests/agent/test_graph.py
autonomous: true
requirements: [TOOL-01, TOOL-02]
gap_closure: true
must_haves:
  truths:
    - "A caller without tool:search_policy permission cannot execute live policy retrieval."
    - "An empty or invalid merchant scope is deny-all at the PolicyKnowledgeService execution boundary and never calls the adapter."
    - "A merchant user without merchant_id receives an empty deny-all scope, never the string 'None'."
    - "A restricted agent:chat-only JWT is covered through the live API/SSE trusted-config path, not only a private helper."
  artifacts:
    - path: "src/agent/nodes/retrieve_policy_evidence.py"
      provides: "Execution-time policy retrieval permission gate"
      contains: "tool:search_policy"
    - path: "src/knowledge/service.py"
      provides: "Deny-all empty merchant-scope enforcement before adapter execution"
      contains: "merchant_scope"
    - path: "tests/test_agent_runs_api.py"
      provides: "Restricted JWT API/SSE trusted-config regression"
      contains: "agent:chat"
  key_links:
    - from: "configurable.permissions"
      to: "PolicyKnowledgeService.search"
      via: "retrieve_policy_evidence pre-execution authorization gate"
      pattern: "tool:search_policy"
    - from: "KnowledgeContext.merchant_scope"
      to: "LegacyRagKnowledgeAdapter.retrieve"
      via: "PolicyKnowledgeService deny-all guard"
      pattern: "merchant_scope"
---

<objective>
Finish the incomplete Phase 9 authorization gap closure by enforcing permissions and merchant scope at the real policy-retrieval execution boundary.

Purpose: prevent a restricted JWT or empty merchant scope from reaching the policy adapter even when trusted-config projection itself is correct.
Output: execution-time permission denial, service-level deny-all merchant scope, safe merchant router projection, and live-path regressions.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-business-tool-facade/09-CONTEXT.md
@.planning/phases/09-business-tool-facade/09-RESEARCH.md
@.planning/phases/09-business-tool-facade/09-REVIEW.md
@.planning/phases/09-business-tool-facade/09-VERIFICATION.md
@.planning/phases/09-business-tool-facade/09-06-SUMMARY.md
@.planning/phases/09-business-tool-facade/09-07-SUMMARY.md

<interfaces>
Policy retrieval remains owned by Phase 8 `PolicyKnowledgeService`; this plan does not move it into `BusinessToolService`.

The trusted retrieval permission is exactly `tool:search_policy`. Missing or empty `configurable.permissions` fails closed.

`KnowledgeContext.merchant_scope` semantics:
- `None` is legacy/unrestricted compatibility behavior owned outside this gap.
- `[]` is explicit deny-all and MUST return without calling the adapter.
- `["*"]` is explicit tenant-wide scope.
- Specific merchant IDs allow only matching explicit merchant filters; an unauthorized explicit filter must deny, not silently widen to an unfiltered search.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce policy retrieval permission on the live execution path</name>
  <files>src/agent/nodes/retrieve_policy_evidence.py, tests/agent/test_nodes/test_retrieve_policy_evidence.py, tests/agent/test_graph.py, tests/test_agent_runs_api.py</files>
  <read_first>
    - src/agent/nodes/retrieve_policy_evidence.py
    - src/api/routers/agent_runs.py
    - src/knowledge/schemas.py
    - tests/agent/test_nodes/test_retrieve_policy_evidence.py
    - tests/agent/test_graph.py
    - tests/test_agent_runs_api.py
    - .planning/phases/09-business-tool-facade/09-06-PLAN.md
    - .planning/phases/09-business-tool-facade/09-06-SUMMARY.md
    - .planning/phases/09-business-tool-facade/09-REVIEW.md
  </read_first>
  <behavior>
    - Missing or empty permissions prevent `PolicyKnowledgeService.search` from being called.
    - Permissions without `tool:search_policy` prevent search from being called and produce a safe permission-denied retrieval error.
    - `tool:search_policy` permits the existing PolicyKnowledgeService path.
    - A real agent:chat-only token reaching the SSE endpoint gives the graph no tool permissions; the regression does not only call `_trusted_tool_config` directly.
  </behavior>
  <action>
Add a fail-closed authorization check in `retrieve_policy_evidence` before constructing or invoking `PolicyKnowledgeService`. On denial, return the node's normal safe error shape with a stable `PERMISSION_DENIED` code, no evidence refs, and an error trace; never call the adapter/service. Update existing node and graph fixtures to provide `tool:search_policy` only where successful retrieval is expected. Replace or supplement the helper-only router regression with an API/SSE test that uses a restricted `agent:chat` token and captures the trusted graph config produced by the live endpoint.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_retrieve_policy_evidence.py tests/agent/test_graph.py tests/test_agent_runs_api.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'tool:search_policy|PERMISSION_DENIED' src/agent/nodes/retrieve_policy_evidence.py` shows a real pre-execution gate and safe denial.
    - `rg -n 'assert_not_awaited|not.*called|PERMISSION_DENIED' tests/agent/test_nodes/test_retrieve_policy_evidence.py` shows denied retrieval cannot call search.
    - `rg -n 'agent:chat|/events|permissions' tests/test_agent_runs_api.py` shows an API/SSE restricted-token regression.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_retrieve_policy_evidence.py tests/agent/test_graph.py tests/test_agent_runs_api.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>Restricted tokens cannot execute live policy retrieval, and the real API/SSE projection is regression-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce deny-all merchant scope before policy adapter execution</name>
  <files>src/knowledge/service.py, src/api/routers/agent_runs.py, tests/knowledge/test_service.py, tests/test_agent_runs_api.py</files>
  <read_first>
    - src/knowledge/service.py
    - src/knowledge/schemas.py
    - src/api/routers/agent_runs.py
    - src/db/models.py
    - tests/knowledge/test_service.py
    - tests/test_agent_runs_api.py
    - .planning/phases/09-business-tool-facade/09-07-PLAN.md
    - .planning/phases/09-business-tool-facade/09-07-SUMMARY.md
    - .planning/phases/09-business-tool-facade/09-REVIEW.md
  </read_first>
  <behavior>
    - `merchant_scope=[]` returns no evidence without calling the adapter.
    - An explicit merchant filter outside a specific non-wildcard scope returns no evidence without calling the adapter.
    - `merchant_scope=["*"]` and valid matching scope retain the existing search path.
    - A merchant-role user with `merchant_id=None` receives `{"merchant_ids": []}`, not `{"merchant_ids": ["None"]}`.
  </behavior>
  <action>
Enforce explicit deny-all semantics inside `PolicyKnowledgeService.search` before adapter invocation. Empty scope and unauthorized explicit merchant filters return a typed `no_evidence` result with empty evidence refs and no adapter call. Preserve legacy `None` compatibility and explicit wildcard behavior. In `_trusted_tool_config`, project a merchant with no merchant_id to an empty merchant ID list. Add behavior tests that assert adapter non-invocation and router projection.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/knowledge/test_service.py tests/test_agent_runs_api.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'merchant_scope|no_evidence' src/knowledge/service.py` shows pre-adapter deny-all enforcement.
    - `rg -n 'assert_not_awaited|not.*called|merchant_scope' tests/knowledge/test_service.py` shows adapter non-invocation coverage.
    - `rg -n 'merchant_id.*None|merchant_ids.*\\[\\]' tests/test_agent_runs_api.py` shows the missing merchant ID regression.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/knowledge/test_service.py tests/test_agent_runs_api.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>Empty and unauthorized merchant scopes deny retrieval before adapter execution, and missing merchant identity cannot become a fake scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trusted run config -> retrieval node | Projected permissions must be enforced before policy execution. |
| KnowledgeContext -> policy adapter | Empty or unauthorized merchant scope must stop before database retrieval. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09-01 | Elevation of Privilege / confused deputy | `retrieve_policy_evidence` | mitigate | Require `tool:search_policy` before calling PolicyKnowledgeService and test no-call denial. |
| T-09-09-02 | Information Disclosure / Elevation of Privilege | `PolicyKnowledgeService.search` | mitigate | Treat empty scope and unauthorized explicit filters as deny-all before adapter execution. |
| T-09-09-03 | Tampering | merchant user with missing identity | mitigate | Project missing merchant_id to empty deny-all scope, never string conversion. |
| T-09-09-04 | Repudiation | helper-only authorization tests | mitigate | Add live API/SSE trusted-config regression and execution-boundary no-call tests. |
</threat_model>

<verification>
Run focused tests after each task, then run the full Phase 9 regression command:
`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/business_tools/ tests/knowledge/test_service.py tests/agent/test_nodes/test_load_business_context.py tests/agent/test_nodes/test_retrieve_policy_evidence.py tests/agent/test_policy_retrieval_ownership.py tests/agent/test_graph.py tests/test_agent_runs_api.py tests/integration/test_auth.py -q --tb=short`
</verification>

<success_criteria>
- Missing retrieval permission blocks live policy retrieval before service/adapter execution.
- Empty or unauthorized merchant scope blocks adapter execution.
- Missing merchant identity projects to deny-all.
- The real API/SSE trusted-config path and execution boundary are both regression-tested.
- The full Phase 9 regression command passes.
</success_criteria>

<output>
After completion, create `.planning/phases/09-business-tool-facade/09-09-SUMMARY.md`.
</output>
