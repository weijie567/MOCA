---
phase: 09-business-tool-facade
plan: 08
type: execute
wave: 2
depends_on: ["09-06", "09-07"]
files_modified:
  - .planning/phases/09-business-tool-facade/09-OWNERSHIP-BOUNDARY.md
  - tests/agent/test_policy_retrieval_ownership.py
autonomous: true
requirements: [TOOL-01, TOOL-02]
gap_closure: true
must_haves:
  truths:
    - "Phase 9 verification evaluates business reads through BusinessToolService and does not require policy knowledge execution through that facade."
    - "Policy retrieval remains executable through Phase 8 PolicyKnowledgeService while Phase 9 retrieval descriptors remain declaration-only."
    - "Re-verification distinguishes the corrected ownership-scope conflict from the two real authorization defects closed by Plans 09-06 and 09-07."
  artifacts:
    - path: ".planning/phases/09-business-tool-facade/09-OWNERSHIP-BOUNDARY.md"
      provides: "Durable authoritative re-verification contract for Phase 8/9 ownership"
      contains: "PolicyKnowledgeService"
    - path: "tests/agent/test_policy_retrieval_ownership.py"
      provides: "Executable ownership regression guard"
      contains: "PolicyKnowledgeService"
  key_links:
    - from: "src/agent/nodes/retrieve_policy_evidence.py"
      to: "src/knowledge/service.py"
      via: "PolicyKnowledgeService.search"
      pattern: "PolicyKnowledgeService"
    - from: "src/business_tools/registry.py"
      to: "retrieval descriptors"
      via: "declaration-only adapter=None contract"
      pattern: "search_policy"
---

<objective>
Resolve the verifier's expanded single-facade claim against the authoritative Phase 9 goal and locked ownership boundary, with a durable re-verification contract and executable regression guard.

Purpose: prevent future verification from silently moving policy knowledge into BusinessToolService while giving the independent post-execution verifier authoritative inputs for evaluating the real authorization gaps.
Output: ownership-boundary artifact and automated cross-facade ownership assertion for independent re-verification.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-business-tool-facade/09-CONTEXT.md
@.planning/phases/09-business-tool-facade/09-RESEARCH.md
@.planning/phases/09-business-tool-facade/09-VERIFICATION.md
@.planning/phases/09-business-tool-facade/09-REVIEW.md
@.planning/phases/09-business-tool-facade/09-06-SUMMARY.md
@.planning/phases/09-business-tool-facade/09-07-SUMMARY.md

<interfaces>
Authoritative Phase 9 goal: "Route read business tools through BusinessToolService using trusted ToolCallContext and typed ToolResultV2."

Locked boundary from 09-CONTEXT.md: `search_policy_adapter` and policy EvidenceRef extraction belong to Phase 8 KnowledgeService; Phase 9 does not re-own Knowledge/RAG retrieval.

The BusinessToolService registry may declare retrieval descriptors as a unified declaration/validation catalog, but live policy execution remains `PolicyKnowledgeService.search`.

`09-VERIFICATION.md` is a read-only description of the original gaps. The executor must not edit or self-certify `09-VERIFICATION.md`, `ROADMAP.md`, or `REQUIREMENTS.md` beyond standard execute-plan tracking behavior. Final verification status, score, and requirements disposition are owned by the independent post-execution gsd-verifier.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add executable policy-retrieval ownership regression</name>
  <files>tests/agent/test_policy_retrieval_ownership.py</files>
  <read_first>
    - tests/agent/test_policy_retrieval_ownership.py (new file; confirm absent before creation)
    - src/agent/nodes/retrieve_policy_evidence.py
    - src/business_tools/registry.py
    - src/knowledge/service.py
    - tests/agent/test_nodes/test_retrieve_policy_evidence.py
    - tests/business_tools/test_registry.py
    - .planning/ROADMAP.md (authoritative Phase 9 goal)
    - .planning/REQUIREMENTS.md (TOOL-01..03 boundaries)
    - .planning/phases/09-business-tool-facade/09-CONTEXT.md (locked policy ownership boundary)
    - .planning/phases/09-business-tool-facade/09-RESEARCH.md (declaration versus execution findings)
    - .planning/phases/09-business-tool-facade/09-PATTERNS.md (registry and retrieval test seams)
    - .planning/phases/09-business-tool-facade/09-VERIFICATION.md (conflicting expanded claim)
    - .planning/phases/09-business-tool-facade/09-REVIEW.md (real authorization findings)
  </read_first>
  <behavior>
    - Live policy retrieval calls `PolicyKnowledgeService.search`.
    - `search_policy`, `search_sop`, and `search_case_memory` descriptors remain declared for catalog/validation purposes with no BusinessToolService adapter.
    - BusinessToolService remains executable for business reads, and no test treats policy EvidenceRef production as a business-tool responsibility.
  </behavior>
  <action>
Create a focused ownership-contract test module. Add a behavioral async test that invokes `retrieve_policy_evidence` with a mocked `PolicyKnowledgeService.search` and asserts it is awaited. Add a registry assertion that the three retrieval descriptors exist and their registered adapters are `None`/declaration-only, while the executable order/refund/ticket descriptors retain adapters in the default BusinessToolService composition. Use public registry/service inspection seams where available; if adapter lookup is private, add no production API solely for this test and instead assert the existing observable invocation result is `unavailable` for declaration-only retrieval descriptors. The test must encode the ROADMAP/CONTEXT boundary, not the verifier's expanded claim.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_policy_retrieval_ownership.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'PolicyKnowledgeService|search_policy|search_sop|search_case_memory|declaration' tests/agent/test_policy_retrieval_ownership.py` returns both sides of the ownership contract.
    - `rg -n 'BusinessToolService.*search_policy|search_policy.*BusinessToolService' tests/agent/test_policy_retrieval_ownership.py` returns no assertion requiring policy execution through BusinessToolService.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_policy_retrieval_ownership.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>An executable regression fails if policy retrieval is moved into BusinessToolService or if the Phase 8 live service seam is removed.</done>
</task>

<task type="auto">
  <name>Task 2: Record authoritative ownership disposition for independent re-verification</name>
  <files>.planning/phases/09-business-tool-facade/09-OWNERSHIP-BOUNDARY.md</files>
  <read_first>
    - .planning/phases/09-business-tool-facade/09-OWNERSHIP-BOUNDARY.md (new file; confirm absent before creation)
    - .planning/ROADMAP.md (authoritative Phase 9 goal and success criteria)
    - .planning/REQUIREMENTS.md (TOOL-01..03 wording)
    - .planning/phases/09-business-tool-facade/09-CONTEXT.md (locked policy ownership boundary)
    - .planning/phases/09-business-tool-facade/09-RESEARCH.md (registry declaration versus execution findings)
    - .planning/phases/09-business-tool-facade/09-PATTERNS.md (registry and retrieval test seams)
    - .planning/phases/09-business-tool-facade/09-VERIFICATION.md (expanded conflicting truth/gap)
    - .planning/phases/09-business-tool-facade/09-REVIEW.md (real auth findings)
    - .planning/phases/09-business-tool-facade/09-06-SUMMARY.md
    - .planning/phases/09-business-tool-facade/09-07-SUMMARY.md
    - tests/agent/test_policy_retrieval_ownership.py
  </read_first>
  <action>
Create `09-OWNERSHIP-BOUNDARY.md` as the durable re-verification input. Quote or precisely reference the ROADMAP Phase 9 goal, the CONTEXT locked "Do NOT own policy knowledge" decision, and the Phase 8 PolicyKnowledgeService owner. State the executable contract: business reads execute through BusinessToolService; policy retrieval executes through PolicyKnowledgeService; retrieval descriptors in the Phase 9 registry are declaration/validation catalog entries and are not a transfer of execution ownership; write/action execution remains outside the facade. List the exact automated ownership test and full Phase 9 regression command.

Write the boundary artifact so the independent post-execution gsd-verifier can use it together with `tests/agent/test_policy_retrieval_ownership.py` and the Plan 09-06/09-07 executable regressions to evaluate the corrected ownership scope and the two real authorization defects. Treat `09-VERIFICATION.md` only as a read-only description of the original gaps. Do not edit or self-certify `09-VERIFICATION.md`, `ROADMAP.md`, or `REQUIREMENTS.md` beyond standard execute-plan tracking behavior; do not recompute final verification status, score, or requirements disposition. Those outcomes are owned by the independent post-execution gsd-verifier. Do not edit CONTEXT, REVIEW, unrelated docs, or drafts.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_policy_retrieval_ownership.py tests/test_agent_runs_api.py tests/agent/test_nodes/test_retrieve_policy_evidence.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Route read business tools through BusinessToolService|Do NOT own policy knowledge|PolicyKnowledgeService|declaration' .planning/phases/09-business-tool-facade/09-OWNERSHIP-BOUNDARY.md` returns all authoritative boundary elements.
    - `rg -n 'invalid scope expansion|authoritative boundary|test_policy_retrieval_ownership|independent.*verifier' .planning/phases/09-business-tool-facade/09-OWNERSHIP-BOUNDARY.md` returns the explicit conflict disposition and executable verifier inputs.
    - `rg -n 'policy retrieval.*BusinessToolService|all business and retrieval tools.*one' .planning/phases/09-business-tool-facade/09-OWNERSHIP-BOUNDARY.md` returns no normative requirement to execute policy retrieval through BusinessToolService.
    - `git diff --name-only -- .planning/phases/09-business-tool-facade/09-VERIFICATION.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/phases/09-business-tool-facade/09-CONTEXT.md .planning/phases/09-business-tool-facade/09-REVIEW.md` returns no changes from this task.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_policy_retrieval_ownership.py tests/test_agent_runs_api.py tests/agent/test_nodes/test_retrieve_policy_evidence.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>The independent post-execution verifier has authoritative boundary and executable-test inputs to distinguish the corrected scope conflict from real authorization closures, without executor self-certification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ROADMAP/CONTEXT → verification | Authoritative phase scope constrains what re-verification may require. |
| BusinessToolService registry → PolicyKnowledgeService | Shared declaration catalog must not become an accidental policy-execution ownership transfer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08-01 | Elevation of Privilege / confused deputy | Token/tool permissions | mitigate | Depend on Plan 09-06 and include its API no-widening evidence in corrected verification. |
| T-09-08-02 | Elevation of Privilege | Role/token intersection | mitigate | Re-verification requires the exact intersection regression from Plan 09-06. |
| T-09-08-03 | Information Disclosure / Elevation of Privilege | Merchant-scope dropping/widening | mitigate | Depend on Plan 09-07 and require exact KnowledgeContext propagation evidence. |
| T-09-08-04 | Tampering / Elevation of Privilege | Policy retrieval ownership boundary | mitigate | Durable boundary artifact plus executable test proving PolicyKnowledgeService live execution and declaration-only retrieval descriptors. |
| T-09-08-05 | Repudiation | Verification history | mitigate | Preserve `09-VERIFICATION.md` unchanged as the original gap record and give the independent verifier durable boundary and executable-test inputs. |
</threat_model>

<verification>
Run focused tests after each task, then run the full Phase 9 regression command:
`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/business_tools/ tests/agent/test_nodes/test_load_business_context.py tests/agent/test_nodes/test_retrieve_policy_evidence.py tests/agent/test_policy_retrieval_ownership.py tests/agent/test_graph.py tests/test_agent_runs_api.py tests/integration/test_auth.py -q --tb=short`
</verification>

<source_coverage_audit>
| Source | Item | Plan | Status |
|---|---|---|---|
| GOAL | Route read business tools through BusinessToolService using trusted ToolCallContext and ToolResultV2 | 09-06, 09-08 | COVERED |
| REQ | TOOL-01 trusted ToolCallContext and business-read facade | 09-06, 09-08 | COVERED |
| REQ | TOOL-02 permission/scope contracts | 09-06, 09-07 | COVERED |
| REQ | TOOL-03 write/action exclusion | Existing 09-02/09-05; ownership regression protects no transfer | COVERED, no invented gap work |
| RESEARCH | Preserve declaration catalog while separating policy execution ownership | 09-08 | COVERED |
| CONTEXT | Do not own policy knowledge; Phase 8 PolicyKnowledgeService remains owner | 09-07, 09-08 | COVERED |
| VERIFICATION | Verified JWT scopes and role intersection | 09-06 | COVERED |
| VERIFICATION | Structured merchant scope reaches KnowledgeContext | 09-07 | COVERED |
| VERIFICATION | Resolve retrieval single-facade conflict under authoritative boundary | 09-08 | COVERED |
</source_coverage_audit>

<success_criteria>
- The authoritative Phase 8/9 ownership boundary is documented as a re-verification contract and protected by an automated test.
- The ownership-boundary artifact explicitly dispositions the original expanded verifier truth as a scope conflict, not an implementation requirement.
- The ownership-boundary artifact and executable regressions provide inputs for the independent verifier to evaluate both real authorization defects without executor self-certification.
- The executor does not edit or recompute status, score, or requirements disposition in `09-VERIFICATION.md`, `ROADMAP.md`, or `REQUIREMENTS.md`.
- The full Phase 9 regression command passes.
</success_criteria>

<output>
After completion, create `.planning/phases/09-business-tool-facade/09-08-SUMMARY.md`.
</output>
