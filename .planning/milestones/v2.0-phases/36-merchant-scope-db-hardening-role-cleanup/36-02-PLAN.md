---
phase: 36-merchant-scope-db-hardening-role-cleanup
plan: 36-02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/models.py
  - src/api/schemas/auth.py
  - src/api/routers/auth.py
  - tests/integration/test_auth.py
  - tests/approvals/test_migration_contract.py
autonomous: true
requirements:
  - MSH-02
  - MSH-03
  - MSH-07
user_setup: []
must_haves:
  truths:
    - "Username principal lookup can be tenant-qualified and cannot silently return an ambiguous principal."
    - "Same-tenant duplicate usernames remain impossible."
    - "Cross-tenant duplicate usernames have an explicit tenant selector path before database uniqueness is relaxed."
    - "A user merchant binding cannot persistently point at a merchant from a different tenant."
  artifacts:
    - path: "src/api/schemas/auth.py"
      provides: "Tenant-aware login and demo-token request contract"
      contains: "tenant_id"
    - path: "src/api/routers/auth.py"
      provides: "Shared tenant-aware username resolver for login/token/demo-token"
      contains: "_resolve_user_for_login"
    - path: "src/db/models.py"
      provides: "Tenant-scoped username ORM uniqueness metadata and user-to-merchant tenant consistency metadata"
      contains: "uq_users_tenant_username|fk_users_merchant_tenant"
    - path: "tests/integration/test_auth.py"
      provides: "Tenant selector and ambiguous username auth tests"
      contains: "tenant_id"
  key_links:
    - from: "src/api/routers/auth.py"
      to: "src/db/models.py"
      via: "select(User).where(User.username == ..., User.tenant_id == ...)"
      pattern: "User.tenant_id"
    - from: "tests/approvals/test_migration_contract.py"
      to: "src/db/models.py"
      via: "ORM metadata checks for tenant username uniqueness and composite user merchant FK"
      pattern: "uq_users_tenant_username|fk_users_merchant_tenant"
---

<objective>
Make username identity tenant-aware before migrations can relax principal uniqueness.

Purpose: implement MSH-03, the persistent tenant-consistency part of MSH-02, and preserve MSH-07 auth behavior. This plan chooses the D-10 transitional tenant-resolution contract: existing username-only clients continue to work while usernames are globally unique, and duplicate usernames require a trusted `tenant_id` selector before resolution.
Output: tenant-aware auth schemas/router helper, ORM metadata for `(tenant_id, username)` plus user merchant tenant consistency, and auth/migration-contract tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-SPEC.md
@.planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-CONTEXT.md
@.planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-RESEARCH.md
@.planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-VALIDATION.md
@.planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-PATTERNS.md
@docs/contract-spec.md
@src/db/models.py
@src/api/schemas/auth.py
@src/api/routers/auth.py
@tests/integration/test_auth.py
@tests/approvals/test_migration_contract.py

<interfaces>
Current auth request contracts:
```python
class LoginRequest(BaseModel):
    username: str
    password: str

class DemoTokenRequest(BaseModel):
    username: str
```

Current login lookup to replace:
```python
stmt = select(User).where(User.username == payload.username)
user = (await session.execute(stmt)).scalar_one_or_none()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tenant-aware auth request and resolver contract</name>
  <files>src/api/schemas/auth.py, src/api/routers/auth.py, tests/integration/test_auth.py</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-SPEC.md
    - .planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-CONTEXT.md
    - src/api/schemas/auth.py
    - src/api/routers/auth.py
    - src/auth/permissions.py
    - tests/integration/test_auth.py
  </read_first>
  <behavior>
    - Test `/api/v1/auth/login` accepts existing `{"username", "password"}` when username resolves to exactly one active user.
    - Test `/api/v1/auth/login` accepts `{"username", "password", "tenant_id"}` and resolves by `(tenant_id, username)`.
    - Test duplicate usernames across tenants without `tenant_id` fail closed with no token.
    - Test `/api/v1/auth/token` form login rejects ambiguous username without tenant context.
    - Test `/api/v1/auth/demo-token` accepts optional `tenant_id` and rejects ambiguous username without it.
  </behavior>
  <action>
    Implement D-09 and D-10 with concrete transitional behavior.

    In `src/api/schemas/auth.py`, add `tenant_id: uuid.UUID | None = None` to `LoginRequest` and `DemoTokenRequest`. Keep `username` and `password` unchanged so existing non-duplicate demo users continue to work.

    In `src/api/routers/auth.py`, add:
    `async def _resolve_user_for_login(session: AsyncSession, *, username: str, tenant_id: uuid.UUID | None) -> User | None`.

    `_resolve_user_for_login` must:
    - If `tenant_id` is provided, execute `select(User).where(User.username == username, User.tenant_id == tenant_id)` and return `.scalar_one_or_none()`.
    - If `tenant_id` is omitted, execute `select(User).where(User.username == username).limit(2)`, collect up to two users, return the single user when exactly one exists, return `None` when none exist, and raise `HTTPException(status_code=401, detail={"code": "UNAUTHORIZED", "message": "Invalid username or password"})` when more than one row exists.
    - Never select by `User.username` alone with `.scalar_one_or_none()` after this change.

    Update `/login`, `/token`, and `/demo-token` to use `_resolve_user_for_login`. For `/token`, OAuth2PasswordRequestForm has no `tenant_id`; keep it username-only transitional and fail closed when duplicates exist. Do not use tenant name, request body merchant id, frontend payload, user merchant binding, or role as tenant selector.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/integration/test_auth.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/api/schemas/auth.py src/api/routers/auth.py tests/integration/test_auth.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "tenant_id: uuid.UUID \\| None|_resolve_user_for_login|User.tenant_id" src/api/schemas/auth.py src/api/routers/auth.py tests/integration/test_auth.py` returns matches.
    - `rg -n "where\\(User.username == .*\\)\\.scalar_one_or_none" src/api/routers/auth.py` returns no matches.
    - `rg -n "tenant_id.*merchant_id|merchant_id.*tenant_id|role.*tenant" src/api/routers/auth.py` returns no tenant-selector inference matches.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/integration/test_auth.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>Auth lookup has an explicit tenant-aware path and fails closed when username-only resolution would be ambiguous.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tenant-scoped username metadata and contract tests</name>
  <files>src/db/models.py, tests/approvals/test_migration_contract.py, tests/integration/test_auth.py</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-SPEC.md
    - .planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-CONTEXT.md
    - src/db/models.py
    - tests/approvals/test_migration_contract.py
    - tests/integration/test_auth.py
  </read_first>
  <behavior>
    - Test SQLAlchemy metadata declares `UniqueConstraint("tenant_id", "username", name="uq_users_tenant_username")`.
    - Test `User.username` no longer uses `unique=True` in ORM metadata after the tenant-scoped unique constraint exists.
    - Test SQLAlchemy metadata declares `UniqueConstraint("id", "tenant_id", name="uq_merchants_id_tenant")` on `Merchant`.
    - Test SQLAlchemy metadata declares `ForeignKeyConstraint(["merchant_id", "tenant_id"], ["merchants.id", "merchants.tenant_id"], name="fk_users_merchant_tenant")` on `User` while preserving nullable `merchant_id`.
    - Test same-tenant duplicate username creation fails in metadata-backed tests.
    - Test an active business user whose `merchant_id` points to a merchant in another tenant is rejected by migration/preflight contract tests.
    - Test cross-tenant duplicate username creation succeeds when auth calls include `tenant_id`.
  </behavior>
  <action>
    Update `src/db/models.py` to make tenant-scoped principal identity and tenant-consistent merchant binding explicit:
    - Import `ForeignKeyConstraint` from SQLAlchemy if it is not already imported.
    - Add `__table_args__ = (UniqueConstraint("id", "tenant_id", name="uq_merchants_id_tenant"),)` to `Merchant`. This supports a composite FK target; it is intentionally redundant with the primary key because PostgreSQL requires the referenced column set to be unique.
    - Add `__table_args__ = (UniqueConstraint("tenant_id", "username", name="uq_users_tenant_username"), ForeignKeyConstraint(["merchant_id", "tenant_id"], ["merchants.id", "merchants.tenant_id"], name="fk_users_merchant_tenant"))` to `User`.
    - Replace `merchant_id: Mapped[uuid.UUID | None] = mapped_column(UUID(as_uuid=True), ForeignKey("merchants.id"))` with a nullable UUID column that participates in the composite `fk_users_merchant_tenant`; do not make `merchant_id` non-null because admins and inactive/invalid legacy rows may remain unbound.
    - Change `username: Mapped[str] = mapped_column(String(120), unique=True, nullable=False)` to `username: Mapped[str] = mapped_column(String(120), nullable=False)`.
    - Keep `tenant_id` non-null and indexed.

    Extend `tests/approvals/test_migration_contract.py` with ORM metadata assertions for `uq_users_tenant_username`, `uq_merchants_id_tenant`, `fk_users_merchant_tenant`, nullable `User.merchant_id`, and absence of a column-level `unique=True` flag on `User.username`. Plan 36-05 owns the Alembic migration that drops the legacy global unique constraint, creates the tenant-scoped username constraint, and applies the composite user merchant FK in live databases; this task updates ORM contract and auth behavior.

    Extend `tests/integration/test_auth.py` with a cross-tenant duplicate username fixture/case. The case must show login with `tenant_id` succeeds for each tenant and login without `tenant_id` returns no token when duplicates exist. Same-tenant duplicate prevention and cross-tenant merchant mismatch rejection can be asserted through the metadata-backed DB fixture or migration-contract static test if the live test fixture cannot insert duplicate rows before Plan 36-05.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/integration/test_auth.py tests/approvals/test_migration_contract.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/db/models.py tests/integration/test_auth.py tests/approvals/test_migration_contract.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "uq_users_tenant_username|UniqueConstraint\\(\"tenant_id\", \"username\"" src/db/models.py tests/approvals/test_migration_contract.py` returns matches.
    - `rg -n "uq_merchants_id_tenant|fk_users_merchant_tenant|ForeignKeyConstraint\\(\\[\"merchant_id\", \"tenant_id\"\\]" src/db/models.py tests/approvals/test_migration_contract.py` returns matches.
    - `rg -n "username: Mapped\\[str\\] = mapped_column\\(String\\(120\\), unique=True" src/db/models.py` returns no matches.
    - `rg -n "merchant_id: Mapped\\[uuid.UUID \\| None\\].*ForeignKey\\(\"merchants.id\"\\)" src/db/models.py` returns no matches.
    - `rg -n "cross.*tenant.*username|ambiguous.*username|tenant_id" tests/integration/test_auth.py` returns matches.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/integration/test_auth.py tests/approvals/test_migration_contract.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>ORM metadata and tests express tenant-scoped username identity plus tenant-consistent user merchant binding, and auth behavior has no ambiguous username-only success path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| login request -> user principal | Untrusted username and optional tenant selector resolve to one authenticated principal. |
| OAuth2 form -> user principal | Legacy form login has no tenant field and must fail closed if username is ambiguous. |
| ORM metadata -> migration | User identity constraints in models are consumed by the Plan 36-05 migration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T36-02 | Spoofing | `src/api/routers/auth.py` | mitigate | Resolve login by `(tenant_id, username)` when tenant id is present; fail closed on duplicate username without tenant context. |
| T36-01 | Elevation of Privilege | auth token principal | mitigate | Token still carries `sub`, `role`, and `tenant_id`; no merchant binding is inferred during login. |
| T36-04 | Information Disclosure | ambiguous login errors | mitigate | Use the existing generic 401 invalid-credentials message for ambiguous username without tenant selector. |
</threat_model>

<verification>
Run focused verification:
`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/integration/test_auth.py tests/approvals/test_migration_contract.py -q --tb=short`

Run lint:
`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/db/models.py src/api/schemas/auth.py src/api/routers/auth.py tests/integration/test_auth.py tests/approvals/test_migration_contract.py`
</verification>

<success_criteria>
- Auth has a tenant-aware principal resolver and no username-only ambiguous success path.
- ORM metadata defines `uq_users_tenant_username`.
- ORM metadata defines `uq_merchants_id_tenant` and `fk_users_merchant_tenant` while keeping `User.merchant_id` nullable.
- Same-tenant duplicate usernames are forbidden; cross-tenant duplicate usernames are resolvable only with explicit tenant id.
- Active business users cannot persistently point at a merchant from another tenant after the Phase 36 migration lands.
- No tenant selector is guessed from merchant id, role, prompt, memory, or frontend scope payload.
</success_criteria>

<output>
After completion, create `.planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-02-SUMMARY.md`.
</output>
