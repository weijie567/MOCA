---
phase: 52-safety-pre-route-node
plan: "02"
type: execute
wave: 2
depends_on:
  - "52-01"
files_modified:
  - src/agent/routing.py
  - src/agent/graph.py
  - tests/architecture/graph_baseline.py
  - tests/architecture/test_canonical_graph_baseline.py
  - tests/test_graph_routing.py
  - tests/agent/test_graph.py
autonomous: true
requirements:
  - CAGM-03
must_haves:
  truths:
    - "The active graph entry path is `START -> receive_request -> safety_pre_route`."
    - "`route_after_safety` is deterministic, side-effect-free, fail-closed, and returns only registered graph node keys."
    - "Unsafe pre-route dispositions cannot enter memory, investigate, approval, or action paths."
  artifacts:
    - path: "src/agent/routing.py"
      provides: "`route_after_safety` router and route allowlist"
      exports: ["route_after_safety"]
    - path: "src/agent/graph.py"
      provides: "Registered `safety_pre_route` node and conditional edge map"
      contains: "builder.add_node(\"safety_pre_route\""
    - path: "tests/architecture/graph_baseline.py"
      provides: "Post-Phase 52 active graph baseline and route map"
      contains: "safety_pre_route"
  key_links:
    - from: "src/agent/graph.py"
      to: "src/agent/nodes/safety_pre_route.py"
      via: "StateGraph node registration"
      pattern: "add_node\\(\"safety_pre_route\""
    - from: "src/agent/graph.py"
      to: "src/agent/routing.py"
      via: "conditional edge router"
      pattern: "add_conditional_edges\\(\\s*\"safety_pre_route\""
    - from: "tests/architecture/test_canonical_graph_baseline.py"
      to: "src/agent/graph.py"
      via: "AST graph source inspection"
      pattern: "receive_request.*safety_pre_route"
---

<objective>
Wire `safety_pre_route` into the active LangGraph path and update the architecture guardrails that prove the new route boundary.

Purpose: CAGM-03 is not complete until runtime graph source routes through `safety_pre_route` and fail-closed safety cases cannot reach legacy intent, memory, investigation, approval, or action boundaries.
Output: `route_after_safety`, active graph insertion, updated Phase 51 baseline tests, and graph-level fail-closed coverage.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<read_first>
@.planning/phases/52-safety-pre-route-node/52-CONTEXT.md
@.planning/phases/52-safety-pre-route-node/52-RESEARCH.md
@.planning/phases/52-safety-pre-route-node/52-PATTERNS.md
@.planning/phases/52-safety-pre-route-node/52-VALIDATION.md
@.planning/phases/51-canonical-graph-baseline-guardrails-and-migration-matrix/51-03-SUMMARY.md
@src/agent/nodes/safety_pre_route.py
@src/agent/routing.py
@src/agent/graph.py
@tests/architecture/graph_baseline.py
@tests/architecture/test_canonical_graph_baseline.py
@tests/test_graph_routing.py
@tests/agent/test_graph.py
</read_first>

<context>
## Locked Decisions Implemented

- D-52-01: Register `safety_pre_route` as a real LangGraph node immediately after `receive_request`.
- D-52-02: Add/expose deterministic `route_after_safety`; it returns only registered graph node keys.
- D-52-03: Safe continuation remains `safety_pre_route -> classify_intent` as temporary Phase 52 compatibility.
- D-52-07 / D-52-08: Unsafe approval/chat and bypass branches route to `clarification_gate` or explicitly tested deterministic `final_response`, before memory/investigate/approval/action.
- D-52-14: Update Phase 51 architecture guardrails for the new migration state.
- D-52-15 / D-52-16: Add graph/router tests for fail-closed unsafe branches and route coverage.
- D-52-17: All test commands use the MOCA-approved `UV_CACHE_DIR=/tmp/uv-cache uv run ...` entrypoint.

## Interfaces

From Plan 01:
```python
async def safety_pre_route(state: AgentState) -> dict: ...
```

From `src/agent/routing.py`:
```python
def route_after_intent(state: AgentState) -> str:
    try:
        route = _route_after_intent(state)
    except Exception:
        return "clarification_gate"
    return route if route in INTENT_ROUTES else "clarification_gate"
```

From `src/agent/graph.py` current shape:
```python
builder.add_edge(START, "receive_request")
builder.add_edge("receive_request", "classify_intent")
builder.add_conditional_edges("classify_intent", route_after_intent, {...})
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add route_after_safety and active graph wiring</name>
  <files>src/agent/routing.py, src/agent/graph.py, tests/test_graph_routing.py, tests/agent/test_graph.py</files>
  <behavior>
    - `SAFETY_ROUTES` is exactly the Phase 52 compatibility set: `{"classify_intent", "clarification_gate", "final_response"}`.
    - `route_after_safety(state)` wraps `_route_after_safety(state)` in the existing fail-closed style and returns `clarification_gate` for exceptions or unregistered route keys.
    - `_route_after_safety` sends `approval_chat_not_trusted`, `requires_clarification=True`, approval-bypass, and approval-like short reply dispositions to `clarification_gate` by default. A `final_response` direct-refusal branch is allowed only when the plan implementation names the deterministic refusal reason and adds a graph test for that exact branch.
    - `_route_after_safety` sends ordinary `none` and supported `safety_sensitive` dispositions to `classify_intent` for Phase 52 safe compatibility.
    - `src/agent/graph.py` registers `safety_pre_route`, changes `receive_request -> classify_intent` to `receive_request -> safety_pre_route`, and adds `builder.add_conditional_edges("safety_pre_route", route_after_safety, {...})`.
    - The legacy `classify_intent` conditional edge map remains unchanged except that unsafe branches should now be stopped earlier; do not route safe requests to `session_context_load` or `contextual_intent_resolve` in Phase 52.
  </behavior>
  <action>Implement `route_after_safety` beside existing deterministic routers and import it into `graph.py`. Wire the path map with `classify_intent`, `clarification_gate`, and `final_response` only. Add focused router tests in `tests/test_graph_routing.py` and graph smoke tests in `tests/agent/test_graph.py` proving untrusted approval chat / approval-bypass inputs produce traces that include `receive_request` then `safety_pre_route` and do not include `classify_intent`, `session_memory_load`, `long_term_memory_retrieve`, `investigate`, `approval_gate`, `action_draft`, or tool calls. This implements D-52-01, D-52-02, D-52-03, D-52-07, D-52-08, D-52-15, and D-52-16.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_graph_routing.py tests/agent/test_graph.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/routing.py src/agent/graph.py tests/test_graph_routing.py tests/agent/test_graph.py</automated>
  </verify>
  <done>`route_after_safety` is total and fail-closed; the runtime graph source starts `START -> receive_request -> safety_pre_route`; graph tests prove unsafe pre-route inputs cannot reach memory/investigate/approval/action/tool paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update static graph baseline and migration guardrails</name>
  <files>tests/architecture/graph_baseline.py, tests/architecture/test_canonical_graph_baseline.py</files>
  <behavior>
    - `CURRENT_ACTIVE_GRAPH_NODES_BASELINE` includes `safety_pre_route` as an active canonical node.
    - `CURRENT_CONDITIONAL_EDGE_BASELINE` includes `("safety_pre_route", "route_after_safety")` with route keys mapped only to registered node destinations.
    - Current active legacy node calculation no longer treats `safety_pre_route` as legacy; remaining legacy nodes stay migration-mode only.
    - Architecture tests explicitly assert `receive_request -> safety_pre_route` rather than the old `receive_request -> classify_intent` path.
    - `test_final_no_debt_gate_is_marked_phase58_scope` remains skipped; do not enforce the final exact 15-node graph in Phase 52.
  </behavior>
  <action>Update the Phase 51 AST guardrail helpers/tests to the post-Phase 52 baseline. Keep remaining legacy rows (`classify_intent`, `session_memory_load`, `extract_slots`, `long_term_memory_retrieve`, `generate_recommendation`, `assess_risk_and_approval`) in migration mode with their existing delete phases. Add a route-totality assertion for `route_after_safety` via `graph_router_route_values()` so router return values are covered by the graph path map.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/test_graph_routing.py tests/agent/test_graph.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check tests/architecture/graph_baseline.py tests/architecture/test_canonical_graph_baseline.py</automated>
  </verify>
  <done>Static architecture tests prove `safety_pre_route` is an active canonical node immediately after `receive_request`, router maps are total over registered node keys, and remaining legacy nodes are still migration-mode compatibility rather than hidden final debt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `safety_pre_route` -> router | Deterministic safety state is converted into a graph destination. |
| router -> LangGraph path map | Route string must match a registered node key and cannot bypass safety gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-03 | Tampering / Denial of Service | `route_after_safety` | mitigate | Add `SAFETY_ROUTES` allowlist, fail-closed wrapper, and static route-totality tests. |
| T-52-05 | Information Disclosure / Elevation of Privilege | graph entry path | mitigate | Change active edge to `receive_request -> safety_pre_route`; graph tests prove unsafe inputs do not reach memory, investigate, approval, action, or tool calls. |
| T-52-08 | Tampering | accidental Phase 53 implementation | mitigate | Safe route remains `classify_intent`; tests and architecture baseline must not include `contextual_intent_resolve` or `session_context_load` as the safe continuation from `safety_pre_route`. |
| T-52-13 | Repudiation | architecture baseline drift | mitigate | AST guardrails assert the active graph node set and conditional route maps from source. |

ASVS L1 posture: no accepted unresolved high-severity threat in this plan. Any path allowing unsafe approval chat into memory/investigate/approval/action is a blocker.
</threat_model>

<verification>
Run after all tasks:

```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/test_graph_routing.py tests/agent/test_graph.py -q --tb=short
UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/routing.py src/agent/graph.py tests/architecture/graph_baseline.py tests/architecture/test_canonical_graph_baseline.py tests/test_graph_routing.py tests/agent/test_graph.py
```
</verification>

<rollback_and_deviation>
- If `route_after_safety` needs service, LLM, repository, memory, tool, approval, action, or external API access, stop; routers must stay deterministic and side-effect-free.
- If safe continuation needs `session_context_load` or `contextual_intent_resolve`, stop; that is Phase 53 / CAGM-04.
- If direct refusal through `final_response` is used for any fail-closed branch, it must have an explicit deterministic reason and graph test; otherwise route to `clarification_gate`.
</rollback_and_deviation>

<out_of_scope>
- No deletion of `classify_intent`; safe-path compatibility is deliberate until Phase 53.
- No Phase 54 slot, Phase 55 memory, Phase 56 recommendation, Phase 57 risk/approval, or Phase 58 no-debt cutover work.
- No new tools, memory reads, business-fact queries, RAG retrieval, approval creation, action drafting, or external action execution.
</out_of_scope>

<source_audit>
| Source | ID | Required item | Plan | Status |
|--------|----|---------------|------|--------|
| GOAL | Phase 52 | Active entry path becomes `START -> receive_request -> safety_pre_route` | 02 | COVERED |
| REQ | CAGM-03 | Registered node immediately after `receive_request`, before memory/investigation/approval/action paths | 02 | COVERED |
| CONTEXT | D-52-01..D-52-03 | Node registration, deterministic router, safe compatibility to `classify_intent` | 02 | COVERED |
| CONTEXT | D-52-14..D-52-17 | Guardrail updates, fail-closed graph tests, route coverage, approved pytest entrypoints | 02 | COVERED |
| RESEARCH | Architecture guardrails | Update AST graph baseline rather than live-provider graph introspection | 02 | COVERED |
| VALIDATION | 52-02-01, 52-02-02 | Graph/router wiring, static guardrails, fail-closed router behavior | 02 | COVERED |
</source_audit>

<success_criteria>
- `src/agent/graph.py` registers `safety_pre_route` and wires `receive_request` to it.
- `route_after_safety` returns only registered path-map keys and fails closed.
- Architecture tests include `safety_pre_route` as active canonical state while preserving remaining legacy nodes as migration-mode only.
- Unsafe pre-route graph tests prove no memory/tool/approval/action side effects.
</success_criteria>

<output>
After completion, create `.planning/phases/52-safety-pre-route-node/52-02-SUMMARY.md`.
</output>
