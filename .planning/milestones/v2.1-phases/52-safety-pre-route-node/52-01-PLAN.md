---
phase: 52-safety-pre-route-node
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/intent_policy.py
  - src/agent/state.py
  - src/agent/nodes/receive_request.py
  - src/agent/nodes/classify_intent.py
  - src/agent/nodes/safety_pre_route.py
  - tests/agent/test_nodes/test_receive_request.py
  - tests/agent/test_nodes/test_classify_intent.py
  - tests/agent/test_nodes/test_safety_pre_route.py
autonomous: true
requirements:
  - CAGM-03
must_haves:
  truths:
    - "A deterministic `safety_pre_route` node exists and records request-risk decisions before legacy intent classification."
    - "Untrusted approval chat, approval-bypass attempts, and standalone approval/action short replies fail closed without invoking LLM, memory, tools, approval, or action code."
    - "Ordinary safety-sensitive but supported requests are tagged as safety-sensitive and can continue to the legacy intent path without creating authority fields."
  artifacts:
    - path: "src/agent/nodes/safety_pre_route.py"
      provides: "New deterministic safety pre-route node"
      exports: ["safety_pre_route"]
    - path: "src/agent/intent_policy.py"
      provides: "Shared pre-route DTO, detector, and approval-like short-reply helper"
      exports: ["PreRouteDecision", "detect_pre_route"]
    - path: "tests/agent/test_nodes/test_safety_pre_route.py"
      provides: "Unit coverage for safety dispositions, forbidden writes, negative controls, and trace visibility"
  key_links:
    - from: "src/agent/nodes/safety_pre_route.py"
      to: "src/agent/intent_policy.py"
      via: "imports existing deterministic safety helpers"
      pattern: "from src\\.agent\\.intent_policy import .*detect_pre_route"
    - from: "tests/agent/test_nodes/test_safety_pre_route.py"
      to: "src/agent/nodes/safety_pre_route.py"
      via: "direct async node invocation"
      pattern: "await safety_pre_route"
---

<objective>
Create the deterministic `safety_pre_route` node for CAGM-03 and lock its unit-level safety contract before graph wiring.

Purpose: Phase 52 extracts request-risk pre-route behavior from the thick legacy `classify_intent` boundary without implementing Phase 53 intent/session-context cutover.
Output: A reusable node, shared deterministic helper surface, trace-visible safety decision state, and focused unit tests proving no LLM/tool/memory/approval/action side effects.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<read_first>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/52-safety-pre-route-node/52-CONTEXT.md
@.planning/phases/52-safety-pre-route-node/52-RESEARCH.md
@.planning/phases/52-safety-pre-route-node/52-PATTERNS.md
@.planning/phases/52-safety-pre-route-node/52-VALIDATION.md
@.planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
@docs/contract-spec.md
@src/agent/intent_policy.py
@src/agent/state.py
@src/agent/nodes/receive_request.py
@src/agent/nodes/classify_intent.py
@tests/agent/test_nodes/test_classify_intent.py
</read_first>

<context>
## Locked Decisions Implemented

- D-52-05: Extract only deterministic request-risk pre-route behavior: `detect_pre_route(...)`, untrusted approval chat, approval-bypass / approval-like short reply guards, and safety-sensitive tagging.
- D-52-06: `safety_pre_route` must not call LLMs, repositories, services, memory, tools, approval, action, RAG, or evidence verification.
- D-52-07 / D-52-08: Untrusted approval chat, standalone approval/action short replies, and explicit approval-bypass attempts must be represented as safety dispositions that fail closed before downstream paths.
- D-52-09: Safety-sensitive supported requests can be tagged and continue later, but the node must not produce `proposed_action`, approval state, or action draft fields.
- D-52-10: Broad semantic unsupported classification stays with intent resolution unless a deterministic request-risk case is explicitly justified.
- D-52-11: The new node must emit/project `safety_pre_route`; the decision cannot exist only inside `intent_classification`.

## Interfaces

From `src/agent/intent_policy.py`:
```python
class PreRouteDecision(BaseModel):
    disposition: Literal["none", "approval_chat_not_trusted", "safety_sensitive", "multi_target_request"] = "none"
    requested_operation: RequestedOperationLiteral | None = None
    reason_codes: list[str] = []
    requires_clarification: bool = False

def detect_pre_route(query: str) -> PreRouteDecision: ...
```

From `src/agent/nodes/receive_request.py`:
```python
async def receive_request(state: AgentState) -> dict:
    trace_steps = [{"node": "receive_request", "status": "completed", ...}]
```

From `src/agent/nodes/classify_intent.py`:
```python
FORBIDDEN_STATE_WRITES = {
    "approval_result", "approval_revision_refs", "trusted_approval_result",
    "resume", "command", "extracted_slots", "active_slots", "risk_signals",
    "final_response", "tool_results", "action_result", "proposed_action",
}
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock safety pre-route node behavior before implementation</name>
  <files>tests/agent/test_nodes/test_safety_pre_route.py, tests/agent/test_nodes/test_receive_request.py, tests/agent/test_nodes/test_classify_intent.py</files>
  <behavior>
    - `safety_pre_route` returns a `pre_route_decision` matching `detect_pre_route(query)` for approval-chat, multi-target, safety-sensitive action/escalation, and ordinary safe requests.
    - Approval-like standalone replies such as `同意`, `approve`, and `doit` are treated as untrusted approval/action chat and require clarification.
    - Negative controls remain safe or legacy-owned: ordinary read requests, pending-slot identifier replies such as `OD-12345`, and ordinary ambiguous non-approval replies do not become approval-bypass failures.
    - The node appends a trace step with `node == "safety_pre_route"` and never replaces the prior `receive_request` trace step.
    - Node output never contains authority or side-effect fields: `proposed_action`, `approval_result`, `approval_revision_refs`, `trusted_approval_result`, `action_draft`, `draft_outcome`, `action_result`, `tool_results`, `business_context`, `retrieved_evidence`, `policy_evidence`, `case_memory`, `memory_context`, `memory_context_bundle`, `risk_assessment`, or `final_response`.
    - `receive_request` resets any new `pre_route_decision` / `safety_flags` ephemeral fields if this plan adds them to `AgentState`.
    - Existing `classify_intent` tests for active-flow slot replies and safety-sensitive parity still pass; Phase 52 must preserve legacy classifier behavior except unsafe branches that will be routed earlier in Plan 02.
  </behavior>
  <action>Create RED tests first. Use existing `tests/agent/test_nodes/test_classify_intent.py` fixture style and direct async node invocation. Include a static/import guard in `test_safety_pre_route.py` that reads `src/agent/nodes/safety_pre_route.py` and fails if it imports or calls forbidden dependency symbols such as `_get_llm`, `llm`, `ToolPlatform`, `Memory`, `Repository`, `Session`, `BusinessFactService`, `PolicyKnowledgeService`, `ApprovalService`, `ActionService`, `investigate`, or `rag_context_build`. Do not make this guard scan generic authority-field strings such as `action_draft`; authority-field coverage belongs to node-output assertions. This implements D-52-06 and the no-authority-field Nyquist coverage.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_safety_pre_route.py tests/agent/test_nodes/test_receive_request.py tests/agent/test_nodes/test_classify_intent.py -q --tb=short</automated>
  </verify>
  <done>Tests fail before the production node exists, then pass after Task 2. Coverage includes pre/post parity, negative controls, trace visibility, no authority fields, and no memory/tool/approval/action side effects for CAGM-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic safety_pre_route node and shared helpers</name>
  <files>src/agent/intent_policy.py, src/agent/state.py, src/agent/nodes/receive_request.py, src/agent/nodes/classify_intent.py, src/agent/nodes/safety_pre_route.py</files>
  <behavior>
    - `safety_pre_route(state)` inspects only the current request/current state, reuses `detect_pre_route(...)`, and returns deterministic routing/trace metadata.
    - Approval-like short replies reuse a shared helper moved/exposed from `classify_intent` to `intent_policy.py`; do not create a second token list or second detector.
    - The smallest state shape is explicit and non-authoritative: `pre_route_decision` as `PreRouteDecision.model_dump()` plus optional `safety_flags` / `routing_hints` metadata. These fields must be reset by `receive_request` if added to `AgentState`.
    - The node only writes `pre_route_decision`, `safety_flags`, `routing_hints`, and `trace_steps`; it does not write LLM outputs, memory/evidence/business facts, approval state, action drafts, tools, proposed actions, or final responses.
    - `classify_intent` remains compatible for safe-path Phase 52 continuation and may still include `classification_trace.pre_route_decision` as a Phase 53 cleanup artifact, but it should reuse the shared short-reply helper rather than owning duplicate logic.
  </behavior>
  <action>Implement `src/agent/nodes/safety_pre_route.py` using the trace-step style from `receive_request.py` and helper contracts from `intent_policy.py`. Preserve D-52-03 by not moving session context before intent and not replacing `classify_intent`. Preserve D-52-10 by limiting unsupported handling to deterministic request-risk cases already represented by pre-route decisions or approval-like short reply guards. If a broader unsupported case appears necessary, stop and record a spec delta rather than silently expanding scope.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_safety_pre_route.py tests/agent/test_nodes/test_receive_request.py tests/agent/test_nodes/test_classify_intent.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/intent_policy.py src/agent/state.py src/agent/nodes/receive_request.py src/agent/nodes/classify_intent.py src/agent/nodes/safety_pre_route.py tests/agent/test_nodes/test_receive_request.py tests/agent/test_nodes/test_classify_intent.py tests/agent/test_nodes/test_safety_pre_route.py</automated>
  </verify>
  <done>`safety_pre_route` is deterministic, trace-visible, covered by focused tests, and side-effect free. Existing classifier parity tests remain green except behavior intentionally moved earlier to the new node.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user chat -> `safety_pre_route` | Untrusted ordinary chat text becomes deterministic safety metadata. |
| `safety_pre_route` -> downstream graph | Safety metadata can influence routing in Plan 02 and must not create authority fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Spoofing / Elevation of Privilege | `safety_pre_route` approval-chat detection | mitigate | Reuse `detect_pre_route(...)`; tests cover `approve APR-1` and Chinese approval text as `approval_chat_not_trusted`. |
| T-52-02 | Elevation of Privilege | approval/action short replies | mitigate | Move/share the existing short-reply helper; tests prove `同意`, `approve`, and `doit` require clarification without LLM calls. |
| T-52-06 | Tampering / Elevation of Privilege | node state writes | mitigate | Tests assert forbidden authority fields are absent from node output. |
| T-52-07 | Information Disclosure / Elevation of Privilege | accidental LLM/tool/memory/repository call | mitigate | Static/import guard fails on forbidden imports/references in `safety_pre_route.py`. |
| T-52-11 | Repudiation | trace visibility | mitigate | Tests require a `trace_steps` entry with `node == "safety_pre_route"` and preserve prior trace steps. |
| T-52-12 | Tampering | broad unsupported classification creep | mitigate | Scope locked to deterministic request-risk cases; any broader unsupported pre-route expansion is a stop-and-record spec delta. |

ASVS L1 posture: no accepted unresolved high-severity threat in this plan. Any failure of T-52-01, T-52-02, T-52-06, or T-52-07 blocks completion.
</threat_model>

<verification>
Run after all tasks:

```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_safety_pre_route.py tests/agent/test_nodes/test_receive_request.py tests/agent/test_nodes/test_classify_intent.py -q --tb=short
UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/intent_policy.py src/agent/state.py src/agent/nodes/receive_request.py src/agent/nodes/classify_intent.py src/agent/nodes/safety_pre_route.py tests/agent/test_nodes/test_receive_request.py tests/agent/test_nodes/test_classify_intent.py tests/agent/test_nodes/test_safety_pre_route.py
```
</verification>

<rollback_and_deviation>
- If the node needs any LLM, repository, memory, tool, approval, RAG, business-fact, or action dependency to pass tests, stop; that violates D-52-06 and requires re-planning.
- If broader unsupported classification is required, record a spec delta / MVP scope decision before implementation per D-52-10.
- If moving short-reply helpers breaks active-flow pending-slot behavior, restore active-flow parity first; Phase 52 only extracts approval/action-like standalone reply safety.
</rollback_and_deviation>

<out_of_scope>
- No graph edge wiring; Plan 02 owns `src/agent/graph.py` and `route_after_safety`.
- No `session_context_load -> contextual_intent_resolve` cutover; Phase 53 owns it.
- No `slot_resolution_gate`, `memory_context_load`, `recommendation_generation`, `risk_gate`, final graph cutover, UI, AI-SPEC, or external action execution work.
</out_of_scope>

<source_audit>
| Source | ID | Required item | Plan | Status |
|--------|----|---------------|------|--------|
| GOAL | Phase 52 | Extract request-risk pre-route into explicit `safety_pre_route` before memory/context enrichment | 01,02,03 | COVERED |
| REQ | CAGM-03 | Explicit registered node after `receive_request`, owning request-risk / unsafe / unsupported / untrusted approval pre-route decisions | 01,02,03 | COVERED |
| CONTEXT | D-52-05..D-52-11 | Deterministic safety split, no side effects, fail-closed dispositions, trace-visible decision | 01 | COVERED |
| CONTEXT | D-52-03, D-52-10 | Preserve Phase 53 boundary and avoid broad unsupported expansion | 01,03 | COVERED |
| RESEARCH | Node pattern | Use `PreRouteDecision`, `detect_pre_route`, shared short-reply helper, trace append style | 01 | COVERED |
| VALIDATION | 52-01-01, 52-01-02 | Unit coverage for deterministic decisions, negative controls, no authority fields, no side effects | 01 | COVERED |
</source_audit>

<success_criteria>
- `src/agent/nodes/safety_pre_route.py` exists and exports `safety_pre_route`.
- The new node appends a canonical `safety_pre_route` trace step and writes only deterministic safety/routing metadata.
- Unit tests cover parity, fail-closed pre-route cases, negative controls, forbidden writes, and forbidden dependencies.
- All verification commands use `UV_CACHE_DIR=/tmp/uv-cache uv run ...`; no bare pytest or bare python -m pytest command appears in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/52-safety-pre-route-node/52-01-SUMMARY.md`.
</output>
