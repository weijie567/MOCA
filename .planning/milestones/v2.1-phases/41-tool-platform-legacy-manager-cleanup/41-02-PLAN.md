---
phase: 41-tool-platform-legacy-manager-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - "41-01"
files_modified:
  - src/agent/nodes/investigate.py
  - src/agent/nodes/action_draft.py
  - tests/test_execute_action.py
  - tests/agent/test_phase22_action_boundary.py
  - tests/agent/test_graph.py
  - tests/agent/test_nodes/test_investigate.py
  - tests/agent/test_policy_retrieval_ownership.py
  - tests/knowledge/test_facade_integration.py
autonomous: true
requirements:
  - TPH-06
must_haves:
  truths:
    - "Production nodes no longer unwrap `_platform` from legacy manager config keys."
    - "Tests inject ToolPlatform-shaped fakes directly."
    - "Fake platforms no longer depend on manager-private `_descriptors` / `_manager.invoke` wiring."
---

<objective>
Migrate production injection seams and test fakes from manager-shaped objects to platform-shaped objects.
</objective>

<context>
@.planning/phases/41-tool-platform-legacy-manager-cleanup/41-CONTEXT.md
@src/agent/nodes/investigate.py
@src/agent/nodes/action_draft.py
@src/tools/platform.py
@tests/test_execute_action.py
@tests/agent/test_phase22_action_boundary.py
@tests/agent/test_graph.py
@tests/agent/test_nodes/test_investigate.py
@tests/agent/test_policy_retrieval_ownership.py
@tests/knowledge/test_facade_integration.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove production legacy manager unwrapping</name>
  <files>src/agent/nodes/investigate.py, src/agent/nodes/action_draft.py</files>
  <action>
    In `investigate.py`, remove fallback from `configurable["tool_manager"]._platform`. Keep `tool_platform`, then `ToolPlatform.with_defaults(session)`, then `ToolPlatform(executors={})`.
    In `action_draft.py`, remove fallback from `configurable["action_tool_manager"]._platform`. Keep `action_tool_platform`, then `tool_platform`, then `ToolPlatform.with_defaults(session)`.
    Do not change tool invocation result semantics.
  </action>
  <verify>
    <automated>rg -n "tool_manager|action_tool_manager|_platform" src/agent/nodes/investigate.py src/agent/nodes/action_draft.py</automated>
  </verify>
  <acceptance_criteria>
    - No production node references `tool_manager`, `action_tool_manager`, or `._platform`.
    - Nodes still import and use `ToolPlatform`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate action and graph tests to platform injection</name>
  <files>tests/test_execute_action.py, tests/agent/test_phase22_action_boundary.py, tests/agent/test_graph.py</files>
  <action>
    Replace `UnifiedToolManager` construction/imports with `ToolPlatform` or platform-shaped fakes.
    In action-draft tests, use `action_tool_platform`.
    In graph tests, remove `_config` logic that unwraps `manager._platform`; pass platform objects directly.
    Rename fake classes from `Fake*Manager` to `Fake*Platform` where helpful.
  </action>
  <verify>
    <automated>uv run pytest tests/test_execute_action.py tests/agent/test_phase22_action_boundary.py tests/agent/test_graph.py -q</automated>
  </verify>
  <acceptance_criteria>
    - These tests no longer import `src.tools.manager`.
    - These tests no longer use `action_tool_manager`, `tool_manager`, or `._platform` manager unwrapping.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate investigate/policy/facade fakes to platform-native fakes</name>
  <files>tests/agent/test_nodes/test_investigate.py, tests/agent/test_policy_retrieval_ownership.py, tests/knowledge/test_facade_integration.py</files>
  <action>
    Convert manager-shaped fakes to implement the `ToolPlatform` facade methods directly: `visible_tools`, `invoke`, `descriptor`, and `event_family`.
    Store descriptors directly on the fake platform if needed; do not hide them behind `_manager._descriptors`.
    Update outdated comments that say manager owns graph-facing execution.
  </action>
  <verify>
    <automated>uv run pytest tests/agent/test_nodes/test_investigate.py tests/agent/test_policy_retrieval_ownership.py tests/knowledge/test_facade_integration.py -q</automated>
  </verify>
  <acceptance_criteria>
    - These tests no longer depend on `_manager._descriptors`, `_manager.invoke`, or manager `_platform`.
    - Focused tests pass.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Plan 41-02 is complete when:
- `rg -n "tool_manager|action_tool_manager|UnifiedToolManager|src\\.tools\\.manager|\\._platform" src/agent/nodes tests/test_execute_action.py tests/agent/test_phase22_action_boundary.py tests/agent/test_graph.py tests/agent/test_nodes/test_investigate.py tests/agent/test_policy_retrieval_ownership.py tests/knowledge/test_facade_integration.py` has no legacy-manager matches except unrelated wording explicitly justified in summary.
- Focused pytest commands above pass.
- Ruff passes for touched Python files.
</verification>

<output>
After completion, create `41-02-SUMMARY.md`.
</output>
