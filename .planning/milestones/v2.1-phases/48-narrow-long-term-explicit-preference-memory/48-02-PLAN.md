---
phase: 48-narrow-long-term-explicit-preference-memory
plan: 02
type: execute
wave: 2
depends_on: [48-01]
files_modified:
  - src/memory/policy.py
  - src/memory/schemas.py
  - src/memory/long_term.py
  - src/memory/semantic_episode.py
  - tests/memory/test_memory_policy.py
  - tests/memory/test_long_term_memory_service.py
  - tests/memory/test_semantic_episode_projection.py
  - tests/memory/test_phase48_long_term_preference_alignment.py
autonomous: true
requirements: [MEM-05]
requirements_addressed: [MEM-05]
must_haves:
  truths:
    - "long-term writes only insert preference memory rows"
    - "only explicit user/admin/human-reviewed long-term source types can auto-publish"
    - "semantic episode projection emits only preference_candidate needs-review long-term candidates"
    - "disallowed long-term source types skip before insertion and emit audit events"
    - "schema/table identity remains backward-compatible; no long_term_memories migration is introduced"
  artifacts:
    - path: "src/memory/policy.py"
      provides: "Phase 48 published long-term source allowlist"
      contains: "PUBLISHED_LONG_TERM_SOURCE_TYPES"
    - path: "src/memory/long_term.py"
      provides: "preference-only and policy-skip write guard"
      contains: "not_preference_memory_kind"
    - path: "src/memory/semantic_episode.py"
      provides: "preference candidate-only semantic projection"
      contains: "preference_candidate"
  key_links:
    - from: "src/memory/policy.py"
      to: "src/memory/long_term.py"
      via: "LongTermMemoryService.write_memory honors skip decisions before insert"
      pattern: "source_type_not_allowed"
    - from: "src/memory/semantic_episode.py"
      to: "src/memory/long_term.py"
      via: "semantic_episode_candidate creates needs_review preference candidate only"
      pattern: "semantic_episode_candidate"
---

<objective>
Narrow long-term write policy and automatic semantic projection to explicit preference-only candidates.

Purpose: Phase 48 cannot rely only on docs. The service must refuse non-preference long-term rows and disallowed automatic sources before insertion, while still allowing semantic episodes to create needs-review preference candidates.
Output: source policy constants, service skip branch, schema default narrowing, semantic episode projection narrowing, and focused tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/48-narrow-long-term-explicit-preference-memory/48-CONTEXT.md
@.planning/phases/48-narrow-long-term-explicit-preference-memory/48-RESEARCH.md
@.planning/phases/48-narrow-long-term-explicit-preference-memory/48-VALIDATION.md
@.planning/phases/48-narrow-long-term-explicit-preference-memory/48-PATTERNS.md
@.planning/phases/48-narrow-long-term-explicit-preference-memory/48-01-SUMMARY.md
@src/memory/policy.py
@src/memory/schemas.py
@src/memory/long_term.py
@src/memory/semantic_episode.py
@tests/memory/test_memory_policy.py
@tests/memory/test_long_term_memory_service.py
@tests/memory/test_semantic_episode_projection.py
@tests/memory/test_phase48_long_term_preference_alignment.py

<interfaces>
Current policy decision shape:
```python
class MemoryPolicyDecision(BaseModel):
    memory_type: MemoryPolicyMemoryType
    decision: Literal["write", "needs_review", "skip"]
    review_status: str | None = None
    reason_code: str
    blocked_by: list[str] = Field(default_factory=list)
```

Current candidate shape:
```python
class LongTermMemoryWriteCandidate(BaseModel):
    tenant_id: uuid.UUID
    run_id: uuid.UUID
    scope_type: LongTermScopeType
    scope_id: str
    memory_kind: LongTermMemoryKind = "fact"
    content: str
    source_type: LongTermSourceType
```
</interfaces>

<source_audit>
SOURCE | ID | Requirement / Decision | Plan | Status
--- | --- | --- | --- | ---
ROADMAP | Phase 48 SC1-SC3 | Preference-only memory, ordinary runs do not write generic long-term, PII/review/audit preserved | 48-02 | COVERED
CONTEXT | D-48-04/D-48-05 | explicit user/admin/human reviewed may publish only through gates | 48-02 | COVERED
CONTEXT | D-48-06/D-48-08 | published source allowlist and semantic candidate exception | 48-02 | COVERED
CONTEXT | D-48-12 | semantic episode may project only preference_candidate | 48-02 | COVERED
RESEARCH | Service skip gap | `write_memory` must honor policy skip before insertion | 48-02 | COVERED
</source_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Narrow long-term source policy and service insertion guards</name>
  <read_first>
    - src/memory/policy.py `AUTO_APPROVED_LONG_TERM_SOURCE_TYPES`, `AUTO_APPROVED_DURABLE_LONG_TERM_SOURCE_TYPES`, `REVIEW_REQUIRED_LONG_TERM_SOURCE_TYPES`, `long_term_memory_policy_decision`
    - src/memory/schemas.py `LongTermMemoryKind`, `LongTermSourceType`, `LongTermMemoryWriteCandidate`
    - src/memory/long_term.py `LongTermMemoryService.write_memory`, `_policy_decision_for_candidate`, PII/tombstone skip branches
    - tests/memory/test_memory_policy.py existing long-term source policy tests
    - tests/memory/test_long_term_memory_service.py existing deterministic source and LLM candidate tests
  </read_first>
  <files>src/memory/policy.py, src/memory/schemas.py, src/memory/long_term.py, tests/memory/test_memory_policy.py, tests/memory/test_long_term_memory_service.py</files>
  <behavior>
    - Test 1: `PUBLISHED_LONG_TERM_SOURCE_TYPES == frozenset({"explicit_user_preference", "explicit_admin_preference", "human_reviewed"})`.
    - Test 2: `long_term_memory_policy_decision("explicit_user_preference").decision == "write"` and review status is `auto_approved`.
    - Test 3: `long_term_memory_policy_decision("explicit_admin_preference").decision == "write"` and review status is `auto_approved`.
    - Test 4: `long_term_memory_policy_decision("human_reviewed").decision == "write"` and review status is `auto_approved`.
    - Test 5: `long_term_memory_policy_decision("semantic_episode_candidate").decision == "needs_review"` and blocked_by is `["source_requires_review"]`.
    - Test 6: `long_term_memory_policy_decision("deterministic_tool_result").decision == "skip"` and reason_code is `source_type_not_allowed`.
    - Test 7: `LongTermMemoryWriteCandidate()` default `memory_kind` is `preference`.
    - Test 8: `LongTermMemoryService.write_memory(...)` skips `memory_kind="fact"` before insertion with reason `not_preference_memory_kind`.
    - Test 9: `LongTermMemoryService.write_memory(...)` skips disallowed `source_type="deterministic_tool_result"` before insertion with reason `source_type_not_allowed`.
  </behavior>
  <action>
    In `src/memory/policy.py`, replace the broad long-term auto-approval constants with:
    ```python
    PUBLISHED_LONG_TERM_SOURCE_TYPES = frozenset(
        {"explicit_user_preference", "explicit_admin_preference", "human_reviewed"}
    )
    AUTO_APPROVED_LONG_TERM_SOURCE_TYPES = PUBLISHED_LONG_TERM_SOURCE_TYPES
    REVIEW_REQUIRED_LONG_TERM_SOURCE_TYPES = frozenset({"semantic_episode_candidate"})
    DISALLOWED_LONG_TERM_SOURCE_TYPES = frozenset(
        {
            "deterministic_tool_result",
            "confirmed_business_outcome",
            "approved_approval_state",
            "llm_candidate",
            "summary_candidate",
            "cross_case_pattern_candidate",
            "behavior_inference",
        }
    )
    ```
    Remove `AUTO_APPROVED_DURABLE_LONG_TERM_SOURCE_TYPES` and the current-business-object durable-source branch from long-term policy. Keep current-business-object logic for case memory unchanged. `long_term_memory_policy_decision(...)` must:
    1. return PII skip before source checks;
    2. return write/auto_approved for `PUBLISHED_LONG_TERM_SOURCE_TYPES`;
    3. return needs_review for `semantic_episode_candidate`;
    4. return skip with `reason_code="source_type_not_allowed"` and `blocked_by=["source_type_not_allowed"]` for every disallowed or unknown long-term source.

    In `src/memory/schemas.py`, keep database-compatible literal values for `LongTermMemoryKind` and `LongTermSourceType`, but change `LongTermMemoryWriteCandidate.memory_kind` default from `"fact"` to `"preference"`. Do not change `src/db/models.py` or migrations in this plan.

    In `src/memory/long_term.py`, add a service guard immediately after tombstone and PII checks and before duplicate/insert logic:
    - if `candidate.memory_kind != "preference"`, emit a `MemoryWriteEvent` with `decision="skip"`, `reason_code="not_preference_memory_kind"`, `blocked_by=["memory_kind"]`, and return `LongTermMemoryWriteResult(status="skipped", memory_id=None, review_status=None, decision="skip", reason_code="not_preference_memory_kind", ...)`.
    - if `policy_decision.decision == "skip"`, emit a skip event using `policy_decision.reason_code` and `policy_decision.blocked_by`, and return skipped without inserting a row.
    Keep existing PII/tombstone/duplicate event behavior intact.

    Update `tests/memory/test_memory_policy.py` and `tests/memory/test_long_term_memory_service.py` by replacing old durable deterministic long-term auto-publish expectations with Phase 48 expectations. Existing case-memory deterministic source tests must remain `needs_review` and must not be weakened.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_memory_policy.py tests/memory/test_long_term_memory_service.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/memory/policy.py` contains `PUBLISHED_LONG_TERM_SOURCE_TYPES`.
    - `src/memory/policy.py` contains `DISALLOWED_LONG_TERM_SOURCE_TYPES`.
    - `src/memory/policy.py` does not contain `AUTO_APPROVED_DURABLE_LONG_TERM_SOURCE_TYPES`.
    - `src/memory/long_term.py` contains `not_preference_memory_kind`.
    - `src/memory/long_term.py` contains `source_type_not_allowed`.
    - `src/memory/schemas.py` contains `memory_kind: LongTermMemoryKind = "preference"`.
    - `tests/memory/test_memory_policy.py` contains `test_long_term_only_explicit_preference_sources_auto_publish`.
    - The automated pytest command exits 0.
  </acceptance_criteria>
  <done>Long-term service insertion is preference-only, published source types are narrowed, and disallowed broad sources skip before row insertion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Narrow semantic episodes to needs-review preference candidates only</name>
  <read_first>
    - src/memory/semantic_episode.py `SemanticEpisodeKind`, `_SUMMARY_KEYS`, `_MEMORY_KIND`, `project_semantic_episode_candidates`, `to_long_term_memory_candidate`
    - tests/memory/test_semantic_episode_projection.py existing projection tests
    - tests/memory/test_phase48_long_term_preference_alignment.py from Plan 48-01
  </read_first>
  <files>src/memory/semantic_episode.py, tests/memory/test_semantic_episode_projection.py, tests/memory/test_phase48_long_term_preference_alignment.py</files>
  <behavior>
    - Test 1: semantic summaries containing `cross_case_patterns`, `similar_cases`, and `strategy_hints` emit no long-term candidates for those keys.
    - Test 2: semantic summaries containing `preference_candidates` emit only `kind="preference_candidate"`.
    - Test 3: emitted candidates keep `source_type="semantic_episode_candidate"` and `review_status="needs_review"`.
    - Test 4: `candidate.to_long_term_memory_candidate().memory_kind == "preference"`.
    - Test 5: writing the candidate through `LongTermMemoryService.write_memory(...)` results in `needs_review`, `is_current=False`, and no retrieval before approval.
    - Test 6: prompt-safe forbidden markers still do not appear in serialized candidates.
  </behavior>
  <action>
    Update `src/memory/semantic_episode.py` so `project_semantic_episode_candidates(...)` only considers `preference_candidate` items. The preferred minimal implementation is:
    - keep `SEMANTIC_EPISODE_SOURCE_TYPE = "semantic_episode_candidate"`;
    - set `SemanticEpisodeKind = Literal["preference_candidate"]` if all local callers/tests allow it, or otherwise keep the broader literal but iterate only over `("preference_candidate",)`;
    - remove or stop using `_CONTENT_PREFIX` and `_MEMORY_KIND` entries for `cross_case_pattern`, `similar_case_hint`, and `strategy_hint`;
    - ensure `to_long_term_memory_candidate()` always returns `memory_kind="preference"`;
    - keep raw payload, policy body, authority body, evidence ref, snapshot, debug, trace, and replay filtering unchanged.

    Update `tests/memory/test_semantic_episode_projection.py` so `test_semantic_episode_projection_creates_candidates_only` expects exactly `{"preference_candidate"}` instead of four kinds. Add a specific regression test named `test_semantic_episode_does_not_project_patterns_strategy_or_similar_cases_to_long_term` that supplies only non-preference semantic keys and asserts the candidate list is empty.

    Extend `tests/memory/test_phase48_long_term_preference_alignment.py` with a static source guard named `test_phase48_semantic_episode_source_mentions_only_preference_candidate_projection`. It should read `src/memory/semantic_episode.py` and assert `preference_candidate` exists while `cross_case_pattern`, `similar_case_hint`, and `strategy_hint` do not appear in the long-term projection map/iteration path. The guard may allow those strings in comments only if the test strips comments first.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_semantic_episode_projection.py tests/memory/test_phase48_long_term_preference_alignment.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/memory/test_semantic_episode_projection.py` contains `test_semantic_episode_does_not_project_patterns_strategy_or_similar_cases_to_long_term`.
    - `src/memory/semantic_episode.py` contains `preference_candidate`.
    - `src/memory/semantic_episode.py` contains `semantic_episode_candidate`.
    - `src/memory/semantic_episode.py` does not use `cross_case_pattern` in `_SUMMARY_KEYS`.
    - The automated pytest command exits 0.
  </acceptance_criteria>
  <done>Semantic episode long-term projection is limited to needs-review preference candidates and cannot project patterns, similar cases, or strategy hints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source type -> long-term policy | Source provenance controls whether a candidate can publish, require review, or skip. |
| candidate -> repository insert | Service must not insert skipped or non-preference candidates. |
| semantic summary -> long-term candidate | Automatic observations can only create preference candidates for review. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-03 | T/E | `src/memory/policy.py` | mitigate | Replace broad durable long-term auto-approval with explicit preference published-source allowlist. |
| T-48-04 | T | `src/memory/long_term.py` | mitigate | Add pre-insert skip branches for non-preference memory kinds and disallowed source types. |
| T-48-05 | I | `src/memory/semantic_episode.py` | mitigate | Project only preference candidates; ignore pattern, similar-case, and strategy keys. |
| T-48-06 | R | `MemoryWriteEvent` emission | mitigate | Reuse existing skip event shape so disallowed candidates remain audited. |
</threat_model>

<verification>
Run the policy/projection gate:
```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_memory_policy.py tests/memory/test_long_term_memory_service.py tests/memory/test_semantic_episode_projection.py tests/memory/test_phase48_long_term_preference_alignment.py -q
```
</verification>

<success_criteria>
- Only `explicit_user_preference`, `explicit_admin_preference`, and `human_reviewed` auto-publish long-term memory.
- `semantic_episode_candidate` creates `needs_review` preference candidates only.
- Non-preference memory kinds and disallowed long-term source types skip before insertion and emit events.
- Case-memory source policy remains unchanged except for test expectations that must stay green.
</success_criteria>

<output>
After completion, create `.planning/phases/48-narrow-long-term-explicit-preference-memory/48-02-SUMMARY.md`.
</output>
