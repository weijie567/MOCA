---
phase: 55-memory-context-load-cutover
plan: "02"
type: execute
wave: 2
depends_on:
  - "55-01"
files_modified:
  - src/agent/graph.py
  - src/agent/routing.py
  - tests/architecture/graph_baseline.py
  - tests/architecture/test_canonical_graph_baseline.py
  - tests/agent/test_graph.py
  - tests/test_graph_routing.py
  - tests/agent/test_intent_routing.py
  - tests/memory/test_phase48_1_memory_compat_alignment.py
autonomous: true
requirements:
  - CAGM-06
user_setup: []
must_haves:
  truths:
    - "Active graph registers `memory_context_load`, not `long_term_memory_retrieve`."
    - "`route_after_slot_resolution` returns `memory_context_load` for canonical and legacy reviewed-memory hints after required slots are satisfied."
    - "Active graph maps `slot_resolution_gate -> memory_context_load -> investigate`."
    - "Active route maps and architecture baseline no longer include Phase 55-owned `long_term_memory_retrieve` as an active legacy node."
    - "Phase 56 `generate_recommendation` and Phase 57 `assess_risk_and_approval` active legacy rows remain unchanged."
    - "Storage identities, memory API paths, config names, and historical trace names are not destructively renamed."
  artifacts:
    - path: src/agent/graph.py
      provides: "active graph cutover to memory_context_load"
      contains: "memory_context_load"
    - path: src/agent/routing.py
      provides: "slot-resolution reviewed-memory route destination"
      contains: "memory_context_load"
    - path: tests/architecture/graph_baseline.py
      provides: "source-verified active graph baseline after Phase 55"
      contains: "memory_context_load"
    - path: tests/agent/test_graph.py
      provides: "compiled graph and smoke coverage for memory_context_load path"
      contains: "memory_context_load"
    - path: tests/memory/test_phase48_1_memory_compat_alignment.py
      provides: "compatibility guard updated to stop requiring active legacy graph destination"
      contains: "needs_reviewed_memory_context"
  key_links:
    - from: src/agent/graph.py
      to: src/agent/nodes/memory_context_load.py
      via: "builder.add_node('memory_context_load', memory_context_load)"
      pattern: "add_node\\(\"memory_context_load\""
    - from: src/agent/routing.py
      to: src/agent/graph.py
      via: "route_after_slot_resolution path map"
      pattern: "\"memory_context_load\""
    - from: tests/architecture/graph_baseline.py
      to: src/agent/graph.py
      via: "AST source baseline"
      pattern: "CURRENT_ACTIVE_GRAPH_NODES_BASELINE"
    - from: tests/agent/test_graph.py
      to: src/agent/nodes/memory_context_load.py
      via: "compiled graph smoke path"
      pattern: "memory_context_load"
---

<objective>
Cut active graph and router wiring from `long_term_memory_retrieve` to `memory_context_load`.

Purpose: Phase 54 intentionally left reviewed-memory-needed slot outcomes pointing to the Phase 55-owned compatibility destination. This plan performs the active runtime cutover while preserving later Phase 56/57 active legacy rows and all memory storage/API/config compatibility surfaces.
Output: active graph/router/baseline/tests route resolved slot state through `memory_context_load` before `investigate`.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-memory-context-load-cutover/55-CONTEXT.md
@.planning/phases/55-memory-context-load-cutover/55-RESEARCH.md
@.planning/phases/55-memory-context-load-cutover/55-PATTERNS.md
@.planning/phases/55-memory-context-load-cutover/55-VALIDATION.md
@.planning/phases/55-memory-context-load-cutover/55-01-SUMMARY.md
@.planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
@.planning/phases/54-slot-resolution-gate-cutover/54-VERIFICATION.md
@src/agent/graph.py
@src/agent/routing.py
@src/agent/nodes/memory_context_load.py
@tests/architecture/graph_baseline.py
@tests/architecture/test_canonical_graph_baseline.py
@tests/agent/test_graph.py
@tests/test_graph_routing.py
@tests/agent/test_intent_routing.py
@tests/memory/test_phase48_1_memory_compat_alignment.py

<interfaces>
Target active graph cutover:
```python
from src.agent.nodes.memory_context_load import memory_context_load

builder.add_node("memory_context_load", memory_context_load)
builder.add_conditional_edges(
    "slot_resolution_gate",
    route_after_slot_resolution,
    {
        "clarification_gate": "clarification_gate",
        "investigate": "investigate",
        "memory_context_load": "memory_context_load",
    },
)
builder.add_edge("memory_context_load", "investigate")
```

Target router route set:
```python
SLOT_RESOLUTION_ROUTES = {"clarification_gate", "investigate", "memory_context_load"}
```
</interfaces>
</context>

<source_coverage_audit>
| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Active graph order routes resolved slot state into `memory_context_load` before `investigate` | Tasks 1-2 update graph/router/baseline and smoke tests. |
| REQ | CAGM-06 | Covered in frontmatter and active graph/router tasks. |
| RESEARCH | Pitfall: active runtime still uses legacy node | Task 1 includes scoped AST/static scan rejecting active legacy registration/edge/route. |
| RESEARCH | Wave 0 gaps: baseline and Phase 48.1 compatibility guard | Tasks 1-2 update both. |
| CONTEXT | D-55-01, D-55-02, D-55-03, D-55-12, D-55-15 | Tasks 1-2 implement active cutover and preserve allowed compatibility. |
| CONTEXT | Deferred Phase 56/57/58 ideas | Static checks preserve Phase 56/57 rows and avoid Phase 58 cleanup. |
</source_coverage_audit>

<compatibility_table>
| Legacy surface | Canonical owner | Reason | Trace projection | Validation | Delete phase |
|----------------|-----------------|--------|------------------|------------|--------------|
| `src/agent/nodes/long_term_memory_retrieve.py` wrapper/import path | `memory_context_load` | Direct imports/tests may remain while active graph stops registering the legacy node | Plan 55-03 projects historical `long_term_memory_retrieve` traces to `memory_context_load`; active graph emits `memory_context_load` | Plan 55-02 static scan rejects active graph registration/route destination; Plan 55-03 vocabulary tests validate projection | Phase 58 |
| `needs_long_term_memory` routing hint | `needs_reviewed_memory_context` / `memory_context_load` | Phase 48.1 retained legacy hint alias for existing callers | Both hints route to `memory_context_load`; trace node is canonical after active graph cutover | `tests/agent/test_intent_routing.py`, `tests/test_graph_routing.py`, `tests/memory/test_phase48_1_memory_compat_alignment.py` | Phase 58 |
</compatibility_table>

<must_haves>
- Active graph registers `memory_context_load`.
- Slot-resolution reviewed-memory route returns `memory_context_load`.
- `memory_context_load` edges directly to `investigate`.
- Phase 55-owned active legacy baseline row is removed.
- Phase 56/57 active legacy rows remain unchanged.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atomically cut over graph, router, and architecture baseline</name>
  <files>src/agent/graph.py, src/agent/routing.py, tests/architecture/graph_baseline.py, tests/architecture/test_canonical_graph_baseline.py, tests/test_graph_routing.py, tests/agent/test_intent_routing.py</files>
  <read_first>
    - src/agent/graph.py
    - src/agent/routing.py
    - src/agent/nodes/memory_context_load.py
    - src/agent/nodes/long_term_memory_retrieve.py
    - tests/architecture/graph_baseline.py
    - tests/architecture/test_canonical_graph_baseline.py
    - tests/test_graph_routing.py
    - tests/agent/test_intent_routing.py
    - .planning/phases/55-memory-context-load-cutover/55-CONTEXT.md
    - .planning/phases/55-memory-context-load-cutover/55-VALIDATION.md
    - .planning/phases/54-slot-resolution-gate-cutover/54-VERIFICATION.md
    - .planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
  </read_first>
  <behavior>
    - `SLOT_RESOLUTION_ROUTES == {"clarification_gate", "investigate", "memory_context_load"}`.
    - `_slot_resolution_route_decision(...)` returns exact string `"memory_context_load"` when required slots are satisfied and routing hints include `needs_reviewed_memory_context`.
    - `_slot_resolution_route_decision(...)` also returns exact string `"memory_context_load"` for retained `needs_long_term_memory`.
    - Unknown intent, malformed required slots, missing required slots, LLM slot extraction errors, exceptions, and unregistered routes still fail closed to `clarification_gate`.
    - `src/agent/graph.py` imports `memory_context_load` from `src.agent.nodes.memory_context_load`.
    - Active graph registers `builder.add_node("memory_context_load", memory_context_load)`.
    - Active graph does not register `builder.add_node("long_term_memory_retrieve", ...)`.
    - Active graph path map from `slot_resolution_gate` maps `"memory_context_load": "memory_context_load"`.
    - Active graph has `builder.add_edge("memory_context_load", "investigate")`.
    - Active graph has no `builder.add_edge("long_term_memory_retrieve", "investigate")`.
    - `CURRENT_ACTIVE_GRAPH_NODES_BASELINE` includes `memory_context_load` and excludes `long_term_memory_retrieve`.
    - `MIGRATION_MODE_LEGACY_NODE_MAP` removes the Phase 55 `long_term_memory_retrieve` row and still contains only Phase 56 `generate_recommendation` and Phase 57 `assess_risk_and_approval`.
    - `generate_recommendation` and `assess_risk_and_approval` active graph registrations, route values, and baseline rows are untouched.
  </behavior>
  <action>
    Update `src/agent/routing.py`, `src/agent/graph.py`, and architecture baseline tests as one atomic logical patch per D-55-01, D-55-02, D-55-03, and D-55-15. In `routing.py`, set `SLOT_RESOLUTION_ROUTES` and `SLOT_ROUTES` to `{"clarification_gate", "investigate", "memory_context_load"}`. In `_slot_resolution_route_decision(...)`, return `([], "memory_context_load", [])` when `_needs_reviewed_memory_context(state)` is true; `_needs_reviewed_memory_context` must continue accepting both `routing_hints["needs_reviewed_memory_context"] is True` and retained alias `routing_hints["needs_long_term_memory"] is True`. Preserve all fail-closed paths to `clarification_gate`. In `src/agent/graph.py`, replace the active import `from src.agent.nodes.long_term_memory_retrieve import long_term_memory_retrieve` with `from src.agent.nodes.memory_context_load import memory_context_load`; replace `builder.add_node("long_term_memory_retrieve", long_term_memory_retrieve)` with `builder.add_node("memory_context_load", memory_context_load)`; change the `slot_resolution_gate` conditional edge map key/destination from `"long_term_memory_retrieve": "long_term_memory_retrieve"` to `"memory_context_load": "memory_context_load"`; replace `builder.add_edge("long_term_memory_retrieve", "investigate")` with `builder.add_edge("memory_context_load", "investigate")`. Update `tests/architecture/graph_baseline.py` so `CURRENT_ACTIVE_GRAPH_NODES_BASELINE` includes `memory_context_load`, excludes `long_term_memory_retrieve`, and `MIGRATION_MODE_LEGACY_NODE_MAP` no longer contains `long_term_memory_retrieve`. Keep the Phase 56 and Phase 57 rows exactly for `generate_recommendation` and `assess_risk_and_approval`. Update `tests/architecture/test_canonical_graph_baseline.py`, `tests/test_graph_routing.py`, and `tests/agent/test_intent_routing.py` to assert the new exact route values and active graph shape. Do not touch `src/agent/graph_vocabulary.py`, docs, `.planning/ARCHITECTURE-DEBT.md`, `55-VALIDATION.md`, or trace/API tests in this task; Plan 55-03 owns projection/docs/closeout.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/test_graph_routing.py tests/agent/test_intent_routing.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/graph.py src/agent/routing.py tests/architecture/graph_baseline.py tests/architecture/test_canonical_graph_baseline.py tests/test_graph_routing.py tests/agent/test_intent_routing.py</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run python -c "import ast,pathlib; graph=pathlib.Path('src/agent/graph.py').read_text(); routing=pathlib.Path('src/agent/routing.py').read_text(); tree=ast.parse(graph); nodes=set(); edges=set(); cond={};\nfor n in ast.walk(tree):\n    if isinstance(n, ast.Call) and isinstance(n.func, ast.Attribute) and n.func.attr=='add_node': nodes.add(n.args[0].value)\n    if isinstance(n, ast.Call) and isinstance(n.func, ast.Attribute) and n.func.attr=='add_edge': edges.add((n.args[0].value, n.args[1].value))\n    if isinstance(n, ast.Call) and isinstance(n.func, ast.Attribute) and n.func.attr=='add_conditional_edges': cond[(n.args[0].value, n.args[1].id)]={k.value:v.value for k,v in zip(n.args[2].keys,n.args[2].values,strict=True)}\nassert 'memory_context_load' in nodes and 'long_term_memory_retrieve' not in nodes; assert ('memory_context_load','investigate') in edges and ('long_term_memory_retrieve','investigate') not in edges; assert cond[('slot_resolution_gate','route_after_slot_resolution')]['memory_context_load']=='memory_context_load'; forbidden=['add_node(\"long_term_memory_retrieve\"','add_edge(\"long_term_memory_retrieve\"','\"long_term_memory_retrieve\": \"long_term_memory_retrieve\"','return \"long_term_memory_retrieve\"']; assert not any(p in graph or p in routing for p in forbidden); print('55-02 active memory graph cutover OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/agent/routing.py` contains `SLOT_RESOLUTION_ROUTES = {"clarification_gate", "investigate", "memory_context_load"}`.
    - `src/agent/routing.py` contains `return [], "memory_context_load", []` for reviewed-memory-needed slot outcomes.
    - `src/agent/graph.py` contains `builder.add_node("memory_context_load", memory_context_load)`.
    - `src/agent/graph.py` contains `builder.add_edge("memory_context_load", "investigate")`.
    - `src/agent/graph.py` contains no active `builder.add_node("long_term_memory_retrieve", ...)`.
    - `tests/architecture/graph_baseline.py` active node baseline includes `memory_context_load` and excludes `long_term_memory_retrieve`.
    - `tests/architecture/graph_baseline.py` migration map does not include `long_term_memory_retrieve`.
    - Phase 56/57 migration rows for `generate_recommendation` and `assess_risk_and_approval` remain present.
  </acceptance_criteria>
  <done>Active graph, router, and baseline are cut over to `memory_context_load` without altering later Phase 56/57 rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update graph smoke and compatibility guard tests</name>
  <files>tests/agent/test_graph.py, tests/test_graph_routing.py, tests/agent/test_intent_routing.py, tests/memory/test_phase48_1_memory_compat_alignment.py</files>
  <read_first>
    - tests/agent/test_graph.py
    - tests/test_graph_routing.py
    - tests/agent/test_intent_routing.py
    - tests/memory/test_phase48_1_memory_compat_alignment.py
    - src/agent/graph.py
    - src/agent/routing.py
    - src/agent/nodes/memory_context_load.py
    - .planning/phases/55-memory-context-load-cutover/55-VALIDATION.md
    - .planning/phases/48.1-memory-context-compatibility-debt-cleanup/48.1-RESEARCH.md
  </read_first>
  <behavior>
    - Compiled graph smoke tests reach `memory_context_load` before `investigate` when reviewed-memory hints are present after slot resolution.
    - Graph smoke tests patch or fake `memory_context_load` instead of `long_term_memory_retrieve`.
    - `route_after_slot_resolution` totality tests include `memory_context_load`.
    - Existing canonical hint `needs_reviewed_memory_context` and retained legacy hint `needs_long_term_memory` both route to `memory_context_load`.
    - Phase 48.1 compatibility guard no longer requires active graph route destination `long_term_memory_retrieve`.
    - Phase 48.1 compatibility guard continues to prohibit destructive deletion/rename of storage identities, public memory API paths, config names, and allowed wrapper/import surfaces.
  </behavior>
  <action>
    Update focused graph smoke tests and compatibility guards after Task 1. In `tests/agent/test_graph.py`, replace active-memory-route monkeypatches/import aliases that target `long_term_memory_retrieve` with `memory_context_load`. Add or update one smoke assertion proving the reviewed-memory-needed path order is `slot_resolution_gate` before `memory_context_load` before `investigate`. Adjust expected `llm_outputs` from active graph runs to read `llm_outputs["memory_context_load"]`; do not require active graph runs to write `llm_outputs["long_term_memory_retrieve"]`. In `tests/test_graph_routing.py` and `tests/agent/test_intent_routing.py`, update exact expected route destinations for both `needs_reviewed_memory_context` and `needs_long_term_memory` to `memory_context_load`. In `tests/memory/test_phase48_1_memory_compat_alignment.py`, replace any assertion that active routing must return `"long_term_memory_retrieve"` with an assertion that both hint names remain present and route to `"memory_context_load"`. Keep the test's storage/API/config compatibility guards: it must still reject destructive deletion or rename of `session_memories`, `long_term_memories`, `case_memories`, `case_working_contexts`, public memory API paths, session config names, and allowed wrapper/import compatibility surfaces. Do not update vocabulary, docs, or validation status in this task.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/agent/test_graph.py tests/test_graph_routing.py tests/agent/test_intent_routing.py tests/memory/test_phase48_1_memory_compat_alignment.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check tests/agent/test_graph.py tests/test_graph_routing.py tests/agent/test_intent_routing.py tests/memory/test_phase48_1_memory_compat_alignment.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/agent/test_graph.py` contains `memory_context_load` active-path assertions.
    - `tests/agent/test_graph.py` does not require active graph traversal through `long_term_memory_retrieve`.
    - Router tests assert `needs_reviewed_memory_context` routes to `memory_context_load`.
    - Router tests assert retained `needs_long_term_memory` routes to `memory_context_load`.
    - `tests/memory/test_phase48_1_memory_compat_alignment.py` no longer asserts `return "long_term_memory_retrieve"` in active routing source.
    - Compatibility guard tests still protect memory storage/API/config names from destructive rename/drop.
  </acceptance_criteria>
  <done>Graph smoke and compatibility guard coverage prove active `memory_context_load` routing while preserving non-destructive memory compatibility boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| slot_resolution_gate -> route_after_slot_resolution | Required-slot resolution result controls whether memory context loading runs. |
| route_after_slot_resolution -> StateGraph path map | Router return values must match registered node keys exactly. |
| memory_context_load -> investigate | Contextual memory crosses into investigation only as hints/context. |
| compatibility guard -> storage/API/config surfaces | Active graph cutover must not destructively rename memory persistence/API/config identities. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-55-02-01 | Tampering | HIGH | `src/agent/routing.py`, `src/agent/graph.py` | mitigate | Update route set, return value, path map, registered node, and direct edge together; run AST static cutover check. |
| T-55-02-02 | Repudiation | HIGH | active graph node identity | mitigate | Architecture baseline removes active `long_term_memory_retrieve` row and tests source graph facts. |
| T-55-02-03 | Elevation of Privilege | HIGH | memory -> investigate boundary | mitigate | Plan 55-01 labels memory contextual-only; Plan 55-02 only changes routing identity and leaves authority tests green. |
| T-55-02-04 | Information Disclosure | HIGH | cross-merchant memory route | mitigate | Preserve reviewed-memory service trusted-scope filtering; do not alter memory service/query scope. |
| T-55-02-05 | Scope Creep / Tampering | MEDIUM | Phase 56/57 active legacy rows | mitigate | Static/baseline acceptance requires `generate_recommendation` and `assess_risk_and_approval` rows remain unchanged. |
| T-55-02-06 | Denial of Service | MEDIUM | router fail-closed behavior | mitigate | Preserve exception and malformed-state fallback to `clarification_gate`; router totality tests run. |
</threat_model>

<verification>
Plan 55-02 is complete when graph/router/baseline tests, graph smoke tests, Phase 48.1 compatibility guard tests, Ruff, and the scoped active-legacy static scan all pass through approved `uv` entrypoints.
</verification>

<success_criteria>
- Active graph registers `memory_context_load` and no active `long_term_memory_retrieve`.
- `route_after_slot_resolution` returns only registered route keys and uses `memory_context_load` for reviewed-memory-needed routes.
- `memory_context_load` edges to `investigate`.
- Architecture baseline removes only the Phase 55-owned active legacy row.
- Later Phase 56/57 active legacy rows remain intact.
- No memory storage/API/config destructive rename is planned or implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/55-memory-context-load-cutover/55-02-SUMMARY.md`.
</output>
