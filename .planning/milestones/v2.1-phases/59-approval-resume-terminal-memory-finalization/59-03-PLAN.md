---
phase: 59-approval-resume-terminal-memory-finalization
plan: 03
type: execute
wave: 3
depends_on:
  - 59-02
files_modified:
  - tests/test_approval_api.py
  - tests/agent/test_memory_write_node.py
  - .planning/phases/59-approval-resume-terminal-memory-finalization/59-VALIDATION.md
  - .planning/ARCHITECTURE-DEBT.md
  - .planning/LOCAL-VALIDATION-ISSUES.md
autonomous: true
requirements:
  - MEM-01
  - MEM-02
  - MEM-03
  - CAGM-08
  - CAGM-09
requirements_addressed:
  - MEM-01
  - MEM-02
  - MEM-03
  - CAGM-08
  - CAGM-09
user_setup: []
must_haves:
  truths:
    - "Regression tests prove approval-resume completed runs persist one assistant message, one thread summary, one successful terminal memory write, one durable session-memory `MemoryWriteEvent`, one terminal memory finalizer trace step, and CWC finalizer metrics."
    - "Regression tests prove approval-resume interrupted-again paths persist no terminal assistant-message, summary, memory event, or finalizer trace row."
    - "Retry/idempotency tests prove partial terminal finalization does not duplicate finalizer surfaces and completed-run reconciliation does not rerun graph/action side effects."
    - "Final verification uses only MOCA-approved `UV_CACHE_DIR=/tmp/uv-cache uv run pytest ...` commands."
  artifacts:
    - path: "tests/test_approval_api.py"
      provides: "Approval completed/interrupted/retry terminal finalizer regression coverage"
    - path: "tests/agent/test_memory_write_node.py"
      provides: "Guard that direct approval-marked memory_write states still skip"
    - path: ".planning/phases/59-approval-resume-terminal-memory-finalization/59-VALIDATION.md"
      provides: "Phase validation sign-off after planned tests pass"
    - path: ".planning/ARCHITECTURE-DEBT.md"
      provides: "Mandatory Chinese Memory-section Phase 59 ledger entry for the approval-resume terminal memory lifecycle architecture fix"
    - path: ".planning/LOCAL-VALIDATION-ISSUES.md"
      provides: "Conditional local validation incident log if debug or test failures occurred"
  key_links:
    - from: "tests/test_approval_api.py"
      to: "src/api/routers/approvals.py"
      via: "approval decision endpoint and fake resume graph"
      pattern: "/api/v1/approvals"
    - from: "tests/test_approval_api.py"
      to: "src/api/services/agent_run_memory.py"
      via: "DB assertions for finalizer surfaces"
      pattern: "agent_run_memory_finalize"
---

<objective>
Lock Phase 59 with focused regressions, a mandatory memory architecture-debt ledger entry, conditional local validation incident logging, and final verification.

Purpose: The source fix is not complete until tests prove completed approval resumes finalize memory surfaces, non-completed resumes skip them, retries are idempotent, and canonical approval/graph boundaries stay intact.
Output: Approval/memory regression tests, validation artifact update, mandatory architecture-debt entry, conditional local-validation issue entry, and final command evidence.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v2.1-MILESTONE-AUDIT.md
@.planning/phases/59-approval-resume-terminal-memory-finalization/59-RESEARCH.md
@.planning/phases/59-approval-resume-terminal-memory-finalization/59-VALIDATION.md
@.planning/phases/59-approval-resume-terminal-memory-finalization/59-PATTERNS.md
@AGENTS.md
@src/api/routers/approvals.py
@src/api/services/agent_run_memory.py
@tests/test_approval_api.py
@tests/test_agent_runs_api.py
@tests/agent/test_memory_write_node.py
@tests/architecture/test_canonical_graph_baseline.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add approval-resume terminal finalizer regressions</name>
  <read_first>
    - tests/test_approval_api.py
    - tests/test_agent_runs_api.py
    - src/api/routers/approvals.py
    - src/api/services/agent_run_memory.py
    - .planning/phases/59-approval-resume-terminal-memory-finalization/59-PATTERNS.md
  </read_first>
  <files>tests/test_approval_api.py</files>
  <behavior>
    - Completed approval resume with approval markers and CWC-eligible state persists exactly one assistant `ConversationMessage` with metadata source `agent_runs.finalizer`.
    - The same completed path persists one thread `ConversationSummary`, one durable session-memory `MemoryWriteEvent`, one `AgentStep` whose `node_name == "agent_run_memory_finalize"`, `metrics_json["memory_write_status"] == "completed"`, and CWC metrics including `case_working_context_status`.
    - Interrupted-again approval resume persists no assistant message, no thread summary, no `MemoryWriteEvent`, and no finalizer `AgentStep`.
    - Retry after a failure between terminal finalization and final `approval_resumed/completed` event records the missing completed resume event without duplicating assistant message, summary, session-memory event, finalizer step, graph call side effects, or action draft.
  </behavior>
  <action>
    In `tests/test_approval_api.py`, extend imports to include any missing SQLAlchemy aggregate helper and DB models needed for assertions: `func`, `ActionDraft`, `AgentStep`, `ConversationMessage`, `ConversationSummary`, and `MemoryWriteEvent`. Add local helpers equivalent to `tests/test_agent_runs_api.py` patterns: `_count_rows(session, model, *filters) -> int` and, if useful, `_messages_for_run(...)`. Extend `FakeResumeGraph` to accept an optional `extra_state: dict | None` and merge that into the returned final state after the default `final_response` and `trace_steps`. Add `test_approval_resume_completed_runs_terminal_memory_finalizer` using `_create_approval(...)`, a fake graph final state containing `approval_result`, `risk_assessment={"approval_required": True}`, and CWC-eligible `active_slots` / business refs from `seeded_session`; assert one assistant message for `bundle.approval.run_id`, message `user_id == run.user_id`, metadata `source == "agent_runs.finalizer"`, one thread summary for the run thread, one finalizer `AgentStep`, `metrics_json["memory_write_status"] == "completed"`, `metrics_json["memory_write_reason_code"] != "not_completed_path"`, `metrics_json["case_working_context_status"]` exists, and exactly one durable `MemoryWriteEvent` for the approval-resume run with `memory_type == "session_slot"` and `decision == "write"`. Extend `test_decide_edit_rebinds_replacement_approval_from_resume_interrupt` or add a separate interrupted-again test to assert zero assistant messages, summaries, memory events, and finalizer steps for the interrupted run. Add a retry/dedupe test that monkeypatches `approvals_router._record_resume_event` to raise once only for `resume_status == "completed"` after terminal finalizer surfaces can be created, retries the same decision, and asserts counts remain one for assistant message, summary, session-memory `MemoryWriteEvent`, finalizer step, completed resume event, and action draft while `len(graph.calls) == 1` across first failure plus retry because the retry takes the completed-run reconciliation branch instead of rerunning graph/action side effects.
  </action>
  <acceptance_criteria>
    - `rg -n "test_approval_resume_completed_runs_terminal_memory_finalizer|agent_run_memory_finalize|agent_runs.finalizer" tests/test_approval_api.py` shows the completed-path regression.
    - `rg -n "memory_write_status.*completed|not_completed_path|case_working_context_status|memory_write_reason_code" tests/test_approval_api.py` shows assertions that terminal memory write completed successfully and approval markers did not cause a skip.
    - `rg -n "MemoryWriteEvent|memory_type == \"session_slot\"|decision == \"write\"" tests/test_approval_api.py` shows durable session-memory write evidence for the approval-resume run.
    - `rg -n "ConversationMessage|ConversationSummary|MemoryWriteEvent|AgentStep|ActionDraft" tests/test_approval_api.py` shows concrete DB surface assertions.
    - `rg -n "resume_status == \"completed\"|simulated.*completed|_record_resume_event" tests/test_approval_api.py` shows the retry/dedupe failure injection is after terminal finalizer surfaces, not before graph resume.
    - `rg -n "len\\(graph.calls\\).*== 1|graph.calls.*== 1" tests/test_approval_api.py` shows completed-run retry reconciliation does not rerun graph resume.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_approval_api.py -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_approval_api.py -q</automated>
  </verify>
  <done>Approval API tests directly cover completed terminal finalization, interrupted skip behavior, and retry idempotency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard direct memory_write approval skips and canonical graph boundaries</name>
  <read_first>
    - tests/agent/test_memory_write_node.py
    - src/agent/nodes/memory_write.py
    - tests/architecture/test_canonical_graph_baseline.py
    - tests/architecture/test_phase34_approval_action_boundaries.py
    - tests/test_agent_runs_api.py
    - .planning/phases/59-approval-resume-terminal-memory-finalization/59-VALIDATION.md
  </read_first>
  <files>tests/agent/test_memory_write_node.py</files>
  <behavior>
    - Direct calls to `memory_write(...)` with approval markers still skip with `reason_code == "not_completed_path"`.
    - Normal terminal finalizer tests still pass after the shared helper move.
    - Canonical graph architecture tests still prove final 15-node vocabulary and no active legacy risk aliases.
  </behavior>
  <action>
    In `tests/agent/test_memory_write_node.py`, add `test_memory_write_node_skips_approval_marked_states` or equivalent. Use the existing `_state(...)` helper and call `memory_write(...)` three times: once with `approval_result={"status": "approved"}`, once with `approval_required=True`, and once with `risk_assessment={"approval_required": True}`. Assert each result has `memory_write_result["status"] == "skipped"` and `memory_write_result["reason_code"] == "not_completed_path"`. Do not add a terminal-finalizer bypass flag to `memory_write(...)`; Phase 59 uses the service sanitizer from Plan 59-01. Run focused normal finalizer and architecture commands to prove CWC/memory and canonical graph boundaries remain intact.
  </action>
  <acceptance_criteria>
    - `rg -n "test_memory_write_node_skips_approval_marked_states|approval_result|approval_required|risk_assessment" tests/agent/test_memory_write_node.py` shows all three direct skip cases.
    - `rg -n "terminal.*finalizer|finalizer.*mode|bypass" src/agent/nodes/memory_write.py tests/agent/test_memory_write_node.py` returns no new terminal bypass flag in the memory node.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_memory_write_node.py -q` passes.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/architecture/test_phase34_approval_action_boundaries.py -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_memory_write_node.py tests/test_agent_runs_api.py::test_completed_agent_run_finalizer_writes_case_working_context tests/test_agent_runs_api.py::test_duplicate_sse_stream_does_not_duplicate_memory_surfaces -q</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/architecture/test_phase34_approval_action_boundaries.py -q</automated>
  </verify>
  <done>Direct `memory_write(...)` approval skip behavior remains locked, and normal finalizer plus canonical graph regressions pass.</done>
</task>

<task type="auto">
  <name>Task 3: Final validation artifact, mandatory architecture-debt entry, and conditional local issue ledger</name>
  <read_first>
    - .planning/phases/59-approval-resume-terminal-memory-finalization/59-VALIDATION.md
    - .planning/ARCHITECTURE-DEBT.md
    - .planning/LOCAL-VALIDATION-ISSUES.md
    - AGENTS.md
    - CLAUDE.md
    - src/api/services/agent_run_memory.py
    - src/api/routers/approvals.py
  </read_first>
  <files>.planning/phases/59-approval-resume-terminal-memory-finalization/59-VALIDATION.md, .planning/ARCHITECTURE-DEBT.md, .planning/LOCAL-VALIDATION-ISSUES.md</files>
  <action>
    Run the full Phase 59 verification command set with the approved MOCA entrypoint only. After all planned tests pass, update `.planning/phases/59-approval-resume-terminal-memory-finalization/59-VALIDATION.md`: set `status: complete`, `nyquist_compliant: true`, and `wave_0_complete: true` in frontmatter; mark each Per-Task Verification Map row `passed`; mark Validation Sign-Off checklist items complete; record the exact commands run under a final evidence section. Append a mandatory Chinese entry to `.planning/ARCHITECTURE-DEBT.md` under the Memory section for Phase 59 because this phase fixes memory lifecycle architecture debt in approval-resume terminal finalization. The entry must include problem/root cause, impact, status, evidence, verification commands, remaining risk, and explicit references to `src/api/services/agent_run_memory.py`, `src/api/routers/approvals.py`, and the Phase 59 regression tests. If any local validation/debug incident occurred while running tests or verifying the API/memory path, append a Chinese entry to `.planning/LOCAL-VALIDATION-ISSUES.md` with symptom, reproduction, evidence command, root cause, handling, remaining issue, and next entry point. If no local validation incident occurred, leave `.planning/LOCAL-VALIDATION-ISSUES.md` unchanged and state that explicitly in the plan summary.
  </action>
  <acceptance_criteria>
    - `rg -n "nyquist_compliant: true|wave_0_complete: true|status: complete" .planning/phases/59-approval-resume-terminal-memory-finalization/59-VALIDATION.md` shows validation sign-off after tests pass.
    - `rg -n "UV_CACHE_DIR=/tmp/uv-cache uv run pytest" .planning/phases/59-approval-resume-terminal-memory-finalization/59-VALIDATION.md` shows the exact final test commands.
    - `rg -n '(^|;|&&)[[:space:]]*(pytest|python -m pytest)\\b' .planning/phases/59-approval-resume-terminal-memory-finalization/59-VALIDATION.md .planning/ARCHITECTURE-DEBT.md .planning/LOCAL-VALIDATION-ISSUES.md` does not show new bare test commands; any historical bare command text must be labeled invalid or unrelated prose.
    - `.planning/ARCHITECTURE-DEBT.md` has a new Chinese Memory-section Phase 59 entry that includes problem/root cause, impact, status, evidence, verification commands, and remaining risk.
    - If `.planning/LOCAL-VALIDATION-ISSUES.md` changed, the new entry is in Chinese and includes reproduction command, root cause, handling, and next entry point.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_approval_api.py tests/test_agent_runs_api.py tests/agent/test_memory_write_node.py tests/agent/test_case_working_context_lifecycle.py tests/memory/test_thread_summary.py tests/architecture/test_canonical_graph_baseline.py tests/architecture/test_phase34_approval_action_boundaries.py -q</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/api/services/agent_run_memory.py src/api/routers/agent_runs.py src/api/routers/approvals.py tests/test_approval_api.py tests/agent/test_memory_write_node.py</automated>
  </verify>
  <done>Phase 59 validation artifact reflects passing automated verification, the Memory architecture-debt ledger records the Phase 59 lifecycle fix, and local validation incidents are logged only if they occurred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fake graph -> approval route | Fake final state must include approval markers and CWC-eligible data to prove the real gap is closed. |
| Retry failure injection -> persistence layer | Simulated failures after terminal surfaces must prove retry idempotency without corrupting approval events or action drafts. |
| Validation output -> archive evidence | Only approved test commands can support Phase 59 validation and milestone archive evidence. |
| Planning ledgers -> project memory/debt record | The mandatory architecture-debt entry must be based on the verified Phase 59 lifecycle fix; local validation entries must be based on actual validation incidents, not speculation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-03-01 | Tampering | approval completed regression | mitigate | Assert concrete DB rows and metrics for assistant message, summary, successful session-memory `MemoryWriteEvent`, finalizer step, memory status, and CWC status. |
| T-59-03-02 | Denial of service | retry/dedupe regression | mitigate | Inject post-finalizer completed-event failure and assert retry records completion without duplicating finalizer surfaces or rerunning graph/action side effects. |
| T-59-03-03 | Elevation of privilege | direct `memory_write(...)` approval markers | mitigate | Add unit coverage that approval-marked direct memory states still skip. |
| T-59-03-04 | Repudiation | validation artifact | mitigate | Record exact `UV_CACHE_DIR=/tmp/uv-cache uv run pytest ...` commands and final statuses. |
| T-59-03-05 | Information disclosure | architecture/local ledgers | mitigate | Ledger entries summarize verified issues and commands, not secrets or raw sensitive payloads. |
</threat_model>

<verification>
Final Phase 59 command set:
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_approval_api.py -q`
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_agent_runs_api.py::test_completed_agent_run_persists_exactly_one_assistant_message tests/test_agent_runs_api.py::test_completed_agent_run_finalizer_writes_case_working_context tests/test_agent_runs_api.py::test_completed_agent_run_finalizer_skips_non_completed_status tests/test_agent_runs_api.py::test_duplicate_sse_stream_does_not_duplicate_memory_surfaces -q`
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_memory_write_node.py tests/agent/test_case_working_context_lifecycle.py tests/memory/test_thread_summary.py -q`
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/architecture/test_phase34_approval_action_boundaries.py -q`
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_approval_api.py tests/test_agent_runs_api.py tests/agent/test_memory_write_node.py tests/agent/test_case_working_context_lifecycle.py tests/memory/test_thread_summary.py tests/architecture/test_canonical_graph_baseline.py tests/architecture/test_phase34_approval_action_boundaries.py -q`
- `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/api/services/agent_run_memory.py src/api/routers/agent_runs.py src/api/routers/approvals.py tests/test_approval_api.py tests/agent/test_memory_write_node.py`
</verification>

<source_audit>
GOAL covered: regression and validation evidence proves approval-resume completed runs use the normal terminal finalizer lifecycle and non-completed paths skip it.
REQ covered: MEM-01, MEM-02, MEM-03, CAGM-08, and CAGM-09 all receive concrete test coverage and final validation.
RESEARCH covered: completed approval finalizer, interrupted skip, retry idempotency, requester identity, direct memory approval skip, CWC metrics, and canonical graph guard commands.
CONTEXT covered: no Phase 59 CONTEXT.md exists.
Deferred ideas excluded: no milestone archive evidence work from Phase 60 is implemented here.
</source_audit>

<success_criteria>
- Approval API regressions prove completed, interrupted-again, and retry/dedupe terminal finalizer behavior.
- Direct memory-write approval skip behavior remains protected.
- Phase 59 validation is signed off using approved uv commands, with a mandatory Memory architecture-debt entry for the lifecycle fix and conditional local-validation incident logging only for actual incidents.
</success_criteria>

<output>
After completion, create `.planning/phases/59-approval-resume-terminal-memory-finalization/59-03-SUMMARY.md`.
</output>
