---
phase: 51-canonical-graph-baseline-guardrails-and-migration-matrix
plan: "02"
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - tests/architecture/test_canonical_graph_baseline.py
autonomous: true
requirements: [CAGM-02]
requirements_addressed: [CAGM-02]
must_haves:
  truths:
    - "The current active graph node set is source-verified against the Phase 51 baseline."
    - "The target canonical graph set is exact and excludes helper/lifecycle names."
    - "Migration-mode tests fail if any active legacy graph node lacks an explicit target mapping."
    - "Router route maps are source-verified, including canonical route labels that currently point to legacy destinations."
    - "Forbidden registered-node drift is rejected, while the final exact no-debt gate remains Phase-58-scoped."
  artifacts:
    - path: "tests/architecture/test_canonical_graph_baseline.py"
      provides: "CAGM-02 architecture guardrail tests"
      contains: "test_current_active_graph_node_set_matches_phase51_baseline"
  key_links:
    - from: "tests/architecture/test_canonical_graph_baseline.py"
      to: "tests/architecture/graph_baseline.py"
      via: "imported helper constants and AST parser functions"
      pattern: "graph_add_node_names"
    - from: "tests/architecture/test_canonical_graph_baseline.py"
      to: "src/agent/graph.py"
      via: "source-parsed active node and conditional-edge checks"
      pattern: "CURRENT_CONDITIONAL_EDGE_BASELINE"
---

<objective>
Add the Phase 51 architecture guardrail suite for the current graph, target graph, migration-mode mapping, router maps, forbidden-node drift, and Phase 58 final no-debt gate.

Purpose: CAGM-02 needs machine-checkable proof before runtime graph rewiring starts.
Output: `tests/architecture/test_canonical_graph_baseline.py`.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
@.planning/phases/51-canonical-graph-baseline-guardrails-and-migration-matrix/51-CONTEXT.md
@.planning/phases/51-canonical-graph-baseline-guardrails-and-migration-matrix/51-RESEARCH.md
@.planning/phases/51-canonical-graph-baseline-guardrails-and-migration-matrix/51-PATTERNS.md
@.planning/phases/51-canonical-graph-baseline-guardrails-and-migration-matrix/51-VALIDATION.md
@tests/architecture/graph_baseline.py
@src/agent/graph.py
@src/agent/routing.py
@src/agent/graph_vocabulary.py
@tests/architecture/test_phase32_static_contract.py
@tests/architecture/test_phase34_approval_action_boundaries.py
@tests/architecture/test_action_draft_boundaries.py
@tests/architecture/test_phase35_replay_eval_boundaries.py

<decision_traceability>
D-51-01: Phase 51 is guardrail/test/docs only; no runtime graph rewiring or runtime node creation.
D-51-02: The target graph constant is exactly the 15 nodes listed in Phase 51 CONTEXT and Phase 50 SPEC.
D-51-03: Migration mode permits the current mixed graph only when every active legacy node maps to a canonical target and named deletion phase.
D-51-04: `slot_extraction`, `normalize_input`, `memory_write`, `trace_close`, and `action_execution` are forbidden registered main-chain nodes.
D-51-05: Tests must use static/source inspection with MOCA-approved `uv run pytest ...` commands.
D-51-06: Documentation/ledger closeout must state runtime graph remains legacy/canonical mixed until later phases.
</decision_traceability>

<interfaces>
Use the helper interface created by Plan 51-01:

```python
from tests.architecture.graph_baseline import (
    CURRENT_ACTIVE_GRAPH_NODES_BASELINE,
    CURRENT_CONDITIONAL_EDGE_BASELINE,
    FORBIDDEN_MAIN_CHAIN_REGISTERED_NODES,
    MIGRATION_MODE_LEGACY_NODE_MAP,
    TARGET_CANONICAL_GRAPH_NODES,
    graph_add_node_names,
    graph_conditional_edge_mappings,
)
```
</interfaces>

<source_audit>
SOURCE | ID | Requirement / Decision | Plan | Status
--- | --- | --- | --- | ---
GOAL | Phase 51 | Source-verified graph guardrails before runtime rewiring | 51-01, 51-02, 51-03 | COVERED
REQ | CAGM-02 | Current graph node set, router route values, legacy-to-target mapping, and no-`slot_extraction` rule are testable | 51-01, 51-02, 51-03 | COVERED
RESEARCH | R-01 | AST/source inspection avoids live LLM/provider/DB setup | 51-01, 51-02 | COVERED
RESEARCH | R-02 | Migration matrix comes from parsed active graph plus explicit Phase 50 mapping, not vocabulary alone | 51-01, 51-02 | COVERED
RESEARCH | R-03 | Final exact no-debt gate exists but is not failing before Phase 58 | 51-02, 51-03 | COVERED
CONTEXT | D-51-01 | No runtime graph behavior changes | 51-01, 51-02, 51-03 | COVERED
CONTEXT | D-51-02 | Exact 15-node target set | 51-01, 51-02 | COVERED
CONTEXT | D-51-03 | Migration-mode matrix and Phase 58 final boundary | 51-01, 51-02, 51-03 | COVERED
CONTEXT | D-51-04 | Forbidden helper/lifecycle names rejected as registered nodes | 51-01, 51-02 | COVERED
CONTEXT | D-51-05 | Approved validation entrypoints only | 51-01, 51-02, 51-03 | COVERED
CONTEXT | D-51-06 | Mixed runtime graph status recorded in closeout docs | 51-03 | COVERED
</source_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test current graph, target graph, and migration-mode mapping</name>
  <read_first>
    - tests/architecture/graph_baseline.py from Plan 51-01
    - .planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md current-to-target matrix
    - src/agent/graph.py active `builder.add_node(...)` registrations
    - src/agent/graph_vocabulary.py vocabulary entries and known `generate_recommendation` gap
    - tests/architecture/test_phase32_static_contract.py vocabulary assertion pattern
  </read_first>
  <files>tests/architecture/test_canonical_graph_baseline.py</files>
  <behavior>
    - `test_current_active_graph_node_set_matches_phase51_baseline`: parsed `graph_add_node_names()` equals `CURRENT_ACTIVE_GRAPH_NODES_BASELINE`.
    - `test_target_canonical_graph_node_set_is_exact_phase50_contract`: `TARGET_CANONICAL_GRAPH_NODES` equals the exact 15-node contract and is disjoint from forbidden main-chain names.
    - `test_migration_mode_maps_every_active_legacy_node_to_target`: every active non-target node in the current baseline is present in `MIGRATION_MODE_LEGACY_NODE_MAP`, every target is in `TARGET_CANONICAL_GRAPH_NODES`, and every row has a `delete_phase` matching Phase 53, 54, 55, 56, or 57.
    - `test_migration_mode_matrix_does_not_depend_on_graph_vocabulary_only`: assert `MIGRATION_MODE_LEGACY_NODE_MAP["generate_recommendation"]["target"] == "recommendation_generation"` independently of `graph_vocabulary.py`. The test may probe `graph_vocabulary.graph_vocabulary_entry("generate_recommendation", kind="node")` as a current-fact observation, but must not require it to be `None`; if a future non-runtime vocabulary alias exists, the test should only require that its target is also `recommendation_generation`.
  </behavior>
  <action>
    Create `tests/architecture/test_canonical_graph_baseline.py` and import `pytest`, the Plan 51-01 helper interface, and `src.agent.graph_vocabulary` only if the implementation uses it as the optional current-fact probe described below.

    Implement the four tests above. Keep current implementation facts separate from target graph facts per D-51-01 and D-51-02. Do not ask the executor to add target runtime nodes or update `src/agent/graph.py`.

    The migration-mode test must compute active legacy nodes as `CURRENT_ACTIVE_GRAPH_NODES_BASELINE - TARGET_CANONICAL_GRAPH_NODES`, not by reading `graph_vocabulary.py`. This makes `generate_recommendation -> recommendation_generation` mandatory regardless of current or future vocabulary coverage. Do not assert that `graph_vocabulary_entry("generate_recommendation", kind="node") is None`; vocabulary absence is only a source-confirmed landmine, not a permanent guardrail.
  </action>
  <verify>
    <automated>uv run pytest tests/architecture/test_canonical_graph_baseline.py::test_current_active_graph_node_set_matches_phase51_baseline tests/architecture/test_canonical_graph_baseline.py::test_target_canonical_graph_node_set_is_exact_phase50_contract tests/architecture/test_canonical_graph_baseline.py::test_migration_mode_maps_every_active_legacy_node_to_target tests/architecture/test_canonical_graph_baseline.py::test_migration_mode_matrix_does_not_depend_on_graph_vocabulary_only -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "test_current_active_graph_node_set_matches_phase51_baseline" tests/architecture/test_canonical_graph_baseline.py` matches.
    - `rg -n "test_target_canonical_graph_node_set_is_exact_phase50_contract" tests/architecture/test_canonical_graph_baseline.py` matches.
    - `rg -n "test_migration_mode_maps_every_active_legacy_node_to_target" tests/architecture/test_canonical_graph_baseline.py` matches.
    - `rg -n "test_migration_mode_matrix_does_not_depend_on_graph_vocabulary_only" tests/architecture/test_canonical_graph_baseline.py` matches.
    - The automated pytest command exits 0.
  </acceptance_criteria>
  <done>Current graph, target graph, and migration-mode mapping are source-verified by architecture tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test router mappings, forbidden drift, and Phase 58 no-debt gate marker</name>
  <read_first>
    - tests/architecture/graph_baseline.py conditional-edge baseline
    - src/agent/graph.py active conditional edge maps
    - src/agent/routing.py route constants and route return values
    - .planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md final no-debt gate
    - tests/architecture/test_action_draft_boundaries.py graph registration drift test pattern
    - tests/architecture/test_phase35_replay_eval_boundaries.py violation-list pattern
  </read_first>
  <files>tests/architecture/test_canonical_graph_baseline.py</files>
  <behavior>
    - `test_current_router_mappings_match_source_baseline`: parsed `graph_conditional_edge_mappings()` equals `CURRENT_CONDITIONAL_EDGE_BASELINE`.
    - `test_current_router_mappings_account_for_legacy_destinations`: route labels `recommendation_generation` currently map to `generate_recommendation`, and legacy destinations are covered by migration-mode mapping.
    - `test_forbidden_internal_or_lifecycle_names_are_not_registered_graph_nodes`: parsed active nodes are disjoint from `FORBIDDEN_MAIN_CHAIN_REGISTERED_NODES`.
    - `test_slot_extraction_drift_is_explicitly_rejected`: if `slot_extraction` appears in active registrations, failure text names `docs/contract-spec.md`, `docs/target-agent-platform-architecture-plan.md`, and Phase 50 SPEC as required review/update surfaces before promotion.
    - `test_final_no_debt_gate_is_marked_phase58_scope`: mark the exact final canonical equality gate as skipped with reason text naming Phase 58; the skipped body should show the final assertion `graph_add_node_names() == TARGET_CANONICAL_GRAPH_NODES`.
  </behavior>
  <action>
    Extend `tests/architecture/test_canonical_graph_baseline.py` with the five tests above.

    The router map test must verify the literal graph source map exactly, including:
    - `("investigate", "route_after_investigate")["recommendation_generation"] == "generate_recommendation"`
    - `("rag_context_build", "route_after_rag_context")["recommendation_generation"] == "generate_recommendation"`
    - `("claim_verify", "route_after_claim_verify")["assess_risk_and_approval"] == "assess_risk_and_approval"`
    - `("approval_gate", "route_after_approval")["assess_risk_and_approval"] == "assess_risk_and_approval"`

    The forbidden drift tests must operate only on parsed `builder.add_node(...)` registrations. Do not fail because `memory_write` exists as a vocabulary/runtime concept or module; D-51-04 forbids it only as a registered main-chain graph node in this phase.

    The Phase 58 final gate test must not fail the current build. Use `pytest.skip("Phase 58 cutover enforces exact canonical graph node set; Phase 51 records the gate.")` before the final equality assertion.
  </action>
  <verify>
    <automated>uv run pytest tests/architecture/test_canonical_graph_baseline.py -q</automated>
    <automated>uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/architecture/test_phase32_static_contract.py tests/architecture/test_phase34_approval_action_boundaries.py -q</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "test_current_router_mappings_match_source_baseline" tests/architecture/test_canonical_graph_baseline.py` matches.
    - `rg -n "test_current_router_mappings_account_for_legacy_destinations" tests/architecture/test_canonical_graph_baseline.py` matches.
    - `rg -n "test_forbidden_internal_or_lifecycle_names_are_not_registered_graph_nodes" tests/architecture/test_canonical_graph_baseline.py` matches.
    - `rg -n "test_slot_extraction_drift_is_explicitly_rejected" tests/architecture/test_canonical_graph_baseline.py` matches.
    - `rg -n "test_final_no_debt_gate_is_marked_phase58_scope" tests/architecture/test_canonical_graph_baseline.py` matches.
    - Focused pytest reports the final no-debt test as skipped, not failed.
    - All automated commands exit 0.
  </acceptance_criteria>
  <done>Router baselines, forbidden registered-node drift, and the Phase 58 exact no-debt marker are covered by architecture tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository source -> architecture tests | Tests parse `src/agent/graph.py` and compare against explicit baselines. |
| current graph facts -> target migration assertions | Tests separate current mixed runtime facts from target canonical contract facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-04 | Tampering | migration-mode tests | mitigate | Derive active legacy nodes from parsed current baseline and require every one to have explicit target/delete metadata. |
| T-51-05 | Elevation of Privilege | forbidden-node drift tests | mitigate | Fail if helper/lifecycle names become registered graph nodes without reviewed contract/spec updates. |
| T-51-06 | Denial of Service | architecture test scan scope | mitigate | Limit scans to `src/agent/graph.py`, helper constants, and focused architecture tests; avoid broad runtime imports. |
</threat_model>

<verification>
Run after this plan:

```bash
uv run pytest tests/architecture/test_canonical_graph_baseline.py -q
uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/architecture/test_phase32_static_contract.py tests/architecture/test_phase34_approval_action_boundaries.py -q
git diff --check
```
</verification>

<success_criteria>
- Focused Phase 51 architecture tests pass with the Phase 58 final no-debt gate skipped by explicit reason.
- Current active graph node set is tested from source and equals the Phase 51 baseline.
- Target canonical node set is tested as exactly 15 nodes.
- Every active legacy node has a target mapping, including `generate_recommendation -> recommendation_generation`.
- Router route maps are source-verified.
- Forbidden registered-node drift is rejected without treating non-graph vocabulary entries as failures.
- No runtime source file is modified by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/51-canonical-graph-baseline-guardrails-and-migration-matrix/51-02-SUMMARY.md`.
</output>
