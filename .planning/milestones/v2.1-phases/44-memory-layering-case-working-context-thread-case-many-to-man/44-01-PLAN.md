---
phase: 44-memory-layering-case-working-context-thread-case-many-to-man
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/models.py
  - src/db/migrations/versions/021_thread_case_links.py
  - src/db/migrations/versions/022_case_working_context.py
  - src/memory/policy.py
  - tests/db/test_phase44_schema.py
autonomous: true
requirements: [MEM-01, MEM-02]
must_haves:
  truths:
    - "A thread_case_links table exists binding conversation_threads to refund_cases.id (UUID)"
    - "A case_working_contexts table exists scoped by (tenant_id, case_id UUID) distinct from session/case/long_term memory tables"
    - "A case_working_context_revisions append-only history table exists"
    - "memory_write_events accepts a case_working_context memory_type value"
    - "alembic upgrade head applies cleanly with a single linear head"
  artifacts:
    - path: "src/db/migrations/versions/021_thread_case_links.py"
      provides: "thread_case_links DDL"
      contains: "thread_case_links"
    - path: "src/db/migrations/versions/022_case_working_context.py"
      provides: "case_working_contexts + revisions DDL + memory_write_events enum extension"
      contains: "case_working_contexts"
    - path: "src/db/models.py"
      provides: "ThreadCaseLink / CaseWorkingContext / CaseWorkingContextRevision ORM models"
      contains: "class CaseWorkingContext"
  key_links:
    - from: "022_case_working_context.py"
      to: "021_thread_case_links.py"
      via: "down_revision chain"
      pattern: "down_revision.*021_thread_case_links"
    - from: "src/db/models.py"
      to: "refund_cases.id"
      via: "case_id UUID ForeignKey"
      pattern: "ForeignKey\\(\"refund_cases.id\"\\)"
---

<objective>
Create the full Phase 44 DDL layer in a single migration-owning plan: the thread↔case
association table (MEM-02), the case_working_contexts + case_working_context_revisions
tables (MEM-01), and the memory_write_events audit enum extension (B4). One plan owns ALL
alembic revisions this phase so no divergent alembic heads are created.

Purpose: Downstream service/lifecycle plans (44-02, 44-03, 44-04) build against stable,
migrated tables and a stable ORM contract — no schema churn mid-service-build.
Output: Two alembic migrations (021, 022), three new ORM models in src/db/models.py, an
extended memory_type CHECK, and a schema smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-memory-layering-case-working-context-thread-case-many-to-man/44-CONTEXT.md
@.planning/phases/44-memory-layering-case-working-context-thread-case-many-to-man/44-RESEARCH.md

<interfaces>
<!-- Verified from repo. Executor uses these directly — no exploration needed. -->

Migration template idioms (src/db/migrations/versions/013_long_term_case_memory.py):
```python
def _jsonb_empty_object() -> sa.TextClause: return sa.text("'{}'::jsonb")
def _jsonb_empty_array() -> sa.TextClause:  return sa.text("'[]'::jsonb")
def _timestamps() -> tuple[sa.Column, sa.Column]:
    return (
        sa.Column("created_at", sa.DateTime(timezone=True), nullable=False, server_default=sa.func.now()),
        sa.Column("updated_at", sa.DateTime(timezone=True), nullable=False, server_default=sa.func.now()),
    )
# JSONB column: postgresql.JSONB(astext_type=sa.Text()), server_default=_jsonb_empty_array()
# Partial index: op.create_index(..., postgresql_where=sa.text("deleted_at IS NULL"))
```

Latest migration head (src/db/migrations/versions/020_memory_write_event_policy_audit.py):
```python
revision: str = "020_memory_write_event_policy_audit"
```

Existing memory_write_events CHECK (src/db/models.py:646-649, migration 013:259-262):
```python
CheckConstraint(
    "memory_type IN ('session_slot', 'long_term_fact', 'case_memory', 'none')",
    name="ck_memory_write_events_memory_type",
)
```

Reused constants in src/db/models.py (line 434):
```python
MEMORY_PII_CLASSIFICATION_CHECK = "pii_classification IN ('none', 'low', 'sensitive', 'prohibited')"
```

Business anchor tables (src/db/models.py):
```python
class RefundCase: __tablename__ = "refund_cases"  # id UUID PK, refund_case_no String(64) unique
class ConversationThread: __tablename__ = "conversation_threads"  # id UUID PK, thread_id String(128), case_id String(128) nullable NO FK (unchanged this phase)
```

policy.py memory-type literal (src/memory/policy.py:14):
```python
MemoryPolicyMemoryType = Literal["session", "long_term", "case"]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: thread_case_links table (migration 021 + ORM model)</name>
  <read_first>
    - src/db/migrations/versions/013_long_term_case_memory.py (template: helpers, create_table, partial indexes)
    - src/db/migrations/versions/020_memory_write_event_policy_audit.py (current head / revision id)
    - src/db/models.py lines 1081-1118 (ConversationThread) and 158-174 (RefundCase)
  </read_first>
  <files>src/db/migrations/versions/021_thread_case_links.py, src/db/models.py</files>
  <action>
    Create migration `021_thread_case_links.py` with `revision = "021_thread_case_links"` and
    `down_revision = "020_memory_write_event_policy_audit"`. Copy the `_jsonb_empty_object`,
    `_jsonb_empty_array`, `_timestamps` helpers from 013. `upgrade()` creates table
    `thread_case_links`:
    - `id` UUID PK
    - `tenant_id` UUID FK→tenants.id NOT NULL
    - `conversation_thread_id` UUID FK→conversation_threads.id NOT NULL
    - `thread_id` String(128) NOT NULL (denormalized business thread key, mirrors ConversationThread idiom)
    - `case_id` UUID FK→refund_cases.id NOT NULL  (canonical case identity per B2 — UUID, never a bare string)
    - `link_source` String(32) NOT NULL
    - `linked_by_run_id` UUID FK→agent_runs.id (nullable)
    - `schema_version` String(48) NOT NULL server_default "thread_case_link.v1"
    - `*_timestamps()`, `deleted_at` DateTime(timezone=True) nullable
    - CheckConstraint `ck_thread_case_links_link_source`: `link_source IN ('run_auto', 'staff_manual', 'import')`
    Indexes: `ix_thread_case_links_tenant_id` on (tenant_id); `ix_thread_case_links_tenant_case` on (tenant_id, case_id) partial `deleted_at IS NULL` (answers "which threads touched this case"); unique `uq_thread_case_links_active` on (tenant_id, conversation_thread_id, case_id) partial `deleted_at IS NULL` (dedup key for B3 lifecycle). `downgrade()` drops indexes then the table.
    In src/db/models.py add `class ThreadCaseLink(TimestampMixin, Base)` mirroring the columns
    (use `Mapped[...]` + `mapped_column`), with `__table_args__` holding the CheckConstraint, and
    the three Index(...) declarations after the class following the file's existing post-class Index idiom.
  </action>
  <verify>
    <automated>uv run python -c "import ast,sys; ast.parse(open('src/db/models.py').read()); ast.parse(open('src/db/migrations/versions/021_thread_case_links.py').read()); print('ok')"</automated>
  </verify>
  <done>Migration 021 parses; `grep -n "thread_case_links" src/db/models.py` matches; `grep -n "down_revision.*020_memory_write_event_policy_audit" src/db/migrations/versions/021_thread_case_links.py` matches.</done>
</task>

<task type="auto">
  <name>Task 2: case_working_contexts + case_working_context_revisions (migration 022 + ORM models)</name>
  <read_first>
    - src/db/migrations/versions/013_long_term_case_memory.py (case_memories block: JSONB columns, CheckConstraints, partial indexes)
    - src/db/models.py lines 507-579 (CaseMemory model + Index idioms) and 430-434 (MEMORY_*_CHECK constants)
    - src/db/migrations/versions/021_thread_case_links.py (this plan Task 1 — for revision chain)
  </read_first>
  <files>src/db/migrations/versions/022_case_working_context.py, src/db/models.py</files>
  <action>
    Create migration `022_case_working_context.py` with `revision = "022_case_working_context"`
    and `down_revision = "021_thread_case_links"`. `upgrade()` creates two tables.

    Table `case_working_contexts` (scope key = (tenant_id, case_id UUID) per B2/D-CWC-1):
    - `id` UUID PK; `tenant_id` UUID FK→tenants.id NOT NULL; `case_id` UUID FK→refund_cases.id NOT NULL
    - `schema_version` String(48) NOT NULL server_default "case_working_context.v1"
    - `authority_class` String(32) NOT NULL server_default "contextual_only"
    - `customer_request` Text nullable; `issue_type` String(64) nullable
    - JSONB NOT NULL (server_default `_jsonb_empty_array()`): `claims_json`, `verified_facts_json`, `missing_info_json`, `evidence_refs_json`, `actions_taken_json`, `policy_refs_json`, `agent_recommendations_json`, `pending_tasks_json`, `commitments_json`
    - JSONB NOT NULL (server_default `_jsonb_empty_object()`): `next_action_json`, `source_ref_json`
    - `version` Integer NOT NULL server_default "1"
    - `updated_by_run_id` UUID FK→agent_runs.id (nullable)
    - `pii_classification` String(32) NOT NULL server_default "none"
    - `*_timestamps()`, `deleted_at` nullable
    - CheckConstraints: `ck_case_working_contexts_authority_class`: `authority_class = 'contextual_only'`; `ck_case_working_contexts_version_positive`: `version > 0`; `ck_case_working_contexts_pii_classification` reusing the `MEMORY_PII_CLASSIFICATION_CHECK` string literal.
    Indexes: `ix_case_working_contexts_tenant_id` (tenant_id); unique `uq_case_working_contexts_active_scope` on (tenant_id, case_id) partial `deleted_at IS NULL` (one active CWC per case, enables read-by-case in 44-03).

    Table `case_working_context_revisions` (B5 append-only history):
    - `id` UUID PK; `tenant_id` UUID FK→tenants.id NOT NULL
    - `case_working_context_id` UUID FK→case_working_contexts.id NOT NULL; `case_id` UUID FK→refund_cases.id NOT NULL
    - `version` Integer NOT NULL (the version this row snapshots)
    - `snapshot_json` JSONB NOT NULL server_default `_jsonb_empty_object()` (full prior content)
    - `edit_source` String(32) NOT NULL; `updated_by_run_id` UUID FK→agent_runs.id nullable
    - `source_ref_json` JSONB NOT NULL server_default `_jsonb_empty_object()`
    - single `created_at` column only (append-only — NO updated_at, NO deleted_at)
    - CheckConstraints: `ck_cwc_revisions_edit_source`: `edit_source IN ('run_auto', 'staff_manual')`; `ck_cwc_revisions_version_positive`: `version > 0`
    - unique index `uq_cwc_revisions_context_version` on (tenant_id, case_working_context_id, version); index `ix_cwc_revisions_case` on (tenant_id, case_id, version)

    Also extend the audit enum (B4): `op.drop_constraint("ck_memory_write_events_memory_type", "memory_write_events", type_="check")` then `op.create_check_constraint("ck_memory_write_events_memory_type", "memory_write_events", "memory_type IN ('session_slot', 'long_term_fact', 'case_memory', 'case_working_context', 'none')")`. `downgrade()` must first drop the 5-value CHECK, then query `memory_write_events` for existing `memory_type = 'case_working_context'` rows with `op.get_bind()`. If any exist, block downgrade with a clear `RuntimeError` message naming the row count and telling the operator to delete/export those audit rows before retrying; only after that guard passes may it restore the 4-value CHECK and drop both new tables and their indexes. Do NOT silently coerce CWC audit rows to another memory_type.

    In src/db/models.py add `class CaseWorkingContext(TimestampMixin, Base)` and
    `class CaseWorkingContextRevision(Base)` (revisions are append-only — do NOT use TimestampMixin;
    declare an explicit `created_at` column like MemoryWriteEvent at line 683). Mirror all columns and
    CheckConstraints; add the post-class Index(...) declarations. Update the CaseWorkingContext
    pii CheckConstraint to reference `MEMORY_PII_CLASSIFICATION_CHECK`.
  </action>
  <verify>
    <automated>uv run python -c "import ast; ast.parse(open('src/db/models.py').read()); ast.parse(open('src/db/migrations/versions/022_case_working_context.py').read()); print('ok')"</automated>
  </verify>
  <done>`grep -n "class CaseWorkingContext\b" src/db/models.py` and `grep -n "class CaseWorkingContextRevision" src/db/models.py` match; `grep -n "case_working_context" src/db/migrations/versions/022_case_working_context.py` matches the extended CHECK; `grep -n "down_revision.*021_thread_case_links" src/db/migrations/versions/022_case_working_context.py` matches.</done>
</task>

<task type="auto">
  <name>Task 3: sync model CHECK + policy literal, run migrations, schema smoke test</name>
  <read_first>
    - src/db/models.py lines 643-659 (MemoryWriteEvent CHECK to keep in sync with migration 022)
    - src/memory/policy.py lines 1-27 (MemoryPolicyMemoryType literal + MemoryPolicyDecision)
    - tests/db/ (pick an existing schema/migration test as structural template)
  </read_first>
  <files>src/db/models.py, src/memory/policy.py, tests/db/test_phase44_schema.py</files>
  <action>
    1. In src/db/models.py update the `ck_memory_write_events_memory_type` CheckConstraint string to
       `"memory_type IN ('session_slot', 'long_term_fact', 'case_memory', 'case_working_context', 'none')"`
       so the ORM matches migration 022.
    2. In src/memory/policy.py add `"case_working_context"` to `MemoryPolicyMemoryType` so downstream
       policy decisions can carry the CWC audit type (44-03 uses it). Do NOT alter session/long_term/case behavior.
    3. Create tests/db/test_phase44_schema.py with DB-backed tests that require compose PostgreSQL or
       perform an explicit environment probe and `pytest.skip` with a clear "PostgreSQL unavailable"
       reason before opening the DB fixture. The tests run `UV_CACHE_DIR=/tmp/uv-cache uv run alembic upgrade head`
       (or assert the metadata creates cleanly against the test engine per the repo's existing db-test
       fixture), then assert `thread_case_links`, `case_working_contexts`, `case_working_context_revisions`
       exist in `Base.metadata.tables`, and assert inserting a `MemoryWriteEvent` with
       `memory_type='case_working_context'` does not violate the CHECK. Add explicit nullable parity
       assertions that ORM and migration define `case_working_contexts.schema_version`, every default
       empty-object/array JSONB column, and `case_working_context_revisions.source_ref_json` as
       `nullable=False`. Add a downgrade round-trip scenario that seeds a
       `memory_write_events.memory_type='case_working_context'` audit row and asserts `UV_CACHE_DIR=/tmp/uv-cache uv run alembic downgrade -1`
       is blocked by the clear guard message before the 4-value CHECK is restored; then run the clean
       downgrade path with no CWC audit rows to prove the migration can still round-trip. Use the project's
       existing async DB test fixture (mirror an existing tests/db test's setup).
    Note: this phase is ADDITIVE — do NOT modify conversation_threads.case_id, do NOT touch
    case_memories / long_term_memories names (D-REDLINE).
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/db/test_phase44_schema.py -x -q</automated>
  </verify>
  <done>Schema test passes under compose PostgreSQL or is explicitly skipped by the Phase 44 DB environment probe; `grep -n "case_working_context" src/memory/policy.py` matches; `UV_CACHE_DIR=/tmp/uv-cache uv run alembic heads` reports a single head at 022_case_working_context.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live schema | DDL alters shared DB structure; additive-only this phase |
| ORM CHECK ↔ migration CHECK | drift between the two silently disables a constraint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | thread_case_links.case_id | mitigate | FK→refund_cases.id (UUID) forbids storing a bare ambiguous string (B2) |
| T-44-02 | Elevation | case_working_contexts.authority_class | mitigate | CHECK pins value to 'contextual_only'; row can never assert authority |
| T-44-03 | Denial-of-service | alembic heads | mitigate | both revisions chained (021→020, 022→021) in one plan → single linear head, no divergent-head migration failure |
| T-44-04 | Repudiation | memory_write_events enum | mitigate | CWC audit type added so CWC writes are auditable, not silently untyped |
| T-44-05 | Tampering | ORM/migration CHECK drift | mitigate | Task 3 syncs both and a test inserts the new enum value |
</threat_model>

<verification>
- `UV_CACHE_DIR=/tmp/uv-cache uv run alembic upgrade head` then `UV_CACHE_DIR=/tmp/uv-cache uv run alembic downgrade -1` twice round-trips cleanly on a DB with no CWC audit rows (no orphan objects).
- Downgrade with an existing `case_working_context` audit row is blocked by the explicit guard and covered by tests.
- `tests/db/test_phase44_schema.py` green under compose PostgreSQL.
- `git grep -n "case_memories\|long_term_memories" src/db/migrations/versions/021_thread_case_links.py src/db/migrations/versions/022_case_working_context.py` returns nothing (red line: no rename/touch).
</verification>

<success_criteria>
Three new tables and an extended audit CHECK exist and migrate cleanly on a single alembic head,
with ORM models matching migrations, and no change to existing memory table names or
conversation_threads.case_id.
</success_criteria>

<output>
After completion, create `.planning/phases/44-memory-layering-case-working-context-thread-case-many-to-man/44-01-SUMMARY.md`
</output>
