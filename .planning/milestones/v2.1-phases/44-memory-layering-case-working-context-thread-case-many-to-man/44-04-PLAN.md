---
phase: 44-memory-layering-case-working-context-thread-case-many-to-man
plan: 04
type: execute
wave: 4
depends_on: [44-01, 44-02, 44-03]
files_modified:
  - docs/contract-spec.md
  - .planning/MEMORY-REDESIGN-DECISIONS.md
  - .planning/LOCAL-VALIDATION-ISSUES.md
  - tests/memory/test_phase44_contract_alignment.py
autonomous: false
requirements: [MEM-01, MEM-02]
must_haves:
  truths:
    - "docs/contract-spec.md §13 defines Case Working Context as a normative memory layer, authority_class=contextual_only, distinct from case_memory precedent"
    - "The spec states CWC is NOT an EvidenceRef and cannot authorize policy/risk/approval/action"
    - "The spec records thread↔case as an additive many-to-many association that does not drop the legacy single case_id"
    - "The three DEFER items remain named with target phases and are unbuilt"
  artifacts:
    - path: "docs/contract-spec.md"
      provides: "normative CWC + thread↔case M:N contract note (B6)"
      contains: "Case Working Context"
  key_links:
    - from: "docs/contract-spec.md §13"
      to: "case_working_contexts table (44-01)"
      via: "normative layer description matches implemented schema"
      pattern: "case_working_context"
---

<objective>
Close B6: reconcile `docs/contract-spec.md` §13 (Memory 设计) with the implemented Case Working
Context layer and the thread↔case association, per the project "spec supports / phase decides" rule,
then run the phase-level verification sweep and confirm the three DEFER boundaries held.

Purpose: Silent divergence between the implemented CWC layer and the normative memory contract is
forbidden by CLAUDE.md. §13 currently has no CWC entry and describes case_memory only as precedent.
This plan adds the normative CWC definition and records the additive M:N. This is a docs + review plan,
so it is NON-autonomous (dual-AI review gate before final, matching the Phase 39 spec-edit precedent).
Output: contract-spec §13 CWC/M:N note, decision-record update, contract-alignment test, review checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-memory-layering-case-working-context-thread-case-many-to-man/44-CONTEXT.md
@.planning/phases/44-memory-layering-case-working-context-thread-case-many-to-man/44-RESEARCH.md
@.planning/MEMORY-REDESIGN-DECISIONS.md

<interfaces>
<!-- Verified from repo. -->

docs/contract-spec.md §13 anchors (Memory 设计):
- §13 layer table at line ~1426 (Working / Session / Long-term / Case memory / Audit rows)
- §13.2 Session memory (line ~1447), §13.3 Long-term (~1490), §13.4 Case memory (~1507), §13.5 Memory write policy (~1513)
- §13.4 currently: "Case memory is precedent retrieval ... never substitutes current business facts"
- Memory architecture decision (line ~1422): memory is contextual assistance, cannot produce EvidenceRefV1,
  cannot authorize actions/approval.

Implemented this phase (44-01..44-03):
- table case_working_contexts, scope (tenant_id, case_id UUID→refund_cases.id), authority_class='contextual_only',
  version + case_working_context_revisions history, claim/fact separated, refs-only (no policy body / raw PII)
- table thread_case_links (additive M:N; conversation_threads.case_id kept, 26 readers untouched)
- memory_write_events.memory_type extended with 'case_working_context'

DEFER items to keep named (44-CONTEXT.md / MEMORY-REDESIGN-DECISIONS.md §六):
DEFER-1 session re-scope; DEFER-2 case_memories precedent repositioning + closed-case extraction;
DEFER-3 long_term narrow explicit-preference.

Phase 44 DB verification rule: DB-backed tests must either run against compose PostgreSQL or contain an
explicit environment-probe skip branch. If compose PostgreSQL is unavailable and DB verification is not
actually run, append a Chinese entry to `.planning/LOCAL-VALIDATION-ISSUES.md` with symptom, reproduction
command, evidence, current root-cause judgment, handling done, remaining issue, and resume command.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: add normative CWC + thread↔case note to contract-spec §13</name>
  <read_first>
    - docs/contract-spec.md lines 1417-1560 (§13 Memory 设计 through §13.5 memory write policy)
    - .planning/phases/44-memory-layering-case-working-context-thread-case-many-to-man/44-CONTEXT.md (D-CWC-1..4, D-REDLINE)
  </read_first>
  <files>docs/contract-spec.md</files>
  <action>
    In §13, add a new subsection (e.g. §13.4a "Case Working Context") and a row in the §13 layer
    table. Normative content: Case Working Context is a case-scoped durable working-state layer
    (table `case_working_contexts`, scope = tenant_id + case_id bound to refund_cases.id UUID),
    authority_class = `contextual_only`. State explicitly: it is NOT an `EvidenceRefV1`, cannot
    authorize policy/risk/approval/action, and is DISTINCT from `case_memory` (which remains reviewed
    cross-case precedent, NOT active case state — clarify §13.4 with this one-line semantic lock per
    D-REDLINE, WITHOUT renaming the table). Record: claims and verified facts are stored separately;
    tool-derived facts store reference/summary + observed_at only; policy body text and sensitive raw
    PII are never stored; the layer is human-correctable and versioned (append-only revisions). Add a
    short note that thread↔case is modeled as an additive many-to-many association table
    (`thread_case_links`) that does NOT drop the legacy single `conversation_threads.case_id`. Do NOT
    modify §8.0 identity fields or any tool-contract section; this is a §13-scoped memory-contract edit.
  </action>
  <verify>
    <automated>grep -n "Case Working Context" docs/contract-spec.md && grep -n "case_working_context" docs/contract-spec.md && grep -n "thread_case_links" docs/contract-spec.md</automated>
  </verify>
  <done>§13 has a normative CWC subsection + layer-table row; §13.4 carries the "case_memory = reviewed precedent, NOT active case state" clarification; spec states CWC is not an EvidenceRef and is additive M:N; no rename of case_memories/long_term_memories.</done>
</task>

<task type="auto">
  <name>Task 2: contract-alignment test + decision-record DEFER trace</name>
  <read_first>
    - .planning/MEMORY-REDESIGN-DECISIONS.md §六 (DEFER-1/2/3)
    - docs/contract-spec.md §13 (Task 1 edit)
    - tests/ (pick a docs/spec-assertion test as structural template if one exists; else a plain unit test)
  </read_first>
  <files>tests/memory/test_phase44_contract_alignment.py, .planning/MEMORY-REDESIGN-DECISIONS.md</files>
  <action>
    Create tests/memory/test_phase44_contract_alignment.py asserting the spec↔impl invariants as
    string checks over docs/contract-spec.md: (a) "Case Working Context" appears in §13; (b) the spec
    associates CWC with authority_class contextual_only; (c) the spec still contains table names
    `case_memories` and `long_term_memories` (red-line: not renamed); (d) `case_working_context`
    appears as a memory_write_events type. In .planning/MEMORY-REDESIGN-DECISIONS.md, confirm the three
    DEFER items remain named with target phases and add a one-line "Phase 44 delivered: CWC layer +
    thread↔case M:N; auto-update hook wiring deferred" trace so the follow-up boundary is explicit.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_phase44_contract_alignment.py -x -q</automated>
  </verify>
  <done>Alignment test passes; `grep -n "DEFER-1\|DEFER-2\|DEFER-3" .planning/MEMORY-REDESIGN-DECISIONS.md` still matches all three; red-line table-name assertions green.</done>
</task>

<task type="auto">
  <name>Task 3: phase-level verification sweep</name>
  <read_first>
    - all 44-0x plan <verification> sections
    - .planning/LOCAL-VALIDATION-ISSUES.md (append any issues found here per project rule)
  </read_first>
  <files>tests/memory/test_phase44_contract_alignment.py, .planning/LOCAL-VALIDATION-ISSUES.md</files>
  <action>
    Run the full Phase 44 test surface and record results with the project test entrypoint: schema (44-01),
    identity+repo (44-02), links+service (44-03), contract alignment (44-04). Before DB-backed tests,
    either start/use compose PostgreSQL or rely on the explicit Phase 44 DB environment-probe skip branch
    added in 44-01/44-02/44-03 tests. Confirm: `UV_CACHE_DIR=/tmp/uv-cache uv run alembic heads` = single head
    (022_case_working_context); no divergent heads. Confirm additive red lines with
    `git grep -n "case_memories\|long_term_memories"` shows only pre-existing references (no rename)
    and conversation_threads.case_id path unchanged. If any DB-backed test cannot run locally
    (no compose PostgreSQL), record that explicitly in .planning/LOCAL-VALIDATION-ISSUES.md with the
    failed/skipped command, evidence, current root-cause judgment, handling done, remaining issue, and
    resume command — do NOT claim DB verification passed without a real DB pass.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/db/test_phase44_schema.py tests/memory/test_case_identity.py tests/memory/test_case_working_context_repo.py tests/memory/test_thread_case_links.py tests/memory/test_case_working_context_service.py tests/memory/test_phase44_contract_alignment.py -q</automated>
  </verify>
  <done>Full Phase 44 test surface green (or blockers recorded in LOCAL-VALIDATION-ISSUES.md); single alembic head confirmed; red-line grep confirms no renames.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>A normative Case Working Context contract note in docs/contract-spec.md §13 plus the additive thread↔case M:N description, with a contract-alignment test and DEFER trace. Per the dual-AI workflow, a spec edit goes through review before it is final (Phase 39 precedent).</what-built>
  <how-to-verify>
    1. Review the §13 diff: does the CWC definition match the implemented schema (scope, authority_class, claim/fact separation, refs-only, versioned)?
    2. Confirm §13.4 case_memory is clarified as precedent (not renamed) and CWC is stated as NOT an EvidenceRef.
    3. Confirm the M:N note says additive (legacy case_id kept).
    4. Route the diff through Codex cross-review per CLAUDE.md dual-AI workflow; adjudicate findings.
  </how-to-verify>
  <resume-signal>Type "approved" once the spec diff passes dual-AI review, or list required changes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implemented schema ↔ normative spec | silent divergence violates the spec-vs-phase rule |
| CWC layer ↔ authoritative services | CWC must never be citable as evidence/authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-16 | Repudiation | undocumented CWC layer | mitigate | §13 normative note + alignment test bind spec to implemented schema (B6) |
| T-44-17 | Elevation | CWC cited as authority | mitigate | spec states contextual_only, not EvidenceRef, cannot authorize policy/risk/approval/action |
| T-44-18 | Tampering | red-line rename creep | mitigate | test asserts case_memories/long_term_memories names still present |
| T-44-19 | Repudiation | lost DEFER boundary | accept | DEFER-1/2/3 remain named with target phases; unbuilt by design |
</threat_model>

<verification>
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_phase44_contract_alignment.py -q` green.
- Full phase test surface green (Task 3) or blockers recorded in LOCAL-VALIDATION-ISSUES.md.
- Spec edit passed dual-AI review (checkpoint) before treated as final.
- `git grep -n "case_memories\|long_term_memories" docs/contract-spec.md` shows the names retained (no rename).
</verification>

<success_criteria>
docs/contract-spec.md §13 normatively defines Case Working Context (contextual_only, not evidence,
distinct from precedent) and the additive thread↔case M:N, the alignment test locks it, and the three
DEFER items remain named and unbuilt — with the spec edit passing dual-AI review.
</success_criteria>

<output>
After completion, create `.planning/phases/44-memory-layering-case-working-context-thread-case-many-to-man/44-04-SUMMARY.md`
</output>
