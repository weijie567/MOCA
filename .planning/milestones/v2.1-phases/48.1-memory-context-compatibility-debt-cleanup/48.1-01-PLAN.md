---
phase: 48.1-memory-context-compatibility-debt-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/conversation/repository.py
  - tests/conversation/test_repository.py
  - tests/memory/test_thread_case_links.py
autonomous: true
requirements: [MEM-COMPAT-01]
requirements_addressed: [MEM-COMPAT-01]
must_haves:
  truths:
    - "Active code paths that need current thread-case relationships read from thread_case_links through ThreadCaseLinkRepository."
    - "conversation_threads.case_id and ConversationSummary.case_id remain legacy/historical metadata only, with no destructive schema migration."
    - "Threads with multiple linked cases are not collapsed to a single legacy case_id value."
  artifacts:
    - path: "src/conversation/repository.py"
      provides: "canonical thread-case relationship read in conversation summary/repository code"
      contains: "ThreadCaseLinkRepository"
    - path: "tests/conversation/test_repository.py"
      provides: "conversation repository regression for canonical thread-case reads"
      contains: "thread_case_links"
    - path: "tests/memory/test_thread_case_links.py"
      provides: "many-to-many repository behavior coverage"
      contains: "list_cases_for_thread"
  key_links:
    - from: "src/conversation/repository.py"
      to: "src/memory/thread_case_links.py"
      via: "ThreadCaseLinkRepository.list_cases_for_thread"
      pattern: "ThreadCaseLinkRepository"
    - from: "src/conversation/repository.py"
      to: "src/db/models.py"
      via: "ConversationSummary.case_id legacy metadata write"
      pattern: "case_id="
---

<objective>
Migrate active conversation thread-case reads away from `conversation_threads.case_id` and onto the canonical `thread_case_links` relationship.

Purpose: Phase 44 introduced many-to-many thread-case links, but conversation summary code still reads the legacy single-case column. This plan makes current relationship reads repository-backed while preserving legacy columns and historical metadata.
Output: updated conversation repository read path and focused tests proving canonical links win and legacy columns are not removed or treated as current relationship truth.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ARCHITECTURE-DEBT.md
@.planning/phases/48.1-memory-context-compatibility-debt-cleanup/48.1-RESEARCH.md
@.planning/phases/48.1-memory-context-compatibility-debt-cleanup/48.1-PATTERNS.md
@.planning/phases/48.1-memory-context-compatibility-debt-cleanup/48.1-VALIDATION.md
@src/conversation/repository.py
@src/memory/thread_case_links.py
@src/db/models.py
@tests/conversation/test_repository.py
@tests/memory/test_thread_case_links.py

<interfaces>
Existing canonical link repository:
```python
class ThreadCaseLinkRepository:
    async def list_cases_for_thread(
        self,
        *,
        tenant_id: uuid.UUID,
        conversation_thread_id: uuid.UUID,
    ) -> list[uuid.UUID]: ...
```

Existing conversation summary method:
```python
async def insert_thread_summary(
    self,
    *,
    tenant_id: uuid.UUID,
    user_id: uuid.UUID,
    thread_id: str,
    ...
) -> ConversationSummary: ...
```
</interfaces>

<source_audit>
SOURCE | ID | Requirement / Decision | Plan | Status
--- | --- | --- | --- | ---
ROADMAP | Phase 48.1 SC1 | Active code reads thread-case relationships from thread_case_links / ThreadCaseLinkRepository | 48.1-01 | COVERED
REQ | MEM-COMPAT-01 | thread-case relationship reads use thread_case_links instead of conversation_threads.case_id | 48.1-01 | COVERED
RESEARCH | Locked must-do | ConversationSummary.case_id remains legacy/historical metadata; no destructive schema migration | 48.1-01 | COVERED
ARCH-DEBT | Phase 48.1 scope | Move active thread-case reader to ThreadCaseLinkRepository | 48.1-01 | COVERED
CONTEXT | User prompt | graph/API/table renames and case_id drops are not in scope | 48.1-01 | COVERED
</source_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add conversation repository coverage for canonical thread-case reads</name>
  <read_first>
    - src/conversation/repository.py `insert_thread_summary` and `link_case`
    - src/memory/thread_case_links.py `ThreadCaseLinkRepository.list_cases_for_thread`
    - src/db/models.py `ConversationThread`, `ThreadCaseLink`, and `ConversationSummary`
    - tests/conversation/test_repository.py repository fixture patterns
    - tests/memory/test_thread_case_links.py many-to-many link fixture patterns
    - .planning/phases/48.1-memory-context-compatibility-debt-cleanup/48.1-PATTERNS.md thread-case repository boundary
  </read_first>
  <files>tests/conversation/test_repository.py, tests/memory/test_thread_case_links.py</files>
  <behavior>
    - Test 1: a thread with one active `thread_case_links` row gets summary legacy metadata from that canonical link, not from `ConversationThread.case_id`.
    - Test 2: a thread with two active `thread_case_links` rows does not collapse the relationship into the legacy `ConversationSummary.case_id` field.
    - Test 3: a thread with no active link does not read `ConversationThread.case_id` as current relationship truth during summary insertion.
    - Test 4: existing `ThreadCaseLinkRepository.list_cases_for_thread(...)` returns all active case IDs for a multi-case thread and ignores soft-deleted links.
  </behavior>
  <action>
    Add focused tests before changing production code. Use existing async DB fixture style from `tests/conversation/test_repository.py` and `tests/memory/test_thread_case_links.py`.

    In the new conversation repository tests, create a thread whose `ConversationThread.case_id` is a conflicting legacy string such as `"LEGACY-CASE-ONLY"`, then create canonical `ThreadCaseLink` rows through `ConversationRepository.link_case(...)` or `ThreadCaseLinkRepository.link_thread_to_case(...)`. Call `ConversationRepository.insert_thread_summary(...)` and assert that the summary does not use the conflicting legacy string as active relationship truth.

    Expected assertions:
    - one canonical active link: `summary.case_id == str(linked_case_id)`;
    - two canonical active links: `summary.case_id is None`;
    - no canonical active links with legacy thread column set: `summary.case_id is None`.

    Keep `ConversationSummary.case_id` in the model and migration untouched. These tests are about active reader precedence, not schema removal.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/conversation/test_repository.py tests/memory/test_thread_case_links.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "LEGACY-CASE-ONLY|thread_case_links|list_cases_for_thread" tests/conversation/test_repository.py tests/memory/test_thread_case_links.py` finds the new regression coverage.
    - The new tests fail before production code changes because `insert_thread_summary` still copies `thread.case_id`.
    - The automated command uses `UV_CACHE_DIR=/tmp/uv-cache uv run pytest` and exits 0 after Task 2.
  </acceptance_criteria>
  <done>Tests specify canonical thread_case_links behavior before the repository implementation changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Use ThreadCaseLinkRepository for summary relationship metadata</name>
  <read_first>
    - src/conversation/repository.py `insert_thread_summary`
    - src/conversation/repository.py `link_case`
    - src/memory/thread_case_links.py `ThreadCaseLinkRepository.list_cases_for_thread`
    - tests/conversation/test_repository.py tests added in Task 1
    - tests/memory/test_thread_case_links.py existing tenant and active-link tests
  </read_first>
  <files>src/conversation/repository.py, tests/conversation/test_repository.py, tests/memory/test_thread_case_links.py</files>
  <behavior>
    - Test 1: `insert_thread_summary(...)` imports or otherwise uses `ThreadCaseLinkRepository`.
    - Test 2: `ConversationSummary.case_id` is written from a single unambiguous canonical link only.
    - Test 3: `ConversationSummary.case_id` is `None` when active canonical links are missing or plural.
    - Test 4: no new migration drops, renames, or alters `conversation_threads.case_id`.
  </behavior>
  <action>
    In `src/conversation/repository.py::insert_thread_summary(...)`, stop assigning `case_id=thread.case_id`.

    Add a private helper in the same class or module, for example `_legacy_summary_case_id_from_links(...)`, that calls `ThreadCaseLinkRepository(self.session).list_cases_for_thread(tenant_id=tenant_id, conversation_thread_id=thread.id)` and returns:
    - `str(case_ids[0])` when exactly one active canonical link exists;
    - `None` when zero or more than one active canonical link exists.

    Use that value for `ConversationSummary.case_id`. Do not read `ConversationThread.case_id` as a fallback for current summary insertion. Add a short code comment only if needed to state that the summary column is legacy/historical metadata and canonical relationships live in `thread_case_links`.

    Leave `ConversationRepository.get_or_create_thread(..., case_id=...)` in place as a legacy metadata compatibility input. This phase must neither delete that parameter nor treat it as current relationship truth; active thread-case relationship reads must come from `thread_case_links`.

    Do not modify `src/db/models.py`, Alembic migrations, `ConversationThread.case_id`, or public API schema. Do not add raw SQL; use `ThreadCaseLinkRepository`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/conversation/test_repository.py tests/memory/test_thread_case_links.py -x -q</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/conversation/repository.py tests/conversation/test_repository.py tests/memory/test_thread_case_links.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "ThreadCaseLinkRepository" src/conversation/repository.py` matches.
    - `rg -n "case_id=thread\\.case_id" src/conversation/repository.py` returns no matches.
    - `rg -n "case_id: str \\| None = None" src/conversation/repository.py` still matches `get_or_create_thread(...)`, proving the legacy metadata input was not removed.
    - `git diff --name-only -- src/db/migrations/versions` returns no files for this plan.
    - Both automated commands exit 0.
  </acceptance_criteria>
  <done>Conversation summary insertion reads canonical active thread-case links and keeps legacy columns as metadata only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conversation repository -> memory repository | Conversation summary code crosses from conversation storage into canonical memory association reads. |
| thread_case_links -> summary metadata | Many-to-many relationship data is projected into a legacy single-value summary column only when unambiguous. |
| tenant data -> repository tests | Thread/case relationship tests must preserve tenant isolation and active-row filtering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48.1-01 | I | `ConversationRepository.insert_thread_summary` | mitigate | Use `ThreadCaseLinkRepository.list_cases_for_thread(...)` and never `thread.case_id` as active relationship truth. |
| T-48.1-02 | T | multi-case summary metadata | mitigate | Return `None` for legacy `ConversationSummary.case_id` when canonical links are plural to avoid false single-case state. |
| T-48.1-03 | E/I | tenant-scoped relationship reads | mitigate | Reuse existing repository tenant and active-link filters instead of raw SQL. |
| T-48.1-04 | R | schema compatibility | mitigate | Keep `conversation_threads.case_id` and `ConversationSummary.case_id` columns unchanged; focused tests cover behavior only. |
</threat_model>

<verification>
Run the focused thread-case plan gate:
```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/conversation/test_repository.py tests/memory/test_thread_case_links.py -x -q
UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/conversation/repository.py tests/conversation/test_repository.py tests/memory/test_thread_case_links.py
```
</verification>

<success_criteria>
- Active thread-case relationship reads in conversation summary insertion go through `ThreadCaseLinkRepository`.
- `case_id=thread.case_id` no longer appears in `src/conversation/repository.py`.
- Legacy case columns remain present and no migration is added.
- Focused conversation and thread-case tests pass with the approved uv runner.
</success_criteria>

<output>
After completion, create `.planning/phases/48.1-memory-context-compatibility-debt-cleanup/48.1-01-SUMMARY.md`.
</output>
