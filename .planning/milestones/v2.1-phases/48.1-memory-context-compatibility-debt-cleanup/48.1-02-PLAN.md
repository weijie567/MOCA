---
phase: 48.1-memory-context-compatibility-debt-cleanup
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/routing.py
  - src/agent/working_state.py
  - src/agent/context/session_memory_bundle.py
  - tests/agent/test_intent_routing.py
  - tests/agent/test_working_state.py
  - tests/agent/test_session_memory_load.py
  - tests/agent/test_nodes/test_generate_recommendation.py
  - tests/agent/context/test_assembler.py
autonomous: true
requirements: [MEM-COMPAT-01]
requirements_addressed: [MEM-COMPAT-01]
must_haves:
  truths:
    - "Agent routing reads session_context before legacy session_memory for slot continuity."
    - "Working-state projection reads session_context before legacy session_memory for open questions and business context refs."
    - "Prompt/session context helpers read session_context_bundle before legacy session_memory_bundle."
    - "session_memory and session_memory_bundle remain fallback compatibility surfaces for old traces and tests."
  artifacts:
    - path: "src/agent/routing.py"
      provides: "canonical session context slot continuity reader"
      contains: "session_context"
    - path: "src/agent/working_state.py"
      provides: "canonical working-state session context projection"
      contains: "session_context"
    - path: "src/agent/context/session_memory_bundle.py"
      provides: "canonical session_context_bundle prompt helper with legacy fallback"
      contains: "session_context_bundle"
  key_links:
    - from: "src/agent/nodes/session_context_load.py"
      to: "src/agent/routing.py"
      via: "session_context.slot_continuity"
      pattern: "session_context"
    - from: "src/agent/context/session_memory_bundle.py"
      to: "src/agent/context/projectors.py"
      via: "SessionMemoryBundle-compatible prompt projection"
      pattern: "SessionContextBundle"
---

<objective>
Migrate active session-context consumers to canonical `session_context` and `session_context_bundle` reads while retaining legacy session-memory fallback.

Purpose: Phase 46 introduced target session-context fields, but routing, working-state projection, and prompt helpers still prefer legacy fields. This plan makes canonical fields win without deleting compatibility projections used by old traces and tests.
Output: canonical-first readers in routing, working-state, and prompt/session helper modules plus focused tests proving canonical precedence and legacy fallback.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ARCHITECTURE-DEBT.md
@.planning/phases/48.1-memory-context-compatibility-debt-cleanup/48.1-RESEARCH.md
@.planning/phases/48.1-memory-context-compatibility-debt-cleanup/48.1-PATTERNS.md
@.planning/phases/48.1-memory-context-compatibility-debt-cleanup/48.1-VALIDATION.md
@src/agent/routing.py
@src/agent/working_state.py
@src/agent/context/session_memory_bundle.py
@src/agent/nodes/session_context_load.py
@src/memory/schemas.py
@src/memory/session_bundle.py
@tests/agent/test_intent_routing.py
@tests/agent/test_working_state.py
@tests/agent/test_session_memory_load.py
@tests/agent/test_nodes/test_generate_recommendation.py
@tests/agent/context/test_assembler.py

<interfaces>
Canonical state emitted by `session_context_load`:
```python
result = {
    "session_context": session_context,
    "session_context_bundle": SessionContextBundle(session_context=context).model_dump(mode="json"),
    "session_memory": session_memory,
}
```

Session context bundle models:
```python
class SessionContextBundle(BaseModel):
    session_context: SessionContextMemory

class SessionMemoryBundle(BaseModel):
    slot_continuity: SlotContinuityMemoryView
```
</interfaces>

<source_audit>
SOURCE | ID | Requirement / Decision | Plan | Status
--- | --- | --- | --- | ---
ROADMAP | Phase 48.1 SC2 | Routing, working-state, and prompt/session helpers read session_context / session_context_bundle as canonical | 48.1-02 | COVERED
REQ | MEM-COMPAT-01 | session_context/session_context_bundle are canonical; session_memory/session_memory_bundle remain fallback | 48.1-02 | COVERED
RESEARCH | Locked must-do | Do not remove session_memory or session_memory_bundle from state or node outputs | 48.1-02 | COVERED
ARCH-DEBT | Phase 48.1 scope | Demote active legacy session readers to compatibility fallback | 48.1-02 | COVERED
CONTEXT | User prompt | `session_memory_enabled` config rename is deferred and not implemented | 48.1-02 | COVERED
</source_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make routing and working-state projection prefer session_context</name>
  <read_first>
    - src/agent/routing.py `resolve_slots_with_metadata`
    - src/agent/working_state.py `_open_questions` and `_business_context_refs`
    - src/agent/nodes/session_context_load.py `_context_result`
    - src/memory/schemas.py `SessionContextMemory` and `SessionContextBundle`
    - tests/agent/test_intent_routing.py slot inheritance tests
    - tests/agent/test_working_state.py working-state projection tests
    - .planning/phases/48.1-memory-context-compatibility-debt-cleanup/48.1-PATTERNS.md canonical reader helper pattern
  </read_first>
  <files>src/agent/routing.py, src/agent/working_state.py, tests/agent/test_intent_routing.py, tests/agent/test_working_state.py</files>
  <behavior>
    - Test 1: when `session_context.slot_continuity.continuity_claimed` is true and legacy `session_memory` conflicts, `resolve_slots_with_metadata(...)` inherits active slots from `session_context`.
    - Test 2: when canonical `session_context` is absent, existing legacy `session_memory` inheritance still works.
    - Test 3: when both canonical and legacy open questions conflict, `project_working_state(...)` returns `session_context` unresolved questions.
    - Test 4: when both canonical and legacy business refs conflict, `project_working_state(...)` returns `session_context` business refs.
    - Test 5: when canonical `session_context` is absent, legacy working-state fallback remains covered.
  </behavior>
  <action>
    Add small local helpers instead of blind string replacement.

    In `src/agent/routing.py`, add a helper such as `_session_slot_continuity(state: AgentState) -> dict[str, Any]` near `resolve_slots_with_metadata(...)`. It must:
    - inspect `state.get("session_context")` first;
    - if it is a mapping with `slot_continuity`, use that mapping;
    - if it is a mapping that itself contains `continuity_claimed`, use the mapping directly;
    - fall back to `state.get("session_memory")` only when canonical data is absent or not a mapping.

    Update `resolve_slots_with_metadata(...)` to use the helper for `continuity_claimed`, `active_slots`, and `slot_metadata`. Preserve current-turn slot precedence and invalidation behavior.

    In `src/agent/working_state.py`, add a helper such as `_session_context_memory(state)` that applies the same canonical-first rule. Use it in `_open_questions(...)` and `_business_context_refs(...)` before the legacy fallback. Keep explicit `open_questions`, explicit `business_context_refs`, and direct `business_context.business_fact_refs` precedence unchanged.

    Do not remove `session_memory` from AgentState, `session_context_load` output, or tests that explicitly cover old trace fallback.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_intent_routing.py tests/agent/test_working_state.py -x -q</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/routing.py src/agent/working_state.py tests/agent/test_intent_routing.py tests/agent/test_working_state.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "session_context" src/agent/routing.py src/agent/working_state.py` matches canonical reader helpers.
    - `rg -n "session_memory" src/agent/routing.py src/agent/working_state.py` still matches fallback paths, not removed compatibility surfaces.
    - `rg -n "canonical|session_context|legacy" tests/agent/test_intent_routing.py tests/agent/test_working_state.py` finds tests proving canonical wins and legacy fallback.
    - Both automated commands exit 0.
  </acceptance_criteria>
  <done>Routing and working-state projection read canonical session context first while preserving legacy fallback behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make prompt/session helper prefer session_context_bundle</name>
  <read_first>
    - src/agent/context/session_memory_bundle.py `load_session_memory_bundle_for_state` and `session_memory_bundle_from_state`
    - src/memory/schemas.py `SessionContextBundle`, `SessionContextMemory`, and `SessionMemoryBundle`
    - src/memory/session_bundle.py `project_session_context_memory`
    - tests/agent/test_session_memory_load.py canonical and legacy node output tests
    - tests/agent/test_nodes/test_generate_recommendation.py existing session bundle prompt tests
    - tests/agent/context/test_assembler.py memory context bundle tests
  </read_first>
  <files>src/agent/context/session_memory_bundle.py, tests/agent/test_session_memory_load.py, tests/agent/test_nodes/test_generate_recommendation.py, tests/agent/context/test_assembler.py</files>
  <behavior>
    - Test 1: `load_session_memory_bundle_for_state(...)` uses `session_context_bundle` from state before invoking the conversation service.
    - Test 2: when `session_context_bundle` and `session_memory_bundle` conflict and both match tenant/user/thread/run, prompt context uses the canonical bundle.
    - Test 3: mismatched canonical bundle is rejected and does not block valid legacy fallback.
    - Test 4: old `session_memory_bundle` still works when canonical bundle is absent.
    - Test 5: service fallback still runs when neither canonical nor legacy state bundle is usable.
  </behavior>
  <action>
    In `src/agent/context/session_memory_bundle.py`, import `SessionContextBundle` and add `session_context_bundle_from_state(state: Mapping[str, Any]) -> SessionMemoryBundle | None`.

    The helper must:
    - read `state.get("session_context_bundle")`;
    - validate `SessionContextBundle` when raw data is a dict or already a model;
    - convert its `session_context` into a `SessionMemoryBundle`-compatible object by carrying tenant/user/thread/run, rolling summary, recent messages, tool summaries, slot continuity, policy hints, prior policy mention refs, and fallback reasons;
    - reuse `_bundle_matches_state(...)` before returning.

    Update `load_session_memory_bundle_for_state(...)` to try canonical `session_context_bundle_from_state(...)` first, then existing `session_memory_bundle_from_state(...)`, then service fallback.

    Keep the existing legacy function name and behavior because old traces/tests may still supply `session_memory_bundle`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_session_memory_load.py tests/agent/test_nodes/test_generate_recommendation.py tests/agent/context/test_assembler.py -x -q</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/context/session_memory_bundle.py tests/agent/test_session_memory_load.py tests/agent/test_nodes/test_generate_recommendation.py tests/agent/context/test_assembler.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "session_context_bundle_from_state|SessionContextBundle" src/agent/context/session_memory_bundle.py` matches.
    - `rg -n "session_memory_bundle_from_state" src/agent/context/session_memory_bundle.py` still matches as fallback.
    - `rg -n "session_context_bundle" tests/agent/test_session_memory_load.py tests/agent/test_nodes/test_generate_recommendation.py tests/agent/context/test_assembler.py` finds canonical precedence coverage.
    - Both automated commands exit 0.
  </acceptance_criteria>
  <done>Prompt/session context assembly consumes canonical session_context_bundle first and keeps legacy bundle fallback intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AgentState -> routing | User/session state affects slot inheritance and downstream route selection. |
| AgentState -> working-state prompt projection | Session context becomes prompt-visible working state but remains contextual-only. |
| AgentState -> prompt/session helper | Bundled conversation memory crosses from graph state into prompt assembly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48.1-05 | T | `resolve_slots_with_metadata` | mitigate | Canonical `session_context` wins over conflicting legacy `session_memory`; current-turn slots still override inherited slots. |
| T-48.1-06 | I | `project_working_state` | mitigate | Working-state tests prove canonical context is prompt-safe and legacy only fills absent canonical data. |
| T-48.1-07 | I/T | `load_session_memory_bundle_for_state` | mitigate | Validate canonical bundle shape and tenant/user/thread/run match before prompt projection. |
| T-48.1-08 | R | legacy trace compatibility | mitigate | Keep `session_memory` and `session_memory_bundle` fallback tests so old traces remain readable. |
</threat_model>

<verification>
Run the focused session-context reader gate:
```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_intent_routing.py tests/agent/test_working_state.py tests/agent/test_session_memory_load.py tests/agent/test_nodes/test_generate_recommendation.py tests/agent/context/test_assembler.py -x -q
UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/routing.py src/agent/working_state.py src/agent/context/session_memory_bundle.py tests/agent/test_intent_routing.py tests/agent/test_working_state.py tests/agent/test_session_memory_load.py tests/agent/test_nodes/test_generate_recommendation.py tests/agent/context/test_assembler.py
```
</verification>

<success_criteria>
- Canonical `session_context` drives slot inheritance and working-state projection when present.
- Canonical `session_context_bundle` drives prompt/session helper output when present and matching state identity.
- Legacy `session_memory` and `session_memory_bundle` remain fallback compatibility surfaces only.
- Focused agent tests and Ruff pass with approved uv commands.
</success_criteria>

<output>
After completion, create `.planning/phases/48.1-memory-context-compatibility-debt-cleanup/48.1-02-SUMMARY.md`.
</output>
