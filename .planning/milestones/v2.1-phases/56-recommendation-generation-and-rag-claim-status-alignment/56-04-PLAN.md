---
phase: 56-recommendation-generation-and-rag-claim-status-alignment
plan: 4
type: execute
wave: 3
depends_on: [56-01, 56-02, 56-03]
files_modified:
  - src/agent/graph_vocabulary.py
  - src/agent/nodes/final_response.py
  - src/api/routers/agent_runs.py
  - frontend/src/components/timeline/TimelineStep.tsx
  - scripts/eval_agent.py
  - docs/contract-spec.md
  - docs/target-agent-platform-architecture-plan.md
  - docs/current-langgraph-architecture.md
  - docs/architecture-overview.md
  - docs/agent-architecture-routing-explanation.md
  - docs/rag-architecture-spec.md
  - README.md
  - .planning/ARCHITECTURE-DEBT.md
  - .planning/DEFERRED-DECISIONS.md
  - .planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-VALIDATION.md
  - .planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-04-SUMMARY.md
  - tests/agent/test_graph_vocabulary.py
  - tests/agent/test_trace.py
  - tests/agent/test_nodes/test_final_response.py
  - tests/agent/test_phase22_final_response.py
  - tests/test_trace_api.py
  - tests/test_agent_runs_api.py
autonomous: true
requirements: [CAGM-07]
user_setup: []
must_haves:
  truths:
    - "Current-run API/SSE/frontend/eval labels recognize recommendation_generation."
    - "Historical generate_recommendation traces remain readable through graph vocabulary projection with Phase 58 delete metadata."
    - "Final response and API projection consume safe verified_evidence_package and claim_verification_bundle fields without leaking debug/verifier projections or treating legacy verifier fields as current-run authority."
    - "Current architecture docs, architecture-debt ledger, and validation artifact record Phase 56 compatibility and closeout status."
    - "Full focused suite, Ruff, artifact command scan, and diff check use approved MOCA entrypoints."
  artifacts:
    - path: "src/agent/graph_vocabulary.py"
      provides: "Phase 56 recommendation alias metadata and projection"
      contains: "PHASE_56_COMPATIBILITY_ALIAS"
    - path: "src/api/routers/agent_runs.py"
      provides: "Current-run SSE/API node label and payload support"
      contains: "recommendation_generation"
    - path: "docs/current-langgraph-architecture.md"
      provides: "Current source graph docs after Phase 56"
    - path: ".planning/ARCHITECTURE-DEBT.md"
      provides: "RAG/agent graph debt ledger update in Chinese"
  key_links:
    - from: "src/api/routers/agent_runs.py"
      to: "src/agent/graph_vocabulary.py"
      via: "target_graph_name(node_name, kind='node')"
      pattern: "target_node_name"
---

<objective>
Close Phase 56 by updating vocabulary/API/frontend/eval projections, safe final wording, docs/debt artifacts, and final validation evidence.

Purpose: satisfy D-56-04, D-56-11, D-56-12, D-56-13, D-56-14, D-56-15, D-56-16, and CAGM-07 closeout requirements.
Output: projection compatibility, current-run labels, safe response/API regressions, documentation/debt updates, and final validation artifact.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-CONTEXT.md
@.planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-RESEARCH.md
@.planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-PATTERNS.md
@.planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-VALIDATION.md
@.planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
@.planning/phases/55-memory-context-load-cutover/55-VERIFICATION.md
@AGENTS.md

<interfaces>
From `src/agent/graph_vocabulary.py`:
```python
class GraphVocabularyEntry:
    legacy_name: str
    target_name: str
    kind: Literal["node", "router"]
    status: Literal["runtime", "compatibility_alias", "deferred_non_runnable", "unknown_passthrough"]
    runnable: bool
    reason_codes: tuple[str, ...] = ()
```

From `src/api/routers/agent_runs.py`:
```python
data["target_node_name"] = target_graph_name(node_name, kind="node")
if node_name == "generate_recommendation":
    recommendation = _as_mapping(update_mapping.get("recommendation_draft"))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 56 vocabulary alias and trace projection tests</name>
  <files>src/agent/graph_vocabulary.py, tests/agent/test_graph_vocabulary.py, tests/agent/test_trace.py, tests/test_trace_api.py</files>
  <read_first>
    AGENTS.md
    .planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-CONTEXT.md
    .planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
    src/agent/graph_vocabulary.py
    tests/agent/test_graph_vocabulary.py
    tests/agent/test_trace.py
    tests/test_trace_api.py
  </read_first>
  <behavior>
    - `graph_vocabulary_entry("recommendation_generation", kind="node")` is runtime and identity-mapped.
    - `graph_vocabulary_entry("generate_recommendation", kind="node")` is compatibility_alias targeting `recommendation_generation`.
    - Phase 56 reason codes include `PHASE_56_COMPATIBILITY_ALIAS`, `HISTORICAL_TRACE_PROJECTION`, `IMPORT_TEST_COMPATIBILITY`, and `DELETE_BY_PHASE_58`.
    - Trace/API projection exposes canonical target for historical `generate_recommendation`.
  </behavior>
  <action>
    Add Phase 56 recommendation alias metadata in `src/agent/graph_vocabulary.py`, following the Phase 54/55 reason-code pattern. Add `recommendation_generation` as a runtime node entry and `generate_recommendation -> recommendation_generation` as a compatibility alias with the exact Phase 56 reason codes. Extend `tests/agent/test_graph_vocabulary.py` and trace/API projection tests so current `recommendation_generation` projects as runtime identity and historical `generate_recommendation` projects to canonical target without rewriting the original implementation node. Do not remove existing Phase 53/54/55 aliases and do not delete the recommendation alias; final alias deletion belongs to Phase 58.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_graph_vocabulary.py tests/agent/test_trace.py tests/test_trace_api.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    `src/agent/graph_vocabulary.py` contains `PHASE_56_COMPATIBILITY_ALIAS`, `HISTORICAL_TRACE_PROJECTION`, `IMPORT_TEST_COMPATIBILITY`, and `DELETE_BY_PHASE_58`.
    Tests assert `target_graph_name("generate_recommendation", kind="node") == "recommendation_generation"`.
    Tests assert `target_graph_name("recommendation_generation", kind="node") == "recommendation_generation"` with status `runtime`.
    Historical trace projection keeps `implementation_node == "generate_recommendation"` and target `recommendation_generation`.
  </acceptance_criteria>
  <done>Graph vocabulary records Phase 56 compatibility precisely and preserves historical trace readability.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update API/SSE/frontend/eval labels and safe final projection</name>
  <files>src/api/routers/agent_runs.py, src/agent/nodes/final_response.py, frontend/src/components/timeline/TimelineStep.tsx, scripts/eval_agent.py, tests/test_agent_runs_api.py, tests/test_trace_api.py, tests/agent/test_nodes/test_final_response.py, tests/agent/test_phase22_final_response.py</files>
  <read_first>
    .planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-CONTEXT.md
    .planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-RESEARCH.md
    src/api/routers/agent_runs.py
    src/agent/nodes/final_response.py
    frontend/src/components/timeline/TimelineStep.tsx
    scripts/eval_agent.py
    tests/agent/test_nodes/test_final_response.py
    tests/agent/test_phase22_final_response.py
    tests/test_agent_runs_api.py
    tests/test_trace_api.py
  </read_first>
  <behavior>
    - API/SSE node messages include `recommendation_generation` while retaining `generate_recommendation` fallback for historical events.
    - API payload extraction recognizes current `recommendation_generation` recommendation updates.
    - Frontend timeline labels `recommendation_generation` as `正在生成处理建议` and keeps legacy `generate_recommendation` readable.
    - Final response distinguishes insufficient evidence, unsafe/invalid RAG context, unsupported claim, manual review, and verifier error where those states are available.
    - Current-run final response authority comes from `claim_verification_bundle` first, then `verified_evidence_package`; legacy `rag_verification`, `verification_route`, `verifier_status`, and `verifier_reason_codes` cannot create an authoritative current-run payload when canonical safe projections are absent.
    - Debug projections and verifier-only projections do not leak through final/API output.
  </behavior>
  <action>
    Update `src/api/routers/agent_runs.py` so `NODE_MESSAGES` includes `recommendation_generation`, existing `generate_recommendation` remains as historical fallback, and `_extract_step_payload` extracts `recommendation_draft` for `node_name in {"recommendation_generation", "generate_recommendation"}`. Update `frontend/src/components/timeline/TimelineStep.tsx` with a `recommendation_generation` label and preserve the existing legacy label. Update `scripts/eval_agent.py` only where it asserts current active runtime node labels; keep explicitly historical fixtures labelled as historical. In `src/agent/nodes/final_response.py`, preserve and strengthen safe route payload behavior so user-visible wording/projection distinguishes `no_evidence` as insufficient evidence, unsafe RAG statuses as manual review/unsafe evidence, blocked/unsupported claims as unsupported claim, `manual_review` as manual review, and `error` as verifier error. Current-run route payload source priority must be: (1) `claim_verification_bundle`, (2) `verified_evidence_package`, (3) historical compatibility fallback only if explicitly labelled non-authoritative and unable to override canonical data. Historical fallback may be used only when an existing persisted-trace or compatibility-projection signal marks the payload as historical, such as graph vocabulary projection metadata, a historical trace implementation node, or another already-existing persisted trace marker; do not invent a new current-run state authority marker in this plan. If no reliable historical marker exists, `final_response` must stop constructing authoritative route payloads from legacy verifier fields and leave legacy display compatibility to API/trace projection tests. If no canonical `claim_verification_bundle` / `verified_evidence_package` is present in a current run, legacy `rag_verification`, `verification_route`, `verifier_status`, and `verifier_reason_codes` must not create current-run final response authority or mark a blocked/unsupported path safe. Add or update API/trace/final-response tests proving current-run `recommendation_generation` labels and payloads are exposed, historical `generate_recommendation` still projects to target without rewriting the original implementation node, canonical safe projections always win over legacy fields, legacy allow/block verifier fields cannot create current-run authority when canonical projections are absent, and sentinel debug/verifier projection strings do not appear in final/API output. Tests should assert semantic payload/source/reason fields rather than brittle exact Chinese final-response copy.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_trace.py tests/test_trace_api.py tests/test_agent_runs_api.py tests/agent/test_nodes/test_final_response.py tests/agent/test_phase22_final_response.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    `src/api/routers/agent_runs.py` and `frontend/src/components/timeline/TimelineStep.tsx` both contain `recommendation_generation`.
    API tests cover `target_node_name == "recommendation_generation"` for both current and historical recommendation events.
    Tests cover `node_name == "recommendation_generation"` recommendation payload extraction.
    Tests prove `verification_route`, `verifier_status`, and `verifier_reason_codes` cannot create current-run final response authority when `claim_verification_bundle` and `verified_evidence_package` are absent.
    Tests prove canonical `claim_verification_bundle` / `verified_evidence_package` payloads win over legacy verifier fields.
    Tests prove any retained historical fallback is gated by an existing historical/compatibility marker and is labelled non-authoritative; absent that marker, legacy verifier fields produce no authoritative current-run route payload.
    Tests prove debug/verifier projection sentinels are absent from final response/API output.
    No Phase 57 `risk_gate` active label replaces `assess_risk_and_approval`.
  </acceptance_criteria>
  <done>Runtime projection surfaces show canonical current labels, keep historical readability, and preserve safe final/API output boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs, architecture debt, validation artifact, and run closeout gates</name>
  <files>docs/current-langgraph-architecture.md, .planning/ARCHITECTURE-DEBT.md, .planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-VALIDATION.md</files>
  <read_first>
    AGENTS.md
    .planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-CONTEXT.md
    .planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-RESEARCH.md
    .planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-VALIDATION.md
    .planning/ARCHITECTURE-DEBT.md
    docs/current-langgraph-architecture.md
    docs/contract-spec.md
    docs/target-agent-platform-architecture-plan.md
    docs/architecture-overview.md
    docs/agent-architecture-routing-explanation.md
    docs/rag-architecture-spec.md
    README.md
    .planning/DEFERRED-DECISIONS.md
    .planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
  </read_first>
  <action>
    Update `docs/current-langgraph-architecture.md` so the current-source graph shows active `recommendation_generation`, documents retained `generate_recommendation` import/test/historical projection compatibility with Phase 58 delete scope, and states that `assess_risk_and_approval` remains Phase 57-owned. Inspect the Phase 50 Documentation Sync Checklist for `docs/contract-spec.md`, `docs/target-agent-platform-architecture-plan.md`, `docs/current-langgraph-architecture.md`, `docs/architecture-overview.md`, `docs/agent-architecture-routing-explanation.md`, `docs/rag-architecture-spec.md`, `README.md`, `.planning/ARCHITECTURE-DEBT.md`, `.planning/DEFERRED-DECISIONS.md`, and current phase artifacts; update any checklist file whose graph node names, route boundaries, or authority semantics become stale after Phase 56. For every checklist file intentionally left unchanged, record a concrete "no update needed" reason in `56-04-SUMMARY.md`. Append a Chinese Phase 56 entry to `.planning/ARCHITECTURE-DEBT.md` under the agent graph/RAG section recording the fixed active recommendation node debt, RAG/claim fail-closed alignment, retained compatibility surfaces, validation evidence, and remaining Phase 57/58 risks. Update `56-VALIDATION.md`: set `nyquist_compliant: true` and `wave_0_complete: true` only after the listed commands pass, mark completed task rows with evidence, and keep all commands using approved `UV_CACHE_DIR=/tmp/uv-cache uv run ...` entrypoints. Run the full command, Ruff command, artifact command scan, and `UV_CACHE_DIR=/tmp/uv-cache uv run git diff --check`. If any validation failure is a real local validation issue, append it to `.planning/LOCAL-VALIDATION-ISSUES.md` in Chinese per AGENTS.md before continuing.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/agent/test_graph.py tests/test_graph_routing.py tests/agent/test_rag_context_routing.py tests/agent/rag_context/test_routing.py tests/knowledge/test_verified_evidence_package.py tests/knowledge/test_claim_verification_bundle.py tests/agent/test_graph_vocabulary.py tests/agent/test_trace.py tests/test_trace_api.py tests/test_agent_runs_api.py tests/agent/test_nodes/test_generate_recommendation.py tests/agent/test_nodes/test_claim_verify.py tests/agent/test_nodes/test_final_response.py tests/agent/test_phase22_final_response.py tests/agent/test_phase22_recommendation_integration.py tests/knowledge/test_facade_integration.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent src/knowledge src/api tests/architecture tests/agent tests/knowledge tests/test_graph_routing.py tests/test_trace_api.py tests/test_agent_runs_api.py</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run python -c 'from pathlib import Path; bad=[str(p) for p in Path(".planning/phases/56-recommendation-generation-and-rag-claim-status-alignment").glob("56-*.md") if any(line.strip().startswith(("pytest","python -m pytest")) for line in p.read_text().splitlines())]; assert not bad, bad'</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run git diff --check</automated>
  </verify>
  <acceptance_criteria>
    `docs/current-langgraph-architecture.md` names active `recommendation_generation` and Phase 58 compatibility cleanup for `generate_recommendation`.
    Phase 50 Documentation Sync Checklist files are updated where Phase 56 changes graph semantics, and `56-04-SUMMARY.md` records "no update needed" reasons for every skipped checklist file.
    `.planning/ARCHITECTURE-DEBT.md` contains a Chinese Phase 56 entry with statuses and remaining Phase 57/58 risk.
    `56-VALIDATION.md` frontmatter has `nyquist_compliant: true` and `wave_0_complete: true` only after closeout commands pass.
    The full command, Ruff command, artifact command scan, and diff check all pass or any failure is recorded in `.planning/LOCAL-VALIDATION-ISSUES.md` with a concrete next entry point.
  </acceptance_criteria>
  <done>Phase 56 docs/debt/validation closeout is source-aligned, tested, and uses only MOCA-approved command entrypoints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Historical trace/API data -> current contract projection | Stored legacy node names remain readable without becoming active runtime nodes. |
| Verifier/debug projection -> user/API response | Internal projections must not leak to user-visible output. |
| Documentation/debt artifacts -> future execution phases | Phase 57 and Phase 58 boundaries must remain explicit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-06 | Availability | `graph_vocabulary.py`, trace/API projection | mitigate | Add Phase 56 alias and projection tests for historical `generate_recommendation` rows. |
| T-56-04 | Information disclosure / tampering | `final_response.py`, API tests | mitigate | Safe projection tests prove legacy verifier/debug fields cannot override or leak beyond canonical bundle projections. |
| T-56-02 | Elevation of privilege | docs/validation closeout | mitigate | Docs and validation artifact record fail-closed RAG statuses and command evidence. |
| T-56-05 | Tampering | docs/current architecture and debt ledger | mitigate | Docs/debt explicitly defer `risk_gate` active rename to Phase 57 and compatibility deletion to Phase 58. |
</threat_model>

<verification>
Quick command:
`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/agent/test_graph_vocabulary.py tests/agent/test_rag_context_routing.py tests/agent/rag_context/test_routing.py -q --tb=short`

Full command:
`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/agent/test_graph.py tests/test_graph_routing.py tests/agent/test_rag_context_routing.py tests/agent/rag_context/test_routing.py tests/knowledge/test_verified_evidence_package.py tests/knowledge/test_claim_verification_bundle.py tests/agent/test_graph_vocabulary.py tests/agent/test_trace.py tests/test_trace_api.py tests/test_agent_runs_api.py tests/agent/test_nodes/test_generate_recommendation.py tests/agent/test_nodes/test_claim_verify.py tests/agent/test_nodes/test_final_response.py tests/agent/test_phase22_final_response.py tests/agent/test_phase22_recommendation_integration.py tests/knowledge/test_facade_integration.py -q --tb=short`

Ruff command:
`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent src/knowledge src/api tests/architecture tests/agent tests/knowledge tests/test_graph_routing.py tests/test_trace_api.py tests/test_agent_runs_api.py`

Artifact command scan:
`UV_CACHE_DIR=/tmp/uv-cache uv run python -c 'from pathlib import Path; bad=[str(p) for p in Path(".planning/phases/56-recommendation-generation-and-rag-claim-status-alignment").glob("56-*.md") if any(line.strip().startswith(("pytest","python -m pytest")) for line in p.read_text().splitlines())]; assert not bad, bad'`
</verification>

<success_criteria>
Projection surfaces, user-visible response safety, docs, architecture debt, validation artifact, and closeout checks all reflect Phase 56 completion while preserving Phase 57 and Phase 58 boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-04-SUMMARY.md`.
</output>
