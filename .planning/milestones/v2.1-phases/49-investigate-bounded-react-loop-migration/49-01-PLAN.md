---
phase: 49-investigate-bounded-react-loop-migration
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/nodes/investigate.py
  - src/agent/nodes/investigate_planner.py
  - src/agent/prompts.py
  - tests/agent/test_nodes/test_investigate.py
autonomous: true
requirements: [GAD-01-IMPL]
requirements_addressed: [GAD-01-IMPL]
must_haves:
  truths:
    - "The LLM planner output contract is exactly one tool action `{next_tool,args,reason}` or one stop decision `{stop,stop_reason}`."
    - "The normal production path constructs and calls the default LLM structured planner; fake planners are test seams only."
    - "Planner output is schema validated before any dispatch decision."
    - "Planner tool choices are constrained to the eight `contract-spec.md` section 12.4 investigate read/retrieval tools."
    - "Current deterministic `plan_next_step` remains available as fallback and is not deleted first."
    - "Fallback stays read-only and still dispatches only through ToolPlatform."
  artifacts:
    - path: "src/agent/nodes/investigate.py"
      provides: "planner invocation seam, validation calls, fallback integration"
      contains: "plan_next_step"
    - path: "src/agent/nodes/investigate_planner.py"
      provides: "optional local planner schema/input validation helpers and default LLM planner wrapper"
      contains: "InvestigatePlanner"
    - path: "src/agent/prompts.py"
      provides: "planner system/user prompt text if the implementation follows existing node prompt patterns"
      contains: "INVESTIGATE"
    - path: "tests/agent/test_nodes/test_investigate.py"
      provides: "planner schema, invalid-output, write-tool, invalid-args, fallback regressions"
      contains: "planner"
  key_links:
    - from: "docs/contract-spec.md"
      to: "src/agent/nodes/investigate.py"
      via: "section 9.4 planner output and section 12.4 allowlist"
      pattern: "next_tool"
    - from: "src/tools/catalog.py"
      to: "src/agent/nodes/investigate.py"
      via: "ToolDescriptor input_schema validation"
      pattern: "input_schema"
---

<objective>
Introduce the bounded-ReAct planner contract, default production LLM structured planner, strict validation, and deterministic fallback shell without yet depending on full multi-step chaining.

Purpose: make the new main control path explicit and testable before changing loop-local slot behavior or expanding trace/replay. This plan must prove that the normal configured path calls the structured planner, invalid planner output cannot dispatch unsafe tools, and existing deterministic behavior remains a fallback safety net only.
Output: planner schema/input helpers, default LLM planner wrapper, validation gates, fallback integration, and focused tests using fake planners only as deterministic test seams.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-investigate-bounded-react-loop-migration/49-CONTEXT.md
@docs/contract-spec.md
@src/agent/nodes/investigate.py
@src/agent/nodes/classify_intent.py
@src/agent/nodes/extract_slots.py
@src/agent/nodes/generate_recommendation.py
@src/agent/prompts.py
@src/tools/catalog.py
@src/tools/contracts.py
@src/tools/platform.py
@src/tools/runtime.py
@src/tools/policy.py
@tests/agent/test_nodes/test_investigate.py

<source_audit>
SOURCE | ID | Requirement / Decision | Plan | Status
--- | --- | --- | --- | ---
CONTRACT | 9.4 | Planner returns exactly one tool action or stop decision | 49-01 | COVERED
CONTRACT | 12.4 | Investigate allowlist has eight read/retrieval tools | 49-01 | COVERED
CONTEXT | Locked fallback | `plan_next_step` is fallback, not deleted | 49-01 | COVERED
CONTEXT | ToolPlatform-only | No service/repository bypass from investigate | 49-01 | COVERED
USER | Tests 1-6 | normal tool, stop, invalid JSON/schema, write/unknown tool, invalid args | 49-01 | COVERED
</source_audit>

<file_scope>
Expected files:
- `src/agent/nodes/investigate.py`
- Optional `src/agent/nodes/investigate_planner.py` or equivalent investigate-local helper if the implementation would otherwise make `investigate.py` too large
- `src/agent/prompts.py` only for investigate planner prompt constants, following existing prompt style
- `tests/agent/test_nodes/test_investigate.py`

Do not modify:
- `docs/contract-spec.md`
- `src/agent/nodes/classify_intent.py`, `src/agent/intent_policy.py`, `IntentResultV3`, `TaskPlan`, `contextual_intent_resolve`
- `src/memory/**`, memory Phase 44-48 schema/service/repository/writer code
- `risk_gate`, `approval_gate`, `action_draft`, action executor logic
- `active_slots` field registry ownership
</file_scope>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define planner action/stop schema and validation tests</name>
  <read_first>
    - docs/contract-spec.md section 9.4 planner output contract
    - docs/contract-spec.md section 12.4 investigate allowlist
    - src/agent/nodes/investigate.py `plan_next_step`, `_select_tool_view`, `_tool_descriptor`, `_case_slots`
    - src/tools/catalog.py investigate declarations
    - src/tools/contracts.py `ToolDescriptor`, `ToolViewV1`
    - src/tools/runtime.py descriptor input validation behavior
    - tests/agent/test_nodes/test_investigate.py fake platform/planner patterns
  </read_first>
  <files>src/agent/nodes/investigate.py, src/agent/nodes/investigate_planner.py, tests/agent/test_nodes/test_investigate.py</files>
  <behavior>
    - Test 1: a planner result shaped as `{"next_tool": "get_order", "args": {"order_no": "ORD-1"}, "reason": "need order"}` validates as a single tool action.
    - Test 2: a planner result shaped as `{"stop": true, "stop_reason": "enough_evidence"}` validates as stop.
    - Test 3: malformed JSON, non-object output, extra mixed action+stop output, missing args/reason, or extra authority fields such as `route_decision`, `approval_required`, `proposed_action`, `confidence`, `evidence_refs` are rejected.
    - Test 4: `stop_reason` outside `enough_evidence`, `no_more_useful_tools`, `max_iterations_reached`, `unrecoverable_error` is rejected.
  </behavior>
  <action>
    Add a strict planner result parser/validator local to the investigate node. Prefer the project's existing validation style; if using Pydantic, forbid extra fields and model the output as exactly one of:
    ```python
    {"next_tool": str, "args": dict[str, Any], "reason": str}
    {"stop": bool, "stop_reason": Literal["enough_evidence", "no_more_useful_tools", "max_iterations_reached", "unrecoverable_error"]}
    ```

    Do not allow batch plans, multiple tools, route decisions, action requests, approval/risk fields, `evidence_refs`, or memory writes in planner output.

    Keep the implementation close to `investigate`. A new `src/agent/nodes/investigate_planner.py` is acceptable only for planner contracts/helpers; it must not import business, knowledge, memory, approval, or action services.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_investigate.py -k "planner or fallback or invalid" -q</automated>
  </verify>
  <acceptance_criteria>
    - Planner output validation is unit-tested without a real LLM.
    - Unknown/extra planner fields fail closed before any `ToolPlatform.invoke(...)` call.
    - `stop_reason` enum validation is explicit and test-covered.
  </acceptance_criteria>
  <done>Planner result shape is a strict local contract and cannot carry authority outside tool choice/stop.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate planner tool choice and args against descriptor schemas</name>
  <read_first>
    - src/tools/catalog.py all eight investigate tool declarations
    - src/tools/runtime.py `validate_json_value` / input validation path
    - src/tools/policy.py runtime availability semantics
    - src/agent/nodes/investigate.py current `ToolPlatform.visible_tools(...)` and `_select_tool_view(...)`
  </read_first>
  <files>src/agent/nodes/investigate.py, src/agent/nodes/investigate_planner.py, tests/agent/test_nodes/test_investigate.py</files>
  <behavior>
    - Test 1: planner-selected `create_coupon_grant_draft` or any other write/action tool is rejected before dispatch.
    - Test 2: planner-selected tool outside the eight-tool allowlist is rejected before dispatch.
    - Test 3: planner-selected allowed tool with args failing the descriptor `input_schema` triggers fallback or safe stop and never reaches `ToolPlatform.invoke(...)`.
    - Test 4: fallback-selected tool is also checked against the same allowlist and descriptor schema before dispatch.
  </behavior>
  <action>
    Define the planner-visible allowlist from the canonical tool declarations/descriptors rather than a second hand-written list where possible, but also assert that the resolved set equals:
    ```python
    {
        "get_order",
        "get_refund_case",
        "get_ticket",
        "get_logistics",
        "get_merchant_risk",
        "search_policy",
        "search_sop",
        "search_case_memory",
    }
    ```

    Validate planner `next_tool` with all of these checks before building a `ToolCallContext`:
    - the tool name is in the resolved §12.4 investigate allowlist;
    - the visible `ToolViewV1` exists for this node/user context;
    - the descriptor exposure/kind is read or retrieval, never write/action;
    - `args` conforms to the descriptor `input_schema`.

    Reuse the same validation helper for deterministic fallback output so fallback cannot become a bypass.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_investigate.py -k "allowlist or write_tool or invalid_args or fallback" -q</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/tools/test_catalog.py tests/tools/test_tool_platform.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "create_coupon_grant_draft" tests/agent/test_nodes/test_investigate.py` finds a write-tool rejection regression.
    - A fake platform test proves invalid planner args produce zero tool invocations.
    - The eight-tool allowlist is asserted in tests or a static guard.
  </acceptance_criteria>
  <done>Planner and fallback choices share one read-only allowlist/schema validation path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add default LLM planner invocation, test seam, and deterministic fallback shell</name>
  <read_first>
    - src/agent/nodes/investigate.py current loop body and scripted `_investigate_plan` test seam
    - src/agent/nodes/classify_intent.py `_get_llm().with_structured_output(...)` retry pattern
    - src/agent/nodes/extract_slots.py `_get_llm().with_structured_output(...)` retry pattern
    - src/agent/prompts.py existing node prompt constants
    - tests/agent/test_nodes/test_investigate.py existing fake manager/scripted plan tests
    - docs/contract-spec.md section 9.4 fallback/error semantics
  </read_first>
  <files>src/agent/nodes/investigate.py, src/agent/nodes/investigate_planner.py, tests/agent/test_nodes/test_investigate.py</files>
  <behavior>
    - Test 1: without `configurable["investigate_planner"]`, the normal path constructs `_get_llm().with_structured_output(...)` and uses the structured planner output.
    - Test 2: a fake planner returning a valid tool action executes one step through `ToolPlatform.invoke(...)`.
    - Test 3: a fake planner returning a valid stop decision ends the loop without tool dispatch and records the stop reason.
    - Test 4: planner timeout/unavailable/malformed output/invalid tool/invalid args triggers deterministic fallback or safe stop.
    - Test 5: the old deterministic behavior remains reachable through `plan_next_step(...)` and existing deterministic tests still pass.
  </behavior>
  <action>
    Implement the default production planner path using the same structured-output pattern already used by `classify_intent`, `extract_slots`, `assess_risk_and_approval`, and `generate_recommendation`: `_get_llm().with_structured_output(...)` plus bounded retry/timeout handling if the existing node pattern supports it. The default path must be active when no test planner is injected.

    Introduce a planner invocation seam suitable for tests without a real LLM. Use `configurable["investigate_planner"]` or the local node's existing injection pattern if one exists, but treat this as a test/override seam only. The production default must not be "planner unavailable" just because no injected fake exists.

    The planner should accept a structured planner input containing:
    - user query and normalized query;
    - primary intent and requested operation;
    - current resolved base slots;
    - loop-local discovered slots, initially empty in this plan;
    - projected observation summaries so far;
    - allowed tools descriptor summary;
    - iteration count;
    - previous attempted keys.

    Convert valid planner output into the existing internal "next step" shape. On timeout, exception, malformed output, invalid tool, invalid args, or LLM construction/invocation failure, call the deterministic fallback wrapper. Keep public `plan_next_step(...)` available because tests and downstream agents may already import it. If renaming is useful, wrap it rather than deleting it:
    ```python
    def plan_next_step(...): ...

    def _deterministic_fallback_plan_next_step(...):
        return plan_next_step(...)
    ```

    Record fallback reason in trace/debug state where current investigate tests already expose plan/failure metadata, but do not add authority fields. Add a test that proves fallback is not entered on the normal successful structured-planner path.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_investigate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The normal non-injected path calls `_get_llm().with_structured_output(...)` and consumes the structured planner result.
    - A valid fake planner action dispatches through `ToolPlatform.invoke(...)`.
    - A valid fake planner stop avoids dispatch and terminates deterministically.
    - Planner failures do not throw from the node unless the current code already treats the platform failure as unrecoverable.
    - Existing focused investigate tests pass.
  </acceptance_criteria>
  <done>The new planner can be exercised under test, and deterministic `plan_next_step` is a fallback safety net rather than the primary controller.</done>
</task>

</tasks>

<rollback_fallback_behavior>
- Roll back this plan by disabling the planner invocation seam and routing through existing `plan_next_step(...)`; no state schema or graph router changes should be required.
- Runtime fallback triggers are: planner timeout, LLM construction/invocation failure, exception, invalid JSON/schema, invalid tool, write tool, invisible tool, invalid args.
- Fallback must still pass through the same allowlist/input-schema validator and then `ToolPlatform.invoke(...)`.
</rollback_fallback_behavior>

<no_regression_checks>
- No diff in intent classifier contracts or tests except imports caused by test command collection.
- No diff in memory Phase 44-48 schema/service/repository/writer files.
- No diff in risk/approval/action node logic.
- `investigate` still does not write `active_slots`, `extracted_slots`, `candidate_slots`, or `evidence_refs`.
</no_regression_checks>

<minimal_test_commands>
```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_investigate.py -q
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/tools/test_catalog.py tests/tools/test_tool_platform.py -q
```
</minimal_test_commands>
