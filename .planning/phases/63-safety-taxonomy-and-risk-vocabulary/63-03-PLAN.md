---
phase: 63-safety-taxonomy-and-risk-vocabulary
plan: "03"
type: execute
wave: 2
depends_on:
  - "63-01"
files_modified:
  - src/agent/nodes/action_draft.py
  - tests/test_execute_action.py
  - tests/actions/test_action_draft_v2.py
  - tests/actions/test_phase34_action_draft_bindings.py
autonomous: true
requirements:
  - SC-63-1
  - SC-63-3
  - D-63-01
  - D-63-02
  - D-63-03
  - D-63-04
  - D-63-07
  - D-63-10
  - D-63-12
requirements_addressed:
  - SC-63-1
  - SC-63-3
  - D-63-01
  - D-63-02
  - D-63-03
  - D-63-04
  - D-63-07
  - D-63-10
  - D-63-12
must_haves:
  truths:
    - "Action draft consumes the canonical taxonomy owner instead of local action sets."
    - "Non-executable dispositions are rejected before ToolPlatform invocation."
    - "No new write tools, external execution, or action service broadening is introduced."
    - "Existing approval, auto-allowed, payload hash, and safety snapshot binding behavior remains green."
  artifacts:
    - path: "src/agent/nodes/action_draft.py"
      provides: "ToolPlatform action-draft boundary migrated to canonical executable action helpers"
      contains: "NON_EXECUTABLE_ACTION"
    - path: "tests/test_execute_action.py"
      provides: "Action draft ToolPlatform no-call regression for manual_review/blocked dispositions"
      contains: "assert_not_awaited"
    - path: "tests/actions/test_phase34_action_draft_bindings.py"
      provides: "Binding compatibility regression remains covered"
      contains: "AUTO_ALLOWED_BINDING_MISMATCH"
  key_links:
    - from: "src/agent/nodes/action_draft.py"
      to: "src/agent/safety/taxonomy.py"
      via: "imports canonical executable action resolver"
      pattern: "from src\\.agent\\.safety\\.taxonomy import"
    - from: "src/agent/nodes/action_draft.py"
      to: "src/tools/platform.py"
      via: "ToolPlatform invoke only after executable action validation"
      pattern: "tool_platform\\.invoke"
---

<objective>
Migrate `action_draft` to the shared taxonomy and enforce the executable-action boundary before any ToolPlatform write-tool call.

Purpose: The action side must not treat `manual_review` or `blocked` as callable action types, even if legacy risk or proposal payloads contain those strings.
Output: Action-draft tests and implementation updates that preserve demo draft behavior and binding checks while rejecting non-executable dispositions.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/63-safety-taxonomy-and-risk-vocabulary/63-CONTEXT.md
@.planning/phases/63-safety-taxonomy-and-risk-vocabulary/63-RESEARCH.md
@.planning/phases/63-safety-taxonomy-and-risk-vocabulary/63-PATTERNS.md
@.planning/phases/63-safety-taxonomy-and-risk-vocabulary/63-VALIDATION.md
@AGENTS.md
@src/agent/safety/taxonomy.py
@src/agent/nodes/action_draft.py
@src/tools/catalog.py
@src/tools/policy.py
@src/actions/schemas.py
@tests/test_execute_action.py
@tests/actions/test_action_draft_v2.py
@tests/actions/test_phase34_action_draft_bindings.py
@tests/architecture/test_action_draft_boundaries.py

<interfaces>
Action draft must preserve:
- `ACTION_TOOL_NAME = "create_coupon_grant_draft"`
- `caller_node="action_draft"` in `project_to_tool_context`
- `draft_outcome.status == "not_executed_demo"` and `external_side_effect is False`
- Existing approval and auto-allowed binding validators before draft persistence
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED action-draft non-executable disposition tests</name>
  <read_first>
    @src/agent/nodes/action_draft.py
    @src/agent/safety/taxonomy.py
    @tests/test_execute_action.py
    @tests/actions/test_action_draft_v2.py
    @tests/actions/test_phase34_action_draft_bindings.py
  </read_first>
  <files>tests/test_execute_action.py, tests/actions/test_action_draft_v2.py, tests/actions/test_phase34_action_draft_bindings.py</files>
  <behavior>
    - Test 1: legacy freeform reject/no-support action text returns a fail-closed action result and does not call `create_coupon_grant_draft`.
    - Test 2: `proposed_action["action_type"] == "manual_review"` returns error code `NON_EXECUTABLE_ACTION_DISPOSITION` and no `action_draft` / `draft_outcome`.
    - Test 3: `proposed_action["action_type"] == "blocked"` returns error code `NON_EXECUTABLE_ACTION_DISPOSITION` and no ToolPlatform invocation.
    - Test 4: executable aliases such as `compensation` still canonicalize to an allowed executable type without adding a new tool.
    - Test 5: existing approval and auto-allowed binding mismatch tests still prove no draft row is created on binding mismatch.
    - Test 6: non-executable action errors use the same safe shape as existing action boundary errors: `action_result.status == "error"`, `action_result.error.code` is stable, `action_result.error.message` does not expose raw taxonomy internals, the final trace step has `node == "action_draft"` and `status == "error"`, and `tool_name` is absent or empty because no tool was invoked.
  </behavior>
  <action>Update tests first. Rewrite `tests/test_execute_action.py::test_execute_action_canonicalizes_legacy_freeform_action_type` so it expects a fail-closed error and `create_draft.assert_not_awaited()` instead of `action_type == "manual_review"`. Add explicit approved-state tests for `manual_review` and `blocked` dispositions. Add a compatibility assertion that `compensation` input is accepted through the taxonomy as `issue_coupon` or another plan-63-owned executable alias, but does not create a new ToolCatalog tool. Keep existing `not_executed_demo`, binding mismatch, and no-draft assertions unchanged.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_execute_action.py tests/actions/test_action_draft_v2.py tests/actions/test_phase34_action_draft_bindings.py -q --tb=short; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>RED tests fail because current `action_draft` still canonicalizes disposition-shaped text to `manual_review` and can pass it toward ToolPlatform.</acceptance_criteria>
  <done>Action-draft non-executable disposition tests exist before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce executable action boundary before ToolPlatform</name>
  <read_first>
    @src/agent/nodes/action_draft.py
    @src/agent/safety/taxonomy.py
    @src/tools/catalog.py
    @src/tools/policy.py
    @tests/architecture/test_action_draft_boundaries.py
  </read_first>
  <files>src/agent/nodes/action_draft.py, tests/test_execute_action.py, tests/actions/test_action_draft_v2.py, tests/actions/test_phase34_action_draft_bindings.py</files>
  <action>Update `src/agent/nodes/action_draft.py` per D-63-01 through D-63-04. Remove local `FULL_REFUND_TERMS`, `ACTIONABLE_ACTIONS`, and `_canonical_action_type`. Import `canonical_executable_action_type` and/or `resolve_action_text` from `src.agent.safety.taxonomy`. After approval/auto-allowed binding eligibility is known but before `project_to_tool_context`, resolve `proposed.get("action_type")`. If resolution returns no executable action and a disposition (`manual_review` or `blocked`), return a structured `action_result` error with code `NON_EXECUTABLE_ACTION_DISPOSITION`, no `action_draft`, no `draft_outcome`, and a trace step with `node="action_draft"` and `status="error"`; do not call `_invoke_action_tool`. If resolution returns no executable action and no known disposition, return `NON_EXECUTABLE_ACTION_TYPE`. These new error codes are internal action-boundary contract values for tests/debug payloads; their messages must be generic enough not to expose registry internals, raw alias lists, tenant data, or hidden policy details. The error shape must stay compatible with existing `VERIFIER_NOT_ALLOW` / `AUTO_ALLOWED_BINDING_REQUIRED` action-result failures. If resolution returns an executable action, set `proposed["action_type"]` and ToolPlatform `args["action_type"]` to that canonical executable value. Preserve `ACTION_TOOL_NAME`, `caller_node="action_draft"`, trusted context projection, safety snapshot refs, approval/auto-allowed binding checks, demo `draft_outcome`, and compatibility `action_result` behavior. Do not edit `src/tools/catalog.py`, `src/actions/service.py`, or add service-level allowlists unless a failing test proves it is directly required for Phase 63 safety; otherwise record broader service hardening as Phase 67 per D-63-16.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_execute_action.py tests/actions/test_action_draft_v2.py tests/actions/test_phase34_action_draft_bindings.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_safety_taxonomy.py tests/architecture/test_action_draft_boundaries.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/nodes/action_draft.py tests/test_execute_action.py tests/actions/test_action_draft_v2.py tests/actions/test_phase34_action_draft_bindings.py</automated>
  </verify>
  <acceptance_criteria>`action_draft.py` imports the shared taxonomy, has no local canonical action function or action sets, rejects `manual_review`/`blocked` before ToolPlatform with stable safe error payloads, and keeps all existing action draft binding/demo tests green.</acceptance_criteria>
  <done>Action draft is migrated to the canonical executable-action boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proposed_action -> ToolPlatform args | Proposed action type is untrusted until canonical executable validation passes. |
| approval/auto-allowed binding -> draft persistence | Existing exact hash/snapshot binding must remain authoritative. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-09 | Elevation of Privilege | `action_draft.action_draft` | mitigate | Reject `manual_review` and `blocked` before ToolPlatform; tests assert tool is not awaited and error payloads do not leak taxonomy internals. |
| T-63-10 | Tampering | action type canonicalization | mitigate | Use `src.agent.safety.taxonomy` helpers and remove local action sets/functions. |
| T-63-11 | Repudiation | approval/auto-allowed binding | mitigate | Keep Phase 34 binding tests green and do not change hash/snapshot matching semantics. |
| T-63-12 | Elevation of Privilege | external execution scope | mitigate | Do not add real external execution, new write tools, or service-level broadening in this plan. |
</threat_model>

<verification>
Run the plan-focused commands:
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_execute_action.py tests/actions/test_action_draft_v2.py tests/actions/test_phase34_action_draft_bindings.py -q --tb=short`
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_safety_taxonomy.py tests/architecture/test_action_draft_boundaries.py -q --tb=short`
- `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/nodes/action_draft.py tests/test_execute_action.py tests/actions/test_action_draft_v2.py tests/actions/test_phase34_action_draft_bindings.py`

If execution discovers a tool-call architecture defect or validates/fixes one, append the verified outcome to `.planning/ARCHITECTURE-DEBT.md` in Chinese. If validation/debug failures are encountered and handled, append them to `.planning/LOCAL-VALIDATION-ISSUES.md`.
</verification>

<success_criteria>
SC-63-3 is satisfied for action draft when route-side action checks and execution-side draft checks use the same taxonomy and no non-executable disposition can reach ToolPlatform.
</success_criteria>

<output>
After completion, create `.planning/phases/63-safety-taxonomy-and-risk-vocabulary/63-03-SUMMARY.md`.
</output>
