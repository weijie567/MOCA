---
phase: 63-safety-taxonomy-and-risk-vocabulary
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/safety/__init__.py
  - src/agent/safety/taxonomy.py
  - tests/agent/test_safety_taxonomy.py
autonomous: true
requirements:
  - SC-63-1
  - SC-63-2
  - D-63-01
  - D-63-02
  - D-63-03
  - D-63-04
  - D-63-05
  - D-63-06
  - D-63-13
requirements_addressed:
  - SC-63-1
  - SC-63-2
  - D-63-01
  - D-63-02
  - D-63-03
  - D-63-04
  - D-63-05
  - D-63-06
  - D-63-13
must_haves:
  truths:
    - "Backend has one canonical owner for action taxonomy and risk vocabulary."
    - "Executable action types are separated from non-executable safety dispositions."
    - "Compatibility aliases are accepted without adding new write tools or external execution."
    - "Risk severity normalization never treats manual_review or blocked as severity values."
  artifacts:
    - path: "src/agent/safety/taxonomy.py"
      provides: "Immutable safety taxonomy registry and helper API"
      contains: "SAFETY_TAXONOMY"
    - path: "src/agent/safety/taxonomy.py"
      provides: "Read-only alias/cue helper surface for downstream risk_gate and intent_policy migration"
      contains: "action_aliases_for"
    - path: "src/agent/safety/__init__.py"
      provides: "Stable package exports for safety taxonomy consumers"
      contains: "__all__"
    - path: "tests/agent/test_safety_taxonomy.py"
      provides: "RED-to-GREEN parity tests for aliases, executable actions, dispositions, and risk vocabulary"
      contains: "test_manual_review_and_blocked_are_dispositions_not_actions"
  key_links:
    - from: "tests/agent/test_safety_taxonomy.py"
      to: "src/agent/safety/taxonomy.py"
      via: "direct import of canonical helper names"
      pattern: "from src\\.agent\\.safety\\.taxonomy import"
    - from: "src/agent/safety/__init__.py"
      to: "src/agent/safety/taxonomy.py"
      via: "explicit re-export"
      pattern: "from src\\.agent\\.safety\\.taxonomy import"
---

<objective>
Create the canonical safety taxonomy owner that later plans consume from risk gate, action draft, intent policy, and routing.

Purpose: Phase 63 must stop action/risk vocabulary drift at the source before migrating callers.
Output: `src/agent/safety/taxonomy.py`, `src/agent/safety/__init__.py`, and RED-to-GREEN taxonomy parity tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/63-safety-taxonomy-and-risk-vocabulary/63-CONTEXT.md
@.planning/phases/63-safety-taxonomy-and-risk-vocabulary/63-RESEARCH.md
@.planning/phases/63-safety-taxonomy-and-risk-vocabulary/63-PATTERNS.md
@.planning/phases/63-safety-taxonomy-and-risk-vocabulary/63-VALIDATION.md
@AGENTS.md
@CLAUDE.md
@src/business/query/registry.py
@src/agent/intent_policy.py
@src/agent/graph_vocabulary.py
@src/agent/nodes/risk_gate.py
@src/agent/nodes/action_draft.py
@tests/agent/test_intent_policy_registry.py

<interfaces>
Existing duplicate behavior to capture before migration:
- `src/agent/nodes/risk_gate.py` currently defines `FULL_REFUND_TERMS`, `ACTIONABLE_ACTIONS`, `_is_actionable_recommendation(action)`, and `_canonical_action_type(action)`.
- `src/agent/nodes/action_draft.py` currently defines the same `FULL_REFUND_TERMS`, `ACTIONABLE_ACTIONS`, and `_canonical_action_type(action)`.
- `src/agent/schemas.py` defines `RiskAssessment.risk_level: Literal["low", "medium", "high"]`; `manual_review` and `blocked` must not become valid severities.
- `src/approvals/schemas.py` keeps `RiskDecisionV1.risk_level: str` for persisted/API compatibility.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED safety taxonomy parity tests</name>
  <read_first>
    @.planning/phases/63-safety-taxonomy-and-risk-vocabulary/63-CONTEXT.md
    @.planning/phases/63-safety-taxonomy-and-risk-vocabulary/63-PATTERNS.md
    @src/agent/nodes/risk_gate.py
    @src/agent/nodes/action_draft.py
    @tests/agent/test_intent_policy_registry.py
  </read_first>
  <files>tests/agent/test_safety_taxonomy.py</files>
  <behavior>
    - Test 1: importing `src.agent.safety.taxonomy` exposes stable helper names and immutable public collections.
    - Test 2: `manual_review` and `blocked` are non-executable dispositions and never appear in `EXECUTABLE_ACTION_TYPES`.
    - Test 3: aliases for `issue_coupon`, `approve_refund`, `full_refund`, and `partial_refund` preserve current risk/draft input acceptance.
    - Test 4: `compensation`, `compensate`, `coupon`, `补偿`, `券`, and `赔付` resolve to executable `issue_coupon` as compatibility aliases, not as new write tools.
    - Test 5: reject/no-support text such as `reject`, `拒绝`, `不建议`, and `无法支持` resolves to disposition `manual_review`, not to an executable action type.
    - Test 6: risk normalization maps legacy `risk_level` values `manual_review` and `blocked` to explicit dispositions while returning severity only from `low|medium|high`.
    - Test 7: pre-route action keyword matching identifies existing direct execution/refund/coupon terms without accepting approval-chat hard negatives.
    - Test 8: read-only alias helper APIs expose `action_aliases_for("full_refund")`, `action_aliases_for("issue_coupon")`, `pre_route_action_aliases()`, `matches_full_refund_alias(text)`, and `matches_compensation_alias(text)` so downstream callers do not recreate local tuples.
    - Test 9: `ActionResolution` exposes stable fields `raw_value`, `executable_action_type`, `disposition`, `matched_alias`, and `match_kind`; `resolve_action_text("full refund")` includes a matched alias and executable action, while `resolve_action_text("拒绝")` includes a matched alias and disposition.
  </behavior>
  <action>Create `tests/agent/test_safety_taxonomy.py` first, per D-63-13. The tests must import the intended public surface from `src.agent.safety.taxonomy`: `SAFETY_TAXONOMY`, `EXECUTABLE_ACTION_TYPES`, `NON_EXECUTABLE_DISPOSITIONS`, `RISK_SEVERITIES`, `RISK_DISPOSITIONS`, `ActionResolution`, `RiskVocabulary`, `PreRouteActionMatch`, `canonical_executable_action_type`, `resolve_action_text`, `is_executable_action_type`, `is_actionable_recommendation`, `detect_pre_route_action_request`, `action_aliases_for`, `pre_route_action_aliases`, `matches_full_refund_alias`, `matches_compensation_alias`, `normalize_risk_vocabulary`, and `risk_assessment_with_disposition`. Assert current input parity for the duplicated risk/draft canonicalizers, but assert the Phase 63 semantic split: `manual_review` and `blocked` are dispositions, not executable action IDs. This RED task intentionally fails until Task 2 creates the module.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_safety_taxonomy.py -q --tb=short; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>The new test file exists, fails for the missing taxonomy module/helper surface, and covers D-63-01 through D-63-06 plus D-63-13.</acceptance_criteria>
  <done>RED parity tests are written and observed failing before production taxonomy implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement immutable safety taxonomy registry</name>
  <read_first>
    @tests/agent/test_safety_taxonomy.py
    @src/business/query/registry.py
    @src/agent/graph_vocabulary.py
    @src/agent/intent_policy.py
  </read_first>
  <files>src/agent/safety/__init__.py, src/agent/safety/taxonomy.py, tests/agent/test_safety_taxonomy.py</files>
  <action>Create `src/agent/safety/taxonomy.py` using the frozen descriptor/read-only registry pattern from `src/business/query/registry.py`. Define frozen dataclasses with slots: `ExecutableActionDescriptor`, `ActionResolution`, `RiskVocabulary`, and `PreRouteActionMatch`. `ActionResolution` must expose `raw_value`, `executable_action_type`, `disposition`, `matched_alias`, and `match_kind` so callers can branch without reparsing strings. Define a module-level `SAFETY_TAXONOMY` owner that builds alias maps from descriptor data. Required executable action IDs are `issue_coupon`, `approve_refund`, `full_refund`, and `partial_refund`; `compensation` is a compatibility alias for `issue_coupon`, not a new external write tool, per D-63-03. Required non-executable dispositions are `manual_review` and `blocked`, per D-63-02. Required risk severities are `low`, `medium`, and `high`; required risk dispositions are `allow`, `approval_required`, `manual_review`, and `blocked`, per D-63-05. Implement helper functions listed in Task 1 with read-only return values (`frozenset`, tuple, or `MappingProxyType`) and no imports from graph nodes. `action_aliases_for(action_type)` must return a read-only `frozenset`; `pre_route_action_aliases()` must return a read-only mapping of pre-route alias groups; `matches_full_refund_alias(text)` and `matches_compensation_alias(text)` must consume the same registry-owned alias data used by `resolve_action_text`. Create `src/agent/safety/__init__.py` with explicit `__all__` re-exports. Do not edit `risk_gate.py`, `action_draft.py`, `intent_policy.py`, or `routing.py` in this plan; downstream plans own caller migration.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_safety_taxonomy.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/safety/__init__.py src/agent/safety/taxonomy.py tests/agent/test_safety_taxonomy.py</automated>
  </verify>
  <acceptance_criteria>`tests/agent/test_safety_taxonomy.py` passes; taxonomy data is immutable; `manual_review` and `blocked` cannot resolve as executable actions; downstream alias helper APIs exist so callers do not recreate local tuples; legacy `risk_level` compatibility is normalized into explicit severity/disposition fields.</acceptance_criteria>
  <done>Canonical safety taxonomy owner exists and is ready for risk, draft, intent, and routing consumers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user/LLM text -> taxonomy helpers | Untrusted strings are normalized before they become action IDs or routing decisions. |
| taxonomy owner -> downstream graph nodes | A shared registry becomes the source of truth for safety decisions in later plans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | Tampering | `src/agent/safety/taxonomy.py` | mitigate | Use immutable descriptors and read-only maps; tests assert public collections cannot be mutated. |
| T-63-02 | Elevation of Privilege | `manual_review` / `blocked` classification | mitigate | Tests require these values to be dispositions only and reject them from `EXECUTABLE_ACTION_TYPES`. |
| T-63-03 | Spoofing | action keyword alias matching | mitigate | Pre-route/action matching accepts only registry-owned aliases and keeps approval-chat hard negatives out. |
| T-63-04 | Information Disclosure | taxonomy test data | accept | Unit tests use synthetic strings only and do not touch tenant/customer data. |
</threat_model>

<verification>
Run the plan-focused commands:
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_safety_taxonomy.py -q --tb=short`
- `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/safety/__init__.py src/agent/safety/taxonomy.py tests/agent/test_safety_taxonomy.py`

If any local validation, startup, RAG/agent/tool-call, or test-environment failure is discovered while executing, append the handled incident to `.planning/LOCAL-VALIDATION-ISSUES.md` in Chinese per `AGENTS.md`.
</verification>

<success_criteria>
SC-63-1 foundation is satisfied when one immutable safety taxonomy owner exists, tests prove executable-vs-disposition separation, and downstream callers can import the stable helper/alias API without graph-node imports or caller-local tuple recreation.
</success_criteria>

<output>
After completion, create `.planning/phases/63-safety-taxonomy-and-risk-vocabulary/63-01-SUMMARY.md`.
</output>
