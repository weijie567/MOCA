---
phase: 48-narrow-long-term-explicit-preference-memory
plan: 04
type: execute
wave: 4
depends_on: [48-03]
files_modified:
  - src/memory/repository.py
  - src/memory/long_term.py
  - src/memory/context_service.py
  - tests/memory/test_long_term_memory_repository.py
  - tests/memory/test_long_term_memory_service.py
  - tests/memory/test_reviewed_memory_context_boundary.py
  - tests/agent/test_reviewed_memory_context_retrieve.py
  - tests/agent/test_memory_evidence_boundary.py
  - tests/test_memory_review_api.py
  - tests/memory/test_phase48_long_term_preference_alignment.py
  - .planning/ARCHITECTURE-DEBT.md
autonomous: true
requirements: [MEM-05]
requirements_addressed: [MEM-05]
must_haves:
  truths:
    - "retrieval returns only published preference rows from explicit_user_preference, explicit_admin_preference, or human_reviewed"
    - "review approval of needs-review long-term candidates publishes source_type as human_reviewed"
    - "semantic_episode_candidate never appears as a published prompt source"
    - "same-scope corrections use supersede and deletions use tombstone/forget; similar preferences are not auto-merged"
    - "reviewed long-term memory remains contextual-only and cannot satisfy policy evidence, business fact, approval/action, audit, or replay authority"
  artifacts:
    - path: "src/memory/repository.py"
      provides: "published preference-only long-term retrieval predicate"
      contains: "PUBLISHED_LONG_TERM_SOURCE_TYPES"
    - path: "src/memory/long_term.py"
      provides: "approval publishing converts reviewed candidates to human_reviewed"
      contains: "human_reviewed"
    - path: ".planning/ARCHITECTURE-DEBT.md"
      provides: "memory subsystem architecture debt trace for Phase 48 narrowing"
      contains: "Phase 48"
  key_links:
    - from: "src/memory/repository.py"
      to: "src/memory/context_service.py"
      via: "context service relies on repository-filtered long-term preference rows"
      pattern: "retrieve_profile_memory"
    - from: "src/memory/long_term.py"
      to: "src/api/routers/memory.py"
      via: "review API approve path publishes pending preference candidates as human_reviewed"
      pattern: "approve_memory"
---

<objective>
Finish retrieval, review publishing, correction, tombstone, and final validation for Phase 48.

Purpose: After write paths are narrowed, prompt-facing retrieval and human review must enforce the same preference-only semantics. Reviewed automatic candidates must not publish with automatic source types, and existing supersede/tombstone behavior must remain the correction path.
Output: retrieval predicate filters, approval source conversion, reviewed-context tests, correction/tombstone tests, final architecture debt record, and full Phase 48 validation.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ARCHITECTURE-DEBT.md
@.planning/phases/48-narrow-long-term-explicit-preference-memory/48-CONTEXT.md
@.planning/phases/48-narrow-long-term-explicit-preference-memory/48-RESEARCH.md
@.planning/phases/48-narrow-long-term-explicit-preference-memory/48-VALIDATION.md
@.planning/phases/48-narrow-long-term-explicit-preference-memory/48-PATTERNS.md
@.planning/phases/48-narrow-long-term-explicit-preference-memory/48-03-SUMMARY.md
@src/memory/repository.py
@src/memory/long_term.py
@src/memory/context_service.py
@src/api/routers/memory.py
@tests/memory/test_long_term_memory_repository.py
@tests/memory/test_long_term_memory_service.py
@tests/memory/test_reviewed_memory_context_boundary.py
@tests/agent/test_reviewed_memory_context_retrieve.py
@tests/agent/test_memory_evidence_boundary.py
@tests/test_memory_review_api.py

<source_audit>
SOURCE | ID | Requirement / Decision | Plan | Status
--- | --- | --- | --- | ---
ROADMAP | Phase 48 SC1 | Retrieval is preference-only and not authority | 48-04 | COVERED
ROADMAP | Phase 48 SC3 | Writes/reviews audited and PII/review governed | 48-04 | COVERED
CONTEXT | D-48-08 | semantic_episode_candidate publishes as human_reviewed after approval | 48-04 | COVERED
CONTEXT | D-48-13-D-48-16 | Keep retrieval seam but return only published preference rows | 48-04 | COVERED
CONTEXT | D-48-20-D-48-22 | Corrections use supersede/tombstone; no auto semantic merge | 48-04 | COVERED
AGENTS | Architecture debt rule | Memory subsystem design fix must be recorded in `.planning/ARCHITECTURE-DEBT.md` | 48-04 | COVERED
</source_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Filter prompt retrieval to published preference rows only</name>
  <read_first>
    - src/memory/repository.py `retrieve_profile_memory`, `_to_long_term_view`, `_long_term_semantic_kind`
    - src/memory/policy.py `PUBLISHED_LONG_TERM_SOURCE_TYPES`
    - src/memory/context_service.py `load_reviewed_memory_context`
    - tests/memory/test_long_term_memory_repository.py retrieval exclusion tests
    - tests/memory/test_reviewed_memory_context_boundary.py reviewed memory context tests
    - tests/agent/test_reviewed_memory_context_retrieve.py node tests
  </read_first>
  <files>src/memory/repository.py, src/memory/context_service.py, tests/memory/test_long_term_memory_repository.py, tests/memory/test_reviewed_memory_context_boundary.py, tests/agent/test_reviewed_memory_context_retrieve.py, tests/memory/test_phase48_long_term_preference_alignment.py</files>
  <behavior>
    - Test 1: approved `memory_kind="fact"` row does not return from `retrieve_profile_memory(...)`.
    - Test 2: approved `memory_kind="pattern"` or `memory_kind="constraint"` row does not return.
    - Test 3: approved preference row with `source_type="deterministic_tool_result"` does not return.
    - Test 4: approved preference row with `source_type="semantic_episode_candidate"` does not return.
    - Test 5: approved/auto-approved preference rows with `explicit_user_preference`, `explicit_admin_preference`, or `human_reviewed` do return.
    - Test 6: `MemoryContextService.load_reviewed_memory_context(...)` returns only those filtered long-term rows while keeping case memory behavior unchanged.
  </behavior>
  <action>
    In `src/memory/repository.py`, import `PUBLISHED_LONG_TERM_SOURCE_TYPES` from `src.memory.policy` and add these filters to `LongTermMemoryRepository.retrieve_profile_memory(...)`:
    ```python
    LongTermMemory.memory_kind == "preference",
    LongTermMemory.source_type.in_(tuple(PUBLISHED_LONG_TERM_SOURCE_TYPES)),
    ```
    Keep all existing filters for tenant, scope, review status, deletion, `is_current`, expiry, prompt-safe PII, and tombstone.

    Update `_long_term_semantic_kind(...)` so preference rows still project as `merchant_preference`. You may leave fallback handling for legacy rows, but repository retrieval must prevent legacy non-preference rows from reaching prompts.

    Do not make `MemoryContextService.load_reviewed_memory_context(...)` duplicate the same source/kind filters if repository filtering is sufficient. Add a narrow assertion/test that context service receives the repository-filtered rows and preserves existing case memory behavior.

    Extend `tests/memory/test_phase48_long_term_preference_alignment.py` with a static guard named `test_phase48_retrieval_filters_published_preference_sources` that asserts `src/memory/repository.py` contains `PUBLISHED_LONG_TERM_SOURCE_TYPES` and `memory_kind == "preference"`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_long_term_memory_repository.py tests/memory/test_reviewed_memory_context_boundary.py tests/agent/test_reviewed_memory_context_retrieve.py tests/memory/test_phase48_long_term_preference_alignment.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/memory/repository.py` contains `PUBLISHED_LONG_TERM_SOURCE_TYPES`.
    - `src/memory/repository.py` contains `LongTermMemory.memory_kind == "preference"`.
    - `tests/memory/test_long_term_memory_repository.py` contains `test_retrieve_profile_memory_returns_only_published_preference_sources`.
    - `tests/memory/test_reviewed_memory_context_boundary.py` contains `deterministic_tool_result` in a long-term exclusion assertion.
    - The automated pytest command exits 0.
  </acceptance_criteria>
  <done>Prompt-facing long-term retrieval returns only current, prompt-safe, non-tombstoned published preference rows from approved source types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish reviewed candidates as human_reviewed and validate correction lifecycle</name>
  <read_first>
    - src/memory/long_term.py `approve_memory`, `supersede_memory`, `forget_long_term_memory`, `delete_memory`
    - src/memory/repository.py `update_review_status`, `forget_memory`
    - src/api/routers/memory.py `_run_long_term_action`
    - tests/memory/test_long_term_memory_service.py review/supersede tests
    - tests/test_memory_review_api.py memory review API tests
    - tests/agent/test_memory_evidence_boundary.py reviewed memory boundary tests
    - .planning/ARCHITECTURE-DEBT.md memory subsystem section
  </read_first>
  <files>src/memory/long_term.py, tests/memory/test_long_term_memory_service.py, tests/test_memory_review_api.py, tests/agent/test_memory_evidence_boundary.py, .planning/ARCHITECTURE-DEBT.md</files>
  <behavior>
    - Test 1: approving a needs-review preference candidate with `source_type="semantic_episode_candidate"` sets row `source_type == "human_reviewed"` and `source_ref_json["source_type"] == "human_reviewed"`.
    - Test 2: approved semantic preference candidate retrieves as `source_type="human_reviewed"`, never `semantic_episode_candidate`.
    - Test 3: approving a needs-review non-preference long-term row raises `ValueError("long-term approval requires preference memory")`.
    - Test 4: superseding a published preference with an explicit user/admin preference replacement keeps one current row and emits `decision="supersede"`.
    - Test 5: a pending review-required supersede keeps the previous preference current until approval; after approval, replacement publishes as `human_reviewed`.
    - Test 6: forget/delete tombstone blocks same content/source identity rewrites.
    - Test 7: writing a similar but not identical same-scope preference does not auto-merge or supersede; both remain current unless `supersede_memory(...)` is called explicitly.
  </behavior>
  <action>
    In `src/memory/long_term.py::approve_memory(...)`, after loading the pending row and before or during status update, enforce:
    - if `memory.memory_kind != "preference"`, raise `ValueError("long-term approval requires preference memory")`;
    - set `memory.source_type = "human_reviewed"`;
    - set `memory.source_ref_json = {**dict(memory.source_ref_json or {}), "source_type": "human_reviewed"}` while preserving run/event/business object keys;
    - then publish with `review_status="approved"` and `is_current=True` through the existing repository path.

    Ensure `approve_memory(...)` still emits `MemoryWriteEvent(decision="write", reason_code=reason_code, memory_type="long_term_fact")` with the candidate hash for the approved row. If candidate hash changes because source type is converted before event generation, update tests to assert the returned event hash equals `_candidate_hash_for_memory(memory)` after source conversion, not the pre-approval candidate hash.

    Update long-term service tests and review API tests to expect reviewed long-term candidate publication as `human_reviewed`. Keep case memory approval behavior unchanged.

    Add tests that correction uses `supersede_memory(...)` and forget/delete uses tombstone behavior. Do not add automatic semantic merge. Add an explicit test named `test_similar_long_term_preferences_do_not_auto_merge_without_supersede` that writes two different same-scope explicit preferences and asserts both current rows exist until supersede is called.

    Append a verified Phase 48 entry to `.planning/ARCHITECTURE-DEBT.md` under the memory subsystem section. Use status `✅ planned fix` or `✅ fixed after execution` depending on execution state. The entry must distinguish target contract from implementation facts and mention: published long-term memory narrowed to explicit preferences, broad durable source auto-publish removed, semantic episode is candidate-only, and retrieval is preference-source filtered.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_long_term_memory_service.py tests/test_memory_review_api.py tests/agent/test_memory_evidence_boundary.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/memory/long_term.py` contains `long-term approval requires preference memory`.
    - `src/memory/long_term.py` contains `"source_type": "human_reviewed"` or equivalent source_ref assignment.
    - `tests/memory/test_long_term_memory_service.py` contains `test_approve_semantic_episode_preference_candidate_publishes_as_human_reviewed`.
    - `tests/memory/test_long_term_memory_service.py` contains `test_similar_long_term_preferences_do_not_auto_merge_without_supersede`.
    - `.planning/ARCHITECTURE-DEBT.md` contains `Phase 48`.
    - The automated pytest command exits 0.
  </acceptance_criteria>
  <done>Human-reviewed long-term candidates publish with human_reviewed source type, and correction/deletion behavior remains explicit via supersede/tombstone.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 48 validation and sync static guards</name>
  <read_first>
    - .planning/phases/48-narrow-long-term-explicit-preference-memory/48-VALIDATION.md
    - tests/memory/test_phase48_long_term_preference_alignment.py
    - tests/architecture/test_memory_contract_delta.py
    - docs/contract-spec.md Section 13.3
    - docs/memory-contract-delta.md
  </read_first>
  <files>tests/memory/test_phase48_long_term_preference_alignment.py, .planning/ARCHITECTURE-DEBT.md</files>
  <action>
    Run the Full Phase Gate from `48-VALIDATION.md`. If any failure is caused by Phase 48 implementation drift, fix it in the relevant source or test file before completing this plan. If any failure is caused by a local environment issue, record it in `.planning/LOCAL-VALIDATION-ISSUES.md` according to AGENTS.md and rerun the command with the approved `UV_CACHE_DIR=/tmp/uv-cache uv run pytest` entrypoint.

    Confirm the static Phase 48 test file checks all final invariants:
    - contract says explicit preference memory only;
    - storage identity is preserved;
    - semantic episode projection is preference-only;
    - repository retrieval filters by `PUBLISHED_LONG_TERM_SOURCE_TYPES` and `memory_kind == "preference"`;
    - Phase 48 plan/validation artifacts use the approved pytest entrypoint.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_phase48_long_term_preference_alignment.py tests/architecture/test_memory_contract_delta.py tests/memory/test_memory_policy.py tests/memory/test_long_term_memory_service.py tests/memory/test_long_term_memory_repository.py tests/memory/test_memory_write_service.py tests/memory/test_semantic_episode_projection.py tests/memory/test_reviewed_memory_context_boundary.py tests/agent/test_memory_write_node.py tests/agent/test_reviewed_memory_context_retrieve.py tests/agent/test_memory_evidence_boundary.py tests/test_memory_review_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The full Phase 48 pytest command exits 0, or any non-code environment issue is recorded in `.planning/LOCAL-VALIDATION-ISSUES.md` with evidence and rerun entrypoint.
    - `tests/memory/test_phase48_long_term_preference_alignment.py` contains `test_phase48_retrieval_filters_published_preference_sources`.
    - `.planning/ARCHITECTURE-DEBT.md` contains `published long-term memory narrowed to explicit preferences`.
  </acceptance_criteria>
  <done>All Phase 48 gates are green or documented with project-approved local validation issue handling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository -> prompt context | Retrieved rows are prompt-visible contextual memory and must be preference-only. |
| review queue -> published memory | Human approval can convert candidates into prompt-visible memory. |
| correction/delete intent -> memory lifecycle | Same-scope changes must use explicit supersede/tombstone behavior, not hidden semantic merging. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-09 | I | `LongTermMemoryRepository.retrieve_profile_memory` | mitigate | Add preference kind and published source type filters before prompt projection. |
| T-48-10 | T/R | `LongTermMemoryService.approve_memory` | mitigate | Convert approved candidate source to `human_reviewed` and reject non-preference approvals. |
| T-48-11 | T | `supersede_memory`, `forget_memory` | mitigate | Keep explicit supersede/tombstone paths and add no-auto-merge regression tests. |
| T-48-12 | E/I | reviewed memory prompt context | mitigate | Boundary tests prove reviewed memory remains contextual-only and cannot satisfy policy/business/action/replay authority. |
| T-48-13 | R | final validation | mitigate | Full Phase 48 pytest gate and architecture debt trace record the memory-subsystem narrowing. |
</threat_model>

<verification>
Run the final Phase 48 gate:
```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest \
  tests/memory/test_phase48_long_term_preference_alignment.py \
  tests/architecture/test_memory_contract_delta.py \
  tests/memory/test_memory_policy.py \
  tests/memory/test_long_term_memory_service.py \
  tests/memory/test_long_term_memory_repository.py \
  tests/memory/test_memory_write_service.py \
  tests/memory/test_semantic_episode_projection.py \
  tests/memory/test_reviewed_memory_context_boundary.py \
  tests/agent/test_memory_write_node.py \
  tests/agent/test_reviewed_memory_context_retrieve.py \
  tests/agent/test_memory_evidence_boundary.py \
  tests/test_memory_review_api.py \
  -q
```
</verification>

<success_criteria>
- Long-term prompt retrieval returns only explicit/human-reviewed preference rows.
- Reviewed automatic candidates publish as `human_reviewed`, never as `semantic_episode_candidate`.
- Corrections use supersede and deletions use tombstone/forget.
- Similar preferences are not auto-merged.
- Memory remains contextual-only and cannot satisfy policy evidence, current business facts, approval/action authority, audit truth, or replay truth.
- Architecture debt and validation artifacts reflect the implemented Phase 48 narrowing.
</success_criteria>

<output>
After completion, create `.planning/phases/48-narrow-long-term-explicit-preference-memory/48-04-SUMMARY.md`.
</output>
