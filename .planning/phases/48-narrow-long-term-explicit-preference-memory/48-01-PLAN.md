---
phase: 48-narrow-long-term-explicit-preference-memory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/contract-spec.md
  - docs/architecture-overview.md
  - docs/memory-contract-delta.md
  - .planning/MEMORY-REDESIGN-DECISIONS.md
  - tests/architecture/test_memory_contract_delta.py
  - tests/memory/test_phase48_long_term_preference_alignment.py
autonomous: true
requirements: [MEM-05]
requirements_addressed: [MEM-05]
must_haves:
  truths:
    - "long_term_memories is documented as explicit preference memory only, not durable fact/profile/pattern/rule storage"
    - "published long-term source types are documented as explicit_user_preference, explicit_admin_preference, and human_reviewed only"
    - "semantic_episode_candidate is documented as needs-review preference candidate input only, never a published long-term source type"
    - "long_term_memories table identity and memory_type='long_term_fact' identity are preserved"
    - "ordinary completed runs and generic run summaries are documented as forbidden long-term write sources"
  artifacts:
    - path: "docs/contract-spec.md"
      provides: "normative Section 13.3 preference-only target contract"
      contains: "explicit preference memory only"
    - path: "tests/memory/test_phase48_long_term_preference_alignment.py"
      provides: "Phase 48 static docs, storage identity, and pytest entrypoint guards"
      contains: "test_phase48_contract_says_long_term_is_explicit_preference_only"
    - path: "tests/architecture/test_memory_contract_delta.py"
      provides: "architecture delta tests updated away from broad durable long-term semantics"
      contains: "explicit preference memory only"
  key_links:
    - from: "docs/contract-spec.md"
      to: "docs/memory-contract-delta.md"
      via: "both documents name the same explicit preference-only source contract"
      pattern: "explicit_user_preference"
---

<objective>
Lock the Phase 48 target contract before runtime code changes.

Purpose: The normative memory contract currently allows broad durable facts and deterministic tool facts as long-term memory. Phase 48 must first narrow the target semantics so subsequent code changes do not silently drift from the spec.
Output: updated contract/docs/decision trace plus static tests that preserve table identity, approved pytest entrypoints, and preference-only long-term semantics.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/MEMORY-REDESIGN-DECISIONS.md
@.planning/phases/48-narrow-long-term-explicit-preference-memory/48-CONTEXT.md
@.planning/phases/48-narrow-long-term-explicit-preference-memory/48-RESEARCH.md
@.planning/phases/48-narrow-long-term-explicit-preference-memory/48-VALIDATION.md
@.planning/phases/48-narrow-long-term-explicit-preference-memory/48-PATTERNS.md
@docs/contract-spec.md
@docs/architecture-overview.md
@docs/memory-contract-delta.md
@tests/architecture/test_memory_contract_delta.py
@src/db/models.py

<source_audit>
SOURCE | ID | Requirement / Decision | Plan | Status
--- | --- | --- | --- | ---
ROADMAP | Phase 48 SC1 | Document and test-lock long-term as explicit preference memory only | 48-01 | COVERED
ROADMAP | Phase 48 SC4 | Preserve `long_term_memories` identity unless reviewed migration proves need | 48-01 | COVERED
CONTEXT | D-48-06 | Published long-term source types narrowed to explicit user/admin/human reviewed | 48-01 | COVERED
CONTEXT | D-48-07/D-48-08 | semantic episode is candidate-only and publish source becomes human_reviewed | 48-01 | COVERED
CONTEXT | D-48-23/D-48-24 | Update `docs/contract-spec.md` Section 13.3 target state, not MVP note only | 48-01 | COVERED
RESEARCH | Contract drift | `memory-contract-delta` and architecture tests still preserve durable fact semantics | 48-01 | COVERED
</source_audit>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the normative long-term memory contract</name>
  <read_first>
    - docs/contract-spec.md Section 13.3, 13.5, 13.6
    - docs/architecture-overview.md memory read/write and node contract sections
    - docs/memory-contract-delta.md `Long-term Memory` and write policy sections
    - .planning/MEMORY-REDESIGN-DECISIONS.md DEFER-3 and Phase 45-47 planning traces
    - .planning/phases/48-narrow-long-term-explicit-preference-memory/48-CONTEXT.md D-48-01 through D-48-24
  </read_first>
  <files>docs/contract-spec.md, docs/architecture-overview.md, docs/memory-contract-delta.md, .planning/MEMORY-REDESIGN-DECISIONS.md</files>
  <action>
    Update `docs/contract-spec.md` Section 13.3 so its first sentence says `Long-term memory stores explicit preference memory only`. The section must state these exact target semantics:
    - Published long-term memory is a contextual preference/hint layer, not operational business state, policy authority, approval/action authority, audit truth, replay truth, or generic run summary storage.
    - Published source types are only `explicit_user_preference`, `explicit_admin_preference`, and `human_reviewed`.
    - `semantic_episode_candidate` may exist only as a `needs_review` preference candidate source; when approved, the published row's `source_type` must become `human_reviewed`.
    - `deterministic_tool_result`, `confirmed_business_outcome`, `approved_approval_state`, `llm_candidate`, `summary_candidate`, `cross_case_pattern_candidate`, `behavior_inference`, run summaries, strategy hints, similar-case hints, and cross-case pattern candidates must not become published prompt-usable long-term memory.
    - Soft operational preferences are allowed as hints, for example `Prefer calming explanatory wording in low-amount refund scenarios`; hard rules such as `must refund`, `must reject`, or policy thresholds belong to policy/config/rule systems, not memory.
    - Default non-admin scope is merchant preference. Tenant-scope preference requires explicit admin save. User-specific preference is deferred to post-Phase 48.
    - The legacy table name `long_term_memories` and identity label `memory_type='long_term_fact'` are preserved for compatibility; Phase 48 narrows service/retrieval semantics without a schema rename.

    Update Section 13.5 so the long-term write policy says explicit user preference, explicit admin preference, and human-reviewed preference are the only publishable long-term write paths. Keep tombstone, supersede, PII, source-ref identity, and audit requirements. Replace any wording that says deterministic tool facts or approved business outcomes may auto-publish as long-term memory with preference-only wording.

    Update `docs/architecture-overview.md` so memory architecture rows describe `long_term_memory_retrieve` / `memory_context_load` as reviewed explicit preference retrieval, not an empty adapter or broad profile/pattern memory. Do not edit `docs/target-agent-platform-architecture-plan.md` in this plan because it is currently dirty outside Phase 48; use `docs/memory-contract-delta.md` as the reconciliation layer.

    Update `docs/memory-contract-delta.md` `Long-term Memory` and write policy sections to supersede the older durable profile/fact/pattern/constraint table. Keep historical implementation facts clearly labeled as legacy/previous target where useful, but the current target contract must say `explicit preference memory only`. Remove target-state claims that deterministic durable tool results can auto-publish as long-term memory.

    Add a Phase 48 planning trace to `.planning/MEMORY-REDESIGN-DECISIONS.md` stating that DEFER-3 is now planned as explicit preference-only long-term memory; published source types are `explicit_user_preference`, `explicit_admin_preference`, and `human_reviewed`; semantic episode output is candidate-only and publishes as `human_reviewed`; table identity remains unchanged.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_memory_contract_delta.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `docs/contract-spec.md` contains `Long-term memory stores explicit preference memory only`.
    - `docs/contract-spec.md` contains `explicit_user_preference`, `explicit_admin_preference`, and `human_reviewed`.
    - `docs/contract-spec.md` contains `semantic_episode_candidate` and `source_type` and `human_reviewed` in Section 13.3.
    - `docs/contract-spec.md` contains `memory_type='long_term_fact'`.
    - `docs/memory-contract-delta.md` contains `explicit preference memory only`.
    - `docs/memory-contract-delta.md` does not contain the target-state sentence `deterministic durable tool results can auto-publish`.
    - `.planning/MEMORY-REDESIGN-DECISIONS.md` contains `Phase 48 planning trace`.
    - The automated pytest command exits 0.
  </acceptance_criteria>
  <done>The normative contract and architecture delta no longer preserve broad durable long-term memory semantics as the Phase 48 target.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Phase 48 static alignment and entrypoint guards</name>
  <read_first>
    - tests/memory/test_phase47_case_precedent_alignment.py
    - tests/memory/test_phase46_session_context_alignment.py
    - tests/architecture/test_memory_contract_delta.py
    - src/db/models.py `LongTermMemory`, `CaseMemory`, `SessionMemory`, `CaseWorkingContext`, `ConversationThread`, `ThreadCaseLink`
    - .planning/phases/48-narrow-long-term-explicit-preference-memory/48-01-PLAN.md
  </read_first>
  <files>tests/memory/test_phase48_long_term_preference_alignment.py, tests/architecture/test_memory_contract_delta.py</files>
  <action>
    Create `tests/memory/test_phase48_long_term_preference_alignment.py` using the static-test style from Phase 46/47. Include tests named exactly:
    - `test_phase48_contract_says_long_term_is_explicit_preference_only`
    - `test_phase48_contract_forbids_long_term_business_state_policy_authority_and_run_summary`
    - `test_phase48_preserves_memory_storage_identity`
    - `test_phase48_plans_use_project_pytest_entrypoint`

    The contract test must read `docs/contract-spec.md` and assert the document contains these strings: `explicit preference memory only`, `explicit_user_preference`, `explicit_admin_preference`, `human_reviewed`, `semantic_episode_candidate`, `needs_review`, `memory_type='long_term_fact'`, `not operational business state`, `not policy authority`, `not approval/action authority`, and `not generic run summary storage`.

    The storage identity test must read `src/db/models.py` and assert these protected strings still exist: `__tablename__ = "long_term_memories"`, `__tablename__ = "case_memories"`, `__tablename__ = "session_memories"`, `__tablename__ = "case_working_contexts"`, `__tablename__ = "thread_case_links"`, and `case_id: Mapped[str | None]` or `case_id` on `ConversationThread`. It must also scan Phase 48 plan files for destructive instructions against those names after stripping fenced code blocks and prohibition language. Unsafe imperative matches should include `drop_table`, `rename_table`, `drop_column`, `rename_column`, `DROP TABLE`, and `ALTER TABLE ... RENAME`.

    The pytest entrypoint test must scan `48-*.md` in the phase directory for pytest commands and assert every command starts with `UV_CACHE_DIR=/tmp/uv-cache uv run pytest`. A bare `pytest` or bare `python -m pytest` in Phase 48 artifacts must fail this test.

    Update `tests/architecture/test_memory_contract_delta.py` so it asserts the new `docs/memory-contract-delta.md` target language includes `explicit preference memory only`, `explicit_user_preference`, `explicit_admin_preference`, and `human_reviewed`, and no longer requires `durable_profile_fact`, `merchant_pattern`, `operational_constraint`, or deterministic tool-result auto-publish as target semantics.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_phase48_long_term_preference_alignment.py tests/architecture/test_memory_contract_delta.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/memory/test_phase48_long_term_preference_alignment.py` contains `test_phase48_contract_says_long_term_is_explicit_preference_only`.
    - `tests/memory/test_phase48_long_term_preference_alignment.py` contains `test_phase48_preserves_memory_storage_identity`.
    - `tests/memory/test_phase48_long_term_preference_alignment.py` contains `UV_CACHE_DIR=/tmp/uv-cache uv run pytest`.
    - `tests/architecture/test_memory_contract_delta.py` contains `explicit preference memory only`.
    - `tests/architecture/test_memory_contract_delta.py` no longer requires `durable_profile_fact` as a target assertion.
    - The automated pytest command exits 0.
  </acceptance_criteria>
  <done>Phase 48 has executable static locks for contract semantics, storage identity, and MOCA-approved test command usage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract -> implementation | The normative spec controls what source policy, write paths, and retrieval are allowed to build. |
| plan artifacts -> execution | Executors follow PLAN.md commands and file lists. Bad plan wording could create destructive memory migrations or invalid test commands. |
| legacy docs -> target docs | Older durable-memory docs can silently preserve rejected semantics if not reconciled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | T | `docs/contract-spec.md` | mitigate | Rewrite Section 13.3 with explicit preference-only published semantics and source type allowlist. |
| T-48-02 | T/I | `tests/memory/test_phase48_long_term_preference_alignment.py` | mitigate | Static tests protect storage identity and forbid destructive plan instructions. |
| T-48-03 | R | Phase 48 plan artifacts | mitigate | Static test enforces `UV_CACHE_DIR=/tmp/uv-cache uv run pytest` entrypoint so verification is attributable to the project environment. |
| T-48-04 | I/E | `docs/memory-contract-delta.md` | mitigate | Update architecture delta tests so older durable fact/pattern semantics cannot remain the target contract. |
</threat_model>

<verification>
Run the docs/static gate:
```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_phase48_long_term_preference_alignment.py tests/architecture/test_memory_contract_delta.py -q
```
</verification>

<success_criteria>
- Contract Section 13.3 says long-term memory is explicit preference memory only.
- Architecture/delta docs no longer preserve deterministic durable fact auto-publish as target long-term semantics.
- Static tests preserve table identity and approved pytest entrypoints.
- No schema migration or destructive storage rename is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/48-narrow-long-term-explicit-preference-memory/48-01-SUMMARY.md`.
</output>
