---
phase: 34-approval-and-actiondraft-boundary-hardening
plan: 34-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/approvals/schemas.py
  - src/actions/schemas.py
  - src/db/models.py
  - src/db/migrations/versions/018_phase34_approval_action_bindings.py
  - tests/approvals/test_phase34_boundary_bindings.py
  - tests/actions/test_phase34_action_draft_bindings.py
  - tests/approvals/test_migration_contract.py
  - tests/actions/test_action_draft_v2.py
autonomous: true
requirements:
  - APF-15
user_setup: []
must_haves:
  truths:
    - "Approval and action draft contracts expose target merchant, business fact, verified evidence, claim verification, risk decision, exact hash, and snapshot bindings."
    - "ApprovalRequest and ActionDraft persistence has first-class nullable legacy-safe fields for Phase 34 bindings."
    - "New v2 approval/action rows can be verified from persisted fields, not reconstructed from prompts, memory, raw tool payloads, or LLM text."
  artifacts:
    - path: "src/approvals/schemas.py"
      provides: "ActionProposal/RiskDecision/approval result binding fields"
      contains: "class RiskDecisionV1"
    - path: "src/actions/schemas.py"
      provides: "ActionDraftV2Data enriched with Phase 34 safe refs"
      contains: "business_fact_refs"
    - path: "src/db/models.py"
      provides: "ApprovalRequest and ActionDraft ORM binding columns"
      contains: "target_merchant_id"
    - path: "src/db/migrations/versions/018_phase34_approval_action_bindings.py"
      provides: "Alembic expansion for approval/action binding fields"
      contains: "phase34_approval_action_bindings"
  key_links:
    - from: "src/approvals/schemas.py"
      to: "src/tools/contracts.py"
      via: "BusinessFactRefV1 typed authority refs"
      pattern: "BusinessFactRefV1"
    - from: "src/actions/schemas.py"
      to: "src/approvals/schemas.py"
      via: "shared risk and target binding field names"
      pattern: "risk_decision_ref|target_merchant_id"
    - from: "src/db/migrations/versions/018_phase34_approval_action_bindings.py"
      to: "src/db/models.py"
      via: "column names match ORM attributes"
      pattern: "target_merchant_id"
---

<objective>
Create the Phase 34 contract and persistence foundation for approval/action boundary bindings.

Purpose: satisfy APF-15 and decisions D-01..D-05, D-11..D-12, D-20, D-24, and D-28..D-31 before graph, approval API, or action draft code consumes new authority fields.
Output: strict DTO fields, ORM columns, Alembic migration, and focused contract/migration tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-approval-and-actiondraft-boundary-hardening/34-CONTEXT.md
@.planning/phases/34-approval-and-actiondraft-boundary-hardening/34-RESEARCH.md
@.planning/phases/34-approval-and-actiondraft-boundary-hardening/34-VALIDATION.md
@.planning/phases/34-approval-and-actiondraft-boundary-hardening/34-PATTERNS.md
@docs/contract-spec.md
@docs/target-agent-platform-architecture-plan.md
@.planning/todos/deferred/2026-06-27-merchant-scope-approval-action.md
@src/approvals/schemas.py
@src/actions/schemas.py
@src/db/models.py
@src/tools/contracts.py

<interfaces>
Existing approval command/result surface:
```python
class ApprovalRequestCreateCommand(BaseModel): ...
class ApprovalDecisionCommand(BaseModel): ...
class TrustedApprovalResultV1(BaseModel): ...
```

Existing action draft surface:
```python
class ActionDraftV2Data(BaseModel): ...
class DraftOutcomeV1(BaseModel): ...
```

Existing business fact ref authority:
```python
class BusinessFactRefV1(BaseModel):
    schema_version: Literal["business_fact_ref.v1"]
    tenant_id: str
    source_system: str
    resource_type: Literal["order", "refund_case", "ticket", "logistics", "merchant_risk"]
    resource_id: str
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add strict Phase 34 binding contracts</name>
  <files>src/approvals/schemas.py, src/actions/schemas.py, tests/approvals/test_phase34_boundary_bindings.py, tests/actions/test_phase34_action_draft_bindings.py</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/34-approval-and-actiondraft-boundary-hardening/34-CONTEXT.md
    - .planning/phases/34-approval-and-actiondraft-boundary-hardening/34-PATTERNS.md
    - docs/contract-spec.md sections 15.3, 15.4, 16.1-16.4
    - src/approvals/schemas.py
    - src/actions/schemas.py
    - src/tools/contracts.py
    - src/knowledge/schemas.py
    - tests/approvals/test_hash_binding.py
    - tests/actions/test_action_draft_v2.py
  </read_first>
  <behavior>
    - Test strict DTOs reject unknown fields for `RiskDecisionV1` and enriched `ActionDraftV2Data`.
    - Test `ApprovalRequestCreateCommand` can carry target merchant, business fact refs, verified evidence refs, claim verification summary, risk decision, payload hash, and safety snapshot refs.
    - Test `TrustedApprovalResultV1` carries the exact target merchant and binding refs needed by action_draft validation.
  </behavior>
  <action>
    Add `RISK_DECISION_SCHEMA_VERSION = "risk_decision.v1"` and `AUTO_ALLOWED_ACTION_BINDING_SCHEMA_VERSION = "auto_allowed_action_binding.v1"` to `src/approvals/schemas.py`.

    Add strict Pydantic models in `src/approvals/schemas.py`:
    - `RiskDecisionV1` with `schema_version: Literal["risk_decision.v1"]`, `tenant_id: str`, `run_id: str`, `action_id: str`, `action_payload_hash: str`, `risk_level: str`, `reason_codes: list[str]`, `policy_config_version: str`, `risk_config_version: str`, `approval_required: bool`, `evaluated_at: str`, `risk_rule_ref: str | None = None`, and `risk_reason: str | None = None`.
    - `TargetMerchantBindingV1` with `schema_version: Literal["target_merchant_binding.v1"] = "target_merchant_binding.v1"`, `target_merchant_id: str`, `source: Literal["business_fact_ref", "business_fact_result"]`, and `business_fact_ref: dict[str, Any]`.
    - `AutoAllowedActionBindingV1` with `schema_version: Literal["auto_allowed_action_binding.v1"]`, `tenant_id: str`, `run_id: str`, `target_merchant_id: str`, `action_payload_hash: str`, `safety_snapshot_ref: str`, `safety_snapshot_hash: str`, `risk_decision_ref: str`, `idempotency_key: str`, `business_fact_refs: list[BusinessFactRefV1]`, `verified_evidence_refs: list[EvidenceRefV1]`, `claim_verification_ref: str | None = None`, and `claim_verification_summary: dict[str, Any] | None = None`.

    Extend `ApprovalRequestCreateCommand`, `ApprovalRequestCreateResult`, `TrustedApprovalResultV1`, and `ApprovalDecisionResult` with these concrete fields: `target_merchant_id: str | None = None`, `target_merchant_ref: TargetMerchantBindingV1 | dict[str, Any] | None = None`, `business_fact_refs: list[BusinessFactRefV1] = Field(default_factory=list)`, `verified_evidence_refs: list[EvidenceRefV1] = Field(default_factory=list)`, `claim_verification_ref: str | None = None`, `claim_verification_summary: dict[str, Any] | None = None`, `risk_decision_ref: str | None = None`, `risk_decision: RiskDecisionV1 | dict[str, Any] | None = None`, and `approval_idempotency_key: str | None = None`. Keep existing `evidence_refs` as a compatibility input; do not remove it in this plan.

    Extend `ActionDraftV2Data` in `src/actions/schemas.py` with `target_merchant_id: str | None`, `target_merchant_ref: dict[str, Any] | None`, `business_fact_refs: list[dict[str, Any]]`, `verified_evidence_refs: list[dict[str, Any]]`, `claim_verification_ref: str | None`, `claim_verification_summary: dict[str, Any] | None`, `risk_decision_ref: str | None`, `risk_decision: dict[str, Any] | None`, and `auto_allowed_binding_ref: str | None = None`.

    Do not add external execution, outbox, reconciliation, compensation, adapter, or action execution record contracts in this task per D-25.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/approvals/test_phase34_boundary_bindings.py tests/actions/test_phase34_action_draft_bindings.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/approvals/schemas.py src/actions/schemas.py tests/approvals/test_phase34_boundary_bindings.py tests/actions/test_phase34_action_draft_bindings.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "class RiskDecisionV1|AUTO_ALLOWED_ACTION_BINDING_SCHEMA_VERSION|class TargetMerchantBindingV1" src/approvals/schemas.py` returns matches.
    - `rg -n "target_merchant_id|business_fact_refs|verified_evidence_refs|claim_verification_summary|risk_decision_ref" src/approvals/schemas.py src/actions/schemas.py` returns matches in both files.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/approvals/test_phase34_boundary_bindings.py tests/actions/test_phase34_action_draft_bindings.py -q --tb=short` exits 0.
    - No new `action_executions`, `action_outbox_events`, `action_reconciliation_jobs`, or `action_compensation_records` symbols are introduced by this task.
  </acceptance_criteria>
  <done>Strict Phase 34 binding DTOs exist, tests prove required fields and extra-field rejection, and no real-execution contract surface was added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist approval and draft binding fields</name>
  <files>src/db/models.py, src/db/migrations/versions/018_phase34_approval_action_bindings.py, tests/approvals/test_migration_contract.py, tests/actions/test_action_draft_v2.py</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/34-approval-and-actiondraft-boundary-hardening/34-CONTEXT.md
    - .planning/phases/34-approval-and-actiondraft-boundary-hardening/34-RESEARCH.md
    - .planning/phases/34-approval-and-actiondraft-boundary-hardening/34-PATTERNS.md
    - src/db/models.py
    - src/db/migrations/versions/008_approval_state_machine.py
    - src/db/migrations/versions/009_action_draft_v2.py
    - tests/approvals/test_migration_contract.py
    - tests/actions/test_action_draft_v2.py
  </read_first>
  <behavior>
    - Test ORM metadata and migration source declare the same Phase 34 columns for `approval_requests`.
    - Test ORM metadata and migration source declare the same Phase 34 columns for `action_drafts`.
    - Test no external execution/outbox/reconciliation/compensation table is created by migration 018.
  </behavior>
  <action>
    Create `src/db/migrations/versions/018_phase34_approval_action_bindings.py` with revision id `"018_phase34_approval_action_bindings"` and `down_revision = "017_tool_policy_events"`.

    Add nullable legacy-safe columns to `ApprovalRequest` and `approval_requests`: `target_merchant_id UUID`, `target_merchant_ref JSONB`, `business_fact_refs JSONB`, `verified_evidence_refs JSONB`, `claim_verification_ref String(128)`, `claim_verification_summary JSONB`, `risk_decision_ref String(128)`, `risk_decision JSONB`, and `approval_idempotency_key String(256)`.

    Add nullable legacy-safe columns to `ActionDraft` and `action_drafts`: `target_merchant_id UUID`, `target_merchant_ref JSONB`, `business_fact_refs JSONB`, `verified_evidence_refs JSONB`, `claim_verification_ref String(128)`, `claim_verification_summary JSONB`, `risk_decision_ref String(128)`, `risk_decision JSONB`, and `auto_allowed_binding_ref String(128)`.

    Add indexes with these exact names: `ix_approval_requests_tenant_target_merchant_status` over `(tenant_id, target_merchant_id, status)`, `ix_approval_requests_tenant_risk_decision_ref` over `(tenant_id, risk_decision_ref)`, `ix_action_drafts_tenant_target_merchant` over `(tenant_id, target_merchant_id)`, and `ix_action_drafts_tenant_risk_decision_ref` over `(tenant_id, risk_decision_ref)`.

    Keep all new columns nullable in migration 018 so legacy rows remain non-executable through existing `legacy_non_executable` and service guards instead of destructive backfill. Do not create `action_executions`, `action_outbox_events`, `action_reconciliation_jobs`, `action_compensation_records`, external adapter tables, or dispatch records.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/approvals/test_migration_contract.py tests/actions/test_action_draft_v2.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run alembic upgrade head</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/db/models.py src/db/migrations/versions/018_phase34_approval_action_bindings.py tests/approvals/test_migration_contract.py tests/actions/test_action_draft_v2.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "target_merchant_id|business_fact_refs|verified_evidence_refs|claim_verification_ref|risk_decision_ref|auto_allowed_binding_ref" src/db/models.py src/db/migrations/versions/018_phase34_approval_action_bindings.py` returns matches.
    - `rg -n "ix_approval_requests_tenant_target_merchant_status|ix_action_drafts_tenant_target_merchant" src/db/models.py src/db/migrations/versions/018_phase34_approval_action_bindings.py` returns matches.
    - `rg -n "action_executions|action_outbox_events|action_reconciliation_jobs|action_compensation_records" src/db/migrations/versions/018_phase34_approval_action_bindings.py` returns no matches.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/approvals/test_migration_contract.py tests/actions/test_action_draft_v2.py -q --tb=short` exits 0.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run alembic upgrade head` exits 0.
  </acceptance_criteria>
  <done>ApprovalRequest and ActionDraft persistence has Phase 34 binding fields, migration 018 upgrades cleanly, and external execution storage remains absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graph state -> approval/action contracts | LLM- and graph-produced material becomes typed service input only after strict DTO validation. |
| ORM/migration -> service authorization | Persisted binding fields become authorization facts for later manager scope and draft creation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01-01 | Tampering | `src/approvals/schemas.py` | mitigate | Add strict `RiskDecisionV1`, `TargetMerchantBindingV1`, and binding fields with `extra="forbid"` so untyped prompt/LLM fields cannot become approval authority. |
| T-34-01-02 | Information Disclosure | `ActionDraftV2Data` / ORM JSON fields | mitigate | Store safe refs and summaries; do not add raw external execution payloads, outbox rows, or full execution records. |
| T-34-01-03 | Repudiation | migration 018 | mitigate | Add explicit target merchant/risk/claim/hash/snapshot columns and named indexes so later approval decisions and draft records can be audited from persisted fields. |
</threat_model>

<verification>
Run after both tasks:

```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/approvals/test_phase34_boundary_bindings.py tests/actions/test_phase34_action_draft_bindings.py tests/approvals/test_migration_contract.py tests/actions/test_action_draft_v2.py -q --tb=short
UV_CACHE_DIR=/tmp/uv-cache uv run alembic upgrade head
UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/approvals/schemas.py src/actions/schemas.py src/db/models.py src/db/migrations/versions/018_phase34_approval_action_bindings.py tests/approvals/test_phase34_boundary_bindings.py tests/actions/test_phase34_action_draft_bindings.py tests/approvals/test_migration_contract.py tests/actions/test_action_draft_v2.py
```
</verification>

<success_criteria>
- APF-15 contract/persistence foundation exists for approval requests and action drafts.
- New DTOs and ORM fields include `target_merchant_id`, `business_fact_refs`, `verified_evidence_refs`, `claim_verification_ref` or `claim_verification_summary`, `risk_decision_ref` or `risk_decision`, exact payload hash, and safety snapshot refs.
- Migration 018 is additive, legacy-safe, and does not create real execution storage.
</success_criteria>

<output>
After completion, create `.planning/phases/34-approval-and-actiondraft-boundary-hardening/34-01-SUMMARY.md`.
</output>
