---
phase: 34-approval-and-actiondraft-boundary-hardening
plan: 34-04
type: execute
wave: 4
depends_on:
  - 34-01
  - 34-02
  - 34-03
files_modified:
  - src/api/routers/agent_runs.py
  - src/api/schemas/agent_runs.py
  - tests/test_agent_runs_api.py
autonomous: true
requirements:
  - APF-15
  - APF-16
user_setup: []
must_haves:
  truths:
    - "The agent_runs approval-interrupt bridge preserves Phase 34 target merchant/business/evidence/claim/risk bindings when creating ApprovalRequest rows."
    - "Live run/SSE approval_required payloads expose only safe refs/summaries and approval revision refs, not raw evidence packages, verifier debug, or action authority bodies."
    - "Spoofed interrupt run/action identity remains fail-closed and cannot create approval rows for attacker-chosen runs."
  artifacts:
    - path: "src/api/routers/agent_runs.py"
      provides: "binding-aware interrupt-to-ApprovalService command bridge"
      contains: "ApprovalRequestCreateCommand"
    - path: "src/api/schemas/agent_runs.py"
      provides: "safe run status response shape for approval interruptions"
      contains: "approval_id"
    - path: "tests/test_agent_runs_api.py"
      provides: "regression coverage for preserved Phase 34 approval interrupt bindings"
      contains: "claim_verification"
  key_links:
    - from: "src/agent/nodes/approval_gate.py"
      to: "src/api/routers/agent_runs.py"
      via: "structured interrupt payload fields are consumed without being dropped"
      pattern: "business_fact_refs|verified_evidence_refs|claim_verification"
    - from: "src/api/routers/agent_runs.py"
      to: "src/approvals/service.py"
      via: "ApprovalRequestCreateCommand carries the exact interrupt bindings into ApprovalService"
      pattern: "ApprovalRequestCreateCommand"
---

<objective>
Close the live run approval-interrupt bridge so Phase 34 bindings survive the API boundary.

Purpose: satisfy APF-15/APF-16 after ApprovalService persists binding fields and before ActionDraft relies on trusted approval results.
Output: binding-aware `agent_runs` interrupt command mapping, safe approval_required SSE/run projection, and regression tests proving the bridge does not drop target merchant/business/evidence/claim/risk refs.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-approval-and-actiondraft-boundary-hardening/34-CONTEXT.md
@.planning/phases/34-approval-and-actiondraft-boundary-hardening/34-PATTERNS.md
@.planning/phases/34-approval-and-actiondraft-boundary-hardening/34-VALIDATION.md
@docs/contract-spec.md
@src/api/routers/agent_runs.py
@src/api/schemas/agent_runs.py
@src/approvals/schemas.py
@src/approvals/service.py
@tests/test_agent_runs_api.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve Phase 34 bindings in interrupt approval creation</name>
  <files>src/api/routers/agent_runs.py, tests/test_agent_runs_api.py</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/34-approval-and-actiondraft-boundary-hardening/34-CONTEXT.md
    - .planning/phases/34-approval-and-actiondraft-boundary-hardening/34-PATTERNS.md
    - docs/contract-spec.md sections 9.5, 10, 15.4, and 15.5
    - src/api/routers/agent_runs.py
    - src/approvals/schemas.py
    - src/approvals/service.py
    - tests/test_agent_runs_api.py
  </read_first>
  <behavior>
    - Test fake graph interrupt payloads include `target_merchant_id`, `target_merchant_ref`, `business_fact_refs`, `verified_evidence_refs`, `claim_verification_ref`, `claim_verification_summary`, `risk_decision_ref`, `risk_decision`, and `approval_idempotency_key`.
    - Test `_approval_create_command_from_interrupt(...)` passes those fields to `ApprovalRequestCreateCommand` and the persisted `ApprovalRequest`.
    - Test existing spoofed interrupt run/action identity cases still create zero approval rows for spoofed runs and zero rows when identity mismatches.
  </behavior>
  <action>
    Update `src/api/routers/agent_runs.py` so `_approval_create_command_from_interrupt(...)` copies Phase 34 binding fields from the structured interrupt payload into `ApprovalRequestCreateCommand`.

    Preserve the existing executable binding requirements for `proposed_action`, `action_payload_hash`, `safety_snapshot_ref`, `safety_snapshot_hash`, config versions, and evidence refs. Add fail-closed validation for missing or malformed Phase 34 fields that are required by the post-34 command contract after Plan 34-03 lands.

    Do not derive target merchant or refs from `requested_by`, prompt text, final response text, memory, raw tool payloads, or user-provided run IDs. Keep `_validate_interrupt_action_identity(...)` as the authority for tenant/run/action identity and ensure any new fields cannot override the trusted run/user context.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_agent_runs_api.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/api/routers/agent_runs.py tests/test_agent_runs_api.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "target_merchant_id|business_fact_refs|verified_evidence_refs|claim_verification|risk_decision" src/api/routers/agent_runs.py tests/test_agent_runs_api.py` returns matches.
    - `rg -n "requested_by.*merchant|merchant_id.*requested_by" src/api/routers/agent_runs.py` returns no matches.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_agent_runs_api.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>agent_runs approval interrupt creation preserves Phase 34 bindings into ApprovalService without weakening spoof protections.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep run approval_required projections safe</name>
  <files>src/api/routers/agent_runs.py, src/api/schemas/agent_runs.py, tests/test_agent_runs_api.py</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/34-approval-and-actiondraft-boundary-hardening/34-CONTEXT.md
    - src/api/routers/agent_runs.py
    - src/api/schemas/agent_runs.py
    - tests/test_agent_runs_api.py
  </read_first>
  <behavior>
    - Test `approval_required` stream events include safe approval id, approval revision refs, target merchant ref/id when safe, business/evidence/claim/risk refs or summaries, and allowed decision metadata.
    - Test stream/run approval payloads do not include raw verified evidence package bodies, raw claim verifier debug, prompt authority bodies, full safety snapshots, or raw action authority internals.
    - Leave broad persisted trace API projection hardening to Phase 35; Phase 34 only ensures stable refs are persisted and live approval-required payloads are safe.
  </behavior>
  <action>
    Extend the approval wait payload returned by `_create_approval_wait_payload_from_interrupt(...)` to include only safe Phase 34 refs/summaries needed by clients and Phase 35 replay/eval work.

    Update `src/api/schemas/agent_runs.py` only if typed run status responses need new optional safe fields. Avoid adding raw payload fields or new trace API surfaces in this plan.

    Keep `src/api/routers/traces.py` out of this plan by design: broad trace/run replay projection coverage is explicitly deferred to Phase 35, while Phase 34 persists stable refs and prevents raw leakage in live approval-required responses.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_agent_runs_api.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/api/routers/agent_runs.py src/api/schemas/agent_runs.py tests/test_agent_runs_api.py</automated>
  </verify>
  <acceptance_criteria>
    - `approval_required` SSE payload tests assert safe Phase 34 refs/summaries are present.
    - `approval_required` SSE payload tests assert raw package/verifier/snapshot/action authority fields are absent.
    - No Phase 34 plan modifies `src/api/routers/traces.py`; the trace projection deferral remains recorded in `34-CONTEXT.md`.
  </acceptance_criteria>
  <done>Live run approval-required projections expose safe Phase 34 refs without expanding trace API scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graph interrupt -> API bridge | Structured approval interrupt data crosses from graph runtime into ApprovalService creation. |
| API bridge -> live client projection | Safe approval-required payloads are shown to clients without exposing raw authority bodies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04-01 | Tampering | interrupt binding fields | mitigate | Preserve exact Phase 34 bindings into ApprovalRequestCreateCommand; fail closed on missing/malformed required fields. |
| T-34-04-02 | Spoofing | run/action identity | mitigate | Keep trusted run/user context and proposed_action identity validation authoritative. |
| T-34-04-03 | Information Disclosure | approval_required payload | mitigate | Project only safe refs/summaries and exclude raw evidence, verifier, snapshot, and action authority bodies. |
</threat_model>

<verification>
Run the focused commands in each task. Bare `pytest` and bare `python -m pytest` are invalid in MOCA and must not be used as evidence.
</verification>

<success_criteria>
- `agent_runs` interrupt creation persists Phase 34 binding fields into approval requests.
- Existing spoofed interrupt/run/action identity tests still pass.
- Live `approval_required` payloads expose safe refs/summaries only.
- Trace API projection work is explicitly deferred to Phase 35, not silently omitted.
</success_criteria>

<output>
After completion, create `.planning/phases/34-approval-and-actiondraft-boundary-hardening/34-04-SUMMARY.md`.
</output>
