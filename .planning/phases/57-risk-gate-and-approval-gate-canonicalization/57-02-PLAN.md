---
phase: 57-risk-gate-and-approval-gate-canonicalization
plan: 02
type: execute
wave: 2
depends_on:
  - 57-01
files_modified:
  - src/agent/graph.py
  - src/agent/routing.py
  - tests/architecture/graph_baseline.py
  - tests/architecture/test_canonical_graph_baseline.py
  - tests/agent/test_graph.py
  - tests/test_graph_routing.py
  - tests/agent/rag_context/test_routing.py
autonomous: true
requirements:
  - CAGM-08
requirements_addressed:
  - CAGM-08
user_setup: []
must_haves:
  truths:
    - "The active `StateGraph` registers `risk_gate`, not `assess_risk_and_approval`."
    - "`claim_verify` and `approval_gate` route maps target active `risk_gate` for current rerisk paths."
    - "Current router return values no longer use `assess_risk_and_approval` as a normal current-run route."
    - "Phase 56 RAG/claim fail-closed gates still prevent unsupported action claims from reaching risk/action paths."
  artifacts:
    - path: "src/agent/graph.py"
      provides: "Active graph registration and route-map destinations for `risk_gate`"
      contains: "builder.add_node(\"risk_gate\""
    - path: "src/agent/routing.py"
      provides: "`route_after_claim_verify` returns `risk_gate` or `final_response`"
      contains: "_CLAIM_VERIFY_ROUTES = {\"risk_gate\", \"final_response\"}"
    - path: "tests/architecture/graph_baseline.py"
      provides: "Static active graph baseline with no active `assess_risk_and_approval` row"
    - path: "tests/architecture/test_canonical_graph_baseline.py"
      provides: "Architecture guard rejecting active legacy risk node registration/routes"
  key_links:
    - from: "src/agent/routing.py"
      to: "src/agent/graph.py"
      via: "`route_after_claim_verify` return value appears in claim_verify path map"
      pattern: "\"risk_gate\": \"risk_gate\""
    - from: "src/agent/graph.py"
      to: "tests/architecture/graph_baseline.py"
      via: "AST/static graph baseline"
      pattern: "CURRENT_ACTIVE_GRAPH_NODES_BASELINE"
---

<objective>
Cut the active graph, router return values, and architecture baseline over to current-run `risk_gate` route values.

Purpose: CAGM-08 requires `risk_gate` to replace active `assess_risk_and_approval` graph naming while preserving Phase 56 RAG/claim fail-closed behavior.
Output: Active graph registration, path maps, router allowlists, and static/integration tests updated to canonical risk node identity.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-CONTEXT.md
@.planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-RESEARCH.md
@.planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-PATTERNS.md
@.planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-02-SUMMARY.md
@.planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-03-SUMMARY.md
@src/agent/graph.py
@src/agent/routing.py
@src/agent/nodes/risk_gate.py
@tests/architecture/graph_baseline.py
@tests/architecture/test_canonical_graph_baseline.py
@tests/agent/test_graph.py
@tests/test_graph_routing.py
@tests/agent/rag_context/test_routing.py

<interfaces>
Required active graph route targets:
```python
builder.add_node("risk_gate", risk_gate, retry_policy=_llm_retry)

builder.add_conditional_edges(
    "claim_verify",
    route_after_claim_verify,
    {"risk_gate": "risk_gate", "final_response": "final_response"},
)

builder.add_conditional_edges(
    "risk_gate",
    route_after_risk,
    {
        "risk_gate": "risk_gate",
        "approval_gate": "approval_gate",
        "action_draft": "action_draft",
        "final_response": "final_response",
    },
)
```

Required router values:
```python
_CLAIM_VERIFY_ROUTES = {"risk_gate", "final_response"}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update active graph and baseline tests for risk_gate</name>
  <read_first>
    - .planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-CONTEXT.md
    - .planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-PATTERNS.md
    - .planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-02-SUMMARY.md
    - src/agent/graph.py
    - src/agent/routing.py
    - tests/architecture/graph_baseline.py
    - tests/architecture/test_canonical_graph_baseline.py
    - tests/agent/test_graph.py
    - tests/test_graph_routing.py
  </read_first>
  <files>src/agent/graph.py, src/agent/routing.py, tests/architecture/graph_baseline.py, tests/architecture/test_canonical_graph_baseline.py, tests/agent/test_graph.py, tests/test_graph_routing.py</files>
  <behavior>
    - Active graph nodes include `risk_gate` and do not include `assess_risk_and_approval`.
    - Claim verification path map contains `"risk_gate": "risk_gate"` and not `"assess_risk_and_approval": "assess_risk_and_approval"`.
    - Risk conditional edge source is `risk_gate`.
    - Approval edit rerisk path map contains `"risk_gate": "risk_gate"`.
    - Architecture baseline no longer counts `assess_risk_and_approval` as an active legacy row.
  </behavior>
  <action>
    Import `risk_gate` from `src.agent.nodes.risk_gate` and register `builder.add_node("risk_gate", risk_gate, retry_policy=_llm_retry)` per D-57-01. Replace the active claim-verify path map with `"risk_gate": "risk_gate"` per D-57-02. Move the `route_after_risk` conditional-edge source from `"assess_risk_and_approval"` to `"risk_gate"`. Ensure the approval-gate path map includes `"risk_gate": "risk_gate"` for edit rerisk. Update `_CLAIM_VERIFY_ROUTES` in `src/agent/routing.py` to exactly `{"risk_gate", "final_response"}` and change `_route_after_claim_verify(...)` to return `"risk_gate"` where it currently returns `"assess_risk_and_approval"`, without changing the Phase 56 conditions that require claim verification/evidence/action support per D-57-06 and D-57-16. Update architecture baseline fixtures and compiled graph tests so `assess_risk_and_approval` is not an active graph node or active path-map destination after this plan.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/agent/test_graph.py tests/test_graph_routing.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `src/agent/graph.py` contains `builder.add_node("risk_gate"` and does not contain `builder.add_node("assess_risk_and_approval"`.
    - `src/agent/graph.py` contains `"claim_verify"` path-map destination `"risk_gate": "risk_gate"`.
    - `src/agent/routing.py` contains `_CLAIM_VERIFY_ROUTES = {"risk_gate", "final_response"}`.
    - `tests/architecture/graph_baseline.py` current active node baseline includes `risk_gate` and excludes `assess_risk_and_approval`.
    - Automated command uses `UV_CACHE_DIR=/tmp/uv-cache uv run pytest`.
  </acceptance_criteria>
  <done>Active graph and static/integration graph tests reject `assess_risk_and_approval` as current-run graph identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve claim fail-closed routing while canonicalizing risk route assertions</name>
  <read_first>
    - .planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-CONTEXT.md
    - .planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-03-SUMMARY.md
    - src/agent/routing.py
    - tests/agent/rag_context/test_routing.py
    - tests/test_graph_routing.py
  </read_first>
  <files>src/agent/routing.py, tests/agent/rag_context/test_routing.py, tests/test_graph_routing.py</files>
  <action>
    Update claim/RAG route tests so expected risk route values are `risk_gate`, not `assess_risk_and_approval`, per D-57-03. Preserve Phase 56 repaired decision table exactly: existing `proposed_action` still requires an allowed `claim_type="action_recommendation"` result; no-preexisting-proposed-action with verified allowed action recommendation can enter risk for binding construction; legacy verifier fields cannot override canonical `claim_verification_bundle`; unsafe/missing RAG context fails closed. Add or update a regression that unsupported proposed action returns `final_response`, and the positive verified action recommendation route returns `risk_gate`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/rag_context/test_routing.py tests/test_graph_routing.py tests/knowledge/test_claim_verification_bundle.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `tests/agent/rag_context/test_routing.py` expected current risk route literals are `risk_gate`.
    - `tests/agent/rag_context/test_routing.py` still contains negative coverage for unsupported `proposed_action`.
    - `tests/test_graph_routing.py` still contains fail-closed route-after-risk binding tests.
    - No test update deletes the Phase 56 legacy verifier non-authority cases.
  </acceptance_criteria>
  <done>Canonical risk route values are used without weakening RAG/claim fail-closed gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Router return value -> StateGraph destination | Untrusted or malformed state could try to route outside registered canonical nodes. |
| Claim verification bundle -> risk route | Claim/evidence support controls whether action-risk processing may start. |
| Approval interrupt resume -> graph rerisk route | Trusted edit resume must rerisk before draft creation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-02-01 | Tampering | `route_after_claim_verify` | mitigate | Route allowlist is exactly `risk_gate` and `final_response`; unexpected routes fall back to `final_response`. |
| T-57-02-02 | Elevation of privilege | claim verification -> risk gate | mitigate | Preserve Phase 56 allowed-action recommendation requirements before risk routing; unsupported claims stay out of risk/approval/action. |
| T-57-02-03 | Denial of service | graph path-map mismatch | mitigate | Update graph path maps and route return values together; architecture tests inspect both static path maps and compiled graph edges. |
| T-57-02-04 | Repudiation | active graph baseline | mitigate | Static baseline rejects active `assess_risk_and_approval` registration/routes and records `risk_gate` as current runtime identity. |
</threat_model>

<verification>
Plan-local verification:
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/agent/test_graph.py tests/test_graph_routing.py tests/agent/rag_context/test_routing.py tests/knowledge/test_claim_verification_bundle.py -q --tb=short`

Static acceptance scan:
- Use an approved `UV_CACHE_DIR=/tmp/uv-cache uv run python -c ...` scan in the summary to confirm `src/agent/graph.py` has no active `builder.add_node("assess_risk_and_approval"` and no active path-map destination to the legacy node.
</verification>

<source_audit>
GOAL covered: active graph and routers now use canonical `risk_gate`.
REQ covered: CAGM-08 active graph naming replacement and deterministic risk/action policy separation foundation.
RESEARCH covered: Pitfall 1 route-map mismatch, Pattern 1 canonical active node, Pattern 4 fail-closed binding chain.
CONTEXT decisions covered: D-57-01, D-57-02, D-57-03, D-57-05, D-57-06, D-57-15, D-57-16, D-57-17.
Deferred ideas excluded: Phase 58 final deletion of all historical compatibility aliases is not performed here.
</source_audit>

<success_criteria>
- Active graph registers `risk_gate`.
- Active current-run graph routes use `risk_gate`, not `assess_risk_and_approval`.
- RAG/claim fail-closed tests still pass through approved commands.
</success_criteria>

<output>
After completion, create `.planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-02-SUMMARY.md`.
</output>
