---
phase: 57-risk-gate-and-approval-gate-canonicalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/nodes/risk_gate.py
  - src/agent/nodes/assess_risk_and_approval.py
  - tests/agent/test_nodes/test_risk_gate.py
  - tests/agent/test_nodes/test_assess_risk_and_approval.py
  - moca.egg-info/SOURCES.txt
autonomous: true
requirements:
  - CAGM-08
requirements_addressed:
  - CAGM-08
user_setup: []
must_haves:
  truths:
    - "Current-run risk/action node execution can be called as `risk_gate` and emits `risk_gate` identity."
    - "Retained `assess_risk_and_approval` behavior is import/test compatibility only and records owner, reason, trace projection, validation, and `DELETE_BY_PHASE_58`."
    - "Risk policy semantics, fail-closed binding behavior, and Phase 56 claim/RAG safety gates are preserved."
  artifacts:
    - path: "src/agent/nodes/risk_gate.py"
      provides: "Canonical `risk_gate` callable delegating to shared risk/action implementation"
      exports: ["risk_gate"]
    - path: "src/agent/nodes/assess_risk_and_approval.py"
      provides: "Shared implementation plus Phase 58-scoped legacy compatibility wrapper"
      contains: "PHASE_57_COMPATIBILITY_ALIAS"
    - path: "tests/agent/test_nodes/test_risk_gate.py"
      provides: "Canonical current-run identity across outputs, traces, errors, fallback metadata, and fail-closed regression coverage"
    - path: "tests/agent/test_nodes/test_assess_risk_and_approval.py"
      provides: "Legacy compatibility metadata and wrapper identity coverage"
  key_links:
    - from: "src/agent/nodes/risk_gate.py"
      to: "src/agent/nodes/assess_risk_and_approval.py"
      via: "shared helper call with `output_key=\"risk_gate\"` and `trace_node=\"risk_gate\"`"
      pattern: "_assess_risk_and_approval_with_identity"
    - from: "tests/agent/test_nodes/test_risk_gate.py"
      to: "src/agent/nodes/risk_gate.py"
      via: "import and execute canonical callable"
      pattern: "risk_gate"
---

<objective>
Create the canonical `risk_gate` callable/module and narrow the old `assess_risk_and_approval` surface to an explicit Phase 58-scoped compatibility wrapper.

Purpose: CAGM-08 requires `risk_gate` to own deterministic risk/action policy semantics while Phase 57 preserves existing fail-closed behavior and avoids deleting historical compatibility prematurely.
Output: Canonical callable, shared implementation identity hook, compatibility metadata, and node-level tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-CONTEXT.md
@.planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-RESEARCH.md
@.planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-PATTERNS.md
@.planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-VALIDATION.md
@.planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
@.planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-03-SUMMARY.md
@src/agent/nodes/assess_risk_and_approval.py
@src/agent/nodes/recommendation_generation.py
@src/agent/nodes/generate_recommendation.py
@tests/agent/test_nodes/test_assess_risk_and_approval.py
@tests/agent/test_nodes/test_generate_recommendation.py

<interfaces>
Current callable pattern to follow:
```python
async def recommendation_generation(state: AgentState, config: RunnableConfig = None) -> dict:
    return await _generate_recommendation_with_identity(
        state,
        config,
        output_key=_CANONICAL_NODE,
        trace_node=_CANONICAL_NODE,
    )
```

Required Phase 57 constants and target values:
```python
_LEGACY_NODE = "assess_risk_and_approval"
_CANONICAL_NODE = "risk_gate"
HISTORICAL_TRACE_PROJECTION = "HISTORICAL_TRACE_PROJECTION"
IMPORT_TEST_COMPATIBILITY = "IMPORT_TEST_COMPATIBILITY"
DELETE_BY_PHASE_58 = "DELETE_BY_PHASE_58"
PHASE_57_COMPATIBILITY_ALIAS = {
    "legacy_surface": _LEGACY_NODE,
    "canonical_owner": _CANONICAL_NODE,
    "reason": IMPORT_TEST_COMPATIBILITY,
    "trace_projection": HISTORICAL_TRACE_PROJECTION,
    "validation_tests": (...),
    "delete_phase": DELETE_BY_PHASE_58,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical and compatibility identity tests</name>
  <read_first>
    - .planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-CONTEXT.md
    - .planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-PATTERNS.md
    - src/agent/nodes/assess_risk_and_approval.py
    - tests/agent/test_nodes/test_assess_risk_and_approval.py
    - tests/agent/test_nodes/test_generate_recommendation.py
  </read_first>
  <files>tests/agent/test_nodes/test_risk_gate.py, tests/agent/test_nodes/test_assess_risk_and_approval.py</files>
  <behavior>
    - Canonical callable test: invoking `risk_gate(...)` writes `llm_outputs["risk_gate"]`, appends trace steps whose `node` is `risk_gate`, records `node_errors`/fail-closed metadata with `risk_gate` identity when error paths are exercised, and does not write current-run `assess_risk_and_approval` identity.
    - Compatibility test: retained `assess_risk_and_approval(...)` remains importable and writes legacy identity only when that legacy wrapper is invoked for import/test compatibility.
    - Metadata test: source contains `PHASE_57_COMPATIBILITY_ALIAS`, `HISTORICAL_TRACE_PROJECTION`, `IMPORT_TEST_COMPATIBILITY`, and `DELETE_BY_PHASE_58`.
    - Source-literal audit test or summary evidence classifies every remaining `assess_risk_and_approval` literal in the node module as shared-helper implementation detail, legacy wrapper/import-test compatibility, historical projection metadata, or Phase 58 deletion candidate.
    - Safety regression test: existing fail-closed behavior for missing claim/evidence/snapshot binding remains covered by the current risk node test matrix.
  </behavior>
  <action>
    Add `tests/agent/test_nodes/test_risk_gate.py` using the existing fake LLM / base state patterns from `tests/agent/test_nodes/test_assess_risk_and_approval.py`. Update `tests/agent/test_nodes/test_assess_risk_and_approval.py` only to add Phase 57 compatibility metadata/wrapper identity assertions, with test names/comments labeling the old callable as legacy compatibility rather than current authority. Add coverage for canonical identity in normal success, fail-closed/fallback, and exception/error-output paths where the current implementation writes `node_errors`, trace metadata, `llm_outputs`, approval-plan metadata, risk-decision/debug metadata, or final-response fallback source. Reference D-57-04, D-57-05, D-57-06, D-57-07, D-57-08, D-57-16, and D-57-17 in test comments or assertion names where useful. Do not weaken or delete existing Phase 34/56 fail-closed tests.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_risk_gate.py tests/agent/test_nodes/test_assess_risk_and_approval.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `tests/agent/test_nodes/test_risk_gate.py` imports `src.agent.nodes.risk_gate`.
    - Test source includes literal `risk_gate` assertions for `llm_outputs` and trace step node.
    - Canonical tests assert error/fail-closed paths do not introduce current-run `node_errors` or fallback metadata with `assess_risk_and_approval` identity.
    - Test source includes literal `PHASE_57_COMPATIBILITY_ALIAS` and `DELETE_BY_PHASE_58` assertions.
    - Legacy wrapper tests are explicitly named/commented as import/test compatibility.
    - Automated command uses `UV_CACHE_DIR=/tmp/uv-cache uv run pytest`, not bare `pytest`.
  </acceptance_criteria>
  <done>Canonical and legacy identity expectations are locked by tests without deleting existing risk/action safety coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement canonical risk_gate wrapper and explicit legacy metadata</name>
  <read_first>
    - .planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-CONTEXT.md
    - .planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-RESEARCH.md
    - .planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-PATTERNS.md
    - src/agent/nodes/assess_risk_and_approval.py
    - src/agent/nodes/recommendation_generation.py
    - src/agent/nodes/generate_recommendation.py
    - moca.egg-info/SOURCES.txt
  </read_first>
  <files>src/agent/nodes/risk_gate.py, src/agent/nodes/assess_risk_and_approval.py, moca.egg-info/SOURCES.txt</files>
  <action>
    Create `src/agent/nodes/risk_gate.py` with `async def risk_gate(state: AgentState, config: RunnableConfig = None) -> dict` that delegates to a shared implementation helper in `assess_risk_and_approval.py` using `output_key="risk_gate"` and `trace_node="risk_gate"` per D-57-01 and D-57-08. Refactor `assess_risk_and_approval.py` so the existing risk/action logic lives behind `_assess_risk_and_approval_with_identity(..., output_key: str, trace_node: str)` or equivalent, while `assess_risk_and_approval(...)` remains a wrapper using `output_key="assess_risk_and_approval"` and `trace_node="assess_risk_and_approval"` for import/test compatibility only. Before changing behavior, audit all existing `assess_risk_and_approval` literals in the module, including node metadata, `node_errors`, trace steps, `llm_outputs`, approval-plan/resume metadata, risk-decision debug data, and final-response fallback source. Parameterize current-run writes through the identity hook; only the legacy wrapper, compatibility metadata, historical projection comments, and Phase 58 delete markers may retain the legacy literal. Add `PHASE_57_COMPATIBILITY_ALIAS` metadata with exact fields: `legacy_surface`, `canonical_owner`, `reason`, `trace_projection`, `validation_tests`, and `delete_phase`, using `DELETE_BY_PHASE_58` as the delete marker per D-57-04 and D-57-16. Preserve `rules/risk_rules.yaml`, deterministic high-risk override, action payload hash computation, safety snapshot persistence, approval-plan construction, blocked/manual-review outcomes, and auto-allowed binding behavior per D-57-05 through D-57-07. If `moca.egg-info/SOURCES.txt` is tracked and missing the new file, add `src/agent/nodes/risk_gate.py` to it; if it is not tracked, do not introduce generated egg-info churn.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_risk_gate.py tests/agent/test_nodes/test_assess_risk_and_approval.py tests/agent/test_phase22_action_boundary.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `src/agent/nodes/risk_gate.py` contains `async def risk_gate`.
    - `src/agent/nodes/risk_gate.py` contains the literal `_CANONICAL_NODE = "risk_gate"` or delegates with the literal `"risk_gate"`.
    - `src/agent/nodes/assess_risk_and_approval.py` contains `PHASE_57_COMPATIBILITY_ALIAS` and `DELETE_BY_PHASE_58`.
    - `src/agent/nodes/assess_risk_and_approval.py` still exports `assess_risk_and_approval`.
    - Current-risk tests prove `risk_gate` identity and existing action-boundary tests remain green.
    - Plan summary includes a literal audit note for remaining `assess_risk_and_approval` hits in `src/agent/nodes/assess_risk_and_approval.py`.
  </acceptance_criteria>
  <done>`risk_gate` is the canonical callable owner, and the legacy callable is explicit compatibility with Phase 58 delete metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claim/RAG state -> risk node | Verified claim/evidence state crosses into action-risk decision logic. |
| Risk node -> approval/action state | Proposed action, hashes, snapshot refs, risk decision, and approval plan become downstream authority. |
| Legacy import surface -> current runtime identity | Historical callable remains importable but must not become active graph authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01-01 | Tampering | `risk_gate` shared implementation | mitigate | Preserve existing fail-closed checks for missing claim bundle, evidence refs, payload hash, snapshot ref/hash, and config versions; add canonical tests before implementation. |
| T-57-01-02 | Elevation of privilege | `risk_gate` LLM output path | mitigate | Keep LLM output to wording/draft assistance only; deterministic code retains risk lowering, approval requirement, route choice, and binding authority per D-57-07. |
| T-57-01-03 | Repudiation | `assess_risk_and_approval` compatibility wrapper | mitigate | Add `PHASE_57_COMPATIBILITY_ALIAS` metadata with owner/reason/trace projection/validation/delete phase and tests that current `risk_gate` identity is separate. |
| T-57-01-04 | Information disclosure | Trace and `llm_outputs` identity split | mitigate | Current-run canonical callable writes `risk_gate`; legacy wrapper writes only legacy compatibility identity, avoiding mixed current/historical authority. |
</threat_model>

<verification>
Plan-local verification:
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_risk_gate.py tests/agent/test_nodes/test_assess_risk_and_approval.py tests/agent/test_phase22_action_boundary.py -q --tb=short`

Wave handoff verification:
- Confirm `src.agent.nodes.risk_gate.risk_gate` imports without importing graph wiring.
- Confirm no active graph registration is changed in this plan; graph cutover is Plan 57-02.
</verification>

<source_audit>
GOAL covered: canonical `risk_gate` callable exists before active graph cutover.
REQ covered: CAGM-08 risk node replacement foundation.
RESEARCH covered: Pattern 1 canonical node with narrow legacy compatibility, Pattern 4 fail-closed binding chain.
CONTEXT decisions covered: D-57-04, D-57-05, D-57-06, D-57-07, D-57-08, D-57-15, D-57-16, D-57-17.
Deferred ideas excluded: Phase 58 final deletion of all compatibility aliases is not performed here.
</source_audit>

<success_criteria>
- `risk_gate` callable exists and writes canonical current-run node identity.
- `assess_risk_and_approval` compatibility wrapper remains importable but carries Phase 58 delete metadata.
- Existing risk/action fail-closed tests and Phase 22 action-boundary tests pass through the approved `uv run` entrypoint.
</success_criteria>

<output>
After completion, create `.planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-01-SUMMARY.md`.
</output>
