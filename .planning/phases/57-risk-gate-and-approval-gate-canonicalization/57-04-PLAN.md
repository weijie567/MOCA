---
phase: 57-risk-gate-and-approval-gate-canonicalization
plan: 04
type: execute
wave: 4
depends_on:
  - 57-03
files_modified:
  - src/agent/graph_vocabulary.py
  - src/api/routers/agent_runs.py
  - frontend/src/components/timeline/TimelineStep.tsx
  - scripts/eval_agent.py
  - scripts/diagnose_latency.py
  - tests/agent/test_graph_vocabulary.py
  - tests/test_agent_runs_api.py
  - tests/agent/test_trace.py
  - tests/test_trace_api.py
  - tests/architecture/test_canonical_graph_baseline.py
  - docs/current-langgraph-architecture.md
  - docs/architecture-overview.md
  - docs/target-agent-platform-architecture-plan.md
  - README.md
  - .planning/ARCHITECTURE-DEBT.md
  - .planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-VALIDATION.md
autonomous: true
requirements:
  - CAGM-08
requirements_addressed:
  - CAGM-08
user_setup: []
must_haves:
  truths:
    - "Current-run runtime vocabulary, API/SSE payloads, frontend labels, eval expected nodes, and diagnostics use `risk_gate`."
    - "Historical `assess_risk_and_approval` traces remain readable only through compatibility projection labeled for Phase 58 deletion."
    - "Current-source docs and architecture debt distinguish Phase 57 delivered runtime identity from Phase 58 final alias deletion."
    - "Phase validation includes approved-entrypoint tests and a static legacy-hit classification."
  artifacts:
    - path: "src/agent/graph_vocabulary.py"
      provides: "`risk_gate` runtime entry and `assess_risk_and_approval -> risk_gate` compatibility alias metadata"
    - path: "src/api/routers/agent_runs.py"
      provides: "Current `risk_gate` SSE/API labels and risk payload extraction"
    - path: "frontend/src/components/timeline/TimelineStep.tsx"
      provides: "Timeline label for `risk_gate`"
    - path: ".planning/ARCHITECTURE-DEBT.md"
      provides: "Graph/RAG/risk approval ledger entry for Phase 57 completion and Phase 58 residuals"
    - path: ".planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-VALIDATION.md"
      provides: "Nyquist validation status and final evidence"
  key_links:
    - from: "src/agent/graph_vocabulary.py"
      to: "src/api/routers/agent_runs.py"
      via: "`target_graph_name(node_name, kind=\"node\")`"
      pattern: "target_node_name"
    - from: "scripts/eval_agent.py"
      to: "src.agent.nodes.risk_gate"
      via: "fake LLM patch for canonical risk node"
      pattern: "risk_gate"
---

<objective>
Close Phase 57 by updating runtime vocabulary/API/frontend/eval/docs/debt/validation surfaces and classifying all remaining legacy hits.

Purpose: CAGM-08 is not complete if current runs still display or evaluate the legacy risk node as current authority. This plan makes `risk_gate` the current vocabulary while preserving historical projection until Phase 58.
Output: Projection/runtime labels, eval/diagnostic harnesses, docs, architecture-debt ledger, validation artifact, and static legacy-hit classification.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-CONTEXT.md
@.planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-RESEARCH.md
@.planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-PATTERNS.md
@.planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-VALIDATION.md
@.planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
@.planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-04-SUMMARY.md
@src/agent/graph_vocabulary.py
@src/api/routers/agent_runs.py
@frontend/src/components/timeline/TimelineStep.tsx
@scripts/eval_agent.py
@scripts/diagnose_latency.py
@docs/current-langgraph-architecture.md
@docs/architecture-overview.md
@docs/target-agent-platform-architecture-plan.md
@README.md
@.planning/ARCHITECTURE-DEBT.md

<interfaces>
Required vocabulary entries:
```python
_entry("risk_gate", "risk_gate", "node", "runtime", True)
_entry(
    "assess_risk_and_approval",
    "risk_gate",
    "node",
    "compatibility_alias",
    False,
    (
        "PHASE_57_COMPATIBILITY_ALIAS",
        "HISTORICAL_TRACE_PROJECTION",
        "IMPORT_TEST_COMPATIBILITY",
        "DELETE_BY_PHASE_58",
    ),
)
```

Required current-run labels:
```python
NODE_MESSAGES["risk_gate"] = "正在评估风险"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update graph vocabulary and API trace projection</name>
  <read_first>
    - .planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-CONTEXT.md
    - .planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-PATTERNS.md
    - src/agent/graph_vocabulary.py
    - src/api/routers/agent_runs.py
    - tests/agent/test_graph_vocabulary.py
    - tests/test_agent_runs_api.py
    - tests/agent/test_trace.py
    - tests/test_trace_api.py
  </read_first>
  <files>src/agent/graph_vocabulary.py, src/api/routers/agent_runs.py, tests/agent/test_graph_vocabulary.py, tests/test_agent_runs_api.py, tests/agent/test_trace.py, tests/test_trace_api.py</files>
  <behavior>
    - `graph_vocabulary_entry("risk_gate", kind="node")` is runtime, target `risk_gate`, runnable true.
    - `graph_vocabulary_entry("assess_risk_and_approval", kind="node")` is compatibility alias, target `risk_gate`, reason codes include Phase 57 and `DELETE_BY_PHASE_58`.
    - API/SSE labels and risk-level extraction support current node name `risk_gate`.
    - Historical trace steps with `node="assess_risk_and_approval"` preserve implementation node and project target `risk_gate`.
  </behavior>
  <action>
    Update `src/agent/graph_vocabulary.py` per D-57-08 so `risk_gate` is a runtime node entry and `assess_risk_and_approval` is a compatibility alias with reason codes `PHASE_57_COMPATIBILITY_ALIAS`, `HISTORICAL_TRACE_PROJECTION`, `IMPORT_TEST_COMPATIBILITY`, and `DELETE_BY_PHASE_58`. Set the legacy alias `runnable` value to false unless existing vocabulary tests require otherwise; if retained true for historical projection compatibility, document the reason in the test and architecture-debt entry. Update `src/api/routers/agent_runs.py` so `NODE_MESSAGES` includes `risk_gate` and risk payload extraction checks `node_name == "risk_gate"` for current runs; keep legacy extraction only as historical compatibility if tests prove existing traces need it. Add tests mirroring Phase 56 vocabulary projection: current `risk_gate` identity mapping and historical `assess_risk_and_approval -> risk_gate` projection without rewriting stored `node`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_graph_vocabulary.py tests/agent/test_trace.py tests/test_trace_api.py tests/test_agent_runs_api.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `src/agent/graph_vocabulary.py` contains runtime entry for literal `risk_gate`.
    - `src/agent/graph_vocabulary.py` contains `PHASE_57_COMPATIBILITY_ALIAS` and `DELETE_BY_PHASE_58` near the legacy alias.
    - `src/api/routers/agent_runs.py` contains `NODE_MESSAGES` entry for `risk_gate`.
    - API projection tests assert `target_node_name == "risk_gate"` for current and historical cases.
  </acceptance_criteria>
  <done>Trace/API projection exposes current `risk_gate` while retaining labeled historical compatibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update frontend, eval, diagnostics, and static current-run vocabulary checks</name>
  <read_first>
    - .planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-CONTEXT.md
    - .planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-PATTERNS.md
    - frontend/src/components/timeline/TimelineStep.tsx
    - scripts/eval_agent.py
    - scripts/diagnose_latency.py
    - tests/architecture/test_canonical_graph_baseline.py
  </read_first>
  <files>frontend/src/components/timeline/TimelineStep.tsx, scripts/eval_agent.py, scripts/diagnose_latency.py, tests/architecture/test_canonical_graph_baseline.py</files>
  <action>
    Add `risk_gate` to the frontend timeline label map with Chinese display text equivalent to the existing risk evaluation label. Keep any `assess_risk_and_approval` frontend label only if it is explicitly for historical timeline display and is covered by static legacy-hit classification per D-57-08 and D-57-16. Update `scripts/eval_agent.py` so `GRAPH_CONTRACT_PATCHED_NODES`, fake LLM keys, imports/patches, and expected node sequences use `risk_gate` for current graph runs; import/patch `src.agent.nodes.risk_gate` for canonical current runs while preserving old module patches only if a historical compatibility test requires them. Update `scripts/diagnose_latency.py` synthetic step names from `assess_risk_and_approval` to `risk_gate`. Add a static architecture test that current-run source/eval/diagnostic literals do not classify `assess_risk_and_approval` as a current runtime node.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/agent/test_graph_vocabulary.py tests/test_agent_runs_api.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/components/timeline/TimelineStep.tsx` contains a `risk_gate` label.
    - `scripts/eval_agent.py` current expected nodes append `risk_gate`, not `assess_risk_and_approval`.
    - `scripts/eval_agent.py` fake LLM patching supports `risk_gate`.
    - `scripts/diagnose_latency.py` synthetic risk step uses `risk_gate`.
    - Any remaining frontend/eval/diagnostic `assess_risk_and_approval` literal is covered by a test or comment that labels it historical compatibility.
  </acceptance_criteria>
  <done>Current UI/eval/diagnostic vocabulary uses `risk_gate`, with legacy hits classified instead of silently remaining current.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs, architecture debt, validation artifact, and legacy-hit classification</name>
  <read_first>
    - .planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-CONTEXT.md
    - .planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-RESEARCH.md
    - .planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-VALIDATION.md
    - .planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
    - .planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-04-SUMMARY.md
    - docs/current-langgraph-architecture.md
    - docs/architecture-overview.md
    - docs/target-agent-platform-architecture-plan.md
    - README.md
    - .planning/ARCHITECTURE-DEBT.md
  </read_first>
  <files>docs/current-langgraph-architecture.md, docs/architecture-overview.md, docs/target-agent-platform-architecture-plan.md, README.md, .planning/ARCHITECTURE-DEBT.md, .planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-VALIDATION.md</files>
  <action>
    Update current-source docs and README diagrams/tables so current runtime graph uses `risk_gate` and `approval_gate` retains request/resume-only semantics per D-57-05 through D-57-12. Do not rewrite `docs/contract-spec.md` unless source review proves the target contract is wrong; the current research says target contract already uses `risk_gate`. Append a Chinese `.planning/ARCHITECTURE-DEBT.md` entry under the Agent Graph / risk approval chain area recording Phase 57 fixed state, evidence commands, and remaining Phase 58 deletion work per project rules. Update `57-VALIDATION.md`: set task rows to green only after commands pass, set `nyquist_compliant: true`, and record final approved-entrypoint commands. Add a static legacy-hit classification in the summary or validation artifact: every remaining `assess_risk_and_approval` hit must be classified as historical compatibility, legacy wrapper/import test, documentation of previous state, or Phase 58 deletion candidate. Do not bulk-rewrite historical DB rows or delete compatibility aliases; Phase 58 owns final no-debt cleanup.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/architecture/test_phase34_approval_action_boundaries.py tests/architecture/test_approval_boundaries.py tests/agent/test_graph.py tests/test_graph_routing.py tests/agent/test_nodes/test_risk_gate.py tests/agent/test_nodes/test_assess_risk_and_approval.py tests/agent/test_phase22_action_boundary.py tests/test_approval_gate.py tests/test_approval_api.py tests/agent/test_graph_vocabulary.py tests/test_agent_runs_api.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `docs/current-langgraph-architecture.md` current graph text contains `risk_gate`.
    - `.planning/ARCHITECTURE-DEBT.md` contains a Phase 57 entry with status, evidence, and Phase 58 residual risk.
    - `57-VALIDATION.md` frontmatter has `nyquist_compliant: true` only after approved commands pass.
    - A static legacy-hit classification exists in `57-VALIDATION.md` or `57-04-SUMMARY.md`.
    - Automated command uses `UV_CACHE_DIR=/tmp/uv-cache uv run pytest`.
  </acceptance_criteria>
  <done>Phase 57 closeout surfaces, debt ledger, validation artifact, and remaining legacy hits are explicit and ready for Phase 58.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stored trace node names -> API/SSE/frontend display | Historical data is projected for operators without rewriting audit history. |
| Eval harness -> fake graph execution | Evaluation patches must exercise the canonical current node, not legacy authority. |
| Documentation/debt ledgers -> future execution | Future Phase 58 cleanup depends on accurate compatibility classification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-04-01 | Repudiation | trace projection | mitigate | Preserve `implementation_node` for historical `assess_risk_and_approval` rows while projecting target `risk_gate` and compatibility status. |
| T-57-04-02 | Tampering | eval harness current node list | mitigate | Update eval expected nodes and fake patches to `risk_gate`; static tests reject legacy current-run classification. |
| T-57-04-03 | Information disclosure | API/SSE risk payload extraction | mitigate | Reuse existing payload extraction; do not expose raw approval/action payloads while changing node names. |
| T-57-04-04 | Elevation of privilege | docs/debt ambiguity | mitigate | Document `approval_gate` as resume-only and record Phase 58 residual compatibility deletion so future work does not treat legacy names as current authority. |
| T-57-04-05 | Denial of service | frontend/eval stale node labels | mitigate | Add current `risk_gate` labels and tests/static checks so current-run timelines/evals do not miss risk gate events. |
</threat_model>

<verification>
Plan-local verification:
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_graph_vocabulary.py tests/agent/test_trace.py tests/test_trace_api.py tests/test_agent_runs_api.py -q --tb=short`
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/agent/test_graph.py tests/test_graph_routing.py tests/test_approval_api.py tests/test_approval_gate.py tests/agent/test_nodes/test_risk_gate.py tests/agent/test_nodes/test_assess_risk_and_approval.py -q --tb=short`
- `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent src/approvals src/api tests/architecture tests/agent tests/test_graph_routing.py tests/test_approval_api.py tests/test_approval_gate.py tests/test_agent_runs_api.py tests/test_trace_api.py`

Static closeout:
- Use an approved `UV_CACHE_DIR=/tmp/uv-cache uv run python -c ...` source scan to classify remaining `assess_risk_and_approval` hits.
- Use `UV_CACHE_DIR=/tmp/uv-cache uv run git diff --check`.
</verification>

<source_audit>
GOAL covered: current vocabulary, display, eval, docs, and validation all expose `risk_gate`.
REQ covered: CAGM-08 phase closeout for active graph naming and risk/approval separation.
RESEARCH covered: Pitfall 5 projection/UI/eval miss, runtime state inventory historical projection, documentation sync checklist.
CONTEXT decisions covered: D-57-03, D-57-04, D-57-08, D-57-09, D-57-10, D-57-15, D-57-16, D-57-17.
Deferred ideas excluded: final deletion of all compatibility aliases and historical projections remains Phase 58.
</source_audit>

<success_criteria>
- Current-run runtime vocabulary/API/frontend/eval/docs use `risk_gate`.
- Legacy `assess_risk_and_approval` hits are compatibility/historical only and classified.
- Final Phase 57 validation uses approved MOCA command entrypoints and records evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-04-SUMMARY.md`.
</output>
