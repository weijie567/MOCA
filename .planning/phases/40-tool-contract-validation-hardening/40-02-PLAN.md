---
phase: 40-tool-contract-validation-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "40-01"
files_modified:
  - src/tools/validation.py
  - tests/tools/test_catalog.py
autonomous: true
requirements:
  - TPH-05
must_haves:
  truths:
    - "The local validator remains the intentional JSON Schema subset; no `jsonschema` dependency is introduced."
    - "Every descriptor schema keyword is either implemented by `validate_json_value` or rejected by a meta guard."
    - "Advertised numeric/string bounds do not silently no-op."
  artifacts:
    - path: "src/tools/validation.py"
      provides: "maxLength, minimum, maximum, exclusiveMaximum validation and supported-key set"
      contains: "SUPPORTED_JSON_SCHEMA_KEYS"
    - path: "tests/tools/test_catalog.py"
      provides: "keyword behavior tests and descriptor schema meta guard"
      contains: "test_all_descriptor_schemas_use_only_supported_validation_keywords"
---

<objective>
Align `validate_json_value` with the schema keywords descriptor authors are allowed to write.

Purpose: Phase 38 made output schemas meaningful, but the validator still ignores some prompt-safe/schema keywords. This plan prevents constraints that look active but do nothing.
Output: implemented `maxLength`, `minimum`, `maximum`, `exclusiveMaximum` behavior plus a meta guard over every descriptor `input_schema` and `output_schema`.
</objective>

<context>
@.planning/phases/40-tool-contract-validation-hardening/40-CONTEXT.md
@src/tools/validation.py
@src/tools/policy.py
@src/tools/catalog.py
@tests/tools/test_catalog.py

<interfaces>
Keep the public helper shape unchanged:

```python
def validate_json_value(value: Any, schema: dict[str, Any]) -> None: ...
_validate_json_value = validate_json_value
```

Allowed implemented schema keys should be explicit and testable. Do not add pattern/format/oneOf/anyOf in this phase.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement advertised string and numeric bounds</name>
  <files>src/tools/validation.py, tests/tools/test_catalog.py</files>
  <read_first>
    - src/tools/validation.py
    - src/tools/policy.py
    - tests/tools/test_catalog.py
  </read_first>
  <action>
    Add `SUPPORTED_JSON_SCHEMA_KEYS` to `src/tools/validation.py` for the implemented subset: `type`, `properties`, `items`, `required`, `additionalProperties`, `enum`, `minLength`, `maxLength`, `minimum`, `maximum`, `exclusiveMinimum`, and `exclusiveMaximum`.
    Implement string `maxLength`.
    Implement numeric `minimum`, `maximum`, and `exclusiveMaximum`; apply numeric bounds to both `integer` and `number`. Preserve existing bool rejection for integer/number and finite-number rejection for `number`.
    Keep the existing exception style (`TypeError` for type mismatch, `ValueError` for constraint violation).
  </action>
  <verify>
    <automated>uv run pytest tests/tools/test_catalog.py::test_json_schema_helper_enforces_string_max_length tests/tools/test_catalog.py::test_json_schema_helper_enforces_numeric_bounds -q</automated>
    <automated>uv run ruff check src/tools/validation.py tests/tools/test_catalog.py</automated>
  </verify>
  <acceptance_criteria>
    - Boundary values pass for `maxLength`, `minimum`, `maximum`, and `exclusiveMaximum`.
    - Out-of-bound values raise `ValueError`.
    - Existing nullable/type-list, enum, object, array, and non-finite-number tests still pass.
  </acceptance_criteria>
  <done>The validator enforces the string and numeric bounds it exposes to descriptor schemas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add descriptor schema meta guard</name>
  <files>tests/tools/test_catalog.py</files>
  <read_first>
    - tests/tools/test_catalog.py
    - src/tools/catalog.py
    - src/tools/validation.py
  </read_first>
  <action>
    Add a recursive schema-key walker in `tests/tools/test_catalog.py` that visits every default catalog descriptor `input_schema` and `output_schema`.
    Add `test_all_descriptor_schemas_use_only_supported_validation_keywords`, comparing each encountered keyword against `SUPPORTED_JSON_SCHEMA_KEYS`.
    The guard should fail with the tool name, schema side, path, and unsupported key if future descriptors add keywords such as `pattern`, `format`, `oneOf`, or `anyOf` before the validator implements them.
    Keep annotation-only `description` out of current descriptor schemas unless validator support is deliberately added later.
  </action>
  <verify>
    <automated>uv run pytest tests/tools/test_catalog.py::test_all_descriptor_schemas_use_only_supported_validation_keywords -q</automated>
    <automated>uv run pytest tests/tools/test_catalog.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "SUPPORTED_JSON_SCHEMA_KEYS|test_all_descriptor_schemas_use_only_supported_validation_keywords" src/tools/validation.py tests/tools/test_catalog.py` finds the constant and guard.
    - The full `tests/tools/test_catalog.py` suite passes through `uv run pytest`.
  </acceptance_criteria>
  <done>Unsupported schema keywords cannot silently enter descriptor input/output schemas.</done>
</task>

</tasks>

<verification>
Plan 40-02 is complete when:
- `uv run pytest tests/tools/test_catalog.py -q` passes.
- `uv run ruff check src/tools/validation.py tests/tools/test_catalog.py` passes.
- `git diff -- pyproject.toml uv.lock docs/contract-spec.md src/tools/contracts.py` is empty.
</verification>

<success_criteria>
- `validate_json_value` supports `maxLength`, `minimum`, `maximum`, and `exclusiveMaximum`.
- Descriptor schemas are guarded against unsupported keywords.
- No new dependency is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/40-tool-contract-validation-hardening/40-02-SUMMARY.md`.
</output>
