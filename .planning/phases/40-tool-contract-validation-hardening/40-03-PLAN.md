---
phase: 40-tool-contract-validation-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - "40-02"
files_modified:
  - tests/architecture/test_tool_contract_backstops.py
  - .planning/LOCAL-VALIDATION-ISSUES.md
autonomous: true
requirements:
  - TPH-05
must_haves:
  truths:
    - "Runtime ownership semantics remain split: policy marks domain identifiers; BusinessFactService/business adapters enforce data-coupled ownership and no-leak behavior."
    - "The advisory `requires_domain_scope_check` marker must have a backstop test so future tools cannot add marker-relevant identifiers while bypassing the business boundary."
    - "This plan must not consume the marker at runtime or rebuild BusinessFactService ownership logic."
  artifacts:
    - path: "tests/architecture/test_tool_contract_backstops.py"
      provides: "ownership marker and business-boundary architecture guard"
      contains: "test_domain_scope_marker_tools_route_through_business_fact_boundary"
    - path: ".planning/LOCAL-VALIDATION-ISSUES.md"
      provides: "conditional append-only local validation blocker record"
      contains: "Phase 40"
---

<objective>
Add a backstop for domain-scope marker handoff without changing runtime architecture.

Purpose: `policy.py` marks order/refund/ticket identifiers with `requires_domain_scope_check`, but policy does not and should not query ownership. The guard must ensure marker-bearing tools stay routed through the business fact boundary where ownership/no-leak behavior lives.
Output: architecture tests plus final no-diff verification for protected contracts.
</objective>

<context>
@.planning/phases/40-tool-contract-validation-hardening/40-CONTEXT.md
@src/tools/policy.py
@src/tools/catalog.py
@src/tools/executors/business.py
@src/business/service.py
@tests/tools/test_tool_platform.py
@tests/business/test_service.py
@tests/architecture

<interfaces>
Current split to preserve:

```python
# policy.py
binding["requires_domain_scope_check"] = True

# tools/executors/business.py
return await self.service.invoke_tool(name, args, ctx)

# business/service.py
BusinessToolService.invoke_tool -> BusinessFactService._read_tool
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add marker-to-business-boundary architecture tests</name>
  <files>tests/architecture/test_tool_contract_backstops.py</files>
  <read_first>
    - src/tools/policy.py
    - src/tools/catalog.py
    - src/tools/executors/business.py
    - src/business/service.py
    - tests/tools/test_tool_platform.py
  </read_first>
  <action>
    Create `tests/architecture/test_tool_contract_backstops.py`.
    Add `test_domain_scope_marker_tools_route_through_business_fact_boundary`: derive marker-bearing tools from `ToolCatalog().descriptors()` by intersecting input schema property names with `src.tools.policy._DOMAIN_SCOPE_CHECK_IDENTIFIERS`; assert the current set is exactly `get_order`, `get_refund_case`, and `get_ticket`, and assert each is `kind == "read"`, `executor == "business"`, `resource_type` is the matching domain resource, and `caller_allowlist == ["investigate"]`.
    Add `test_business_tool_executor_delegates_to_business_tool_service`: use source inspection or direct fake service execution to prove `BusinessToolExecutor.execute(...)` delegates to `BusinessToolService.invoke_tool(...)` and does not import repositories/adapters directly.
    Add `test_business_fact_service_denies_invalid_scope_before_adapter`: construct `BusinessFactService` with a fake `get_order` adapter and an invalid/empty merchant scope; assert the result is `permission_denied`, `scope_check_result == "denied"`, no business refs/fact are present, and the fake adapter was not called. This confirms there is a fail-closed boundary before data fetch for invalid scope.
  </action>
  <verify>
    <automated>uv run pytest tests/architecture/test_tool_contract_backstops.py -q</automated>
    <automated>uv run ruff check tests/architecture/test_tool_contract_backstops.py</automated>
  </verify>
  <acceptance_criteria>
    - The new architecture test fails if a future marker-bearing descriptor is not routed through `executor == "business"`.
    - The fake adapter test performs no DB I/O and proves invalid scope denial occurs before adapter invocation.
    - Runtime policy behavior is unchanged; existing `tests/tools/test_tool_platform.py` marker behavior still passes.
  </acceptance_criteria>
  <done>Domain-scope marker handoff has a test backstop without moving ownership checks into policy.</done>
</task>

<task type="auto">
  <name>Task 2: Run final Phase 40 verification and protected no-diff guards</name>
  <files>.planning/LOCAL-VALIDATION-ISSUES.md</files>
  <read_first>
    - .planning/LOCAL-VALIDATION-ISSUES.md
    - .planning/phases/40-tool-contract-validation-hardening/40-CONTEXT.md
    - tests/tools/test_catalog.py
    - tests/tools/test_tool_platform.py
    - tests/architecture/test_tool_contract_backstops.py
    - tests/agent/test_tools/test_unified_tool_manager.py
    - tests/test_execute_action.py
  </read_first>
  <action>
    Run focused verification through project-approved entrypoints:
    `uv run pytest tests/tools/test_catalog.py tests/tools/test_tool_platform.py tests/architecture/test_tool_contract_backstops.py -q`;
    `uv run pytest tests/agent/test_tools/test_unified_tool_manager.py::test_action_draft_caller_can_dispatch_action_tool tests/test_execute_action.py::test_action_draft_with_service_approval_result_creates_draft tests/test_execute_action.py::test_action_draft_tool_success_missing_draft_outcome_fails_closed tests/test_execute_action.py::test_action_draft_tool_success_invalid_draft_outcome_fails_closed -q`;
    `uv run ruff check src/tools/catalog.py src/tools/validation.py tests/tools/test_catalog.py tests/tools/test_tool_platform.py tests/architecture/test_tool_contract_backstops.py tests/agent/test_tools/test_unified_tool_manager.py tests/test_execute_action.py`;
    `git diff -- docs/contract-spec.md src/tools/contracts.py src/tools/manager.py src/tools/__init__.py`.
    If any local environment failure is discovered and handled, append a Chinese entry to `.planning/LOCAL-VALIDATION-ISSUES.md` following project rules. Do not stage or commit unrelated pre-existing local validation log changes.
  </action>
  <verify>
    <automated>uv run pytest tests/tools/test_catalog.py tests/tools/test_tool_platform.py tests/architecture/test_tool_contract_backstops.py -q</automated>
    <automated>uv run pytest tests/agent/test_tools/test_unified_tool_manager.py::test_action_draft_caller_can_dispatch_action_tool tests/test_execute_action.py::test_action_draft_with_service_approval_result_creates_draft tests/test_execute_action.py::test_action_draft_tool_success_missing_draft_outcome_fails_closed tests/test_execute_action.py::test_action_draft_tool_success_invalid_draft_outcome_fails_closed -q</automated>
    <automated>uv run ruff check src/tools/catalog.py src/tools/validation.py tests/tools/test_catalog.py tests/tools/test_tool_platform.py tests/architecture/test_tool_contract_backstops.py tests/agent/test_tools/test_unified_tool_manager.py tests/test_execute_action.py</automated>
    <automated>git diff -- docs/contract-spec.md src/tools/contracts.py src/tools/manager.py src/tools/__init__.py</automated>
  </verify>
  <acceptance_criteria>
    - Focused tests and ruff pass through `uv run`.
    - Protected no-diff guard prints nothing.
    - `UnifiedToolManager` behavior/API files are untouched.
    - If `.planning/LOCAL-VALIDATION-ISSUES.md` is changed, the new entry is append-only and directly tied to a Phase 40 local validation issue.
  </acceptance_criteria>
  <done>Phase 40 has final verification evidence and protected contract/API no-diff proof.</done>
</task>

</tasks>

<verification>
Plan 40-03 is complete when:
- `uv run pytest tests/tools/test_catalog.py tests/tools/test_tool_platform.py tests/architecture/test_tool_contract_backstops.py -q` passes.
- The focused action/legacy adapter regression command passes.
- `uv run ruff check ...` passes for touched source/tests.
- `git diff -- docs/contract-spec.md src/tools/contracts.py src/tools/manager.py src/tools/__init__.py` is empty.
</verification>

<success_criteria>
- Marker-bearing domain lookup tools are guarded to remain business-boundary reads.
- Business boundary invalid-scope denial is proven before adapter invocation.
- Phase 40 leaves `UnifiedToolManager`, spec, envelope, and §8.0 identity contracts untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/40-tool-contract-validation-hardening/40-03-SUMMARY.md` and `.planning/phases/40-tool-contract-validation-hardening/40-VERIFICATION.md`.
</output>
