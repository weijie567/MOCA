---
phase: 64-rag-risk-label-unification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/rag_context/risk_labels.py
  - tests/agent/rag_context/test_risk_labels.py
autonomous: true
requirements:
  - RAG-LABEL-01
  - RAG-LABEL-02
requirements_addressed:
  - RAG-LABEL-01
  - RAG-LABEL-02
must_haves:
  truths:
    - "RAG risk labels have one canonical backend owner."
    - "Existing label strings remain compatible, including manual_review_sensitive."
    - "Unknown labels are filtered fail-closed."
  artifacts:
    - path: "src/agent/rag_context/risk_labels.py"
      provides: "Canonical RAG risk label registry and helper API"
      contains: "RAG_RISK_LABEL_REGISTRY"
    - path: "tests/agent/rag_context/test_risk_labels.py"
      provides: "Unit/parity tests for label groups and helper behavior"
      contains: "test_registry_preserves_existing_label_strings"
  key_links:
    - from: "tests/agent/rag_context/test_risk_labels.py"
      to: "src/agent/rag_context/risk_labels.py"
      via: "direct registry import"
      pattern: "from src.agent.rag_context.risk_labels import"
---

<objective>
Create the canonical RAG risk label registry that later caller-migration plans consume.

Purpose: stop label-set drift at the source before modifying builder, recommendation generation, verifier, routing, and metrics.
Output: `src/agent/rag_context/risk_labels.py` and RED-to-GREEN unit/parity tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-rag-risk-label-unification/64-CONTEXT.md
@.planning/phases/64-rag-risk-label-unification/64-RESEARCH.md
@.planning/phases/64-rag-risk-label-unification/64-PATTERNS.md
@AGENTS.md
@src/agent/safety/taxonomy.py
@src/business/query/registry.py
@src/agent/rag_context/builder.py
@src/agent/rag_context/metrics.py
@src/agent/rag_context/verifier.py
@src/agent/rag_context/routing.py
@src/agent/nodes/recommendation_generation.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED RAG risk label registry tests</name>
  <files>tests/agent/rag_context/test_risk_labels.py</files>
  <read_first>
    @.planning/phases/64-rag-risk-label-unification/64-CONTEXT.md
    @.planning/phases/64-rag-risk-label-unification/64-PATTERNS.md
    @src/agent/rag_context/builder.py
    @src/agent/rag_context/metrics.py
    @src/agent/rag_context/verifier.py
    @src/agent/rag_context/routing.py
    @src/agent/nodes/recommendation_generation.py
  </read_first>
  <action>Create `tests/agent/rag_context/test_risk_labels.py` first. The tests must import the intended public API from `src.agent.rag_context.risk_labels`: `RAG_RISK_LABEL_REGISTRY`, `SAFE_EVIDENCE_RISK_LABELS`, `PROMPT_SAFE_RISK_LABELS`, `SEMANTIC_REVIEW_RISK_LABELS`, `MANUAL_REVIEW_TRIGGER_RISK_LABELS`, `ROUTING_RISK_LABELS`, `METRIC_LEVEL3_TRIGGER_LABELS`, `ROUTE_MANUAL_REVIEW_REASONS`, `ROUTE_STALE_OR_OCR_REASONS`, `filter_safe_evidence_risk_labels`, `filter_prompt_safe_risk_labels`, `is_prompt_safe_risk_label`, `requires_semantic_review_for_risk_hints`, `routing_risk_labels`, and `metric_level3_trigger_labels`. Assert the exact compatibility label set: `authority_checked`, `conflict`, `freshness_risk`, `high_risk`, `latest_version_checked`, `manual_review_sensitive`, `ocr_low_confidence`, `provenance_available`, `source_locator_available`, and `stale_evidence`. Assert `manual_review_sensitive` is in prompt-safe, semantic-review, manual-review-trigger, routing, and metric level-3 trigger groups. Assert unknown labels such as `raw_debug_secret` are dropped by both filter helpers. This RED task should fail until Task 2 adds the registry module.</action>
  <verify>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/rag_context/test_risk_labels.py -q --tb=short; test $? -ne 0</verify>
  <acceptance_criteria>
    - `tests/agent/rag_context/test_risk_labels.py` imports `src.agent.rag_context.risk_labels`.
    - Running the test command fails before production implementation because the module/helper surface does not exist.
  </acceptance_criteria>
  <done>RED registry tests are written and observed failing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement immutable RAG risk label registry</name>
  <files>src/agent/rag_context/risk_labels.py, tests/agent/rag_context/test_risk_labels.py</files>
  <read_first>
    @tests/agent/rag_context/test_risk_labels.py
    @src/agent/safety/taxonomy.py
    @src/business/query/registry.py
  </read_first>
  <action>Create `src/agent/rag_context/risk_labels.py`. Add a module docstring that explicitly distinguishes evidence/context risk labels from verifier/routing reason codes: risk labels are prompt-safe evidence/context labels such as `manual_review_sensitive`; route reason codes such as `semantic_provider_timeout` are not risk labels and are grouped in this module only when they are directly coupled to RAG verifier/routing semantics. Use immutable `frozenset` groups and a lightweight `RagRiskLabelRegistry` object named `RAG_RISK_LABEL_REGISTRY`. Define `SAFE_EVIDENCE_RISK_LABELS` and `PROMPT_SAFE_RISK_LABELS` with the full compatibility set including `manual_review_sensitive`. Define `SEMANTIC_REVIEW_RISK_LABELS` as `conflict`, `stale_evidence`, `ocr_low_confidence`, and `manual_review_sensitive`. Define `MANUAL_REVIEW_TRIGGER_RISK_LABELS` as the same four labels unless implementation evidence requires a strict subset; the name must include `TRIGGER` to avoid implying all labels are route reason codes. Define `ROUTING_RISK_LABELS` as `conflict`, `manual_review_sensitive`, `ocr_low_confidence`, and `stale_evidence`, and expose `routing_risk_labels()` returning a `frozenset`. Define `METRIC_LEVEL3_TRIGGER_LABELS` as `high_risk`, `manual_review_sensitive`, `semantic_timeout`, `semantic_provider_error`, and `semantic_malformed_output`; the `TRIGGER` name is required because this group intentionally includes semantic provider markers as well as evidence risk labels. Define `ROUTE_MANUAL_REVIEW_REASONS` with existing routing reasons `conflicting_evidence`, `conflict`, `manual_review_sensitive`, `semantic_ambiguous`, `semantic_provider_timeout`, `semantic_provider_error`, `semantic_provider_malformed`, `semantic_budget_claim_count_exceeded`, `semantic_budget_evidence_count_exceeded`, `semantic_budget_input_chars_exceeded`, `needs_semantic_review`, and `level2_partial_overlap_ambiguous`. Define `ROUTE_STALE_OR_OCR_REASONS` as `stale_evidence`, `freshness_invalid`, `effective_date_invalid`, and `ocr_low_confidence`. Implement helper functions that preserve order while filtering duplicate labels. Add explicit `__all__` containing `MANUAL_REVIEW_TRIGGER_RISK_LABELS`, `METRIC_LEVEL3_TRIGGER_LABELS`, `ROUTING_RISK_LABELS`, `routing_risk_labels`, and `metric_level3_trigger_labels`. Do not import graph nodes, LLM classes, database models, or frontend code.</action>
  <verify>
    - UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/rag_context/test_risk_labels.py -q --tb=short
    - UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/rag_context/risk_labels.py tests/agent/rag_context/test_risk_labels.py
  </verify>
  <acceptance_criteria>
    - `src/agent/rag_context/risk_labels.py` contains `RAG_RISK_LABEL_REGISTRY`.
    - `manual_review_sensitive` is accepted in every required trigger group.
    - The module docstring explains that route reason codes are not evidence risk labels.
    - Unknown labels are filtered out and never returned from helper APIs.
    - The focused pytest and ruff commands pass.
  </acceptance_criteria>
  <done>The registry exists and passes its unit/parity tests.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01 | Tampering | RAG label groups | mitigate | Immutable registry groups and parity tests preserve exact compatibility labels. |
| T-64-02 | Information Disclosure | prompt-safe label filtering | mitigate | Unknown labels remain filtered and tests assert fail-closed behavior. |
| T-64-03 | Repudiation | future label changes | mitigate | Explicit test assertions document the current label contract. |
</threat_model>

<verification>
Before declaring plan complete:
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/rag_context/test_risk_labels.py -q --tb=short`
- `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/rag_context/risk_labels.py tests/agent/rag_context/test_risk_labels.py`
</verification>

<success_criteria>
Plan 01 is complete when the canonical registry exists, all existing RAG label strings are represented, `manual_review_sensitive` is no longer missing from prompt-safe label ownership, and unknown labels remain fail-closed.
</success_criteria>

<output>
After completion, create `.planning/phases/64-rag-risk-label-unification/64-01-SUMMARY.md`.
</output>
