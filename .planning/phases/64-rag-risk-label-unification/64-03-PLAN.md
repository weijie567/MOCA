---
phase: 64-rag-risk-label-unification
plan: "03"
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - src/agent/rag_context/verifier.py
  - src/agent/rag_context/routing.py
  - src/agent/rag_context/metrics.py
  - tests/agent/rag_context/test_risk_labels.py
  - tests/agent/rag_context/test_semantic_verifier.py
  - tests/agent/rag_context/test_routing.py
  - tests/agent/rag_context/test_metrics.py
autonomous: true
requirements:
  - RAG-LABEL-01
  - RAG-LABEL-02
requirements_addressed:
  - RAG-LABEL-01
  - RAG-LABEL-02
must_haves:
  truths:
    - "Verifier semantic-review triggers derive from the RAG label registry."
    - "Routing manual-review and stale/OCR reason groups derive from the RAG label registry."
    - "Metrics level-3 trigger labels derive from the RAG label registry."
    - "Domain-rule reason algorithms are unchanged."
  artifacts:
    - path: "src/agent/rag_context/verifier.py"
      provides: "Registry-backed semantic-review trigger checks"
      contains: "requires_semantic_review_for_risk_hints"
    - path: "src/agent/rag_context/routing.py"
      provides: "Registry-backed route reason groups"
      contains: "ROUTE_MANUAL_REVIEW_REASONS"
  key_links:
    - from: "src/agent/rag_context/metrics.py"
      to: "src/agent/rag_context/risk_labels.py"
      via: "metric trigger helper import"
      pattern: "metric_level3_trigger_labels"
---

<objective>
Migrate verifier, routing, and metrics RAG label/reason trigger semantics to the registry.

Purpose: eliminate the remaining backend-local RAG label sets while preserving deterministic verifier and routing behavior.
Output: updated verifier/routing/metrics plus focused parity tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-rag-risk-label-unification/64-CONTEXT.md
@.planning/phases/64-rag-risk-label-unification/64-01-SUMMARY.md
@.planning/phases/64-rag-risk-label-unification/64-02-SUMMARY.md
@src/agent/rag_context/risk_labels.py
@src/agent/rag_context/verifier.py
@src/agent/rag_context/routing.py
@src/agent/rag_context/metrics.py
@src/agent/rag_context/domain_rules.py
@tests/agent/rag_context/test_semantic_verifier.py
@tests/agent/rag_context/test_verifier.py
@tests/agent/rag_context/test_routing.py
@tests/agent/rag_context/test_metrics.py
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add registry parity tests for verifier/routing/metrics triggers</name>
  <files>tests/agent/rag_context/test_risk_labels.py, tests/agent/rag_context/test_metrics.py</files>
  <read_first>
    @src/agent/rag_context/risk_labels.py
    @src/agent/rag_context/verifier.py
    @src/agent/rag_context/routing.py
    @src/agent/rag_context/metrics.py
    @tests/agent/rag_context/test_semantic_verifier.py
    @tests/agent/rag_context/test_routing.py
  </read_first>
  <action>Add or extend tests so registry-owned `SEMANTIC_REVIEW_RISK_LABELS` exactly contains `conflict`, `stale_evidence`, `ocr_low_confidence`, and `manual_review_sensitive`; `ROUTE_MANUAL_REVIEW_REASONS` contains `manual_review_sensitive`, `conflict`, and semantic provider error/timeout/malformed reasons; `ROUTE_STALE_OR_OCR_REASONS` contains `stale_evidence`, `freshness_invalid`, `effective_date_invalid`, and `ocr_low_confidence`; and `METRIC_LEVEL3_TRIGGER_LABELS` includes `high_risk`, `manual_review_sensitive`, `semantic_timeout`, `semantic_provider_error`, and `semantic_malformed_output`. Add a metric test if missing that proves `manual_review_sensitive` still makes `_level3_triggered` true through public evaluation helpers or a focused internal helper assertion.</action>
  <verify>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/rag_context/test_risk_labels.py tests/agent/rag_context/test_metrics.py -q --tb=short</verify>
  <acceptance_criteria>
    - Tests make registry trigger semantics explicit.
    - Tests do not require changing domain-rule algorithms.
  </acceptance_criteria>
  <done>Trigger parity tests are ready before caller migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate verifier semantic-review trigger checks</name>
  <files>src/agent/rag_context/verifier.py, tests/agent/rag_context/test_semantic_verifier.py</files>
  <read_first>
    @src/agent/rag_context/risk_labels.py
    @src/agent/rag_context/verifier.py
    @tests/agent/rag_context/test_semantic_verifier.py
  </read_first>
  <action>Import `requires_semantic_review_for_risk_hints` from `src.agent.rag_context.risk_labels`. Replace the two local set literals `{\"conflict\", \"stale_evidence\", \"ocr_low_confidence\", \"manual_review_sensitive\"}` in `check_level2_support` and `should_run_level3_semantic_verification` with this helper. Preserve return values, reason codes, semantic verifier provider behavior, and text matching logic.</action>
  <verify>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/rag_context/test_semantic_verifier.py tests/agent/rag_context/test_verifier.py -q --tb=short</verify>
  <acceptance_criteria>
    - `verifier.py` imports `requires_semantic_review_for_risk_hints`.
    - Semantic verifier tests remain green.
  </acceptance_criteria>
  <done>Verifier semantic-review triggers use the registry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate routing and metrics trigger groups</name>
  <files>src/agent/rag_context/routing.py, src/agent/rag_context/metrics.py, tests/agent/rag_context/test_routing.py, tests/agent/rag_context/test_metrics.py</files>
  <read_first>
    @src/agent/rag_context/risk_labels.py
    @src/agent/rag_context/routing.py
    @src/agent/rag_context/metrics.py
    @tests/agent/rag_context/test_routing.py
    @tests/agent/rag_context/test_metrics.py
  </read_first>
  <action>In `routing.py`, remove local `_ROUTE_MANUAL_REVIEW_REASONS` and `_ROUTE_STALE_OR_OCR_REASONS` definitions and import the registry groups as `_ROUTE_MANUAL_REVIEW_REASONS` and `_ROUTE_STALE_OR_OCR_REASONS` aliases or reference the registry names directly. Keep `_ROUTE_REFUSE_REASONS`, `_ROUTE_INSUFFICIENT_REASONS`, `_ROUTE_REGENERATE_REASONS`, and outcome sets local because they are broader route reason/status groups, not RAG risk labels.

In `metrics.py`, remove local `_SAFE_EVIDENCE_RISK_LABELS` and `_ROUTING_RISK_LABELS`; import `filter_safe_evidence_risk_labels`, `routing_risk_labels`, and `metric_level3_trigger_labels`. Apply this exact mapping:

| Existing logic | New registry helper/group | Must preserve |
|----------------|---------------------------|---------------|
| `_SAFE_EVIDENCE_RISK_LABELS` in `_risk_hints_from_evidence_refs` | `filter_safe_evidence_risk_labels(...)` | Only filter evidence risk labels; do not treat verifier reason codes as safe evidence labels. |
| `_ROUTING_RISK_LABELS` in citation-map reason extraction | `routing_risk_labels()` | Preserve current routing risk hint projection for `conflict`, `manual_review_sensitive`, `ocr_low_confidence`, and `stale_evidence`. |
| `_level3_triggered` checks for `high_risk`, `manual_review_sensitive`, and semantic provider markers | `metric_level3_trigger_labels()` | Preserve level-3 trigger semantics across both evidence labels and semantic provider markers. |

Do not merge these three semantics into one set. Preserve metric names, thresholds, status handling, and production-verifier path.</action>
  <verify>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/rag_context/test_routing.py tests/agent/rag_context/test_metrics.py -q --tb=short</verify>
  <acceptance_criteria>
    - `routing.py` no longer defines `_ROUTE_MANUAL_REVIEW_REASONS` or `_ROUTE_STALE_OR_OCR_REASONS` as local set literals.
    - `metrics.py` no longer defines `_SAFE_EVIDENCE_RISK_LABELS` or `_ROUTING_RISK_LABELS`.
    - Routing and metrics focused tests pass.
  </acceptance_criteria>
  <done>Routing and metrics consume registry-owned RAG label/reason groups.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-07 | Tampering | verifier trigger labels | mitigate | Both verifier semantic-review checks call one registry helper. |
| T-64-08 | Denial of Service | semantic verifier/domain rules | mitigate | Plan forbids algorithm rewrite; existing semantic/domain tests remain green. |
| T-64-09 | Repudiation | routing/eval semantics | mitigate | Routing and metrics tests pin registry-owned trigger groups. |
</threat_model>

<verification>
Before declaring plan complete:
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/rag_context/test_semantic_verifier.py tests/agent/rag_context/test_verifier.py tests/agent/rag_context/test_routing.py tests/agent/rag_context/test_metrics.py tests/agent/rag_context/test_risk_labels.py -q --tb=short`
- `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/rag_context/verifier.py src/agent/rag_context/routing.py src/agent/rag_context/metrics.py tests/agent/rag_context/test_semantic_verifier.py tests/agent/rag_context/test_routing.py tests/agent/rag_context/test_metrics.py tests/agent/rag_context/test_risk_labels.py`
</verification>

<success_criteria>
Plan 03 is complete when verifier, routing, and metrics no longer maintain duplicate RAG risk label trigger sets and all existing deterministic verifier/routing behavior remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/64-rag-risk-label-unification/64-03-SUMMARY.md`.
</output>
