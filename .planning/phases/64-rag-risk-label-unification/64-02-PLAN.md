---
phase: 64-rag-risk-label-unification
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/agent/rag_context/builder.py
  - src/agent/nodes/recommendation_generation.py
  - tests/agent/rag_context/test_context_builder.py
  - tests/agent/test_nodes/test_recommendation_generation.py
autonomous: true
requirements:
  - RAG-LABEL-01
  - RAG-LABEL-02
requirements_addressed:
  - RAG-LABEL-01
  - RAG-LABEL-02
must_haves:
  truths:
    - "ContextBuilder uses the registry for prompt-safe RAG labels."
    - "manual_review_sensitive survives risk_hints into safe citation/risk-label projections."
    - "Recommendation generation uses the same safe and routing label groups as the registry."
  artifacts:
    - path: "src/agent/rag_context/builder.py"
      provides: "Registry-backed prompt-safe risk label filtering"
      contains: "filter_prompt_safe_risk_labels"
    - path: "src/agent/nodes/recommendation_generation.py"
      provides: "Registry-backed evidence risk label filtering"
      contains: "filter_safe_evidence_risk_labels"
  key_links:
    - from: "src/agent/rag_context/builder.py"
      to: "src/agent/rag_context/risk_labels.py"
      via: "registry helper import"
      pattern: "from src.agent.rag_context.risk_labels import"
---

<objective>
Migrate prompt-context and recommendation-generation label filtering to the Phase 64 registry.

Purpose: fix the known `manual_review_sensitive` builder propagation bug and prevent recommendation generation from keeping a divergent local label set.
Output: updated builder/recommendation callers plus focused regression tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-rag-risk-label-unification/64-CONTEXT.md
@.planning/phases/64-rag-risk-label-unification/64-01-SUMMARY.md
@src/agent/rag_context/risk_labels.py
@src/agent/rag_context/builder.py
@src/agent/nodes/recommendation_generation.py
@tests/agent/rag_context/test_context_builder.py
@tests/agent/test_nodes/test_recommendation_generation.py
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED builder regression for manual_review_sensitive projection</name>
  <files>tests/agent/rag_context/test_context_builder.py</files>
  <read_first>
    @tests/agent/rag_context/test_context_builder.py
    @src/agent/rag_context/builder.py
    @src/agent/rag_context/schemas.py
  </read_first>
  <action>Add an async test to `tests/agent/rag_context/test_context_builder.py` proving `manual_review_sensitive` survives `ContextBuilder.build(..., risk_hints=[{"evidence_id": ..., "labels": ["manual_review_sensitive", "raw_debug_secret"]}])` into every existing safe context field that currently projects risk labels. Before writing assertions, inspect `src/agent/rag_context/schemas.py` and existing context-builder tests; do not add new context fields just to satisfy this test. In the current schema this should include citation-level labels and top-level `risk_labels` on existing prompt/final/memory/replay safe contexts. Assert `raw_debug_secret` is absent from the serialized bundle. This test should fail before `builder.py` imports the registry because current `_SAFE_RISK_LABELS` filters the label out.</action>
  <verify>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/rag_context/test_context_builder.py -q --tb=short; test $? -ne 0</verify>
  <acceptance_criteria>
    - The new test mentions `manual_review_sensitive` and `raw_debug_secret`.
    - Before caller migration, the test fails because `manual_review_sensitive` is missing from builder projection.
  </acceptance_criteria>
  <done>RED builder propagation regression is present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate builder to prompt-safe registry filtering</name>
  <files>src/agent/rag_context/builder.py, tests/agent/rag_context/test_context_builder.py</files>
  <read_first>
    @src/agent/rag_context/risk_labels.py
    @src/agent/rag_context/builder.py
    @tests/agent/rag_context/test_context_builder.py
  </read_first>
  <action>Remove local `_SAFE_RISK_LABELS` from `src/agent/rag_context/builder.py`. Import `filter_prompt_safe_risk_labels` from `src.agent.rag_context.risk_labels`. Update `_risk_labels_by_evidence_id` so it calls `filter_prompt_safe_risk_labels(hint.get("labels") or [])` and keeps existing per-evidence order/dedup semantics. Do not change tenant filtering, evidence lookup, truncation, trusted context projection, or domain-rule behavior.</action>
  <verify>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/rag_context/test_context_builder.py -q --tb=short</verify>
  <acceptance_criteria>
    - `builder.py` no longer contains `_SAFE_RISK_LABELS`.
    - `builder.py` imports `filter_prompt_safe_risk_labels`.
    - Context builder tests pass.
  </acceptance_criteria>
  <done>Builder uses registry-owned prompt-safe filtering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate recommendation generation label helpers</name>
  <files>src/agent/nodes/recommendation_generation.py, tests/agent/test_nodes/test_recommendation_generation.py</files>
  <read_first>
    @src/agent/rag_context/risk_labels.py
    @src/agent/nodes/recommendation_generation.py
    @tests/agent/test_nodes/test_recommendation_generation.py
  </read_first>
  <action>Remove local `_SAFE_EVIDENCE_RISK_LABELS` and `_ROUTING_RISK_LABELS` from `recommendation_generation.py`. Import `filter_safe_evidence_risk_labels` and `routing_risk_labels` from `src.agent.rag_context.risk_labels` only where the current code uses the corresponding old set. Update `_risk_hints_from_state` and any local risk label filtering to call registry helpers while preserving current output order and duplicate removal. Keep `_ACTIONABLE_RECOMMENDATIONS` unchanged; it belongs to Phase 63 action taxonomy and is not part of Phase 64 unless already migrated. Do not change LLM prompts, package validation, or evidence-required intent policy.</action>
  <verify>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_recommendation_generation.py -q --tb=short</verify>
  <acceptance_criteria>
    - `recommendation_generation.py` no longer contains `_SAFE_EVIDENCE_RISK_LABELS` or `_ROUTING_RISK_LABELS`.
    - Existing `test_risk_hints_merge_state_and_evidence_labels` still passes with `manual_review_sensitive` and `ocr_low_confidence`.
  </acceptance_criteria>
  <done>Recommendation generation consumes registry-owned RAG risk label groups.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-04 | Tampering | `ContextBuilder` label projection | mitigate | RED regression proves `manual_review_sensitive` cannot be silently dropped. |
| T-64-05 | Information Disclosure | prompt/final context labels | mitigate | Unknown labels are still filtered by registry helper and serialized-bundle assertions. |
| T-64-06 | Elevation of Privilege | recommendation risk hints | mitigate | Recommendation generation consumes the same safe/routing groups and cannot invent broader labels. |
</threat_model>

<verification>
Before declaring plan complete:
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/rag_context/test_context_builder.py tests/agent/test_nodes/test_recommendation_generation.py -q --tb=short`
- `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/rag_context/builder.py src/agent/nodes/recommendation_generation.py tests/agent/rag_context/test_context_builder.py tests/agent/test_nodes/test_recommendation_generation.py`
</verification>

<success_criteria>
Plan 02 is complete when `manual_review_sensitive` survives builder projection, unknown labels remain filtered, and prompt/recommendation code no longer owns copied RAG label sets.
</success_criteria>

<output>
After completion, create `.planning/phases/64-rag-risk-label-unification/64-02-SUMMARY.md`.
</output>
