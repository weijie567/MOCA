---
phase: 43-intent-recognition-multi-intent-tier-a
plan: 01
name: intent-policy-task-plan-contracts
status: ready
type: execute
wave: 1
depends_on: []
requirements:
  - IDR-02
requirements_addressed:
  - IDR-02
files_modified:
  - src/agent/intent_policy.py
  - tests/agent/test_intent_task_plan.py
autonomous: true
must_haves:
  truths:
    - "Intent policy can represent N=1 and N>1 user requests as a bounded TaskPlan without changing IntentResultV3."
    - "N=1 plan construction is behavior-equivalent to the existing single-intent effective route fields."
    - "Modifier normalization is conservative: small_talk drops, complaint folds only on the locked whitelist, and independent secondary intents stay explicit."
    - "Only a contiguous read_only prefix is executable; later non-read-only steps are returned as deferred steps."
    - "Invalid plans fail closed to the existing single-intent path and record plan_invalid_fallback_single."
  artifacts:
    - path: "src/agent/intent_policy.py"
      provides: "TaskStep/TaskPlan contracts plus deterministic construction, normalization, serialization, and prefix helpers"
      contains: "class TaskPlan"
    - path: "tests/agent/test_intent_task_plan.py"
      provides: "Policy-level IDR-02 tests for N=1 equivalence, normalization, deferral, and fail-closed behavior"
      contains: "test_invalid_plan_fails_closed_to_single_intent"
  key_links:
    - from: "src/agent/intent_policy.py"
      to: "src/agent/intent_policy.py::resolve_risk_decision"
      via: "executable prefix helper"
      pattern: "resolve_risk_decision\\(.*\\)\\.tier == \"read_only\""
---

<objective>
Create the Phase 43 intent-policy foundation: frozen TaskStep/TaskPlan contracts, deterministic plan construction from the existing single LLM result fields, conservative normalization, state-safe serialization helpers, read-only prefix selection, and fail-closed policy tests.

Purpose: IDR-02 must preserve multi-intent utterances without adding a new model call or weakening the single-intent route contract.
Output: policy helpers and focused non-DB tests that downstream classify-state wiring can consume directly.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-intent-recognition-multi-intent-tier-a/43-CONTEXT.md
@.planning/phases/43-intent-recognition-multi-intent-tier-a/43-RESEARCH.md
@.planning/phases/43-intent-recognition-multi-intent-tier-a/43-PATTERNS.md
@.planning/phases/43-intent-recognition-multi-intent-tier-a/43-VALIDATION.md
@.planning/intent-multi-a-codex-brief.md
@.planning/ARCHITECTURE-DEBT.md
@src/agent/intent_policy.py
@tests/agent/test_intent_routing.py
@tests/agent/test_intent_policy_registry.py
@tests/agent/test_nodes/test_classify_intent.py

<decision_trace>
D-01 Locked scope: add TaskStep/TaskPlan in src/agent/intent_policy.py, derive from existing LLM result fields and keyword signals, no new LLM call, no IntentResultV3/docs/prompts change, no new risk taxonomy.
D-02 Task plan contract: stable s1/s2/s3 IDs, intent/operation/entities/depends_on/relation fields, terminal_step_id points to a real step, max 3 steps, modifier relations do not appear in final steps.
D-03 Normalization: secondary small_talk drops; secondary complaint_escalation folds only for the locked root whitelist; independent query/draft/action intents remain explicit steps.
D-04 Execution semantics (Tier A single-step): the current turn only processes s1. executable_prefix is [s1] when s1 risk tier is read_only, else []; it is at most one step and is observational only. All steps after s1 (plan.steps[1:]) become deferred_steps regardless of tier. The effective single-intent step is always s1.
D-05 Trace/response payloads: serialize TaskPlan, executable prefix, deferred steps, and normalization records to plain dict/list values before state writes.
D-06 Tests: focused non-DB tests cover N=1 equivalence, dependency deferral, complaint folding, independent non-folding, high-risk deferral, fail-closed fallback, deferred visibility, and small-talk dropping.
D-08 Deferred scope: no automatic read-to-draft/action execution chain, no DAG/resume/parallel execution, no confidence calibration, no R0-R3 taxonomy.
</decision_trace>

<interfaces>
Existing Phase 42 policy contracts and helpers:
```python
@dataclass(frozen=True)
class SemanticIntent:
    intent: IntentLiteral
    operation: RequestedOperationLiteral
    entities: Mapping[str, Any]
    raw_confidence: float | None
    keyword_signals: tuple[IntentLiteral, ...]
    arbitration: tuple[str, ...]

@dataclass(frozen=True)
class RiskDecision:
    tier: RiskTierLiteral
    evidence_required: bool
    approval_required: bool
    reason_codes: tuple[str, ...]

def resolve_risk_decision(
    primary_intent: str,
    requested_operation: str,
    role: str | None = None,
    channel: str | None = None,
    routing_hints: dict[str, Any] | None = None,
) -> RiskDecision: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TaskPlan contracts and serialization tests</name>
  <files>src/agent/intent_policy.py, tests/agent/test_intent_task_plan.py</files>
  <read_first>
    - `.planning/phases/43-intent-recognition-multi-intent-tier-a/43-CONTEXT.md` decisions D-01, D-02, D-05, D-08 above.
    - `.planning/phases/43-intent-recognition-multi-intent-tier-a/43-PATTERNS.md` sections for `src/agent/intent_policy.py` and `tests/agent/test_intent_task_plan.py`.
    - `src/agent/intent_policy.py` existing frozen dataclasses, `IntentPolicyRegistry`, `_operation_for_selected_intent`, `REQUESTED_OPERATIONS`, `ORDINARY_INTENTS`, and `RiskTierLiteral` imports.
    - `tests/agent/test_intent_policy_registry.py` for registry wrapper expectations.
  </read_first>
  <action>
    Add `TaskStep` and `TaskPlan` frozen dataclasses beside `SemanticIntent`, `RiskDecision`, and `ClarificationDecision` per D-01 and D-02. Add a narrow relation literal covering `root`, `dependency`, `modifier`, and `parallel`, but ensure final `TaskPlan.steps` validation rejects `relation == "modifier"`.

    Add state-safe payload helpers in `src/agent/intent_policy.py`: `task_step_payload(step: TaskStep) -> dict[str, Any]`, `task_plan_payload(plan: TaskPlan | None) -> dict[str, Any] | None`, and helper payloads for executable/deferred step lists. Payloads must convert mappings, tuples, and dataclasses to plain dict/list/scalar values only.

    Add unit tests in new `tests/agent/test_intent_task_plan.py` that construct `SemanticIntent` directly and assert the dataclass field names, frozen behavior, N=1 `s1`/`root`/empty-dependency contract, terminal step validation, max 3 step validation, and payload serialization shape. Do not import or modify `src/agent/schemas.py`, `src/agent/prompts.py`, or `docs/contract-spec.md`.
  </action>
  <verify>
    <automated>uv run pytest tests/agent/test_intent_task_plan.py::test_task_plan_contract_serializes_to_plain_payload -q</automated>
    <automated>uv run pytest tests/agent/test_intent_policy_registry.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "class TaskStep|class TaskPlan|def task_plan_payload|def task_step_payload" src/agent/intent_policy.py` finds all four symbols.
    - `rg -n "relation=\"modifier\"" tests/agent/test_intent_task_plan.py` exists only in a validation/rejection assertion, not as a valid final step assertion.
    - `uv run pytest tests/agent/test_intent_task_plan.py::test_task_plan_contract_serializes_to_plain_payload -q` exits 0.
    - `git diff --exit-code -- src/agent/schemas.py src/agent/prompts.py docs/contract-spec.md` exits 0.
  </acceptance_criteria>
  <done>TaskPlan contracts exist, serialize to state-safe payloads, and preserve the locked schema/spec/prompt no-change boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build deterministic normalization and read-only prefix policy</name>
  <files>src/agent/intent_policy.py, tests/agent/test_intent_task_plan.py</files>
  <read_first>
    - `.planning/intent-multi-a-codex-brief.md` sections 3 and 4 for deterministic construction, normalization, fail-closed, and Tier A s1-only execution semantics.
    - `.planning/phases/43-intent-recognition-multi-intent-tier-a/43-VALIDATION.md` rows 43-01-01 and 43-01-02.
    - `src/agent/intent_policy.py` `derive_keyword_signals`, `arbitrate_intent`, `_operation_for_selected_intent`, `resolve_risk_decision`, and `RISK_POLICY_TABLE`.
    - `tests/agent/test_intent_routing.py` risk-tier examples for read_only, draft_only, suggest_action, approval_required, and forbidden_in_chat.
  </read_first>
  <action>
    Add a deterministic builder in `src/agent/intent_policy.py`, for example `build_task_plan(...)`, that accepts the effective `SemanticIntent`, raw secondary intents, requested operation, and candidate slots from the existing single LLM result. It must not call an LLM and must not import `ChatOpenAI`.

    Implement D-03 normalization exactly: drop secondary `small_talk` and record `modifier_dropped:small_talk`; fold secondary `complaint_escalation` only when the root intent is one of `compensation_suggestion`, `refund_troubleshooting`, `ticket_reply_draft`, `order_status_inquiry`, or `policy_qa`, and record `modifier_folded:complaint_as_severity`; keep `order_status_inquiry`, `refund_troubleshooting`, `policy_qa`, `ticket_reply_draft`, `compensation_suggestion`, `appeal_or_unban`, and `action_request` as explicit independent steps when secondary unless the locked complaint/small_talk rule applies.

    Implement same-intent merging explicitly. If a secondary intent duplicates the root or another planned step, do not create a duplicate step for the same intent. Merge it into the existing step only when `candidate_slots` already contains a non-lossy combined entity shape, such as list-valued identifier fields; record `same_intent_entities_merged`. If `candidate_slots` only contains scalar values and the builder cannot prove multiple entities without changing downstream slot shape, keep the single existing step with available entities and record `same_intent_entity_merge_limited`. Do not invent list-valued IDs from separate scalar slots.

    Add fail-closed validation: invalid intent/operation, `terminal_step_id` not present, final step count greater than 3, or final modifier relation returns no usable plan and includes `plan_invalid_fallback_single` in normalization records instead of throwing.

    Add an executable-prefix helper, for example `select_executable_prefix(...)`: call `resolve_risk_decision(s1.intent, s1.operation, ...)` for the root step only; return prefix = `[s1]` if its tier == `"read_only"` else `[]`, and deferred = `plan.steps[1:]` (all steps after s1, regardless of tier). Do not walk a multi-step contiguous read prefix — Tier A processes only s1 this turn. Never execute any deferred step.

    Extend `tests/agent/test_intent_task_plan.py` with exact behavior tests named:
    - `test_single_intent_plan_preserves_effective_fields`
    - `test_read_then_reply_draft_defers_second_step`
    - `test_second_read_step_is_deferred_not_dropped`  # 两个 read 次意图：s1 effective、第二个 read 步进 deferred，不静默丢（锁 B1）
    - `test_complaint_modifier_folded_with_safety_note_metadata`
    - `test_independent_secondary_intents_are_not_folded`
    - `test_high_risk_second_step_deferred`
    - `test_invalid_plan_fails_closed_to_single_intent`
    - `test_same_intent_secondary_merges_when_entities_are_non_lossy`
    - `test_same_intent_scalar_merge_traces_limited_entity_shape`
    - `test_small_talk_secondary_dropped`
  </action>
  <verify>
    <automated>uv run pytest tests/agent/test_intent_task_plan.py -q</automated>
    <automated>uv run ruff check src/agent/intent_policy.py tests/agent/test_intent_task_plan.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/agent/test_intent_task_plan.py -q` exits 0.
    - `rg -n "modifier_dropped:small_talk|modifier_folded:complaint_as_severity|plan_invalid_fallback_single|same_intent_entities_merged|same_intent_entity_merge_limited" src/agent/intent_policy.py tests/agent/test_intent_task_plan.py` finds all five records.
    - `rg -n "resolve_risk_decision\\(" src/agent/intent_policy.py` shows the executable-prefix helper uses the existing risk layer.
    - `rg -n "ChatOpenAI|with_structured_output|CLASSIFY_INTENT_SYSTEM" src/agent/intent_policy.py` has no matches.
    - `uv run ruff check src/agent/intent_policy.py tests/agent/test_intent_task_plan.py` exits 0.
  </acceptance_criteria>
  <done>Policy-level multi-intent planning, normalization, deferral, and fail-closed behavior are deterministic and test-locked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM result -> policy plan | Untrusted structured classifier output is transformed into internal TaskPlan contracts. |
| Policy plan -> AgentState | Internal dataclasses must be serialized before entering checkpointed graph state. |
| Multi-intent plan -> execution gate | Secondary draft/action/escalation requests must not bypass existing risk policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering | TaskPlan payload helpers | mitigate | Serialize TaskPlan/TaskStep/prefix/deferred values to plain dict/list/scalar values and test payload shape. |
| T-43-02 | Elevation of Privilege | executable prefix helper | mitigate | Use `resolve_risk_decision(...).tier == "read_only"` for s1 only; return all later steps as deferred, never executable. |
| T-43-06 | Denial of Service | plan builder | mitigate | Enforce max 3 final steps and fail closed to single-intent path with `plan_invalid_fallback_single`. |
</threat_model>

<verification>
Plan 43-01 is complete when:
- `uv run pytest tests/agent/test_intent_task_plan.py -q` passes.
- `uv run pytest tests/agent/test_intent_policy_registry.py tests/agent/test_intent_routing.py -q` passes.
- `uv run ruff check src/agent/intent_policy.py tests/agent/test_intent_task_plan.py` passes.
- `git diff --exit-code -- src/agent/schemas.py src/agent/prompts.py docs/contract-spec.md` exits 0.
</verification>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status
GOAL | Phase 43 | Bounded TaskPlan, s1-only current-turn handling, deferred confirmations, single-intent compatibility | 43-01, 43-02, 43-03 | COVERED
REQ | IDR-02 | Preserve multi-intent utterances as bounded Tier A TaskPlan with intact single-intent route contract | 43-01, 43-02, 43-03 | COVERED
RESEARCH | R-01 | Policy contracts, normalization, state-safe serialization, fail-closed plan validation | 43-01 | COVERED
RESEARCH | R-02 | `multi_target_request` guard handling without clearing approval/safety guards | 43-02 | COVERED
RESEARCH | R-03 | Final-response decoration across early returns and llm_outputs sync | 43-03 | COVERED
RESEARCH | R-04 | receive_request reset for task_plan/deferred_steps | 43-02 | COVERED
CONTEXT | D-01 | Locked scope and forbidden schema/spec/prompt/risk-taxonomy changes | 43-01, 43-02, 43-03 | COVERED
CONTEXT | D-02 | TaskStep/TaskPlan contract | 43-01 | COVERED
CONTEXT | D-03 | Conservative normalization | 43-01 | COVERED
CONTEXT | D-04 | Tier A processes only s1; all s2+ steps are deferred regardless of tier | 43-01, 43-02 | COVERED
CONTEXT | D-05 | Trace and final response visibility | 43-02, 43-03 | COVERED
CONTEXT | D-06 | Focused non-DB tests and required verification commands | 43-01, 43-02, 43-03 | COVERED
</source_audit>

<success_criteria>
- TaskStep and TaskPlan exist as frozen policy contracts.
- Plan construction uses existing semantic/root state and secondary intents only.
- N=1 policy tests preserve effective single-intent fields.
- Secondary draft/action/escalation steps after s1 are deferred.
- Invalid plans fail closed without exceptions or silent drops.
</success_criteria>

<output>
After completion, create `.planning/phases/43-intent-recognition-multi-intent-tier-a/43-01-SUMMARY.md`.
</output>
