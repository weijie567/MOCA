---
phase: 43-intent-recognition-multi-intent-tier-a
plan: 03
name: final-response-deferred-presentation-and-verification
status: complete
type: execute
wave: 3
depends_on:
  - 43-02
requirements:
  - IDR-02
requirements_addressed:
  - IDR-02
files_modified:
  - src/agent/nodes/final_response.py
  - tests/agent/test_nodes/test_final_response.py
  - .planning/ARCHITECTURE-DEBT.md
autonomous: true
must_haves:
  truths:
    - "Every final_response branch that emits visible text decorates deferred user requests before returning."
    - "Folded complaint severity creates a visible safety note even when deferred_steps is empty."
    - "Whenever llm_outputs.final_response exists, response_text matches the decorated final_response text."
    - "Required Phase 43 focused, graph, static, and full regression commands pass through uv run."
    - "Forbidden scope files and boundaries remain untouched: IntentResultV3, docs/contract-spec.md, prompts, risk-tier taxonomy, and no extra LLM call."
  artifacts:
    - path: "src/agent/nodes/final_response.py"
      provides: "Shared deferred-step and complaint-note response decorator"
      contains: "deferred_steps"
    - path: "tests/agent/test_nodes/test_final_response.py"
      provides: "Branch coverage for deferred confirmations, complaint note, and llm_outputs response_text sync"
      contains: "deferred_steps"
    - path: ".planning/ARCHITECTURE-DEBT.md"
      provides: "Intent-recognition debt ledger update for ID-04 after verified implementation"
      contains: "ID-04"
  key_links:
    - from: "src/agent/nodes/final_response.py"
      to: "AgentState.deferred_steps"
      via: "shared response decoration helper"
      pattern: "deferred_steps"
    - from: "tests/agent/test_nodes/test_final_response.py"
      to: "src/agent/nodes/final_response.py"
      via: "early-return response branches"
      pattern: "response_text"
---

<objective>
Make deferred multi-intent work visible to the user and close Phase 43 with regression and no-go verification.

Purpose: IDR-02 is not complete until later user requests are visible in final responses, complaint folding leaves a correction affordance, and the no-scope-creep boundaries are mechanically checked.
Output: final-response decorator/tests plus a verified architecture-debt ledger update for ID-04.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-intent-recognition-multi-intent-tier-a/43-CONTEXT.md
@.planning/phases/43-intent-recognition-multi-intent-tier-a/43-RESEARCH.md
@.planning/phases/43-intent-recognition-multi-intent-tier-a/43-PATTERNS.md
@.planning/phases/43-intent-recognition-multi-intent-tier-a/43-VALIDATION.md
@.planning/intent-multi-a-codex-brief.md
@.planning/ARCHITECTURE-DEBT.md
@.planning/phases/43-intent-recognition-multi-intent-tier-a/43-01-SUMMARY.md
@.planning/phases/43-intent-recognition-multi-intent-tier-a/43-02-SUMMARY.md
@src/agent/nodes/final_response.py
@src/agent/state.py
@tests/agent/test_nodes/test_final_response.py
@tests/agent/test_graph.py
@tests/architecture/test_phase32_static_contract.py

<interfaces>
Existing final_response early returns to decorate:
```python
if blocked_response and state.get("safety_snapshot_verified") is False: ...
if isinstance(clarification_request, dict): ...
if verification is not None: ...
if draft.get("recommended_action") == "retrieval_error": ...
if draft.get("recommended_action") in {"insufficient_evidence", "citation_invalid"}: ...
if _can_render_business_fact_response(state, draft): ...
return {"final_response": response_text, "llm_outputs": {...}}
```

Payloads produced by 43-02:
```python
state["deferred_steps"] == list[dict[str, Any]]
state["classification_trace"]["plan_normalization"] == list[str] | list[dict[str, Any]]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Decorate final responses with deferred confirmations and complaint note</name>
  <files>src/agent/nodes/final_response.py, tests/agent/test_nodes/test_final_response.py</files>
  <read_first>
    - `.planning/phases/43-intent-recognition-multi-intent-tier-a/43-CONTEXT.md` decisions D-05 and D-08.
    - `.planning/phases/43-intent-recognition-multi-intent-tier-a/43-PATTERNS.md` final_response and test_final_response sections.
    - `.planning/phases/43-intent-recognition-multi-intent-tier-a/43-VALIDATION.md` row 43-03-01 and threat T-43-04.
    - `src/agent/nodes/final_response.py` `_mapping`, `_string_values`, `_business_fact_llm_output`, `_verification_llm_output`, and every early return in `final_response`.
    - `tests/agent/test_nodes/test_final_response.py` deterministic template tests and clarification early-return test.
  </read_first>
  <action>
    Add one shared helper in `src/agent/nodes/final_response.py`, for example `_decorate_deferred_response(response_text: str, state: AgentState) -> str`, and use it before every return that emits user-visible response text.

    The helper must defensively parse `state.get("deferred_steps")` as a list of dicts, derive readable labels from each step's `intent` and `operation`, append a confirmation prompt that visibly lists every deferred request, and ask whether the user wants to continue. This presentation is separate from `clarification_request` and must not reuse missing-slot reason channels.

    The same helper must inspect `classification_trace.plan_normalization` for `modifier_folded:complaint_as_severity` and append a visible complaint-safety note containing the phrase `投诉情绪` even when `deferred_steps` is empty.

    Ensure branches that build `llm_outputs["final_response"]` use the decorated text as `response_text`. The retrieval-error branch currently returns no final_response llm_outputs; either keep that shape unchanged and decorate `final_response`, or add llm_outputs only if existing tests and conventions support it. Do not leak raw tool/action payloads into the decoration.

    Extend `tests/agent/test_nodes/test_final_response.py` with branch coverage for clarification, verification/manual-review or insufficient-evidence, retrieval-error, blocked safety-snapshot, business-fact, and completed-response paths. Each new test must assert deferred request visibility and, when `llm_outputs["final_response"]` exists, exact sync between `result["final_response"]` and `result["llm_outputs"]["final_response"]["response_text"]`. Add a complaint-folding test with `deferred_steps == []` that asserts `投诉情绪` is visible.
  </action>
  <verify>
    <automated>uv run pytest tests/agent/test_nodes/test_final_response.py -q</automated>
    <automated>uv run ruff check src/agent/nodes/final_response.py tests/agent/test_nodes/test_final_response.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "deferred_steps|plan_normalization|投诉情绪|response_text" src/agent/nodes/final_response.py tests/agent/test_nodes/test_final_response.py` finds production and test coverage for all four patterns.
    - `rg -n "retrieval_error|safety_snapshot_verified|insufficient_evidence|business_fact|manual_review|clarification" tests/agent/test_nodes/test_final_response.py` finds explicit coverage for every user-visible early-return family listed in this plan.
    - `uv run pytest tests/agent/test_nodes/test_final_response.py -q` exits 0.
    - `uv run ruff check src/agent/nodes/final_response.py tests/agent/test_nodes/test_final_response.py` exits 0.
    - In every tested branch with `llm_outputs["final_response"]`, `result["final_response"] == result["llm_outputs"]["final_response"]["response_text"]`.
  </acceptance_criteria>
  <done>Deferred steps and complaint-folding safety notes are visible across final_response early returns and synchronized to llm_outputs.</done>
</task>

<task type="auto">
  <name>Task 2: Run final regression, no-go sweep, and update architecture debt ledger</name>
  <files>.planning/ARCHITECTURE-DEBT.md</files>
  <read_first>
    - `.planning/phases/43-intent-recognition-multi-intent-tier-a/43-VALIDATION.md` full suite, lint command, threat references, and validation sign-off.
    - `.planning/intent-multi-a-codex-brief.md` sections 7, 8, and 9 for verification and one-vote blockers.
    - `AGENTS.md` local validation and architecture-debt ledger rules.
    - `.planning/ARCHITECTURE-DEBT.md` ID-04 and ID-DESIGN entries.
    - `git diff --stat` and `git diff --name-only` output after implementation.
  </read_first>
  <action>
    Run the full Phase 43 verification map and no-go sweep. If any local validation command fails due to an environment, startup, or test issue and you diagnose or fix it, append a Chinese entry to `.planning/LOCAL-VALIDATION-ISSUES.md` per AGENTS.md.

    Update `.planning/ARCHITECTURE-DEBT.md` under the intent-recognition section for ID-04 after the implementation and tests are green. Record the status as fixed/verified only if the commands below pass, and cite Phase 43 plus the changed source/test files. Keep target-state ID-DESIGN distinctions clear: Tier A is implemented; Tier B/C plan execution, DAG/resume/parallel behavior, and ID-02 confidence calibration remain not implemented.

    Do not edit `docs/contract-spec.md`, `src/agent/prompts.py`, or `src/agent/schemas.py`. Do not introduce any new LLM call, automatic read-to-draft/action chain, DAG/resume/parallel execution behavior, R0-R3 taxonomy, or confidence calibration.
  </action>
  <verify>
    <automated>uv run pytest tests/agent/test_intent_task_plan.py tests/agent/test_nodes/test_classify_intent.py tests/agent/test_nodes/test_final_response.py tests/agent/test_nodes/test_receive_request.py -q</automated>
    <automated>uv run pytest tests/agent/test_intent_adapter.py tests/agent/test_intent_policy_registry.py tests/agent/test_intent_golden_contract.py tests/agent/test_intent_routing.py tests/agent/test_nodes/test_classify_intent.py tests/agent/test_graph.py tests/architecture/test_phase32_static_contract.py -q</automated>
    <automated>uv run ruff check src/agent tests/agent</automated>
    <automated>git diff --exit-code -- docs/contract-spec.md src/agent/prompts.py src/agent/schemas.py</automated>
  </verify>
  <acceptance_criteria>
    - Focused command exits 0: `uv run pytest tests/agent/test_intent_task_plan.py tests/agent/test_nodes/test_classify_intent.py tests/agent/test_nodes/test_final_response.py tests/agent/test_nodes/test_receive_request.py -q`.
    - Full required command exits 0: `uv run pytest tests/agent/test_intent_adapter.py tests/agent/test_intent_policy_registry.py tests/agent/test_intent_golden_contract.py tests/agent/test_intent_routing.py tests/agent/test_nodes/test_classify_intent.py tests/agent/test_graph.py tests/architecture/test_phase32_static_contract.py -q`.
    - Lint exits 0: `uv run ruff check src/agent tests/agent`.
    - `git diff --exit-code -- docs/contract-spec.md src/agent/prompts.py src/agent/schemas.py` exits 0.
    - `rg -n "TaskPlan|task_plan|deferred_steps|modifier_folded:complaint_as_severity|plan_invalid_fallback_single" src/agent tests/agent .planning/ARCHITECTURE-DEBT.md` finds implementation, test, and ledger evidence.
    - `rg -n "R0|R1|R2|R3|dag|resume|parallel execution|confidence calibration" src/agent/intent_policy.py src/agent/nodes/classify_intent.py src/agent/nodes/final_response.py` has no matches introduced by Phase 43 implementation text.
  </acceptance_criteria>
  <done>Phase 43 has full local verification, forbidden-file no-diff evidence, and an updated intent-recognition architecture-debt record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AgentState.deferred_steps -> user-visible response | Internal plan metadata becomes user-facing text and must be bounded and non-sensitive. |
| Early response branches -> llm_outputs | Multiple deterministic branches must keep final_response and llm_outputs synchronized. |
| Implementation diff -> authorization/action boundary | Multi-intent recognition must not create automatic execution behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-02 | Elevation of Privilege | final response / phase no-go sweep | mitigate | Verify deferred draft/action/escalation steps are only presented for confirmation and no automatic chain is added. |
| T-43-04 | Repudiation / Tampering | final_response early returns | mitigate | Route every visible response through one decorator and assert llm_outputs response_text sync in tests. |
| T-43-07 | Information Disclosure | deferred-step rendering | mitigate | Render only intent/operation-level labels from serialized steps, not raw tool/action payloads or hidden evidence internals. |
</threat_model>

<verification>
Overall Phase 43 gate:
- `uv run pytest tests/agent/test_intent_task_plan.py tests/agent/test_nodes/test_classify_intent.py tests/agent/test_nodes/test_final_response.py tests/agent/test_nodes/test_receive_request.py -q`
- `uv run pytest tests/agent/test_intent_adapter.py tests/agent/test_intent_policy_registry.py tests/agent/test_intent_golden_contract.py tests/agent/test_intent_routing.py tests/agent/test_nodes/test_classify_intent.py tests/agent/test_graph.py tests/architecture/test_phase32_static_contract.py -q`
- `uv run ruff check src/agent tests/agent`
- `git diff --exit-code -- docs/contract-spec.md src/agent/prompts.py src/agent/schemas.py`
</verification>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status
GOAL | Phase 43 | Surface all non-executed steps as deferred confirmations in trace and final response | 43-03 | COVERED
REQ | IDR-02 | deferred_steps visible in trace/final response while no non-read-only work executes in current turn | 43-03 | COVERED
RESEARCH | R-03 | final_response early-return decoration and llm_outputs response_text sync | 43-03 | COVERED
RESEARCH | R-05 | graph/regression/static full verification with MOCA-safe commands | 43-03 | COVERED
CONTEXT | D-05 | final responses visibly list deferred user requests and complaint folding safety note | 43-03 | COVERED
CONTEXT | D-06 | required focused/full pytest and ruff commands | 43-03 | COVERED
CONTEXT | D-08 | forbidden scope sweep: no schema/spec/prompt/taxonomy/LLM-chain changes | 43-03 | COVERED
</source_audit>

<success_criteria>
- Deferred confirmations and complaint note are visible in final_response outputs.
- llm_outputs final response text is synchronized wherever present.
- Focused, full, graph/static, and lint gates pass through `uv run`.
- Forbidden source files have no diff.
- `.planning/ARCHITECTURE-DEBT.md` truthfully records Tier A completion and remaining non-Tier-A scope.
</success_criteria>

<output>
After completion, create `.planning/phases/43-intent-recognition-multi-intent-tier-a/43-03-SUMMARY.md`.
</output>
