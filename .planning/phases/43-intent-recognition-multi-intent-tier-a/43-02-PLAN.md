---
phase: 43-intent-recognition-multi-intent-tier-a
plan: 02
name: classify-state-task-plan-wiring
status: ready
type: execute
wave: 2
depends_on:
  - 43-01
requirements:
  - IDR-02
requirements_addressed:
  - IDR-02
files_modified:
  - src/agent/state.py
  - src/agent/nodes/receive_request.py
  - src/agent/nodes/classify_intent.py
  - tests/agent/test_nodes/test_receive_request.py
  - tests/agent/test_nodes/test_classify_intent.py
autonomous: true
must_haves:
  truths:
    - "AgentState has additive task_plan and deferred_steps fields and receive_request resets both every turn."
    - "classify_intent writes serialized task_plan, executable_prefix, deferred_steps, and plan_normalization into classification_trace."
    - "N=1 effective fields remain compatible: primary_intent, requested_operation, risk_tier, route_decision, required slots, and current tests do not regress."
    - "Valid Tier A multi-target plans do not get blocked by the legacy multi_target_request clarification path."
    - "Approval-chat and safety-sensitive pre-route guards remain fail-closed and are not cleared by multi-intent handling."
  artifacts:
    - path: "src/agent/state.py"
      provides: "Additive AgentState fields for serialized task plan and deferred steps"
      contains: "task_plan"
    - path: "src/agent/nodes/receive_request.py"
      provides: "Per-turn reset for task_plan and deferred_steps"
      contains: "\"deferred_steps\": []"
    - path: "src/agent/nodes/classify_intent.py"
      provides: "TaskPlan integration and effective single-intent compatibility fields"
      contains: "plan_normalization"
    - path: "tests/agent/test_nodes/test_classify_intent.py"
      provides: "Node-level coverage for serialized plan state, multi-target guard handling, and no-go guard preservation"
      contains: "multi_target_request"
  key_links:
    - from: "src/agent/nodes/classify_intent.py"
      to: "src/agent/intent_policy.py"
      via: "TaskPlan builder and prefix helpers from 43-01"
      pattern: "build_task_plan|select_executable_prefix"
    - from: "src/agent/nodes/receive_request.py"
      to: "src/agent/state.py"
      via: "ephemeral per-turn reset"
      pattern: "\"task_plan\": None"
---

<objective>
Wire the policy-level TaskPlan into graph state and classification without changing the existing single-intent route surface.

Purpose: downstream routers and nodes must continue consuming one effective intent/operation/risk decision while the full multi-intent plan is preserved in trace/state for deferred presentation.
Output: AgentState additions, receive_request reset, classify_intent integration, and focused node tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-intent-recognition-multi-intent-tier-a/43-CONTEXT.md
@.planning/phases/43-intent-recognition-multi-intent-tier-a/43-RESEARCH.md
@.planning/phases/43-intent-recognition-multi-intent-tier-a/43-PATTERNS.md
@.planning/phases/43-intent-recognition-multi-intent-tier-a/43-VALIDATION.md
@.planning/intent-multi-a-codex-brief.md
@.planning/phases/43-intent-recognition-multi-intent-tier-a/43-01-SUMMARY.md
@src/agent/state.py
@src/agent/nodes/receive_request.py
@src/agent/nodes/classify_intent.py
@src/agent/routing.py
@src/agent/nodes/clarification_gate.py
@src/agent/schemas.py
@tests/agent/test_nodes/test_receive_request.py
@tests/agent/test_nodes/test_classify_intent.py

<interfaces>
Existing classify-state conversion point:
```python
def intent_result_to_state(
    result: IntentResultV3,
    prior_llm_outputs: dict[str, Any] | None = None,
    pre_route: PreRouteDecision | None = None,
    user_query: str = "",
    role: str | None = None,
    channel: str | None = None,
) -> dict[str, Any]: ...
```

Existing per-turn reset owner:
```python
async def receive_request(state: AgentState) -> dict:
    """Reset per-turn state so checkpointed graph context cannot leak stale context."""
```

Existing routing guard behavior to preserve:
```python
if routing_hints.get("pre_route_disposition") == "approval_chat_not_trusted":
    return "clarification_gate"
if routing_hints.get("clarification_reason") == "approval_chat_not_trusted":
    return "clarification_gate"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add state fields and per-turn reset coverage</name>
  <files>src/agent/state.py, src/agent/nodes/receive_request.py, tests/agent/test_nodes/test_receive_request.py</files>
  <read_first>
    - `.planning/phases/43-intent-recognition-multi-intent-tier-a/43-CONTEXT.md` decisions D-01 and D-05.
    - `.planning/phases/43-intent-recognition-multi-intent-tier-a/43-VALIDATION.md` row 43-02-02 and Wave 0 reset requirement.
    - `src/agent/state.py` AgentState ephemeral section around `classification_trace`, `secondary_intents`, and `routing_hints`.
    - `src/agent/nodes/receive_request.py` return dict that resets ephemeral fields.
    - `tests/agent/test_nodes/test_receive_request.py` existing `test_receive_request_resets_ephemeral` and AgentState annotation tests.
  </read_first>
  <action>
    Add `task_plan: dict[str, Any] | None` and `deferred_steps: list[dict[str, Any]]` to the AgentState ephemeral section near `classification_trace` and `secondary_intents` per D-01.

    Update `receive_request` to reset `"task_plan": None` and `"deferred_steps": []` every turn near the existing `classification_trace` and `secondary_intents` reset. This is a stale-state security control, not a presentation feature.

    Extend `tests/agent/test_nodes/test_receive_request.py` so a stale state containing `task_plan` and `deferred_steps` resets to `None` and `[]`. Add or extend an annotation test asserting both fields exist in `AgentState.__annotations__`.
  </action>
  <verify>
    <automated>uv run pytest tests/agent/test_nodes/test_receive_request.py -q</automated>
    <automated>uv run ruff check src/agent/state.py src/agent/nodes/receive_request.py tests/agent/test_nodes/test_receive_request.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "task_plan: dict\\[str, Any\\] \\| None|deferred_steps: list\\[dict\\[str, Any\\]\\]" src/agent/state.py` finds both AgentState annotations.
    - `rg -n "\"task_plan\": None|\"deferred_steps\": \\[\\]" src/agent/nodes/receive_request.py` finds both reset values.
    - `uv run pytest tests/agent/test_nodes/test_receive_request.py -q` exits 0.
    - `uv run ruff check src/agent/state.py src/agent/nodes/receive_request.py tests/agent/test_nodes/test_receive_request.py` exits 0.
  </acceptance_criteria>
  <done>Per-turn TaskPlan/deferred state cannot leak across checkpointed turns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate TaskPlan into classify_intent while preserving route fields</name>
  <files>src/agent/nodes/classify_intent.py, tests/agent/test_nodes/test_classify_intent.py</files>
  <read_first>
    - `.planning/phases/43-intent-recognition-multi-intent-tier-a/43-CONTEXT.md` decisions D-01, D-04, D-05, and D-08.
    - `.planning/phases/43-intent-recognition-multi-intent-tier-a/43-PATTERNS.md` classify_intent, routing, and FakeLLM sections.
    - `.planning/phases/43-intent-recognition-multi-intent-tier-a/43-VALIDATION.md` row 43-02-01.
    - `src/agent/nodes/classify_intent.py` `_semantic_payload`, `_risk_payload`, `_classify_layers`, `intent_result_to_state`, `_deterministic_classification_update`, and classifier fallback path.
    - `src/agent/routing.py` approval/safety clarification guard behavior.
    - `src/agent/schemas.py` `IntentResultV3`, which must not be modified.
    - `tests/agent/test_nodes/test_classify_intent.py` `_intent_v3`, FakeLLM use, and existing N=1 field assertions.
  </read_first>
  <action>
    Import the TaskPlan helpers produced by Plan 43-01 into `src/agent/nodes/classify_intent.py`. In `intent_result_to_state`, build the plan after `_apply_pre_route_to_semantic(...)` establishes the effective root semantic and before `route_after_intent(update)`.

    Compute executable prefix and deferred steps with the 43-01 helper. The effective single-intent compatibility fields (`primary_intent`, `requested_operation`, `risk_tier`, `required_slots`, `current_intent`, `last_intent`, `route_decision`) always come from s1 (the root step), behavior-equivalent to the existing single-intent handling for s1. `executable_prefix` is `[s1]` when s1 is `read_only` else `[]` and is recorded in `classification_trace` for observability only. All steps after s1 go into `deferred_steps` regardless of tier. This path must never become a plan-driven automatic multi-step read / draft / action / escalation chain. Preserve existing required-slot policy lookup for s1. For non-read-only s1, keep the behavior equivalent to the existing single-intent handling for s1 and do not add any action/draft/approval state such as `proposed_action`, `action_draft`, `approval_result`, or `action_result`.

    Handle the legacy `multi_target_request` guard exactly: after a valid TaskPlan exists, clear only the current-turn clarification effect of `pre_route.disposition == "multi_target_request"` by not setting `routing_hints["requires_clarification"]` or `routing_hints["clarification_reason"]` for that disposition and by passing a non-clarifying pre-route value into `decide_clarification`. Keep `classification_trace["pre_route_decision"]` recording the original pre-route decision. Do not clear or weaken `approval_chat_not_trusted` or `safety_sensitive` guards.

    Add `task_plan`, `deferred_steps`, `executable_prefix`, and `plan_normalization` to `classification_trace`, and write serialized `task_plan` plus `deferred_steps` to the returned update. Keep `llm_outputs["intent_classification"]["classification_trace"]` exactly synchronized with `update["classification_trace"]`.

    Preserve deterministic active-flow/short-reply and classifier-validation fallback paths. If those paths add plan fields, they must be N=1-equivalent or empty and must not introduce multi-intent behavior.

    Extend `tests/agent/test_nodes/test_classify_intent.py` with focused tests for: N=1 compatibility fields, serialized plan trace/state values, valid multi-target s1-only handling, high-risk secondary deferral, approval-chat guard still routing to clarification, safety-sensitive guard still applying existing risk/route behavior, and non-read-only `s1` compatibility behavior. The non-read-only `s1` test must assert `classification_trace["executable_prefix"] == []`, the effective fields still match existing single-intent handling for `s1`, later steps are deferred, and no action/draft/approval state such as `proposed_action`, `action_draft`, `approval_result`, or `action_result` appears in the classify update. Add a test asserting a two-read-step plan keeps s1 effective and puts the second read step into deferred_steps (not silently dropped), with executable_prefix == ["s1"].
  </action>
  <verify>
    <automated>uv run pytest tests/agent/test_nodes/test_classify_intent.py -q</automated>
    <automated>uv run pytest tests/agent/test_intent_routing.py tests/agent/test_intent_golden_contract.py -q</automated>
    <automated>uv run ruff check src/agent/nodes/classify_intent.py tests/agent/test_nodes/test_classify_intent.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "\"task_plan\"|\"deferred_steps\"|\"executable_prefix\"|\"plan_normalization\"" src/agent/nodes/classify_intent.py tests/agent/test_nodes/test_classify_intent.py` finds all four trace/state keys in production code and tests.
    - `rg -n "multi_target_request" src/agent/nodes/classify_intent.py tests/agent/test_nodes/test_classify_intent.py` finds explicit guard handling and test coverage.
    - `rg -n "executable_prefix.*\\[\\]|proposed_action|action_draft|approval_result|action_result" tests/agent/test_nodes/test_classify_intent.py` finds non-read s1 prefix-empty and no automatic action/draft state assertions.
    - `rg -n "approval_chat_not_trusted|safety_sensitive" tests/agent/test_nodes/test_classify_intent.py` finds regression assertions that these guards remain protected.
    - `uv run pytest tests/agent/test_nodes/test_classify_intent.py -q` exits 0.
    - `uv run pytest tests/agent/test_intent_routing.py tests/agent/test_intent_golden_contract.py -q` exits 0.
    - `git diff --exit-code -- src/agent/schemas.py src/agent/prompts.py docs/contract-spec.md` exits 0.
  </acceptance_criteria>
  <done>Classification preserves the full task plan while exposing only existing effective single-intent fields to current routers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TaskPlan policy output -> graph state | New plan data crosses into checkpointed LangGraph state. |
| Pre-route detection -> router hints | Legacy clarification hints influence whether a user request can continue. |
| Effective plan step -> existing route contract | Multi-intent state must collapse to one safe effective route for current-turn execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-02 | Elevation of Privilege | classify_intent effective fields | mitigate | Only s1 can become current effective route fields; all s2+ steps remain in deferred_steps regardless of tier. |
| T-43-03 | Tampering | multi_target_request guard handling | mitigate | Neutralize only valid-plan multi_target clarification; keep approval_chat_not_trusted and safety_sensitive guards intact with tests. |
| T-43-05 | Information Disclosure / Tampering | receive_request reset | mitigate | Reset task_plan and deferred_steps on every turn to avoid stale deferred request leakage. |
</threat_model>

<verification>
Plan 43-02 is complete when:
- `uv run pytest tests/agent/test_nodes/test_receive_request.py tests/agent/test_nodes/test_classify_intent.py -q` passes.
- `uv run pytest tests/agent/test_intent_task_plan.py tests/agent/test_intent_routing.py tests/agent/test_intent_golden_contract.py -q` passes.
- `uv run ruff check src/agent/state.py src/agent/nodes/receive_request.py src/agent/nodes/classify_intent.py tests/agent/test_nodes/test_receive_request.py tests/agent/test_nodes/test_classify_intent.py` passes.
- `git diff --exit-code -- src/agent/schemas.py src/agent/prompts.py docs/contract-spec.md` exits 0.
</verification>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status
GOAL | Phase 43 | Preserve multiple requests while existing routers consume one effective current-turn route | 43-02 | COVERED
REQ | IDR-02 | Existing single-intent route contract stays intact while N>1 records ordered steps and deferred_steps | 43-02 | COVERED
RESEARCH | R-02 | multi_target_request guard handling without clearing approval/safety guards | 43-02 | COVERED
RESEARCH | R-04 | receive_request reset for task_plan/deferred_steps | 43-02 | COVERED
CONTEXT | D-01 | Add AgentState task_plan/deferred_steps only | 43-02 | COVERED
CONTEXT | D-04 | Effective current-turn fields always come from s1; all s2+ steps are deferred regardless of tier | 43-02 | COVERED
CONTEXT | D-05 | classification_trace contains task_plan, executable_prefix, deferred_steps, and plan_normalization | 43-02 | COVERED
CONTEXT | D-08 | No automatic read-to-draft/action execution chain or schema/prompt/spec changes | 43-02 | COVERED
</source_audit>

<success_criteria>
- AgentState and receive_request own the new ephemeral fields safely.
- classify_intent returns serialized task_plan/deferred_steps and synchronized classification_trace/llm_outputs.
- N=1 route fields and existing tests remain green.
- Valid multi-target requests process only s1 while preserving all s2+ steps as deferred.
- Approval and safety-sensitive pre-route guards remain fail-closed.
</success_criteria>

<output>
After completion, create `.planning/phases/43-intent-recognition-multi-intent-tier-a/43-02-SUMMARY.md`.
</output>
