---
phase: 14-demo-action-executor-boundary
plan: 03
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  - src/agent/nodes/action_draft.py
  - src/agent/nodes/execute_action.py
  - src/agent/graph.py
  - src/tools/catalog.py
  - src/tools/manager.py
  - tests/architecture/test_action_draft_boundaries.py
  - tests/test_execute_action.py
  - tests/test_graph_routing.py
  - tests/agent/test_graph.py
autonomous: true
requirements:
  - DEMO-01
  - DEMO-02
must_haves:
  truths:
    - "The canonical LangGraph node is named action_draft."
    - "requested_operation=\"execute_action\" remains an intent-layer value only."
    - "The create_coupon_grant_draft tool remains node-only and callable by action_draft."
    - "Any retained execute_action path is a quarantined compatibility shim, not a second write path."
  artifacts:
    - path: "src/agent/nodes/action_draft.py"
      provides: "Canonical action draft graph node"
      contains: "async def action_draft"
    - path: "src/agent/nodes/execute_action.py"
      provides: "Compatibility shim only"
      contains: "Phase 14 compatibility shim"
    - path: "src/agent/graph.py"
      provides: "Graph registration and route keys for action_draft"
      contains: "action_draft"
    - path: "tests/architecture/test_action_draft_boundaries.py"
      provides: "Static owner/quarantine boundary tests"
      contains: "execute_action"
  key_links:
    - from: "src/agent/graph.py"
      to: "src/agent/nodes/action_draft.py"
      via: "StateGraph.add_node and route_after_* mappings"
      pattern: "action_draft"
    - from: "src/tools/catalog.py"
      to: "src/tools/manager.py"
      via: "caller_allowlist and write-side-effect guard"
      pattern: "caller_allowlist.*action_draft"
---

<objective>
Rename the graph action path to the canonical `action_draft` boundary and quarantine the legacy `execute_action` name.

Purpose: Demo draft semantics must be visible in graph/node/tool ownership, while intent taxonomy can still record that the user requested an execution.
Output: Canonical node file, graph routing updates, node-only tool allowlist updates, compatibility shim with a Phase 15 removal gate, and static boundary tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-demo-action-executor-boundary/14-CONTEXT.md
@.planning/phases/14-demo-action-executor-boundary/14-RESEARCH.md
@.planning/phases/14-demo-action-executor-boundary/14-VALIDATION.md
@.planning/phases/14-demo-action-executor-boundary/14-PATTERNS.md
@docs/phase-13-17-architecture-plan.md
@docs/contract-spec.md
@src/agent/nodes/execute_action.py
@src/agent/graph.py
@src/tools/catalog.py
@src/tools/manager.py
@tests/test_execute_action.py
@tests/test_graph_routing.py
@tests/agent/test_graph.py
@tests/architecture/test_approval_boundaries.py

<interfaces>
Current contracts to preserve and rewrite:

```text
src/agent/nodes/execute_action.py:
- ACTION_TOOL_NAME = "create_coupon_grant_draft"
- REQUIRED_APPROVAL_RESULT_FIELDS includes Phase 13 revision/hash fields
- async def execute_action(state, config)

src/agent/graph.py:
- route_after_risk(state) currently returns "execute_action"
- route_after_approval(state) currently returns "execute_action"
- builder.add_node("execute_action", execute_action)

src/tools/catalog.py / src/tools/manager.py:
- Tool descriptor caller_allowlist currently includes "execute_action"
- Manager write-side-effect guard currently recognizes caller_node == "execute_action"
```
</interfaces>
</context>

<architecture_alignment>
## Architecture Alignment

Owner and contract first:
- Canonical owner package: `src/agent/nodes/action_draft.py` owns graph node semantics; `src/actions` remains service/persistence owner.
- Contract: graph routes return only canonical node key `action_draft` for approved or auto-allowed draft creation.

Move/rewrite to owner:
- Move node implementation to `action_draft`; update graph registration, route maps, trace step node names, tool caller context, and tests.
- Keep `create_coupon_grant_draft` as the tool name per D-20.

Delete/quarantine old paths:
- `src/agent/nodes/execute_action.py`, if retained, is a named compatibility shim only. Owner: Phase 14 action draft boundary. Forbidden new references: no `src/` imports of `src.agent.nodes.execute_action` except the shim file itself. Removal gate: Phase 15 Replay Event Contract must remove the shim before Phase 15 verification once replay/timeline compatibility handles legacy node names; target date no later than 2026-07-16 unless Phase 15 is replanned.
- Do not rename intent-layer `requested_operation="execute_action"` per D-23.

Boundary tests before expansion:
- Add AST/static tests proving canonical graph imports `action_draft`, `execute_action` is not registered as a node, and the shim is not imported by new backend code.
</architecture_alignment>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical action_draft boundary tests</name>
  <files>tests/architecture/test_action_draft_boundaries.py, tests/test_graph_routing.py, tests/agent/test_graph.py</files>
  <read_first>
    - docs/phase-13-17-architecture-plan.md
    - .planning/phases/14-demo-action-executor-boundary/14-CONTEXT.md
    - tests/architecture/test_approval_boundaries.py
    - tests/test_graph_routing.py
    - tests/agent/test_graph.py
    - src/agent/graph.py
    - src/agent/nodes/execute_action.py
  </read_first>
  <behavior>
    - Test 1: `route_after_risk` returns `action_draft` for verified auto-allowed actions.
    - Test 2: `route_after_approval` returns `action_draft` for trusted approved `approval_result.v1`.
    - Test 3: graph node set includes `action_draft` and does not register `execute_action`.
    - Test 4: source import scan forbids `src.agent.nodes.execute_action` imports outside the compatibility shim and tests explicitly marked as legacy/intent taxonomy checks.
  </behavior>
  <action>
    Create `tests/architecture/test_action_draft_boundaries.py` using the AST import-scan style from `tests/architecture/test_approval_boundaries.py`. Update graph routing tests to expect canonical route key `action_draft` per D-18 while preserving tests that `requested_operation="execute_action"` remains valid intent taxonomy per D-23. Add explicit assertions that the compatibility shim has an owner comment and Phase 15 removal gate text per D-19 and D-22.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_action_draft_boundaries.py tests/test_graph_routing.py tests/agent/test_graph.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "action_draft|Phase 15|requested_operation.*execute_action|compatibility shim" tests/architecture/test_action_draft_boundaries.py tests/test_graph_routing.py tests/agent/test_graph.py` returns matches.
    - Tests distinguish intent taxonomy `execute_action` from graph node key `action_draft`.
    - Static test allowlist does not permit new source imports of `src.agent.nodes.execute_action`.
  </acceptance_criteria>
  <done>Boundary tests define the canonical action_draft graph owner before route rewrites.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move node implementation to action_draft and quarantine execute_action</name>
  <files>src/agent/nodes/action_draft.py, src/agent/nodes/execute_action.py, tests/test_execute_action.py, tests/architecture/test_action_draft_boundaries.py</files>
  <read_first>
    - .planning/phases/14-demo-action-executor-boundary/14-CONTEXT.md
    - src/agent/nodes/execute_action.py
    - src/actions/service.py
    - src/tools/executors/action.py
    - tests/test_execute_action.py
    - tests/architecture/test_action_draft_boundaries.py
  </read_first>
  <behavior>
    - Test 1: invoking `action_draft(state, config)` calls `create_coupon_grant_draft` through `UnifiedToolManager` and returns `action_draft` plus `draft_outcome`.
    - Test 2: node output does not build or pass the final idempotency key shape; the service owns it.
    - Test 3: `execute_action(state, config)` shim delegates to `action_draft` and contains no independent tool invocation or persistence code.
  </behavior>
  <action>
    Create `src/agent/nodes/action_draft.py` by moving the implementation from `execute_action.py` and renaming the async entrypoint to `action_draft` per D-18. Update trace step node labels, `ToolCallContext.caller_node`, and tool call IDs to use `action_draft`, hard-quarantining backend execute language for draft semantics per D-21. Preserve Phase 13 `_trusted_approval_result` hash/revision validation. Return state keys `action_draft` and `draft_outcome`; any `action_result` compatibility field must be draft-only/not-executed and must not use `status="success"` as external success per D-06. Keep tool name `create_coupon_grant_draft` per D-20. Replace `src/agent/nodes/execute_action.py` with a short compatibility shim that imports and delegates to `action_draft`, includes the exact owner/removal gate text from this plan, and contains no direct `UnifiedToolManager`, `ActionToolExecutor`, `ActionService`, or repository imports per D-19 and D-22.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_execute_action.py tests/architecture/test_action_draft_boundaries.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/nodes/action_draft.py src/agent/nodes/execute_action.py tests/test_execute_action.py tests/architecture/test_action_draft_boundaries.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "async def action_draft|caller_node=\"action_draft\"|draft_outcome|not_executed_demo" src/agent/nodes/action_draft.py tests/test_execute_action.py` returns matches.
    - `rg -n "UnifiedToolManager|ActionToolExecutor|ActionService|ActionDraftRepository" src/agent/nodes/execute_action.py` returns no matches.
    - `rg -n "Phase 14 compatibility shim|Phase 15 Replay Event Contract|2026-07-16" src/agent/nodes/execute_action.py` returns matches.
  </acceptance_criteria>
  <done>The canonical graph implementation lives in action_draft; execute_action is a quarantined delegating shim only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire graph routes and node-only tool allowlist to action_draft</name>
  <files>src/agent/graph.py, src/tools/catalog.py, src/tools/manager.py, tests/test_graph_routing.py, tests/agent/test_graph.py, tests/architecture/test_action_draft_boundaries.py</files>
  <read_first>
    - docs/contract-spec.md
    - src/agent/graph.py
    - src/tools/catalog.py
    - src/tools/manager.py
    - src/agent/nodes/action_draft.py
    - tests/test_graph_routing.py
    - tests/agent/test_graph.py
    - tests/architecture/test_action_draft_boundaries.py
  </read_first>
  <behavior>
    - Test 1: `StateGraph.add_node("action_draft", action_draft)` is used and `add_node("execute_action", ...)` is absent.
    - Test 2: conditional edge maps from risk and approval use `action_draft`.
    - Test 3: `create_coupon_grant_draft` descriptor allows only caller `action_draft` for node-only write behavior.
    - Test 4: `requested_operation="execute_action"` still routes through safety path and does not get renamed in intent tests.
  </behavior>
  <action>
    Update `src/agent/graph.py` imports, `route_after_risk`, `route_after_approval`, `builder.add_node`, conditional edge maps, and graph edge to use `action_draft` per D-18. Update `src/tools/catalog.py` so `create_coupon_grant_draft` keeps the same tool name but its `caller_allowlist` is `["action_draft"]` per D-20. Update `src/tools/manager.py` write-side-effect/caller guard to recognize `action_draft`, not `execute_action`, per D-21. Keep any intent/requested-operation enum or string value `execute_action` unchanged per D-23.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_graph_routing.py tests/agent/test_graph.py tests/architecture/test_action_draft_boundaries.py tests/test_execute_action.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/graph.py src/tools/catalog.py src/tools/manager.py tests/test_graph_routing.py tests/agent/test_graph.py tests/architecture/test_action_draft_boundaries.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "add_node\\(\"action_draft\"|\"action_draft\": \"action_draft\"|add_edge\\(\"action_draft\", \"final_response\"\\)" src/agent/graph.py` returns matches.
    - `rg -n "add_node\\(\"execute_action\"|\"execute_action\": \"execute_action\"" src/agent/graph.py` returns no matches.
    - `rg -n "caller_allowlist=\\[\"action_draft\"\\]|caller_node == \"action_draft\"" src/tools/catalog.py src/tools/manager.py` returns matches.
    - `rg -n "requested_operation.*execute_action" src tests` still finds intent taxonomy references, proving D-23 was not renamed.
  </acceptance_criteria>
  <done>Graph and node-only tool dispatch use `action_draft` as the canonical write boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Intent taxonomy -> graph route | User-requested execution intent must map to safe draft creation, not real execution. |
| Graph node -> tool manager | Only the canonical node can call the write-like draft tool. |
| Legacy compatibility shim -> canonical node | Shim must not become a second persistence path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T14-01 | Elevation of Privilege | `src/agent/graph.py` | mitigate | Graph routes to `action_draft` only; no `action_execution` node/table/path is added. |
| T14-02 | Tampering | `action_draft` node -> ActionService | mitigate | Node passes Phase 13 binding refs and lets service construct key/reuse checks from Plan 02. |
| T14-03 | Tampering | `action_draft` node args | mitigate | Node no longer applies `"unknown"` target fallback; service missing-target validation remains authoritative. |
| T14-04 | Spoofing / Repudiation | node/state output | mitigate | Node output uses `draft_outcome` semantics; Plan 04 final/API wording consumes `draft_outcome`. |
| T14-05 | Information Disclosure | node trace step | mitigate | Node trace exposes tool name/status only; Plan 04 event payload uses safe refs. |
| T14-06 | Elevation of Privilege | `src/agent/nodes/execute_action.py` | mitigate | Compatibility shim is owned, forbidden for new imports, delegation-only, and has a Phase 15 removal gate/date. |
</threat_model>

<verification>
Run all commands below after completing the tasks:

```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_action_draft_boundaries.py tests/test_execute_action.py tests/test_graph_routing.py tests/agent/test_graph.py -q --tb=short
UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/nodes/action_draft.py src/agent/nodes/execute_action.py src/agent/graph.py src/tools/catalog.py src/tools/manager.py tests/architecture/test_action_draft_boundaries.py tests/test_execute_action.py tests/test_graph_routing.py tests/agent/test_graph.py
```
</verification>

<success_criteria>
- Canonical graph node key is `action_draft` everywhere in graph registration and routing.
- The tool remains `create_coupon_grant_draft` and is node-only for `action_draft`.
- `requested_operation="execute_action"` remains unchanged as intent-layer taxonomy.
- `execute_action` compatibility is a documented shim with owner, forbidden new references, tests, Phase 15 removal gate, and no independent write path.
</success_criteria>

<output>
After completion, create `.planning/phases/14-demo-action-executor-boundary/14-03-SUMMARY.md`.
</output>
