---
phase: 14-demo-action-executor-boundary
plan: 04
type: execute
wave: 4
depends_on:
  - 14-03
files_modified:
  - src/api/routers/approvals.py
  - src/agent/nodes/final_response.py
  - src/agent/events.py
  - src/actions/service.py
  - src/tools/executors/action.py
  - src/api/routers/traces.py
  - src/repositories/trace_repo.py
  - tests/test_approval_api.py
  - tests/test_approval_integration.py
  - tests/agent/test_nodes/test_final_response.py
  - tests/agent/test_events.py
  - tests/test_trace_api.py
  - tests/actions/test_action_draft_v2.py
  - tests/architecture/test_action_draft_boundaries.py
  - .planning/phases/14-demo-action-executor-boundary/14-COVERAGE.md
autonomous: true
requirements:
  - DEMO-01
  - DEMO-02
must_haves:
  truths:
    - "Approval resume reconciliation treats draft_outcome.v1 as the success signal."
    - "Final/backend/API wording says a draft was created and no coupon/refund/ticket/external action was executed."
    - "Trace emits and exposes action_draft_created with safe refs only and no action_execution_* event in demo mode."
    - "Final verification proves no external execution/outbox/reconciliation/compensation path was introduced."
  artifacts:
    - path: "src/api/routers/approvals.py"
      provides: "Approval resume reconciliation using draft_outcome"
      contains: "not_executed_demo"
    - path: "src/agent/nodes/final_response.py"
      provides: "Honest draft-created/not-executed final wording"
      contains: "未执行"
    - path: "src/agent/events.py"
      provides: "action_draft_created minimal event registration"
      contains: "action_draft_created"
    - path: "src/api/routers/traces.py"
      provides: "/trace action draft output with draft_outcome"
      contains: "draft_outcome"
    - path: ".planning/phases/14-demo-action-executor-boundary/14-COVERAGE.md"
      provides: "Phase 14 coverage and source audit"
      contains: "DEMO-01"
  key_links:
    - from: "src/actions/service.py"
      to: "src/agent/events.py"
      via: "emit_event(event_type='action_draft_created') after new draft creation"
      pattern: "action_draft_created"
    - from: "src/api/routers/approvals.py"
      to: "src/agent/nodes/action_draft.py"
      via: "approval resume reconciliation calls canonical action_draft and checks draft_outcome"
      pattern: "draft_outcome"
    - from: "src/repositories/trace_repo.py"
      to: "src/api/routers/traces.py"
      via: "safe trace projection of draft_outcome"
      pattern: "draft_outcome"
---

<objective>
Complete the demo boundary across API, final response, trace events, trace reads, and final coverage.

Purpose: A durable draft boundary is only safe if every user/auditor-facing surface says draft-created/not-executed and every trace/event path avoids raw payloads and execution claims.
Output: `draft_outcome` reconciliation, safe final/API wording, `action_draft_created` event emission, `/trace` draft outcome projection, negative tests, and coverage artifact.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-demo-action-executor-boundary/14-CONTEXT.md
@.planning/phases/14-demo-action-executor-boundary/14-RESEARCH.md
@.planning/phases/14-demo-action-executor-boundary/14-VALIDATION.md
@.planning/phases/14-demo-action-executor-boundary/14-PATTERNS.md
@docs/phase-13-17-architecture-plan.md
@docs/contract-spec.md
@docs/eval-test-plan.md
@src/api/routers/approvals.py
@src/agent/nodes/final_response.py
@src/agent/events.py
@src/api/routers/traces.py
@src/repositories/trace_repo.py
@tests/test_approval_api.py
@tests/test_approval_integration.py
@tests/agent/test_nodes/test_final_response.py
@tests/agent/test_events.py
@tests/test_trace_api.py

<interfaces>
Current contracts to preserve and rewrite:

```text
src/api/routers/approvals.py:
- _recoverable_resume_retry_result(...) currently imports execute_action
- reconciliation currently checks action_result.status == "success"

src/agent/nodes/final_response.py:
- async def final_response(state)
- _approval_outcome_text(..., action_result) currently uses action_result.status == "success"

src/agent/events.py:
- MINIMAL_EVENT_TYPES
- EVENT_RETENTION_CLASSIFICATION
- async def emit_event(...)

src/repositories/trace_repo.py:
- async def get_action_drafts(run_id)
- def build_timeline(steps, approvals, approval_steps, drafts)

src/api/routers/traces.py:
- action_drafts response projection currently includes id/action_type/status
```
</interfaces>
</context>

<architecture_alignment>
## Architecture Alignment

Owner and contract first:
- Canonical owner packages: `src/actions` emits draft-created event refs, `src/agent/events.py` owns minimal event registration, API/final/trace consumers own wording/projection only.
- Contract: `draft_outcome.v1` is the success signal; demo mode never claims external execution.

Move/rewrite to owner:
- Approval resume calls the canonical `action_draft` node and checks `draft_outcome.status == "not_executed_demo"` plus `external_side_effect is False`.
- Final response templates consume `draft_outcome`, not `action_result.status == "success"`.
- Trace reads include `draft_outcome` but not raw `ActionDraft.payload`.

Delete/quarantine old paths:
- Remove backend success-sentinel dependency on `action_result.status == "success"` for demo draft success.
- Do not create `/replay`, ReplayEventV3 read switch, lifecycle finalizer, retention model, action execution, outbox, reconciliation, compensation, or external adapter calls.

Boundary tests before expansion:
- Add forbidden phrase tests, event redaction tests, and static no-external-surface tests before the final full verification gate.
</architecture_alignment>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace API and final response success sentinels with draft_outcome</name>
  <files>src/api/routers/approvals.py, src/agent/nodes/final_response.py, tests/test_approval_api.py, tests/test_approval_integration.py, tests/agent/test_nodes/test_final_response.py</files>
  <read_first>
    - .planning/phases/14-demo-action-executor-boundary/14-CONTEXT.md
    - src/api/routers/approvals.py
    - src/agent/nodes/final_response.py
    - src/agent/nodes/action_draft.py
    - tests/test_approval_api.py
    - tests/test_approval_integration.py
    - tests/agent/test_nodes/test_final_response.py
  </read_first>
  <behavior>
    - Test 1: approval resume reconciliation succeeds when `draft_outcome.status == "not_executed_demo"` and `external_side_effect is False`.
    - Test 2: approval resume reconciliation records an action-draft error when `draft_outcome` is missing or says external side effect occurred.
    - Test 3: final response positive wording includes the draft id and states no coupon/refund/ticket/external action was executed.
    - Test 4: forbidden phrases are absent from backend/final/API success strings.
  </behavior>
  <action>
    Update `src/api/routers/approvals.py` to import/call `action_draft` instead of `execute_action` and to evaluate success through `draft_outcome.status == "not_executed_demo"` and `draft_outcome.external_side_effect is False` per D-06 and D-07. Update `src/agent/nodes/final_response.py` so approved and auto-allowed draft paths say a draft was created and no coupon/refund/ticket/external action was executed per D-08. Do not use wording such as `waiting for final issuance`, `issued coupon`, `refunded`, `closed ticket`, `executed`, `external success`, `等待最终发放`, `已发放`, `已退款`, `已关闭工单`, or `执行成功`; add forbidden phrase tests per D-10.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_final_response.py tests/test_approval_api.py tests/test_approval_integration.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/api/routers/approvals.py src/agent/nodes/final_response.py tests/test_approval_api.py tests/test_approval_integration.py tests/agent/test_nodes/test_final_response.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "draft_outcome|not_executed_demo|external_side_effect" src/api/routers/approvals.py src/agent/nodes/final_response.py tests/test_approval_api.py tests/test_approval_integration.py tests/agent/test_nodes/test_final_response.py` returns matches.
    - `rg -n "action_result.*status.*success|execute_action\\(" src/api/routers/approvals.py src/agent/nodes/final_response.py` returns no demo success-sentinel matches.
    - `rg -n "waiting for final issuance|issued coupon|refunded|closed ticket|external success|等待最终发放|已发放|已退款|已关闭工单|执行成功" src/api/routers/approvals.py src/agent/nodes/final_response.py tests/agent/test_nodes/test_final_response.py tests/test_approval_api.py tests/test_approval_integration.py` returns no positive demo wording.
  </acceptance_criteria>
  <done>API/final response surfaces classify draft creation by draft_outcome and never claim external execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit safe action_draft_created events and extend /trace draft output</name>
  <files>src/agent/events.py, src/actions/service.py, src/tools/executors/action.py, src/api/routers/traces.py, src/repositories/trace_repo.py, tests/agent/test_events.py, tests/test_trace_api.py</files>
  <read_first>
    - docs/contract-spec.md
    - .planning/phases/14-demo-action-executor-boundary/14-CONTEXT.md
    - src/agent/events.py
    - src/actions/service.py
    - src/tools/executors/action.py
    - src/api/routers/traces.py
    - src/repositories/trace_repo.py
    - tests/agent/test_events.py
    - tests/test_trace_api.py
  </read_first>
  <behavior>
    - Test 1: `action_draft_created` is registered in `MINIMAL_EVENT_TYPES` and retention classification is `minimal_event`.
    - Test 2: new draft creation emits one `action_draft_created` event with `resource_refs` containing only `draft_id`, `target_id`, `action_payload_hash`, and `safety_snapshot_hash`.
    - Test 3: event `redacted_payload` contains `action_type`, `execution_mode="demo"`, and `external_side_effect=False`, with no raw action payload/tool args.
    - Test 4: `/trace` action_drafts and timeline detail include `draft_outcome` and exclude raw `ActionDraft.payload`.
  </behavior>
  <action>
    Add `action_draft_created` to `MINIMAL_EVENT_TYPES` in `src/agent/events.py` per D-24. Wire draft event emission after a new draft row is created, using the existing `emit_event` minimal envelope and per-run sequence allocator. Pass `thread_id` and `trace_id` from `ToolCallContext` through `ActionToolExecutor` into `ActionService` as needed. Emit only safe refs: `draft_id`, `target_id`, `action_payload_hash`, `safety_snapshot_hash`. Redacted payload must be exactly safe metadata such as `action_type`, `execution_mode:"demo"`, and `external_side_effect:false`, with no raw action payload/tool args/full `ActionDraft.payload`, per D-25. Extend `src/repositories/trace_repo.py` and `src/api/routers/traces.py` so `/trace` action draft output includes `draft_outcome` and never includes raw `payload`, without adding ReplayEventV3, a new replay API, or an event-store-first trace read switch per D-26.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_events.py tests/test_trace_api.py tests/agent/test_tools/test_create_coupon_grant_draft.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/events.py src/actions/service.py src/tools/executors/action.py src/api/routers/traces.py src/repositories/trace_repo.py tests/agent/test_events.py tests/test_trace_api.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "action_draft_created|draft_id|target_id|action_payload_hash|safety_snapshot_hash|external_side_effect" src/agent/events.py src/actions/service.py src/tools/executors/action.py tests/agent/test_events.py` returns matches.
    - `rg -n "raw_payload|raw_args|arguments|payload" tests/agent/test_events.py` includes negative assertions for event redaction.
    - `rg -n "draft_outcome" src/api/routers/traces.py src/repositories/trace_repo.py tests/test_trace_api.py` returns matches.
    - `rg -n "\"payload\"|payload\\]" src/api/routers/traces.py src/repositories/trace_repo.py` returns no trace projection of raw `ActionDraft.payload`.
  </acceptance_criteria>
  <done>Trace/event surfaces show safe draft refs/outcome only and emit no action execution events in demo mode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add negative boundary coverage and phase coverage artifact</name>
  <files>tests/actions/test_action_draft_v2.py, tests/architecture/test_action_draft_boundaries.py, tests/agent/test_events.py, tests/test_trace_api.py, .planning/phases/14-demo-action-executor-boundary/14-COVERAGE.md</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/14-demo-action-executor-boundary/14-CONTEXT.md
    - .planning/phases/14-demo-action-executor-boundary/14-RESEARCH.md
    - .planning/phases/14-demo-action-executor-boundary/14-VALIDATION.md
    - docs/phase-13-17-architecture-plan.md
    - tests/actions/test_action_draft_v2.py
    - tests/architecture/test_action_draft_boundaries.py
    - tests/agent/test_events.py
    - tests/test_trace_api.py
  </read_first>
  <behavior>
    - Test 1: no SQLAlchemy metadata table names include `action_executions`, `action_outbox_events`, `action_reconciliation_jobs`, or `action_compensation_records`.
    - Test 2: no demo source path imports external adapters, outbox workers, reconciliation, or compensation modules.
    - Test 3: no `action_execution_*` event is emitted or registered as a Phase 14 demo event path.
    - Test 4: Phase coverage artifact maps GOAL, DEMO-01, DEMO-02, D-01..D-27, research constraints, and deferred Phase 15/17 items with no MISSING rows.
  </behavior>
  <action>
    Add negative tests across schema, architecture, events, and trace surfaces proving demo mode has no execution rows, no external-only records, no external adapter/outbox/reconciliation/compensation imports, no `action_execution_*` events, no raw action payload trace output, and no external-success wording per D-05 and D-27. Keep frontend timeline label cleanup out of this phase and record it as Phase 15-owned unless a backend contract test strictly requires it per D-09. Create `.planning/phases/14-demo-action-executor-boundary/14-COVERAGE.md` with requirement coverage, compatibility disposition for `execute_action`, deferred owner rows for Phase 15 and Phase 17 items, and the source audit table below. Mark every relevant item as COVERED and record excluded deferred items as DEFERRED_WITH_OWNER, not MISSING.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/actions/test_action_draft_v2.py tests/architecture/test_action_draft_boundaries.py tests/agent/test_events.py tests/test_trace_api.py tests/agent/test_nodes/test_final_response.py tests/test_approval_api.py tests/test_approval_integration.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "action_executions|action_outbox_events|action_reconciliation_jobs|action_compensation_records|action_execution_" tests/actions/test_action_draft_v2.py tests/architecture/test_action_draft_boundaries.py tests/agent/test_events.py` returns negative-test matches.
    - `rg -n "DEMO-01|DEMO-02|D-01|D-27|DEFERRED_WITH_OWNER|execute_action compatibility" .planning/phases/14-demo-action-executor-boundary/14-COVERAGE.md` returns matches.
    - `rg -n "MISSING" .planning/phases/14-demo-action-executor-boundary/14-COVERAGE.md` returns no relevant open Phase 14 rows.
    - Full pytest and ruff commands pass.
  </acceptance_criteria>
  <done>Negative and coverage gates prove Phase 14 is draft-only, honest, and fully traceable to source artifacts.</done>
</task>

</tasks>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | Phase 14 | Enforce durable draft-only demo boundary with exact approval/snapshot binding | 14-01..14-04 | COVERED | Schema, service, graph, API, trace, and verification plans cover the goal.
REQ | DEMO-01 | Demo mode creates durable draft and draft_outcome only, with no execution row or external side effect | 14-01, 14-02, 14-04 | COVERED | Schema, service result, event/trace, and negative tests.
REQ | DEMO-02 | Demo wording and hash/revision guards cannot claim or authorize real execution | 14-02, 14-03, 14-04 | COVERED | Idempotency, graph binding, final/API wording, and compatibility tests.
RESEARCH | Schema | Full action_draft.v2 and draft_outcome.v1 persistence | 14-01, 14-02 | COVERED | Nullable legacy columns, complete new rows.
RESEARCH | Idempotency | Service-owned key and exact safety snapshot conflict | 14-02 | COVERED | Key shape and conflict tests.
RESEARCH | Graph | Canonical node action_draft and execute_action shim policy | 14-03 | COVERED | Owner/removal gate and static tests.
RESEARCH | Trace | Safe action_draft_created and /trace draft_outcome | 14-04 | COVERED | Minimal envelope only, no ReplayEventV3 read switch.
CONTEXT | D-01..D-05 | Draft schema, outcome, legacy nullable, no execution/outbox | 14-01, 14-02, 14-04 | COVERED | No Phase 17 tables.
CONTEXT | D-06..D-10 | draft_outcome success signal and honest wording | 14-02, 14-04 | COVERED | Final/API forbidden phrase tests.
CONTEXT | D-11..D-17 | Trusted idempotency key, target_id, auto_allowed, exact reuse | 14-02 | COVERED | Service boundary and repository conflicts.
CONTEXT | D-18..D-23 | Canonical action_draft node, tool name unchanged, intent execute_action unchanged | 14-03 | COVERED | Compatibility shim only.
CONTEXT | D-24..D-27 | action_draft_created, safe refs, /trace draft_outcome, negative tests | 14-04 | COVERED | Event/trace/coverage gates.
CONTEXT | Deferred | ReplayEventV3, lifecycle finalizer, richer replay API | None | DEFERRED_WITH_OWNER | Phase 15 per locked context.
CONTEXT | Deferred | External execution, outbox, reconciliation, compensation | None | DEFERRED_WITH_OWNER | Phase 17 per locked context.
CONTEXT | Deferred | Frontend timeline label cleanup | None | DEFERRED_WITH_OWNER | Phase 15 unless backend contract tests require it; this plan does not modify frontend.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Approval API -> graph resume | Trusted approval result must resume to draft only when outcome proves not executed demo. |
| ActionService -> AgentTraceEvent | Event emission must expose safe refs only and use the existing minimal envelope. |
| Trace API -> client/auditor | Trace output must be useful without leaking raw action payload or implying execution. |
| Final response -> user | User-facing wording must not misrepresent a draft as a completed external action. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T14-01 | Elevation of Privilege | final verification / architecture tests | mitigate | Negative tests assert no execution/outbox/reconciliation/compensation tables/imports/events in demo mode. |
| T14-02 | Tampering | approval resume + service reuse | mitigate | Plan 02 key/binding checks are consumed by API/graph; final tests cover mismatched bindings. |
| T14-03 | Tampering | service and trace projections | mitigate | Plan 02 rejects missing target_id; trace/event tests require safe target refs only after validation. |
| T14-04 | Spoofing / Repudiation | `final_response` and approval API | mitigate | Forbidden phrase tests block external-success wording and require draft-created/not-executed text. |
| T14-05 | Information Disclosure | `AgentTraceEvent` and `/trace` | mitigate | `action_draft_created` uses safe resource refs/redacted payload; tests reject raw payload/tool args and `action_execution_*` demo events. |
| T14-06 | Elevation of Privilege | `execute_action` compatibility shim | mitigate | Coverage artifact records owner/removal gate; static tests forbid new imports and prove canonical path is used. |
</threat_model>

<verification>
Run all commands below after completing the tasks:

```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_execute_action.py tests/test_approval_integration.py tests/test_trace_api.py tests/agent/test_graph.py tests/agent/test_nodes/test_final_response.py tests/agent/test_tools/test_create_coupon_grant_draft.py tests/architecture/test_action_draft_boundaries.py tests/actions/test_action_draft_v2.py tests/agent/test_events.py -q --tb=short
UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q --tb=short
UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests
```
</verification>

<success_criteria>
- `draft_outcome.v1` drives graph/API/final success semantics.
- Backend/final/API wording states draft created and no coupon/refund/ticket/external action was executed.
- `action_draft_created` events use safe refs only and demo mode emits no `action_execution_*` events.
- `/trace` includes `draft_outcome` without raw action payload and without a new replay API.
- Coverage artifact has no relevant MISSING rows and preserves Phase 15/17 deferrals with named owners.
</success_criteria>

<output>
After completion, create `.planning/phases/14-demo-action-executor-boundary/14-04-SUMMARY.md`.
</output>
