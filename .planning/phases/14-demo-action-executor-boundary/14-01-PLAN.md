---
phase: 14-demo-action-executor-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/actions/schemas.py
  - src/db/models.py
  - src/db/migrations/versions/009_action_draft_v2.py
  - tests/actions/test_action_draft_v2.py
autonomous: true
requirements:
  - DEMO-01
  - DEMO-02
must_haves:
  truths:
    - "New demo drafts persist action_draft.v2 fields, including exact payload and safety snapshot bindings."
    - "New demo drafts persist draft_outcome.v1 with status=not_executed_demo and external_side_effect=false."
    - "Legacy action_drafts rows may keep new columns nullable, but Phase 14-created rows must be complete."
    - "The live PostgreSQL schema is upgraded to include the Phase 14 action_drafts columns before downstream code runs."
  artifacts:
    - path: "src/actions/schemas.py"
      provides: "Typed DraftOutcomeV1 and ActionDraftV2Data contracts"
      contains: "not_executed_demo"
    - path: "src/db/models.py"
      provides: "ActionDraft ORM columns for action_draft.v2 and draft_outcome.v1"
      contains: "draft_outcome"
    - path: "src/db/migrations/versions/009_action_draft_v2.py"
      provides: "Alembic expansion migration for action_drafts"
      contains: "009_action_draft_v2"
    - path: "tests/actions/test_action_draft_v2.py"
      provides: "Schema and migration contract tests"
      contains: "action_draft.v2"
  key_links:
    - from: "src/db/migrations/versions/009_action_draft_v2.py"
      to: "src/db/models.py"
      via: "matching action_drafts column names"
      pattern: "action_payload_hash|safety_snapshot_hash|draft_outcome"
    - from: "src/actions/schemas.py"
      to: "src/db/models.py"
      via: "schema_version and draft_outcome constants used by new draft rows"
      pattern: "not_executed_demo"
---

<objective>
Create the Phase 14 persistence contract for durable demo action drafts.

Purpose: Phase 14 cannot enforce draft-only behavior until `action_drafts` stores the exact Phase 13 binding fields and a durable `draft_outcome.v1`.
Output: ORM fields, Alembic migration `009_action_draft_v2`, typed schema helpers, contract tests, and a live schema upgrade.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-demo-action-executor-boundary/14-CONTEXT.md
@.planning/phases/14-demo-action-executor-boundary/14-RESEARCH.md
@.planning/phases/14-demo-action-executor-boundary/14-VALIDATION.md
@.planning/phases/14-demo-action-executor-boundary/14-PATTERNS.md
@docs/phase-13-17-architecture-plan.md
@docs/contract-spec.md
@src/actions/schemas.py
@src/db/models.py
@src/db/migrations/versions/008_approval_state_machine.py
@tests/approvals/test_migration_contract.py

<interfaces>
Current contracts to preserve and extend:

```text
src/actions/schemas.py:
- class ActionDraftData(BaseModel)
- class ActionToolCompatResult(BaseModel)

src/db/models.py:
- class ActionSafetySnapshot(Base)
- class ApprovalRequest(TimestampMixin, Base)
- class ActionDraft(TimestampMixin, Base)
- class AgentTraceEvent(TimestampMixin, Base)

src/db/migrations/versions/008_approval_state_machine.py:
- revision = "008_approval_state_machine"
- down_revision = "007_session_memories"
- nullable expand/backfill/enforce pattern for legacy rows
```
</interfaces>
</context>

<architecture_alignment>
## Architecture Alignment

Owner and contract first:
- Canonical owner package: `src/actions` owns action draft contracts; `src/db/models.py` and Alembic own durable schema.
- Normative contract fields from `docs/contract-spec.md`: `action_draft.v2` rows carry `schema_version`, `tenant_id`, `run_id`, `draft_id`, `proposed_action`, `action_payload_hash`, nullable `approval_ref`, `safety_snapshot_ref`, `safety_snapshot_hash`, `idempotency_key`, `status`, and `created_at`.
- Phase 14 implementation extensions: `target_id`, `approval_revision_ref`, `execution_mode="demo"`, `draft_version`, `lifecycle_status`, `retention_policy`, and `draft_outcome`. These are implementation fields for safer demo-mode validation, lifecycle readiness, and replay readiness; they do not become normative contract fields unless `docs/contract-spec.md` is updated.
- If the ORM keeps the existing `payload` column as the persisted `proposed_action` body, treat that as a storage-name mapping and document it in coverage. Do not update `docs/contract-spec.md` or mark the field missing solely because implementation storage uses the existing column name.
- Database uniqueness contract: action drafts must enforce `unique (tenant_id, idempotency_key)` per `docs/contract-spec.md` Section 18.3. Do not keep or recreate global unique `idempotency_key`.
- `draft_outcome.v1` shape is exactly `{"schema_version":"draft_outcome.v1","status":"not_executed_demo","external_side_effect":false,...}` with tenant/run/draft refs where available.

Move/rewrite to owner:
- Add schema helpers before service behavior uses them.
- Mirror every ORM column in the Alembic migration and contract tests.

Delete/quarantine old paths:
- Do not create `action_executions`, outbox, reconciliation, compensation, or external action tables.
- Keep legacy pre-v2 rows nullable only for migration compatibility; they are not an authorization surface.

Boundary tests before expansion:
- `tests/actions/test_action_draft_v2.py` must fail if action_drafts lacks binding/outcome columns or if migration 009 creates external-only tables.
</architecture_alignment>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define draft outcome and schema contract tests</name>
  <files>src/actions/schemas.py, tests/actions/test_action_draft_v2.py</files>
  <read_first>
    - docs/phase-13-17-architecture-plan.md
    - docs/contract-spec.md
    - .planning/phases/14-demo-action-executor-boundary/14-CONTEXT.md
    - .planning/phases/14-demo-action-executor-boundary/14-PATTERNS.md
    - src/actions/schemas.py
    - tests/approvals/test_migration_contract.py
  </read_first>
  <behavior>
    - Test 1: `DraftOutcomeV1()` or its factory produces `schema_version="draft_outcome.v1"`, `status="not_executed_demo"`, and `external_side_effect is False`.
    - Test 2: `ActionDraftV2Data` rejects unknown fields and requires `action_payload_hash`, `safety_snapshot_ref`, `safety_snapshot_hash`, `target_id`, `approval_revision_ref`, `draft_outcome`, and `execution_mode`.
    - Test 3: `ActionDraftV2Data` exposes the exact `schema_version="action_draft.v2"` and `execution_mode="demo"` contract values without depending on ORM metadata.
  </behavior>
  <action>
    Add or extend `tests/actions/test_action_draft_v2.py` before implementation. Then add `DraftOutcomeV1` and `ActionDraftV2Data` to `src/actions/schemas.py` using Pydantic v2 `ConfigDict(extra="forbid")`. Use exact literals `draft_outcome.v1`, `not_executed_demo`, `action_draft.v2`, and `demo` per D-01 and D-03. Do not add a generic `create_action_draft` abstraction and do not rename the existing tool `create_coupon_grant_draft` per D-20.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/actions/test_action_draft_v2.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/actions/schemas.py tests/actions/test_action_draft_v2.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "class DraftOutcomeV1|class ActionDraftV2Data|not_executed_demo|draft_outcome.v1" src/actions/schemas.py tests/actions/test_action_draft_v2.py` returns matches.
    - `rg -n "extra=.forbid.|ConfigDict\\(extra=\"forbid\"\\)" src/actions/schemas.py` shows both new schema classes are strict.
    - `tests/actions/test_action_draft_v2.py` schema-helper tests cover `schema_version`, `draft_outcome.v1`, `not_executed_demo`, `external_side_effect is False`, and `execution_mode="demo"` without importing `Base.metadata` or `src.db.models`.
  </acceptance_criteria>
  <done>Schema helper tests pass and the typed contract exists for downstream service/repository work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ORM and Alembic action_draft.v2 expansion</name>
  <files>src/db/models.py, src/db/migrations/versions/009_action_draft_v2.py, tests/actions/test_action_draft_v2.py</files>
  <read_first>
    - docs/phase-13-17-architecture-plan.md
    - docs/contract-spec.md
    - src/db/models.py
    - src/db/migrations/versions/008_approval_state_machine.py
    - src/db/migrations/versions/005_approval_tables.py
    - tests/actions/test_action_draft_v2.py
  </read_first>
  <behavior>
    - Test 1: Contract test asserts `Base.metadata.tables["action_drafts"]` contains `schema_version`, `action_payload_hash`, `safety_snapshot_ref`, `safety_snapshot_hash`, `target_id`, `approval_revision_ref`, `draft_outcome`, `execution_mode`, `draft_version`, `lifecycle_status`, and `retention_policy`.
    - Test 2: ORM metadata and migration source enforce `UniqueConstraint("tenant_id", "idempotency_key", name="uq_action_drafts_tenant_idempotency_key")` or equivalent Alembic unique constraint.
    - Test 3: Migration source revises `008_approval_state_machine` and adds the same columns to `action_drafts`.
    - Test 4: Migration source does not contain `action_executions`, `action_outbox_events`, `reconciliation`, or `compensation`.
  </behavior>
  <action>
    Extend `ActionDraft` in `src/db/models.py` with nullable-for-legacy columns: `schema_version` defaulting to `"action_draft.v2"`, `target_id`, `approval_revision_ref`, `action_payload_hash`, `safety_snapshot_ref`, `safety_snapshot_hash`, `draft_outcome` JSONB, `execution_mode` defaulting to `"demo"`, `draft_version` defaulting to `1`, `lifecycle_status` defaulting to `"active"`, and `retention_policy` defaulting to `"phase14_demo_draft"` per D-01, D-02, and D-03. Treat `target_id`, `approval_revision_ref`, `execution_mode`, `draft_version`, `lifecycle_status`, `retention_policy`, and `draft_outcome` as Phase 14 implementation extensions unless `docs/contract-spec.md` is updated. If the existing `payload` column remains the storage column for contract `proposed_action`, keep that implementation mapping explicit in tests and final coverage. Create `src/db/migrations/versions/009_action_draft_v2.py` with `revision="009_action_draft_v2"` and `down_revision="008_approval_state_machine"`. Keep new columns nullable for legacy rows and assert v2 completeness only for Phase 14-created rows per D-04. Replace any global `idempotency_key` uniqueness with the normative `unique (tenant_id, idempotency_key)` contract; the Alembic migration must drop/replace `uq_action_drafts_idempotency_key` if present and create `uq_action_drafts_tenant_idempotency_key`. Add only columns/indexes needed for action draft lookup; do not create external execution, outbox, reconciliation, or compensation tables per D-05.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/actions/test_action_draft_v2.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/db/models.py src/db/migrations/versions/009_action_draft_v2.py tests/actions/test_action_draft_v2.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "revision: str = \"009_action_draft_v2\"|down_revision: str \\| None = \"008_approval_state_machine\"" src/db/migrations/versions/009_action_draft_v2.py` returns matches.
    - `rg -n "schema_version|target_id|approval_revision_ref|action_payload_hash|safety_snapshot_ref|safety_snapshot_hash|draft_outcome|execution_mode|draft_version|lifecycle_status|retention_policy" src/db/models.py src/db/migrations/versions/009_action_draft_v2.py` returns matches in both files.
    - `rg -n "tenant_id.*idempotency_key|idempotency_key.*tenant_id|uq_action_drafts_tenant_idempotency_key" src/db/models.py src/db/migrations/versions/009_action_draft_v2.py tests/actions/test_action_draft_v2.py` returns matches.
    - `rg -n "UniqueConstraint\\(\"idempotency_key\"|unique=True.*idempotency_key|uq_action_drafts_idempotency_key" src/db/models.py src/db/migrations/versions/009_action_draft_v2.py` returns no matches after the migration replaces the old global uniqueness model.
    - Tests or coverage explicitly document whether contract `proposed_action` is stored in the existing `payload`/`payload_json` column or a new field; the executor must not silently change `docs/contract-spec.md` to match implementation naming.
    - `tests/actions/test_action_draft_v2.py` imports `Base` from `src.db.models` and asserts `Base.metadata.tables["action_drafts"]` has the Phase 14 ORM columns listed in Task 2 behavior.
    - `rg -n "action_executions|action_outbox_events|reconciliation|compensation" src/db/migrations/versions/009_action_draft_v2.py` returns no matches.
  </acceptance_criteria>
  <done>ORM metadata and Alembic migration express the same action_draft.v2 persistence contract without Phase 17 tables.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Wave schema readiness gate</name>
  <files>src/db/models.py, src/db/migrations/versions/009_action_draft_v2.py</files>
  <read_first>
    - alembic.ini
    - docker-compose.yml
    - src/db/migrations/versions/009_action_draft_v2.py
    - src/db/models.py
    - .planning/phases/14-demo-action-executor-boundary/14-VALIDATION.md
  </read_first>
  <action>
    This task is an environment/schema readiness gate, not a feature implementation task. After Task 1 and Task 2 schema file edits are complete, run the blocking schema push command exactly: `UV_CACHE_DIR=/tmp/uv-cache uv run alembic upgrade head`. If it fails because PostgreSQL is unavailable, run `docker compose ps` and then `docker compose up -d postgres redis`, wait for healthy services, and rerun the same Alembic command. If Alembic still fails, stop the wave and report the environment/schema failure without modifying code. Do not proceed to downstream plans or final plan verification until Alembic succeeds against the project `alembic.ini` configuration.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run alembic upgrade head</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/actions/test_action_draft_v2.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - Alembic output completes without exception and reports database at head.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/actions/test_action_draft_v2.py -q --tb=short` passes after the schema upgrade.
    - If Docker services were started, `docker compose ps` shows the PostgreSQL service running before the second Alembic attempt.
  </acceptance_criteria>
  <done>The live database schema is at Alembic head with Phase 14 action_draft.v2 columns before service or graph work starts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ORM/migration -> PostgreSQL | Schema changes affect the authoritative draft persistence surface. |
| Legacy rows -> new v2 rows | Old nullable rows must not become executable authorization facts. |
| Phase 14 -> Phase 17 | Demo persistence must not introduce external execution storage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T14-01 | Elevation of Privilege | `009_action_draft_v2.py` | mitigate | Migration contract tests assert no `action_executions`, outbox, reconciliation, or compensation tables are created. |
| T14-02 | Tampering | `ActionDraft` binding columns | mitigate | Persist `action_payload_hash`, `safety_snapshot_ref`, and `safety_snapshot_hash` as first-class draft fields for exact reuse checks in Plan 02. |
| T14-03 | Tampering | `ActionDraft.target_id` | mitigate | Add `target_id` as a draft field; Plan 02 rejects missing values before draft creation. |
| T14-04 | Spoofing / Repudiation | `DraftOutcomeV1` | mitigate | Draft outcome contract uses `status="not_executed_demo"` and `external_side_effect=false`; Plan 04 enforces wording. |
| T14-05 | Information Disclosure | `draft_outcome` / future trace projection | mitigate | Store only outcome refs/status flags here; raw payload redaction is enforced in Plan 04 trace/event tests. |
| T14-06 | Elevation of Privilege | `execute_action` compatibility alias | mitigate | No alias is added in this plan; Plan 03 quarantines any retained alias with owner/removal tests. |
</threat_model>

<verification>
Run all commands below after completing the tasks:

```bash
UV_CACHE_DIR=/tmp/uv-cache uv run alembic upgrade head
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/actions/test_action_draft_v2.py -q --tb=short
UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/actions/schemas.py src/db/models.py src/db/migrations/versions/009_action_draft_v2.py tests/actions/test_action_draft_v2.py
```
</verification>

<success_criteria>
- `action_drafts` has the Phase 14 v2 binding, outcome, version, lifecycle, and retention fields.
- New typed schema helpers encode `draft_outcome.v1` as not executed demo with no external side effect.
- Migration 009 upgrades from Phase 13 head and creates no Phase 17 execution/outbox surfaces.
- Blocking Alembic upgrade has passed before Plan 02 starts.
</success_criteria>

<output>
After completion, create `.planning/phases/14-demo-action-executor-boundary/14-01-SUMMARY.md`.
</output>
