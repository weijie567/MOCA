---
phase: 14-demo-action-executor-boundary
plan: 06
type: execute
wave: 5
depends_on:
  - 14-04
  - 14-05
files_modified:
  - tests/actions/test_action_draft_v2.py
  - tests/architecture/test_action_draft_boundaries.py
  - tests/agent/test_events.py
  - tests/test_trace_api.py
  - tests/agent/test_nodes/test_final_response.py
  - .planning/phases/14-demo-action-executor-boundary/14-COVERAGE.md
autonomous: true
requirements:
  - DEMO-01
  - DEMO-02
must_haves:
  truths:
    - "Final verification proves no external execution/outbox/reconciliation/compensation path was introduced."
    - "Final verification proves no backend success surface depends on action_result.status == success."
    - "Coverage maps GOAL, DEMO-01, DEMO-02, D-01..D-27, research constraints, and deferred Phase 15/17 items with no MISSING rows."
  artifacts:
    - path: "tests/actions/test_action_draft_v2.py"
      provides: "Negative persistence tests for no execution/outbox tables"
      contains: "action_executions"
    - path: "tests/architecture/test_action_draft_boundaries.py"
      provides: "Static boundary tests for compatibility shims and external paths"
      contains: "action_result"
    - path: "tests/agent/test_events.py"
      provides: "Negative event tests for no action_execution_* demo events"
      contains: "action_execution_"
    - path: "tests/test_trace_api.py"
      provides: "Trace redaction negative tests"
      contains: "draft_outcome"
    - path: "tests/agent/test_nodes/test_final_response.py"
      provides: "Forbidden wording final gate"
      contains: "draft_outcome"
    - path: ".planning/phases/14-demo-action-executor-boundary/14-COVERAGE.md"
      provides: "Phase 14 coverage and source audit"
      contains: "DEMO-01"
  key_links:
    - from: "tests/architecture/test_action_draft_boundaries.py"
      to: "src/agent/nodes/execute_action.py"
      via: "compatibility shim owner/removal gate scan"
      pattern: "Phase 15 Replay Event Contract"
    - from: "tests/architecture/test_action_draft_boundaries.py"
      to: "src"
      via: "forbidden action_result.status success dependency scan"
      pattern: "action_result"
    - from: ".planning/phases/14-demo-action-executor-boundary/14-COVERAGE.md"
      to: ".planning/phases/14-demo-action-executor-boundary/14-CONTEXT.md"
      via: "D-01..D-27 source audit mapping"
      pattern: "D-27"
---

<objective>
Close Phase 14 with negative boundary coverage and a source coverage artifact.

Purpose: The phase is only complete if tests prove draft-only behavior stayed inside the intended boundary and every source requirement has an owned outcome.
Output: Negative tests for external execution paths and deprecated compatibility dependencies, full focused/full-suite gates, and `14-COVERAGE.md`.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-demo-action-executor-boundary/14-CONTEXT.md
@.planning/phases/14-demo-action-executor-boundary/14-RESEARCH.md
@.planning/phases/14-demo-action-executor-boundary/14-VALIDATION.md
@.planning/phases/14-demo-action-executor-boundary/14-PATTERNS.md
@docs/phase-13-17-architecture-plan.md
@docs/contract-spec.md
@tests/actions/test_action_draft_v2.py
@tests/architecture/test_action_draft_boundaries.py
@tests/agent/test_events.py
@tests/test_trace_api.py
@tests/agent/test_nodes/test_final_response.py

<interfaces>
Final gate surfaces:

```text
tests/actions/test_action_draft_v2.py:
- Schema/migration negative table coverage from Plan 14-01

tests/architecture/test_action_draft_boundaries.py:
- execute_action shim import boundary from Plan 14-03
- action_result.status success dependency boundary from Plans 14-02/14-03

tests/agent/test_events.py:
- action_draft_created safe event coverage from Plan 14-05

tests/test_trace_api.py:
- draft_outcome projection and raw payload exclusion from Plan 14-05

tests/agent/test_nodes/test_final_response.py:
- forbidden phrase coverage from Plan 14-04
```
</interfaces>
</context>

<architecture_alignment>
## Architecture Alignment

Owner and contract first:
- Canonical owner package: this plan owns final verification and coverage documentation only; it must not introduce new runtime behavior beyond negative tests needed to prove the boundary.
- Contract: Phase 14 remains draft-only, compatibility-owned, and explicitly defers Phase 15 replay/lifecycle and Phase 17 external execution.

Move/rewrite to owner:
- Add final static and runtime negative assertions to the tests already owning each surface.
- Create `14-COVERAGE.md` as the source audit and compatibility disposition record.

Delete/quarantine old paths:
- Do not add new implementation code except test support needed for negative assertions.
- Do not add frontend timeline cleanup, ReplayEventV3, lifecycle finalizer, event-store read switch, external execution, outbox, reconciliation, compensation, or adapters.
- Do not leave `execute_action` or `action_result` compatibility without Phase 15 replacement/removal gate text.

Boundary tests before expansion:
- Full focused and full-suite commands run after negative tests and coverage artifact are in place.
</architecture_alignment>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add negative boundary coverage for draft-only semantics</name>
  <files>tests/actions/test_action_draft_v2.py, tests/architecture/test_action_draft_boundaries.py, tests/agent/test_events.py, tests/test_trace_api.py, tests/agent/test_nodes/test_final_response.py</files>
  <read_first>
    - .planning/phases/14-demo-action-executor-boundary/14-CONTEXT.md
    - .planning/phases/14-demo-action-executor-boundary/14-RESEARCH.md
    - docs/phase-13-17-architecture-plan.md
    - tests/actions/test_action_draft_v2.py
    - tests/architecture/test_action_draft_boundaries.py
    - tests/agent/test_events.py
    - tests/test_trace_api.py
    - tests/agent/test_nodes/test_final_response.py
  </read_first>
  <behavior>
    - Test 1: no SQLAlchemy metadata table names include `action_executions`, `action_outbox_events`, `action_reconciliation_jobs`, or `action_compensation_records`.
    - Test 2: no demo source path imports external adapters, outbox workers, reconciliation, or compensation modules.
    - Test 3: no `action_execution_*` event is emitted or registered as a Phase 14 demo event path.
    - Test 4: no source path outside explicit legacy tests depends on `action_result.status == "success"` for demo success.
    - Test 5: final/API wording has no forbidden external-success phrases.
  </behavior>
  <action>
    Add negative tests across schema, architecture, events, trace, and final-response surfaces proving demo mode has no execution rows, no external-only records, no external adapter/outbox/reconciliation/compensation imports, no `action_execution_*` events, no raw action payload trace output, no external-success wording, and no new dependency on `action_result.status == "success"` per D-05, D-06, D-10, and D-27. Keep frontend timeline label cleanup out of this phase and record it as Phase 15-owned unless a backend contract test strictly requires it per D-09.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/actions/test_action_draft_v2.py tests/architecture/test_action_draft_boundaries.py tests/agent/test_events.py tests/test_trace_api.py tests/agent/test_nodes/test_final_response.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "action_executions|action_outbox_events|action_reconciliation_jobs|action_compensation_records|action_execution_" tests/actions/test_action_draft_v2.py tests/architecture/test_action_draft_boundaries.py tests/agent/test_events.py` returns negative-test matches.
    - `rg -n "action_result.*status.*success|status.*success.*action_result" tests/architecture/test_action_draft_boundaries.py` returns static negative-test matches.
    - `rg -n "waiting for final issuance|issued coupon|refunded|closed ticket|external success|等待最终发放|已发放|已退款|已关闭工单|执行成功" tests/agent/test_nodes/test_final_response.py` returns forbidden-phrase test matches.
  </acceptance_criteria>
  <done>Negative tests prove Phase 14 did not add real execution paths or legacy success dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Create coverage artifact and run final gates</name>
  <files>.planning/phases/14-demo-action-executor-boundary/14-COVERAGE.md</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/14-demo-action-executor-boundary/14-CONTEXT.md
    - .planning/phases/14-demo-action-executor-boundary/14-RESEARCH.md
    - .planning/phases/14-demo-action-executor-boundary/14-VALIDATION.md
    - docs/phase-13-17-architecture-plan.md
  </read_first>
  <action>
    Create `.planning/phases/14-demo-action-executor-boundary/14-COVERAGE.md` with requirement coverage, compatibility disposition for `execute_action` and `action_result`, deferred owner rows for Phase 15 and Phase 17 items, and the source audit table below. Use evidence-first status assignment: a row may be marked COVERED only when it cites concrete implementation files, tests, and passing command results; use PARTIAL or MISSING for any item without evidence, even if the plan intended to cover it. Record excluded deferred items as DEFERRED_WITH_OWNER, not MISSING. Include an explicit contract-boundary row that separates normative `docs/contract-spec.md` fields from Phase 14 implementation extensions (`target_id`, `approval_revision_ref`, `execution_mode`, `draft_version`, `lifecycle_status`, `retention_policy`, persisted `draft_outcome`) and records any `payload`/`payload_json` storage mapping for contract `proposed_action`. Explicitly record that `execute_action` shim and `action_result` compatibility must be removed/replaced by Phase 15 Replay Event Contract before Phase 15 verification, target no later than 2026-07-16 unless Phase 15 is replanned. If Phase 15 is delayed, split, or replanned, update ROADMAP/REQUIREMENTS/14-COVERAGE with a new owner phase, removal gate, and target date before Phase 14 verification can pass.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_execute_action.py tests/test_approval_integration.py tests/test_trace_api.py tests/agent/test_graph.py tests/agent/test_nodes/test_final_response.py tests/agent/test_tools/test_create_coupon_grant_draft.py tests/architecture/test_action_draft_boundaries.py tests/actions/test_action_draft_v2.py tests/agent/test_events.py tests/agent/test_nodes/test_receive_request.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "DEMO-01|DEMO-02|D-01|D-27|DEFERRED_WITH_OWNER|execute_action compatibility|action_result compatibility|2026-07-16" .planning/phases/14-demo-action-executor-boundary/14-COVERAGE.md` returns matches.
    - `rg -n "normative contract fields|implementation extensions|proposed_action.*payload|payload.*proposed_action|docs/contract-spec.md" .planning/phases/14-demo-action-executor-boundary/14-COVERAGE.md` returns matches proving the spec/phase boundary and storage-name mapping are recorded.
    - Each COVERED row in `.planning/phases/14-demo-action-executor-boundary/14-COVERAGE.md` includes at least one concrete evidence reference: implementation file path, test file path, or command result. Rows without evidence are marked PARTIAL or MISSING.
    - `rg -n "MISSING" .planning/phases/14-demo-action-executor-boundary/14-COVERAGE.md` returns no relevant open Phase 14 rows unless the executor stops and reports the gap instead of forcing coverage.
    - `rg -n "Phase 15.*delayed|replanned|owner phase|removal gate|target date" .planning/phases/14-demo-action-executor-boundary/14-COVERAGE.md` returns matches documenting the compatibility fallback gate.
    - Focused pytest, full pytest, and ruff commands pass.
  </acceptance_criteria>
  <done>Coverage artifact and final gates prove Phase 14 is draft-only, honest, and fully traceable to source artifacts.</done>
</task>

</tasks>

<source_audit>
The rows below are target coverage rows, not pre-approved facts. When creating
`14-COVERAGE.md`, the executor must attach concrete evidence to every COVERED
row. If implementation files, tests, or command results do not prove a row, the
executor must downgrade that row to PARTIAL or MISSING and stop verification on
open Phase 14 MISSING items.

SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | Phase 14 | Enforce durable draft-only demo boundary with exact approval/snapshot binding | 14-01..14-06 | COVERED | Schema, service/state, graph, API, trace, and final verification plans cover the goal.
REQ | DEMO-01 | Demo mode creates durable draft and draft_outcome only, with no execution row or external side effect | 14-01, 14-02, 14-05, 14-06 | COVERED | Schema, service result, state reset, event/trace, and negative tests.
REQ | DEMO-02 | Demo wording and hash/revision guards cannot claim or authorize real execution | 14-02, 14-03, 14-04, 14-06 | COVERED | Idempotency, graph binding, final/API wording, and compatibility tests.
RESEARCH | Schema | Full action_draft.v2 and draft_outcome.v1 persistence | 14-01, 14-02 | COVERED | Nullable legacy columns, complete new rows.
RESEARCH | State/reset | action_draft, draft_outcome, execution_mode must be state-defined and reset per turn | 14-02 | COVERED | AgentState and receive_request reset tests.
RESEARCH | Idempotency | Service-owned key and exact safety snapshot conflict | 14-02 | COVERED | Key shape and conflict tests.
RESEARCH | Graph | Canonical node action_draft and execute_action shim policy | 14-03 | COVERED | Owner/removal gate and static tests.
RESEARCH | Compatibility | action_result compatibility retained only as draft-only deprecated output | 14-02, 14-03, 14-04, 14-06 | COVERED | New dependencies on action_result.status success are forbidden; Phase 15 replacement/removal gate recorded.
RESEARCH | Trace | Safe action_draft_created and /trace draft_outcome | 14-05 | COVERED | Minimal envelope only, no ReplayEventV3 read switch.
CONTEXT | D-01..D-05 | Draft schema, outcome, legacy nullable, no execution/outbox | 14-01, 14-02, 14-06 | COVERED | No Phase 17 tables.
CONTEXT | D-06..D-10 | draft_outcome success signal, compatibility action_result limits, and honest wording | 14-02, 14-04, 14-06 | COVERED | Final/API forbidden phrase and success-sentinel tests.
CONTEXT | D-11..D-17 | Trusted idempotency key, target_id, auto_allowed, exact reuse | 14-02 | COVERED | Service boundary and repository conflicts.
CONTEXT | D-18..D-23 | Canonical action_draft node, tool name unchanged, intent execute_action unchanged | 14-03, 14-06 | COVERED | Compatibility shim only with Phase 15 removal gate.
CONTEXT | D-24..D-27 | action_draft_created, safe refs, /trace draft_outcome, negative tests | 14-05, 14-06 | COVERED | Event/trace/coverage gates.
CONTEXT | Deferred | ReplayEventV3, lifecycle finalizer, richer replay API | None | DEFERRED_WITH_OWNER | Phase 15 per locked context.
CONTEXT | Deferred | External execution, outbox, reconciliation, compensation | None | DEFERRED_WITH_OWNER | Phase 17 per locked context.
CONTEXT | Deferred | Frontend timeline label cleanup | None | DEFERRED_WITH_OWNER | Phase 15 unless backend contract tests require it; this plan does not modify frontend.
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 14 demo -> Phase 17 external action | Demo must not create execution rows, external events, or adapter paths. |
| Compatibility surfaces -> future Phase 15 | Temporary shims/outputs must have explicit owner and removal/replacement gate. |
| Coverage artifact -> execution planning | The source audit must not silently omit locked decisions or deferred owners. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T14-06-01 | Elevation of Privilege | final verification / architecture tests | mitigate | Negative tests assert no execution/outbox/reconciliation/compensation tables/imports/events in demo mode. |
| T14-06-02 | Spoofing / Repudiation | `action_result` compatibility | mitigate | Static tests forbid new dependencies on `action_result.status == "success"` and coverage records Phase 15 replacement/removal gate/date. |
| T14-06-03 | Elevation of Privilege | `execute_action` compatibility shim | mitigate | Coverage artifact records owner/removal gate; static tests forbid new imports and prove canonical path is used. |
| T14-06-04 | Information Disclosure | trace/event final gates | mitigate | Final tests reject raw payload trace/event output and external execution event names. |
</threat_model>

<verification>
Run all commands below after completing the tasks:

```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_execute_action.py tests/test_approval_integration.py tests/test_trace_api.py tests/agent/test_graph.py tests/agent/test_nodes/test_final_response.py tests/agent/test_tools/test_create_coupon_grant_draft.py tests/architecture/test_action_draft_boundaries.py tests/actions/test_action_draft_v2.py tests/agent/test_events.py tests/agent/test_nodes/test_receive_request.py -q --tb=short
UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q --tb=short
UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests
```
</verification>

<success_criteria>
- Negative tests prove demo mode creates no external execution/outbox/reconciliation/compensation path.
- Static tests prove `execute_action` shim and `action_result` compatibility are owned, forbidden for new dependencies, and gated for Phase 15 replacement/removal by 2026-07-16 unless Phase 15 is replanned.
- Coverage artifact has no relevant MISSING rows and preserves Phase 15/17 deferrals with named owners.
</success_criteria>

<output>
After completion, create `.planning/phases/14-demo-action-executor-boundary/14-06-SUMMARY.md`.
</output>
