---
phase: 03-langgraph-core
plan: "06"
type: execute
wave: 6
depends_on: ["03-05"]
files_modified:
  - src/agent/trace.py
  - src/agent/nodes/retrieve_policy_evidence.py
  - src/agent/nodes/generate_recommendation.py
  - tests/agent/test_trace.py
  - tests/agent/test_nodes/test_retrieve_policy_evidence.py
  - tests/agent/test_nodes/test_generate_recommendation.py
  - tests/agent/test_graph.py
autonomous: true
gap_closure: true
requirements:
  - AGNT-04
  - AGNT-05
  - AGNT-06
  - RAG-05
  - SAFE-06

must_haves:
  truths:
    - "AgentStep rows queried by run_id preserve node names, all tool call names from tools_called, and evidence_refs for retrieval/recommendation steps."
    - "Same-thread LangGraph memory retains prior evidence_refs across turns even when receive_request resets retrieved_evidence for the new turn."
    - "Current-turn recommendations and final responses still use only current retrieved_evidence/recommendation_draft; retained evidence_refs are memory references, not automatic answer evidence."
    - "The read-only Phase 3 graph remains free of approval interrupts and write-tool execution."
  artifacts:
    - path: "src/agent/trace.py"
      provides: "AgentStep persistence that maps trace_steps.tools_called and trace_steps.evidence_refs into existing DB columns"
      contains: "write_agent_steps"
    - path: "src/agent/nodes/retrieve_policy_evidence.py"
      provides: "Retrieved evidence ref extraction, trace attachment, and persistent evidence_refs merge"
      contains: "evidence_refs"
    - path: "src/agent/nodes/generate_recommendation.py"
      provides: "Validated recommendation evidence_refs in trace and persistent memory"
      contains: "validate_citations"
    - path: "tests/agent/test_trace.py"
      provides: "DB-backed run_id query regression test for persisted tool and evidence trace"
      contains: "AgentStep"
    - path: "tests/agent/test_graph.py"
      provides: "Same-thread evidence memory regression through MemorySaver"
      contains: "test_same_thread_evidence_refs_survive_next_turn"
  key_links:
    - from: "src/agent/nodes/retrieve_policy_evidence.py"
      to: "src/agent/trace.py"
      via: "trace_steps[].evidence_refs and trace_steps[].tools_called"
      pattern: "evidence_refs"
    - from: "src/agent/nodes/generate_recommendation.py"
      to: "src/agent/trace.py"
      via: "validated recommendation evidence_refs on trace step"
      pattern: "validate_citations"
    - from: "src/api/routers/agent.py"
      to: "agent_steps"
      via: "write_agent_steps(session, run_id=run_id, trace_steps=trace_steps)"
      pattern: "write_agent_steps"
    - from: "LangGraph MemorySaver/PostgresSaver"
      to: "AgentState.evidence_refs"
      via: "persistent state field not reset by receive_request"
      pattern: "evidence_refs"
---

<objective>
Close Phase 03 verification gaps without replanning completed work.

Purpose: The completed happy path already returns trace summaries and current-turn cited answers, but verification found two hollow data flows: DB AgentStep rows lose tool/evidence details, and same-thread evidence memory is declared but never written. This plan makes both data flows concrete and tested.

Output: Existing trace persistence stores tool and evidence details by run_id; evidence_refs are written into persistent AgentState memory across same-thread turns; focused regression tests prove both gaps are closed.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/ming/projects/MOCA/.planning/PROJECT.md
@/Users/ming/projects/MOCA/.planning/ROADMAP.md
@/Users/ming/projects/MOCA/.planning/REQUIREMENTS.md
@/Users/ming/projects/MOCA/.planning/phases/03-langgraph-core/03-CONTEXT.md
@/Users/ming/projects/MOCA/.planning/phases/03-langgraph-core/03-VERIFICATION.md
@/Users/ming/projects/MOCA/.planning/phases/03-langgraph-core/03-04-SUMMARY.md
@/Users/ming/projects/MOCA/.planning/phases/03-langgraph-core/03-05-SUMMARY.md

<interfaces>
Existing DB schema is sufficient; do not add a migration.

From src/db/models.py:
```python
class AgentStep(TimestampMixin, Base):
    tool_name: Mapped[str | None] = mapped_column(String(64))
    tool_input_summary: Mapped[dict[str, Any] | None] = mapped_column(JSONB)
    tool_output_summary: Mapped[dict[str, Any] | None] = mapped_column(JSONB)
    evidence_refs: Mapped[list[dict[str, Any]] | None] = mapped_column(JSONB)
```

Use the existing columns as follows:
```python
# For trace_steps with a tools_called list:
tool_name = ",".join(tools_called)  # e.g. "get_order,get_refund_case"
tool_output_summary = {"tools_called": tools_called, ...existing_tool_output_summary}

# For trace_steps with evidence refs:
evidence_refs = [
    {"doc_key": str, "chunk_id": str, "title": str | None, "confidence": float | None, "retrieved_at": iso_datetime}
]
```

From src/agent/state.py:
```python
class AgentState(TypedDict, total=False):
    evidence_refs: list[EvidenceRef]  # persistent memory: survives across turns
    retrieved_evidence: dict[str, Any] | None  # ephemeral: reset by receive_request each turn
    recommendation_draft: dict[str, Any] | None
    trace_steps: list[dict[str, Any]] | None
```

Existing trace step shapes:
```python
# retrieve_policy_evidence
{"node": "retrieve_policy_evidence", "status": status, "tools_called": ["search_policy"], ...}

# load_business_context
{"node": "load_business_context", "status": status, "tools_called": ["get_order", ...], ...}

# generate_recommendation
{"node": "generate_recommendation", "status": status, "model_name": settings.llm_model, ...}
```
</interfaces>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Full execution trace, tool calls, evidence references queryable by run_id | Task 1 and Task 3 |
| GOAL | Same-thread memory retains previously retrieved evidence | Task 2 and Task 3 |
| REQ | AGNT-05 same-thread memory includes evidence_refs | Task 2 and Task 3 |
| REQ | AGNT-06 structured execution trace records tools and evidence | Task 1 and Task 3 |
| REQ | SAFE-06 complete audit log by run_id | Task 1 and Task 3 |
| REQ | AGNT-04/RAG-05 evidence refs remain concrete doc/chunk references | Task 2 and Task 3 |
| RESEARCH | Use existing AgentRun/AgentStep trace persistence path | Task 1 |
| CONTEXT D-05 | AgentStep includes evidence_refs and tool summaries | Task 1 |
| CONTEXT D-07 | evidence_refs are persistent memory; retrieved_evidence is per-turn ephemeral | Task 2 |
| CONTEXT D-08 | Phase 3 tools remain read-only | All tasks, no write tools added |
| CONTEXT Deferred | Approval interrupt and write tools remain deferred to Phase 4 | Excluded from this gap plan |
</source_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist tool and evidence details into AgentStep rows</name>
  <files>
    src/agent/trace.py,
    tests/agent/test_trace.py
  </files>
  <read_first>
    - /Users/ming/projects/MOCA/src/agent/trace.py — current write_agent_steps implementation and build_trace_summary normalization pattern
    - /Users/ming/projects/MOCA/src/db/models.py — AgentRun and AgentStep field names and JSONB columns
    - /Users/ming/projects/MOCA/src/api/routers/agent.py — confirms write_agent_steps is the API persistence path
    - /Users/ming/projects/MOCA/tests/conftest.py — AsyncSession fixture and test database setup
    - /Users/ming/projects/MOCA/.planning/phases/03-langgraph-core/03-VERIFICATION.md — gap 1 details
  </read_first>
  <behavior>
    - Test 1: A trace step with tools_called=["get_order", "get_refund_case"] persists one AgentStep row whose tool_name is "get_order,get_refund_case" and whose tool_output_summary contains {"tools_called": ["get_order", "get_refund_case"]}.
    - Test 2: A trace step with evidence_refs persists AgentStep.evidence_refs unchanged enough to query by run_id and find doc_key/chunk_id.
    - Test 3: Existing trace steps using tool_name still persist exactly as before.
  </behavior>
  <action>
    Implement tests first in `tests/agent/test_trace.py`.

    Add a DB-backed async test using the existing `session` fixture:
    - Insert an AgentRun with `write_agent_run()`.
    - Call `write_agent_steps()` with three trace steps:
      1. `load_business_context` with `tools_called=["get_order", "get_refund_case"]`.
      2. `retrieve_policy_evidence` with `tools_called=["search_policy"]` and `evidence_refs=[{"doc_key": "policy_refund_timeout", "chunk_id": "chunk_001", "title": "退款超时规则", "confidence": 0.82, "retrieved_at": "..."}]`.
      3. `legacy_tool_step` with `tool_name="get_ticket"`.
    - Query `AgentStep` rows by `run_id`, ordered by `step_index`.
    - Assert the first row has `tool_name == "get_order,get_refund_case"` and `tool_output_summary["tools_called"] == ["get_order", "get_refund_case"]`.
    - Assert the second row has `tool_name == "search_policy"` and `evidence_refs[0]["doc_key"] == "policy_refund_timeout"` and `evidence_refs[0]["chunk_id"] == "chunk_001"`.
    - Assert the third row keeps `tool_name == "get_ticket"`.

    Then update `src/agent/trace.py`:
    - Add `_normalize_tools(step: dict[str, Any]) -> list[str]` that returns `step["tools_called"]` when present, plus `step["tool_name"]` when present and not duplicated.
    - Add `_normalize_tool_name(step)` that joins normalized tools with commas, or returns None if there are no tools. Keep within the existing String(64) column by only joining the Phase 3 read-only tool names; this set is bounded (`get_order`, `get_refund_case`, `get_ticket`, `search_policy`).
    - Add `_normalize_tool_output_summary(step)` that starts from `step.get("tool_output_summary") or {}` and, when normalized tools are non-empty, adds `"tools_called": tools`.
    - In `write_agent_steps()`, set `tool_name=_normalize_tool_name(step)` and `tool_output_summary=_normalize_tool_output_summary(step)`.
    - Continue setting `evidence_refs=step.get("evidence_refs")`; Task 2 will ensure nodes populate it.
    - Do not add an AgentStep.tools_called column or any Alembic migration. Existing columns represent the trace data reliably for Phase 3.
  </action>
  <verify>
    <automated>cd /Users/ming/projects/MOCA && UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_trace.py -q --tb=short</automated>
    <automated>cd /Users/ming/projects/MOCA && UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/trace.py tests/agent/test_trace.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/agent/test_trace.py` exists and queries `AgentStep` by `run_id`.
    - The DB-backed test fails before the trace normalization change and passes after it.
    - `write_agent_steps()` persists all names from `tools_called` into existing AgentStep columns.
    - `write_agent_steps()` preserves existing `tool_name` trace steps.
    - `AgentStep.evidence_refs` remains JSONB-backed and no migration file is added.
  </acceptance_criteria>
  <done>Persisted AgentStep rows are queryable by run_id and include tool call names plus evidence_refs using the existing schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write persistent evidence_refs memory from retrieval and validated citations</name>
  <files>
    src/agent/nodes/retrieve_policy_evidence.py,
    src/agent/nodes/generate_recommendation.py,
    tests/agent/test_nodes/test_retrieve_policy_evidence.py,
    tests/agent/test_nodes/test_generate_recommendation.py
  </files>
  <read_first>
    - /Users/ming/projects/MOCA/src/agent/state.py — AgentState.evidence_refs persistent memory contract
    - /Users/ming/projects/MOCA/src/agent/nodes/receive_request.py — confirms retrieved_evidence is reset and evidence_refs is not reset
    - /Users/ming/projects/MOCA/src/agent/nodes/retrieve_policy_evidence.py — current search_policy result handling
    - /Users/ming/projects/MOCA/src/agent/nodes/generate_recommendation.py — citation validation and recommendation_draft flow
    - /Users/ming/projects/MOCA/tests/agent/test_nodes/test_retrieve_policy_evidence.py — existing evidence gate tests
    - /Users/ming/projects/MOCA/tests/agent/test_nodes/test_generate_recommendation.py — existing citation validation tests
    - /Users/ming/projects/MOCA/.planning/phases/03-langgraph-core/03-CONTEXT.md — D-07 and D-09
    - /Users/ming/projects/MOCA/.planning/phases/03-langgraph-core/03-VERIFICATION.md — gap 2 details
  </read_first>
  <behavior>
    - Test 1: retrieve_policy_evidence with strong evidence returns `evidence_refs` containing doc_key, chunk_id, title, confidence, retrieved_at and adds the same refs to its trace step.
    - Test 2: retrieve_policy_evidence with no evidence preserves existing state["evidence_refs"] instead of replacing memory with an empty list.
    - Test 3: generate_recommendation returns trace step evidence_refs matching citation-validated draft evidence_refs.
    - Test 4: invalid citations stripped by validate_citations are not written into persistent evidence_refs or trace evidence_refs.
  </behavior>
  <action>
    Implement tests first.

    In `tests/agent/test_nodes/test_retrieve_policy_evidence.py`, add:
    - `test_retrieve_policy_evidence_writes_persistent_evidence_refs`: mock `search_policy()` to return strong evidence. Call `retrieve_policy_evidence()`. Assert:
      - `result["evidence_refs"]` contains one item with `doc_key`, `chunk_id`, `title`, `confidence`, `retrieved_at`.
      - The appended trace step has `evidence_refs` with the same `doc_key` and `chunk_id`.
    - `test_retrieve_policy_evidence_preserves_previous_refs_on_no_evidence`: pass state with existing `evidence_refs=[{"doc_key": "prior_doc", "chunk_id": "prior_chunk", ...}]`; mock no evidence; assert returned `evidence_refs` still contains the prior ref and `recommendation_draft.recommended_action == "insufficient_evidence"`.

    In `tests/agent/test_nodes/test_generate_recommendation.py`, add:
    - `test_generate_recommendation_trace_contains_validated_evidence_refs`: strong retrieved evidence and FakeLLM cites `chunk_001`; assert the appended `generate_recommendation` trace step has evidence_refs containing `chunk_001`, and returned `evidence_refs` includes it.
    - Extend the invalid citation test so it asserts invalid chunk IDs are absent from returned `evidence_refs` and trace `evidence_refs`.

    Then update `src/agent/nodes/retrieve_policy_evidence.py`:
    - Add helper `_evidence_refs_from_result(result, retrieved_at)` that reads `result["data"]["evidence"]` and emits a compact list of refs:
      `doc_key`, `chunk_id`, `title`, `confidence` from `score`, and `retrieved_at`.
    - Add helper `_merge_evidence_refs(existing, new)` that de-duplicates by `(doc_key, chunk_id)` and preserves existing refs when new is empty.
    - On successful retrieval with evidence, return `"evidence_refs": merged_refs`.
    - On no-evidence or retrieval error, return `"evidence_refs": state.get("evidence_refs") or []` so prior memory survives.
    - Add `evidence_refs` to the `retrieve_policy_evidence` trace step when evidence exists. Keep `tools_called=["search_policy"]`.

    Then update `src/agent/nodes/generate_recommendation.py`:
    - After citation validation strips invalid refs, derive validated refs from `draft["evidence_refs"]`.
    - Add `retrieved_at` when absent; preserve `doc_key`, `chunk_id`, `title`, and `section` when present.
    - Merge validated refs into persistent `state["evidence_refs"]` using the same de-duplication behavior as retrieval. It is acceptable to duplicate a small helper locally or import a non-private helper if you make one public in `retrieve_policy_evidence.py`.
    - Add those validated refs to the `generate_recommendation` trace step. Adjust `_trace_step()` to accept optional `evidence_refs`.
    - If citation validation strips every ref and sets `recommended_action="citation_invalid"`, return no new evidence_refs beyond existing memory.
  </action>
  <verify>
    <automated>cd /Users/ming/projects/MOCA && UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_retrieve_policy_evidence.py tests/agent/test_nodes/test_generate_recommendation.py -q --tb=short</automated>
    <automated>cd /Users/ming/projects/MOCA && UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/nodes/retrieve_policy_evidence.py src/agent/nodes/generate_recommendation.py tests/agent/test_nodes/test_retrieve_policy_evidence.py tests/agent/test_nodes/test_generate_recommendation.py</automated>
  </verify>
  <acceptance_criteria>
    - `retrieve_policy_evidence()` writes compact persistent `evidence_refs` after successful retrieval.
    - `retrieve_policy_evidence()` keeps prior `evidence_refs` when current retrieval has no evidence or errors.
    - `generate_recommendation()` writes only citation-validated evidence refs to trace and persistent memory.
    - Invalid citation refs do not enter persistent memory.
    - `receive_request()` continues resetting `retrieved_evidence` and does not reset persistent `evidence_refs`.
  </acceptance_criteria>
  <done>AgentState.evidence_refs has real writers, retains prior evidence across same-thread turns, and trace steps carry evidence refs for DB persistence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the verifier gaps with graph-level regressions</name>
  <files>
    tests/agent/test_graph.py,
    tests/agent/test_trace.py
  </files>
  <read_first>
    - /Users/ming/projects/MOCA/tests/agent/test_graph.py — existing MemorySaver graph tests and patch helpers
    - /Users/ming/projects/MOCA/tests/agent/test_trace.py — DB trace persistence test from Task 1
    - /Users/ming/projects/MOCA/src/agent/graph.py — graph node order remains unchanged
    - /Users/ming/projects/MOCA/src/agent/trace.py — write_agent_steps and build_trace_summary contracts
    - /Users/ming/projects/MOCA/.planning/phases/03-langgraph-core/03-VERIFICATION.md — both gaps to close
  </read_first>
  <behavior>
    - Test 1: Same-thread turn 1 retrieves evidence, turn 2 receives no evidence, and final_state["evidence_refs"] still contains turn 1 evidence while final_state["retrieved_evidence"] reflects turn 2 no-evidence.
    - Test 2: Persisted DB AgentStep rows queried by run_id contain both `tools_called`-derived tool names and evidence_refs.
  </behavior>
  <action>
    Extend `tests/agent/test_graph.py` with `test_same_thread_evidence_refs_survive_next_turn`:
    - Use the existing `MemorySaver` compiled graph pattern.
    - Turn 1: patch `search_policy` to return strong evidence with `doc_key="policy_refund_timeout"` and `chunk_id="chunk_001"`.
    - Invoke graph with thread_id `"evidence-memory-thread"` and query `"退款超时规则是什么？"`.
    - Assert turn 1 final state has `evidence_refs` containing `chunk_001`.
    - Turn 2: patch `search_policy` to return no evidence with `retrieval_status="no_evidence"`, `best_score=0.0`, `evidence=[]`.
    - Invoke the same graph with the same thread_id and query `"这个新问题没有规则依据"`.
    - Assert turn 2 `retrieved_evidence["data"]["evidence"] == []` to prove receive_request reset the ephemeral retrieval for the new turn.
    - Assert turn 2 `evidence_refs` still contains `{"doc_key": "policy_refund_timeout", "chunk_id": "chunk_001"}` from turn 1.
    - Assert turn 2 `recommendation_draft["recommended_action"] == "insufficient_evidence"` so retained evidence memory is not used as current-turn evidence.

    Keep `tests/agent/test_trace.py` focused on persisted DB rows and run_id query. If Task 1 already added the run_id query assertions, leave them there and do not duplicate DB setup in graph tests.

    Run the focused tests, then the full agent suite. Fix any regressions in tests only if the production behavior already satisfies the stated requirements; otherwise fix production code.
  </action>
  <verify>
    <automated>cd /Users/ming/projects/MOCA && UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_graph.py tests/agent/test_trace.py -q --tb=short -m "not live"</automated>
    <automated>cd /Users/ming/projects/MOCA && UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/ -q --tb=short -m "not live"</automated>
    <automated>cd /Users/ming/projects/MOCA && UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `test_same_thread_evidence_refs_survive_next_turn` fails against the current verifier-gap implementation and passes after Task 2.
    - `tests/agent/test_trace.py` proves DB AgentStep rows queried by run_id include tool names and evidence refs.
    - Existing graph tests still pass: happy path, insufficient evidence, order not found, LLM parse retry, cross-turn isolation, trace summary shape.
    - Full test suite passes.
  </acceptance_criteria>
  <done>Automated tests directly prove both 03-VERIFICATION gaps are closed at DB persistence and same-thread graph memory levels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trace_steps → agent_steps | Node-produced trace dicts become persisted audit rows queryable by run_id. |
| retrieved policy evidence → checkpointer state | Compact evidence refs enter same-thread memory; full policy chunk text must not be stored in persistent memory. |
| same-thread memory → next-turn answer generation | Prior evidence refs survive for audit/memory, but current-turn answer evidence must still come from retrieved_evidence/recommendation_draft. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-GAP-01 | Information Disclosure | AgentState.evidence_refs | mitigate | Persist only compact doc_key/chunk_id/title/confidence/retrieved_at refs; do not store full chunk text, prompts, order details, or tool outputs in persistent memory per D-07c and D-05e. |
| T-03-GAP-02 | Tampering | trace_steps tools_called | mitigate | Normalize tool names from bounded Phase 3 read-only tool names and test persisted DB rows by run_id; no user-provided tool names are accepted. |
| T-03-GAP-03 | Repudiation | AgentStep query by run_id | mitigate | DB-backed regression test asserts AgentStep rows retain node index, tool names, and evidence refs for audit replay by run_id. |
| T-03-GAP-04 | Information Disclosure | retained evidence memory | mitigate | Graph regression proves retained evidence_refs do not override current-turn no-evidence recommendation; final answer remains gated by current retrieved_evidence and citation-validated draft. |
| T-03-GAP-05 | Elevation of Privilege | Phase 3 graph behavior | accept | This plan adds no write tools, approval paths, or interrupts; existing read-only authorization checks remain unchanged. |
</threat_model>

<verification>
```bash
cd /Users/ming/projects/MOCA

UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_trace.py -q --tb=short
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_retrieve_policy_evidence.py tests/agent/test_nodes/test_generate_recommendation.py -q --tb=short
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_graph.py tests/agent/test_trace.py -q --tb=short -m "not live"
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/ -q --tb=short -m "not live"
UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q --tb=short
UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent tests/agent
```
</verification>

<success_criteria>
- No new Alembic migration is added.
- Existing completed plans 03-01 through 03-05 remain untouched.
- `src/agent/trace.py` persists tools_called-derived names and evidence_refs into AgentStep rows.
- `src/agent/nodes/retrieve_policy_evidence.py` writes compact evidence_refs into persistent memory and trace steps.
- `src/agent/nodes/generate_recommendation.py` writes only citation-validated evidence_refs into persistent memory and trace steps.
- DB-backed tests can query AgentStep rows by run_id and find tool/evidence trace data.
- Same-thread MemorySaver graph test proves prior evidence_refs survive a second turn even though retrieved_evidence is reset.
- Phase 3 remains read-only with no approval interruption or write tools.
</success_criteria>

<output>
After completion, create `.planning/phases/03-langgraph-core/03-06-SUMMARY.md`
</output>
