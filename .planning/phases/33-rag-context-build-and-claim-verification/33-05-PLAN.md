---
phase: 33-rag-context-build-and-claim-verification
plan: 33-05
type: execute
wave: 5
depends_on:
  - 33-04
files_modified:
  - src/agent/nodes/claim_verify.py
  - src/agent/routing.py
  - src/agent/graph.py
  - src/agent/graph_vocabulary.py
  - tests/agent/test_nodes/test_claim_verify.py
  - tests/agent/rag_context/test_routing.py
  - tests/agent/test_graph.py
  - tests/agent/test_graph_vocabulary.py
  - tests/knowledge/test_claim_verification_bundle.py
autonomous: true
requirements:
  - APF-14
user_setup: []
must_haves:
  truths:
    - "claim_verify is a runnable graph node and the only writer for claim_verification_bundle, blocked_claims, and safe_support_refs."
    - "route_after_claim_verify maps semantic bundle route to registered graph keys."
    - "blocked/manual/error bundles cannot reach risk or action paths."
---

# Plan 33-05: ClaimVerify Node And RouteAfterClaimVerify

<objective>
Register `claim_verify` as the post-generation claim verification node and wire deterministic graph routing from recommendation output to risk or final response.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/33-rag-context-build-and-claim-verification/33-CONTEXT.md
@.planning/phases/33-rag-context-build-and-claim-verification/33-RESEARCH.md
@.planning/phases/33-rag-context-build-and-claim-verification/33-PATTERNS.md
@.planning/phases/33-rag-context-build-and-claim-verification/33-04-SUMMARY.md
@docs/contract-spec.md
@src/agent/routing.py
@src/agent/graph.py
@src/agent/graph_vocabulary.py
@src/knowledge/service.py
@tests/agent/test_graph.py
@tests/agent/test_graph_vocabulary.py
</context>

<must_haves>
- `claim_verify(state, config)` reads `material_claims`, `verified_evidence_package`, `business_context`, and `proposed_action`.
- The node calls `PolicyKnowledgeService.verify_claims(...)` and returns only `claim_verification_bundle`, `blocked_claims`, `safe_support_refs`, compatibility verifier route/status fields if needed, and trace step metadata for node `claim_verify`.
- `route_after_recommendation` routes material claims, proposed action, or user-visible policy/business/action claims to `claim_verify`.
- `route_after_claim_verify` returns only `assess_risk_and_approval` or `final_response`; it never returns semantic bundle route values such as `continue` directly.
</must_haves>

<tasks>

<task id="33-05-01" type="auto" tdd="true">
  <name>Task 33-05-01: Implement claim_verify writer node</name>
  <files>src/agent/nodes/claim_verify.py, tests/agent/test_nodes/test_claim_verify.py, tests/knowledge/test_claim_verification_bundle.py</files>
  <read_first>
- docs/contract-spec.md
- .planning/phases/33-rag-context-build-and-claim-verification/33-CONTEXT.md
- .planning/phases/33-rag-context-build-and-claim-verification/33-04-SUMMARY.md
- src/knowledge/service.py
- src/agent/state.py
- tests/knowledge/test_claim_verification_bundle.py
  </read_first>
  <action>
Create `src/agent/nodes/claim_verify.py` with `claim_verify(state: AgentState, config: RunnableConfig) -> dict`. Read `material_claims`, `verified_evidence_package`, `business_context`, and `proposed_action`; call `PolicyKnowledgeService.verify_claims(...)`; and return `claim_verification_bundle`, `blocked_claims`, `safe_support_refs`, compatibility verifier route/status fields if needed by current downstream code, and an appended trace step for `claim_verify`.

On malformed bundle or expected verifier errors, return `ClaimVerificationBundleV1(overall_status="error", route="final_response", ...)` with reason code `claim_verify_error`. Do not write `rag_context_status`, `verified_evidence_package`, `citation_map`, `evidence_map`, `material_claims`, `proposed_action`, `risk_assessment`, `approval_plan`, or `action_draft`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_claim_verify.py tests/knowledge/test_claim_verification_bundle.py -q --tb=short</automated>
    <automated>uv run ruff check src/agent/nodes/claim_verify.py tests/agent/test_nodes/test_claim_verify.py</automated>
  </verify>
  <acceptance_criteria>
- `test -f src/agent/nodes/claim_verify.py` succeeds.
- `rg -n "async def claim_verify|verify_claims|claim_verification_bundle|blocked_claims|safe_support_refs" src/agent/nodes/claim_verify.py tests/agent/test_nodes/test_claim_verify.py` finds node writer behavior.
- `rg -n "verified_evidence_package|citation_map|evidence_map|material_claims|proposed_action|risk_assessment|approval_plan|action_draft" tests/agent/test_nodes/test_claim_verify.py` finds no-write assertions for non-owned fields.
  </acceptance_criteria>
  <done>`claim_verify` writes only claim verification outputs and fail-closes verifier errors.</done>
</task>

<task id="33-05-02" type="auto" tdd="true">
  <name>Task 33-05-02: Wire claim verification graph and router</name>
  <files>src/agent/routing.py, src/agent/graph.py, src/agent/graph_vocabulary.py, tests/agent/rag_context/test_routing.py, tests/agent/test_graph.py, tests/agent/test_graph_vocabulary.py</files>
  <read_first>
- docs/contract-spec.md
- .planning/phases/33-rag-context-build-and-claim-verification/33-CONTEXT.md
- src/agent/routing.py
- src/agent/graph.py
- src/agent/graph_vocabulary.py
- tests/agent/rag_context/test_routing.py
- tests/agent/test_graph.py
- tests/agent/test_graph_vocabulary.py
  </read_first>
  <action>
Update `src/agent/routing.py`: add `route_after_claim_verify(state: AgentState) -> str`, include `claim_verify` in `_RECOMMENDATION_ROUTES`, and update `route_after_recommendation` so `material_claims`, `proposed_action`, or user-visible policy/business/action claims route to `claim_verify`; no-claim/no-action answer-only paths may route to `final_response`.

`route_after_claim_verify` maps `ClaimVerificationBundleV1.route="continue"` plus proposed action or risk signals to `assess_risk_and_approval`; `continue` without action/risk can route to `final_response`; `final_response`, `manual_review`, `error`, blocked claims, unknown state, and malformed bundles route to `final_response`.

Update `src/agent/graph.py`: register `builder.add_node("claim_verify", claim_verify)`, map `route_after_recommendation` key `claim_verify` to node `claim_verify`, and add conditional edges from `claim_verify` through `route_after_claim_verify` to `assess_risk_and_approval` and `final_response`.

Update `src/agent/graph_vocabulary.py`: promote `claim_verify` from `deferred_non_runnable` to `runtime` with `runnable=True`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/rag_context/test_routing.py tests/agent/test_graph.py tests/agent/test_graph_vocabulary.py tests/agent/test_nodes/test_claim_verify.py -q --tb=short</automated>
    <automated>uv run ruff check src/agent/routing.py src/agent/graph.py src/agent/graph_vocabulary.py tests/agent/rag_context/test_routing.py tests/agent/test_graph.py tests/agent/test_graph_vocabulary.py</automated>
  </verify>
  <acceptance_criteria>
- `rg -n "def route_after_claim_verify|claim_verify|_RECOMMENDATION_ROUTES" src/agent/routing.py src/agent/graph.py tests/agent/rag_context/test_routing.py tests/agent/test_graph.py` finds router and graph wiring.
- `rg -n "claim_verify.*runtime|runnable=True|runnable is True" src/agent/graph_vocabulary.py tests/agent/test_graph_vocabulary.py` finds vocabulary promotion.
- `tests/agent/rag_context/test_routing.py` asserts `route_after_claim_verify` never returns semantic bundle route value `continue`.
  </acceptance_criteria>
  <done>`claim_verify` is registered, runtime/runnable, and routes verified vs blocked bundles correctly.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Mitigation |
| --- | --- | --- | --- |
| T-33-05-01 | Tampering | `claim_verify` | Node-level tests prove only bundle fields are written. |
| T-33-05-02 | Elevation of privilege | `route_after_claim_verify` | Blocked/manual/error bundles route to `final_response`; only verified `continue` paths can reach risk. |
| T-33-05-03 | Repudiation | graph vocabulary | Runtime/runnable vocabulary tests prove Phase 33 promotion is explicit. |
</threat_model>

<verification>
```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_claim_verify.py tests/agent/rag_context/test_routing.py tests/agent/test_graph.py tests/agent/test_graph_vocabulary.py tests/knowledge/test_claim_verification_bundle.py -q --tb=short
uv run ruff check src/agent/nodes/claim_verify.py src/agent/routing.py src/agent/graph.py src/agent/graph_vocabulary.py tests/agent/test_nodes/test_claim_verify.py tests/agent/rag_context/test_routing.py tests/agent/test_graph.py tests/agent/test_graph_vocabulary.py
git diff --check
```
</verification>

<success_criteria>
- `claim_verify` is registered, runnable, and the only writer for claim bundle fields.
- `route_after_claim_verify` is deterministic and returns only registered graph keys.
- Blocked/manual/error bundles cannot reach risk/action paths.
</success_criteria>

<notes>
- Contract/spec delta: none planned.
- Downstream risk/action enforcement moves to Plan 33-06.
</notes>

<output>
After completion, create `.planning/phases/33-rag-context-build-and-claim-verification/33-05-SUMMARY.md`.
</output>
