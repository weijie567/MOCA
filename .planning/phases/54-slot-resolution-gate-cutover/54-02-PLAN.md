---
phase: 54-slot-resolution-gate-cutover
plan: "02"
type: execute
wave: 2
depends_on:
  - "54-01"
files_modified:
  - src/agent/graph.py
  - src/agent/routing.py
  - src/agent/intent_policy.py
  - tests/architecture/graph_baseline.py
  - tests/architecture/test_canonical_graph_baseline.py
  - tests/agent/test_graph.py
  - tests/test_graph_routing.py
  - tests/agent/test_intent_routing.py
  - tests/agent/test_nodes/test_contextual_intent_resolve.py
  - tests/agent/test_nodes/test_slot_resolution_gate.py
  - .planning/phases/54-slot-resolution-gate-cutover/54-02-SUMMARY.md
autonomous: true
requirements:
  - CAGM-05
user_setup: []
must_haves:
  truths:
    - "Active graph registers `slot_resolution_gate`, not `extract_slots`."
    - "`contextual_intent_resolve` routes slot-required intents to `slot_resolution_gate`."
    - "Active graph conditional edge from `slot_resolution_gate` uses `route_after_slot_resolution`."
    - "`route_after_slots` is not an active graph router after cutover; if retained, it delegates only."
    - "No registered main-chain graph node named `slot_extraction` exists."
    - "Reviewed-memory / long-term memory compatibility route still targets `long_term_memory_retrieve` until Phase 55."
    - "No active `memory_context_load`, `recommendation_generation`, `risk_gate`, or Phase 58 no-debt cleanup is introduced."
    - "Graph/router/path-map/baseline tests agree on the same active route values."
  artifacts:
    - path: src/agent/graph.py
      provides: "active Phase 54 graph cutover"
      contains: "slot_resolution_gate"
    - path: src/agent/routing.py
      provides: "active contextual route values and slot router"
      contains: "route_after_slot_resolution"
    - path: src/agent/intent_policy.py
      provides: "slot-required intent route policy values"
      contains: "slot_resolution_gate"
    - path: tests/architecture/graph_baseline.py
      provides: "source-verified active graph baseline after Phase 54"
      contains: "slot_resolution_gate"
    - path: tests/architecture/test_canonical_graph_baseline.py
      provides: "static no-drift coverage for node set and router maps"
      contains: "test_router_return_values_are_covered_by_registered_path_maps"
    - path: tests/agent/test_graph.py
      provides: "compiled graph and runtime smoke coverage"
      contains: "slot_resolution_gate"
    - path: tests/test_graph_routing.py
      provides: "route totality and fail-closed regression tests"
      contains: "route_after_contextual_intent"
  key_links:
    - from: src/agent/graph.py
      to: src/agent/nodes/slot_resolution_gate.py
      via: "builder.add_node('slot_resolution_gate', slot_resolution_gate)"
      pattern: "add_node\\(\"slot_resolution_gate\""
    - from: src/agent/graph.py
      to: src/agent/routing.py
      via: "conditional edge from contextual_intent_resolve uses route_after_contextual_intent"
      pattern: "route_after_contextual_intent"
    - from: src/agent/graph.py
      to: src/agent/routing.py
      via: "conditional edge from slot_resolution_gate uses route_after_slot_resolution"
      pattern: "route_after_slot_resolution"
    - from: tests/architecture/graph_baseline.py
      to: src/agent/graph.py
      via: "AST extraction of add_node and add_conditional_edges"
      pattern: "graph_conditional_edge_mappings"
---

<objective>
把 active graph/router/policy/baseline 原子切到 `slot_resolution_gate` 和 `route_after_slot_resolution`。

Purpose: CAGM-05 的运行时 cutover 必须同时更新 route return values、graph path maps、architecture baseline 和 smoke tests，否则 active route values 与 path maps 会 drift。
Output: Active graph 不再注册 `extract_slots`，slot-required route 指向 `slot_resolution_gate`，slot gate active router 为 `route_after_slot_resolution`，同时保留 Phase 55 前的 `long_term_memory_retrieve` compatibility destination。
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-slot-resolution-gate-cutover/54-CONTEXT.md
@.planning/phases/54-slot-resolution-gate-cutover/54-RESEARCH.md
@.planning/phases/54-slot-resolution-gate-cutover/54-VALIDATION.md
@.planning/phases/54-slot-resolution-gate-cutover/54-PATTERNS.md
@.planning/phases/54-slot-resolution-gate-cutover/54-01-SUMMARY.md
@.planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
@docs/contract-spec.md
@docs/current-langgraph-architecture.md
@src/agent/graph.py
@src/agent/routing.py
@src/agent/intent_policy.py
@src/agent/nodes/slot_resolution_gate.py
@src/agent/nodes/contextual_intent_resolve.py
@tests/architecture/graph_baseline.py
@tests/architecture/test_canonical_graph_baseline.py
@tests/agent/test_graph.py
@tests/test_graph_routing.py
@tests/agent/test_intent_routing.py
@tests/agent/test_nodes/test_contextual_intent_resolve.py

<interfaces>
Graph builder pattern to cut over:
```python
builder.add_node("contextual_intent_resolve", contextual_intent_resolve, retry_policy=_llm_retry)
builder.add_conditional_edges("contextual_intent_resolve", route_after_contextual_intent, {...})
builder.add_conditional_edges("slot_resolution_gate", route_after_slot_resolution, {...})
```

Router contract from 54-01:
```python
def route_after_slot_resolution(state: AgentState) -> str: ...
def route_after_slots(state: AgentState) -> str: ...
```

Phase 54 route destinations allowed by decision:
```python
CONTEXTUAL_INTENT_ROUTES == {"clarification_gate", "final_response", "investigate", "slot_resolution_gate"}
SLOT_ROUTES == {"clarification_gate", "investigate", "long_term_memory_retrieve"}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atomically cut over active route, policy, graph, and baseline</name>
  <files>src/agent/routing.py, src/agent/intent_policy.py, src/agent/graph.py, tests/architecture/graph_baseline.py, tests/architecture/test_canonical_graph_baseline.py, tests/agent/test_graph.py, tests/test_graph_routing.py, tests/agent/test_intent_routing.py, tests/agent/test_nodes/test_contextual_intent_resolve.py</files>
  <read_first>
    - src/agent/routing.py
    - src/agent/intent_policy.py
    - src/agent/graph.py
    - src/agent/nodes/slot_resolution_gate.py
    - tests/architecture/graph_baseline.py
    - tests/architecture/test_canonical_graph_baseline.py
    - tests/agent/test_graph.py
    - tests/test_graph_routing.py
    - tests/agent/test_intent_routing.py
    - tests/agent/test_nodes/test_contextual_intent_resolve.py
    - .planning/phases/54-slot-resolution-gate-cutover/54-CONTEXT.md
    - .planning/phases/54-slot-resolution-gate-cutover/54-VALIDATION.md
    - .planning/phases/54-slot-resolution-gate-cutover/54-01-SUMMARY.md
    - .planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
  </read_first>
  <behavior>
    - `route_after_contextual_intent({"primary_intent":"refund_troubleshooting", ...})` returns exact string `slot_resolution_gate`.
    - `route_after_contextual_intent` allowlist contains `slot_resolution_gate` and does not contain `extract_slots`.
    - `IntentRouteLiteral` and every slot-required `IntentDefinition.initial_route` use `slot_resolution_gate`, not `extract_slots`.
    - `contextual_intent_resolve` classification trace `route_decision` becomes `slot_resolution_gate` for slot-required outputs.
    - `route_after_intent`, if retained, still delegates to `route_after_contextual_intent` and therefore returns `slot_resolution_gate` for slot-required intents.
    - `src/agent/graph.py` imports `slot_resolution_gate` from `src.agent.nodes.slot_resolution_gate` and does not import `extract_slots` as an active graph node.
    - Compiled graph node set includes `slot_resolution_gate` and excludes `extract_slots`.
    - Conditional edge map for `("contextual_intent_resolve", "route_after_contextual_intent")` maps route key `slot_resolution_gate` to destination `slot_resolution_gate`.
    - Conditional edge map for `("slot_resolution_gate", "route_after_slot_resolution")` maps `clarification_gate`, `investigate`, and `long_term_memory_retrieve`.
    - There is no active conditional edge source `extract_slots` and no active router `route_after_slots` in `src/agent/graph.py`.
    - `CURRENT_ACTIVE_GRAPH_NODES_BASELINE` removes `extract_slots`, adds `slot_resolution_gate`, and still keeps `long_term_memory_retrieve`, `generate_recommendation`, and `assess_risk_and_approval` as later-phase active legacy nodes.
    - `MIGRATION_MODE_LEGACY_NODE_MAP` no longer includes the Phase 54-owned `extract_slots` row; known later-phase active legacy rows for Phase 55 `long_term_memory_retrieve`, Phase 56 `generate_recommendation`, and Phase 57 `assess_risk_and_approval` remain untouched.
    - `route_after_slot_resolution` returns `long_term_memory_retrieve` only when required slots are satisfied and routing hints include `needs_reviewed_memory_context` or `needs_long_term_memory`.
    - Unknown intent, low confidence, approval-chat mismatch, required-slot policy mismatch, malformed state, and monkeypatched unregistered route still fail closed to `clarification_gate`.
    - Forbidden main-chain nodes still reject `slot_extraction`.
  </behavior>
  <action>
    Update `src/agent/routing.py`, `src/agent/intent_policy.py`, `src/agent/graph.py`, and architecture baseline files as one atomic logical patch within this task per D-19. In `routing.py`, set `CONTEXTUAL_INTENT_ROUTES` to `{"clarification_gate", "final_response", "investigate", "slot_resolution_gate"}` and make `_route_after_contextual_intent` return `slot_resolution_gate` whenever `SlotPolicyRegistry.required_slots_for(intent)` has `all_of` or `any_of`. Keep no-slot investigative intents returning `investigate`, direct response intents returning `final_response`, and unsafe/unknown/malformed branches returning `clarification_gate`. Keep `IntentRouteLiteral` scoped to policy initial routes; do not widen it to include `clarification_gate` solely because the contextual router allowlist contains fail-closed destinations. Keep `SLOT_ROUTES` as `{"clarification_gate", "investigate", "long_term_memory_retrieve"}`; do not add active `memory_context_load`. In `intent_policy.py`, change `IntentRouteLiteral` and all slot-required `IntentDefinition.initial_route` values from `extract_slots` to `slot_resolution_gate`; direct/no-slot route values stay unchanged. In `src/agent/graph.py`, replace the active import `from src.agent.nodes.extract_slots import extract_slots` with `from src.agent.nodes.slot_resolution_gate import slot_resolution_gate`; replace `builder.add_node("extract_slots", extract_slots, retry_policy=_llm_retry)` with `builder.add_node("slot_resolution_gate", slot_resolution_gate, retry_policy=_llm_retry)`; update the `contextual_intent_resolve` path map so route key `slot_resolution_gate` maps to destination `slot_resolution_gate`; replace the active slot conditional edge source/router from `("extract_slots", route_after_slots, ...)` to `("slot_resolution_gate", route_after_slot_resolution, ...)`. Keep the slot path map values exactly `{"clarification_gate": "clarification_gate", "investigate": "investigate", "long_term_memory_retrieve": "long_term_memory_retrieve"}` so Phase 55 memory-needed paths still reach current `long_term_memory_retrieve`. Update `tests/architecture/graph_baseline.py` and `tests/architecture/test_canonical_graph_baseline.py` in the same task so active baseline includes `slot_resolution_gate`, removes active `extract_slots`, removes the Phase 54-owned `extract_slots` row from `MIGRATION_MODE_LEGACY_NODE_MAP`, and preserves later-phase active legacy rows for `long_term_memory_retrieve`, `generate_recommendation`, and `assess_risk_and_approval`. Update `tests/test_graph_routing.py`, `tests/agent/test_intent_routing.py`, `tests/agent/test_nodes/test_contextual_intent_resolve.py`, `tests/agent/test_nodes/test_slot_resolution_gate.py`, and relevant graph route-totality tests to assert exact new route values, canonical classification trace route decisions, active graph path maps, canonical slot node importability, and `route_after_slots` delegate-only compatibility. Do not add `builder.add_node("memory_context_load", ...)`, `builder.add_node("recommendation_generation", ...)`, `builder.add_node("risk_gate", ...)`, any `slot_extraction` node, or Phase 58 exact no-debt equality.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_graph_routing.py tests/agent/test_intent_routing.py tests/agent/test_nodes/test_contextual_intent_resolve.py tests/agent/test_nodes/test_slot_resolution_gate.py tests/agent/test_required_slots.py tests/architecture/test_canonical_graph_baseline.py tests/agent/test_graph.py::test_graph_compiles_with_investigate tests/agent/test_graph.py::test_legacy_graph_runtime_names_project_to_target_vocabulary tests/agent/test_graph.py::test_all_router_return_keys_have_edges -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/routing.py src/agent/intent_policy.py src/agent/graph.py tests/test_graph_routing.py tests/agent/test_intent_routing.py tests/agent/test_nodes/test_contextual_intent_resolve.py tests/agent/test_nodes/test_slot_resolution_gate.py tests/architecture/graph_baseline.py tests/architecture/test_canonical_graph_baseline.py tests/agent/test_graph.py</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run python -c "import ast,pathlib; tree=ast.parse(pathlib.Path('src/agent/graph.py').read_text()); nodes=set(); edges={}; forbidden={'extract_slots','slot_extraction','memory_context_load','recommendation_generation','risk_gate'}; import_names=set();\nfor n in ast.walk(tree):\n    if isinstance(n, ast.ImportFrom): import_names.update(a.name for a in n.names)\n    if isinstance(n, ast.Call) and isinstance(n.func, ast.Attribute) and n.func.attr=='add_node': nodes.add(n.args[0].value)\n    if isinstance(n, ast.Call) and isinstance(n.func, ast.Attribute) and n.func.attr=='add_conditional_edges': edges[(n.args[0].value, n.args[1].id)]={k.value:v.value for k,v in zip(n.args[2].keys,n.args[2].values,strict=True)}\nassert 'slot_resolution_gate' in nodes; assert forbidden.isdisjoint(nodes); assert 'extract_slots' not in import_names; assert ('slot_resolution_gate','route_after_slot_resolution') in edges; assert ('extract_slots','route_after_slots') not in edges; assert edges[('contextual_intent_resolve','route_after_contextual_intent')]['slot_resolution_gate']=='slot_resolution_gate'; print('54-02 atomic graph/router/policy cutover OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/agent/routing.py` contains `slot_resolution_gate` in `CONTEXTUAL_INTENT_ROUTES`.
    - `src/agent/routing.py` no longer returns `extract_slots` from `_route_after_contextual_intent`.
    - `src/agent/intent_policy.py` has no `initial_route="extract_slots"` values.
    - `src/agent/graph.py` contains active `builder.add_node("slot_resolution_gate", slot_resolution_gate, retry_policy=_llm_retry)`.
    - `src/agent/graph.py` contains no active `builder.add_node("extract_slots", ...)`.
    - `src/agent/graph.py` contains no active `add_conditional_edges("extract_slots", route_after_slots, ...)`.
    - `tests/architecture/graph_baseline.py` active node baseline includes `slot_resolution_gate` and excludes `extract_slots`.
    - `tests/architecture/graph_baseline.py` legacy migration map does not include `extract_slots` after Phase 54 active deletion.
    - Tests assert `route_after_contextual_intent` and `route_after_intent` return `slot_resolution_gate` for slot-required intents.
    - Tests still assert reviewed-memory hints route to `long_term_memory_retrieve` only after slot requirements are satisfied.
    - Static scan confirms no active `slot_extraction`, `memory_context_load`, `recommendation_generation`, or `risk_gate` registration was introduced.
  </acceptance_criteria>
  <done>Route values, policy values, active graph path maps, and architecture baseline are cut over together to `slot_resolution_gate` / `route_after_slot_resolution` with no intermediate route/graph drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update graph smoke tests for canonical slot gate and compatibility memory route</name>
  <files>tests/agent/test_graph.py, tests/test_graph_routing.py, tests/agent/test_intent_routing.py, tests/agent/test_nodes/test_contextual_intent_resolve.py, tests/agent/test_nodes/test_slot_resolution_gate.py</files>
  <read_first>
    - tests/agent/test_graph.py
    - tests/test_graph_routing.py
    - tests/agent/test_intent_routing.py
    - tests/agent/test_nodes/test_contextual_intent_resolve.py
    - tests/agent/test_nodes/test_slot_resolution_gate.py
    - src/agent/graph.py
    - src/agent/routing.py
    - src/agent/nodes/slot_resolution_gate.py
    - src/agent/nodes/long_term_memory_retrieve.py
    - .planning/phases/54-slot-resolution-gate-cutover/54-VALIDATION.md
  </read_first>
  <behavior>
    - Existing refund/order graph smoke tests monkeypatch `src.agent.nodes.slot_resolution_gate._get_llm`, not `extract_slots._get_llm`.
    - Happy path with required slots resolved reaches `investigate`.
    - Same-thread accepted inherited slot path feeds `active_slots` into `investigate`.
    - Wrong-thread or stale inherited session slot routes to `clarification_gate`.
    - Reviewed-memory compatibility hint reaches existing `long_term_memory_retrieve` and records `llm_outputs["long_term_memory_retrieve"]`, not `memory_context_load`.
    - Graph route-totality tests cover `route_after_slot_resolution` and retain only one explicit compatibility delegate assertion for `route_after_slots`.
    - Contextual intent node tests expect `classification_trace["route_decision"] == "slot_resolution_gate"` for slot-required results.
  </behavior>
  <action>
    Update the runtime smoke and router tests to use the canonical slot gate module after Task 1. Task 2 may touch overlapping test files only for focused smoke/regression assertions; it must not modify route constants, active graph path maps, intent policy values, or architecture baseline files. If those surfaces are still wrong, return to Task 1 instead of fixing them here. In `tests/agent/test_graph.py`, import `src.agent.nodes.slot_resolution_gate as slot_resolution_gate_module`, update smoke assertions to use `route_after_slot_resolution`, and change graph E2E monkeypatches from `extract_slots_module._get_llm` to `slot_resolution_gate_module._get_llm`. Keep tests that prove historical `extract_slots` projection out of active graph-membership assertions; those belong to 54-03 vocabulary/API compatibility coverage. Ensure graph smoke tests still prove D-05/D-14 by routing reviewed-memory hints to `long_term_memory_retrieve` after required slots are satisfied. In router tests, replace active expected destination `extract_slots` with `slot_resolution_gate` and active slot router name `route_after_slots` with `route_after_slot_resolution`. Keep a single explicit delegate test asserting `route_after_slots(state) == route_after_slot_resolution(state)`. Do not update docs, vocabulary status, API labels, or validation artifacts in this plan; 54-03 owns those.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_required_slots.py tests/agent/test_nodes/test_slot_resolution_gate.py tests/agent/test_graph.py tests/test_graph_routing.py tests/agent/test_intent_routing.py tests/agent/test_nodes/test_contextual_intent_resolve.py tests/architecture/test_canonical_graph_baseline.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check tests/agent/test_graph.py tests/test_graph_routing.py tests/agent/test_intent_routing.py tests/agent/test_nodes/test_contextual_intent_resolve.py tests/agent/test_nodes/test_slot_resolution_gate.py</automated>
  </verify>
  <acceptance_criteria>
    - Graph smoke tests patch `slot_resolution_gate_module._get_llm`.
    - At least one graph smoke test proves `slot_resolution_gate -> long_term_memory_retrieve` remains the Phase 55 compatibility path for reviewed-memory hints.
    - Router tests cover `route_after_slot_resolution` as the active slot router.
    - Only compatibility-specific tests mention `route_after_slots`.
    - No runtime smoke test requires active graph node `extract_slots`.
  </acceptance_criteria>
  <done>Focused graph/router smoke suite proves canonical slot gate runtime behavior and Phase 55 compatibility destination.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contextual_intent_resolve -> slot_resolution_gate | Slot-required route values cross from intent policy into slot gate runtime. |
| slot_resolution_gate -> route_after_slot_resolution | Active graph conditional edge uses canonical deterministic router. |
| slot_resolution_gate -> long_term_memory_retrieve | Phase 55-owned memory compatibility route remains active only after required slots are satisfied. |
| architecture baseline -> source graph | Static AST checks must reflect source facts, not only vocabulary projection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-02-01 | Tampering / Route Drift | `routing.py`, `intent_policy.py`, `graph.py` | mitigate | Update route values, policy initial routes, graph path maps, baseline, and smoke tests in one plan per D-19. |
| T-54-02-02 | Repudiation | active graph node name | mitigate | Remove active `extract_slots` registration and assert `slot_resolution_gate` is registered; historical projection remains 54-03 scope per D-15/D-16. |
| T-54-02-03 | Elevation of Privilege | `slot_extraction` accidental graph node | mitigate | Architecture/static tests assert `slot_extraction` is not registered per D-06 and Phase 50 SPEC. |
| T-54-02-04 | Information Disclosure | reviewed-memory compatibility route | mitigate | Keep `long_term_memory_retrieve` reachable only after required slots are resolved and explicit reviewed/long-term memory hints exist per D-05/D-14. |
| T-54-02-05 | Denial of Service | router exception / unregistered route | mitigate | Preserve fail-closed wrappers returning `clarification_gate` for contextual and slot routers per D-12. |
| T-54-02-06 | Scope Creep | Phase 55/56/57/58 active names | mitigate | Static scan rejects active `memory_context_load`, `recommendation_generation`, and `risk_gate` registrations; Phase 58 exact no-debt cleanup is not implemented here. |
</threat_model>

<verification>
Plan 54-02 is complete when focused architecture, graph, router, contextual intent, slot gate, and required-slot tests pass, Ruff passes, and static scans prove active graph uses `slot_resolution_gate` / `route_after_slot_resolution` with no active `extract_slots`, `route_after_slots`, or `slot_extraction`.
</verification>

<success_criteria>
- Active graph registers `slot_resolution_gate`.
- Active graph no longer registers `extract_slots`.
- `contextual_intent_resolve` route values and graph path map agree on `slot_resolution_gate`.
- `slot_resolution_gate` active conditional edge uses `route_after_slot_resolution`.
- `route_after_slots` is compatibility delegate only.
- Phase 55 compatibility destination remains `long_term_memory_retrieve`; no active `memory_context_load` is introduced.
- Architecture baseline no longer lists `extract_slots` as active legacy node.
</success_criteria>

<output>
After completion, create `.planning/phases/54-slot-resolution-gate-cutover/54-02-SUMMARY.md`. The summary must list the exact active node set delta, confirm no registered `slot_extraction`, confirm `long_term_memory_retrieve` remains the Phase 55 compatibility destination, and state whether any local validation/debug issue required appending `.planning/LOCAL-VALIDATION-ISSUES.md`.
</output>
