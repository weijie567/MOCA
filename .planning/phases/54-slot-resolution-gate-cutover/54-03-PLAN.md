---
phase: 54-slot-resolution-gate-cutover
plan: "03"
type: execute
wave: 3
depends_on:
  - "54-02"
files_modified:
  - src/agent/graph_vocabulary.py
  - src/api/routers/agent_runs.py
  - tests/agent/test_graph_vocabulary.py
  - tests/agent/test_trace.py
  - tests/test_trace_api.py
  - tests/test_agent_runs_api.py
  - docs/current-langgraph-architecture.md
  - .planning/ARCHITECTURE-DEBT.md
  - .planning/phases/54-slot-resolution-gate-cutover/54-VALIDATION.md
  - .planning/phases/54-slot-resolution-gate-cutover/54-03-SUMMARY.md
autonomous: true
requirements:
  - CAGM-05
user_setup: []
must_haves:
  truths:
    - "`slot_resolution_gate` and `route_after_slot_resolution` are runtime graph vocabulary entries."
    - "`extract_slots` and `route_after_slots` remain only compatibility aliases with owner, reason, trace projection, validation evidence, and delete phase no later than Phase 58."
    - "API/SSE labels understand new runtime node name `slot_resolution_gate` while preserving historical `extract_slots` display/projection."
    - "Current architecture doc describes active source path `contextual_intent_resolve -> slot_resolution_gate -> route_after_slot_resolution`."
    - "Architecture debt ledger closes active `extract_slots` deletion and records any retained compatibility surfaces."
    - "54-VALIDATION.md records executed evidence only after focused tests/scans pass."
    - "Final scans prove no active `extract_slots`, no registered `slot_extraction`, no disallowed test entrypoints in Phase 54 artifacts, and no Phase 55/56/57/58 active scope creep."
  artifacts:
    - path: src/agent/graph_vocabulary.py
      provides: "runtime/compatibility graph vocabulary projection"
      contains: "slot_resolution_gate"
    - path: src/api/routers/agent_runs.py
      provides: "SSE node labels and target-node projection"
      contains: "NODE_MESSAGES"
    - path: tests/agent/test_graph_vocabulary.py
      provides: "runtime and compatibility alias assertions"
      contains: "route_after_slot_resolution"
    - path: tests/agent/test_trace.py
      provides: "trace summary projection assertions"
      contains: "slot_resolution_gate"
    - path: tests/test_trace_api.py
      provides: "timeline router projection coverage"
      contains: "route_after_slot_resolution"
    - path: tests/test_agent_runs_api.py
      provides: "SSE target node projection coverage"
      contains: "target_node_name"
    - path: docs/current-langgraph-architecture.md
      provides: "current-source graph snapshot after Phase 54"
      contains: "slot_resolution_gate"
    - path: .planning/ARCHITECTURE-DEBT.md
      provides: "intent/graph compatibility debt ledger closeout"
      contains: "Phase 54"
    - path: .planning/phases/54-slot-resolution-gate-cutover/54-VALIDATION.md
      provides: "Phase 54 final command evidence and artifact scan status"
      contains: "nyquist_compliant"
  key_links:
    - from: src/agent/graph_vocabulary.py
      to: tests/agent/test_graph_vocabulary.py
      via: "runtime and compatibility projection tests"
      pattern: "graph_vocabulary_entry"
    - from: src/api/routers/agent_runs.py
      to: tests/test_agent_runs_api.py
      via: "_sse_event target_node_name projection"
      pattern: "_sse_event"
    - from: docs/current-langgraph-architecture.md
      to: src/agent/graph.py
      via: "source-fact active graph path"
      pattern: "slot_resolution_gate"
    - from: .planning/ARCHITECTURE-DEBT.md
      to: .planning/phases/54-slot-resolution-gate-cutover/54-VALIDATION.md
      via: "shared validation evidence and retained compatibility rows"
      pattern: "Phase 54"
---

<objective>
完成 Phase 54 vocabulary、API label、docs、architecture debt、validation closeout。

Purpose: Runtime cutover 后，trace/replay/API/docs 必须同步区分 active runtime 名称与历史兼容名称，否则 `extract_slots` 会继续被误读为当前 graph authority。
Output: Runtime vocabulary promotion、compatibility alias ledger、SSE label/tests、current architecture doc、architecture debt closeout、Phase 54 validation evidence 和 final artifact scans。
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/54-slot-resolution-gate-cutover/54-CONTEXT.md
@.planning/phases/54-slot-resolution-gate-cutover/54-RESEARCH.md
@.planning/phases/54-slot-resolution-gate-cutover/54-VALIDATION.md
@.planning/phases/54-slot-resolution-gate-cutover/54-PATTERNS.md
@.planning/phases/54-slot-resolution-gate-cutover/54-02-SUMMARY.md
@.planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
@docs/current-langgraph-architecture.md
@docs/contract-spec.md
@src/agent/graph_vocabulary.py
@src/api/routers/agent_runs.py
@tests/agent/test_graph_vocabulary.py
@tests/agent/test_trace.py
@tests/test_trace_api.py
@tests/test_agent_runs_api.py
@.planning/ARCHITECTURE-DEBT.md
@AGENTS.md

<interfaces>
Vocabulary entry shape:
```python
@dataclass(frozen=True)
class GraphVocabularyEntry:
    legacy_name: str
    target_name: str
    kind: TargetGraphKind
    status: TargetGraphStatus
    runnable: bool
    reason_codes: tuple[str, ...] = ()
```

Trace projection helper:
```python
def project_trace_step_for_contract(step: Mapping[str, Any]) -> dict[str, Any]: ...
```

SSE projection helper:
```python
def _sse_event(..., node_name: str | None = None) -> dict[str, str]:
    if node_name:
        data["target_node_name"] = target_graph_name(node_name, kind="node")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote runtime vocabulary and API/SSE label coverage</name>
  <files>src/agent/graph_vocabulary.py, src/api/routers/agent_runs.py, tests/agent/test_graph_vocabulary.py, tests/agent/test_trace.py, tests/test_trace_api.py, tests/test_agent_runs_api.py</files>
  <read_first>
    - src/agent/graph_vocabulary.py
    - src/api/routers/agent_runs.py
    - tests/agent/test_graph_vocabulary.py
    - tests/agent/test_trace.py
    - tests/test_trace_api.py
    - tests/test_agent_runs_api.py
    - .planning/phases/54-slot-resolution-gate-cutover/54-CONTEXT.md
    - .planning/phases/54-slot-resolution-gate-cutover/54-PATTERNS.md
    - .planning/phases/54-slot-resolution-gate-cutover/54-02-SUMMARY.md
  </read_first>
  <behavior>
    - Vocabulary test asserts `graph_vocabulary_entry("slot_resolution_gate", kind="node").status == "runtime"` and runnable is `True`.
    - Vocabulary test asserts `graph_vocabulary_entry("route_after_slot_resolution", kind="router").status == "runtime"` and runnable is `True`.
    - Vocabulary test asserts `extract_slots` is `compatibility_alias` with minimum reason codes `PHASE_54_COMPATIBILITY_ALIAS`, `HISTORICAL_TRACE_PROJECTION`, `IMPORT_TEST_COMPATIBILITY`, and `DELETE_BY_PHASE_58`.
    - Vocabulary test asserts `route_after_slots` is `compatibility_alias` with minimum reason codes `PHASE_54_COMPATIBILITY_ALIAS`, `HISTORICAL_TRACE_PROJECTION`, `IMPORT_TEST_COMPATIBILITY`, and `DELETE_BY_PHASE_58`.
    - Vocabulary test asserts there is exactly one entry for each `(legacy_name, kind)` pair among `slot_resolution_gate` node, `route_after_slot_resolution` router, `extract_slots` node, and `route_after_slots` router.
    - Trace projection test preserves historical `node == "extract_slots"` while projecting target node `slot_resolution_gate` and compatibility status.
    - Trace projection test covers new runtime `slot_resolution_gate` step with implementation node and target node both equal to `slot_resolution_gate` and status `runtime`.
    - Trace/API router projection test covers historical `route_after_slots` and runtime `route_after_slot_resolution`.
    - SSE test covers `_sse_event(..., node_name="slot_resolution_gate")` with `target_node_name == "slot_resolution_gate"` and keeps the historical `extract_slots` projection assertion.
    - `NODE_MESSAGES` includes a Chinese label for `slot_resolution_gate`; retained `extract_slots` label is treated as historical display compatibility.
  </behavior>
  <action>
    Update `src/agent/graph_vocabulary.py` per D-15, D-16, and D-17. Modify existing entries rather than appending duplicate entries: promote exact entries `slot_resolution_gate` node and `route_after_slot_resolution` router to `status="runtime"`, and assert uniqueness for each `(legacy_name, kind)` pair. Retain `extract_slots` node and `route_after_slots` router only as `compatibility_alias`. Compatibility aliases must include the minimum reason codes `PHASE_54_COMPATIBILITY_ALIAS`, `HISTORICAL_TRACE_PROJECTION`, `IMPORT_TEST_COMPATIBILITY`, and `DELETE_BY_PHASE_58`; validation evidence and owner detail can live in docs/debt rather than long free-text reason codes. Do not mark retained aliases as runtime. In `src/api/routers/agent_runs.py`, add `NODE_MESSAGES["slot_resolution_gate"] = "正在确认关键信息"` or equivalent concise Chinese runtime label; keep `extract_slots` only for historical/persisted rows. Update `tests/agent/test_graph_vocabulary.py`, `tests/agent/test_trace.py`, `tests/test_trace_api.py`, and `tests/test_agent_runs_api.py` to prove both runtime projection for new names and compatibility projection for retained historical names. Do not modify `src/agent/graph.py`, `src/agent/routing.py`, `docs/current-langgraph-architecture.md`, `.planning/ARCHITECTURE-DEBT.md`, or `54-VALIDATION.md` in this task.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_graph_vocabulary.py tests/agent/test_trace.py tests/test_trace_api.py tests/test_agent_runs_api.py::test_sse_event_projects_target_node_name_without_rewriting_legacy_node_name -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/graph_vocabulary.py src/api/routers/agent_runs.py tests/agent/test_graph_vocabulary.py tests/agent/test_trace.py tests/test_trace_api.py tests/test_agent_runs_api.py</automated>
  </verify>
  <acceptance_criteria>
    - `slot_resolution_gate` node vocabulary status is `runtime`.
    - `route_after_slot_resolution` router vocabulary status is `runtime`.
    - `extract_slots` and `route_after_slots` vocabulary status is `compatibility_alias`, not runtime.
    - Retained compatibility aliases include minimum reason codes `PHASE_54_COMPATIBILITY_ALIAS`, `HISTORICAL_TRACE_PROJECTION`, `IMPORT_TEST_COMPATIBILITY`, and `DELETE_BY_PHASE_58`.
    - Tests prove there are no duplicate `(legacy_name, kind)` vocabulary entries for the Phase 54 slot node/router names.
    - `src/api/routers/agent_runs.py` has a `NODE_MESSAGES` entry for `slot_resolution_gate`.
    - Tests cover both runtime and historical compatibility projection without rewriting stored historical node names.
  </acceptance_criteria>
  <done>Graph vocabulary and API/SSE label projection match Phase 54 active runtime and retained historical compatibility semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Update current architecture docs and debt ledger</name>
  <files>docs/current-langgraph-architecture.md, .planning/ARCHITECTURE-DEBT.md</files>
  <read_first>
    - docs/current-langgraph-architecture.md
    - .planning/ARCHITECTURE-DEBT.md
    - .planning/phases/54-slot-resolution-gate-cutover/54-CONTEXT.md
    - .planning/phases/54-slot-resolution-gate-cutover/54-02-SUMMARY.md
    - .planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
    - AGENTS.md
  </read_first>
  <action>
    Update `docs/current-langgraph-architecture.md` as a current-source snapshot after Phase 54. The graph diagram and source-fact summary must show active path `contextual_intent_resolve -> slot_resolution_gate -> route_after_slot_resolution`, active node set containing `slot_resolution_gate`, and no active `extract_slots`. The compatibility ledger table must mark active `extract_slots` deletion closed by Phase 54 and list any retained `src/agent/nodes/extract_slots.py` wrapper/import/test or historical trace/API label surface with exact owner `slot_resolution_gate`, reason, trace projection, validation evidence, and delete phase no later than Phase 58. Keep Phase 55 `long_term_memory_retrieve`, Phase 56 `generate_recommendation`, and Phase 57 `assess_risk_and_approval` rows active and unchanged except for context wording; do not claim `memory_context_load`, `recommendation_generation`, or `risk_gate` is active. Update `.planning/ARCHITECTURE-DEBT.md` under the canonical graph / intent-recognition area with a Chinese Phase 54 closeout entry: problem/root cause, impact, fix status, evidence commands, retained compatibility surfaces, remaining Phase 55/58 risks, and explicit statement that `slot_extraction` remains unregistered. Do not mark `54-VALIDATION.md` green in this task; Task 3 owns final validation status, per-task evidence rows, and artifact scan conclusions after the final validation commands pass. If execution discovers local validation/debug issues, append `.planning/LOCAL-VALIDATION-ISSUES.md` following AGENTS.md and reference that entry from the plan summary; do not invent issue entries without evidence.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run python -c "import pathlib; docs=pathlib.Path('docs/current-langgraph-architecture.md').read_text(); debt=pathlib.Path('.planning/ARCHITECTURE-DEBT.md').read_text(); assert 'contextual_intent_resolve' in docs and 'slot_resolution_gate' in docs and 'route_after_slot_resolution' in docs; assert 'Phase 54' in debt and 'slot_resolution_gate' in debt and 'extract_slots' in debt; print('54-03 docs/debt text checks OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/current-langgraph-architecture.md` describes active `slot_resolution_gate`, not active `extract_slots`.
    - Current architecture compatibility ledger records retained `extract_slots` / `route_after_slots` surfaces only as compatibility, with delete phase no later than Phase 58.
    - `.planning/ARCHITECTURE-DEBT.md` has a Phase 54 entry with status, evidence, retained surfaces, and remaining risk.
    - Task 2 does not mark `54-VALIDATION.md` green; Task 3 is the only task in this plan that owns final validation status and command evidence updates.
    - If `.planning/LOCAL-VALIDATION-ISSUES.md` was updated during execution, `54-03-SUMMARY.md` references the entry; if not, summary states no new local validation issue was discovered.
  </acceptance_criteria>
  <done>Docs and architecture debt ledger reflect the executed Phase 54 cutover with retained compatibility boundaries; validation status remains owned by Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Run final focused validation and artifact scans</name>
  <files>.planning/phases/54-slot-resolution-gate-cutover/54-VALIDATION.md, .planning/phases/54-slot-resolution-gate-cutover/54-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/54-slot-resolution-gate-cutover/54-VALIDATION.md
    - .planning/phases/54-slot-resolution-gate-cutover/54-01-SUMMARY.md
    - .planning/phases/54-slot-resolution-gate-cutover/54-02-SUMMARY.md
    - src/agent/graph.py
    - src/agent/routing.py
    - tests/architecture/graph_baseline.py
    - AGENTS.md
  </read_first>
  <action>
    Run the final Phase 54 focused validation suite and update `54-VALIDATION.md` with exact outputs. Task 3 is the only task in this plan that may set `status`, `nyquist_compliant`, `wave_0_complete`, per-task status rows, final command evidence, and artifact scan conclusions green. Required checks: full focused pytest suite from `54-VALIDATION.md`; Ruff on changed agent/API/test files; static scan proving no active `extract_slots` registration or `route_after_slots` conditional edge; static scan proving no registered `slot_extraction`; static scan proving Phase 54 did not activate Phase 55 `memory_context_load`, Phase 56 `recommendation_generation`, Phase 57 `risk_gate`, or Phase 58 exact no-debt cleanup; artifact scan proving Phase 54 artifacts and `docs/current-langgraph-architecture.md` contain no disallowed test entrypoint command. Use only `UV_CACHE_DIR=/tmp/uv-cache uv run ...` command forms. If any local validation/debug failure occurs, handle it, append `.planning/LOCAL-VALIDATION-ISSUES.md` with Chinese evidence, and reference it in `54-03-SUMMARY.md`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_required_slots.py tests/agent/test_nodes/test_receive_request.py tests/agent/test_nodes/test_slot_resolution_gate.py tests/agent/test_nodes/test_extract_slots.py tests/agent/test_nodes/test_contextual_intent_resolve.py tests/agent/test_graph.py tests/test_graph_routing.py tests/agent/test_intent_routing.py tests/agent/test_intent_golden_contract.py tests/agent/test_session_memory_integration.py tests/agent/test_graph_vocabulary.py tests/agent/test_trace.py tests/test_trace_api.py tests/architecture/test_canonical_graph_baseline.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_agent_runs_api.py::test_sse_event_projects_target_node_name_without_rewriting_legacy_node_name -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent src/api/routers/agent_runs.py tests/agent tests/architecture tests/test_graph_routing.py tests/test_trace_api.py tests/test_agent_runs_api.py</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run python -c "import ast,pathlib; tree=ast.parse(pathlib.Path('src/agent/graph.py').read_text()); nodes=set(); edge_keys=set();\nfor n in ast.walk(tree):\n    if isinstance(n, ast.Call) and isinstance(n.func, ast.Attribute) and n.func.attr=='add_node': nodes.add(n.args[0].value)\n    if isinstance(n, ast.Call) and isinstance(n.func, ast.Attribute) and n.func.attr=='add_conditional_edges': edge_keys.add((n.args[0].value, n.args[1].id))\nassert 'slot_resolution_gate' in nodes; assert 'extract_slots' not in nodes; assert 'slot_extraction' not in nodes; assert ('extract_slots','route_after_slots') not in edge_keys; assert {'memory_context_load','recommendation_generation','risk_gate'}.isdisjoint(nodes); print('54-03 active graph scan OK')"</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run python -c "import pathlib,re; pt='py'+'test'; roots=[pathlib.Path('.planning/phases/54-slot-resolution-gate-cutover'), pathlib.Path('docs/current-langgraph-architecture.md')]; bad=[]\ndef command_contexts(text):\n    contexts=[]\n    contexts += re.findall(r'<automated>(.*?)</automated>', text, flags=re.S)\n    for lang, body in re.findall(r'```([A-Za-z0-9_-]*)\\n(.*?)```', text, flags=re.S):\n        if lang.lower() in {'bash','sh','zsh','shell','console'}: contexts.append(body)\n    for snippet in re.findall(chr(96)+r'([^'+chr(96)+r']*)'+chr(96), text):\n        stripped=snippet.strip()\n        if stripped.startswith('$ ') or stripped.startswith(pt+' ') or stripped.startswith('python -m '+pt+' '): contexts.append(stripped)\n    return contexts\nfor root in roots:\n    files=[root] if root.is_file() else [p for p in root.rglob('*') if p.is_file()]\n    for f in files:\n        for context in command_contexts(f.read_text(errors='ignore')):\n            for line in context.splitlines():\n                stripped=line.strip().lstrip('$ ').strip()\n                if stripped == pt or stripped.startswith(pt+' ') or stripped == 'python -m '+pt or stripped.startswith('python -m '+pt+' '): bad.append(f'{f}: {stripped}')\nprint('OK' if not bad else '\\n'.join(bad)); raise SystemExit(1 if bad else 0)"</automated>
  </verify>
  <acceptance_criteria>
    - Focused pytest suite passes with the approved project entrypoint.
    - SSE projection test for legacy `extract_slots` and runtime `slot_resolution_gate` passes.
    - Ruff passes on changed source/test surfaces.
    - Static graph scan proves active node set includes `slot_resolution_gate` and excludes `extract_slots`, `slot_extraction`, `memory_context_load`, `recommendation_generation`, and `risk_gate`.
    - Static graph scan proves no active conditional edge source/router pair `("extract_slots", "route_after_slots")` remains.
    - Artifact scan finds no disallowed test runner entrypoint commands in Phase 54 artifacts or current architecture doc.
    - `54-VALIDATION.md` records the exact command evidence and final scan conclusions.
  </acceptance_criteria>
  <done>Phase 54 final validation and no-scope-creep scans are recorded with no open validation gaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime trace -> historical projection | New runtime traces use canonical names; historical names remain projected without rewriting stored records. |
| API/SSE label surface -> operator display | Node labels must show canonical runtime progress while preserving old persisted event readability. |
| source graph -> current architecture docs | Docs must describe current source facts, not target contract as if already implemented. |
| implementation evidence -> architecture debt ledger | Debt status changes only after concrete tests/scans pass. |
| validation artifacts -> downstream verification | Phase artifacts must avoid invalid local test entrypoints and scope creep claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-03-01 | Repudiation | `graph_vocabulary.py`, trace/API projection | mitigate | Mark `slot_resolution_gate` / `route_after_slot_resolution` runtime and retained legacy names compatibility-only with reason codes and delete phase per D-16/D-17. |
| T-54-03-02 | Information Disclosure | SSE / trace payload labels | mitigate | Add canonical label without exposing raw slot/provenance data; projection helper only adds target names/status. |
| T-54-03-03 | Tampering | architecture docs / debt ledger | mitigate | Update docs and ledger from source/test evidence only; keep target contract and current-source facts separated per project rule. |
| T-54-03-04 | Denial of Service | invalid validation commands | mitigate | Artifact scan rejects disallowed test entrypoints in Phase 54 artifacts; all verify commands use `UV_CACHE_DIR=/tmp/uv-cache uv run ...`. |
| T-54-03-05 | Scope Creep | Phase 55/56/57/58 active nodes | mitigate | Static graph scan rejects active `memory_context_load`, `recommendation_generation`, and `risk_gate` registrations and does not enforce Phase 58 final exact no-debt cleanup here. |
| T-54-03-06 | Integrity | retained compatibility aliases | mitigate | Compatibility table requires owner, reason, trace projection, validation evidence, and delete phase no later than Phase 58. |
</threat_model>

<verification>
Plan 54-03 is complete when vocabulary/API/trace tests, focused Phase 54 suite, Ruff, docs/debt text checks, active graph scans, no-`slot_extraction` scan, no-scope-creep scan, and no-disallowed-entrypoint artifact scan all pass and are recorded in `54-VALIDATION.md`.
</verification>

<success_criteria>
- `slot_resolution_gate` and `route_after_slot_resolution` are runtime vocabulary entries.
- `extract_slots` and `route_after_slots` are compatibility aliases only, with delete phase no later than Phase 58.
- API/SSE labels cover `slot_resolution_gate`.
- Current architecture docs and architecture debt ledger match source facts after Phase 54.
- Final validation evidence is recorded using only approved project entrypoints.
- Final scans prove no active `extract_slots`, no registered `slot_extraction`, and no Phase 55/56/57/58 active scope creep.
</success_criteria>

<output>
After completion, create `.planning/phases/54-slot-resolution-gate-cutover/54-03-SUMMARY.md`. The summary must include final command evidence, compatibility surfaces retained with delete phase, architecture debt update location, validation status, and whether `.planning/LOCAL-VALIDATION-ISSUES.md` was appended during execution.
</output>
