---
phase: 56-recommendation-generation-and-rag-claim-status-alignment
plan: 3
type: execute
wave: 2
depends_on: [56-01]
files_modified:
  - src/agent/routing.py
  - tests/agent/test_rag_context_routing.py
  - tests/agent/rag_context/test_routing.py
  - tests/knowledge/test_verified_evidence_package.py
  - tests/knowledge/test_claim_verification_bundle.py
autonomous: true
requirements: [CAGM-07]
user_setup: []
must_haves:
  truths:
    - "RAG status routing is finite, schema-aligned, and fails closed for missing, unknown, malformed, unsafe, stale, conflicting, invalid, and build-error states."
    - "partial RAG status can generate only for deterministic low-risk answer-only or policy-QA flows, never action-bound or high-risk flows."
    - "Proposed actions cannot reach assess_risk_and_approval without explicit verified action-recommendation claim allowance."
    - "Legacy verifier fields cannot override a missing, blocked, or non-continuing claim_verification_bundle."
  artifacts:
    - path: "src/agent/routing.py"
      provides: "Fail-closed deterministic RAG and claim route gates"
      contains: "RAG_CONTEXT_STATUSES"
    - path: "tests/agent/test_rag_context_routing.py"
      provides: "RAG status totality and partial-routing regression tests"
    - path: "tests/agent/rag_context/test_routing.py"
      provides: "Claim verification action gate regression tests"
  key_links:
    - from: "src/agent/routing.py"
      to: "src/knowledge/schemas.py"
      via: "RAG_CONTEXT_STATUSES schema vocabulary"
      pattern: "RAG_CONTEXT_STATUSES"
    - from: "src/agent/routing.py"
      to: "claim_verification_bundle"
      via: "route_after_claim_verify canonical bundle gate"
      pattern: "allows_action_recommendation"
---

<objective>
Harden deterministic RAG and claim routing so unsafe evidence or unsupported claims cannot enter action paths.

Purpose: satisfy D-56-05 through D-56-11, preserve Phase 57 scope per D-56-15, and close CAGM-07 safety semantics.
Output: schema-aligned status routing, explicit action-claim gate, and router/DTO tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-CONTEXT.md
@.planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-RESEARCH.md
@.planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-PATTERNS.md
@.planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-VALIDATION.md
@.planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
@AGENTS.md

This plan can run in Wave 2 parallel with 56-02 after 56-01 because it touches `src/agent/routing.py` and RAG/claim tests, while 56-02 owns `src/agent/graph.py` and graph baseline files. Both converge in 56-04.

<interfaces>
From `src/knowledge/schemas.py`:
```python
RAG_CONTEXT_STATUSES = (
    "not_required", "verified", "partial", "no_evidence", "unauthorized",
    "stale", "conflict", "invalid_hash", "invalid_scope", "build_error",
)
class ClaimVerificationBundleV1(BaseModel):
    overall_status: Literal["verified", "blocked", "manual_review", "not_required", "error"]
    route: Literal["continue", "final_response", "manual_review"]
    blocked_claims: list[str]
```

From current `src/agent/routing.py`:
```python
def route_after_rag_context(state: AgentState) -> str: ...
def route_after_claim_verify(state: AgentState) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align RAG status vocabulary and partial fail-closed routing</name>
  <files>src/agent/routing.py, tests/agent/test_rag_context_routing.py, tests/knowledge/test_verified_evidence_package.py</files>
  <read_first>
    AGENTS.md
    .planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-CONTEXT.md
    .planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-RESEARCH.md
    src/agent/routing.py
    src/knowledge/schemas.py
    tests/agent/test_rag_context_routing.py
    tests/knowledge/test_verified_evidence_package.py
  </read_first>
  <behavior>
    - Router status vocabulary equals `set(src.knowledge.schemas.RAG_CONTEXT_STATUSES)`.
    - Missing or unknown `rag_context_status` routes to `final_response`.
    - `no_evidence`, `unauthorized`, `stale`, `conflict`, `invalid_hash`, `invalid_scope`, and `build_error` route to `final_response`.
    - `partial` routes to `recommendation_generation` only for deterministic low-risk answer-only or policy-QA flows and routes to `final_response` for proposed-action, action-bound, high-risk, or approval-required state.
  </behavior>
  <action>
    In `src/agent/routing.py`, eliminate free-form RAG status drift by deriving or asserting the router status set from `src.knowledge.schemas.RAG_CONTEXT_STATUSES`. Keep public `route_after_rag_context` exception handling fail-closed to `final_response`. Add tests in `tests/agent/test_rag_context_routing.py` for every status in the approved vocabulary plus missing/unknown/malformed status cases. Strengthen or add tests proving `partial` cannot route to `recommendation_generation` when `proposed_action`, action intent/operation, high risk, approval-required risk, or unsafe evidence indicators are present. Keep `verified` and allowed `not_required` behavior unchanged. Do not let any unsafe RAG status promote `evidence_refs`, approval snapshots, risk lowering, approval, or action-draft authority; this task proves the route gate blocks those paths.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_rag_context_routing.py tests/knowledge/test_verified_evidence_package.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    `tests/agent/test_rag_context_routing.py` checks every exact status: `not_required`, `verified`, `partial`, `no_evidence`, `unauthorized`, `stale`, `conflict`, `invalid_hash`, `invalid_scope`, and `build_error`.
    A test fails if router and `src.knowledge.schemas.RAG_CONTEXT_STATUSES` diverge.
    Tests prove unsafe statuses and missing/unknown status return `final_response`.
    Tests prove action-bound `partial` returns `final_response`.
  </acceptance_criteria>
  <done>RAG routing is schema-aligned, total over known statuses, and fail-closed for unsafe evidence states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Require explicit action-claim support before risk routing</name>
  <files>src/agent/routing.py, tests/agent/rag_context/test_routing.py, tests/knowledge/test_claim_verification_bundle.py</files>
  <read_first>
    .planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-CONTEXT.md
    .planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-RESEARCH.md
    src/agent/routing.py
    src/agent/nodes/claim_verify.py
    src/knowledge/schemas.py
    tests/agent/rag_context/test_routing.py
    tests/knowledge/test_claim_verification_bundle.py
  </read_first>
  <behavior>
    - `route_after_recommendation` sends material claims, user-visible claims, and proposed actions to `claim_verify`.
    - `route_after_claim_verify` returns `final_response` when `claim_verification_bundle` is missing, blocked, manual-review, error, or has route other than `continue`.
    - `route_after_claim_verify` returns `final_response` for `proposed_action` with `verified/continue` bundle unless at least one action-recommendation claim result has `allows_action_recommendation is True`.
    - Legacy `verification_route`, `verifier_status`, and `verifier_reason_codes` cannot override the canonical bundle.
  </behavior>
  <action>
    Strengthen `_route_after_claim_verify` in `src/agent/routing.py`: after canonical bundle route/status and blocked-claim checks pass, route to current Phase 57 risk node `assess_risk_and_approval` only when one of these is true: a non-action risk signal requires risk assessment without a proposed action, or a proposed action/action-recommendation claim is present and `_has_verified_action_recommendation(state)` is true. A proposed action alone must not be sufficient. Add negative tests where `proposed_action` plus `overall_status="verified"` and `route="continue"` but no allowed action-recommendation result returns `final_response`. Add positive tests where an `action_recommendation` result with `allows_action_recommendation=True` reaches `assess_risk_and_approval`. Add tests that legacy verifier fields set to allow cannot overcome a missing canonical bundle, blocked claims, `overall_status="blocked"`, or `route="final_response"`. Do not rename `assess_risk_and_approval` to `risk_gate`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/rag_context/test_routing.py tests/knowledge/test_claim_verification_bundle.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    `tests/agent/rag_context/test_routing.py` contains a case where proposed action without allowed action claim returns `final_response`.
    `tests/agent/rag_context/test_routing.py` contains a case where allowed `action_recommendation` claim result returns `assess_risk_and_approval`.
    Tests prove `verification_route="allow"` cannot bypass a missing or blocked `claim_verification_bundle`.
    `src/agent/routing.py` still returns current Phase 57 node value `assess_risk_and_approval`, not `risk_gate`.
  </acceptance_criteria>
  <done>Claim verification is the canonical action gate, and unsupported action claims cannot enter risk/approval/action paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RAG package -> recommendation routing | Evidence status controls whether generation can proceed. |
| Generated claims -> risk/action routing | Claim bundle controls whether proposed actions can reach risk/approval/action nodes. |
| Compatibility verifier fields -> canonical bundle | Legacy projection fields remain non-authoritative. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-02 | Tampering / elevation of privilege | `route_after_rag_context` | mitigate | Schema-aligned status tests and action-bound `partial` tests fail closed to `final_response`. |
| T-56-03 | Elevation of privilege | `route_after_claim_verify` | mitigate | Proposed actions require explicit allowed `action_recommendation` claim result before current risk node. |
| T-56-04 | Tampering | legacy verifier fields | mitigate | Router tests prove legacy fields cannot override missing/blocked canonical bundle. |
| T-56-05 | Tampering | risk route value | mitigate | Preserve `assess_risk_and_approval` as Phase 57 current node; do not introduce `risk_gate`. |
</threat_model>

<verification>
Plan-local command:
`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_rag_context_routing.py tests/agent/rag_context/test_routing.py tests/knowledge/test_verified_evidence_package.py tests/knowledge/test_claim_verification_bundle.py -q --tb=short`

Wave command after 56-02 and 56-03:
`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/agent/test_graph.py tests/test_graph_routing.py tests/agent/test_rag_context_routing.py tests/agent/rag_context/test_routing.py tests/knowledge/test_verified_evidence_package.py tests/knowledge/test_claim_verification_bundle.py tests/agent/test_graph_vocabulary.py -q --tb=short`
</verification>

<success_criteria>
RAG evidence statuses and claim bundle semantics are deterministic, schema-aligned, and fail closed before unsafe evidence or unsupported action claims can reach current risk/approval/action paths.
</success_criteria>

<output>
After completion, create `.planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-03-SUMMARY.md`.
</output>
