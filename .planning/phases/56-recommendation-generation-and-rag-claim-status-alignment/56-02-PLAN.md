---
phase: 56-recommendation-generation-and-rag-claim-status-alignment
plan: 2
type: execute
wave: 2
depends_on: [56-01]
files_modified:
  - src/agent/graph.py
  - tests/architecture/graph_baseline.py
  - tests/architecture/test_canonical_graph_baseline.py
  - tests/agent/test_graph.py
  - tests/test_graph_routing.py
autonomous: true
requirements: [CAGM-07]
user_setup: []
must_haves:
  truths:
    - "Active StateGraph registers recommendation_generation and no longer registers generate_recommendation."
    - "Route maps from investigate and rag_context_build send recommendation_generation route values to the recommendation_generation node."
    - "route_after_recommendation is attached to recommendation_generation."
    - "assess_risk_and_approval remains the only active legacy graph row for Phase 57."
  artifacts:
    - path: "src/agent/graph.py"
      provides: "Active graph node and route-map cutover"
      contains: "builder.add_node(\"recommendation_generation\""
    - path: "tests/architecture/graph_baseline.py"
      provides: "Phase 56 active graph baseline"
      contains: "recommendation_generation"
    - path: "tests/architecture/test_canonical_graph_baseline.py"
      provides: "Static graph guardrails for Phase 56/57 boundary"
  key_links:
    - from: "src/agent/graph.py"
      to: "src/agent/nodes/recommendation_generation.py"
      via: "import and StateGraph.add_node"
      pattern: "from src.agent.nodes.recommendation_generation import recommendation_generation"
---

<objective>
Cut over the active graph/router baseline to `recommendation_generation` while preserving the Phase 57 `assess_risk_and_approval` legacy row.

Purpose: satisfy D-56-01, D-56-02, D-56-14, D-56-15, and CAGM-07 active graph naming.
Output: graph registration, path maps, and architecture tests proving canonical recommendation node activation.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-CONTEXT.md
@.planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-RESEARCH.md
@.planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-PATTERNS.md
@.planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-VALIDATION.md
@.planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
@.planning/phases/55-memory-context-load-cutover/55-VERIFICATION.md
@AGENTS.md

<interfaces>
Current graph pattern:
```python
builder.add_node("generate_recommendation", generate_recommendation, retry_policy=_llm_retry)
"recommendation_generation": "generate_recommendation"
builder.add_conditional_edges("generate_recommendation", route_after_recommendation, ...)
```

Phase 56 target:
```python
builder.add_node("recommendation_generation", recommendation_generation, retry_policy=_llm_retry)
"recommendation_generation": "recommendation_generation"
builder.add_conditional_edges("recommendation_generation", route_after_recommendation, ...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register active recommendation_generation and update graph baseline</name>
  <files>src/agent/graph.py, tests/architecture/graph_baseline.py, tests/architecture/test_canonical_graph_baseline.py</files>
  <read_first>
    AGENTS.md
    .planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-CONTEXT.md
    .planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
    .planning/phases/55-memory-context-load-cutover/55-VERIFICATION.md
    src/agent/graph.py
    tests/architecture/graph_baseline.py
    tests/architecture/test_canonical_graph_baseline.py
  </read_first>
  <behavior>
    - `graph_add_node_names()` contains `recommendation_generation`.
    - `graph_add_node_names()` does not contain `generate_recommendation`.
    - `MIGRATION_MODE_LEGACY_NODE_MAP` contains `assess_risk_and_approval` only.
    - `risk_gate` is not registered in Phase 56.
  </behavior>
  <action>
    In `src/agent/graph.py`, replace the graph import with `from src.agent.nodes.recommendation_generation import recommendation_generation`, register `builder.add_node("recommendation_generation", recommendation_generation, retry_policy=_llm_retry)`, change the `investigate` and `rag_context_build` path-map destinations for route value `recommendation_generation` to `"recommendation_generation"`, and move `route_after_recommendation` so its source is `"recommendation_generation"`. In `tests/architecture/graph_baseline.py`, replace `generate_recommendation` with `recommendation_generation` in `CURRENT_ACTIVE_GRAPH_NODES_BASELINE`, remove `generate_recommendation` from `MIGRATION_MODE_LEGACY_NODE_MAP`, keep `assess_risk_and_approval` mapped to `risk_gate` with delete phase `Phase 57`, and update `CURRENT_CONDITIONAL_EDGE_BASELINE` keys/destinations. In `tests/architecture/test_canonical_graph_baseline.py`, update assertions so Phase 56 closes only the recommendation row and explicitly preserves the Phase 57 row. Do not register `risk_gate`, delete compatibility aliases, or implement Phase 58 no-debt cleanup.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    `src/agent/graph.py` contains `builder.add_node("recommendation_generation", recommendation_generation`.
    `src/agent/graph.py` does not contain `builder.add_node("generate_recommendation"`.
    `tests/architecture/graph_baseline.py` maps only `assess_risk_and_approval` in `MIGRATION_MODE_LEGACY_NODE_MAP`.
    `tests/architecture/test_canonical_graph_baseline.py` asserts `generate_recommendation` is absent and `assess_risk_and_approval` remains present.
  </acceptance_criteria>
  <done>Active graph source and static baseline prove recommendation canonicalization while preserving Phase 57 scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update graph integration and route-map tests</name>
  <files>tests/agent/test_graph.py, tests/test_graph_routing.py, tests/architecture/test_canonical_graph_baseline.py</files>
  <read_first>
    .planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-CONTEXT.md
    src/agent/graph.py
    src/agent/routing.py
    tests/agent/test_graph.py
    tests/test_graph_routing.py
    tests/architecture/test_canonical_graph_baseline.py
  </read_first>
  <behavior>
    - Graph tests inspect source/compiled graph expectations using `recommendation_generation`.
    - Route-map tests prove `route_after_investigate` and `route_after_rag_context` route value `recommendation_generation` reaches node `recommendation_generation`.
    - Route-map tests prove `claim_verify -> assess_risk_and_approval` remains unchanged for Phase 57.
  </behavior>
  <action>
    Update current graph and routing tests that still assert `generate_recommendation` as an active node or destination. Add or adjust tests so path maps from `investigate` and `rag_context_build` contain exact pair `"recommendation_generation": "recommendation_generation"`, the recommendation conditional edge source is `("recommendation_generation", "route_after_recommendation")`, and route destinations no longer include active `generate_recommendation`. Keep assertions that `claim_verify`, `route_after_risk`, and `approval_gate` still reference `assess_risk_and_approval`; this is required by D-56-15 and Phase 57. Do not change `src/agent/routing.py` in this plan unless a test import name must be adjusted without behavior change.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/agent/test_graph.py tests/test_graph_routing.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    Focused graph tests pass with active `recommendation_generation`.
    `rg -n '"recommendation_generation": "generate_recommendation"|\\("generate_recommendation", "route_after_recommendation"\\)' tests/architecture tests/agent tests/test_graph_routing.py` returns no active-baseline expectation hits.
    Tests still contain `assess_risk_and_approval` Phase 57 preservation assertions.
  </acceptance_criteria>
  <done>Graph integration tests and route-map tests agree with the Phase 56 active graph target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Router route value -> StateGraph destination | Deterministic route strings cross into registered node execution. |
| Phase 56 graph cutover -> Phase 57 risk boundary | Recommendation rename must not change risk/approval ownership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01 | Tampering | `src/agent/graph.py` | mitigate | Active `StateGraph.add_node(...)` uses `recommendation_generation`; static tests reject active `generate_recommendation`. |
| T-56-05 | Tampering / elevation of privilege | `tests/architecture/graph_baseline.py` | mitigate | Baseline preserves `assess_risk_and_approval` as the only active legacy row and rejects early `risk_gate` registration. |
| T-56-06 | Availability | route-map compatibility | mitigate | Historical projection is deferred to 56-04; this plan changes only active graph destinations. |
</threat_model>

<verification>
Plan-local command:
`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/agent/test_graph.py tests/test_graph_routing.py -q --tb=short`

Wave command after 56-02 and 56-03:
`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/agent/test_graph.py tests/test_graph_routing.py tests/agent/test_rag_context_routing.py tests/agent/rag_context/test_routing.py tests/knowledge/test_verified_evidence_package.py tests/knowledge/test_claim_verification_bundle.py tests/agent/test_graph_vocabulary.py -q --tb=short`
</verification>

<success_criteria>
The active graph registers `recommendation_generation`, route maps target that canonical node, active `generate_recommendation` graph registration is gone, and `assess_risk_and_approval` remains Phase 57-owned.
</success_criteria>

<output>
After completion, create `.planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-02-SUMMARY.md`.
</output>
