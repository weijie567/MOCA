---
phase: 56-recommendation-generation-and-rag-claim-status-alignment
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/nodes/recommendation_generation.py
  - src/agent/nodes/generate_recommendation.py
  - tests/agent/test_nodes/test_generate_recommendation.py
  - tests/agent/test_phase22_recommendation_integration.py
autonomous: true
requirements: [CAGM-07]
user_setup: []
must_haves:
  truths:
    - "Canonical recommendation runs expose recommendation_generation as callable, trace node, and llm_outputs key."
    - "Legacy generate_recommendation remains only import/test/historical compatibility until Phase 58."
    - "Recommendation generation writes candidate draft/claims/action/missing_info/evidence_refs only; verifier-owned fields remain owned by claim_verify."
  artifacts:
    - path: "src/agent/nodes/recommendation_generation.py"
      provides: "Canonical recommendation_generation callable"
      exports: ["recommendation_generation"]
    - path: "src/agent/nodes/generate_recommendation.py"
      provides: "Narrow legacy compatibility wrapper"
      contains: "DELETE_BY_PHASE_58"
    - path: "tests/agent/test_nodes/test_generate_recommendation.py"
      provides: "Canonical and legacy identity regression tests"
  key_links:
    - from: "src/agent/nodes/recommendation_generation.py"
      to: "src/agent/nodes/generate_recommendation.py"
      via: "shared implementation or delegated compatibility helper"
      pattern: "recommendation_generation"
---

<objective>
Create the canonical `recommendation_generation` node callable and keep `generate_recommendation` only as a documented compatibility import/test surface.

Purpose: satisfy D-56-01, D-56-03, D-56-04, D-56-08, D-56-09, and CAGM-07 before graph wiring changes.
Output: canonical callable module, narrow legacy wrapper/import compatibility, and node-level tests proving canonical trace/output identity.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-CONTEXT.md
@.planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-RESEARCH.md
@.planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-PATTERNS.md
@.planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-VALIDATION.md
@.planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
@.planning/phases/55-memory-context-load-cutover/55-VERIFICATION.md
@AGENTS.md

<interfaces>
From `src/agent/nodes/generate_recommendation.py`:
```python
async def generate_recommendation(state: AgentState, config: RunnableConfig = None) -> dict:
    ...
outputs = {**(state.get("llm_outputs") or {}), "generate_recommendation": draft}
MaterialClaimV1(..., generated_from_step="recommendation_generation", ...)
```

Required canonical contract:
```python
async def recommendation_generation(state: AgentState, config: RunnableConfig = None) -> dict:
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical recommendation_generation callable and identity tests</name>
  <files>src/agent/nodes/recommendation_generation.py, src/agent/nodes/generate_recommendation.py, tests/agent/test_nodes/test_generate_recommendation.py</files>
  <read_first>
    AGENTS.md
    .planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-CONTEXT.md
    .planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-PATTERNS.md
    .planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
    src/agent/nodes/generate_recommendation.py
    tests/agent/test_nodes/test_generate_recommendation.py
  </read_first>
  <behavior>
    - Canonical import succeeds from `src.agent.nodes.recommendation_generation`.
    - Canonical completed path writes `llm_outputs["recommendation_generation"]`, not a new `llm_outputs["generate_recommendation"]`.
    - Canonical trace appends `node == "recommendation_generation"`.
    - Canonical insufficient-evidence path writes the insufficient-evidence draft under `recommendation_generation`.
    - Legacy `generate_recommendation` remains importable for existing tests.
  </behavior>
  <action>
    Update tests first, then create `src/agent/nodes/recommendation_generation.py` exporting `async def recommendation_generation(state: AgentState, config: RunnableConfig = None) -> dict`. Refactor `src/agent/nodes/generate_recommendation.py` so the implementation can be invoked with explicit `output_key` and `trace_node` values. The canonical callable must use `output_key="recommendation_generation"` and `trace_node="recommendation_generation"`. The legacy `generate_recommendation` function may delegate with legacy identity for import/test compatibility only. Do not edit `src/agent/graph.py` in this plan. Do not write `claim_verification_bundle`, `blocked_claims`, `safe_support_refs`, `verifier_status`, `verification_route`, or `verifier_reason_codes` from recommendation generation per D-56-09 and D-56-11.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_generate_recommendation.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    `src/agent/nodes/recommendation_generation.py` exists and contains `async def recommendation_generation`.
    Tests assert `llm_outputs["recommendation_generation"]` and trace node `recommendation_generation`.
    Canonical tests prove the canonical callable does not add `llm_outputs["generate_recommendation"]`.
    Existing legacy import tests for `generate_recommendation` still pass.
  </acceptance_criteria>
  <done>Canonical callable identity is test-locked, and node-level tests pass through the approved `uv` entrypoint.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock compatibility metadata and recommendation authority boundary</name>
  <files>src/agent/nodes/generate_recommendation.py, tests/agent/test_nodes/test_generate_recommendation.py, tests/agent/test_phase22_recommendation_integration.py</files>
  <read_first>
    .planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-CONTEXT.md
    .planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-RESEARCH.md
    src/agent/nodes/generate_recommendation.py
    src/agent/nodes/claim_verify.py
    tests/agent/test_nodes/test_generate_recommendation.py
    tests/agent/test_phase22_recommendation_integration.py
  </read_first>
  <behavior>
    - Compatibility surface records legacy surface `generate_recommendation`, canonical owner `recommendation_generation`, reason `IMPORT_TEST_COMPATIBILITY`, trace projection expectation, validation tests, and delete phase `Phase 58`.
    - Recommendation generation emits `MaterialClaimV1.generated_from_step == "recommendation_generation"`.
    - Recommendation generation results do not include verifier-owned fields.
  </behavior>
  <action>
    Add explicit compatibility metadata near the legacy export with exact strings `PHASE_56_COMPATIBILITY_ALIAS`, `HISTORICAL_TRACE_PROJECTION`, `IMPORT_TEST_COMPATIBILITY`, and `DELETE_BY_PHASE_58` or constants consumed by tests. Extend node/integration tests so any result with `material_claims` uses `generated_from_step == "recommendation_generation"` and no result from either canonical or legacy import writes verifier-owned state keys. Keep any legacy mirror documented; do not add active graph registration or Phase 58 deletion work.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_generate_recommendation.py tests/agent/test_phase22_recommendation_integration.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    `src/agent/nodes/generate_recommendation.py` contains `DELETE_BY_PHASE_58` and `recommendation_generation`.
    Tests fail if recommendation generation writes verifier-owned state keys.
    Tests fail if generated material claims use `generate_recommendation` as `generated_from_step`.
    No Phase 57 `risk_gate` rename and no Phase 58 deletion is implemented.
  </acceptance_criteria>
  <done>Compatibility metadata and generation authority boundaries are explicit, tested, and scoped to Phase 58 cleanup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output -> AgentState | Structured recommendation draft enters graph state but cannot verify evidence or claims. |
| Legacy import -> canonical runtime | `generate_recommendation` remains compatibility while active runtime will use canonical identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01 | Tampering | `src/agent/nodes/recommendation_generation.py` | mitigate | Canonical callable emits `recommendation_generation`; graph registration is deferred to 56-02. |
| T-56-04 | Tampering | `src/agent/nodes/generate_recommendation.py` | mitigate | Tests prove recommendation generation cannot write verifier-owned compatibility fields. |
| T-56-06 | Availability | legacy `generate_recommendation` import surface | mitigate | Preserve narrow import/test compatibility with Phase 58 delete metadata. |
</threat_model>

<verification>
Plan-local command:
`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_generate_recommendation.py tests/agent/test_phase22_recommendation_integration.py -q --tb=short`

Phase quick command after this plan:
`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/agent/test_graph_vocabulary.py tests/agent/test_rag_context_routing.py tests/agent/rag_context/test_routing.py -q --tb=short`
</verification>

<success_criteria>
`recommendation_generation` exists as a canonical callable with canonical trace/output identity; `generate_recommendation` remains only narrow compatibility; recommendation generation cannot mark claims or evidence safe.
</success_criteria>

<output>
After completion, create `.planning/phases/56-recommendation-generation-and-rag-claim-status-alignment/56-01-SUMMARY.md`.
</output>
