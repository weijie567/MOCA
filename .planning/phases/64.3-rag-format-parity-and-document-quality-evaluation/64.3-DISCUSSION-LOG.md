# Phase 64.3: RAG Format Parity And Document Quality Evaluation - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-10
**Phase:** 64.3-rag-format-parity-and-document-quality-evaluation
**Mode:** Autopilot `--auto`; the recommended option was selected for every gray area under the user's explicit autopilot authorization.
**Areas discussed:** Gold identity, parser scoring, retrieval isolation, baseline policy

---

## Gold identity

| Option | Description | Selected |
|--------|-------------|----------|
| Shared semantic Gold | One canonical fact/anchor set per policy, reused across formats; optional format-specific locator constraints; no chunk IDs | ✓ |
| Per-format Gold | Duplicate expected answers and locations for Markdown/digital/scanned variants | |
| Reuse legacy chunk Gold | Extend the existing 22-case expected chunk-ID schema | |

**Choice:** Shared semantic Gold.
**Notes:** This remains valid when parsers or Phase 64.4 chunk boundaries differ, while manifest/Gold hashes preserve reproducibility.

---

## Parser scoring

| Option | Description | Selected |
|--------|-------------|----------|
| Layered metrics with comparison-only normalization | Score facts, structure, tables, provenance, pages, OCR, and warnings separately; preserve production output | ✓ |
| One aggregate text similarity score | Collapse all parser quality into a single score | |
| Normalize parser output before scoring | Rewrite parser output into a common canonical form | |

**Choice:** Layered metrics with evaluation-only normalization.
**Notes:** Protected numeric/date/negation anchors remain exact; misses receive actionable safe reason codes.

---

## Retrieval isolation

| Option | Description | Selected |
|--------|-------------|----------|
| Fixed evaluation tenant with allowlisted round cleanup | Real ingestion/retrieval, one format per round, strict pre/post cleanup proof, fail closed | ✓ |
| Transaction rollback only | Run all ingestion/retrieval in one transaction and rely on rollback | |
| Broad database reset | Truncate RAG tables between rounds | |

**Choice:** Fixed evaluation tenant with allowlisted round cleanup.
**Notes:** Cleanup must preserve Phase 64.2 immutable historical identity/replay records and cannot touch non-evaluation tenants.

---

## Baseline policy

| Option | Description | Selected |
|--------|-------------|----------|
| Truthful versioned baseline, even when red | Canonical JSON plus Markdown, full configuration/hashes, explicit thresholds and stage attribution | ✓ |
| Require all target thresholds before recording | Do not check in a baseline until every metric is green | |
| Report scores without gates | Produce metrics but avoid explicit pass/fail or threshold versioning | |

**Choice:** Truthful versioned baseline, even when red.
**Notes:** Quality misses are named deferrals; evaluator correctness and reproducibility are Phase 64.3 requirements. Thresholds cannot be relaxed silently.

---

## the agent's Discretion

- Schema/module names, report layout, reason-code spelling, evaluation UUID/run-token representation, and dependency-ordered plan slicing within the locked constraints.

## Deferred Ideas

- Phase 64.4 owns token-aware chunking and A/B reindex validation.
- Mixed corpus, parser remediation, and retrieval tuning require named later phases/backlog ownership after this baseline.
