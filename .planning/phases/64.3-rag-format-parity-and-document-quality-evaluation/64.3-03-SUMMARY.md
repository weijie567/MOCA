---
phase: 64.3-rag-format-parity-and-document-quality-evaluation
plan: "03"
subsystem: rag-evaluation
tags: [postgresql, pgvector, alembic, rag, retrieval, evaluation, tdd]

# Dependency graph
requires:
  - phase: 64.3-01
    provides: canonical three-format corpus, semantic Gold questions, and evaluation contracts
  - phase: 64.2
    provides: immutable document/chunk versions and exact historical replay identity
provides:
  - Durable fixed-tenant evaluation ownership with per-step lock/CAS validation
  - Exact cleanup and crash-recovery classification that preserves immutable evidence
  - Three isolated production ingestion/retrieval rounds over identical ordered Gold questions
  - Fail-closed provider CLI with safe prerequisite and outcome taxonomy
affects: [64.3-04, rag-evaluation, provider-baseline, immutable-replay]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Evaluation-only durable owner row with short-transaction lock/CAS transitions
    - Exact attempt-job claim before exact deletion
    - Recording delegate around the production retrieval engine
    - Baseline-ineligible contract tests and fail-closed provider diagnostics

key-files:
  created:
    - src/db/migrations/versions/029_phase64_3_rag_eval_rounds.py
    - src/repositories/rag_evaluation_round_repo.py
    - src/rag/evaluation/retrieval_rounds.py
    - scripts/eval_rag_format_parity.py
    - tests/eval/test_rag_retrieval_round_isolation.py
  modified:
    - src/db/models.py
    - src/repositories/rag_ingestion_job_repo.py

key-decisions:
  - "Evaluation mutation authority is one fixed tenant plus an exact marker, allowlist, run token, round token, format, progress cursor, and CAS version; no production-tenant discovery is permitted."
  - "Ingestion recovery classifies durable database projection and advances only an exact complete projection; reservation-only, job-only, failure, and malformed states never silently advance."
  - "Provider retrieval is recorded by forwarding the single call made through PolicyKnowledgeService; no second query or repository-side substitute is allowed."
  - "Unavailable infrastructure emits unavailable_prerequisite with baseline_eligible=false and no quality score or pass claim."

patterns-established:
  - "Isolation proof: every mutating step re-locks the owner row, verifies exact identity/progress/state, locks heads deterministically, and commits through CAS."
  - "Cleanup proof: delete only exact current children and a CAS-claimed exact attempt job while retaining heads and all immutable versions."

requirements-completed: [ROADMAP-SC-3, ROADMAP-SC-5]

# Metrics
duration: 33min
completed: 2026-08-10
---

# Phase 64.3 Plan 03: Retrieval Round Isolation And Provider Runtime Summary

**Durable fail-closed evaluation ownership now drives three isolated production ingestion/retrieval rounds while preserving Phase 64.2 immutable replay evidence and refusing fake provider baselines.**

## Performance

- **Duration:** 33 min
- **Started:** 2026-08-10T11:21:29Z
- **Completed:** 2026-08-10T11:54:32Z
- **Tasks:** 3
- **Files modified:** 8

## Accomplishments

- Added migration 029 and an evaluation-only control model with fixed identity, exact three-document allowlist, one-active-round partial uniqueness, durable reservation/progress state, proof snapshots, leases, and terminal state.
- Added a repository boundary that denies mismatched identity before mutation, serializes every transition with owner-row locking and CAS, recognizes crash projections, and cleans only exact current children and exact claimed jobs without deleting document heads or immutable versions.
- Added a real runtime over `IngestionService`, `PolicyRetrievalEngine`, and `PolicyKnowledgeService`, with the same 18 ordered Gold questions for Markdown, digital PDF, and scanned PDF and a forwarding recording delegate that observes the service-triggered retrieval exactly once.
- Added an explicit provider-only CLI that validates the fixed evaluation tenant, schema/pgvector, rollout, embedding provider, and OCR languages, then emits only safe `unavailable_prerequisite`, `execution_error`, `completed_quality_fail`, or completed results.
- Proved migration reversibility and production-history preservation against an isolated PostgreSQL/pgvector instance, including active-round downgrade refusal and the documented safe-abandon sequence.

## Task Commits

Each task used an explicit RED then GREEN sequence:

1. **Task 1 RED: Add failing ownership/isolation tests** - `38372df` (`test`)
2. **Task 1 GREEN: Enforce durable evaluation round isolation** - `15f1d48` (`feat`)
3. **Task 2 RED: Add failing retrieval runtime tests** - `3c5f159` (`test`)
4. **Task 2 GREEN: Run isolated production retrieval rounds** - `496d975` (`feat`)
5. **Task 3 RED: Add failing provider CLI safety tests** - `91d9621` (`test`)
6. **Task 3 GREEN: Expose fail-closed provider parity CLI** - `49c3295` (`feat`)
7. **Task 3 correctness follow-up: Resume durable ingestion progress safely** - `16f3007` (`fix`)

## Files Created/Modified

- `src/db/models.py` - Adds the durable `RagEvaluationRound` evaluation control model and one-active-round partial unique index.
- `src/db/migrations/versions/029_phase64_3_rag_eval_rounds.py` - Creates evaluation-only schema objects, refuses unsafe downgrade, and drops only those objects after terminal cleanup.
- `src/repositories/rag_evaluation_round_repo.py` - Implements ownership validation, CAS progress, projection classification, proof capture, exact cleanup, expiry, and terminal transitions.
- `src/repositories/rag_ingestion_job_repo.py` - Adds exact post-reservation candidate locking, attempt enumeration, and full-identity job deletion seams.
- `src/rag/evaluation/retrieval_rounds.py` - Runs the isolated three-format production path and exports typed round/case observations.
- `scripts/eval_rag_format_parity.py` - Adds the provider CLI, prerequisite probes, safe result projection, and nonzero unavailable/error exits.
- `tests/eval/test_rag_retrieval_round_isolation.py` - Covers denial-before-mutation, ORM/migration contracts, crash projections, immutable preservation, real-service wiring, and CLI safety taxonomy.

## Decisions Made

- Kept the control plane evaluation-only: migration 029 adds no production columns and mutates no production rows.
- Used one generic external isolation denial while retaining stable internal reason codes for diagnosis without tenant/state enumeration.
- Required database-derived reservation time and exact `(tenant_id, doc_key, source_checksum, created_at >= reserved_at)` candidate selection before recording and deleting a job UUID.
- Preserved `PolicyDocument` heads and immutable `PolicyDocumentVersion`/`PolicyChunkVersion` evidence; cleanup is limited to exact current block/chunk children and an exact attempt job.
- Treated an unavailable embedding provider as a prerequisite outcome, not a zero-quality result. A checked-in provider baseline therefore remains blocked until the real provider is configured.

## Verification

- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_rag_retrieval_round_isolation.py tests/knowledge/test_facade_integration.py -q --tb=short` -> **35 passed**.
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_rag_format_parity_contract.py tests/eval/test_rag_parser_parity.py tests/eval/test_rag_retrieval_round_isolation.py tests/knowledge/test_facade_integration.py -q --tb=short` -> **81 passed**.
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/integration/test_phase64_2_integrity_matrix.py::test_old_run_resolves_original_after_reingestion -q --tb=short` -> **1 passed**, confirming exact historical replay remains intact.
- `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check` over all seven plan files -> **passed**; `ruff format --check` -> **passed**; `git diff --check` -> **passed**.
- Forbidden runtime-path scan for first-active tenant lookup, truncate, prefix delete, `--fake`, and reset-all -> **no matches**.

### PostgreSQL migration and isolation gate

An isolated `pgvector/pgvector:0.8.2-pg16` PostgreSQL instance with no persistent volume exercised the real Alembic chain and repository model:

- A direct full upgrade correctly stopped at the pre-existing Phase 64.2 migration 026 staged cutover gate when its fresh dual-write proof was absent.
- After installing the required healthy Phase 64.2 proof in that isolated database, upgrade 025 -> 029 succeeded.
- A second non-terminal row for the fixed evaluation tenant failed the partial unique constraint.
- Downgrade 029 -> 028 refused an active row. After `expire -> exact zero-current proof -> abandoned`, downgrade succeeded.
- Production sentinel state survived downgrade and re-upgrade: one document head, one immutable document version, one immutable chunk version, and both immutable-history triggers remained (`t|1|1|1|t|t`).
- Re-upgrade reached `029_phase64_3_rag_eval_rounds`; a direct owner claim produced `markdown`, state version `1`, progress `0`, and next index `1`.
- The temporary database container was stopped and removed after verification.

### Provider preflight

- On the normal host, real CLI execution returned `unavailable_prerequisite`, `baseline_eligible=false`, and exit 2 for unavailable database runtime and embedding provider.
- Against the migrated isolated database, the same CLI preflight reduced the missing prerequisites to the embedding provider only; OCR `chi_sim+eng`, schema/pgvector, tenant marker, and rollout checks passed.
- No score, quality pass/fail, fake fallback, credential, DSN, raw parser/provider payload, absolute source path, or cross-tenant fact was emitted.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected the ORM index assertion in the initial RED test**

- **Found during:** Task 1 RED
- **Issue:** The first assertion attempted to create a set of SQLAlchemy column objects instead of comparing the index column-key set, obscuring the intended schema failure.
- **Fix:** Asserted the index column keys, then confirmed RED still failed because `RagEvaluationRound` did not yet exist.
- **Files modified:** `tests/eval/test_rag_retrieval_round_isolation.py`
- **Verification:** The corrected RED suite failed at the planned missing-model boundary before GREEN implementation.
- **Committed in:** `38372df`

**2. [Rule 1 - Bug] Allowed safe same-token recovery from durable nonzero progress**

- **Found during:** Overall verification after Task 3
- **Issue:** The first runtime implementation only accepted a freshly claimed round at progress zero, so a same-token retry could not resume reservation-only, job-only, failure, or exact-complete projections even though the durable repository could classify them safely.
- **Fix:** Re-lock and re-read the durable round, retain exact identity/allowlist/state/progress denial checks, then resume the current projection through the existing classification and CAS protocol.
- **Files modified:** `src/rag/evaluation/retrieval_rounds.py`, `tests/eval/test_rag_retrieval_round_isolation.py`
- **Verification:** Added focused recovery coverage; all 35 focused and 81 phase-integration tests pass.
- **Committed in:** `16f3007`

---

**Total deviations:** 2 auto-fixed Rule 1 bugs.
**Impact on plan:** Both fixes were required to make the planned tests and same-token recovery semantics correct; neither broadened the file surface or architecture.

## Issues Encountered

- Alembic offline SQL generation is not a valid gate for this repository because pre-existing migration 016 reads database state even in offline mode. The plan was instead verified against an isolated real PostgreSQL/pgvector database.
- The real migration chain correctly required the pre-existing Phase 64.2 staged cutover proof at migration 026. The proof was installed only in the disposable isolated database; no production or shared database state was changed.
- Two shell wrappers initially used zsh special names (`status` while capturing the CLI exit code and `path` during the first self-check), causing wrapper-only errors. The CLI had already returned the correct safe payload; reruns with neutral variable names confirmed exit 2 and all self-check items without wrapper noise.
- Five non-failing warnings remain in the focused facade suite: one LangChain pending-deprecation warning and four pre-existing unawaited `AsyncMock` warnings from `src/memory/session_bundle.py:77`. They are outside this plan's file surface.

## Known Stubs

None. The CLI's empty dataset projection is an intentional safe diagnostic used only when manifest/Gold prerequisites cannot be loaded; it is baseline-ineligible and cannot reach provider scoring.

## User Setup Required

Configure the project's real embedding provider before Plan 04 can record a provider baseline. Until then the CLI intentionally returns `unavailable_prerequisite` with exit 2.

## Next Phase Readiness

- Plan 04 can consume `run_retrieval_parity(...)` and its typed round observations once the real embedding provider is available.
- The fixed evaluation tenant, migration 029, ownership/cleanup protocol, and production service adapter are implemented and locally verified.
- Phase closeout must remain blocked from claiming a provider baseline while the embedding provider prerequisite is unavailable.

## Self-Check: PASSED

- All seven plan files exist and are covered by the expected RED/GREEN commit sequence.
- Commits `38372df`, `15f1d48`, `3c5f159`, `496d975`, `91d9621`, `49c3295`, and `16f3007` exist in order.
- No blocking stubs or unplanned security surface were found; all new trust-boundary surfaces correspond to threats T64.3-04 through T64.3-09 in the plan.

---
*Phase: 64.3-rag-format-parity-and-document-quality-evaluation*
*Completed: 2026-08-10*
