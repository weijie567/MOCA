---
status: complete
phase: 64.3-rag-format-parity-and-document-quality-evaluation
source:
  - 64.3-01-SUMMARY.md
  - 64.3-02-SUMMARY.md
  - 64.3-03-SUMMARY.md
  - 64.3-04-SUMMARY.md
  - 64.3-05-SUMMARY.md
started: 2026-08-10T16:24:00Z
updated: 2026-08-10T16:30:15Z
mode: autopilot-self-detected
---

# Phase 64.3 UAT

## Current Test

[testing complete]

## Tests

### 1. Deterministic contract gate

expected: Maintainer runs the documented contract target through the repository `uv` environment and all fixture, parser, isolation, recovery, and report contracts pass.
result: pass
evidence: `make eval-rag-format-parity-contract` completed with 143 passed and 1 dependency warning.

### 2. Direct production-parser evaluation

expected: Maintainer runs the parser target without DB, embedder, or retrieval; it evaluates exactly nine fixtures and writes a truthful parser-direct quality result.
result: pass
evidence: `make eval-rag-format-parity-parser` wrote `rag_parser_parity.json` with `mode=parser_direct`, nine `variant_results`, and `outcome=completed_quality_fail`. The target returned 1 because the completed quality gate is red; the generated diagnostic artifact was inspected and then removed.

### 3. Provider prerequisite fail-closed behavior

expected: The provider target refuses to mutate anything when the required run token or rollout version is absent.
result: pass
evidence: With both variables unset, `make eval-rag-format-parity-provider` exited 2 and reported `RAG_FORMAT_PARITY_RUN_TOKEN is required` before provider or database execution.

### 4. Sealed canonical provider baseline

expected: The checked-in real-provider JSON strictly loads as an eligible completed outcome, owns the exact case/failure set, and renders byte-identically to Markdown.
result: pass
evidence: Strict loading proved `completed_quality_fail`, `baseline_eligible=true`, `execution_kind=full_provider`, 54 cases, 45 failures, six parser gate inputs, and byte-identical JSON-to-Markdown projection. The fresh run used token `64f30400-0000-4000-8000-000000000008` and all three durable rows shared the independently recomputed run identity seal.

### 5. Isolation and no production behavior drift

expected: The evaluation leaves no mutable current projection or provider process/container, preserves immutable history, and does not modify forbidden production RAG owners.
result: pass
evidence: The sealed run proved current blocks/chunks/jobs `0/0/0`, immutable documents/chunks `9/53`, all three rounds terminal, and no diagnostic/process/container residual. `git diff 8392b0b --` was empty for production parser, chunker, embedder, retrieval/service, ContextBuilder, verifier, claim verifier, and legacy Gold paths.

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none]
