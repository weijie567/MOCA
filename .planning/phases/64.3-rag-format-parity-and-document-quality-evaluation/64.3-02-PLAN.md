---
phase: 64.3-rag-format-parity-and-document-quality-evaluation
plan: "02"
name: Direct Parser Parity Evaluator
type: execute
wave: 2
depends_on: [64.3-01]
files_modified:
  - src/rag/evaluation/parser_parity.py
  - scripts/eval_rag_parser_parity.py
  - tests/eval/test_rag_parser_parity.py
autonomous: true
requirements: [ROADMAP-SC-2]
must_haves:
  truths:
    - "A maintainer can evaluate all nine validated fixtures by calling the production ParserRegistry DTO boundary directly, without a tenant, database, embedder, ingestion service, or retrieval service."
    - "Parse status, semantic anchors, headings/structure, tables, provenance/locators, PDF page coverage, scanned-PDF OCR diagnostics, and warnings/failures are scored independently rather than hidden behind one aggregate."
    - "Comparison normalization never mutates fixtures or ParseResult/ParsedBlock values, and protected numbers, dates, percentages, negations, policy IDs, table headers, and critical rows remain exact."
    - "Every miss has stable, safely bounded attribution and deterministic ordering without raw parser/OCR payloads, absolute paths, bytes, exceptions, or stack traces."
  artifacts:
    - {path: src/rag/evaluation/parser_parity.py, provides: "Pure comparison/scoring plus direct ParserRegistry orchestration", contains: "evaluate_parser_parity"}
    - {path: scripts/eval_rag_parser_parity.py, provides: "Persistence-free all-nine-fixture parser CLI", contains: "--output"}
    - {path: tests/eval/test_rag_parser_parity.py, provides: "Boundary, scoring, normalization, safety, and determinism coverage", contains: "test_parser_mode_has_no_persistence_or_retrieval_dependency"}
  key_links:
    - {from: src/rag/evaluation/parser_parity.py, to: src/rag/parsers/registry.py, via: "ParserRegistry.parse(path, doc_key=..., source_type=..., metadata=...)", pattern: "parser_registry.parse"}
    - {from: src/rag/evaluation/parser_parity.py, to: src/rag/evaluation/contracts.py, via: "validated FormatParityDataset and shared EvaluationOutcome", pattern: "FormatParityDataset"}
    - {from: scripts/eval_rag_parser_parity.py, to: src/rag/evaluation/parser_parity.py, via: "thin CLI adapter", pattern: "evaluate_parser_parity"}
---

<objective>
Build the direct production-parser format-parity evaluator and its persistence-free executable entry point.

Purpose: Measure parser and OCR fidelity independently so later retrieval failures can be attributed without conflating tenant, persistence, embeddings, chunking, or ranking.
Output: Deterministic parser scoring, safe per-case diagnostics, an all-nine-fixture CLI, and focused contract tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-CONTEXT.md
@.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-RESEARCH.md
@.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-VALIDATION.md
@.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-PATTERNS.md
@src/rag/parsers/base.py
@src/rag/parsers/registry.py
@src/rag/ingestion.py
@tests/rag/test_parser_contract.py

<interfaces>
Consume the Plan 01 contract without creating a second loader:
```python
dataset = load_format_parity_contract(manifest_path, gold_path, repository_root=repository_root)
```

Call the production parser with the exact ingestion-owned shape:
```python
parse_result = parser_registry.parse(
    fixture.path,
    doc_key=fixture.doc_key,
    source_type=fixture.source_type,
    metadata=fixture.metadata,
)
```

Export one stable intermediate result for Plan 04:
```python
def evaluate_parser_parity(
    dataset: FormatParityDataset,
    *,
    parser_registry: ParserRegistry | None = None,
    generated_at: str,
) -> ParserParityRunV1: ...
```

`ParsedBlock` already owns `block_type`, `text`, `normalized_text`, `parser_name`, `parser_version`, `page_number`, `box`, `table_metadata`, `ocr_metadata`, and `warnings`; `ParseResult` owns `status`, blocks, warnings, `failure_code`, and `safe_message`. Score these DTOs without modifying them.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement independent parser dimensions and safe deterministic attribution</name>
  <read_first>@src/rag/parsers/base.py @src/rag/ingestion.py @src/rag/evaluation/contracts.py @evaluation/golden/rag_format_parity_gold.json @tests/rag/test_parser_contract.py @.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-CONTEXT.md</read_first>
  <files>src/rag/evaluation/parser_parity.py, tests/eval/test_rag_parser_parity.py</files>
  <action>Per D-06 through D-08, write focused tests first, then implement frozen result records and pure scoring helpers for: parse status; semantic-anchor recall; heading/expected-section preservation; critical table/header/row preservation; locator/provenance coverage; PDF page coverage; scanned-PDF OCR diagnostics; and warning/failure codes. Emit anchor/structure/locator observations before aggregates, retaining `policy_id`, `variant`, `case_id`, `anchor_id`, expected locator summary, matched/missed status, exactly one primary stage (`parser`, `ocr`, `provenance` at this layer), and stable reason codes. Implement evaluator-owned comparison normalization that only collapses enumerated whitespace/layout artifacts on copied strings; protected anchor matching must preserve exact numbers, dates, percentages, negations, policy IDs, headers, and critical rows. Safe snippets are UTF-8 text capped at 160 characters after control-character/path/payload redaction. Sort results by policy ID, variant order `markdown,digital_pdf,scanned_pdf`, case ID, anchor ID, and reason code; inject `generated_at`. Project only an allowlist of scalar/count/locator fields modeled on `build_safe_ingestion_report`; do not serialize arbitrary DTO dictionaries, Path, bytes, exceptions, stack traces, raw OCR metadata, parser dumps, or local paths. Per D-07, never write fixture bytes, `ParsedBlock.text`, `ParsedBlock.normalized_text`, or canonical evidence text.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_rag_parser_parity.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/rag/evaluation/parser_parity.py tests/eval/test_rag_parser_parity.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/rag/evaluation/parser_parity.py` exposes separate fields/results for every D-06 dimension; no single aggregate can erase a dimension failure.
    - Tests independently fail and pass anchor, heading, table, locator/page, OCR, and warning/failure cases using real `ParseResult`/`ParsedBlock` DTO shapes.
    - Tests prove comparison normalization leaves protected numeric/date/percentage/negation/policy/table anchors exact and does not mutate input DTOs.
    - Serialized diagnostics contain no absolute path, bytes, traceback, exception text, parser dump, raw OCR/provider payload, or arbitrary metadata key.
    - Two runs over identical DTOs and injected clocks produce byte-equivalent ordered result dictionaries.
  </acceptance_criteria>
  <done>Parser/OCR/structure/provenance scoring is auditable, deterministic, safe, and independent of production data mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run all nine fixtures through the production ParserRegistry boundary</name>
  <read_first>@src/rag/parsers/registry.py @src/rag/parsers/base.py @src/rag/ingestion.py @src/rag/evaluation/contracts.py @src/rag/evaluation/parser_parity.py @evaluation/rag_sources/format_parity_manifest.jsonl @evaluation/golden/rag_format_parity_gold.json @scripts/eval_rag.py</read_first>
  <files>src/rag/evaluation/parser_parity.py, scripts/eval_rag_parser_parity.py, tests/eval/test_rag_parser_parity.py</files>
  <action>Per D-05, implement `evaluate_parser_parity(...)` to iterate the already validated 3-policy/9-variant dataset and invoke `ParserRegistry.parse(path, doc_key=..., source_type=..., metadata=...)` exactly as production ingestion does. Filter only empty blocks exactly as ingestion does before scoring, but do not copy ingestion job, chunking, embedding, repository, tenant, transaction, or retrieval setup. Create `scripts/eval_rag_parser_parity.py` as a thin CLI with explicit `--manifest`, `--gold`, `--output`, and injectable `--generated-at`; it validates the Plan 01 contract before parsing, writes sorted `parser_parity_run.v1` JSON, and records schema version, command/mode `parser_direct`, all input hashes, parser/OCR versions, per-variant dimensions, safe failures, prerequisites, and shared `EvaluationOutcome`. It must attempt all nine variants unless contract failure makes evaluation untrustworthy. Missing OCR executable/traineddata is `unavailable_prerequisite`; when the OCR runtime is present but returns empty, garbled, or zero-anchor-recall output, execution completed and the result is `completed_quality_fail` with primary stage `ocr`, never unavailable. Parser invariant failure is `execution_error`, and other completed metric misses are `completed_quality_fail`; none may be converted into a passing fake run. The module and CLI must not import `src.db`, SQLAlchemy, `EmbeddingService`, `IngestionService`, `PolicyKnowledgeService`, or retrieval modules. Deterministic DTO/registry doubles are test-only and every such result is labeled `contract_test`, never provider or checked-in baseline.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_rag_parser_parity.py tests/eval/test_rag_format_parity_contract.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/rag/evaluation/parser_parity.py scripts/eval_rag_parser_parity.py tests/eval/test_rag_parser_parity.py</automated>
  </verify>
  <acceptance_criteria>
    - A registry spy observes exactly 9 calls with the production keyword signature and the exact three doc keys/three allowed variant pairs.
    - Importing `src.rag.evaluation.parser_parity` and `scripts.eval_rag_parser_parity` constructs no tenant, DB session, embedder, ingestion service, knowledge service, or retriever; static/import tests reject those dependencies.
    - Contract invalidity stops before the first parser call; one fixture parse failure is recorded safely without skipping the other validated fixtures.
    - OCR absence yields `unavailable_prerequisite`, a completed below-target run yields `completed_quality_fail`, and deterministic doubles cannot emit a provider-baseline mode.
    - Available OCR returning empty/garbled output or zero anchor recall is explicitly tested as `completed_quality_fail` with primary stage `ocr`, not `unavailable_prerequisite`.
    - The CLI JSON records Plan 01 manifest/Gold/fixture hashes and stable per-policy/per-format/per-case results without changing any production parser/chunker/retrieval file.
  </acceptance_criteria>
  <done>The direct parser evaluator executes the production DTO path for the exact nine fixtures with no persistence or retrieval dependency.</done>
</task>

</tasks>

<threat_model>
Security posture: ASVS L1; execution blocks on any unresolved high-severity threat.

| Threat ID | STRIDE | Boundary | Disposition | Mitigation |
|---|---|---|---|---|
| T64.3-03 | Tampering | comparison normalization -> semantic result | mitigate | Normalize copies only; protect exact identifiers, values, negations, headers, and rows with tests. |
| T64.3-09 | Information Disclosure | parser/OCR DTO -> evaluator report | mitigate | Strict allowlist, 160-character sanitized snippets, safe reason codes, and forbidden-key/path/payload serialization tests. |
| T64.3-10 | Spoofing | deterministic double -> claimed baseline | mitigate | Result mode records execution kind; contract doubles cannot produce parser/provider baseline status. |
| T64.3-11 | Denial of Service | malformed/oversized fixture -> direct parser runner | accept | Production parser limits/timeouts remain authoritative; evaluator records safe failure and continues the bounded nine-item set. |
</threat_model>

<validation_mapping>
| Validation ID | Coverage | Automated gate |
|---|---|---|
| 64.3-02-01 | D-05 direct production call shape with no tenant/DB/embedder/retrieval dependency | `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_rag_parser_parity.py -q --tb=short` |
| 64.3-02-02 | D-06..D-08 independent scoring, protected normalization, safe attribution, and stable ordering | Same focused parser suite |
</validation_mapping>

<verification>
Run both task commands. Confirm the parser module/CLI import graph excludes persistence, embedding, ingestion, knowledge service, and retrieval modules. The deterministic suite may use DTO/registry doubles but must never write or claim the provider/OCR-backed baseline. Bare `pytest` and bare `python -m pytest` are invalid in MOCA.
</verification>

<success_criteria>
ROADMAP success criterion 2 is satisfied: the exact nine fixtures can be scored through the production parser boundary without tenant/database/embedding/retrieval dependencies, with independently auditable fidelity, structure, table, provenance, page, OCR, and diagnostic results.
</success_criteria>

<output>
After completion, create `.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-02-SUMMARY.md`.
</output>
