---
phase: 64.3-rag-format-parity-and-document-quality-evaluation
plan: "01"
name: Contract And Semantic Gold
type: execute
wave: 1
depends_on: []
files_modified:
  - evaluation/rag_sources/format_parity_manifest.jsonl
  - evaluation/rag_sources/README.md
  - evaluation/rag_sources/fixtures/refund_eligibility_and_return/refund_eligibility_and_return.md
  - evaluation/rag_sources/fixtures/refund_eligibility_and_return/refund_eligibility_and_return.digital.pdf
  - evaluation/rag_sources/fixtures/refund_eligibility_and_return/refund_eligibility_and_return.scanned.pdf
  - evaluation/rag_sources/fixtures/quality_compensation_and_approval/quality_compensation_and_approval.md
  - evaluation/rag_sources/fixtures/quality_compensation_and_approval/quality_compensation_and_approval.digital.pdf
  - evaluation/rag_sources/fixtures/quality_compensation_and_approval/quality_compensation_and_approval.scanned.pdf
  - evaluation/rag_sources/fixtures/cross_border_and_digital_goods/cross_border_and_digital_goods.md
  - evaluation/rag_sources/fixtures/cross_border_and_digital_goods/cross_border_and_digital_goods.digital.pdf
  - evaluation/rag_sources/fixtures/cross_border_and_digital_goods/cross_border_and_digital_goods.scanned.pdf
  - evaluation/golden/rag_format_parity_gold.json
  - src/rag/evaluation/__init__.py
  - src/rag/evaluation/contracts.py
  - tests/eval/test_rag_format_parity_contract.py
autonomous: true
requirements: [ROADMAP-SC-1]
must_haves:
  truths:
    - "Evaluation cannot start unless the physical fixture contract contains exactly three canonical policy groups and exactly nine allowed Markdown/digital-PDF/scanned-PDF variants whose paths, metadata, lineage, and bytes validate."
    - "The three stale Markdown hashes are not merely refreshed: repository history proves the current checked-in Markdown is canonical, and the complete Markdown/PDF/manifest family is regenerated, reviewed, and reconciled atomically."
    - "Each policy has one format-independent semantic truth shared by all three variants, with stable fact/anchor IDs and no generated chunk-ID binding or per-format answer duplication."
    - "Manifest bytes, Gold bytes, and every fixture's bytes have independent SHA-256 identities that downstream parser, retrieval, and report plans can consume."
  artifacts:
    - {path: src/rag/evaluation/contracts.py, provides: "Strict format-parity manifest, semantic Gold, hash, and execution-outcome contracts", contains: "load_format_parity_contract"}
    - {path: evaluation/golden/rag_format_parity_gold.json, provides: "Single-source semantic Gold for three canonical policies", contains: "rag_format_parity_gold.v1"}
    - {path: tests/eval/test_rag_format_parity_contract.py, provides: "Exact 3/9, lineage, hash, Gold-schema, and prohibited chunk-ID tests", contains: "test_checked_in_format_parity_contract_is_valid"}
  key_links:
    - {from: src/rag/evaluation/contracts.py, to: evaluation/rag_sources/format_parity_manifest.jsonl, via: "strict repository-root-relative loader and byte hashing", pattern: "format_parity_manifest"}
    - {from: src/rag/evaluation/contracts.py, to: evaluation/golden/rag_format_parity_gold.json, via: "policy_id and stable semantic case/anchor references", pattern: "rag_format_parity_gold"}
    - {from: evaluation/rag_sources/format_parity_manifest.jsonl, to: evaluation/rag_sources/fixtures, via: "exact relative paths and SHA-256 values", pattern: "sha256"}
---

<objective>
Establish the trusted physical-fixture and shared semantic-Gold contract for the format-parity baseline.

Purpose: Prevent stale or ambiguous fixture lineage, format-specific truth drift, and chunk-bound expectations from contaminating every downstream parser and retrieval result.
Output: A reconciled three-policy/nine-variant fixture family, strict versioned loader, semantic Gold, independent content hashes, and deterministic contract tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-CONTEXT.md
@.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-RESEARCH.md
@.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-VALIDATION.md
@.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-PATTERNS.md
@docs/quality/rag-quality-plan.md
@evaluation/rag_sources/README.md
@evaluation/rag_sources/build_fixtures.py
@evaluation/rag_sources/format_parity_manifest.jsonl

<interfaces>
The manifest authority is the exact fixed key set:
```python
FORMAT_PARITY_DOC_KEYS = (
    "eval_refund_eligibility_and_return",
    "eval_quality_compensation_and_approval",
    "eval_cross_border_and_digital_goods",
)
FORMAT_VARIANTS = (
    ("markdown", "policy_markdown"),
    ("digital_pdf", "policy_pdf"),
    ("scanned_pdf", "policy_pdf"),
)
```

Plan 01 must export stable downstream contracts with this functional surface:
```python
def load_format_parity_contract(
    manifest_path: Path,
    gold_path: Path,
    *,
    repository_root: Path,
) -> FormatParityDataset: ...

class FormatParityDataset:
    manifest_hash: str
    gold_hash: str
    fixture_hashes: dict[str, str]
    policies: tuple[FormatParityPolicy, ...]
```

The shared execution outcome vocabulary is exactly `completed_pass`, `completed_quality_fail`, `unavailable_prerequisite`, and `execution_error`; later plans consume it rather than defining local aliases.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install the fail-closed 3/9 contract and expose the current lineage mismatch</name>
  <read_first>@evaluation/rag_sources/README.md @evaluation/rag_sources/build_fixtures.py @evaluation/rag_sources/format_parity_manifest.jsonl @src/knowledge/service.py @tests/test_rag_eval.py @.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-CONTEXT.md</read_first>
  <files>src/rag/evaluation/__init__.py, src/rag/evaluation/contracts.py, tests/eval/test_rag_format_parity_contract.py</files>
  <action>Per D-01 and D-04, create frozen Pydantic contracts with `ConfigDict(extra="forbid", frozen=True)` and a strict loader that accepts only the three `FORMAT_PARITY_DOC_KEYS`, exactly one variant for each `FORMAT_VARIANTS` tuple per group, `parity_group == doc_key`, the Markdown path as `source_of_truth`, repository-root-relative non-escaping paths, existing regular files, Markdown `pages=null`, both PDFs with positive page counts, digital PDFs with positive `extractable_text_chars`, scanned PDFs with `extractable_text_chars=0`, and exact streamed SHA-256 matches. Reject absolute paths, `..` escapes, symlinks escaping the fixture root, duplicate/missing/extra groups or variants, unsupported source-type/format pairs, malformed hashes, and bad metadata before returning any evaluator input. Define `EvaluationOutcome`, semantic Gold envelope models, and `FormatParityDataset`/hash fields now so Plans 02-04 have one interface owner. Add tests that prove tampered temporary fixtures fail closed and that the checked-in manifest is currently rejected for the three known Markdown mismatches: manifest `b59685.../e7fb86.../c4bd19...` versus current bytes `81654b.../f7c115.../864182...`; also prove all six current PDF hashes match. Do not edit manifest or fixture bytes in this task, and do not add a bypass, warning-only mode, or hash-refresh helper.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_rag_format_parity_contract.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/rag/evaluation/__init__.py src/rag/evaluation/contracts.py tests/eval/test_rag_format_parity_contract.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/rag/evaluation/contracts.py` contains `FORMAT_PARITY_DOC_KEYS`, `FORMAT_VARIANTS`, `EvaluationOutcome`, `FormatParityDataset`, and `load_format_parity_contract`.
    - `tests/eval/test_rag_format_parity_contract.py` asserts exactly 3 groups/9 variants and covers duplicate, missing, extra, path-escape, metadata, source-linkage, and checksum rejection.
    - The focused test records the three actual Markdown mismatches and six matching PDFs without modifying any manifest or fixture byte.
    - No loader option permits checksum, exact-count, source-of-truth, or path validation to be skipped.
  </acceptance_criteria>
  <done>The deterministic contract owner exists and turns the current lineage inconsistency into an explicit fail-closed gate.</done>
</task>

<task type="auto">
  <name>Task 2: Record the resolved canonical fixture lineage</name>
  <read_first>@evaluation/rag_sources/README.md @evaluation/rag_sources/build_fixtures.py @evaluation/rag_sources/format_parity_manifest.jsonl @evaluation/rag_sources/fixtures/refund_eligibility_and_return/refund_eligibility_and_return.md @evaluation/rag_sources/fixtures/quality_compensation_and_approval/quality_compensation_and_approval.md @evaluation/rag_sources/fixtures/cross_border_and_digital_goods/cross_border_and_digital_goods.md</read_first>
  <files>evaluation/rag_sources/README.md</files>
  <action>Record the already-resolved lineage in the README reconciliation section before fixture mutation: Markdown is the declared source of truth; repository history contains only fixture introduction commit `660a571`; no prior Markdown revision matches the stale manifest hashes; therefore the current checked-in Markdown is canonical and all six PDFs plus the complete manifest must be regenerated/reviewed as one family. Include the Task 1 mismatch evidence and the exact history commands/results. Per D-01/D-04, do not change fixture bytes or hashes in this task and never represent a three-hash refresh as reconciliation.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_rag_format_parity_contract.py -q --tb=short &amp;&amp; rg -n '660a571' evaluation/rag_sources/README.md &amp;&amp; rg -n -i 'current.*markdown.*canonical|markdown.*source of truth' evaluation/rag_sources/README.md &amp;&amp; rg -n -i 'hash-only|hash only' evaluation/rag_sources/README.md</automated>
  </verify>
  <acceptance_criteria>
    - README identifies current Markdown as canonical and cites commit `660a571`, the absence of a matching prior revision, and the three stale/current hash pairs.
    - The recorded route covers Markdown, both PDF variants, manifest metadata, and all hashes as one fixture family.
    - A hash-only refresh is explicitly rejected and cannot satisfy the task.
  </acceptance_criteria>
  <done>The repository-proven canonical lineage and atomic reconciliation route are recorded before regeneration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reconcile the fixture family and author the shared semantic Gold</name>
  <read_first>@evaluation/rag_sources/README.md @evaluation/rag_sources/build_fixtures.py @evaluation/rag_sources/format_parity_manifest.jsonl @docs/quality/rag-quality-plan.md @evaluation/golden/rag_cases.jsonl @src/rag/evaluation/contracts.py @tests/eval/test_rag_format_parity_contract.py</read_first>
  <files>evaluation/rag_sources/README.md, evaluation/rag_sources/format_parity_manifest.jsonl, evaluation/rag_sources/fixtures/refund_eligibility_and_return/refund_eligibility_and_return.md, evaluation/rag_sources/fixtures/refund_eligibility_and_return/refund_eligibility_and_return.digital.pdf, evaluation/rag_sources/fixtures/refund_eligibility_and_return/refund_eligibility_and_return.scanned.pdf, evaluation/rag_sources/fixtures/quality_compensation_and_approval/quality_compensation_and_approval.md, evaluation/rag_sources/fixtures/quality_compensation_and_approval/quality_compensation_and_approval.digital.pdf, evaluation/rag_sources/fixtures/quality_compensation_and_approval/quality_compensation_and_approval.scanned.pdf, evaluation/rag_sources/fixtures/cross_border_and_digital_goods/cross_border_and_digital_goods.md, evaluation/rag_sources/fixtures/cross_border_and_digital_goods/cross_border_and_digital_goods.digital.pdf, evaluation/rag_sources/fixtures/cross_border_and_digital_goods/cross_border_and_digital_goods.scanned.pdf, evaluation/golden/rag_format_parity_gold.json, src/rag/evaluation/contracts.py, tests/eval/test_rag_format_parity_contract.py</files>
  <action>Apply the resolved current-Markdown route atomically: run `UV_CACHE_DIR=/tmp/uv-cache uv run python evaluation/rag_sources/build_fixtures.py`, review all six generated PDFs for five-page content/ordering/table equivalence, and commit the complete regenerated family plus manifest. Document canonical revision `660a571`, the absence of an alternative matching Markdown revision, generator command/version, reviewed page counts/text-layer expectations, and final hashes in `evaluation/rag_sources/README.md`. Then, per D-02/D-03, create `rag_format_parity_gold.v1` with exactly the three policy IDs; each policy has 8-12 stable semantic anchors and 6-8 stable retrieval cases covering fact, exception, amount-or-time-limit, table, cross-section, and no-answer categories. Each case binds `case_id`, `policy_id`, category, question, expected section, protected anchors, optional table/header/row anchors, optional additive PDF/OCR locator constraints, and explicit no-answer behavior. Reject `expected_chunk_ids`, generated IDs, per-format copied answers, and unprotected changes to numbers, dates, percentages, negations, policy IDs, table headers, or critical rows. Compute manifest, Gold, and each fixture hash independently and expose them through `FormatParityDataset`; baseline identity must derive from schema/config plus these hashes, never a timestamp or output filename. Replace the temporary checked-in-mismatch assertion with `test_checked_in_format_parity_contract_is_valid` and add schema/category/stable-ID/chunk-ID-prohibition/hash-change tests.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_rag_format_parity_contract.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/rag/evaluation/__init__.py src/rag/evaluation/contracts.py tests/eval/test_rag_format_parity_contract.py</automated>
  </verify>
  <acceptance_criteria>
    - `load_format_parity_contract(...)` accepts the checked-in family and returns exactly 3 policies, 9 variants, 3 independent policy Gold entries, a manifest hash, a Gold hash, and 9 fixture hashes.
    - `evaluation/rag_sources/README.md` contains the approved lineage evidence and complete reconciliation route; the diff is not limited to three manifest SHA strings.
    - Every policy has 8-12 anchors and 6-8 cases; the suite covers all six required categories with stable unique IDs and shared format-independent truth.
    - `rg -n 'expected_chunk_ids|expected_chunk_id' evaluation/golden/rag_format_parity_gold.json` returns no matches.
    - Mutating fixture, manifest, or Gold bytes changes the corresponding independent hash and prevents reuse of the prior baseline identity.
  </acceptance_criteria>
  <done>The reconciled 3/9 physical contract and one semantic Gold source are valid, hash-owned, and ready for both evaluator paths.</done>
</task>

</tasks>

<threat_model>
Security posture: ASVS L1; execution blocks on any unresolved high-severity threat.

| Threat ID | STRIDE | Boundary | Disposition | Mitigation |
|---|---|---|---|---|
| T64.3-01 | Tampering | checked-in manifest/Gold -> evaluator inputs | mitigate | Exact schema, independent SHA-256 checks, fixed 3/9 cardinality, and fail-closed load before evaluation. |
| T64.3-02 | Spoofing | manifest path/source linkage -> fixture bytes | mitigate | Repository-root-relative canonicalization, fixed doc keys/variant pairs, source-of-truth equality, and escaping/symlink rejection. |
| T64.3-03 | Repudiation | lineage resolution -> regenerated fixture family | mitigate | Repository-history proof plus README record of canonical revision, evidence, generator command, page review, and final hashes. |
| T64.3-04 | Information Disclosure | invalid fixture/parser metadata -> contract error | mitigate | Stable reason codes and bounded identifiers only; never emit source bytes, absolute paths, raw payloads, stack traces, or credentials. |
</threat_model>

<validation_mapping>
| Validation ID | Coverage | Automated gate |
|---|---|---|
| 64.3-01-01 | D-01/D-04 exact manifest, lineage, and hash contract | `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_rag_format_parity_contract.py -q --tb=short` |
| 64.3-01-02 | D-01/D-04 README record of repository-proven current-Markdown lineage and hash-only prohibition | Focused contract suite plus deterministic README `rg` evidence command from Task 2 |
| 64.3-01-03 | D-02/D-03 semantic Gold shape, cases, anchors, and no chunk-ID binding | Same focused contract suite |
</validation_mapping>

<verification>
Run the focused pytest and Ruff commands after Tasks 1 and 3. Task 2 records the repository-proven current-Markdown lineage before mutation; it is not permission to weaken the contract. Confirm the final diff contains the complete regenerated fixture family, never only three edited hash strings. Bare `pytest` and bare `python -m pytest` are invalid in MOCA.
</verification>

<success_criteria>
ROADMAP success criterion 1 is satisfied: exactly three canonical policy groups and nine validated format variants share one stable semantic Gold, all bytes and contract artifacts are independently hash-owned, and lineage ambiguity cannot pass the preflight.
</success_criteria>

<output>
After completion, create `.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-01-SUMMARY.md`.
</output>
