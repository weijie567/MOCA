---
phase: 64.3-rag-format-parity-and-document-quality-evaluation
fixed_at: 2026-08-10T15:56:17Z
review_path: .planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-REVIEW.md
iteration: 2
findings_in_scope: 4
fixed: 4
skipped: 0
status: all_fixed
---

# Phase 64.3: Code Review Fix Report

**Fixed at:** 2026-08-10T15:56:17Z
**Source review:** `.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-REVIEW.md`
**Iteration:** 2

**Summary:**

- Findings in scope: 4
- Fixed: 4
- Skipped: 0
- Iteration 2 atomic commits: 3 (WR-04 and WR-08 share one strongly coupled recovery-identity commit)
- Cumulative findings fixed: 11 across 2 iterations and 8 atomic commits

## Fixed Issues

### WR-01: Gold anchors are not bound to their recorded source-block locators

**Files modified:** `src/rag/evaluation/retrieval_rounds.py`, `src/repositories/rag_evaluation_round_repo.py`, `tests/eval/test_rag_retrieval_round_isolation.py`
**Commit:** `89a90c8`
**Status:** fixed: requires human verification
**Applied fix:** The evaluation-only repository now reads the exact current chunk, source refs, and referenced `DocumentBlock` rows inside the same short retrieval transaction. It verifies `hit.text` against `EvidenceRef.text_hash` using `evidence_text_hash.v1`, verifies current chunk and source-block hashes, binds each anchor to an exact source-block locator and allowed page, and requires anchors from distinct Gold sections to have a satisfiable distinct-section block assignment. Raw block text never leaves the repository proof function or enters a serialized DTO.
**Verification:** Added regressions for the two-section/one-locator counterexample, exact two-block success, allowed page binding, and hit/ref hash drift.

### WR-03: Strict loading trusts a relabeled case despite contradictory raw signals

**Files modified:** `src/rag/evaluation/reporting.py`, `tests/eval/test_rag_format_parity_report.py`
**Commit:** `01429a7`
**Status:** fixed: requires human verification
**Applied fix:** `CaseResultRowV1` now persists safe typed parser stage/reason inputs. The model validator and `_case_row()` share one classification function that derives `passed`, `primary_stage`, and `reason_codes` with the same parser → no-answer → retrieval → chunking → provenance → fallback priority. Strict loading also rejects contradictory no-answer flags, fallback values, and anchor counts.
**Verification:** Added a canonical tamper regression that relabels a persisted semantic-anchor miss as passed and removes its matching failure; strict loading now rejects it.

### WR-04: A crash after all terminal rounds cannot resume canonical report persistence

**Files modified:** `scripts/eval_rag_format_parity.py`, `src/rag/evaluation/retrieval_rounds.py`, `src/repositories/rag_evaluation_round_repo.py`, `src/db/models.py`, `src/db/migrations/versions/029_phase64_3_rag_eval_rounds.py`, `tests/eval/test_rag_retrieval_round_isolation.py`
**Commit:** `638b5b4`
**Status:** fixed: requires human verification
**Applied fix:** Claim/resume now returns a typed outcome representing either an active owner or an already-completed three-round sequence. A completed sequence is revalidated and reconstructed as `RetrievalParityRunV1`, allowing the full-provider caller to build and atomically persist the canonical report without new ingestion, retrieval, cleanup, or round creation.
**Verification:** Added a post-final-terminal/pre-report crash-window regression that reconstructs all three ordered terminal rounds and proves no new round mutation occurs; mismatched reconstruction identity fails closed.

### WR-08: Same-token recovery is not sealed to one input, config, and effective-time identity

**Files modified:** `scripts/eval_rag_format_parity.py`, `src/db/models.py`, `src/db/migrations/versions/029_phase64_3_rag_eval_rounds.py`, `src/repositories/rag_evaluation_round_repo.py`, `src/rag/evaluation/retrieval_rounds.py`, `tests/eval/test_rag_retrieval_round_isolation.py`
**Commit:** `638b5b4`
**Status:** fixed: requires human verification
**Applied fix:** Every evaluation round now persists a non-null lowercase 64-hex `run_identity_hash`. The allowlisted hash covers manifest, Gold, logical fixture checksums, generator identity, dataset baseline identity, generated/effective time, execution mode, embedding provider/model/dimensions, retrieval/RRF/rewrite/rerank/no-evidence configuration, fixed tenant/owner, and rollout version. Sequence validation, owner locking, CAS, round creation, advanced-document skipping, each provider search, and completed-run reconstruction compare the identity. Skipped documents additionally compare the current variant checksum to the recorded structured projection and re-prove that projection before synthesizing a success observation.
**Verification:** Added Gold, fixture, generated-at, mode, rollout, and provider-config hash drift coverage; same-token drift is rejected before create/resume mutation; document checksum drift is rejected before structured proof; all three rounds inherit one hash.

## Verification Summary

- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_rag_format_parity_contract.py tests/eval/test_rag_parser_parity.py tests/eval/test_rag_retrieval_round_isolation.py tests/eval/test_rag_format_parity_report.py -q --tb=short` → **143 passed, 1 warning**.
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_rag_eval.py tests/test_rag_ablation_eval.py tests/eval tests/rag tests/knowledge -q --tb=short` → **442 passed, 8 warnings**.
- Scoped `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check ...` → **All checks passed**.
- `git diff --check fd0f15c..HEAD` → **passed**.
- Diff from iteration-2 review HEAD `fd0f15c` touches only the evaluator CLI, evaluation reporting/retrieval owners, evaluation repository, round ORM/migration, and evaluation tests.
- Forbidden-owner scan found no changes to production parser/chunker/embedder, knowledge retrieval/service, ContextBuilder, verifier, or legacy Gold.

## Required Fresh Baseline Rerun

The checked-in canonical baseline was not edited. Its case rows do not contain the new required `parser_primary_stage` and `parser_reason_codes` fields, so it is intentionally stale under the iteration-2 strict loader. A fresh database is also required because unpublished migration `029` now adds the non-null `run_identity_hash` column and constraint.

Regenerate `baseline.json` and `baseline.md` only through a real fresh `full-provider`/OCR execution after these commits, using the fixed evaluation tenant, a fresh run token, and one explicit stable `--generated-at` value. Do not hand-migrate or synthesize the canonical files. The rerun is also required to measure WR-01's stricter exact source-block locator proof.

## Cumulative Commit Evidence

### Iteration 1

- `8105b3e` — WR-01 recorded locator proof
- `175149e` — WR-02 case-specific PDF pages
- `dc26464` — WR-03 contradictory canonical report rejection
- `ee9f367` — WR-04/WR-05/WR-06 round isolation and projection proof
- `3299e66` — WR-07 invalid contract classification

### Iteration 2

- `89a90c8` — WR-01 exact source-block anchor binding
- `01429a7` — WR-03 raw-signal case reclassification
- `638b5b4` — WR-04/WR-08 terminal recovery and durable run identity

## Skipped Issues

None — all in-scope findings were fixed.

---

_Fixed: 2026-08-10T15:56:17Z_
_Fixer: Codex (gsd-code-fixer)_
_Iteration: 2_
