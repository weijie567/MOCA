---
phase: 64.3-rag-format-parity-and-document-quality-evaluation
plan: "04"
subsystem: rag-evaluation
tags: [rag, evaluation, reporting, pgvector, dashscope, tesseract, pydantic, tdd]

# Dependency graph
requires:
  - phase: 64.3-02
    provides: direct production parser evaluation across the nine canonical fixtures
  - phase: 64.3-03
    provides: fixed-tenant provider ingestion/retrieval rounds with durable isolation proof
provides:
  - Strict canonical report schema owning metrics, target gates, identity, and failure attribution
  - Deterministic Markdown projection loaded only from validated canonical JSON
  - Fail-closed full-provider orchestration with baseline-ineligible safe diagnostics
  - Truthful completed-quality-fail provider baseline from all three real format rounds
affects: [64.3-05, 64.4-rag-evaluation, rag-quality, production-parser-hardening]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Frozen extra-forbid Pydantic report and diagnostic contracts
    - Canonical JSON ownership with deterministic Markdown projection
    - Atomic canonical-pair persistence with loader and byte-projection verification
    - Safe segmented report_config/report_build/report_persist diagnostics

key-files:
  created:
    - src/rag/evaluation/reporting.py
    - tests/eval/test_rag_format_parity_report.py
    - evaluation/reports/rag_format_parity/v1/baseline.json
    - evaluation/reports/rag_format_parity/v1/baseline.md
  modified:
    - scripts/eval_rag_format_parity.py

key-decisions:
  - "Canonical JSON is the only metric/gate/outcome owner; Markdown is a deterministic validated projection and performs no scoring."
  - "Only completed parser-direct plus real provider rounds with all isolation proofs may write the canonical pair; unavailable/error outcomes remain diagnostics."
  - "The macOS OCR temp realpath normalization is recorded as the non-sensitive enum explicit_macos_private_tmp and participates in configured baseline identity."
  - "Persistence destinations are excluded from the reproducibility command descriptor and configured identity; exact input hashes and execution identity remain recorded."
  - "The completed red baseline remains unchanged: no parser, chunker, embedder, retrieval, reranker, or fixture tuning was performed."

patterns-established:
  - "Report safety: strict allowlisted schemas plus recursive forbidden-key/value scanning before canonical serialization."
  - "Provider observability: config, report-build, and persistence failures emit stable safe reason codes without exception text, paths, DSNs, or provider payloads."

requirements-completed: [ROADMAP-SC-3, ROADMAP-SC-4]

# Metrics
duration: 1h41m
completed: 2026-08-10
---

# Phase 64.3 Plan 04: Canonical Reporting And Provider Baseline Summary

**Strict canonical JSON/Markdown reporting now preserves a real three-format DashScope/Tesseract provider baseline as an attributable `completed_quality_fail`, with atomic persistence and fail-closed diagnostics.**

## Performance

- **Duration:** 1h 41m
- **Started:** 2026-08-10T12:03:11Z
- **Completed:** 2026-08-10T13:43:53Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Added `rag_format_parity_report.v1`, whose frozen/extra-forbid canonical JSON owns outcomes, input/toolchain/config identity, eight immutable targets, case/policy/format/overall metrics, and exactly one primary failure stage per completed miss.
- Added deterministic Markdown rendering from a strict loader's JSON-mode dump. The checked baseline Markdown is a 12,045-byte byte-for-byte projection of the canonical JSON and contains no independent scoring logic.
- Added `--mode full-provider` orchestration with fixed evaluation identity, real parser/OCR, DashScope embeddings, production ingestion/retrieval, three durable isolation rounds, atomic canonical-pair persistence, and `rag_format_parity_diagnostic.v1` for unavailable/error paths.
- Completed real run token `64f30400-0000-4000-8000-000000000006` under runtime identity `ocr_temp_directory_mode=explicit_macos_private_tmp`; Markdown, digital PDF, and scanned PDF rounds all reached durable `completed/index3/done` with zero mutable blocks/chunks/jobs after exact cleanup and 9/53 retained immutable document/chunk versions.
- Persisted an honest red baseline: `baseline_eligible=true`, 54 cases, 46 attributed failures, Hit@5 `0.977778`, cross-format Hit@5 spread `0.066667`. Retrieval Hit@5 and spread gates pass; parse success, digital/scanned anchor, table preservation, and PDF locator gates fail and remain red.

## Task Commits

Each TDD task used an explicit RED then GREEN sequence:

1. **Task 1 RED: Add failing canonical report contract tests** - `f82b074` (`test`)
2. **Task 1 GREEN: Implement canonical parity reporting** - `fe109d2` (`feat`)
3. **Task 2 RED: Add failing full-provider persistence tests** - `78db1cb` (`test`)
4. **Task 2 GREEN: Persist honest provider parity baseline** - `518b26b` (`feat`)

The Task 2 GREEN commit includes the targeted command-length RED/GREEN correction, explicit OCR temp runtime identity, and segmented post-run diagnostic coverage developed under the existing Task 2 RED gate.

## Files Created/Modified

- `src/rag/evaluation/reporting.py` - Owns strict report/runtime/metric/gate/failure schemas, aggregation, safety validation, loader, configured identity, and deterministic Markdown projection.
- `scripts/eval_rag_format_parity.py` - Runs the full real-provider pipeline, validates isolated prerequisites, records runtime identity, persists the canonical pair atomically, and emits safe segmented diagnostics.
- `tests/eval/test_rag_format_parity_report.py` - Covers exact metric math, target parity, attribution, four outcomes, loader/projection determinism, runtime identity, canonical eligibility, atomic failure behavior, and diagnostic safety.
- `evaluation/reports/rag_format_parity/v1/baseline.json` - Canonical real-provider red baseline; SHA-256 `e621884b169f60b37911aa5c55bbcb1caeaf8d44f20e21ce257fd0f309728d94`.
- `evaluation/reports/rag_format_parity/v1/baseline.md` - Deterministic human projection; SHA-256 `ce1d0741f4187674b113c8a561217bca0b0bba0e73cd4b89c0de15c3da4cc86d`.

## Decisions Made

- Kept the eight accepted `rag_format_parity_targets.v1` thresholds exact; the real run was recorded red rather than tuned to pass.
- Counted only real parser-direct and provider observations. Contract-test/fake modes and incomplete isolation/case sets remain baseline-ineligible.
- Excluded timestamps, timings, run token, token observations, command text, and persistence paths from configured baseline identity; included the non-sensitive OCR temp-directory mode because it changes runtime behavior.
- Used command-level `TMPDIR=/private/tmp` only to normalize the real macOS temp directory path seen by unmodified pytesseract/Tesseract. The canonical report records the safe enum, not an arbitrary user path.
- Removed output and diagnostic destinations from the reproducibility command descriptor after exact preflight proved they exceeded the 512-character schema bound; input hashes and execution/tenant/rollout identity remain explicit.
- Preserved all measured parser defects as evidence. Digital PDF `hidden_text_ignored` remains a known false positive from unsupported pdfplumber attributes, and the temp-path dependency remains production parser/runtime debt for Plan 05 and post-Phase 64.3 parser hardening.

## Verification

- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_rag_format_parity_report.py tests/eval/test_rag_parser_parity.py tests/eval/test_rag_retrieval_round_isolation.py -q --tb=short` -> **83 passed**, with one pre-existing LangChain pending-deprecation warning.
- Ruff check and format check over the three Plan 04 source/test files -> **passed**; `git diff --check` -> **passed**.
- Strict `load_format_parity_report(...)` validation succeeded; rendering its JSON-mode dump reproduced the checked Markdown byte-for-byte.
- Synthetic config preflight with the final argparse values succeeded at command length 377, recorded `explicit_macos_private_tmp`, and proved persistence paths absent from the command descriptor.
- The real provider run completed all three database rounds, retained immutable history, cleaned all mutable evaluation children/jobs, and wrote only after completed proof. The disposable no-volume `pgvector/pgvector:0.8.2-pg16` container was then stopped and removed.
- The legacy 22-case Gold SHA-256 remains `b902695607997913d154e836ed17356df3968290e7c5e2f7fd0eb5f862be3e1b`.
- Recursive secret/DSN/raw-path scan found no forbidden value in either canonical artifact.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Bridged evaluation and production checksum representations**

- **Found during:** Task 2 real-provider execution
- **Issue:** The evaluation owner reserved a 64-character digest while production ingestion jobs persisted `sha256:<digest>`, so successful jobs could not satisfy exact-attempt proof.
- **Fix:** The Plan 03 owner added strict one-way owner-digest canonicalization for candidate/all-attempt lookup, head classification, and exact deletion. Arbitrary/already-prefixed owner values remain rejected.
- **Files modified:** `src/repositories/rag_evaluation_round_repo.py`, `src/repositories/rag_ingestion_job_repo.py`, `tests/eval/test_rag_retrieval_round_isolation.py`
- **Verification:** Plans 01-04 related suite passed; the final real run completed all three formats.
- **Committed in:** `4c00863` (RED), `c4b71c3` (GREEN)

**2. [Rule 1 - Bug] Closed retrieval read transactions before exact cleanup**

- **Found during:** Task 2 real-provider execution
- **Issue:** Service searches left an implicit SQLAlchemy transaction open, so the following explicit cleanup transaction failed and obscured the stable reason.
- **Fix:** Plan 03 wrapped each service search plus its single recording capture in a short transaction that closes or rolls back before cleanup.
- **Files modified:** `src/rag/evaluation/retrieval_rounds.py`, `tests/eval/test_rag_retrieval_round_isolation.py`
- **Verification:** Transaction-boundary tests and the final three-round cleanup proof passed.
- **Committed in:** `7ea1b99` (RED), `be32691` (GREEN)

**3. [Rule 1 - Bug] Classified controlled scanned malformed-source ingestion as quality evidence**

- **Found during:** Task 2 real-provider execution
- **Issue:** Available OCR that returned the controlled `malformed_source` result stopped the round as execution error, preventing the planned truthful quality-failure path.
- **Fix:** Plan 03 permits only a second real persisted scanned-PDF `malformed_source` failure to advance after exact job deletion, zero-residual proof, and immutable non-regression; all other errors remain execution errors.
- **Files modified:** `src/rag/evaluation/retrieval_rounds.py`, `src/repositories/rag_evaluation_round_repo.py`, `tests/eval/test_rag_retrieval_round_isolation.py`
- **Verification:** 112 related tests passed before the final provider run.
- **Committed in:** `bb88d1d` (RED), `9186cb9` (GREEN)

**4. [Rule 3 - Blocking] Recorded and normalized the macOS OCR temp runtime**

- **Found during:** Task 2 fixture/runtime diagnosis
- **Issue:** On this host `/tmp` resolves to `/private/tmp`; pytesseract handed Tesseract the symlink-form temporary PNG path and Leptonica failed to read it. The same fixtures were fully readable when the temp directory used its real path.
- **Fix:** Ran the final provider command with `TMPDIR=/private/tmp`, added the safe `explicit_macos_private_tmp` runtime identity, included it in configured identity, and rendered it in the reproducibility section. No parser or fixture was modified.
- **Files modified:** `src/rag/evaluation/reporting.py`, `scripts/eval_rag_format_parity.py`, `tests/eval/test_rag_format_parity_report.py`
- **Verification:** All 45 scanned pages completed OCR in the read-only diagnosis; the final real run completed scanned ingestion/retrieval and strict report persistence.
- **Committed in:** `518b26b`

**5. [Rule 1 - Bug] Replaced silent post-run report failures with segmented safe diagnostics**

- **Found during:** Task 2 canonical persistence
- **Issue:** A completed three-round run failed runtime-config validation because its command descriptor exceeded 512 characters, while `main` silently returned exit 2 for post-run `OSError/ValueError` without writing a diagnostic.
- **Fix:** Removed non-identity persistence destinations from the command descriptor, added exact config preflight coverage, and separated `report_config_failed`, `report_build_failed`, and `report_persist_failed` diagnostics that never include exception text.
- **Files modified:** `scripts/eval_rag_format_parity.py`, `tests/eval/test_rag_format_parity_report.py`
- **Verification:** Targeted RED reproduced `command:string_too_long`; GREEN validates length 377 and all three safe failure paths, followed by successful canonical persistence.
- **Committed in:** `518b26b` (under Task 2 RED `78db1cb`)

---

**Total deviations:** 5 auto-fixed (3 Rule 1 bugs, 2 Rule 3 blockers).
**Impact on plan:** Each fix was required for the specified real-provider, fail-closed canonical baseline. Target thresholds, production parser/fixture behavior, retrieval scoring, and canonical eligibility were not relaxed.

## Issues Encountered

- Four baseline-ineligible diagnostics were produced while isolating checksum, transaction, scanned progression, and incomplete-proof failures. Their exact hashes/reason codes were recorded during execution, then the four explicitly named files were deleted after the successful canonical run; no diagnostics were committed.
- A completed provider run initially produced no artifact because the overlong command failed strict config validation after the provider work. The new segmented diagnostic boundary prevents recurrence and was verified before the final run.
- A one-off config preflight initially omitted `repository_root`; the corrected command was rerun and produced the decisive `command:string_too_long` result. This local validation command incident was reported for `.planning/LOCAL-VALIDATION-ISSUES.md`.
- A diagnostic-hash shell loop briefly used zsh's special `path` variable and hid `PATH`, causing `shasum`/`jq` lookup failures. Renaming the loop variable produced the verified hashes without changing artifacts; this was reported as a local validation command incident.
- `shasum` emitted harmless locale fallback warnings. Hash values were stable, and the loader/projection verification used the project Python environment.

## Known Stubs

None. Baseline-ineligible diagnostic models intentionally have no metrics/gates/quality fields; that is a fail-closed contract, not an unwired data source.

## User Setup Required

None for the committed artifacts. Re-running the live baseline requires the declared DashScope credential, isolated migrated PostgreSQL/pgvector evaluation tenant, Tesseract `chi_sim+eng`, and the recorded macOS temp-directory normalization on this host.

## Next Phase Readiness

- Plan 05 can load one strict canonical real-provider baseline and consume its outcome, gates, identities, failures, and artifact hashes without ad-hoc field extraction.
- The red result is actionable evidence: retrieval Hit@5/spread pass, while parser success, PDF/scanned anchors, table preservation, and locator coverage require documented follow-up.
- Plan 05 must record the verified production parser debts. Target ownership is post-Phase 64.3 production parser hardening (or the explicit Phase 64.4 owner selected during closeout): resolve pytesseract temp input paths inside `OcrEngine`, and make pdfplumber word-attribute requests version-compatible without weakening genuine hidden-text detection.
- No provider process or disposable database container remains.

## Self-Check: PASSED

- All five implementation/artifact files and this Summary exist.
- Commits `f82b074`, `fe109d2`, `78db1cb`, and `518b26b` exist in the required RED/GREEN order.
- The four explicitly named diagnostic files and the disposable `moca-rag-eval-64304` container are absent.
- Strict loader, byte-for-byte projection, tests, Ruff, formatting, legacy Gold hash, secret scan, and `git diff --check` all passed.

---
*Phase: 64.3-rag-format-parity-and-document-quality-evaluation*
*Completed: 2026-08-10*
