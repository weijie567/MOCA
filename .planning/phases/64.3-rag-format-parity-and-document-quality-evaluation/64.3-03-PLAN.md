---
phase: 64.3-rag-format-parity-and-document-quality-evaluation
plan: "03"
name: Retrieval Round Isolation And Provider Runtime
type: execute
wave: 2
depends_on: [64.3-01]
files_modified:
  - src/db/models.py
  - src/db/migrations/versions/029_phase64_3_rag_eval_rounds.py
  - src/repositories/rag_evaluation_round_repo.py
  - src/repositories/rag_ingestion_job_repo.py
  - src/rag/evaluation/retrieval_rounds.py
  - scripts/eval_rag_format_parity.py
  - tests/eval/test_rag_retrieval_round_isolation.py
autonomous: true
requirements: [ROADMAP-SC-3, ROADMAP-SC-5]
must_haves:
  truths:
    - "Retrieval parity can mutate data only for the one fixed evaluation tenant after every transaction re-locks and CAS-validates the durable owner marker, exact three-doc allowlist, run token, round token, round format, progress cursor, and state version."
    - "Markdown, digital-PDF, and scanned-PDF run as three isolated rounds; each round ingests exactly one equivalent variant for each canonical policy and asks the identical semantic-Gold questions."
    - "Every round proves clean/compatible pre-state and clean post-state for current documents, blocks, chunks, and jobs while retaining all Phase 64.2 immutable document/chunk versions and historical replay resolution."
    - "Provider mode uses the real IngestionService and production PolicyKnowledgeService/PolicyRetrievalEngine; deterministic fakes test contracts only and can never produce a baseline claim."
    - "Missing PostgreSQL/provider/OCR prerequisites are reported as unavailable_prerequisite, metric misses as completed_quality_fail, and isolation/evaluator invariant failures as execution_error."
  artifacts:
    - {path: src/repositories/rag_evaluation_round_repo.py, provides: "Durable evaluation ownership, lock, state proof, and exact cleanup boundary", contains: "RagEvaluationRoundRepository"}
    - {path: src/rag/evaluation/retrieval_rounds.py, provides: "Three-round production ingestion/retrieval orchestration", contains: "run_retrieval_parity"}
    - {path: tests/eval/test_rag_retrieval_round_isolation.py, provides: "Denial-before-mutation, immutable-history, runtime, and taxonomy coverage", contains: "test_wrong_round_token_denies_before_mutation"}
  key_links:
    - {from: src/rag/evaluation/retrieval_rounds.py, to: src/rag/ingestion.py, via: "IngestionService.ingest_document with production parser/chunker/embedder/versioning", pattern: "ingest_document"}
    - {from: src/rag/evaluation/retrieval_rounds.py, to: src/knowledge/service.py, via: "PolicyKnowledgeService.search over PolicyRetrievalEngine", pattern: "knowledge_service.search"}
    - {from: src/rag/evaluation/retrieval_rounds.py, to: src/repositories/rag_evaluation_round_repo.py, via: "owned round claim, pre/post proof, and exact cleanup", pattern: "RagEvaluationRoundRepository"}
    - {from: src/repositories/rag_evaluation_round_repo.py, to: src/db/migrations/versions/029_phase64_3_rag_eval_rounds.py, via: "ORM/Alembic evaluation control-row parity", pattern: "rag_evaluation_rounds"}
---

<objective>
Create the fail-closed evaluation-owned isolation boundary and provider runtime for three production retrieval-parity rounds.

Purpose: Produce trustworthy cross-format retrieval observations without contaminating rounds, selecting production tenants, bypassing production services, or weakening Phase 64.2 immutable replay history.
Output: Durable round ownership schema/repository, isolated round state machine, real ingestion/retrieval adapter, provider CLI, and deterministic/DB-backed safety tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-CONTEXT.md
@.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-RESEARCH.md
@.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-VALIDATION.md
@.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-PATTERNS.md
@.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-CONTEXT.md
@src/rag/ingestion.py
@src/knowledge/schemas.py
@src/knowledge/service.py
@src/knowledge/retrieval.py
@src/repositories/evidence_version_repo.py
@src/repositories/policy_document_repo.py
@src/repositories/policy_chunk_repo.py
@src/repositories/document_block_repo.py
@src/repositories/rag_ingestion_job_repo.py
@src/db/models.py
@src/db/migrations/versions/028_phase64_2_memory_lifecycle.py

<interfaces>
Use these evaluation ownership constants as one code-owned contract:
```python
FORMAT_PARITY_TENANT_ID = UUID("64300000-0000-4000-8000-000000000001")
FORMAT_PARITY_OWNER_MARKER = "moca.rag_format_parity.v1"
FORMAT_PARITY_DOC_KEYS = (
    "eval_refund_eligibility_and_return",
    "eval_quality_compensation_and_approval",
    "eval_cross_border_and_digital_goods",
)
ROUND_FORMATS = ("markdown", "digital_pdf", "scanned_pdf")
```

Production boundaries already expose:
```python
await IngestionService(session, embedder, tenant_id).ingest_document(
    file_path,
    doc_meta,
    expected_rollout_version=expected_rollout_version,
)
await PolicyKnowledgeService(PolicyRetrievalEngine(session, embedder=embedder)).search(
    KnowledgeSearchRequest(...),
    KnowledgeContext(...),
)
```

Export the Plan 04 input contract:
```python
async def run_retrieval_parity(
    dataset: FormatParityDataset,
    *,
    session: AsyncSession,
    embedder: EmbeddingService,
    owner: EvaluationRoundIdentity,
    generated_at: str,
) -> RetrievalParityRunV1: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add durable fail-closed evaluation ownership and exact cleanup</name>
  <read_first>@src/db/models.py @src/db/migrations/versions/028_phase64_2_memory_lifecycle.py @src/repositories/evidence_version_repo.py @src/repositories/policy_document_repo.py @src/repositories/policy_chunk_repo.py @src/repositories/document_block_repo.py @src/repositories/rag_ingestion_job_repo.py @tests/integration/test_phase64_2_integrity_matrix.py</read_first>
  <files>src/db/models.py, src/db/migrations/versions/029_phase64_3_rag_eval_rounds.py, src/repositories/rag_evaluation_round_repo.py, src/repositories/rag_ingestion_job_repo.py, tests/eval/test_rag_retrieval_round_isolation.py</files>
  <action>Per D-10 through D-12, write denial, concurrent-insert, interleaving, crash-recovery, and reversible-migration tests first. Add evaluation-only control rows with exact tenant/marker/tokens/format/doc allowlist, state/CAS version, next index/step reservation, attempt doc key, expected source checksum, database reservation timestamp, nullable claimed job UUID, lease, proofs, head mapping, immutable counts, safe failure, and timestamps. Migration 029 chains from 028, adds only the control table/indexes/checks, uses RESTRICT tenant binding, mutates no production row, and enforces one non-terminal round per tenant with a partial unique index. Downgrade drops evaluation objects only, refuses non-terminal rows, and documents/tests `expire -> exact proof -> same-owner cleanup -> terminal abandoned -> downgrade`; upgrade-downgrade-upgrade preserves production rows/history/FKs/triggers. Implement every step in a fresh transaction with owner-row lock, identity/progress proof, deterministic head locks, exact state proof, one CAS reservation, then commit. Extend `RagIngestionJobRepository` with evaluation-safe exact attempt seams: lock candidate jobs by `(tenant_id, doc_key, source_checksum, created_at >= database reservation timestamp)`; require zero or exactly one candidate; CAS-record its UUID on the owner row before deletion; and delete only by the full `(job_id, tenant_id, doc_key, source_checksum)` predicate. This supports `doc_id=NULL` without a broad doc-key delete. Model recovery states: reservation-only/no job; one NULL-doc job-only; terminal failure job; exact complete head+blocks+chunks+immutable binding+success job; or malformed/multiple/mismatched state. Reservation-only retries the same index; job-only/failure records and exactly deletes the one attempt then retries; only exact complete advances; malformed never advances and fails closed unless full proof authorizes exact child cleanup. Current blocks/chunks are still cleaned only by exact tenant+document UUID; heads and immutable versions remain. Different tokens deny; stale claims require explicit expiry/abandon and proof. First use requires the fixed evaluation-only tenant and zero/exact compatible heads; extra documents are detected, never deleted. Never add truncate/global/prefix cleanup, PolicyDocument/history deletion, FK/trigger weakening, or tenant fallback.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_rag_retrieval_round_isolation.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/db/models.py src/db/migrations/versions/029_phase64_3_rag_eval_rounds.py src/repositories/rag_evaluation_round_repo.py src/repositories/rag_ingestion_job_repo.py tests/eval/test_rag_retrieval_round_isolation.py</automated>
  </verify>
  <acceptance_criteria>
    - ORM/migration parity tests prove the exact tenant/marker/format/key/state/version/progress constraints, the non-terminal partial unique index, concurrent claim denial, and `down_revision="028_phase64_2_memory_lifecycle"`; upgrade-downgrade-upgrade changes only evaluation objects and downgrade refuses active rounds until the documented safe-abandon sequence completes.
    - Wrong tenant/marker/key/run token/round token/format/version and extra state all deny before mutation with one generic external error and internal stable reason codes.
    - Repository source contains no truncate, global delete, prefix delete, PolicyDocument delete, PolicyDocumentVersion delete/update, or PolicyChunkVersion delete/update.
    - Current block/chunk cleanup is reachable only after fresh ownership proof and exact tenant+document UUID predicates; NULL-doc job cleanup first proves a unique post-reservation candidate, CAS-records its UUID, and deletes by job UUID+tenant+doc_key+checksum.
    - Tests preserve immutable document/chunk row counts and exact historical resolution across current-child cleanup.
    - Interleaving/recovery tests cover reservation-only, NULL-doc job-only, failure-job, exact complete projection, multiple/mismatched candidate, and malformed projection states; only exact complete advances CAS and no broad doc-key job delete exists.
  </acceptance_criteria>
  <done>A durable control plane owns evaluation mutations and cannot erase or weaken immutable evidence/replay history.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Orchestrate isolated production ingestion and identical retrieval questions</name>
  <read_first>@src/rag/evaluation/contracts.py @src/repositories/rag_evaluation_round_repo.py @src/rag/ingestion.py @src/knowledge/schemas.py @src/knowledge/service.py @src/knowledge/retrieval.py @src/rag/embedder.py @evaluation/golden/rag_format_parity_gold.json @tests/knowledge/test_facade_integration.py</read_first>
  <files>src/rag/evaluation/retrieval_rounds.py, tests/eval/test_rag_retrieval_round_isolation.py</files>
  <action>Per D-09/D-10/D-12/D-13, write state-machine/interleaving tests first, then implement one bounded `run_retrieval_parity(...)` owner without assuming a lock can span the full round. For each format, claim/prove pre-state in a short transaction. Before each ingestion, reopen, re-lock/CAS-validate exact ownership and `next_document_index`, reserve one document, and commit; invoke exactly one real ingestion; then reopen and classify the repository state using Task 1's job-only/complete/failure/malformed taxonomy. Only an exact complete projection with canonical immutable binding and success job CAS-advances; job-only/failure stays at the same index after exact cleanup/retry, and malformed state fails closed. Before retrieval, reopen/revalidate the completed three-document proof. Construct a real `KnowledgeSearchRequest` and `KnowledgeContext` whose trusted `tenant_id` is asserted equal to `64300000-0000-4000-8000-000000000001`. Wrap the real `PolicyRetrievalEngine` in an evaluation-only recording delegate that forwards the single `retrieve_run` call and captures its safe ranked hits/fallback/rewrite/rerank diagnostics; pass that delegate to real `PolicyKnowledgeService.search(...)`. Do not call retrieval twice and do not query repositories directly. Execute identical ordered Gold questions and capture service results plus the delegate's corresponding safe run observations for Hit@1/3/5, reciprocal rank, anchors, no-answer, locator, config/status/fallback/rewrite/rerank fields. Before and after cleanup, reopen/revalidate the same token/progress, remove exact current children, and prove zero current blocks/chunks/jobs plus retained heads and nondecreasing immutable counts. `finally` requests cleanup through the same protocol; cleanup/proof failure overrides quality results with `execution_error`. Same-token recovery resumes only after proof; mismatched token/version denies; stale work uses explicit expiry/abandon. Preserve per-transaction lock order. Never implement script-local search, alternate embeddings, production parser/chunker/ranking tuning, a second retrieval, or direct repository retrieval. Test doubles use the same interfaces only as `contract_test`, baseline-ineligible.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_rag_retrieval_round_isolation.py tests/knowledge/test_facade_integration.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/rag/evaluation/retrieval_rounds.py tests/eval/test_rag_retrieval_round_isolation.py</automated>
  </verify>
  <acceptance_criteria>
    - A service spy observes three ordered rounds, exactly three ingestions per round, and byte-identical ordered question IDs/text across formats.
    - Provider runtime constructs real `IngestionService`, `PolicyRetrievalEngine`, and `PolicyKnowledgeService`; a recording delegate forwards exactly one engine call triggered by `service.search`, and no script-local search, second retrieval, or repository call substitutes for them.
    - Pre-state, exactly-three-current-state, post-state, and immutable count/head proofs are present for every round, including failed ingestion/retrieval cases.
    - Tests inject crashes after the job-trace commit and after the complete final persistence commit plus a competing runner between transactions; job-only retries without advancing, exact complete state advances once, malformed state fails closed, and no long-lived lock assumption is required.
    - Every real request uses `KnowledgeSearchRequest` plus a `KnowledgeContext` whose tenant is the fixed evaluation tenant; service output and the one delegated retrieval run are correlated without a second query.
    - Tests show old immutable versions and Phase 64.2 exact historical replay resolution survive all three rounds and cleanup.
    - Fake/contract execution always has `baseline_eligible=false` and cannot construct a provider baseline result.
  </acceptance_criteria>
  <done>The production ingestion/retrieval path executes the same Gold questions in three isolated, immutable-history-preserving rounds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose provider runtime with honest prerequisite and failure taxonomy</name>
  <read_first>@scripts/eval_rag.py @scripts/eval_rag_ablation.py @src/rag/evaluation/contracts.py @src/rag/evaluation/retrieval_rounds.py @src/config.py @src/rag/embedder.py @src/rag/parsers/pdf.py @tests/eval/test_rag_retrieval_round_isolation.py</read_first>
  <files>scripts/eval_rag_format_parity.py, src/rag/evaluation/retrieval_rounds.py, tests/eval/test_rag_retrieval_round_isolation.py</files>
  <action>Per D-09/D-11/D-13/D-17, create an async CLI adapter with explicit `--mode provider`, `--manifest`, `--gold`, `--tenant-id`, `--owner-marker`, `--run-token`, `--expected-rollout-version`, `--output`, and injected `--generated-at`. Require fixed tenant `64300000-0000-4000-8000-000000000001`, owner marker `moca.rag_format_parity.v1`, UUID token, migrated PostgreSQL/pgvector, canonical rollout, real configured embedding provider, and Tesseract `chi_sim+eng`. Treat the fixed UUID as explicitly provisioned evaluation-only infrastructure that must never be assigned to a production tenant; require exact tenant marker/state preflight before first claim. Never resolve the first active tenant, accept a production tenant, discover tenants by fallback, or expose destructive reset. Missing DB/schema/provider/OCR/config is `unavailable_prerequisite`; contract/isolation/malformed/unsafe/residual state is `execution_error`; completed misses/pass retain completed outcomes. Emit safe prerequisite/stage fields only, never credentials, DSNs, provider/parser payloads, source bytes/paths, traces, or cross-tenant facts. Provider mode has no fake flag; injected test doubles remain baseline-ineligible. Exit nonzero for unavailable/error, while completed outcomes remain reportable. If infrastructure is unavailable, produce the safe diagnostic required by Plan 04 and block phase closeout rather than weakening runtime.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_rag_retrieval_round_isolation.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check scripts/eval_rag_format_parity.py src/rag/evaluation/retrieval_rounds.py tests/eval/test_rag_retrieval_round_isolation.py</automated>
  </verify>
  <acceptance_criteria>
    - CLI tests reject absent/wrong tenant, marker, run token, rollout epoch, and any attempted fake/baseline combination before mutation.
    - Missing DB/provider/OCR inputs serialize only named safe prerequisites under `unavailable_prerequisite`; no zero score or pass/fail quality conclusion is emitted.
    - Provider execution is wired to real ingestion/embedding/knowledge retrieval and marks completed quality misses `completed_quality_fail` without changing thresholds or production behavior.
    - `rg -n 'first active|truncate|delete.*prefix|--fake|reset-all' scripts/eval_rag_format_parity.py src/rag/evaluation/retrieval_rounds.py src/repositories/rag_evaluation_round_repo.py` returns no forbidden runtime path.
    - Output contains no credential, DSN, raw provider/parser payload, absolute source path, stack trace, or cross-tenant existence detail.
  </acceptance_criteria>
  <done>The real provider runtime is explicit, isolated, safe, and incapable of presenting unavailable or fake execution as a quality baseline.</done>
</task>

</tasks>

<threat_model>
Security posture: ASVS L1; execution blocks on any unresolved high-severity threat.

| Threat ID | STRIDE | Boundary | Disposition | Mitigation |
|---|---|---|---|---|
| T64.3-04 | Spoofing / Elevation of Privilege | CLI identity -> evaluation data mutation | mitigate | Fixed tenant, exact owner marker/doc keys, UUID run+round tokens, durable progress, and fresh-transaction lock/CAS validation before every step. |
| T64.3-05 | Tampering | cleanup -> current/immutable RAG state | mitigate | Exact tenant+document child deletes only; pre/post proofs; no document/version deletes; existing RESTRICT/append-only guards retained. |
| T64.3-06 | Information Disclosure | scope/provider failure -> report | mitigate | One non-enumerating isolation error and allowlisted prerequisite/diagnostic projection; no credentials, DSNs, paths, payloads, or cross-tenant facts. |
| T64.3-07 | Repudiation | retrieval round -> baseline inputs | mitigate | Durable owner row records tokens, format, state version, head mappings, immutable counts, timestamps, and safe terminal result. |
| T64.3-08 | Denial of Service | partial/failed round -> following format | mitigate | Durable reservations, same-token proof-and-resume, explicit stale expiry/abandon transition, protocol-driven `finally` cleanup, and residual-state execution failure before the next round. |
| T64.3-09 | Tampering | migration rollback -> production RAG schema/history | mitigate | Downgrade refuses active rounds, drops evaluation-only objects, and upgrade-downgrade-upgrade tests prove production rows, FKs, triggers, and immutable history unchanged. |
</threat_model>

<validation_mapping>
| Validation ID | Coverage | Automated gate |
|---|---|---|
| 64.3-03-01 | D-10..D-12 wrong identity/extra state denial before mutation | `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_rag_retrieval_round_isolation.py -q --tb=short` |
| 64.3-03-02 | D-12 pre/post isolation with append-only immutable history and replay preservation | Same focused repository/service suite |
| 64.3-03-03 | D-09/D-13/D-17 real production runtime, fake exclusion, and unavailable taxonomy | Same focused CLI/service suite |
</validation_mapping>

<verification>
Run all three task commands. Inspect the migration/repository for exact predicates and absence of destructive history operations. Provider/OCR execution is a separate non-CI gate; deterministic tests prove contracts but must not claim the baseline. Bare `pytest` and bare `python -m pytest` are invalid in MOCA.
</verification>

<success_criteria>
ROADMAP success criteria 3 and the cleanup/isolation portion of criterion 5 are satisfied: provider-backed retrieval uses production ingestion and knowledge services across three identical-question rounds, every mutation is durably evaluation-owned and fail-closed, and current cleanup preserves immutable evidence/replay history.
</success_criteria>

<output>
After completion, create `.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-03-SUMMARY.md`.
</output>
