---
phase: 64.3-rag-format-parity-and-document-quality-evaluation
plan: "04"
name: Canonical Reporting And Provider Baseline
type: execute
wave: 3
depends_on: [64.3-02, 64.3-03]
files_modified:
  - src/rag/evaluation/reporting.py
  - scripts/eval_rag_format_parity.py
  - tests/eval/test_rag_format_parity_report.py
  - evaluation/reports/rag_format_parity/v1/baseline.json
  - evaluation/reports/rag_format_parity/v1/baseline.md
autonomous: true
requirements: [ROADMAP-SC-3, ROADMAP-SC-4]
user_setup:
  - service: dashscope
    why: "Real text-embedding-v4 ingestion and retrieval for the provider-backed parity baseline"
    env_vars:
      - name: DASHSCOPE_API_KEY
        source: "Existing MOCA DashScope/OpenAI-compatible provider credentials"
  - service: local-rag-runtime
    why: "Provider baseline requires migrated PostgreSQL/pgvector and scanned-PDF OCR"
    env_vars:
      - name: DATABASE_URL
        source: "MOCA isolated evaluation database configuration"
    dashboard_config:
      - task: "Provision the exact evaluation tenant 64300000-0000-4000-8000-000000000001 and install Tesseract chi_sim+eng traineddata"
        location: "Isolated local/evaluation runtime; never a production tenant"
must_haves:
  truths:
    - "Canonical JSON is the sole owner of metrics, gates, outcomes, hashes, configuration, prerequisites, and failure attribution; Markdown is a deterministic projection of that JSON."
    - "Reports expose Hit@1/3/5, MRR, semantic-anchor coverage, no-answer/fallback correctness, locator coverage, and results by case, policy, and format."
    - "Every completed miss has exactly one primary stage among parser, OCR, chunking, retrieval, and provenance, with safe secondary reason codes where useful."
    - "The accepted initial target gates remain unchanged and are reported beside observations; a completed provider run may truthfully be red without being relabeled, hidden, or tuned in this phase."
    - "Pass, quality failure, unavailable prerequisites, and evaluator/isolation execution errors are mutually exclusive outcomes, and only real provider/OCR execution may occupy the canonical baseline paths."
    - "Unavailable/error execution writes only a versioned baseline-ineligible diagnostic schema to a caller-selected non-baseline path; unavailable prerequisites block Phase 64.3 closeout until a real completed provider run exists."
    - "Provider reproducibility means exact recorded input hashes, generator/toolchain/config identity, command, and attributable observations; it does not promise bit-identical live-provider scores across separate executions."
  artifacts:
    - {path: src/rag/evaluation/reporting.py, provides: "Canonical metric, gate, attribution, JSON, and Markdown projection owner", contains: "render_markdown"}
    - {path: tests/eval/test_rag_format_parity_report.py, provides: "Metric math, target profile, taxonomy, determinism, safety, and projection tests", contains: "test_markdown_is_projection_of_canonical_json"}
    - {path: evaluation/reports/rag_format_parity/v1/baseline.json, provides: "Versioned real-provider canonical baseline when prerequisites are available", contains: "rag_format_parity_report.v1"}
    - {path: evaluation/reports/rag_format_parity/v1/baseline.md, provides: "Deterministic human projection of baseline.json", contains: "RAG Format Parity Baseline"}
  key_links:
    - {from: src/rag/evaluation/reporting.py, to: src/rag/evaluation/parser_parity.py, via: "ParserParityRunV1 case/dimension inputs", pattern: "ParserParityRunV1"}
    - {from: src/rag/evaluation/reporting.py, to: src/rag/evaluation/retrieval_rounds.py, via: "RetrievalParityRunV1 rank/anchor/fallback/locator inputs", pattern: "RetrievalParityRunV1"}
    - {from: scripts/eval_rag_format_parity.py, to: src/rag/evaluation/reporting.py, via: "build canonical JSON once, then render Markdown from that object", pattern: "render_markdown"}
    - {from: evaluation/reports/rag_format_parity/v1/baseline.md, to: evaluation/reports/rag_format_parity/v1/baseline.json, via: "deterministic projection with no independent metric calculation", pattern: "baseline.json"}
---

<objective>
Create the canonical report pipeline and run the truthful real-provider format-parity baseline.

Purpose: Turn parser and retrieval observations into reproducible, safely attributable evidence whose targets cannot drift and whose unavailable/error states cannot masquerade as quality results.
Output: Canonical report/scoring code, deterministic Markdown projection, full-provider orchestration, focused tests, and versioned baseline artifacts when prerequisites are available.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-CONTEXT.md
@.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-RESEARCH.md
@.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-VALIDATION.md
@.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-PATTERNS.md
@docs/quality/rag-quality-plan.md
@docs/quality/evaluation.md
@src/rag/evaluation/contracts.py
@src/rag/evaluation/parser_parity.py
@src/rag/evaluation/retrieval_rounds.py
@scripts/eval_rag_format_parity.py
@scripts/eval_rag_ablation.py
@tests/test_rag_ablation_eval.py

<interfaces>
Plan 04 consumes the completed intermediate contracts:
```python
parser_run: ParserParityRunV1 = evaluate_parser_parity(...)
retrieval_run: RetrievalParityRunV1 = await run_retrieval_parity(...)
```

It owns one report surface:
```python
def build_format_parity_report(
    *,
    dataset: FormatParityDataset,
    parser_run: ParserParityRunV1,
    retrieval_run: RetrievalParityRunV1,
    runtime_config: FormatParityRuntimeConfigV1,
    generated_at: str,
) -> FormatParityReportV1: ...

def render_markdown(report: Mapping[str, Any]) -> str: ...

def load_format_parity_report(path: Path) -> FormatParityReportV1: ...
```

The exact outcome vocabulary inherited from Plan 01 is:
```text
completed_pass
completed_quality_fail
unavailable_prerequisite
execution_error
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make canonical JSON own metrics, gates, attribution, and deterministic Markdown</name>
  <read_first>@src/rag/evaluation/contracts.py @src/rag/evaluation/parser_parity.py @src/rag/evaluation/retrieval_rounds.py @scripts/eval_rag_ablation.py @tests/test_rag_ablation_eval.py @docs/quality/rag-quality-plan.md @.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-CONTEXT.md</read_first>
  <files>src/rag/evaluation/reporting.py, tests/eval/test_rag_format_parity_report.py</files>
  <action>Per D-13 through D-16, write metric/taxonomy/snapshot tests first, then implement pure aggregation. Canonical `rag_format_parity_report.v1` records outcome; baseline eligibility; command/execution kind; time; manifest/Gold/generator/fixture hashes; parser/OCR and embedding/retrieval/RRF/rewrite/reranker/threshold/rollout identity; safe prerequisites/tokens/timings/counts; case/policy/format rows; failures; and gates. Its baseline identity covers inputs/config/toolchain, not time/path. Separately executed live metrics may vary; one canonical object's JSON-to-Markdown projection is byte-deterministic. Implement strict `FormatParityReportV1` plus `load_format_parity_report(path)` using frozen/extra-forbid validation so Plan 05 never trusts four ad-hoc fields. Calculate required metrics and one primary stage per miss. Define the eight immutable target gates and lock them to the accepted quality-plan values. Threshold change requires a new profile and rationale. `render_markdown(validated_mapping)` never recalculates, sorts deterministically, and serialized forms are scanned for forbidden data.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_rag_format_parity_report.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/rag/evaluation/reporting.py tests/eval/test_rag_format_parity_report.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert exact Hit@1/3/5, MRR, anchor, fallback, locator, and spread math at case/policy/format/overall levels.
    - `rag_format_parity_targets.v1` contains the eight exact gates above, and a test rejects silent value changes under the same profile version.
    - A parity test locks `rag_format_parity_targets.v1` to the accepted values in `docs/quality/rag-quality-plan.md`; provider reproducibility is described as input/config/toolchain-attributable rerunnability, never bit-identical scores across live runs.
    - Every completed failed case has exactly one primary `parser|ocr|chunking|retrieval|provenance` stage; malformed/unattributable completed rows become `execution_error` rather than guessed quality scores.
    - `render_markdown(canonical) == render_markdown(json.loads(json.dumps(canonical, sort_keys=True)))` and matches the checked snapshot; Markdown owns no metric or threshold function.
    - `load_format_parity_report` rejects extra/malformed/unsafe fields, and rendering its validated JSON-mode dump reproduces the canonical Markdown byte-for-byte.
    - Pass, quality-fail, unavailable, and execution-error fixtures are mutually exclusive and preserve complete safe provenance without forbidden fields.
  </acceptance_criteria>
  <done>One canonical result object owns every metric, gate, identity, outcome, and failure row, with Markdown as a deterministic view only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run and persist the honest full-provider baseline</name>
  <read_first>@scripts/eval_rag_format_parity.py @src/rag/evaluation/contracts.py @src/rag/evaluation/parser_parity.py @src/rag/evaluation/retrieval_rounds.py @src/rag/evaluation/reporting.py @docs/quality/rag-quality-plan.md @evaluation/rag_sources/README.md @tests/eval/test_rag_format_parity_report.py</read_first>
  <files>scripts/eval_rag_format_parity.py, tests/eval/test_rag_format_parity_report.py, evaluation/reports/rag_format_parity/v1/baseline.json, evaluation/reports/rag_format_parity/v1/baseline.md</files>
  <action>Extend the Plan 03 CLI with explicit `--mode full-provider`, calling the Plan 02 direct parser evaluator for all nine fixtures and Plan 03 real ingestion/retrieval runtime for three rounds before constructing one canonical report. Require the explicitly provisioned evaluation-only tenant `64300000-0000-4000-8000-000000000001`, marker, UUID run token, rollout version, migrated PostgreSQL/pgvector, real embedding provider, and Tesseract `chi_sim+eng`; the UUID must never be assigned to a production tenant, and exact preflight state/marker proof is mandatory. No tenant fallback or fake baseline flag exists. Canonical baseline paths are writable only when both paths ran real production services and reached a completed outcome. Write sorted canonical JSON, derive Markdown from the same object, and re-read/compare before atomic rename. Define `rag_format_parity_diagnostic.v1` with only outcome, baseline_eligible=false, execution kind, safe prerequisites/reason codes, available input identities, token, and injected timestamp. Require an explicit non-baseline diagnostic path, reject canonical aliases, and atomically write diagnostics for unavailable/error while leaving baseline untouched. Diagnostics have no metrics/gates/quality conclusion and cannot close the phase. If execution completes but misses targets, write a truthful red canonical pair without tuning. Add tests for fake exclusion, path alias rejection, diagnostic schema/eligibility, atomic failures, Markdown round-trip, generator identity presence, and the unchanged legacy Gold hash.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_rag_format_parity_report.py tests/eval/test_rag_parser_parity.py tests/eval/test_rag_retrieval_round_isolation.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/rag/evaluation/reporting.py scripts/eval_rag_format_parity.py tests/eval/test_rag_format_parity_report.py</automated>
    <provider>UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/eval_rag_format_parity.py --mode full-provider --manifest evaluation/rag_sources/format_parity_manifest.jsonl --gold evaluation/golden/rag_format_parity_gold.json --tenant-id 64300000-0000-4000-8000-000000000001 --owner-marker moca.rag_format_parity.v1 --run-token "$RAG_FORMAT_PARITY_RUN_TOKEN" --expected-rollout-version "$EVIDENCE_ROLLOUT_VERSION" --output-dir evaluation/reports/rag_format_parity/v1 --diagnostic-output "evaluation/reports/rag_format_parity/v1/diagnostics/$RAG_FORMAT_PARITY_RUN_TOKEN.json"</provider>
  </verify>
  <acceptance_criteria>
    - Deterministic tests prove only a completed real `full-provider` run can atomically write canonical `baseline.json`/`baseline.md`.
    - With available prerequisites, both artifacts exist, share identical schema/outcome/hash/config/gate/failure facts, and may truthfully contain `completed_quality_fail`.
    - With unavailable prerequisites, neither baseline path is created or replaced, and the diagnostic outcome is neither pass nor quality failure.
    - Unavailable/error output validates as `rag_format_parity_diagnostic.v1` at an explicit non-baseline path, cannot alias either canonical filename, and an unavailable result blocks phase closeout rather than flowing into Plan 05 as completed evidence.
    - JSON records all fixture/manifest/Gold hashes and parser/OCR/embedding/retrieval/RRF/rewrite/reranker/rollout configuration required by D-14.
    - Metric misses remain red under the unchanged target profile; no production parser, chunker, embedder, retrieval, reranker, ContextBuilder, or claim-verifier source file is modified.
    - The legacy 22-case RAG golden hash remains exactly `b902695607997913d154e836ed17356df3968290e7c5e2f7fd0eb5f862be3e1b`.
  </acceptance_criteria>
  <done>A real provider/OCR run produces a reproducible canonical baseline that is honest when red; unavailable/error runs cannot masquerade as the baseline.</done>
</task>

</tasks>

<threat_model>
Security posture: ASVS L1; execution blocks on any unresolved high-severity threat.

| Threat ID | STRIDE | Boundary | Disposition | Mitigation |
|---|---|---|---|---|
| T64.3-07 | Tampering | scored rows/config -> canonical gates | mitigate | Pure aggregation, immutable versioned target profile, hash/config provenance, and exact metric goldens. |
| T64.3-12 | Spoofing | fake/unavailable run -> canonical baseline | mitigate | Baseline eligibility requires real parser/provider/production-service execution and completed outcome; canonical paths remain untouched otherwise. |
| T64.3-13 | Repudiation | provider run -> checked-in artifacts | mitigate | Run/round tokens, command/mode, timestamp, all input hashes, versions/config, prerequisites, and atomic writes are recorded. |
| T64.3-14 | Information Disclosure | runtime diagnostics -> JSON/Markdown | mitigate | Allowlisted safe fields plus forbidden credential/DSN/path/payload/prompt/traceback tests on both serialized forms. |
| T64.3-15 | Tampering | JSON -> Markdown projection | mitigate | Markdown reads canonical fields only; round-trip/snapshot tests prohibit independent metric, gate, outcome, or attribution calculations. |
</threat_model>

<validation_mapping>
| Validation ID | Coverage | Automated gate |
|---|---|---|
| 64.3-04-01 | D-13..D-16 canonical metrics, attribution, JSON ownership, and deterministic Markdown | `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_rag_format_parity_report.py -q --tb=short` |
| 64.3-04-02 | D-15..D-17 unchanged targets, four outcomes, baseline eligibility, and honest provider result | Full focused three-suite command plus explicit non-CI provider command |
</validation_mapping>

<verification>
Run both deterministic task commands. Run the provider command only with the declared isolated prerequisites and explicit diagnostic output. A completed red baseline is valid phase evidence and must stay red; an unavailable prerequisite is neither quality pass nor quality fail, must not create/replace canonical baseline files, and blocks phase closeout pending a real completed provider run. Bare `pytest` and bare `python -m pytest` are invalid in MOCA.
</verification>

<success_criteria>
ROADMAP success criteria 3 and 4 are satisfied: the baseline is reproducible from recorded inputs/configuration, exposes all required cross-format retrieval metrics and stage attribution, preserves fixed targets, and distinguishes honest red quality from unavailable prerequisites and evaluator errors.
</success_criteria>

<output>
After completion, create `.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-04-SUMMARY.md`.
</output>
