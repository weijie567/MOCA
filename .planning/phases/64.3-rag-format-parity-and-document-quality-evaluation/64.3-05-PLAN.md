---
phase: 64.3-rag-format-parity-and-document-quality-evaluation
plan: "05"
name: Final Regression Documentation And Ledgers
type: execute
wave: 4
depends_on: [64.3-04]
files_modified:
  - Makefile
  - docs/quality/evaluation.md
  - .planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-VALIDATION.md
  - .planning/LOCAL-VALIDATION-ISSUES.md
  - .planning/ARCHITECTURE-DEBT.md
autonomous: true
requirements: [ROADMAP-SC-5]
must_haves:
  truths:
    - "Maintainers have one documented deterministic contract gate and one explicit real-provider/OCR baseline command with prerequisites and outcome semantics."
    - "Focused format-parity tests and existing RAG evaluation/parser/knowledge regressions pass through the repository uv environment, while a completed quality-red provider baseline remains truthful; unavailable/error provider evidence blocks phase closeout."
    - "Any concrete local validation failure or verified RAG architecture debt found during execution is recorded in the correct Chinese ledger with commands/evidence, disposition, remaining risk, and next entry point."
    - "Every observed baseline miss has a named owner: Phase 64.4 only for token/chunk-boundary work, and separately named parsing/table, retrieval-quality, or mixed-corpus follow-up work for their domains."
    - "The phase changes evaluation-only contracts/runtime/persistence and documentation; production parser, chunker, embedding, retrieval/reranker, ContextBuilder, and claim-verifier behavior remain unchanged."
  artifacts:
    - {path: docs/quality/evaluation.md, provides: "Current format-parity commands, prerequisites, report fields, outcome semantics, targets, and baseline status", contains: "RAG format parity"}
    - {path: .planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-VALIDATION.md, provides: "Final command evidence and Nyquist sign-off", contains: "64.3-05-02"}
    - {path: .planning/LOCAL-VALIDATION-ISSUES.md, provides: "Concrete local validation incidents only", contains: "Phase 64.3"}
  key_links:
    - {from: Makefile, to: scripts/eval_rag_format_parity.py, via: "explicit provider baseline target", pattern: "eval-rag-format-parity-provider"}
    - {from: docs/quality/evaluation.md, to: evaluation/reports/rag_format_parity/v1/baseline.json, via: "canonical baseline status and reproducibility contract", pattern: "baseline.json"}
    - {from: .planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-VALIDATION.md, to: tests/eval, via: "recorded uv pytest/Ruff phase gates", pattern: "UV_CACHE_DIR=/tmp/uv-cache uv run pytest"}
---

<objective>
Close Phase 64.3 with executable commands, regression evidence, truthful baseline documentation, verified ledgers, and an explicit no-production-behavior-drift gate.

Purpose: Make the evaluator maintainable and auditable while routing observed quality gaps to named owners instead of silently tuning production RAG or rewriting targets.
Output: Make targets, current evaluation documentation, completed validation evidence, verified issue/debt entries, and final regression results.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-CONTEXT.md
@.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-RESEARCH.md
@.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-VALIDATION.md
@.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-04-SUMMARY.md
@docs/quality/evaluation.md
@docs/quality/rag-quality-plan.md
@Makefile
@.planning/LOCAL-VALIDATION-ISSUES.md
@.planning/ARCHITECTURE-DEBT.md

<interfaces>
The final commands must remain distinct:
```text
deterministic contract/focused tests
direct parser evaluator (no DB/embedder/retrieval)
full-provider baseline (fixed evaluation tenant + DB/provider/OCR)
existing RAG regression suite
```

The provider report status is authoritative from canonical JSON only:
```text
completed_pass          -> completed quality gate pass
completed_quality_fail  -> completed truthful red baseline; route misses by stage
unavailable_prerequisite -> no quality conclusion and no canonical baseline replacement
execution_error         -> evaluator/isolation failure and no quality conclusion
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish exact deterministic and provider execution contracts</name>
  <read_first>@Makefile @docs/quality/evaluation.md @docs/quality/rag-quality-plan.md @scripts/eval_rag_parser_parity.py @scripts/eval_rag_format_parity.py @src/rag/evaluation/reporting.py @.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-04-SUMMARY.md</read_first>
  <files>Makefile, docs/quality/evaluation.md</files>
  <action>Per D-14 through D-17, add Make targets `eval-rag-format-parity-contract`, `eval-rag-format-parity-parser`, and `eval-rag-format-parity-provider` without altering the existing `eval-rag` target or legacy 22-case command. The contract target runs the four focused test files through `UV_CACHE_DIR=/tmp/uv-cache uv run pytest ... -q --tb=short`; parser target runs `UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/eval_rag_parser_parity.py` with the checked-in manifest/Gold and an explicit output; provider target runs `UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/eval_rag_format_parity.py --mode full-provider` with fixed tenant `64300000-0000-4000-8000-000000000001`, marker `moca.rag_format_parity.v1`, required `RAG_FORMAT_PARITY_RUN_TOKEN`, required `EVIDENCE_ROLLOUT_VERSION`, canonical v1 report directory, and an explicit `diagnostics/&lt;run-token&gt;.json` output. Read `64.3-04-SUMMARY.md` first: continue only when it records `completed_pass` or `completed_quality_fail` and then read the reported canonical JSON/Markdown paths; if it records unavailable/error, read only the reported diagnostic path, document the missing/failing gate, and stop Phase 64.3 closeout without claiming completion. Document exact prerequisites, metrics, hash/config provenance, unchanged target gates, primary-stage taxonomy, `rag_format_parity_diagnostic.v1`, and the four outcomes. A truthful red completed baseline is acceptable; unavailable/error diagnostics are baseline-ineligible and block closeout. Reference actual artifact status and preserve that deterministic fakes prove contracts only.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_rag_format_parity_contract.py tests/eval/test_rag_parser_parity.py tests/eval/test_rag_retrieval_round_isolation.py tests/eval/test_rag_format_parity_report.py tests/test_rag_eval.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check scripts/eval_rag_parser_parity.py scripts/eval_rag_format_parity.py src/rag/evaluation tests/eval</automated>
  </verify>
  <acceptance_criteria>
    - `Makefile` contains all three new target names and retains the existing `eval-rag:\n\tuv run python scripts/eval_rag.py` mapping unchanged.
    - `docs/quality/evaluation.md` lists the exact fixed tenant, owner marker, required environment/runtime prerequisites, canonical paths, metrics, target profile, and four outcomes.
    - Docs never call a deterministic fake a parser/provider baseline and never describe unavailable prerequisites as a quality pass/fail.
    - Docs identify canonical JSON as result owner and Markdown as its deterministic projection; no independent Markdown scoring is described.
    - All recorded pytest/Ruff/Python commands use `uv run` and contain no bare `pytest` or bare `python -m pytest` entry.
  </acceptance_criteria>
  <done>Maintainers have exact, safe, reproducible entry points and truthful interpretation rules for deterministic and provider execution.</done>
</task>

<task type="auto">
  <name>Task 2: Run final regression gates and close verified evidence ledgers</name>
  <read_first>@AGENTS.md @.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-VALIDATION.md @.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-04-SUMMARY.md @.planning/LOCAL-VALIDATION-ISSUES.md @.planning/ARCHITECTURE-DEBT.md @docs/quality/evaluation.md</read_first>
  <files>.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-VALIDATION.md, .planning/LOCAL-VALIDATION-ISSUES.md, .planning/ARCHITECTURE-DEBT.md, docs/quality/evaluation.md</files>
  <action>Read `64.3-04-SUMMARY.md` and branch on its authoritative outcome: for `completed_pass|completed_quality_fail`, read the exact canonical artifact paths named there and continue; for `unavailable_prerequisite|execution_error`, read only the named `rag_format_parity_diagnostic.v1`, record the blocking gate, and stop without green closeout. Run the exact focused suite, existing RAG gate `tests/test_rag_eval.py tests/test_rag_ablation_eval.py tests/eval tests/rag tests/knowledge`, and scoped Ruff. Record command/status/counts in `64.3-VALIDATION.md`; set green flags only after deterministic gates pass and a completed real-provider canonical pair exists. Append Chinese local-incident entries for the stale-Markdown-lineage incident and every concrete failure encountered. Append to `.planning/ARCHITECTURE-DEBT.md` only if repository/test evidence establishes a qualifying RAG subsystem defect/compromise/fix; otherwise record `no qualifying architecture debt found` with the evidence audit in `64.3-VALIDATION.md` and leave that ledger unchanged. Route every observed miss by primary stage to Phase 64.4 only for token/chunking, otherwise to the named parsing/table, retrieval-quality, or post-64.4 mixed-corpus follow-up. Run a machine-checkable phase-diff allowlist from stable pre-plan base `8392b0b`: permit planning/docs/fixture/Gold/report/Make/test changes plus `src/rag/evaluation/**`, `src/repositories/rag_evaluation_round_repo.py`, `src/db/models.py`, migration `029_phase64_3_rag_eval_rounds.py`, and the two evaluator scripts; fail if the diff contains existing `src/rag/parsers/**`, `src/rag/chunker.py`, `src/rag/embedder.py`, `src/knowledge/retrieval.py`, `src/knowledge/service.py`, `src/agent/rag_context/builder.py`, `src/agent/rag_context/verifier.py`, `src/agent/nodes/claim_verify.py`, or `evaluation/golden/rag_cases.jsonl`. Also fail static scans for truncate/global delete/broad-prefix cleanup/first-active-tenant fallback in Phase 64.3 runtime files.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_rag_format_parity_contract.py tests/eval/test_rag_parser_parity.py tests/eval/test_rag_retrieval_round_isolation.py tests/eval/test_rag_format_parity_report.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_rag_eval.py tests/test_rag_ablation_eval.py tests/eval tests/rag tests/knowledge -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/rag/evaluation src/repositories/rag_evaluation_round_repo.py src/db/models.py src/db/migrations/versions/029_phase64_3_rag_eval_rounds.py scripts/eval_rag_parser_parity.py scripts/eval_rag_format_parity.py tests/eval &amp;&amp; test -z "$(git diff --name-only 8392b0b...HEAD -- src/rag/parsers src/rag/chunker.py src/rag/embedder.py src/knowledge/retrieval.py src/knowledge/service.py src/agent/rag_context/builder.py src/agent/rag_context/verifier.py src/agent/nodes/claim_verify.py evaluation/golden/rag_cases.jsonl)" &amp;&amp; test -z "$(rg -n -i 'truncate|delete.{0,40}prefix|first.{0,20}active.{0,20}tenant' src/rag/evaluation src/repositories/rag_evaluation_round_repo.py scripts/eval_rag_format_parity.py || true)"</automated>
  </verify>
  <acceptance_criteria>
    - Both focused and existing RAG commands exit 0 through the MOCA `uv` environment, and the scoped Ruff command exits 0.
    - `64.3-VALIDATION.md` maps all 12 validation rows to actual command evidence and turns green flags on only after the final gates pass.
    - Provider outcome in docs/validation matches canonical JSON exactly; a red completed run stays red, while unavailable/error states claim no baseline quality result.
    - Every actual local validation incident has one correctly scoped Chinese ledger entry; verified RAG architecture findings/fixes are ledgered, while a no-finding audit is recorded only in validation and leaves `ARCHITECTURE-DEBT.md` unchanged.
    - Every metric miss has one named destination, with Phase 64.4 used only for token/chunking-owned findings.
    - Phase diff and static scans show no existing production parser/chunker/embedder/retrieval/reranker/ContextBuilder/claim-verifier change, no legacy golden change, no production-tenant fallback, and no truncate/global/broad-prefix cleanup.
  </acceptance_criteria>
  <done>Deterministic and existing RAG gates are green, provider status is truthful, verified findings are ledgered with named owners, and production RAG behavior has not drifted.</done>
</task>

</tasks>

<threat_model>
Security posture: ASVS L1; execution blocks on any unresolved high-severity threat.

| Threat ID | STRIDE | Boundary | Disposition | Mitigation |
|---|---|---|---|---|
| T64.3-16 | Spoofing / Repudiation | command/report status -> phase closeout claim | mitigate | Docs and validation read canonical JSON outcome; exact commands/results are recorded; unavailable/red/error cannot be relabeled. |
| T64.3-17 | Information Disclosure | provider/test failure -> docs and ledgers | mitigate | Record safe command/status/reason evidence only; exclude credentials, DSNs, raw payloads, paths, stack traces, and cross-tenant facts. |
| T64.3-18 | Tampering | metric miss -> threshold/follow-up record | mitigate | Immutable target profile plus named stage-owner mapping; phase rejects silent threshold edits and in-scope production tuning. |
| T64.3-19 | Elevation of Privilege | evaluation cleanup/config -> production RAG | mitigate | Final diff/static guards forbid production tenant fallback, broad destructive cleanup, and changes to existing production RAG behavior owners. |
</threat_model>

<validation_mapping>
| Validation ID | Coverage | Automated gate |
|---|---|---|
| 64.3-05-01 | ROADMAP SC-5 focused plus existing RAG regression and no production drift | Full focused and existing RAG uv pytest commands plus scoped Ruff |
| 64.3-05-02 | D-15 verified ledgers, truthful baseline state, and named follow-up ownership | Report test plus artifact/grep/diff evidence recorded in `64.3-VALIDATION.md` |
</validation_mapping>

<source_coverage_audit>
| Source | ID | Planned capability | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Reproducible 3-policy/9-variant parser and retrieval baseline through production paths without production RAG behavior change | 01-05 | COVERED |
| REQ | ROADMAP-SC-1 | Exact 3/9 manifest, shared semantic Gold, format-independent anchors | 01 | COVERED |
| REQ | ROADMAP-SC-2 | Direct persistence-free parser evaluator and independent quality dimensions | 02 | COVERED |
| REQ | ROADMAP-SC-3 | Fixed-tenant isolated production ingestion/retrieval rounds and required metrics | 03-04 | COVERED |
| REQ | ROADMAP-SC-4 | Versioned hash/config-owned baseline with truthful red result and attribution | 04 | COVERED |
| REQ | ROADMAP-SC-5 | Safe cleanup, existing regressions, ledgers, docs, and no production drift | 03,05 | COVERED |
| CONTEXT | D-01..D-04 | Fixture authority, semantic Gold, case coverage, independent hashes | 01 | COVERED |
| CONTEXT | D-05..D-08 | Direct ParserRegistry, dimension scoring, comparison-only normalization, safe deterministic misses | 02 | COVERED |
| CONTEXT | D-09..D-13 | Real services, fixed isolated rounds, fail-closed cleanup, immutable history, retrieval metrics | 03-04 | COVERED |
| CONTEXT | D-14..D-17 | Canonical JSON projection, honest red baseline, unchanged targets, CI/provider split | 04-05 | COVERED |
| RESEARCH | — | Stale Markdown lineage must be reconciled, never hash-only refreshed | 01 | COVERED |
| RESEARCH | — | Durable evaluation ownership and current-projection cleanup preserve Phase 64.2 history | 03 | COVERED |
| RESEARCH | — | Four outcomes, stage attribution, safe diagnostics, and real-provider eligibility | 02-05 | COVERED |
| RESEARCH | — | No production parser/chunker/embedding/retrieval/reranker tuning | 05 | COVERED |

Deferred ideas remain excluded exactly as decided: token-aware chunking/reindex/A-B selection belongs to Phase 64.4; mixed corpus, parser/table remediation, retrieval tuning, and DOCX/XLSX/PPTX parity retain their named destinations from CONTEXT.md.
</source_coverage_audit>

<verification>
Run the task commands exactly as written. Branch from `64.3-04-SUMMARY.md`, read only artifacts valid for that outcome, and verify canonical report status before updating docs/ledgers. Do not set Nyquist/approval green or complete Phase 64.3 on unavailable/error provider evidence or a deterministic fake. Bare `pytest` and bare `python -m pytest` are invalid in MOCA.
</verification>

<success_criteria>
ROADMAP success criterion 5 and phase closeout are satisfied: focused/existing RAG gates pass, provider outcome remains truthful, verified incidents/debt and named quality owners are recorded, and no production RAG behavior or non-evaluation data boundary has changed.
</success_criteria>

<output>
After completion, create `.planning/phases/64.3-rag-format-parity-and-document-quality-evaluation/64.3-05-SUMMARY.md`.
</output>
