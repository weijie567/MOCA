---
phase: 58-canonical-graph-cutover-and-no-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/graph_vocabulary.py
  - tests/architecture/test_canonical_graph_baseline.py
  - tests/agent/test_graph_vocabulary.py
  - tests/architecture/test_phase32_static_contract.py
  - tests/architecture/test_memory_contract_delta.py
  - tests/architecture/test_phase34_approval_action_boundaries.py
  - tests/memory/test_phase48_1_memory_compat_alignment.py
  - scripts/classify_phase58_legacy_hits.py
autonomous: true
requirements:
  - CAGM-09
requirements_addressed:
  - CAGM-09
user_setup: []
must_haves:
  truths:
    - "Active graph registrations equal the Phase 50 final 15 canonical nodes."
    - "`src/agent/graph_vocabulary.py` no longer exposes migration-era legacy names as active runtime compatibility aliases."
    - "Phase 58 has a reusable static classifier that reports legacy-name totals, categories, zero active-runtime hits, and zero unclassified rows."
  artifacts:
    - path: "src/agent/graph_vocabulary.py"
      provides: "Canonical current-runtime graph vocabulary plus explicitly separate historical projection support"
    - path: "tests/architecture/test_canonical_graph_baseline.py"
      provides: "Live final no-debt gate replacing the skipped Phase 58 marker"
    - path: "tests/agent/test_graph_vocabulary.py"
      provides: "Graph vocabulary regression coverage for canonical-only current runtime names"
    - path: "scripts/classify_phase58_legacy_hits.py"
      provides: "Repeatable legacy-hit classifier for Phase 58 validation"
      exports: ["main"]
  key_links:
    - from: "tests/architecture/test_canonical_graph_baseline.py"
      to: "tests/architecture/graph_baseline.py"
      via: "TARGET_CANONICAL_GRAPH_NODES and graph_add_node_names()"
      pattern: "graph_add_node_names\\(\\) == TARGET_CANONICAL_GRAPH_NODES"
    - from: "scripts/classify_phase58_legacy_hits.py"
      to: "src/agent/graph_vocabulary.py"
      via: "active-runtime legacy classification"
      pattern: "active_runtime_legacy"
---

<objective>
Lock the active graph and vocabulary as canonical-only current runtime surfaces, and create the Phase 58 static classifier foundation.

Purpose: CAGM-09 can only close if the final no-debt gate is executable and graph vocabulary no longer treats old names as current runtime aliases.
Output: Updated graph vocabulary contract, live architecture tests, migrated compatibility-alignment tests, and a reusable classifier script.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58-canonical-graph-cutover-and-no-debt-cleanup/58-CONTEXT.md
@.planning/phases/58-canonical-graph-cutover-and-no-debt-cleanup/58-RESEARCH.md
@.planning/phases/58-canonical-graph-cutover-and-no-debt-cleanup/58-PATTERNS.md
@.planning/phases/58-canonical-graph-cutover-and-no-debt-cleanup/58-VALIDATION.md
@.planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
@.planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-VALIDATION.md
@.planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-05-SUMMARY.md
@src/agent/graph.py
@src/agent/routing.py
@src/agent/graph_vocabulary.py
@tests/architecture/graph_baseline.py
@tests/architecture/test_canonical_graph_baseline.py
@tests/agent/test_graph_vocabulary.py
</context>

<interfaces>
From `src/agent/graph_vocabulary.py`:
```python
@dataclass(frozen=True)
class GraphVocabularyEntry:
    legacy_name: str
    target_name: str
    kind: TargetGraphKind
    status: TargetGraphStatus
    runnable: bool
    reason_codes: tuple[str, ...] = ()

def graph_vocabulary_entry(name: str, *, kind: TargetGraphKind | None = None) -> GraphVocabularyEntry | None: ...
def target_graph_name(name: str, *, kind: TargetGraphKind | None = None) -> str: ...
def project_trace_step_for_contract(step: Mapping[str, Any]) -> dict[str, Any]: ...
```

From `tests/architecture/graph_baseline.py`:
```python
TARGET_CANONICAL_GRAPH_NODES = frozenset({...15 canonical node names...})
def graph_add_node_names(path: Path = GRAPH_PATH) -> frozenset[str]: ...
def graph_router_route_values() -> dict[str, frozenset[str]]: ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Activate final graph/vocabulary no-debt gate</name>
  <read_first>
    - .planning/phases/58-canonical-graph-cutover-and-no-debt-cleanup/58-CONTEXT.md
    - .planning/phases/58-canonical-graph-cutover-and-no-debt-cleanup/58-RESEARCH.md
    - .planning/phases/58-canonical-graph-cutover-and-no-debt-cleanup/58-PATTERNS.md
    - .planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
    - src/agent/graph.py
    - src/agent/routing.py
    - src/agent/graph_vocabulary.py
    - tests/architecture/graph_baseline.py
    - tests/architecture/test_canonical_graph_baseline.py
    - tests/agent/test_graph_vocabulary.py
    - tests/architecture/test_phase32_static_contract.py
    - tests/architecture/test_memory_contract_delta.py
    - tests/architecture/test_phase34_approval_action_boundaries.py
    - tests/memory/test_phase48_1_memory_compat_alignment.py
  </read_first>
  <files>src/agent/graph_vocabulary.py, tests/architecture/test_canonical_graph_baseline.py, tests/agent/test_graph_vocabulary.py, tests/architecture/test_phase32_static_contract.py, tests/architecture/test_memory_contract_delta.py, tests/architecture/test_phase34_approval_action_boundaries.py, tests/memory/test_phase48_1_memory_compat_alignment.py</files>
  <behavior>
    - Test 1: `graph_add_node_names() == TARGET_CANONICAL_GRAPH_NODES` is asserted without `pytest.skip`.
    - Test 2: `graph_vocabulary_entry()` returns runtime entries only for current canonical nodes/routers; old names such as `classify_intent`, `extract_slots`, `generate_recommendation`, and `assess_risk_and_approval` are not active runtime vocabulary aliases.
    - Test 3: Historical stored-row projection, if retained, is exercised only through a separately named historical projection path and does not set `target_graph_status` to `compatibility_alias`.
  </behavior>
  <action>
    Implement D-58-01, D-58-02, D-58-04, D-58-05, and D-58-09. Replace `test_final_no_debt_gate_is_marked_phase58_scope` with live assertions that active `StateGraph.add_node(...)` names equal `TARGET_CANONICAL_GRAPH_NODES`, active route values exclude the Phase 50 legacy names, and `FORBIDDEN_MAIN_CHAIN_REGISTERED_NODES` remain absent. Refactor `src/agent/graph_vocabulary.py` so `_ENTRIES` contains current canonical runtime node/router names only. Remove `DELETE_BY_PHASE_58` reason-code constants and `compatibility_alias` rows from active vocabulary. If historical trace readability still needs legacy-to-target projection, create a separately named historical projection mapping used only by `project_trace_step_for_contract`; do not expose those rows through `graph_vocabulary_entry()` as active graph vocabulary. Treat `memory_write` as outside the current main-chain runtime vocabulary per Phase 50: remove it from active runtime entries or classify it as non-main/internal in a way that cannot satisfy final graph-node assertions. Update architecture/static tests that previously required Phase 52-57 compatibility aliases so they now assert canonical current runtime vocabulary and explicitly separate historical projection from current runtime semantics.
  </action>
  <acceptance_criteria>
    - `tests/architecture/test_canonical_graph_baseline.py::test_final_no_debt_gate_is_marked_phase58_scope` contains no `pytest.skip`.
    - `src/agent/graph_vocabulary.py` contains no `DELETE_BY_PHASE_58`.
    - `src/agent/graph_vocabulary.py` contains no active `_entry(..., "compatibility_alias", ...)` row for `classify_intent`, `session_memory_load`, `extract_slots`, `long_term_memory_retrieve`, `generate_recommendation`, `assess_risk_and_approval`, `route_after_intent`, or `route_after_slots`.
    - `tests/agent/test_graph_vocabulary.py` asserts canonical runtime identity for the 15 final nodes and current routers.
    - Static tests no longer require `PHASE_53_COMPATIBILITY_ALIAS`, `PHASE_54_COMPATIBILITY_ALIAS`, `PHASE_55_COMPATIBILITY_ALIAS`, `PHASE_56_COMPATIBILITY_ALIAS`, or `PHASE_57_COMPATIBILITY_ALIAS` in active vocabulary.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/agent/test_graph_vocabulary.py tests/architecture/test_phase32_static_contract.py tests/architecture/test_memory_contract_delta.py tests/architecture/test_phase34_approval_action_boundaries.py tests/memory/test_phase48_1_memory_compat_alignment.py -q --tb=short</automated>
  </verify>
  <done>Final graph/vocabulary no-debt assertions are live and old graph names are no longer active runtime compatibility aliases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add reusable Phase 58 legacy-hit classifier</name>
  <read_first>
    - .planning/phases/58-canonical-graph-cutover-and-no-debt-cleanup/58-CONTEXT.md
    - .planning/phases/58-canonical-graph-cutover-and-no-debt-cleanup/58-VALIDATION.md
    - .planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-VALIDATION.md
    - src/agent/graph.py
    - src/agent/routing.py
    - src/agent/graph_vocabulary.py
    - tests/architecture/test_canonical_graph_baseline.py
  </read_first>
  <files>scripts/classify_phase58_legacy_hits.py, tests/architecture/test_canonical_graph_baseline.py</files>
  <behavior>
    - Test 1: Running the classifier prints total hits, file count, category counts, active-runtime hit count, and unclassified row count.
    - Test 2: Generated Phase 58 validation artifacts and the classifier script itself are explicitly excluded or self-classified so they do not create recursive failures.
    - Test 3: `--strict` exits non-zero if any active-runtime or unclassified row exists.
  </behavior>
  <action>
    Implement D-58-10, D-58-11, and D-58-12. Create `scripts/classify_phase58_legacy_hits.py` with a `main()` entrypoint. Scan at least these legacy strings: `classify_intent`, `session_memory_load`, `extract_slots`, `long_term_memory_retrieve`, `generate_recommendation`, `assess_risk_and_approval`, `route_after_intent`, and `route_after_slots`. Include roots `README.md`, `docs`, `src`, `tests`, `frontend`, `scripts`, `eval`, `rules`, `.planning/ARCHITECTURE-DEBT.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/STATE.md`, and `.planning/phases/58-canonical-graph-cutover-and-no-debt-cleanup`. Exclude generated Phase 58 validation output and the classifier file from self-counting unless the row is intentionally categorized as classifier implementation. Classify rows into explicit categories such as `active_runtime_legacy`, `historical_data_read_projection`, `legacy_wrapper_or_import_test`, `previous_state_documentation`, `phase58_cleanup_artifact`, and `classifier_implementation`. The script must fail `--strict` when `active_runtime_legacy > 0` or `unclassified_rows > 0`. Add an architecture test that imports or subprocess-runs the classifier enough to prove the command is available and strict mode enforces those counters. Use only approved MOCA entrypoints in test and documentation command strings.
  </action>
  <acceptance_criteria>
    - `scripts/classify_phase58_legacy_hits.py` exists and exposes `main`.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/classify_phase58_legacy_hits.py --strict` is the documented classifier command.
    - Classifier output includes literal fields or labels for `total_hits`, `files`, `category_counts`, `active_runtime_legacy`, and `unclassified_rows`.
    - Classifier excludes `.planning/phases/58-canonical-graph-cutover-and-no-debt-cleanup/58-VALIDATION.md` from recursive self-counting.
    - A focused architecture test fails if classifier strict mode stops enforcing zero active-runtime and zero unclassified rows.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/classify_phase58_legacy_hits.py --strict</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py -q --tb=short</automated>
  </verify>
  <done>Phase 58 has a repeatable strict legacy-hit classifier ready for per-wave and final validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source graph -> static architecture tests | The tests decide whether current runtime graph identity is canonical. |
| Historical trace names -> graph vocabulary | Stored old names must not become active runtime vocabulary authority. |
| Static classifier -> phase closeout | Classifier output determines whether residual legacy-name rows are acceptable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01-01 | Tampering | `src/agent/graph_vocabulary.py` | mitigate | Remove active `compatibility_alias` rows and require canonical-only runtime vocabulary assertions. |
| T-58-01-02 | Repudiation | `tests/architecture/test_canonical_graph_baseline.py` | mitigate | Convert the skipped final gate into live assertions against `TARGET_CANONICAL_GRAPH_NODES`. |
| T-58-01-03 | Repudiation / Tampering | `scripts/classify_phase58_legacy_hits.py` | mitigate | Strict mode fails on active-runtime or unclassified legacy rows and excludes recursive generated artifacts. |
| T-58-01-04 | Information disclosure | historical trace projection | accept | Historical node names may remain in stored-row readability only; they are not secrets and must be labeled as historical data-read projection. |
</threat_model>

<verification>
Plan-local checks:
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/agent/test_graph_vocabulary.py tests/architecture/test_phase32_static_contract.py tests/architecture/test_memory_contract_delta.py tests/architecture/test_phase34_approval_action_boundaries.py tests/memory/test_phase48_1_memory_compat_alignment.py -q --tb=short`
- `UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/classify_phase58_legacy_hits.py --strict`
- `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/graph_vocabulary.py tests/architecture/test_canonical_graph_baseline.py tests/agent/test_graph_vocabulary.py tests/architecture/test_phase32_static_contract.py tests/architecture/test_memory_contract_delta.py tests/architecture/test_phase34_approval_action_boundaries.py tests/memory/test_phase48_1_memory_compat_alignment.py scripts/classify_phase58_legacy_hits.py`
</verification>

<source_audit>
GOAL covered: final active graph/vocabulary no-debt gate becomes machine-verifiable.
REQ covered: CAGM-09 active legacy node names and runtime compatibility aliases removed/internalized.
RESEARCH covered: Wave 0 final no-debt gate, `graph_vocabulary.py` alias cleanup, static legacy-hit classifier.
CONTEXT decisions covered: D-58-01, D-58-02, D-58-04, D-58-05, D-58-09, D-58-10, D-58-11, D-58-12.
Deferred ideas excluded: none listed in Phase 58 context.
</source_audit>

<success_criteria>
- Active graph nodes equal the Phase 50 canonical 15-node set.
- Graph vocabulary no longer advertises migration-era legacy names as active runtime aliases.
- Strict legacy-hit classifier exists and is usable through approved MOCA commands.
</success_criteria>

<output>
After completion, create `.planning/phases/58-canonical-graph-cutover-and-no-debt-cleanup/58-01-SUMMARY.md`.
</output>
