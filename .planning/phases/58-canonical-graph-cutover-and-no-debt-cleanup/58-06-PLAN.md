---
phase: 58-canonical-graph-cutover-and-no-debt-cleanup
plan: 06
type: execute
wave: 6
depends_on:
  - 58-05
files_modified:
  - tests/conftest.py
  - tests/agent/test_graph.py
  - tests/test_graph_routing.py
  - tests/agent/test_intent_routing.py
  - tests/agent/test_intent_golden_contract.py
  - tests/agent/test_required_slots.py
  - tests/agent/test_session_memory_integration.py
  - tests/agent/test_empty_session_adapter.py
autonomous: true
requirements:
  - CAGM-09
requirements_addressed:
  - CAGM-09
user_setup: []
must_haves:
  truths:
    - "Graph tests and shared fixtures patch canonical node modules only."
    - "Routing tests assert canonical route values and public canonical route helpers only."
    - "Shared fixtures contain no deleted wrapper imports or legacy route delegate imports."
  artifacts:
    - path: "tests/conftest.py"
      provides: "Shared canonical patch seams"
    - path: "tests/agent/test_graph.py"
      provides: "Canonical active graph behavior coverage"
    - path: "tests/test_graph_routing.py"
      provides: "Canonical routing and route-authority coverage"
  key_links:
    - from: "tests/agent/test_graph.py"
      to: "src/agent/graph.py"
      via: "canonical graph node and patch target assertions"
      pattern: "recommendation_generation|risk_gate"
    - from: "tests/test_graph_routing.py"
      to: "src/agent/routing.py"
      via: "canonical public route helper imports"
      pattern: "route_after_contextual_intent|route_after_slot_resolution"
---

<objective>
Migrate graph, routing, and shared fixture patch seams to canonical modules after wrapper/helper cleanup.

Purpose: CAGM-09 requires source tests and fixtures to stop preserving old graph node names as import or route compatibility paths.
Output: Canonical graph/routing tests, canonical fixture patch seams, and no public legacy route delegate imports.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58-canonical-graph-cutover-and-no-debt-cleanup/58-CONTEXT.md
@.planning/phases/58-canonical-graph-cutover-and-no-debt-cleanup/58-RESEARCH.md
@.planning/phases/58-canonical-graph-cutover-and-no-debt-cleanup/58-PATTERNS.md
@.planning/phases/58-canonical-graph-cutover-and-no-debt-cleanup/58-05-SUMMARY.md
@tests/conftest.py
@tests/agent/test_graph.py
@tests/test_graph_routing.py
@tests/agent/test_intent_routing.py
@tests/agent/test_intent_golden_contract.py
@tests/agent/test_required_slots.py
@tests/agent/test_session_memory_integration.py
@tests/agent/test_empty_session_adapter.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retarget graph tests and shared fixtures to canonical modules</name>
  <read_first>
    - .planning/phases/58-canonical-graph-cutover-and-no-debt-cleanup/58-05-SUMMARY.md
    - tests/conftest.py
    - tests/agent/test_graph.py
    - tests/agent/test_empty_session_adapter.py
  </read_first>
  <files>tests/conftest.py, tests/agent/test_graph.py, tests/agent/test_empty_session_adapter.py</files>
  <behavior>
    - Test 1: Graph tests patch `contextual_intent_resolve`, `slot_resolution_gate`, `memory_context_load`, `recommendation_generation`, and `risk_gate`, not deleted wrappers.
    - Test 2: Shared fixtures expose canonical node patch seams only.
    - Test 3: Empty-session adapter coverage contains no deleted wrapper import path.
  </behavior>
  <action>
    Implement D-58-04 and D-58-05 for graph tests and shared fixtures. Update `tests/conftest.py`, `tests/agent/test_graph.py`, and `tests/agent/test_empty_session_adapter.py` to call, patch, and assert canonical modules only. Retarget old intent helpers to `src.agent.nodes.contextual_intent_resolve`, old session helpers to `src.agent.nodes.session_context_load`, old slot helpers to `src.agent.nodes.slot_resolution_gate`, old memory retrieval helpers to `src.agent.nodes.memory_context_load`, recommendation helpers to `src.agent.nodes.recommendation_generation`, and risk helpers to `src.agent.nodes.risk_gate`. Preserve active graph semantics and do not add a risk self-loop.
  </action>
  <acceptance_criteria>
    - `tests/agent/test_graph.py` contains no `src.agent.nodes.classify_intent`, `src.agent.nodes.session_memory_load`, `src.agent.nodes.extract_slots`, `src.agent.nodes.long_term_memory_retrieve`, `src.agent.nodes.generate_recommendation`, or `src.agent.nodes.assess_risk_and_approval` patch target.
    - `tests/conftest.py` contains no deleted wrapper module import.
    - `tests/agent/test_empty_session_adapter.py` contains no deleted wrapper module import.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_graph.py tests/agent/test_empty_session_adapter.py -q --tb=short</automated>
  </verify>
  <done>Graph tests and shared fixtures use canonical node patch seams only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retarget routing tests to canonical helpers and values</name>
  <read_first>
    - tests/test_graph_routing.py
    - tests/agent/test_intent_routing.py
    - tests/agent/test_intent_golden_contract.py
    - tests/agent/test_required_slots.py
    - tests/agent/test_session_memory_integration.py
  </read_first>
  <files>tests/test_graph_routing.py, tests/agent/test_intent_routing.py, tests/agent/test_intent_golden_contract.py, tests/agent/test_required_slots.py, tests/agent/test_session_memory_integration.py</files>
  <behavior>
    - Test 1: Routing tests assert canonical route values and contain no public `route_after_intent` or `route_after_slots` imports.
    - Test 2: Routing tests contain no patch target for `generate_recommendation` or `assess_risk_and_approval`.
    - Test 3: Missing-slot, clarification, final-response, recommendation, and risk routes keep existing behavior with canonical names.
  </behavior>
  <action>
    Implement D-58-04 and D-58-05 for routing tests. Update `tests/test_graph_routing.py`, `tests/agent/test_intent_routing.py`, `tests/agent/test_intent_golden_contract.py`, `tests/agent/test_required_slots.py`, and `tests/agent/test_session_memory_integration.py` to import public canonical route helpers such as `route_after_contextual_intent` and `route_after_slot_resolution`, and to assert canonical route values only. Remove compatibility assertions that existed solely to keep old route helper names or wrapper imports valid. Preserve active graph behavior for clarification, final response, recommendation generation, risk gate, and fail-closed invalid policy cases.
  </action>
  <acceptance_criteria>
    - `tests/test_graph_routing.py` asserts `recommendation_generation` and `risk_gate` as current route values.
    - `tests/test_graph_routing.py`, `tests/agent/test_intent_routing.py`, `tests/agent/test_intent_golden_contract.py`, `tests/agent/test_required_slots.py`, and `tests/agent/test_session_memory_integration.py` do not import public `route_after_intent` or `route_after_slots`.
    - No routing test asserts an active route destination of `classify_intent`, `session_memory_load`, `extract_slots`, `long_term_memory_retrieve`, `generate_recommendation`, or `assess_risk_and_approval`.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_graph_routing.py tests/agent/test_intent_routing.py tests/agent/test_intent_golden_contract.py tests/agent/test_required_slots.py tests/agent/test_session_memory_integration.py -q --tb=short</automated>
  </verify>
  <done>Routing tests use canonical helper names and canonical route values only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures -> production node modules | Fixtures must patch canonical modules without preserving deleted import paths. |
| Routing tests -> graph route authority | Tests must not continue to bless legacy route helper names or destinations. |
| Graph tests -> active runtime semantics | Test migration must preserve behavior while removing compatibility imports. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-06-01 | Tampering | `tests/conftest.py` and graph tests | mitigate | Require canonical patch seams and no deleted wrapper imports. |
| T-58-06-02 | Spoofing / Tampering | routing tests | mitigate | Remove public legacy route delegate imports and assert canonical route values only. |
| T-58-06-03 | Repudiation | active graph behavior tests | mitigate | Keep graph/routing focused suites in verification after patch seam migration. |
</threat_model>

<verification>
Plan-local checks:
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_graph.py tests/test_graph_routing.py tests/agent/test_intent_routing.py tests/agent/test_intent_golden_contract.py tests/agent/test_required_slots.py tests/agent/test_session_memory_integration.py tests/agent/test_empty_session_adapter.py -q --tb=short`
- `UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/classify_phase58_legacy_hits.py --strict`
</verification>

<source_audit>
GOAL covered: graph/routing/shared fixture import-test cleanup.
REQ covered: CAGM-09 active legacy node names, route delegates, and runtime compatibility aliases removed/internalized.
RESEARCH covered: graph/routing patch seam cleanup and route delegate removal coverage.
CONTEXT decisions covered: D-58-04, D-58-05, D-58-10, D-58-12.
Deferred ideas excluded: no production data rewrite and no new graph behavior.
</source_audit>

<success_criteria>
- Graph tests and shared fixtures patch canonical modules only.
- Routing tests import canonical public route helpers and assert canonical values only.
- Focused graph/routing suites pass through approved MOCA commands.
</success_criteria>

<output>
After completion, create `.planning/phases/58-canonical-graph-cutover-and-no-debt-cleanup/58-06-SUMMARY.md`.
</output>
