---
phase: 58-canonical-graph-cutover-and-no-debt-cleanup
plan: 04
type: execute
wave: 3
depends_on:
  - 58-01
  - 58-02
files_modified:
  - src/api/routers/approvals.py
  - src/agent/graph.py
  - tests/test_approval_api.py
  - tests/test_approval_gate.py
  - tests/test_graph_routing.py
  - tests/approvals/test_needs_info_resume.py
  - tests/approvals/test_service_transitions.py
autonomous: true
requirements:
  - CAGM-09
requirements_addressed:
  - CAGM-09
user_setup: []
must_haves:
  truths:
    - "Approval resume authority accepts only canonical `risk_gate` as graph route authority."
    - "Persisted historical retry metadata can be read only through a bounded server-side compatibility path, if retained, and emits canonical `risk_gate`."
    - "Fresh/current legacy resume values fail closed and cannot route the graph."
  artifacts:
    - path: "src/api/routers/approvals.py"
      provides: "Approval retry reconstruction and resume-route canonicalization boundary"
    - path: "src/agent/graph.py"
      provides: "Canonical approval resume routing gate"
    - path: "tests/test_approval_api.py"
      provides: "API regression coverage for persisted retry and fresh legacy route rejection"
  key_links:
    - from: "src/api/routers/approvals.py"
      to: "src/agent/graph.py"
      via: "TrustedApprovalResultV1.resume_route"
      pattern: "risk_gate"
---

<objective>
Harden approval retry data-read compatibility and route authority after legacy risk wrapper deletion.

Purpose: D-58-06 allows historical persisted retry rows to remain readable, but they must never authorize a legacy graph route.
Output: Bounded approval retry canonicalization, route-authority tests, and no active legacy resume route.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/58-canonical-graph-cutover-and-no-debt-cleanup/58-CONTEXT.md
@.planning/phases/58-canonical-graph-cutover-and-no-debt-cleanup/58-RESEARCH.md
@.planning/phases/58-canonical-graph-cutover-and-no-debt-cleanup/58-PATTERNS.md
@.planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
@.planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-03-SUMMARY.md
@.planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-05-SUMMARY.md
@src/api/routers/approvals.py
@src/agent/graph.py
@tests/test_approval_api.py
@tests/test_approval_gate.py
@tests/test_graph_routing.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bound persisted retry compatibility to data-read canonicalization</name>
  <read_first>
    - .planning/phases/58-canonical-graph-cutover-and-no-debt-cleanup/58-CONTEXT.md
    - .planning/phases/58-canonical-graph-cutover-and-no-debt-cleanup/58-RESEARCH.md
    - .planning/phases/57-risk-gate-and-approval-gate-canonicalization/57-03-SUMMARY.md
    - src/api/routers/approvals.py
    - tests/test_approval_api.py
    - tests/approvals/test_needs_info_resume.py
    - tests/approvals/test_service_transitions.py
  </read_first>
  <files>src/api/routers/approvals.py, tests/test_approval_api.py, tests/approvals/test_needs_info_resume.py, tests/approvals/test_service_transitions.py</files>
  <behavior>
    - Test 1: A persisted retry event containing historical `resume_route="assess_risk_and_approval"` is accepted only inside server-side retry reconstruction after existing approval/run/hash/snapshot/version checks and emits `resume_route="risk_gate"`.
    - Test 2: Fresh/current request or service payloads containing `resume_route="assess_risk_and_approval"` are rejected with the existing conflict/fail-closed behavior.
    - Test 3: No API response exposes a legacy resume route as graph authority.
  </behavior>
  <action>
    Implement D-58-06. Replace `LEGACY_RISK_ROUTE` active-looking compatibility with a clearly named historical data-read mapping such as `HISTORICAL_RETRY_ROUTE_TO_CANONICAL = {"assess_risk_and_approval": "risk_gate"}` scoped to persisted approval event metadata reconstruction only. Keep canonical `CANONICAL_RISK_ROUTE = "risk_gate"` as the only route value allowed to leave `_terminal_decision_result_for_retry(...)`, `_should_resume_graph(...)`, and API response payloads. If source review proves old persisted rows no longer need read compatibility, delete the legacy-route acceptance branch and update tests to prove old retry rows fail closed; otherwise keep the bounded branch and name it historical persisted-row read compatibility. Preserve tenant/run/approval id, payload hash, snapshot ref/hash, request version, level version, and assignment version validation before any resume payload is constructed.
  </action>
  <acceptance_criteria>
    - `src/api/routers/approvals.py` contains no `LEGACY_RISK_ROUTE` constant.
    - Any remaining literal `assess_risk_and_approval` in `src/api/routers/approvals.py` is adjacent to `HISTORICAL_RETRY_ROUTE_TO_CANONICAL` or equivalent persisted-row read wording.
    - `tests/test_approval_api.py` has one test for persisted historical retry canonicalization to `risk_gate` if the read-compatibility branch remains.
    - `tests/test_approval_api.py` has one test proving fresh/current legacy resume route input fails closed.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_approval_api.py tests/approvals/test_needs_info_resume.py tests/approvals/test_service_transitions.py -q --tb=short</automated>
  </verify>
  <done>Approval API retry compatibility is bounded to persisted-row data reads and emits canonical `risk_gate` only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove graph resume route authority is canonical-only</name>
  <read_first>
    - src/agent/graph.py
    - src/api/routers/approvals.py
    - tests/test_graph_routing.py
    - tests/test_approval_gate.py
    - tests/test_approval_api.py
    - tests/approvals/test_needs_info_resume.py
    - tests/approvals/test_service_transitions.py
  </read_first>
  <files>src/agent/graph.py, src/api/routers/approvals.py, tests/test_graph_routing.py, tests/test_approval_gate.py, tests/test_approval_api.py, tests/approvals/test_needs_info_resume.py, tests/approvals/test_service_transitions.py</files>
  <behavior>
    - Test 1: `route_after_approval` routes edited superseded approvals to `risk_gate` only when trusted result bindings and new action payload hash are valid.
    - Test 2: A trusted-looking result with legacy `resume_route="assess_risk_and_approval"` routes to `final_response` or otherwise fails closed.
    - Test 3: Approval gate tests do not patch deleted legacy risk modules.
  </behavior>
  <action>
    Tighten graph/API authority tests so `route_after_approval`, `_should_resume_graph`, and approval-gate resume fixtures accept only canonical `risk_gate` as current route authority. Remove or migrate tests that patch `src.agent.nodes.assess_risk_and_approval`; use `src.agent.nodes.risk_gate` instead. Keep ordinary chat approval text untrusted and preserve Phase 57 risk/approval separation: `approval_gate` handles request/resume state, while `risk_gate` owns risk/action policy. Do not add a risk self-loop or direct route to `action_draft` from edited approvals without `risk_gate`.
  </action>
  <acceptance_criteria>
    - `tests/test_graph_routing.py` asserts legacy `resume_route="assess_risk_and_approval"` does not route to that legacy value.
    - `tests/test_approval_gate.py` contains no `src.agent.nodes.assess_risk_and_approval` patch target.
    - `src/agent/graph.py` route map for `approval_gate` contains `"risk_gate": "risk_gate"` and no legacy risk destination.
    - Approval tests still pass for accept/approve/reject/respond/edit lifecycle outcomes.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_approval_api.py tests/test_approval_gate.py tests/test_graph_routing.py tests/approvals/test_needs_info_resume.py tests/approvals/test_service_transitions.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/api/routers/approvals.py src/agent/graph.py tests/test_approval_api.py tests/test_approval_gate.py tests/test_graph_routing.py tests/approvals/test_needs_info_resume.py tests/approvals/test_service_transitions.py</automated>
  </verify>
  <done>Graph resume route authority is canonical-only, with approval lifecycle tests passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted approval metadata -> API resume payload | Stored metadata is untrusted until server-side approval/run/hash/snapshot/version checks pass. |
| API resume payload -> LangGraph route | Only canonical `risk_gate` may become graph route authority. |
| Ordinary chat -> approval resume | Chat text cannot create trusted approval results. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-04-01 | Spoofing / Elevation of privilege | `src/api/routers/approvals.py` | mitigate | Bound historical retry route mapping to persisted-row reconstruction after binding checks and emit `risk_gate` only. |
| T-58-04-02 | Elevation of privilege | `src/agent/graph.py::route_after_approval` | mitigate | Route edited approvals to `risk_gate` only with valid trusted result and new action payload hash. |
| T-58-04-03 | Tampering | approval API tests | mitigate | Add fresh/current legacy resume rejection coverage. |
| T-58-04-04 | Repudiation | retry compatibility comments | mitigate | Rename compatibility as historical data-read compatibility, not graph vocabulary compatibility. |
</threat_model>

<verification>
Plan-local checks:
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_approval_api.py tests/test_approval_gate.py tests/test_graph_routing.py tests/approvals/test_needs_info_resume.py tests/approvals/test_service_transitions.py -q --tb=short`
- `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/api/routers/approvals.py src/agent/graph.py tests/test_approval_api.py tests/test_approval_gate.py tests/test_graph_routing.py tests/approvals/test_needs_info_resume.py tests/approvals/test_service_transitions.py`
- `UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/classify_phase58_legacy_hits.py --strict`
</verification>

<source_audit>
GOAL covered: approval retry compatibility and route authority are canonical-only for current graph resume.
REQ covered: CAGM-09 active legacy route names removed/internalized with no dual runtime route.
RESEARCH covered: approval retry canonicalization safety and historical persisted metadata boundary.
CONTEXT decisions covered: D-58-03, D-58-04, D-58-06, D-58-10, D-58-12.
Deferred ideas excluded: no production row rewrite and no approval/action contract expansion.
</source_audit>

<success_criteria>
- Historical retry read compatibility, if retained, emits canonical `risk_gate` only.
- Fresh/current legacy resume routes fail closed.
- Approval graph route authority remains deterministic and canonical-only.
</success_criteria>

<output>
After completion, create `.planning/phases/58-canonical-graph-cutover-and-no-debt-cleanup/58-04-SUMMARY.md`.
</output>
