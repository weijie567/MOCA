---
phase: 29-tool-platform-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tools/contracts.py
  - src/tools/policy.py
  - src/tools/platform.py
  - src/tools/runtime.py
  - src/tools/projection.py
  - src/tools/manager.py
  - src/replay/decision_events.py
  - src/replay/validators.py
  - src/db/models.py
  - src/db/migrations/versions/017_tool_policy_events.py
  - src/agent/nodes/investigate.py
  - src/conversation/service.py
  - tests/tools/test_tool_platform.py
  - tests/replay/test_tool_policy_events.py
  - tests/replay/test_decision_events.py
  - tests/replay/test_replay_migration_contract.py
  - tests/agent/test_tools/test_unified_tool_manager.py
  - tests/agent/test_nodes/test_investigate.py
  - tests/conversation/test_service.py
  - tests/architecture/test_tool_boundaries.py
autonomous: true
requirements: [APF-06, APF-07]
must_haves:
  truths:
    - "Planner-visible tools come only from ToolPlatform.visible_tools(...) as prompt-safe ToolViewV1 projections derived from ToolDescriptor."
    - "ToolViewV1 exposes only name, description, prompt-safe input_schema, safe_usage_notes, and result_contract_version."
    - "Visibility requires both policy visibility and runtime availability; hidden and unavailable catalog tools are recorded outside planner prompts."
    - "Every ToolPlatform.invoke(...) call creates and enforces a fresh ToolPolicyDecision(decision_stage='runtime_auth') before executor dispatch."
    - "Runtime authorization checks caller, permission, explicit merchant scope, side-effect class, approval/safety/idempotency required fields, input schema, output schema, and availability."
    - "ToolPolicyDecision is embedded into Phase 28 decision events through emit_decision_event(...); no parallel envelope or event table exists."
    - "Graph state, prompt state, and conversation normalized storage consume ToolResultProjector outputs, never unprojected ToolResultV2.data."
  artifacts:
    - path: "src/tools/contracts.py"
      provides: "ToolViewV1, ToolPolicyDecision, ToolInvocationOutcome, and ToolResultProjectionV1 contracts"
      exports: ["ToolViewV1", "ToolPolicyDecision", "ToolInvocationOutcome", "ToolResultProjectionV1"]
    - path: "src/tools/policy.py"
      provides: "ToolPolicyEngine, core reason-code validation, visibility/runtime auth decisions, prompt-safe schema projection, and resource binding"
      exports: ["ToolPolicyEngine", "TOOL_POLICY_CORE_REASON_CODES", "validate_tool_policy_reason_codes"]
    - path: "src/tools/runtime.py"
      provides: "ToolRuntime hard boundary chain before and after executor dispatch"
      exports: ["ToolRuntime"]
    - path: "src/tools/platform.py"
      provides: "Graph-facing ToolPlatform facade with visible_tools(...) and invoke(...)"
      exports: ["ToolPlatform"]
    - path: "src/tools/projection.py"
      provides: "ToolResultProjector four-layer projection contract"
      exports: ["ToolResultProjector"]
    - path: "src/tools/manager.py"
      provides: "UnifiedToolManager compatibility adapter delegating to ToolPlatform"
      exports: ["UnifiedToolManager"]
    - path: "src/replay/validators.py"
      provides: "Registered tool policy visibility/runtime event types and retention classification"
    - path: "src/db/models.py"
      provides: "AgentTraceEvent event_type check constraint updated for tool policy decision event types"
    - path: "src/db/migrations/versions/017_tool_policy_events.py"
      provides: "Alembic migration replacing ck_agent_trace_events_event_type with tool policy event types"
    - path: "src/agent/nodes/investigate.py"
      provides: "Investigate integration through ToolPlatform.visible_tools(...) and ToolPlatform.invoke(...)"
    - path: "src/conversation/service.py"
      provides: "Conversation tool-result writes using projector-normalized data and prompt projection"
    - path: "tests/tools/test_tool_platform.py"
      provides: "APF-06/APF-07 contract and runtime policy coverage"
    - path: "tests/replay/test_tool_policy_events.py"
      provides: "Tool policy event payload/redaction/registry coverage"
  key_links:
    - from: "src/agent/nodes/investigate.py"
      to: "src/tools/platform.py"
      via: "await platform.visible_tools(caller='investigate', ctx=...) and await platform.invoke(...)"
      pattern: "ToolPlatform|visible_tools\\(|\\.invoke\\("
    - from: "src/tools/platform.py"
      to: "src/tools/policy.py"
      via: "ToolPlatform delegates visibility and runtime_auth decisions to ToolPolicyEngine"
      pattern: "ToolPolicyEngine"
    - from: "src/tools/runtime.py"
      to: "src/replay/decision_events.py"
      via: "runtime emits tool_policy_runtime_auth_recorded with ToolPolicyDecision redacted payload"
      pattern: "emit_decision_event"
    - from: "src/tools/platform.py"
      to: "src/replay/decision_events.py"
      via: "visible_tools emits batched tool_policy_visibility_recorded event when a session/replay identity is available"
      pattern: "tool_policy_visibility_recorded"
    - from: "src/tools/runtime.py"
      to: "src/tools/projection.py"
      via: "executor ToolResultV2 is projected before the outcome leaves ToolRuntime"
      pattern: "ToolResultProjector"
    - from: "src/conversation/service.py"
      to: "src/tools/projection.py"
      via: "append_tool_result uses supplied ToolResultProjectionV1 or builds one through ToolResultProjector"
      pattern: "ToolResultProjector|normalized_result"
---

<objective>
Create the Phase 29 tool platform boundary for APF-06 and APF-07.

Purpose: Replace scattered tool allowlists and raw descriptor prompt surfaces with a descriptor-derived `ToolPlatform` facade that separates planner visibility, runtime authorization, execution, result projection, and replay decision-event recording.

Output: Test-first Phase 29 contract coverage; `ToolViewV1`; `ToolPolicyDecision`; `ToolPolicyEngine`; `ToolRuntime`; `ToolResultProjector`; `ToolPlatform`; explicit tool-policy decision event types; a `UnifiedToolManager` compatibility adapter; and narrow investigate/conversation integrations.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-tool-platform-boundary/29-CONTEXT.md
@.planning/phases/29-tool-platform-boundary/29-RESEARCH.md
@.planning/phases/29-tool-platform-boundary/29-VALIDATION.md
@.planning/phases/29-tool-platform-boundary/29-PATTERNS.md
@.planning/phases/28-decision-event-foundation/28-CONTEXT.md
@.planning/phases/28-decision-event-foundation/28-01-PLAN.md
@docs/contract-spec.md
@docs/target-agent-platform-architecture-plan.md
@src/tools/catalog.py
@src/tools/contracts.py
@src/tools/manager.py
@src/tools/validation.py
@src/tools/manager_results.py
@src/platform/context_projections.py
@src/platform/trusted_context.py
@src/replay/decision_events.py
@src/replay/validators.py
@src/db/models.py
@src/db/migrations/versions/010_replay_event_v3.py
@src/db/migrations/versions/016_agent_run_memory_idempotency.py
@src/agent/nodes/investigate.py
@src/conversation/service.py
@tests/tools/test_catalog.py
@tests/agent/test_tools/test_unified_tool_manager.py
@tests/agent/test_nodes/test_investigate.py
@tests/replay/test_decision_events.py
@tests/replay/test_replay_migration_contract.py
@tests/conversation/test_service.py
@tests/architecture/test_tool_boundaries.py

<interfaces>
Key contracts and implementation anchors the executor must preserve:

From `src/tools/catalog.py`:
```python
class ToolDescriptor(BaseModel):
    name: str
    description: str = ""
    kind: Literal["read", "retrieval", "write"]
    input_schema: dict[str, Any]
    output_schema: dict[str, Any]
    risk_level: Literal["read", "retrieval", "write"]
    side_effect: Literal["none", "read_only", "retrieval", "write"]
    required_permission: str
    caller_allowlist: list[str]
    event_family: Literal["tool_call_*", "rag_retrieval_*", "action"] | None
    resource_type: str | None
    executor: Literal["business", "knowledge", "memory", "action"] | None = None
    exposure: Literal["planner_visible", "node_only", "internal"] = "planner_visible"
    requires_approval: bool = False
    requires_safety_snapshot: bool = False
    requires_idempotency_key: bool = False
```

From `src/tools/contracts.py`:
```python
class ToolCallContext(BaseModel):
    schema_version: Literal["tool_context.v2"] = "tool_context.v2"
    tenant_id: str
    user_id: str
    role: str
    permissions: list[str]
    merchant_scope: dict[str, Any] | list[str]
    thread_id: str
    run_id: str
    trace_id: str
    request_id: str
    tool_call_id: str
    caller_node: str
    deadline_at: datetime | None = None
    effective_at: str | None = None
    attempt: int = 1
    max_attempts: int = 1
    idempotency_key: str | None = None
    approval_ref: str | None = None
    safety_snapshot_ref: str | None = None
    policy_snapshot_ref: str | None = None

class ToolResultV2(BaseModel):
    schema_version: Literal["tool_result.v2"] = "tool_result.v2"
    status: Literal["success", "partial_success", "not_found", "permission_denied", "timeout", "unavailable", "conflict", "invalid_request", "invalid_response", "error"]
    data: dict[str, Any] | None
    summary: str
    source_system: str
    policy_evidence_refs: list[EvidenceRefV1]
    business_fact_refs: list[BusinessFactRefV1]
```

From `src/replay/decision_events.py`:
```python
async def emit_decision_event(
    session: AsyncSession,
    *,
    replay_context: ReplayContext | None = None,
    event_type: str,
    actor: dict[str, Any],
    resource_refs: dict[str, Any],
    redacted_payload: dict[str, Any],
    reason_code: str | None = None,
    reason_codes: list[str] | None = None,
    versions: Mapping[str, str | None] | None = None,
) -> dict[str, Any]: ...
```

Current replacement points:
- `src/tools/manager.py::UnifiedToolManager.descriptors("investigate")` currently returns raw `ToolDescriptor` objects.
- `src/tools/manager.py::UnifiedToolManager.invoke(...)` currently owns runtime checks procedurally and returns `ToolResultV2`.
- `src/agent/nodes/investigate.py` currently uses `ALLOWLIST`, `manager.descriptors("investigate")`, `manager.descriptor(...)`, and `manager.invoke(...)`.
- `src/agent/nodes/investigate.py::_accumulate_tool_result(...)` currently reads `result.data` for business facts, case memory, retrieval status, and score.
- `src/conversation/service.py::append_tool_result(...)` currently sets `normalized_result_json = result.data or {}`.
</interfaces>
</context>

<source_audit>
## Primary Goal And Requirements

| Source | ID | Requirement / Decision | Plan Task(s) | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | ROADMAP-29 | Replace scattered tool allowlists with descriptor-driven planner views, runtime authorization, result projection, and decision events. | 1-6 | COVERED | ToolView, policy engine, runtime, projector, event registry, manager adapter, and investigate/conversation wiring are all planned. |
| REQ | APF-06 | Planner visibility from ToolDescriptor into prompt-safe ToolView, not raw descriptors/adapters/internal policy fields/side effects. | 1, 2, 4, 5 | COVERED | `ToolViewV1` and `ToolPlatform.visible_tools(...)` replace raw descriptor prompt use. |
| REQ | APF-07 | Runtime invocation emits ToolPolicyDecision and rechecks authorization, resource scope, side-effect class, input/output schema even when visible. | 1, 2, 3, 4, 5 | COVERED | `ToolRuntime.invoke(...)` enforces a fresh runtime_auth decision and emits Phase 28 decision events. |
| ROADMAP | SC-01 | Planner-visible tools are prompt-safe ToolView projections derived from ToolDescriptor. | 1, 2, 4, 5 | COVERED | Tests assert minimal fields and schema projection. |
| ROADMAP | SC-02 | Runtime invocation rechecks caller, permission, resource scope, side-effect class, and schema validation. | 1, 2, 4 | COVERED | Runtime denial matrix covers caller, permission, merchant scope, side effect, schema, approval, safety, idempotency, and availability. |
| ROADMAP | SC-03 | Tests prove visible does not imply allowed, hidden/write tools do not leak into planner prompts, and raw adapter payloads stay out of graph state. | 1, 4, 5, 6 | COVERED | Wave 0 creates these tests first; later tasks make them pass. |

## Phase 29 Locked Decisions

| Source | ID | Requirement / Decision | Plan Task(s) | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| CONTEXT | D-01 | Minimal ToolViewV1 fields: name, description, prompt-safe input_schema, safe_usage_notes, result_contract_version. | 1, 2, 4 | COVERED | `ToolViewV1` strict model exposes only these fields. |
| CONTEXT | D-02 | Raw policy/runtime fields do not enter planner view. | 1, 2, 5 | COVERED | Prompt leakage tests check forbidden field names. |
| CONTEXT | D-03 | ToolView input_schema is prompt-safe projection retaining field essentials. | 1, 2 | COVERED | `project_prompt_safe_input_schema(...)` uses a recursive key allowlist. |
| CONTEXT | D-04 | Strip defaults/examples/internal validation/permission/resource/adapter details from input_schema. | 1, 2 | COVERED | Schema projection tests include forbidden recursive keys. |
| CONTEXT | D-05 | ToolView is not authorization; runtime_auth still required. | 1, 2, 4 | COVERED | Visible-but-denied tests prove runtime auth runs after visibility. |
| CONTEXT | D-06 | Planner visibility equals policy visibility intersected with runtime availability. | 1, 2, 4 | COVERED | Availability checks use executor presence and `executor.has_tool(name)`. |
| CONTEXT | D-07 | Hidden, denied, and unavailable remain distinct states. | 1, 2, 4 | COVERED | Reason codes distinguish `hidden_by_policy`, runtime denial codes, and `tool_unavailable`. |
| CONTEXT | D-08 | visible_tools records full low-payload visibility decisions outside prompt. | 1, 3, 4 | COVERED | `tool_policy_visibility_recorded` batches all catalog decisions; prompt receives views only. |
| CONTEXT | D-09 | ToolPolicyDecision is domain object, not event envelope. | 1, 2, 3 | COVERED | Model excludes event_id, sequence, occurred_at, run_id, tenant_id. |
| CONTEXT | D-10 | Embed ToolPolicyDecision through Phase 28 emit_decision_event path; no parallel event system. | 1, 3, 4 | COVERED | Explicit events are registered on `agent_trace_events`; no new table/envelope. |
| CONTEXT | D-11 | Visibility decisions may be batched; runtime auth usually one per invoke. | 1, 3, 4 | COVERED | Batch visibility event and per-invocation runtime event tests. |
| CONTEXT | D-12 | Core reason code enum is stable and contract-tested. | 1, 2 | COVERED | Core codes listed and enforced in `src/tools/policy.py`. |
| CONTEXT | D-13 | Extension reason codes use `<namespace>.<snake_case>`; freeform unknown forbidden in tool-policy paths. | 1, 2 | COVERED | Tool-policy validator accepts namespaced extensions and rejects unknown non-namespaced tool-policy codes. |
| CONTEXT | D-14 | Visibility-stage decisions do not emit runtime-only reason codes. | 1, 2 | COVERED | Policy tests assert visibility reasons exclude schema/approval/safety/idempotency. |
| CONTEXT | D-15 | policy_version comes from ToolPolicyEngine. | 1, 2, 4 | COVERED | Engine owns `policy_version="tool_policy.v1"` and ignores caller-supplied policy version. |
| CONTEXT | D-16 | data_classification comes from descriptor/default, not LLM/args. | 1, 2 | COVERED | Engine maps descriptor default to `internal` unless descriptor is extended later. |
| CONTEXT | D-17 | resource_scope_binding is produced by runtime from args plus ToolCallContext. | 1, 2, 4 | COVERED | Runtime auth decision binds merchant/order/refund/ticket identifiers. |
| CONTEXT | D-18 | Runtime denial returns safe ToolResultV2 error and writes runtime auth event. | 1, 3, 4 | COVERED | Denied tests check no executor dispatch, safe error, and event emission. |
| CONTEXT | D-19 | Programmer/config failures may raise and are caught by tests. | 1, 2, 4 | COVERED | Invalid policy construction and missing event identity tests raise. |
| CONTEXT | D-20 | Establish ToolPlatform, ToolPolicyEngine, ToolRuntime, ToolResultProjector. | 2, 4 | COVERED | New modules/classes are named explicitly. |
| CONTEXT | D-21 | Boundaries are minimum public contracts, not every future capability. | 2, 4, 6 | COVERED | Scope excludes generic frameworks/artifact store while delivering named boundary methods. |
| CONTEXT | D-22 | ToolPlatform is graph-facing facade with visible_tools(...) and invoke(...). | 4, 5 | COVERED | Investigate uses `ToolPlatform` directly; manager delegates. |
| CONTEXT | D-23 | ToolPolicyEngine owns visibility/runtime auth only. | 2, 4 | COVERED | It does not execute, write graph state, persist conversation records, or project prompts. |
| CONTEXT | D-24 | ToolRuntime owns schema validation, runtime_auth, gates, dispatch, output validation, projection, safe errors, event emission. | 4 | COVERED | Task 4 action enumerates exact chain order. |
| CONTEXT | D-25 | Reuse deadline_at/max_attempts; no retry/rate-limit/timeout/feature flag/artifact-store frameworks. | 4, 5 | COVERED | Runtime reads existing context fields and does not add framework files. |
| CONTEXT | D-26 | UnifiedToolManager is legacy compatibility adapter delegating to ToolPlatform. | 5 | COVERED | Manager tests assert delegation and no new policy logic. |
| CONTEXT | D-27 | Existing executors stay thin; do not rewrite domain internals or Phase 30 scope into runtime. | 4, 5 | COVERED | ToolRuntime orchestrates executors but leaves service internals alone. |
| CONTEXT | D-28 | Investigate migration limited to visible_tools and invoke integration points. | 5 | COVERED | Plan preserves bounded loop and `plan_next_step(...)`. |
| CONTEXT | D-29 | Bounded loop, plan_next_step, termination, executors, state accumulation remain compatible; broader planner loop waits for Phase 32. | 5, 6 | COVERED | Tests update integration boundaries without rewriting loop semantics. |
| CONTEXT | D-30 | Runtime scope auth performs platform-layer resource binding and clear scope-deny checks only. | 2, 4 | COVERED | Explicit merchant mismatch deny; domain lookups marked incomplete. |
| CONTEXT | D-31 | Bind tenant_id, merchant_id, order_id/order_no, refund_id/refund_case_no, ticket_id. | 1, 2, 4 | COVERED | Resource binding tests cover all current arg shapes. |
| CONTEXT | D-32 | Explicit out-of-scope merchant_id denies before executor dispatch. | 1, 2, 4 | COVERED | Runtime scope tests assert executor not called. |
| CONTEXT | D-33 | Domain lookup ids mark incomplete/requires_domain_scope_check. | 1, 2, 4 | COVERED | Binding includes `requires_domain_scope_check=true` for order/refund/ticket. |
| CONTEXT | D-34 | Domain ownership/freshness/anti-enumeration/current business fact authority belong to Phase 30. | 2, 4 | DEFERRED | Explicitly deferred to Phase 30 per user decision; no fake ownership check planned. |
| CONTEXT | D-35 | ToolResultProjector emits normalized, prompt projection/text, audit/resource refs, debug projection. | 1, 4, 6 | COVERED | `ToolResultProjectionV1` exposes these layers. |
| CONTEXT | D-36 | raw_artifact_ref/hash optional only; no DB schema/artifact store. | 4, 6 | COVERED | Existing raw ref/hash fields are reused; no new artifact table. |
| CONTEXT | D-37 | ToolResultV2.data is raw-ish for every tool kind. | 1, 4, 6 | COVERED | Tests seed raw sentinel payloads across result kinds. |
| CONTEXT | D-38 | Graph state must not receive unprojected ToolResultV2.data. | 1, 5, 6 | COVERED | Investigate accumulation uses projection normalized/prompt surfaces. |
| CONTEXT | D-39 | Prompt projection is structured first with bounded text_for_prompt. | 1, 4, 6 | COVERED | Projector produces `prompt_projection` and `text_for_prompt`. |
| CONTEXT | D-40 | Prompts do not consume normalized_result or raw data directly. | 1, 5, 6 | COVERED | Prompt tests inspect ToolView/projection surfaces only. |
| CONTEXT | D-41 | Projector does not own event emission; policy/runtime write events. | 3, 4 | COVERED | Projector returns refs; runtime/platform emit policy events. |
| CONTEXT | D-42 | Projection-level events are out of scope and may be reconsidered in Phase 35. | 4, 6 | DEFERRED | Explicitly deferred to Phase 35; no projection event type planned. |

## Research, Validation, And Pattern Map Coverage

| Source | ID | Finding / Risk / Recommendation | Plan Task(s) | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| RESEARCH | R-01 | Boundary refactor, not external runtime; keep existing catalog/executor/event seams. | 2-6 | COVERED | No new external service or dynamic discovery. |
| RESEARCH | R-02 | Establish ToolPlatform, ToolPolicyEngine, ToolRuntime, ToolResultProjector around existing seams. | 2, 4 | COVERED | Direct new module/class targets. |
| RESEARCH | R-03 | Replace `UnifiedToolManager.descriptors("investigate")` raw descriptor prompt path. | 1, 5 | COVERED | Investigate uses ToolViews only. |
| RESEARCH | R-04 | Replace investigate `ALLOWLIST` planner authorization source without rewriting loop semantics. | 5 | COVERED | Validation uses visible ToolView names and runtime auth, not `ALLOWLIST`. |
| RESEARCH | R-05 | Add explicit event types if chosen and update validators, ORM check, migration, replay migration tests. | 3 | COVERED | Chooses `tool_policy_visibility_recorded` and `tool_policy_runtime_auth_recorded`. |
| RESEARCH | R-06 | Generic replay currently accepts snake_case only; namespaced extensions need tests. | 1, 2 | COVERED | Generic replay regex accepts namespaced strings; tool-policy validator enforces core-or-namespaced. |
| RESEARCH | R-07 | Conversation service initializes normalized_result_json from result.data; route new writes through projector. | 1, 6 | COVERED | `append_tool_result` accepts/uses `ToolResultProjectionV1`. |
| RESEARCH | R-08 | Existing investigate helper projection still has raw-ish result.data replacement points. | 1, 5, 6 | COVERED | Projection ownership moves to `ToolResultProjector`. |
| RESEARCH | R-09 | Hidden/write tools must not leak into planner prompt. | 1, 2, 5 | COVERED | Tests assert `create_coupon_grant_draft` and descriptor-only fields absent. |
| RESEARCH | R-10 | Visible does not imply allowed. | 1, 4 | COVERED | Runtime auth denial emits event and returns safe error. |
| RESEARCH | R-11 | Tool policy decisions are not business facts. | 2, 4, 6 | COVERED | No business_fact_refs are created from policy decisions. |
| RESEARCH | R-12 | Scope binding must not overclaim domain ownership. | 2, 4 | COVERED | Domain lookup-dependent IDs are marked incomplete for Phase 30. |
| VALIDATION | W0-01 | Create `tests/tools/test_tool_platform.py`. | 1 | COVERED | Wave 0 first task creates it before production code. |
| VALIDATION | W0-02 | Create `tests/replay/test_tool_policy_events.py` if explicit event types are introduced. | 1, 3 | COVERED | Event tests created before event implementation. |
| VALIDATION | W0-03 | Extend architecture boundary tests. | 1, 5 | COVERED | Graph/platform boundary checks added. |
| VALIDATION | W0-04 | Extend investigate tests for ToolViews and projector output. | 1, 5, 6 | COVERED | Prompt and graph-state leakage tests added. |
| VALIDATION | W0-05 | Extend conversation tests for projector-normalized data. | 1, 6 | COVERED | Conversation storage raw-leak regression added. |
| PATTERN | P-01 | Copy strict Pydantic extra-forbid contract style. | 2, 4 | COVERED | New contracts use `ConfigDict(extra="forbid")`. |
| PATTERN | P-02 | Keep executors thin and behind runtime. | 4 | COVERED | Runtime dispatches current executors; no executor policy logic. |
| PATTERN | P-03 | Use MerchantScopeV1 for explicit merchant mismatch. | 2, 4 | COVERED | Resource binding parses/validates scope through existing trusted context contract. |
| PATTERN | P-04 | Use Phase 28 emit_decision_event; no parallel envelope/table. | 3, 4 | COVERED | New event types still write `agent_trace_events`. |
| PATTERN | P-05 | Prompt context consumes safe tool summaries. | 4, 5, 6 | COVERED | Projector prompt projection feeds prompt-safe summaries. |
| PATTERN | P-06 | If event types are added, update registry/model/migration/test together. | 3 | COVERED | One task owns all event-type artifacts. |

## Explicit Deferrals And Exclusions

| Source | Item | Status | Target |
| --- | --- | --- | --- |
| CONTEXT deferred | Full target graph/planner-loop migration. | DEFERRED | Phase 32 Intent Graph Migration |
| CONTEXT deferred | Full current business fact authority, domain ownership checks, freshness, and anti-enumeration behavior. | DEFERRED | Phase 30 BusinessFactService Boundary |
| CONTEXT deferred | Generic retry/rate-limit/feature-flag/runtime policy infrastructure. | EXCLUDED | Not in Phase 29 |
| CONTEXT deferred | DB-backed raw artifact persistence/full artifact store. | EXCLUDED | Beyond Phase 29 |
| CONTEXT deferred | Projection-level replay events. | DEFERRED | Phase 35 Replay and Eval Hardening if needed |
| CONTEXT deferred | Dynamic external tool/MCP discovery. | DEFERRED | APF-FUT-03 |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 RED tests for tool platform contracts</name>
  <files>tests/tools/test_tool_platform.py, tests/replay/test_tool_policy_events.py, tests/replay/test_decision_events.py, tests/replay/test_replay_migration_contract.py, tests/agent/test_tools/test_unified_tool_manager.py, tests/agent/test_nodes/test_investigate.py, tests/conversation/test_service.py, tests/architecture/test_tool_boundaries.py</files>
  <read_first>
    - `.planning/phases/29-tool-platform-boundary/29-CONTEXT.md`
    - `.planning/phases/29-tool-platform-boundary/29-VALIDATION.md`
    - `.planning/phases/29-tool-platform-boundary/29-PATTERNS.md`
    - `src/tools/catalog.py`
    - `src/tools/contracts.py`
    - `src/tools/manager.py`
    - `src/replay/decision_events.py`
    - `src/replay/validators.py`
    - `src/db/models.py`
    - `src/db/migrations/versions/010_replay_event_v3.py`
    - `src/agent/nodes/investigate.py`
    - `src/conversation/service.py`
    - `tests/tools/test_catalog.py`
    - `tests/agent/test_tools/test_unified_tool_manager.py`
    - `tests/agent/test_nodes/test_investigate.py`
    - `tests/conversation/test_service.py`
    - `tests/replay/test_decision_events.py`
    - `tests/replay/test_replay_migration_contract.py`
    - `tests/architecture/test_tool_boundaries.py`
  </read_first>
  <behavior>
    - RED test 1: `ToolViewV1` exposes exactly `name`, `description`, `input_schema`, `safe_usage_notes`, and `result_contract_version`; forbidden descriptor fields are absent from both model dumps and investigate planner prompt inputs.
    - RED test 2: `ToolViewV1.input_schema` keeps `type`, `properties`, `required`, `minLength`, `maxLength`, `minimum`, `maximum`, `enum`, and `description`, and strips `default`, `examples`, `x-internal`, `x-permission`, `x-resource-policy`, `x-adapter`, `required_permission`, `caller_allowlist`, `side_effect`, and `executor`.
    - RED test 3: `ToolPlatform.visible_tools(caller="investigate", ctx=...)` returns only policy-visible and runtime-available views while a batched visibility decision contains hidden/write/unavailable catalog tools outside the prompt.
    - RED test 4: `create_coupon_grant_draft`, `required_permission`, `caller_allowlist`, `side_effect`, `event_family`, and executor names never appear in investigate planner-visible tool payloads.
    - RED test 5: A visible `get_order` invocation with missing permission or explicit out-of-scope `merchant_id` returns safe `ToolResultV2(permission_denied)` and emits `ToolPolicyDecision(decision_stage="runtime_auth")`; the executor is not called.
    - RED test 6: Runtime auth covers `caller_not_allowed`, `missing_permission`, `scope_denied`, `side_effect_blocked`, `schema_invalid`, `approval_required`, `safety_snapshot_required`, `idempotency_required`, and `tool_unavailable`.
    - RED test 7: `ToolPolicyDecision` rejects event envelope fields and accepts only core reason codes or `<namespace>.<snake_case>` extension codes; freeform unknown tool-policy reason codes are rejected.
    - RED test 8: Explicit event types `tool_policy_visibility_recorded` and `tool_policy_runtime_auth_recorded` are registered in replay validators, ORM check constraints, and Alembic migration source.
    - RED test 9: `ToolResultProjector` output is used by investigate graph state and conversation normalized storage; raw sentinels inside `ToolResultV2.data` do not appear in `business_context`, `tool_results`, prompt summaries, or `ToolResultRecord.normalized_result_json`.
    - RED test 10: `UnifiedToolManager` delegates to `ToolPlatform` for visibility and runtime invoke compatibility, and no graph node imports tool executors directly.
  </behavior>
  <action>Create the Wave 0 tests before production implementation. Add `tests/tools/test_tool_platform.py` with focused unit tests for `ToolViewV1`, prompt-safe schema projection, visibility filtering, runtime auth decision matrix, resource binding, denied-dispatch prevention, and `ToolResultProjector`. Add `tests/replay/test_tool_policy_events.py` with async tests for batched visibility event payloads, per-invocation runtime auth events, redaction/resource-ref guards, and no raw descriptor/schema/args payload. Extend `tests/replay/test_decision_events.py` for namespaced reason-code compatibility in generic replay emission while keeping generic snake_case compatibility. Extend `tests/replay/test_replay_migration_contract.py` to discover the latest migration `017_tool_policy_events.py` and assert its event-type check values equal `src.replay.validators.REPLAY_EVENT_TYPES`. Extend `tests/agent/test_tools/test_unified_tool_manager.py` for compatibility adapter delegation and unchanged `invoke(...) -> ToolResultV2` return behavior. Extend `tests/agent/test_nodes/test_investigate.py` for ToolView-only planner surfaces, no write-tool prompt leakage, visible-does-not-imply-allowed runtime denial, and projector-only graph accumulation. Extend `tests/conversation/test_service.py` so `append_tool_result(...)` stores projector-normalized data rather than raw `result.data`. Extend `tests/architecture/test_tool_boundaries.py` so graph nodes may import `src.tools.platform.ToolPlatform` but must not import `src.tools.executors.*`, and domain packages must not import `src.tools.runtime` or `src.tools.policy` internals.</action>
  <acceptance_criteria>
    - `tests/tools/test_tool_platform.py` contains `ToolViewV1`, `ToolPolicyDecision`, `ToolPolicyEngine`, `ToolPlatform`, `ToolRuntime`, `ToolResultProjector`, `test_tool_view_exposes_only_prompt_safe_fields`, `test_prompt_safe_schema_projection_strips_descriptor_policy_and_adapter_metadata`, `test_visible_tools_records_hidden_and_unavailable_decisions_outside_prompt`, `test_runtime_auth_rechecks_visible_tool_before_dispatch`, and `test_tool_result_projector_blocks_raw_data_from_prompt_and_graph_surfaces`.
    - `tests/replay/test_tool_policy_events.py` contains `tool_policy_visibility_recorded`, `tool_policy_runtime_auth_recorded`, `emit_decision_event`, and negative assertions for `raw_args`, `raw_payload`, `raw_tool_output`, `input_schema`, `required_permission`, and `caller_allowlist`.
    - `tests/agent/test_nodes/test_investigate.py` contains assertions that planner-visible payloads do not include `ToolDescriptor`, `create_coupon_grant_draft`, `side_effect`, `required_permission`, `caller_allowlist`, `event_family`, or `executor`.
    - `tests/conversation/test_service.py` contains a regression where raw sentinel keys in `ToolResultV2.data` are absent from `normalized_result_json` and `prompt_summary`.
    - `tests/architecture/test_tool_boundaries.py` contains checks for `src.tools.platform` facade usage and forbidden `src.tools.executors` imports from `src/agent/nodes`.
    - The RED verification command exits non-zero before production work and the captured output contains one of `cannot import name 'ToolViewV1'`, `No module named 'src.tools.platform'`, `No module named 'src.tools.policy'`, `No module named 'src.tools.runtime'`, or `No module named 'src.tools.projection'`.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set +e; output=$(uv run pytest tests/tools/test_tool_platform.py tests/replay/test_tool_policy_events.py tests/agent/test_nodes/test_investigate.py tests/conversation/test_service.py tests/architecture/test_tool_boundaries.py -q 2>&1); status=$?; printf "%s\n" "$output"; test "$status" -ne 0 && printf "%s\n" "$output" | rg "cannot import name .ToolViewV1.|No module named .src\\.tools\\.(platform|policy|runtime|projection)."'</automated>
  </verify>
  <done>Wave 0 APF-06/APF-07 tests exist, cover the validation strategy and T-29 threat matrix, and fail only because the planned production contracts/boundaries are not implemented yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tool policy contracts and policy engine</name>
  <files>src/tools/contracts.py, src/tools/policy.py, src/replay/decision_events.py, tests/tools/test_tool_platform.py, tests/replay/test_decision_events.py</files>
  <read_first>
    - `.planning/phases/29-tool-platform-boundary/29-CONTEXT.md`
    - `.planning/phases/29-tool-platform-boundary/29-PATTERNS.md`
    - `docs/contract-spec.md` sections 8.0, 12.5, 12.6, and 17.2
    - `src/tools/catalog.py`
    - `src/tools/contracts.py`
    - `src/tools/validation.py`
    - `src/platform/trusted_context.py`
    - `src/replay/decision_events.py`
    - `tests/tools/test_tool_platform.py`
    - `tests/replay/test_decision_events.py`
  </read_first>
  <behavior>
    - `ToolViewV1.model_dump()` contains exactly the five locked prompt-visible fields and rejects extras.
    - `ToolPolicyDecision.model_dump()` contains no event envelope fields and validates `decision_stage`, `decision`, core/namespaced reason codes, `policy_version`, `data_classification`, and optional `resource_scope_binding`.
    - `ToolPolicyEngine.visibility_decisions(...)` returns one decision per catalog descriptor and never uses runtime-only reason codes during visibility.
    - `ToolPolicyEngine.tool_views_for_decisions(...)` returns ToolViews only for decisions with `decision="visible"` and availability true.
    - `ToolPolicyEngine.runtime_auth(...)` validates caller, permission, side-effect class, explicit merchant scope, approval/safety/idempotency required fields, and resource binding from validated args and ToolCallContext.
    - Generic `emit_decision_event(...)` continues accepting unknown snake_case reason codes and also accepts namespaced `<namespace>.<snake_case>` strings; tool-policy contract paths enforce the core-or-namespaced rule in `src/tools/policy.py`.
  </behavior>
  <action>Update `src/tools/contracts.py` with strict Pydantic contracts: `ToolViewV1`, `ToolPolicyDecision`, `ToolResultProjectionV1`, and `ToolInvocationOutcome`. `ToolViewV1` must use `ConfigDict(extra="forbid")` and expose only `name: str`, `description: str`, `input_schema: dict[str, Any]`, `safe_usage_notes: list[str]`, and `result_contract_version: Literal["tool_result.v2"] = "tool_result.v2"`; do not add `schema_version` because D-01 locks the minimal planner fields. `ToolPolicyDecision` must use `ConfigDict(extra="forbid")`, include `schema_version: Literal["tool_policy_decision.v1"]`, `tool_name`, `caller`, `decision_stage: Literal["visibility", "runtime_auth"]`, `decision: Literal["visible", "hidden", "allowed", "denied"]`, `reason_codes: list[str]`, `required_scopes: list[str]`, `matched_scope: str | None`, `policy_version: str`, `data_classification: Literal["public", "internal", "sensitive", "restricted"]`, `resource_scope_binding: dict[str, Any] | None`, `runtime_available: bool | None`, and `availability_summary: str | None`. It must reject `event_id`, `sequence`, `occurred_at`, `run_id`, and `tenant_id` as extras. Create `src/tools/policy.py` with `TOOL_POLICY_CORE_REASON_CODES = {"visible", "hidden_by_policy", "caller_not_allowed", "missing_permission", "scope_denied", "side_effect_blocked", "schema_invalid", "approval_required", "safety_snapshot_required", "idempotency_required", "tool_unavailable"}`, `TOOL_POLICY_RUNTIME_ONLY_REASON_CODES = {"schema_invalid", "approval_required", "safety_snapshot_required", "idempotency_required"}`, `TOOL_POLICY_EXTENSION_REASON_PATTERN = re.compile(r"^[a-z][a-z0-9_]*\\.[a-z][a-z0-9_]*$")`, `validate_tool_policy_reason_codes(...)`, `project_prompt_safe_input_schema(...)`, and `ToolPolicyEngine`. `project_prompt_safe_input_schema(...)` must recursively retain only schema shape keys `type`, `properties`, `items`, `required`, `minLength`, `maxLength`, `minimum`, `maximum`, `exclusiveMinimum`, `exclusiveMaximum`, `enum`, `description`, and `additionalProperties`; it must drop defaults/examples/internal/policy/adapter keys. `ToolPolicyEngine` must use `policy_version="tool_policy.v1"` unless a constructor argument overrides it, derive `data_classification="internal"` for existing descriptors, derive `required_scopes=[descriptor.required_permission]`, check visibility from `descriptor.exposure == "planner_visible"` and `caller in descriptor.caller_allowlist`, check runtime availability from a supplied availability map, and build resource bindings for `tenant_id`, `merchant_id`, `order_id`, `order_no`, `refund_id`, `refund_case_no`, and `ticket_id`. Explicit `merchant_id` outside `MerchantScopeV1` denies with `scope_denied`; order/refund/ticket identifiers set `requires_domain_scope_check=True` without denying. Update `src/replay/decision_events.py` `REASON_CODE_PATTERN` to accept `^[a-z][a-z0-9_]*(\\.[a-z][a-z0-9_]*)?$` so namespaced extension codes can pass through generic replay after tool-policy validation. Keep generic replay compatibility with arbitrary snake_case reason codes; do not add a global replay allowlist.</action>
  <acceptance_criteria>
    - `src/tools/contracts.py` contains `class ToolViewV1`, `class ToolPolicyDecision`, `class ToolResultProjectionV1`, `class ToolInvocationOutcome`, and each new model has `ConfigDict(extra="forbid")`.
    - `src/tools/contracts.py` `ToolViewV1` fields are exactly `name`, `description`, `input_schema`, `safe_usage_notes`, and `result_contract_version`; `rg -n "schema_version.*tool_view|required_permission|caller_allowlist|side_effect|executor" src/tools/contracts.py` shows no ToolViewV1 leakage.
    - `src/tools/policy.py` contains all eleven core reason codes, `TOOL_POLICY_RUNTIME_ONLY_REASON_CODES`, `ToolPolicyEngine`, `project_prompt_safe_input_schema`, `requires_domain_scope_check`, and `MerchantScopeV1`.
    - `src/tools/policy.py` contains the regex string `^[a-z][a-z0-9_]*\\.[a-z][a-z0-9_]*$` and rejects unknown non-namespaced tool-policy reason codes in tests.
    - `src/replay/decision_events.py` contains the regex string `^[a-z][a-z0-9_]*(\\.[a-z][a-z0-9_]*)?$`, and existing generic replay snake_case tests remain green.
    - `uv run pytest tests/tools/test_tool_platform.py::test_tool_view_exposes_only_prompt_safe_fields tests/tools/test_tool_platform.py::test_prompt_safe_schema_projection_strips_descriptor_policy_and_adapter_metadata tests/tools/test_tool_platform.py::test_tool_policy_decision_is_not_an_event_envelope tests/tools/test_tool_platform.py::test_visibility_stage_forbids_runtime_only_reason_codes tests/replay/test_decision_events.py -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/tools/test_tool_platform.py::test_tool_view_exposes_only_prompt_safe_fields tests/tools/test_tool_platform.py::test_prompt_safe_schema_projection_strips_descriptor_policy_and_adapter_metadata tests/tools/test_tool_platform.py::test_tool_policy_decision_is_not_an_event_envelope tests/tools/test_tool_platform.py::test_visibility_stage_forbids_runtime_only_reason_codes tests/replay/test_decision_events.py -q</automated>
  </verify>
  <done>Tool policy contracts and the policy engine exist, validate the locked reason-code semantics, project prompt-safe schemas, and keep generic replay reason-code compatibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Register explicit tool policy decision events</name>
  <files>src/replay/validators.py, src/db/models.py, src/db/migrations/versions/017_tool_policy_events.py, tests/replay/test_tool_policy_events.py, tests/replay/test_replay_migration_contract.py</files>
  <read_first>
    - `.planning/phases/28-decision-event-foundation/28-01-PLAN.md`
    - `.planning/phases/29-tool-platform-boundary/29-CONTEXT.md`
    - `.planning/phases/29-tool-platform-boundary/29-PATTERNS.md`
    - `src/replay/validators.py`
    - `src/replay/decision_events.py`
    - `src/db/models.py`
    - `src/db/migrations/versions/010_replay_event_v3.py`
    - `src/db/migrations/versions/016_agent_run_memory_idempotency.py`
    - `tests/replay/test_tool_policy_events.py`
    - `tests/replay/test_replay_migration_contract.py`
  </read_first>
  <behavior>
    - `validate_event_type("tool_policy_visibility_recorded")` and `validate_event_type("tool_policy_runtime_auth_recorded")` pass.
    - `retention_for_event_type(...)` returns `tool_policy_event` for both new event types.
    - `AgentTraceEvent` ORM check constraint allows both event types.
    - Alembic migration `017_tool_policy_events.py` drops and recreates `ck_agent_trace_events_event_type` with all current registry values and downgrades to the previous value set.
    - Migration-contract tests compare the current registry to the latest event-type check migration, not only migration 010.
  </behavior>
  <action>Choose explicit Phase 29 event types per user discretion: `tool_policy_visibility_recorded` and `tool_policy_runtime_auth_recorded`. Update `src/replay/validators.py` by adding both strings to `REPLAY_EVENT_TYPES` and `EVENT_RETENTION_CLASSIFICATION` with value `"tool_policy_event"`. Update `src/db/models.py` `AgentTraceEvent.__table_args__` `ck_agent_trace_events_event_type` string to include both event types in deterministic sorted order with the existing event type literals. Add `src/db/migrations/versions/017_tool_policy_events.py` with `revision = "017_tool_policy_events"` and `down_revision = "016_agent_run_memory_idempotency"`. The migration must drop `ck_agent_trace_events_event_type`, recreate it with all current event types including the two tool-policy events, and downgrade by restoring the pre-Phase-29 event-type check. Update `tests/replay/test_replay_migration_contract.py` so `MIGRATION_PATH` or helper logic points to `017_tool_policy_events.py` for event-type check comparison while keeping existing 010 replay V3 column/index assertions intact. Add `tests/replay/test_tool_policy_events.py` assertions that both event types are registered and retained as `tool_policy_event`. Do not create any new table, model, event envelope, or schema_version.</action>
  <acceptance_criteria>
    - `src/replay/validators.py` contains `tool_policy_visibility_recorded`, `tool_policy_runtime_auth_recorded`, and `"tool_policy_event"`.
    - `src/db/models.py` `ck_agent_trace_events_event_type` contains `tool_policy_visibility_recorded` and `tool_policy_runtime_auth_recorded`.
    - `src/db/migrations/versions/017_tool_policy_events.py` exists and contains `revision: str = "017_tool_policy_events"`, `down_revision: str | None = "016_agent_run_memory_idempotency"`, `op.drop_constraint("ck_agent_trace_events_event_type"`, `op.create_check_constraint("ck_agent_trace_events_event_type"`, and both tool policy event type strings.
    - `tests/replay/test_replay_migration_contract.py` contains `017_tool_policy_events.py` and still asserts migration event type values equal `REPLAY_EVENT_TYPES`.
    - `rg -n "CREATE TABLE|create_table|tool_policy_events|DecisionEventEnvelopeV2|schema_version.*tool_policy" src/db/migrations/versions/017_tool_policy_events.py src/db/models.py src/replay/validators.py` prints no matches except event type string literals containing `tool_policy`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/replay/test_tool_policy_events.py tests/replay/test_replay_migration_contract.py -q</automated>
  </verify>
  <done>Tool policy decision events are first-class registered minimal replay events with ORM and Alembic constraints aligned to the replay registry, without a parallel event table or envelope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement ToolRuntime, ToolResultProjector, and ToolPlatform facade</name>
  <files>src/tools/platform.py, src/tools/runtime.py, src/tools/projection.py, src/tools/policy.py, src/tools/contracts.py, src/tools/manager_results.py, tests/tools/test_tool_platform.py, tests/replay/test_tool_policy_events.py</files>
  <read_first>
    - `.planning/phases/29-tool-platform-boundary/29-CONTEXT.md`
    - `.planning/phases/29-tool-platform-boundary/29-VALIDATION.md`
    - `.planning/phases/29-tool-platform-boundary/29-PATTERNS.md`
    - `src/tools/catalog.py`
    - `src/tools/contracts.py`
    - `src/tools/policy.py`
    - `src/tools/manager.py`
    - `src/tools/validation.py`
    - `src/tools/manager_results.py`
    - `src/platform/context_projections.py`
    - `src/platform/trusted_context.py`
    - `src/replay/decision_events.py`
    - `src/replay/validators.py`
    - `tests/tools/test_tool_platform.py`
    - `tests/replay/test_tool_policy_events.py`
  </read_first>
  <behavior>
    - `ToolPlatform.visible_tools(caller, ctx, session=...)` returns a `list[ToolViewV1]` only, not a wrapper containing decisions; internal/full decisions are emitted or retained outside prompt-facing return.
    - Visibility emits one `tool_policy_visibility_recorded` event when a valid session and identity are available, with low-payload fields only: tool_name, decision_stage, decision, reason_codes, policy_version, data_classification, runtime_available, availability_summary.
    - `ToolPlatform.invoke(name, args, ctx, session=...)` returns `ToolInvocationOutcome` with `tool_result`, `projection`, `policy_decision`, and optional `policy_event_id`.
    - `ToolRuntime.invoke(...)` chain order is descriptor lookup, input schema validation, runtime_auth decision, side-effect gate, approval/safety/idempotency gates, executor dispatch, output schema validation, result projection, safe error mapping, decision event emission.
    - Runtime denials and schema failures emit runtime auth decision events before returning safe `ToolResultV2` errors.
    - Executor exceptions and invalid outputs return safe errors without raw exception text or raw adapter payload.
    - `ToolResultProjector` emits normalized result, structured prompt projection, `text_for_prompt`, audit/resource refs, debug projection, and optional raw ref/hash; it does not emit events and does not create artifact storage.
  </behavior>
  <action>Create `src/tools/projection.py` with `ToolResultProjector.project(tool_name, result, tool_call_id, tool_result_id=None, raw_result_ref=None, raw_result_hash=None, policy_decision_ref=None) -> ToolResultProjectionV1`. The projection must set `normalized_result` from safe typed surfaces: status, source_system, bounded summary, business_fact_refs, policy_evidence_refs, audit_ref, error code/source/safe_message, retrieval_status/best_score only when those scalar keys are present in `result.data`, and sanitized case-memory items using the existing investigate `_case_memory_items` shape. It must not copy raw `result.data` wholesale. It must set `prompt_projection` with `tool_name`, `status`, `summary`, `business_fact_refs`, `policy_candidate_refs`, `resource_refs`, `warnings`, `safe_error`, `redaction_applied`, and `text_for_prompt`; `text_for_prompt` must be bounded to 500 characters and exclude raw/debug/private keys. It must set `debug_projection` to safe booleans/counts such as `had_raw_data`, `redaction_applied`, `business_fact_ref_count`, `policy_evidence_ref_count`, and `status`; no raw payload values. Create `src/tools/runtime.py` with `ToolRuntime`, using current `UnifiedToolManager.invoke(...)` gate order from `src/tools/manager.py` as the source pattern but moving policy decisions to `ToolPolicyEngine`. Create `src/tools/platform.py` with `ToolPlatform(catalog=None, executors=None, policy_engine=None, runtime=None, projector=None)`, `with_defaults(session)`, `visible_tools(...)`, `invoke(...)`, `descriptor(...)`, `event_family(...)`, and compatibility helpers. Runtime availability must be computed from descriptor executor names and executor `has_tool(name)`. `visible_tools(...)` must filter to views only and emit/record hidden/unavailable decisions outside the returned list. `invoke(...)` must emit `tool_policy_runtime_auth_recorded` through `emit_decision_event(...)` using `resource_refs` with safe identifiers such as `tool_name`, `tool_call_id`, `resource_type`, and `resource_scope_binding`; `redacted_payload` must not include args, schemas, raw result data, or descriptor internals. Update `src/tools/manager_results.py` if needed so policy denials can use `source="policy"` in `ToolError`. Do not implement a generic retry framework, rate limiter, feature flag system, full timeout wrapper, raw artifact store, or external tool discovery.</action>
  <acceptance_criteria>
    - `src/tools/platform.py` contains `class ToolPlatform`, `def with_defaults`, `async def visible_tools`, `async def invoke`, `def descriptor`, and `def event_family`.
    - `src/tools/runtime.py` contains `class ToolRuntime`, `validate_json_value`, `runtime_auth`, `output_schema`, `ToolResultProjector`, and `emit_decision_event`.
    - `src/tools/projection.py` contains `class ToolResultProjector`, `def project`, `normalized_result`, `prompt_projection`, `text_for_prompt`, `audit_refs`, `resource_refs`, `debug_projection`, and no call to `emit_decision_event`.
    - `rg -n "raw_result|artifact_store|rate_limit|feature_flag|retry_framework|external_tool|mcp" src/tools/platform.py src/tools/runtime.py src/tools/projection.py` shows no new generic framework or artifact-store implementation; existing `raw_result_ref`/`raw_result_hash` contract names are allowed.
    - `uv run pytest tests/tools/test_tool_platform.py tests/replay/test_tool_policy_events.py -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/tools/test_tool_platform.py tests/replay/test_tool_policy_events.py -q</automated>
  </verify>
  <done>The ToolPlatform facade, runtime boundary chain, result projector, safe denial mapping, runtime availability filtering, and policy decision events are implemented and covered by focused tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Convert manager compatibility and investigate integration to ToolPlatform</name>
  <files>src/tools/manager.py, src/agent/nodes/investigate.py, tests/agent/test_tools/test_unified_tool_manager.py, tests/agent/test_nodes/test_investigate.py, tests/architecture/test_tool_boundaries.py</files>
  <read_first>
    - `.planning/phases/29-tool-platform-boundary/29-CONTEXT.md`
    - `.planning/phases/29-tool-platform-boundary/29-PATTERNS.md`
    - `src/tools/platform.py`
    - `src/tools/runtime.py`
    - `src/tools/policy.py`
    - `src/tools/projection.py`
    - `src/tools/manager.py`
    - `src/agent/nodes/investigate.py`
    - `src/platform/context_projections.py`
    - `src/platform/trusted_context.py`
    - `tests/agent/test_tools/test_unified_tool_manager.py`
    - `tests/agent/test_nodes/test_investigate.py`
    - `tests/architecture/test_tool_boundaries.py`
  </read_first>
  <behavior>
    - `UnifiedToolManager` public methods remain usable for compatibility: `with_defaults(...)`, `descriptors(...)`, `descriptor(...)`, `invoke(...) -> ToolResultV2`, and `event_family(...)`.
    - `UnifiedToolManager` delegates visibility/runtime behavior to an internal `ToolPlatform`; it does not add new policy/runtime logic.
    - `investigate` uses `ToolPlatform.visible_tools(caller="investigate", ctx=...)` for planner surface and validates planned tool names against returned `ToolViewV1.name` values.
    - `investigate` uses `ToolPlatform.invoke(...)` for runtime calls and accumulates from `outcome.projection`.
    - `plan_next_step(...)`, bounded loop iteration/deadline/max_attempt behavior, lifecycle event family behavior, and deterministic fallback tool selection remain compatible.
    - The module-level `ALLOWLIST` no longer acts as planner authorization; if retained for compatibility tests, it is not used to authorize planner selections.
  </behavior>
  <action>Refactor `src/tools/manager.py` so `UnifiedToolManager.__init__(...)` builds `self._platform = ToolPlatform(catalog=..., executors=...)` and keeps `self._descriptors`/`self._executors` only if needed for backward-compatible inspection. Add `visible_tools(...)` to the manager as a delegate for existing callers. Keep `descriptors(caller_node)` returning descriptors for compatibility only by mapping `ToolPlatform.visible_tools(...)` names back to descriptors when a `ToolCallContext` is supplied or by preserving the old raw descriptor method for tests that explicitly assert compatibility; do not use this raw method from investigate. Keep `invoke(...) -> ToolResultV2` by delegating to `await self._platform.invoke(...); return outcome.tool_result`. Update tests to assert manager delegation and return compatibility. Refactor `src/agent/nodes/investigate.py`: construct or accept `tool_platform = configurable.get("tool_platform") or configurable.get("tool_manager") if it exposes visible_tools/invoke else ToolPlatform.with_defaults(session)`. Build a `ToolCallContext` from trusted context before planner visibility using a deterministic `tool_call_id` such as `visibility:{run_id}` and `caller_node="investigate"`, then call `await tool_platform.visible_tools(caller="investigate", ctx=visibility_ctx, session=session_or_none)`. Pass the returned ToolViews into `plan_next_step(...)`. Replace `_validate_planner_step(step, manager, descriptors)` with validation against `{view.name for view in tool_views}` and dict args only; remove descriptor/ALLOWLIST/write checks from planner validation because visibility already hides those and runtime auth still enforces. For runtime, replace `descriptor = manager.descriptor(tool_name)`, `family = manager.event_family(tool_name)`, and `result = await manager.invoke(...)` with platform equivalents; call `outcome = await tool_platform.invoke(tool_name, args, tool_ctx, session=session_or_none)` and use `outcome.tool_result` plus `outcome.projection`. Preserve `_emit_tool_event(...)` lifecycle events using descriptor/family from platform. Update `_accumulate_tool_result(...)` to consume `ToolResultProjectionV1` normalized/prompt surfaces and typed refs, not `result.data` directly. Keep case-memory/retrieval scalar behavior by reading sanitized `projection.normalized_result`, not raw data. Do not rewrite `plan_next_step(...)` or the broader planner loop before Phase 32.</action>
  <acceptance_criteria>
    - `src/agent/nodes/investigate.py` imports `ToolPlatform` and no longer imports `TOOL_CALL_TOOLS` or `RAG_RETRIEVAL_TOOLS` for planner authorization.
    - `rg -n "ALLOWLIST|descriptor_names|descriptor\\.kind|manager\\.descriptors\\(|manager\\.invoke\\(" src/agent/nodes/investigate.py` shows no active planner authorization or runtime dispatch through the old manager path; compatibility comments are allowed only if not used.
    - `src/agent/nodes/investigate.py` contains `visible_tools(`, `ToolPlatform`, `outcome.projection`, and no assignment `context["facts"][ref.resource_type] = _without_raw_payload(result.data or {})`.
    - `src/tools/manager.py` contains `ToolPlatform` and delegates `invoke(...)` to `self._platform.invoke`.
    - `tests/agent/test_nodes/test_investigate.py` contains ToolView prompt leakage assertions and verifies visible-but-runtime-denied behavior through ToolPlatform.
    - `tests/architecture/test_tool_boundaries.py` passes with no graph-node imports of `src.tools.executors`.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/agent/test_tools/test_unified_tool_manager.py tests/agent/test_nodes/test_investigate.py tests/architecture/test_tool_boundaries.py -q</automated>
  </verify>
  <done>Existing manager callers stay compatible, while investigate planner/runtime integration targets ToolPlatform and no longer uses raw descriptors or a scattered allowlist as the prompt boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Route conversation storage through projector and run phase gates</name>
  <files>src/conversation/service.py, src/tools/projection.py, src/agent/nodes/investigate.py, tests/conversation/test_service.py, tests/agent/test_nodes/test_investigate.py, tests/tools/test_tool_platform.py</files>
  <read_first>
    - `.planning/phases/29-tool-platform-boundary/29-CONTEXT.md`
    - `.planning/phases/29-tool-platform-boundary/29-VALIDATION.md`
    - `src/tools/contracts.py`
    - `src/tools/projection.py`
    - `src/conversation/service.py`
    - `src/agent/nodes/investigate.py`
    - `tests/conversation/test_service.py`
    - `tests/agent/test_nodes/test_investigate.py`
    - `tests/tools/test_tool_platform.py`
  </read_first>
  <behavior>
    - `ConversationService.append_tool_result(...)` stores `normalized_result_json` from `ToolResultProjectionV1.normalized_result`, not `result.data`.
    - Prompt summaries use `ToolResultProjectionV1.text_for_prompt` / prompt projection rather than rebuilding from raw data.
    - `ToolResultRecord.raw_result_ref` and `raw_result_hash` remain optional refs only; no artifact store/table is created.
    - Existing prompt-context loading remains compatible and excludes raw/private/debug payload sentinels.
    - Phase-level targeted commands from `29-VALIDATION.md` pass.
  </behavior>
  <action>Update `src/conversation/service.py::append_tool_result(...)` to accept an optional keyword-only `projection: ToolResultProjectionV1 | None = None`. If absent, instantiate `ToolResultProjector` and project the supplied `ToolResultV2` with the provided `tool_call_id`, `tool_result_id`, `raw_result_ref`, and `raw_result_hash`. Set `normalized_result_json=projection.normalized_result`, `prompt_summary=projection.text_for_prompt`, `business_fact_refs_json=projection.prompt_projection.get("business_fact_refs", business_fact_refs)`, `policy_evidence_refs_json=projection.prompt_projection.get("policy_candidate_refs", policy_evidence_refs)`, and `audit_ref=result.audit_ref or projection.audit_refs.get("audit_ref")`. Return `ToolResultPromptSummary` from projection fields only. Update `src/agent/nodes/investigate.py::_append_tool_result_record(...)` to pass `outcome.projection` into conversation service when persistence is enabled and to return a prompt summary built from the same projection when persistence is disabled. Keep existing conversation message, tool-call, and prompt-context APIs compatible. Add/adjust tests so raw keys `raw_payload`, `private_reasoning`, `approval_authority_body`, `debug_trace`, `secret`, `raw_tool_output`, and a phone-like sentinel never appear in `normalized_result_json`, `prompt_summary`, `business_context`, or `tool_results`. Run final focused phase gates and record any local environment failure in `.planning/LOCAL-VALIDATION-ISSUES.md` in Chinese only if a command fails for an environment/setup reason.</action>
  <acceptance_criteria>
    - `src/conversation/service.py` contains `projection: ToolResultProjectionV1 | None = None`, `ToolResultProjector`, and `normalized_result_json=projection.normalized_result`.
    - `rg -n "normalized_result_json = result\\.data|normalized_result_json=result\\.data|prompt_summary = _build_prompt_summary" src/conversation/service.py` prints no matches for the old raw-data path.
    - `src/agent/nodes/investigate.py` passes `projection=` to `append_tool_result` and has no direct graph-state write from `result.data`.
    - `tests/conversation/test_service.py` asserts raw sentinel keys are absent from `ToolResultRecord.normalized_result_json` and `prompt_summary`.
    - `uv run pytest tests/tools/test_tool_platform.py tests/agent/test_nodes/test_investigate.py tests/conversation/test_service.py -q` passes.
    - `uv run pytest tests/tools/test_tool_platform.py tests/agent/test_tools/test_unified_tool_manager.py tests/agent/test_nodes/test_investigate.py tests/replay/test_decision_events.py -q` passes.
    - `uv run pytest tests/tools tests/agent/test_tools/test_unified_tool_manager.py tests/agent/test_nodes/test_investigate.py tests/conversation/test_service.py tests/replay tests/architecture/test_tool_boundaries.py -q` passes or any environment-only failure is recorded in `.planning/LOCAL-VALIDATION-ISSUES.md` with the command, evidence, root-cause judgment, and next retry point.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/tools/test_tool_platform.py tests/agent/test_nodes/test_investigate.py tests/conversation/test_service.py -q</automated>
    <automated>uv run pytest tests/tools/test_tool_platform.py tests/agent/test_tools/test_unified_tool_manager.py tests/agent/test_nodes/test_investigate.py tests/replay/test_decision_events.py -q</automated>
    <automated>uv run pytest tests/tools tests/agent/test_tools/test_unified_tool_manager.py tests/agent/test_nodes/test_investigate.py tests/conversation/test_service.py tests/replay tests/architecture/test_tool_boundaries.py -q</automated>
  </verify>
  <done>Conversation persistence, graph state, prompt projection, and final targeted tests prove raw adapter payloads no longer cross the tool platform boundary unprojected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| ToolCatalog -> ToolPolicyEngine -> ToolViewV1 | Full descriptors contain policy/runtime metadata and cross into prompt-facing planner visibility only through a whitelisted projection. |
| TrustedContext / ToolCallContext -> ToolPolicyEngine.runtime_auth | Trusted identity, permissions, merchant scope, and caller-local fields cross into authorization; LLM/user args cannot widen these fields. |
| ToolRuntime -> domain executor | Runtime checks must complete before executor dispatch; denied calls must not reach business/knowledge/memory/action executors. |
| ToolResultV2.data -> ToolResultProjector -> graph/conversation/prompt | Raw-ish adapter output crosses into graph/prompt/persistence only through normalized and prompt-safe projections. |
| ToolPolicyDecision -> emit_decision_event -> agent_trace_events | Tool policy decisions cross into durable replay audit through Phase 28 minimal event envelope and redaction guards. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-29-01 | Information Disclosure | `ToolViewV1`, `ToolPolicyEngine.project_prompt_safe_input_schema`, investigate prompt surface | mitigate | Tasks 1, 2, and 5 add strict ToolView field whitelist, recursive schema-key allowlist, and tests proving descriptor-only fields do not enter planner prompts. |
| T-29-02 | Elevation of Privilege | `ToolPlatform.visible_tools`, `ToolRuntime.invoke` | mitigate | Tasks 1, 2, and 4 prove visibility is not authorization; runtime creates fresh `decision_stage="runtime_auth"` decisions and denies before dispatch. |
| T-29-03 | Elevation of Privilege | `ToolRuntime` boundary chain | mitigate | Task 4 centralizes caller, permission, schema, resource, side-effect, approval, safety, idempotency, availability, executor, and output validation gates with a denial matrix in `tests/tools/test_tool_platform.py`. |
| T-29-04 | Spoofing / Elevation of Privilege | Resource binding in `ToolPolicyEngine.runtime_auth` | mitigate | Tasks 2 and 4 derive scope from `ToolCallContext.merchant_scope`; explicit merchant mismatch denies; order/refund/ticket IDs set `requires_domain_scope_check=true` for Phase 30. |
| T-29-05 | Repudiation | Visibility decisions | mitigate | Tasks 3 and 4 emit a low-payload `tool_policy_visibility_recorded` event containing visible/hidden/unavailable decisions for all catalog tools while returning only visible views to prompts. |
| T-29-06 | Tampering | Replay event registry, ORM check, migration | mitigate | Task 3 registers explicit event types in `src/replay/validators.py`, `src/db/models.py`, `017_tool_policy_events.py`, and migration-contract tests; no parallel envelope/table is created. |
| T-29-07 | Information Disclosure | Tool policy event payloads | mitigate | Tasks 1, 3, and 4 keep event payloads to controlled ToolPolicyDecision fields and test redaction guards against raw descriptors, args, schemas, adapter payloads, permission internals, PII, and secrets. |
| T-29-08 | Information Disclosure / Prompt Injection | `ToolResultProjector`, investigate accumulation, conversation storage | mitigate | Tasks 4, 5, and 6 route all graph/prompt/conversation tool result consumption through projector normalized and prompt projections; tests use raw sentinel payloads. |
| T-29-09 | Tampering | `UnifiedToolManager` compatibility adapter | mitigate | Task 5 converts manager to delegate to ToolPlatform and extends architecture tests so new policy/runtime logic lives in ToolPolicyEngine/ToolRuntime, not manager or executors. |
</threat_model>

<verification>
Run these after all tasks:

1. `uv run pytest tests/tools/test_tool_platform.py tests/replay/test_tool_policy_events.py -q`
2. `uv run pytest tests/replay/test_decision_events.py tests/replay/test_replay_migration_contract.py -q`
3. `uv run pytest tests/agent/test_tools/test_unified_tool_manager.py tests/agent/test_nodes/test_investigate.py tests/conversation/test_service.py tests/architecture/test_tool_boundaries.py -q`
4. `uv run pytest tests/tools/test_tool_platform.py tests/agent/test_tools/test_unified_tool_manager.py tests/agent/test_nodes/test_investigate.py tests/replay/test_decision_events.py -q`
5. `uv run pytest tests/tools tests/agent/test_tools/test_unified_tool_manager.py tests/agent/test_nodes/test_investigate.py tests/conversation/test_service.py tests/replay tests/architecture/test_tool_boundaries.py -q`
6. `rg -n "manager\\.descriptors\\(|ALLOWLIST|result\\.data or \\{\\}|normalized_result_json = result\\.data|required_permission|caller_allowlist|side_effect|executor" src/agent/nodes/investigate.py src/conversation/service.py tests/agent/test_nodes/test_investigate.py tests/conversation/test_service.py` must show no active raw descriptor/raw data prompt or graph boundary use; tests may contain forbidden strings only as negative assertions.
7. `rg -n "create_table|tool_policy_events|artifact_store|rate_limit|feature_flag|MCP|mcp" src/tools src/db/migrations/versions/017_tool_policy_events.py` must show no new table/artifact-store/generic framework/dynamic discovery implementation.
</verification>

<success_criteria>
- Planner-visible tool lists come from `ToolPlatform.visible_tools(...)` and contain only minimal prompt-safe `ToolViewV1` entries.
- Hidden, write, unavailable, and raw descriptor/policy/runtime fields are not present in investigate planner prompt surfaces.
- Runtime invocation through `ToolPlatform.invoke(...)` enforces a fresh `ToolPolicyDecision(decision_stage="runtime_auth")` and denies before executor dispatch on caller, permission, side-effect, schema, explicit merchant scope, approval, safety, idempotency, or availability failures.
- Tool policy decision events use Phase 28 `emit_decision_event(...)`, registered event types, and `agent_trace_events`; no parallel event envelope/table exists.
- Namespaced extension reason codes are supported by generic replay while tool-policy contract paths reject freeform unknown codes.
- `UnifiedToolManager` remains a compatibility adapter and does not own new policy/runtime logic.
- Investigate graph state, prompt summaries, and conversation normalized tool-result storage consume `ToolResultProjector` outputs rather than unprojected `ToolResultV2.data`.
- No generic retry/rate-limit/feature-flag/full timeout/artifact-store/dynamic MCP discovery/full planner-loop migration/BusinessFactService authority checks are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/29-tool-platform-boundary/29-01-SUMMARY.md`.
</output>
