---
phase: 48.1-memory-context-compatibility-debt-cleanup
plan: "03"
type: execute
wave: 2
depends_on: ["48.1-02"]
files_modified:
  - src/agent/routing.py
  - src/agent/nodes/reviewed_memory_context_retrieve.py
  - tests/agent/test_intent_routing.py
  - tests/agent/test_graph.py
  - tests/agent/test_memory_evidence_boundary.py
  - tests/agent/test_reviewed_memory_context_retrieve.py
autonomous: true
requirements: [MEM-COMPAT-01]
requirements_addressed: [MEM-COMPAT-01]
must_haves:
  truths:
    - "Canonical routing hint needs_reviewed_memory_context routes to reviewed memory context loading."
    - "Legacy routing hint needs_long_term_memory remains a backward-compatible alias."
    - "Runtime graph node names session_memory_load and long_term_memory_retrieve remain unchanged."
    - "Reviewed memory remains contextual-only and does not become policy, business, approval/action, or replay authority."
  artifacts:
    - path: "src/agent/routing.py"
      provides: "canonical reviewed-memory routing hint alias"
      contains: "needs_reviewed_memory_context"
    - path: "src/agent/nodes/reviewed_memory_context_retrieve.py"
      provides: "canonical hint support for trusted actor merchant-scope fallback"
      contains: "needs_reviewed_memory_context"
    - path: "tests/agent/test_graph.py"
      provides: "graph routing coverage with preserved compatibility node name"
      contains: "long_term_memory_retrieve"
  key_links:
    - from: "src/agent/routing.py"
      to: "src/agent/graph.py"
      via: "route_after_slots returns existing long_term_memory_retrieve node key"
      pattern: "long_term_memory_retrieve"
    - from: "src/agent/nodes/long_term_memory_retrieve.py"
      to: "src/agent/nodes/reviewed_memory_context_retrieve.py"
      via: "compatibility wrapper forwards to reviewed memory context"
      pattern: "reviewed_memory_context_retrieve"
---

<objective>
Introduce the canonical reviewed-memory routing hint while preserving the legacy long-term-memory route alias and graph node names.

Purpose: After Phase 48, the old `needs_long_term_memory` name is misleading because the node loads reviewed memory context, not only explicit preference memory. This plan adds `needs_reviewed_memory_context` as the semantic hint and keeps compatibility for old traces and existing graph topology.
Output: route/helper alias support and tests proving canonical and legacy hints behave identically while runtime node names remain unchanged.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ARCHITECTURE-DEBT.md
@.planning/phases/48.1-memory-context-compatibility-debt-cleanup/48.1-RESEARCH.md
@.planning/phases/48.1-memory-context-compatibility-debt-cleanup/48.1-PATTERNS.md
@.planning/phases/48.1-memory-context-compatibility-debt-cleanup/48.1-VALIDATION.md
@.planning/phases/48.1-memory-context-compatibility-debt-cleanup/48.1-02-SUMMARY.md
@src/agent/routing.py
@src/agent/nodes/reviewed_memory_context_retrieve.py
@src/agent/nodes/long_term_memory_retrieve.py
@src/agent/graph.py
@src/agent/graph_vocabulary.py
@tests/agent/test_intent_routing.py
@tests/agent/test_graph.py
@tests/agent/test_memory_evidence_boundary.py
@tests/agent/test_reviewed_memory_context_retrieve.py

<interfaces>
Existing route return key:
```python
SLOT_ROUTES = {"clarification_gate", "investigate", "long_term_memory_retrieve"}
```

Compatibility wrapper to preserve:
```python
async def long_term_memory_retrieve(state: AgentState, config: RunnableConfig) -> dict:
    result = await reviewed_memory_context_retrieve(...)
    ...
```
</interfaces>

<source_audit>
SOURCE | ID | Requirement / Decision | Plan | Status
--- | --- | --- | --- | ---
ROADMAP | Phase 48.1 SC3 | Introduce canonical reviewed-memory routing hint and keep needs_long_term_memory as alias | 48.1-03 | COVERED
ROADMAP | Phase 48.1 SC4 | Do not rename graph trace names | 48.1-03 | COVERED
REQ | MEM-COMPAT-01 | reviewed-memory loading gains canonical needs_reviewed_memory_context while legacy alias remains | 48.1-03 | COVERED
RESEARCH | Explicit non-goal | Runtime graph names session_memory_load and long_term_memory_retrieve remain compatibility wrappers | 48.1-03 | COVERED
ARCH-DEBT | Phase 48.1 scope | reviewed memory context is not long-term-only | 48.1-03 | COVERED
</source_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add canonical reviewed-memory route hint with legacy alias</name>
  <read_first>
    - src/agent/routing.py `_route_after_slots`
    - src/agent/nodes/reviewed_memory_context_retrieve.py `_uses_legacy_actor_merchant_scope`
    - src/agent/nodes/long_term_memory_retrieve.py compatibility wrapper
    - tests/agent/test_intent_routing.py long-term memory route test
    - tests/agent/test_reviewed_memory_context_retrieve.py reviewed context tests
    - .planning/phases/48.1-memory-context-compatibility-debt-cleanup/48.1-PATTERNS.md reviewed-memory hint alias pattern
  </read_first>
  <files>src/agent/routing.py, src/agent/nodes/reviewed_memory_context_retrieve.py, tests/agent/test_intent_routing.py, tests/agent/test_reviewed_memory_context_retrieve.py</files>
  <behavior>
    - Test 1: `route_after_slots(...)` returns `"long_term_memory_retrieve"` when `routing_hints={"needs_reviewed_memory_context": True}` and required slots are complete.
    - Test 2: `route_after_slots(...)` still returns `"long_term_memory_retrieve"` when `routing_hints={"needs_long_term_memory": True}`.
    - Test 3: missing required slots still route to clarification and do not skip slot completeness just because either hint is true.
    - Test 4: reviewed-memory trusted actor merchant-scope fallback activates for the canonical hint as well as the legacy hint.
  </behavior>
  <action>
    In `src/agent/routing.py`, add a helper such as `_needs_reviewed_memory_context(state: AgentState) -> bool` that reads `state.get("routing_hints")` only when it is a dict and returns true when either:
    - `routing_hints.get("needs_reviewed_memory_context") is True`, or
    - `routing_hints.get("needs_long_term_memory") is True`.

    Update `_route_after_slots(...)` to call the helper after required-slot completeness passes. Keep the returned node key exactly `"long_term_memory_retrieve"` so graph trace/replay compatibility is unchanged.

    In `src/agent/nodes/reviewed_memory_context_retrieve.py`, replace or wrap `_uses_legacy_actor_merchant_scope(...)` with a helper that checks both hints. The helper name should reflect reviewed memory semantics, for example `_uses_reviewed_memory_actor_merchant_scope_hint(...)`. Keep legacy alias behavior explicitly covered by tests.

    Do not rename `SLOT_ROUTES`, `long_term_memory_retrieve`, graph nodes, graph vocabulary aliases, or LLM output compatibility metrics in this task.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_intent_routing.py tests/agent/test_reviewed_memory_context_retrieve.py -x -q</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/routing.py src/agent/nodes/reviewed_memory_context_retrieve.py tests/agent/test_intent_routing.py tests/agent/test_reviewed_memory_context_retrieve.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "needs_reviewed_memory_context" src/agent/routing.py src/agent/nodes/reviewed_memory_context_retrieve.py tests/agent/test_intent_routing.py tests/agent/test_reviewed_memory_context_retrieve.py` matches.
    - `rg -n "needs_long_term_memory" src/agent/routing.py src/agent/nodes/reviewed_memory_context_retrieve.py tests/agent/test_intent_routing.py tests/agent/test_reviewed_memory_context_retrieve.py` still matches alias coverage.
    - `rg -n "return \"long_term_memory_retrieve\"" src/agent/routing.py` matches.
    - Both automated commands exit 0.
  </acceptance_criteria>
  <done>Canonical and legacy reviewed-memory hints route consistently without changing graph node keys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove graph alias and contextual-only boundaries remain stable</name>
  <read_first>
    - src/agent/graph.py node registration for `session_memory_load` and `long_term_memory_retrieve`
    - src/agent/graph_vocabulary.py compatibility alias entries
    - src/agent/nodes/long_term_memory_retrieve.py compatibility metrics
    - tests/agent/test_graph.py graph node and reviewed-memory route tests
    - tests/agent/test_memory_evidence_boundary.py contextual-only memory boundary tests
    - tests/architecture/test_memory_contract_delta.py graph vocabulary guard patterns
  </read_first>
  <files>tests/agent/test_graph.py, tests/agent/test_memory_evidence_boundary.py</files>
  <behavior>
    - Test 1: full graph run with `needs_reviewed_memory_context` reaches the existing `"long_term_memory_retrieve"` node and returns reviewed-memory compatibility metrics.
    - Test 2: full graph run with `needs_long_term_memory` still reaches the same node.
    - Test 3: `src/agent/graph.py` still registers nodes named `"session_memory_load"` and `"long_term_memory_retrieve"`.
    - Test 4: graph vocabulary still maps `"session_memory_load"` to `"session_context_load"` and `"long_term_memory_retrieve"` to `"memory_context_load"` as compatibility aliases.
    - Test 5: reviewed-memory context loaded through either hint remains contextual-only and is not projected as evidence/business/action/approval/replay authority.
  </behavior>
  <action>
    Extend existing graph tests rather than renaming runtime graph code. Add a canonical-hint variant beside the current legacy-hint test in `tests/agent/test_graph.py`. Assert the final state still has legacy compatibility output keys such as `long_term_memory`, `case_memory`, and `llm_outputs["long_term_memory_retrieve"]` where existing tests already require them.

    Extend `tests/agent/test_memory_evidence_boundary.py` with a canonical-hint case if the file currently covers only `needs_long_term_memory`. The assertion should use the same forbidden authority markers already present in the file and prove the canonical hint does not change memory authority.

    Do not edit `src/agent/graph.py`, `src/agent/graph_vocabulary.py`, or `src/agent/nodes/long_term_memory_retrieve.py` unless a test exposes an actual incompatibility. If any of those files must be edited, preserve the exact strings `"session_memory_load"` and `"long_term_memory_retrieve"`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_graph.py tests/agent/test_memory_evidence_boundary.py -x -q</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check tests/agent/test_graph.py tests/agent/test_memory_evidence_boundary.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "needs_reviewed_memory_context" tests/agent/test_graph.py tests/agent/test_memory_evidence_boundary.py` matches.
    - `rg -n "session_memory_load|long_term_memory_retrieve" src/agent/graph.py src/agent/graph_vocabulary.py tests/agent/test_graph.py` matches the preserved compatibility names.
    - `git diff -- src/agent/graph.py src/agent/graph_vocabulary.py src/agent/nodes/long_term_memory_retrieve.py` is empty unless a failing test required a compatibility-preserving fix.
    - Both automated commands exit 0.
  </acceptance_criteria>
  <done>Graph execution and contextual-only boundaries are stable for both canonical and legacy reviewed-memory hints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| routing hints -> graph route | User/request-derived hint state affects graph route selection after slot completeness. |
| graph route -> reviewed memory context | Routing to memory context can expose contextual memory to prompt assembly. |
| compatibility node names -> trace/replay/eval | Runtime node names are part of existing trace and test compatibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48.1-09 | T | `_route_after_slots` | mitigate | Check canonical and legacy hints only after required slots are complete; preserve route allowlist. |
| T-48.1-10 | I | reviewed memory context | mitigate | Boundary tests prove canonical hint does not promote memory to evidence, business fact, approval/action, or replay authority. |
| T-48.1-11 | R | graph trace compatibility | mitigate | Tests assert exact graph node names and vocabulary alias mappings remain unchanged. |
| T-48.1-12 | S/I | trusted actor merchant-scope fallback | mitigate | Reviewed-memory helper tests prove fallback is tied to explicit canonical or legacy routing hints and trusted_context actor scope. |
</threat_model>

<verification>
Run the focused reviewed-memory hint gate:
```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_intent_routing.py tests/agent/test_graph.py tests/agent/test_memory_evidence_boundary.py tests/agent/test_reviewed_memory_context_retrieve.py -x -q
UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/routing.py src/agent/nodes/reviewed_memory_context_retrieve.py tests/agent/test_intent_routing.py tests/agent/test_graph.py tests/agent/test_memory_evidence_boundary.py tests/agent/test_reviewed_memory_context_retrieve.py
```
</verification>

<success_criteria>
- `needs_reviewed_memory_context` is accepted as the canonical reviewed-memory routing hint.
- `needs_long_term_memory` remains accepted as an alias.
- Runtime graph names and compatibility wrapper metrics remain unchanged.
- Reviewed memory remains contextual-only under both hints.
</success_criteria>

<output>
After completion, create `.planning/phases/48.1-memory-context-compatibility-debt-cleanup/48.1-03-SUMMARY.md`.
</output>
