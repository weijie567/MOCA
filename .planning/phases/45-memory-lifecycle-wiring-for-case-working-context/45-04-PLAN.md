---
phase: 45-memory-lifecycle-wiring-for-case-working-context
plan: 04
type: execute
wave: 4
depends_on: [45-01, 45-02, 45-03]
files_modified:
  - docs/contract-spec.md
  - tests/memory/test_phase45_contract_alignment.py
  - .planning/MEMORY-REDESIGN-DECISIONS.md
  - .planning/ARCHITECTURE-DEBT.md
  - .planning/phases/45-memory-lifecycle-wiring-for-case-working-context/45-VALIDATION.md
autonomous: true
requirements: [MEM-01, MEM-02]
must_haves:
  truths:
    - "docs/contract-spec.md records Phase 45 CWC lifecycle fields, active read, run_auto link, and terminal finalizer writeback semantics"
    - "Contract tests lock CWC contextual-only authority, no graph-global active_slots writer, no case_memories backfill, no LLM summarizer, and no destructive legacy table/column changes"
    - "Phase 45 validation strategy rows are satisfied by automated tests using only the MOCA-approved uv entrypoint"
    - "Memory architecture debt and redesign decision records distinguish implemented lifecycle wiring from remaining out-of-scope memory redesign ideas"
  artifacts:
    - path: "docs/contract-spec.md"
      provides: "normative CWC lifecycle and AgentState alignment"
      contains: "case_working_context_lifecycle_status"
    - path: "tests/memory/test_phase45_contract_alignment.py"
      provides: "red-line and contract alignment tests"
      contains: "Phase 45"
    - path: ".planning/MEMORY-REDESIGN-DECISIONS.md"
      provides: "Phase 45 defer closure trace"
      contains: "Phase 45"
    - path: ".planning/ARCHITECTURE-DEBT.md"
      provides: "Chinese memory-subsystem ledger entry"
      contains: "Phase 45"
  key_links:
    - from: "docs/contract-spec.md"
      to: "src/agent/state.py"
      via: "AgentState field registry"
      pattern: "case_working_context_lifecycle_status"
    - from: "tests/memory/test_phase45_contract_alignment.py"
      to: "src/memory/case_working_context_lifecycle.py"
      via: "red-line sweeps"
      pattern: "case_working_context_lifecycle"
---

<objective>
Align the contract and final verification artifacts to the implemented Phase 45 lifecycle wiring.

Purpose: Phase 45 crosses the normative memory contract, runtime lifecycle, and project planning records. This final plan locks the public contract, verifies red lines, and records the completed memory-subsystem debt closure without silently expanding into ReAct or other deferred memory redesign ideas.
Output: contract-spec updates, Phase 45 contract/red-line tests, planning ledger updates, validation status updates, and final targeted verification.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/MEMORY-REDESIGN-DECISIONS.md
@.planning/DEFERRED-DECISIONS.md
@.planning/ARCHITECTURE-DEBT.md
@.planning/phases/45-memory-lifecycle-wiring-for-case-working-context/45-CONTEXT.md
@.planning/phases/45-memory-lifecycle-wiring-for-case-working-context/45-RESEARCH.md
@.planning/phases/45-memory-lifecycle-wiring-for-case-working-context/45-VALIDATION.md
@.planning/phases/45-memory-lifecycle-wiring-for-case-working-context/45-PATTERNS.md
@.planning/phases/45-memory-lifecycle-wiring-for-case-working-context/45-01-SUMMARY.md
@.planning/phases/45-memory-lifecycle-wiring-for-case-working-context/45-02-SUMMARY.md
@.planning/phases/45-memory-lifecycle-wiring-for-case-working-context/45-03-SUMMARY.md
@docs/contract-spec.md
@tests/memory/test_phase44_contract_alignment.py
@src/agent/state.py
@src/agent/nodes/receive_request.py
@src/agent/nodes/investigate.py
@src/memory/case_working_context_lifecycle.py
@src/api/services/agent_run_memory.py

<interfaces>
Contract sections to update:
- docs/contract-spec.md Section 9.4: `memory_context_load` state writes/read semantics and `memory_write`/finalizer lifecycle note.
- docs/contract-spec.md AgentState registry: add `case_working_context` and `case_working_context_lifecycle_status` as contextual-only loaded-view fields.
- docs/contract-spec.md Section 13.4a and 13.5: CWC active read, run_auto link, deterministic terminal writeback, PII/ref-only projection, failure isolation, and conflict skip semantics.

Phase 45 red lines from CONTEXT.md:
- D-45-01 and D-45-02: no ReAct implementation and no graph-global active_slots writer.
- D-45-07: no CWC backfill from case_memories and no guessed case identity.
- D-45-10: append_message remains non-linking.
- D-45-17: no changes to long_term_memories, case_memories, or session-memory write policy.
</interfaces>

<source_audit>
SOURCE | ID | Feature / Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | Phase 45 | Resolve canonical case, run_auto link, active CWC read, terminal CWC writeback, ReAct decoupling | 45-01..45-04 | COVERED | Roadmap goal is finalized from CONTEXT and implemented across all plans.
REQ | MEM-01 | CWC read/write surface lifecycle hooks deferred from Phase 44 | 45-01, 45-02, 45-03, 45-04 | COVERED | Active read and terminal writeback close the named MEM-01 defer.
REQ | MEM-02 | thread-case M:N association used as real run lifecycle link | 45-02, 45-03, 45-04 | COVERED | `ConversationRepository.link_case(... link_source="run_auto")` gets run callers.
RESEARCH | adapter boundary | Stable lifecycle adapter, core CWC service graph-agnostic | 45-01, 45-02, 45-03 | COVERED | Business rules live in adapter; repository/service stay graph-neutral.
RESEARCH | finalizer hook | Prefer `src/api/services/agent_run_memory.py` terminal finalizer | 45-03 | COVERED | Writeback runs after terminal response artifacts.
RESEARCH | active-read seam | Extend reviewed memory context seam before investigate | 45-02 | COVERED | Existing graph edge is preserved.
RESEARCH | validation architecture | New lifecycle, contract, and finalizer tests | 45-01..45-04 | COVERED | All commands use `UV_CACHE_DIR=/tmp/uv-cache uv run ...`.
CONTEXT | D-45-01 | GAD-01 locked to A / loop-local | 45-04 | COVERED | Red-line test rejects investigate graph-global active_slots writer.
CONTEXT | D-45-02 | CWC lifecycle decoupled from ReAct/topology | 45-01, 45-02, 45-04 | COVERED | No graph edge changes required; adapter boundary used.
CONTEXT | D-45-03 | Stable lifecycle adapter/service boundary | 45-01 | COVERED | `CaseWorkingContextLifecycleAdapter`.
CONTEXT | D-45-04 | Terminal finalizer preferred write hook | 45-03 | COVERED | `agent_run_memory.py` integration.
CONTEXT | D-45-05 | Do not hide writeback inside final_response; memory_write optional | 45-03, 45-04 | COVERED | Finalizer adapter hook, no final_response writeback.
CONTEXT | D-45-06 | Active read after identity resolution before investigate | 45-02 | COVERED | memory_context_load compatibility seam.
CONTEXT | D-45-07 | No trusted case => skip, no case_memories backfill | 45-01, 45-02, 45-04 | COVERED | Negative tests and red-line sweep.
CONTEXT | D-45-08 | CWC exposed as contextual-only additive state | 45-01, 45-02, 45-04 | COVERED | AgentState/bundle fields and contract alignment.
CONTEXT | D-45-09 | Link current thread with run_auto/current_run_id | 45-02, 45-03 | COVERED | Read and terminal write paths link/dedupe.
CONTEXT | D-45-10 | append_message remains non-linking | 45-02, 45-04 | COVERED | Tests prove non-linking.
CONTEXT | D-45-11 | Link dedupe/failure status, no silent CWC row | 45-02 | COVERED | Link failure skips active read/write status.
CONTEXT | D-45-12 | Terminal write eligibility rules | 45-03 | COVERED | Completed + final_response + resolved case only; skip matrix tested.
CONTEXT | D-45-13 | Call audited CWC service | 45-03 | COVERED | `write_case_working_context(...)`.
CONTEXT | D-45-14 | Memory failure/conflict must not roll back response | 45-03 | COVERED | Finalizer preservation tests.
CONTEXT | D-45-15 | Deterministic projection, no LLM summarizer | 45-03, 45-04 | COVERED | Projection helper and red-line grep.
CONTEXT | D-45-16 | Safe summaries/refs only; no raw PII/policy body | 45-03, 45-04 | COVERED | Projection tests and PII block.
CONTEXT | D-45-17 | Existing session/long-term/case memory compatible | 45-02, 45-04 | COVERED | No table rename/session policy changes; red-line tests.
CONTEXT | D-45-18 | Approved test entrypoint only | 45-01..45-04 | COVERED | Every command uses UV_CACHE_DIR + uv run.
CONTEXT | D-45-19 | Required active read/link/write/finalizer/red-line tests | 45-01..45-04 | COVERED | Validation strategy rows mapped.
</source_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align contract-spec with Phase 45 lifecycle fields</name>
  <read_first>
    - docs/contract-spec.md Section 9.4 (node contract table)
    - docs/contract-spec.md AgentState field registry
    - docs/contract-spec.md Section 13.4a and 13.5 (CWC + memory write policy)
    - src/agent/state.py (implemented field names)
    - src/api/services/agent_run_memory.py (implemented finalizer metrics/result names)
    - src/memory/case_working_context_lifecycle.py (implemented adapter/status names)
  </read_first>
  <files>docs/contract-spec.md, tests/memory/test_phase45_contract_alignment.py</files>
  <behavior>
    - Test 1: contract-spec Section 9.4 says `memory_context_load` writes `case_working_context` and `case_working_context_lifecycle_status` as contextual-only run state.
    - Test 2: AgentState registry includes `case_working_context` and `case_working_context_lifecycle_status`, with writer `memory_context_load / CaseWorkingContextLifecycleAdapter`.
    - Test 3: Section 13.4a records active CWC read keyed by tenant + `refund_cases.id`, run_auto thread link, terminal deterministic writeback, PII/ref-only boundary, and finalizer failure isolation.
    - Test 4: Section 13.5 records `case_working_context` memory_write_events as audit records only and not evidence/policy/approval/action authority.
  </behavior>
  <action>
    Update docs/contract-spec.md to reflect the implemented Phase 45 field and lifecycle names exactly: `case_working_context`, `case_working_context_lifecycle_status`, `CaseWorkingContextLifecycleAdapter`, `link_source="run_auto"`, and terminal finalizer CWC writeback through `CaseWorkingContextService.write_case_working_context(...)`. State that active read occurs in the memory_context_load compatibility path before investigate consumes memory. State that terminal CWC writeback is a best-effort memory side effect after final response persistence, not a user-response authority path. State that CWC conflict, PII block, and write failure are reported in status/trace and do not roll back assistant message/thread summary/approval/action/user response artifacts. Add tests in `tests/memory/test_phase45_contract_alignment.py` that read the spec as text and assert these exact terms.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_phase45_contract_alignment.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "case_working_context_lifecycle_status" docs/contract-spec.md` matches.
    - `grep -n "run_auto" docs/contract-spec.md` matches in the CWC lifecycle section.
    - The Phase 45 contract alignment pytest passes.
  </acceptance_criteria>
  <done>Normative contract names, lifecycle semantics, and failure semantics match the implemented Phase 45 code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add red-line and validation-scope sweeps</name>
  <read_first>
    - tests/memory/test_phase44_contract_alignment.py (existing contract text test style)
    - src/memory/case_working_context_lifecycle.py (projection/link/read implementation)
    - src/api/services/agent_run_memory.py (finalizer writeback implementation)
    - src/agent/nodes/investigate.py (active_slots red-line surface)
    - src/agent/graph.py (graph topology red-line surface)
    - .planning/phases/45-memory-lifecycle-wiring-for-case-working-context/45-VALIDATION.md
  </read_first>
  <files>tests/memory/test_phase45_contract_alignment.py</files>
  <behavior>
    - Test 1: lifecycle/finalizer code contains no LLM or summarizer dependency names (`ChatOpenAI`, `OpenAI`, `llm`, `summarize`, `summary_llm`) for CWC projection.
    - Test 2: lifecycle/finalizer code does not import or call CaseMemoryRepository/CaseMemoryService and does not use `case_memories` as active CWC fallback.
    - Test 3: `src/agent/nodes/investigate.py` does not assign or return an `active_slots` update for graph-global slot writing.
    - Test 4: `src/agent/graph.py` still does not add a ReAct loop or add a `memory_write` graph edge; final_response remains terminal graph edge.
    - Test 5: source still contains `case_memories`, `long_term_memories`, and `conversation_threads.case_id` references only as retained legacy/schema names, not as renames/drops in Phase 45 code.
    - Test 6: Phase 45 PLAN and VALIDATION files contain no unapproved raw pytest entrypoint commands.
  </behavior>
  <action>
    Extend `tests/memory/test_phase45_contract_alignment.py` with static red-line tests. Use `Path(...).read_text()` plus targeted assertions or AST where needed. For the investigate active_slots red line, use AST to inspect returned dict literals or direct source assertions that block `return {"active_slots": ...}` and `result["active_slots"] = ...` patterns in `src/agent/nodes/investigate.py`; do not reject legitimate reads of active_slots. For approved-entrypoint scans, read `.planning/phases/45-memory-lifecycle-wiring-for-case-working-context/45-*-PLAN.md` and `45-VALIDATION.md`; reject pytest verification commands that do not start with `UV_CACHE_DIR=/tmp/uv-cache uv run pytest`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_phase45_contract_alignment.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "bare" tests/memory/test_phase45_contract_alignment.py` matches the approved-entrypoint guard.
    - `grep -n "active_slots" tests/memory/test_phase45_contract_alignment.py` matches the red-line test.
    - The red-line pytest passes.
  </acceptance_criteria>
  <done>Phase 45 red-line tests prove the implementation did not smuggle in ReAct slot feedback, case_memory backfill, LLM projection, destructive legacy schema changes, or invalid test commands.</done>
</task>

<task type="auto">
  <name>Task 3: Update planning ledgers and run final targeted verification</name>
  <read_first>
    - .planning/MEMORY-REDESIGN-DECISIONS.md (Phase 44 defer markers and Phase 45 trace location)
    - .planning/ARCHITECTURE-DEBT.md (memory subsystem section and Chinese entry style)
    - .planning/phases/45-memory-lifecycle-wiring-for-case-working-context/45-VALIDATION.md (Nyquist rows)
    - .planning/phases/45-memory-lifecycle-wiring-for-case-working-context/45-01-SUMMARY.md
    - .planning/phases/45-memory-lifecycle-wiring-for-case-working-context/45-02-SUMMARY.md
    - .planning/phases/45-memory-lifecycle-wiring-for-case-working-context/45-03-SUMMARY.md
  </read_first>
  <files>.planning/MEMORY-REDESIGN-DECISIONS.md, .planning/ARCHITECTURE-DEBT.md, .planning/phases/45-memory-lifecycle-wiring-for-case-working-context/45-VALIDATION.md</files>
  <action>
    Append a Phase 45 completion trace to `.planning/MEMORY-REDESIGN-DECISIONS.md` naming the closed defers: active CWC read, thread-case `run_auto` link caller, and terminal CWC writeback. Keep out-of-scope items explicitly scoped to their named later phases or existing deferred records; do not relabel them as implemented. Append a Chinese memory-subsystem entry to `.planning/ARCHITECTURE-DEBT.md` with status `✅已修复验证`, covering the original Phase 44 lifecycle defer/root cause, Phase 45 fix, evidence files/phase summaries/tests, and residual risks. Update `45-VALIDATION.md` only after the final commands pass: set `nyquist_compliant: true`, `wave_0_complete: true`, mark the listed task rows green, and add the exact final verification commands/results. If any command fails, do not set those fields true; record the failure and entry point for continuation.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_context_refs.py tests/agent/test_case_working_context_lifecycle.py tests/agent/test_nodes/test_receive_request.py tests/agent/test_reviewed_memory_context_retrieve.py tests/memory/test_case_identity.py tests/memory/test_thread_case_links.py tests/memory/test_case_working_context_service.py tests/test_agent_runs_api.py tests/memory/test_phase44_contract_alignment.py tests/memory/test_phase45_contract_alignment.py -q</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/memory/context_refs.py src/memory/case_working_context_lifecycle.py src/agent/state.py src/agent/nodes/receive_request.py src/agent/nodes/reviewed_memory_context_retrieve.py src/api/services/agent_run_memory.py tests/memory/test_context_refs.py tests/agent/test_case_working_context_lifecycle.py tests/agent/test_nodes/test_receive_request.py tests/agent/test_reviewed_memory_context_retrieve.py tests/test_agent_runs_api.py tests/memory/test_phase45_contract_alignment.py</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run alembic heads</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/MEMORY-REDESIGN-DECISIONS.md` contains "Phase 45" and all three closure names: "active CWC read", "run_auto", "terminal CWC writeback".
    - `.planning/ARCHITECTURE-DEBT.md` contains a Chinese Phase 45 memory entry with `✅已修复验证`.
    - `45-VALIDATION.md` has `nyquist_compliant: true` and `wave_0_complete: true` only if the final pytest/ruff/alembic commands passed.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run alembic heads` reports single head `022_case_working_context (head)`.
  </acceptance_criteria>
  <done>Planning ledgers distinguish implemented Phase 45 lifecycle wiring from remaining deferred memory ideas, and the final targeted suite is green through the MOCA-approved entrypoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| implementation -> normative contract | Runtime CWC lifecycle behavior must be reflected in docs without overstating authority. |
| static red-line tests -> source code | Contract tests verify prohibited behavior did not enter source. |
| validation docs -> phase completion record | Completion status must reflect commands that actually passed through the project environment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | S/I | contract/spec and tests | mitigate | Contract tests assert tenant-scoped `refund_cases.id` active read/write semantics and run_auto link caller; no cross-tenant fallback is documented or allowed. |
| T-45-02 | E | docs, AgentState registry, red-line tests | mitigate | Spec labels CWC contextual-only and tests reject imports/claims that promote CWC into evidence, policy, approval, action, or replay authority. |
| T-45-03 | I | red-line tests and validation | mitigate | Static tests reject LLM/summarizer projection, policy body text, raw tool payload persistence, and invalid PII bypass; Phase 45 validation includes PII block coverage. |
| T-45-04 | D | finalizer contract and tests | mitigate | Spec and tests require assistant message/thread summary/user response preservation on CWC error, block, or conflict. |
| T-45-05 | R | memory_write_events and planning ledgers | mitigate | Spec/ledger require run_id/source_ref-bound `case_working_context` audit rows and explicit status/reason trace; completion record cites exact tests. |
| T-45-06 | T | conflict tests and contract | mitigate | Static and DB tests require expected_version conflict skip semantics; docs state conflicts do not overwrite active CWC. |
</threat_model>

<verification>
Run the final targeted Phase 45 suite:
```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_context_refs.py tests/agent/test_case_working_context_lifecycle.py tests/agent/test_nodes/test_receive_request.py tests/agent/test_reviewed_memory_context_retrieve.py tests/memory/test_case_identity.py tests/memory/test_thread_case_links.py tests/memory/test_case_working_context_service.py tests/test_agent_runs_api.py tests/memory/test_phase44_contract_alignment.py tests/memory/test_phase45_contract_alignment.py -q
UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/memory/context_refs.py src/memory/case_working_context_lifecycle.py src/agent/state.py src/agent/nodes/receive_request.py src/agent/nodes/reviewed_memory_context_retrieve.py src/api/services/agent_run_memory.py tests/memory/test_context_refs.py tests/agent/test_case_working_context_lifecycle.py tests/agent/test_nodes/test_receive_request.py tests/agent/test_reviewed_memory_context_retrieve.py tests/test_agent_runs_api.py tests/memory/test_phase45_contract_alignment.py
UV_CACHE_DIR=/tmp/uv-cache uv run alembic heads
```
</verification>

<success_criteria>
- Contract-spec, tests, and planning ledgers agree on implemented Phase 45 lifecycle behavior.
- Red-line tests pass for ReAct decoupling, no graph-global active_slots writer, no case_memories backfill, no LLM summarizer, no destructive table/column changes, and approved test entrypoints.
- `45-VALIDATION.md` is marked compliant only after commands pass.
- All D-45-01 through D-45-19 are covered by implementation, tests, or explicit out-of-scope red-line checks; no deferred idea from CONTEXT.md is implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/45-memory-lifecycle-wiring-for-case-working-context/45-04-SUMMARY.md`.
</output>
