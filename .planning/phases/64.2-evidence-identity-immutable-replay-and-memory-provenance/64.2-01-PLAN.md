---
phase: 64.2-evidence-identity-immutable-replay-and-memory-provenance
plan: "01"
name: Canonical Evidence Identity Owner And Additive Immutable Schema
type: execute
wave: 1
depends_on: []
files_modified:
  - src/knowledge/evidence_identity.py
  - src/knowledge/schemas.py
  - src/db/models.py
  - src/db/migrations/versions/025_phase64_2_immutable_evidence.py
  - tests/knowledge/test_evidence_identity.py
  - tests/knowledge/test_immutable_evidence_migration.py
autonomous: true
requirements: [SC-64.2-2, SC-64.2-3]
must_haves:
  truths:
    - "Only one tenant-bound owner can mint or validate canonical evidence identity from persisted immutable material."
    - "The first migration is expansion-only: it creates nullable/additive immutable structures and performs no row backfill or read/write cutover."
    - "Legacy aliases are compatibility inputs, never a second canonical identity algorithm."
  artifacts:
    - {path: src/knowledge/evidence_identity.py, provides: "Canonical identity, parser, validation result, and reason codes", contains: "CanonicalEvidenceIdentityV1"}
    - {path: src/db/migrations/versions/025_phase64_2_immutable_evidence.py, provides: "Additive immutable evidence and rollout schema", contains: "policy_document_versions"}
    - {path: tests/knowledge/test_immutable_evidence_migration.py, provides: "Expansion-only migration contract", contains: "test_upgrade_performs_no_backfill"}
  key_links:
    - {from: src/knowledge/schemas.py, to: src/knowledge/evidence_identity.py, via: "EvidenceRefV1 carries the owner-produced immutable binding", pattern: "CanonicalEvidenceIdentityV1"}
    - {from: src/db/models.py, to: src/db/migrations/versions/025_phase64_2_immutable_evidence.py, via: "ORM/Alembic parity", pattern: "PolicyDocumentVersion"}
---

<objective>Establish the single canonical evidence-identity owner and an additive immutable persistence foundation. This plan intentionally does not backfill data, change ingestion writers, switch retrieval reads, or bind replay emitters; those operations belong to Plans 02 and 06.</objective>
<execution_context>@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md @/Users/ming/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md @docs/contract-spec.md @.planning/ROADMAP.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-CONTEXT.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-RESEARCH.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-VALIDATION.md @src/common/canonical_hash.py @src/knowledge/schemas.py @src/knowledge/text_hash.py @src/db/models.py @src/db/migrations/versions/015_rag_production_ingestion_ocr.py @src/db/migrations/versions/024_phase64_1_resume_attempt_lease.py</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define the sole canonical evidence identity contract</name>
  <read_first>@docs/contract-spec.md @src/common/canonical_hash.py @src/knowledge/schemas.py @src/knowledge/text_hash.py @tests/knowledge/test_evidence_projection.py @tests/knowledge/test_text_hash.py</read_first>
  <files>src/knowledge/evidence_identity.py, src/knowledge/schemas.py, tests/knowledge/test_evidence_identity.py</files>
  <action>Implement frozen `CanonicalEvidenceIdentityV1`, `CanonicalEvidenceResolutionV1`, and stable internal/external reason-code types in `src/knowledge/evidence_identity.py`. Canonical hash material is exactly `schema_version=evidence_identity.v1`, trusted `tenant_id`, persisted `document_version_id`, persisted `chunk_version_id`, `doc_key`, positive `document_version`, `chunk_id`, positive `chunk_version`, and `text_hash`; output is `sha256:&lt;64 lowercase hex&gt;` through canonical hash profile v1. Extend the existing `EvidenceRefV1` rather than defining another evidence-ref schema. Keep `retrieved_at`, `retrieval_config_version`, `score`, and `rank` outside identity material. Parse `doc_key/chunk_id@vN` only as a legacy compatibility input and return one of `canonical`, `legacy_resolved`, `legacy_unresolved`, or `invalid`; syntax alone can never return canonical. Recompute tenant/IDs/versions/hash at a trusted repository boundary and map all external tenant/hash/missing/ambiguous failures to one generic result while retaining internal reason codes.</action>
  <verify><automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/knowledge/test_evidence_identity.py tests/knowledge/test_evidence_projection.py tests/knowledge/test_text_hash.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/knowledge/evidence_identity.py src/knowledge/schemas.py tests/knowledge/test_evidence_identity.py</automated></verify>
  <acceptance_criteria>`tests/knowledge/test_evidence_identity.py` proves deterministic persisted-material identity, score/rank independence, malformed legacy handling, cross-tenant/hash/version rejection, one generic external failure, and no second downstream evidence-ref schema.</acceptance_criteria>
  <done>Canonical identity goldens and trust-boundary negatives pass through the single owner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add immutable version and rollout structures without backfill</name>
  <read_first>@src/db/models.py @src/db/migrations/versions/015_rag_production_ingestion_ocr.py @src/db/migrations/versions/024_phase64_1_resume_attempt_lease.py @tests/replay/test_replay_migration_contract.py @tests/rag/test_ingestion_safety.py</read_first>
  <files>src/db/models.py, src/db/migrations/versions/025_phase64_2_immutable_evidence.py, tests/knowledge/test_immutable_evidence_migration.py</files>
  <action>Add append-only `PolicyDocumentVersion` and `PolicyChunkVersion` ORM/table structures with tenant-bound composite constraints, positive document/chunk versions, content/text hashes, lifecycle status, and nullable supersession/correction links. Add nullable `agent_trace_events.evidence_snapshot_refs_json`. Add an `evidence_identity_rollouts` record with nullable `dual_write_enabled_at`, `backfill_watermark`, `reconciled_through`, `canonical_read_enabled_at`, and audit counts so Plan 02 has an observable cutover state. Migration 025 must be schema expansion only: no `INSERT ... SELECT`, no update of current `PolicyDocument`/`PolicyChunk` rows, no backfill helper invocation, no non-null enforcement, and no read/write flag activation. Its downgrade must refuse to drop immutable history once any version or snapshot row exists. Test upgrade from head 024, constraints/default-null rollout state, zero copied rows despite seeded mutable heads, and guarded downgrade.</action>
  <verify><automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/knowledge/test_immutable_evidence_migration.py tests/replay/test_replay_migration_contract.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/db/models.py src/db/migrations/versions/025_phase64_2_immutable_evidence.py tests/knowledge/test_immutable_evidence_migration.py</automated></verify>
  <acceptance_criteria>Upgrade creates only additive nullable structures; seeded mutable heads produce zero immutable/backfill rows; all identity FKs/checks are tenant/version bound; downgrade cannot discard referenced history.</acceptance_criteria>
  <done>Migration 025 is proven expansion-only and ready for dual-write installation before any backfill.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Boundary | Disposition | Mitigation |
|---|---|---|---|---|
| T64.2-01 | Spoofing | caller alias -> canonical identity | mitigate | Recompute complete tenant-bound persisted material; never trust supplied IDs. |
| T64.2-02 | Tampering | mutable head -> historical identity | mitigate | Append-only version schema with exact hashes and guarded downgrade. |
| T64.2-03 | Elevation of Privilege | legacy row -> canonical evidence | mitigate | Plan 01 performs no backfill; resolver distinguishes unresolved compatibility input. |
| T64.2-08 | Information Disclosure | mismatch -> external caller | mitigate | Generic external failure with internal-only reason codes. |
</threat_model>
<verification>Run both task commands. Bare pytest and bare Python are invalid. The migration test must explicitly prove the absence of backfill, not merely successful table creation.</verification>
<success_criteria>Plans 02 and 04 can consume one immutable identity contract and additive schema without any pre-cutover row having been inferred or promoted.</success_criteria>
<output>After completion, create `64.2-01-SUMMARY.md`.</output>
