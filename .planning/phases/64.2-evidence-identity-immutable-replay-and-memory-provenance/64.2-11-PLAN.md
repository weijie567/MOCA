---
phase: 64.2-evidence-identity-immutable-replay-and-memory-provenance
plan: "11"
name: Approval Fixture Rollout State And Final Regression Gate
type: execute
wave: 8
depends_on: ["64.2-10"]
files_modified:
  - tests/conftest.py
  - tests/architecture/test_evidence_memory_integrity_boundaries.py
autonomous: true
gap_closure: true
requirements: [SC-64.2-2]
must_haves:
  truths:
    - "The approval mock graph seeds a fixture-local, database-constraint-valid current-read-gate EvidenceIdentityRollout singleton before exact current-evidence validation runs."
    - "The rollout state is scoped to mock_graph/_seed_approval_policy and is not globally added to test_engine, preserving tests that intentionally exercise missing or disabled rollout state."
    - "High-risk approve, reject, and idempotent resume paths plus the latest-decision-context matrix expose exactly one stable approval_id and keep one-draft semantics."
    - "Production exact evidence validation remains fail closed; no legacy alias or mutable-head fallback is added."
    - "After both gap plans pass, repository Ruff and one serial complete pytest suite close the post-review UAT regression gate."
    - "The Phase 64.2 architecture guard recognizes the complete 01-11 plan graph and still rejects missing dependencies, cycles, wave inversions, and unordered shared-file edits."
  artifacts:
    - {path: tests/conftest.py, provides: "Approval-only canonical-read rollout control-plane fixture", contains: "EvidenceIdentityRollout"}
    - {path: tests/architecture/test_evidence_memory_integrity_boundaries.py, provides: "Exact Phase 64.2 01-11 plan-graph guard", contains: "range(1, 12)"}
  key_links:
    - {from: tests/conftest.py, to: src/repositories/evidence_version_repo.py, via: "mock_graph supplies an enabled non-quarantined rollout before validate_current_evidence requests current identities", pattern: "canonical_reads_enabled"}
    - {from: tests/conftest.py, to: tests/test_approval_integration.py, via: "shared mock_graph yields canonical evidence that reaches approval_gate", pattern: "mock_graph"}
    - {from: tests/architecture/test_evidence_memory_integrity_boundaries.py, to: .planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance, via: "exact glob and dependency graph assertion cover all numbered plans", pattern: "64.2-??-PLAN.md"}
---

<objective>Make the shared approval graph fixture represent the migrated production evidence-rollout control plane, then run the sole post-gap repository-wide regression gate.</objective>

<purpose>Close UAT gap 2 at its fixture owner and prove approval interrupt/resume behavior without weakening exact current-evidence validation or contaminating unrelated database tests.</purpose>

<output>A fixture-local enabled rollout setup in `tests/conftest.py`, four restored approval integration nodes, one serial green complete suite, and a `64.2-11-SUMMARY.md` execution record.</output>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-CONTEXT.md
@.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-RESEARCH.md
@.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-UAT.md
@.planning/debug/phase64-2-approval-payload.md
@.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-02-SUMMARY.md
@.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-04-SUMMARY.md
@.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-09-SUMMARY.md
@.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-10-SUMMARY.md
@src/db/models.py
@src/repositories/evidence_version_repo.py
@tests/test_approval_integration.py
@tests/integration/test_phase64_1_runtime_safety_matrix.py

<interfaces>
`EvidenceIdentityRollout` is the singleton control-plane row (`id=1`). For this focused current-read gate fixture, the database and repository require a stable nonnegative rollout epoch, non-null `dual_write_enabled_at`, `canonical_reads_enabled=true`, and no quarantine. Watermark/reconciled fields and audit metadata are not evidence of a real migration lifecycle here and remain unset/default. `EvidenceVersionRepository.get_current_identities_by_keys` rejects a missing, disabled, or quarantined singleton before resolving current immutable bindings.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Seed current-read rollout gate only for the approval mock graph</name>
  <files>tests/conftest.py</files>
  <behavior>
    - "The ordinary approval mock_graph database has one enabled, non-quarantined EvidenceIdentityRollout singleton before the graph validates evidence."
    - "The complete approval integration module plus the latest-decision-context matrix node reach their existing approval or low-risk terminal outcomes without fixture regressions."
    - "Unrelated tests using test_engine without mock_graph still control missing/disabled rollout state themselves."
  </behavior>
  <action>Per D-05, D-06, and D-12, import `EvidenceIdentityRollout` into `tests/conftest.py` and add a private approval-fixture helper called only from `_seed_approval_policy` or immediately inside `mock_graph`. The helper must assert the singleton is absent, then add the minimum database-constraint-valid row that represents only the current-read gate precondition: `id=1`, a stable nonnegative rollout epoch, non-null UTC `dual_write_enabled_at`, `canonical_reads_enabled=True`, optional matching UTC `canonical_reads_enabled_at`, `canonical_reads_disabled_at=None`, and `quarantine_reason=None`. Leave backfill watermark/reconciled-through unset and audit metadata at its default/empty value; do not claim that this create_all-based fixture ran dual-write health proof, watermark backfill, or reconciliation. Flush it before the graph can call `validate_current_evidence`. Keep the existing repository-minted document/chunk identity and current/immutable rows unchanged. Do not seed the singleton in the global `test_engine` fixture, because evidence-cutover tests intentionally own missing/inactive state; do not alter `src/repositories/evidence_version_repo.py`, approval serializers, graph routing, or any product fallback. Per D-19, run the complete changed approval integration module plus the exact Phase 64.1 matrix node in one serial pytest invocation and ensure no other pytest/uv PostgreSQL process is active before starting.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_approval_integration.py tests/integration/test_phase64_1_runtime_safety_matrix.py::test_high_action_uses_latest_decision_context_before_one_approved_draft -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check tests/conftest.py</automated>
  </verify>
  <acceptance_criteria>The complete approval integration module and exact matrix node pass serially, including approve/reject/idempotent, low-risk, and expired-decision behavior; scoped Ruff passes; the fixture diff changes only approval-scoped current-read gate setup.</acceptance_criteria>
  <done>UAT gap 2 is closed at the fixture control-plane boundary, and the production approval payload/resume path is proven healthy under canonical-read availability.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the exact architecture guard to the 01-11 plan graph</name>
  <files>tests/architecture/test_evidence_memory_integrity_boundaries.py</files>
  <action>Update the Phase 64.2 plan loader and exact graph assertion from plans 01-09 to plans 01-11 now that the two reviewed gap-closure plans are part of the phase. Preserve all structural mutation checks for missing dependencies, cycles, wave inversion, same-wave shared files, and unordered shared-file edits; do not exclude plans merely because `gap_closure: true`. Keep Plan 09 decision-coverage assertions scoped to the original D-01..20 ownership table rather than pretending the gap plans introduced new context decisions. The parent autopilot closeout, not this architecture test task, will synchronize ROADMAP's plan count and checklist after execution evidence exists.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_evidence_memory_integrity_boundaries.py::test_phase64_2_plan_graph_is_exact_acyclic_and_shared_file_ordered tests/architecture/test_evidence_memory_integrity_boundaries.py::test_phase64_2_plan_graph_guard_rejects_structural_mutations -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check tests/architecture/test_evidence_memory_integrity_boundaries.py</automated>
  </verify>
  <acceptance_criteria>Both exact plan-graph nodes pass with plans 01-11 present; the mutation guard retains every existing negative structural assertion; scoped Ruff passes.</acceptance_criteria>
  <done>The final full suite can load the reviewed 01-11 plan graph while preserving dependency and shared-file ordering enforcement.</done>
</task>

<task type="auto">
  <name>Task 3: Run the sole post-gap repository-wide regression gate</name>
  <files>tests/conftest.py, tests/architecture/test_evidence_memory_integrity_boundaries.py</files>
  <action>After Task 1 and Plan 10 are committed, confirm no other pytest/uv PostgreSQL test process is active. Run repository Ruff first, then run the complete pytest suite exactly once and serially with the mandated MOCA environment. Do not use `-n`, concurrent plan execution, bare `pytest`, bare `python -m pytest`, or `--lf` as a substitute for the explicit complete suite. Record exact pass/skip/warning counts and duration in the plan summary for the parent UAT closeout. If any failure appears, stop, preserve the output, and append the incident in Chinese to `.planning/LOCAL-VALIDATION-ISSUES.md` before diagnosis; do not relabel a failing complete suite as success.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q --tb=short &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>Repository Ruff, the one explicit complete serial suite, and `git diff --check` all pass; exact counts are captured in `64.2-11-SUMMARY.md`; no pytest process remains afterward. The parent closeout then synchronizes ROADMAP from 9 plans to 11.</acceptance_criteria>
  <done>Both diagnosed post-review fixture gaps are covered by a clean, serial repository-wide regression result owned exactly once by Plan 11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test schema setup -> exact current-evidence repository | `Base.metadata.create_all` omits migration data, so approval fixture setup must explicitly represent only the enabled current-read gate without claiming migration/reconciliation execution or changing global test semantics. |
| Canonical evidence -> approval gate | Only exact current canonical evidence may unlock approval interruption; a missing rollout must fail closed rather than fall back. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T64.2-01 | Spoofing/Tampering | Approval graph evidence input | mitigate | Retain repository-minted exact refs/current immutable rows; add only the missing enabled singleton precondition. |
| T64.2-03 | Tampering | Rollout control-plane fixture | mitigate | Seed a minimal constraint-valid, enabled, non-quarantined singleton in approval scope only; leave reconciliation-only fields unset and never bypass the repository gate. |
| T64.2-08 | Information disclosure | Missing/cross-scope evidence before approval | mitigate | Keep generic exact-validator failure behavior untouched and prove the positive fixture reaches approval only with valid canonical state. |
</threat_model>

<source_audit>

| SOURCE | ID | Coverage | Plan | Status |
|---|---|---|---|---|
| GOAL | Phase 64.2 | Approval and agent consumers validate canonically identified immutable source material under the deployed rollout state | 11 | COVERED |
| REQ | SC-64.2-1 | Canonical RAG observation promotion regression is closed by Plan 10 | 10 | COVERED |
| REQ | SC-64.2-2 | Agent/approval current-evidence validation uses the one exact tenant/scope/immutable owner | 10, 11 | COVERED |
| REQ | SC-64.2-3..5 | Replay and memory requirements remain green from Plans 01-09 and are rechecked by Plan 11's sole complete suite | 01-09, 11 | COVERED / UNCHANGED |
| RESEARCH | Current-read rollout gate | Approval fixture represents the production migration's enabled singleton instead of bypassing exact reads | 11 | COVERED |
| CONTEXT | D-01..04 | Authoritative promotion remains fail closed and unchanged | 05, 07, 09, 10, 11 | COVERED |
| CONTEXT | D-05, D-06 | Canonical owner, exact scope, and immutable bindings remain mandatory before approval | 10, 11 | COVERED |
| CONTEXT | D-07..12 | No compatibility fallback or mutable replay substitution is introduced; external failures stay generic | 01, 02, 06, 09, 10, 11 | COVERED |
| CONTEXT | D-13..17 | Memory identity/provenance/lifecycle remain implemented by Plans 03/07/08/09 and are rechecked, not modified | 03, 07, 08, 09, 11 | COVERED / UNCHANGED |
| CONTEXT | D-18 | RAG and approval fixture domains are separate numbered plans with an explicit dependency | 10, 11 | COVERED |
| CONTEXT | D-19 | Four exact PostgreSQL nodes and one final serial complete suite are explicit automated gates | 11 | COVERED |
| CONTEXT | D-20 | No production/spec scope delta is introduced; parent closeout updates UAT only from actual evidence | 10, 11 | COVERED |
| CONTEXT | Deferred ideas | Retrieval quality, PII governance, operation dispatch, LLM gateway, and broad service decomposition remain excluded | NONE | EXCLUDED AS DEFERRED |

</source_audit>

<verification>
- Plan 10 is complete before Plan 11 starts; the two ownership domains never edit the same file.
- Task 1's complete approval integration module and exact matrix node run in one serial process and pass under the enabled fixture-local rollout.
- Task 2 updates and verifies the exact 01-11 plan graph before repository-wide validation.
- Task 3 owns the explicit repository-wide `UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q --tb=short` command exactly once across the gap plan set.
- Repository Ruff and `git diff --check` pass, and no bare test/tool command is accepted as evidence.
</verification>

<success_criteria>
- `test_engine` remains rollout-neutral; `mock_graph` alone establishes the minimal valid enabled current-read state without claiming reconciliation.
- The Phase 64.2 architecture guard covers the exact 01-11 dependency graph.
- The four named approval nodes pass through interrupt, decision, resume, and one-draft assertions.
- No production validator, repository, graph, or serializer change is needed.
- The complete serial suite and repository Ruff pass after both gap fixes.
</success_criteria>

<output>After completion, create `.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-11-SUMMARY.md` with exact focused and full-suite results.</output>
