---
phase: 64.2-evidence-identity-immutable-replay-and-memory-provenance
plan: "10"
name: Canonical RAG Combined-Status Fixture Repair
type: execute
wave: 7
depends_on: ["64.2-09"]
files_modified:
  - tests/agent/test_nodes/test_rag_context_build.py
autonomous: true
gap_closure: true
requirements: [SC-64.2-1, SC-64.2-2]
must_haves:
  truths:
    - "The combined wrong-scope, stale-version, invalid-hash, and malformed-ref node test reaches exact current-evidence validation with owner-minted canonical refs and full immutable rows."
    - "Canonical combined input still produces invalid_hash precedence while the wrong-scope candidate uses the bounded evidence_unavailable reason and leaks no identity on ordinary projections."
    - "No product compatibility fallback, reduced evidence alias, or caller-trusted identity path is introduced to make the regression pass."
  artifacts:
    - {path: tests/agent/test_nodes/test_rag_context_build.py, provides: "Canonical node fixture and combined fail-closed precedence regression", contains: "get_current_canonical_evidence_rows_by_keys"}
  key_links:
    - {from: tests/agent/test_nodes/test_rag_context_build.py, to: src/knowledge/evidence_identity.py, via: "fixture mints persisted canonical material before projecting EvidenceRefV1", pattern: "PersistedEvidenceIdentityMaterialV1"}
    - {from: tests/agent/test_nodes/test_rag_context_build.py, to: src/knowledge/service.py, via: "fake exposes the exact current-row API consumed by validate_current_evidence", pattern: "get_current_canonical_evidence_rows_by_keys"}
---

<objective>Repair the stale RAG node fixture so the post-review exact-validator path, not a compatibility alias path, determines combined invalid-evidence status and reason precedence.</objective>

<purpose>Close UAT gap 1 without weakening the Phase 64.2 canonical identity trust boundary. This is a test-fixture migration only; production evidence validation remains unchanged.</purpose>

<output>A canonical combined-status regression in `tests/agent/test_nodes/test_rag_context_build.py` and a `64.2-10-SUMMARY.md` execution record.</output>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-CONTEXT.md
@.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-RESEARCH.md
@.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-UAT.md
@.planning/debug/phase64-2-rag-status.md
@.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-02-SUMMARY.md
@.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-09-SUMMARY.md
@tests/knowledge/test_verified_evidence_package.py
@src/knowledge/evidence_identity.py
@src/knowledge/schemas.py
@src/knowledge/service.py

<interfaces>
Use the existing canonical fixture contract from `tests/knowledge/test_verified_evidence_package.py`: `PersistedEvidenceIdentityMaterialV1` -> `mint_canonical_evidence_identity(...)` -> `EvidenceRefV1.from_canonical_identity(...)`; a canonical fake row spreads `ref.to_canonical_identity().model_dump(mode="json")`; the retriever implements both canonical and current-canonical row methods.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate the combined-status node fixture to exact canonical identity</name>
  <files>tests/agent/test_nodes/test_rag_context_build.py</files>
  <behavior>
    - "Canonical combined wrong-scope + stale-version + invalid-hash input returns rag_context_status=invalid_hash."
    - "The wrong-scope candidate is rejected with bounded evidence_unavailable; the stale candidate records latest_version_invalid; the mutated-content candidate records text_hash_mismatch."
    - "Malformed input still contributes candidate_ref_invalid, all three rejected/stale lists preserve their intended membership, and ordinary projections expose none of the rejected evidence IDs."
  </behavior>
  <action>Per D-05, D-06, and D-12, replace the local reduced `_ref` helper with the canonical owner pattern already used by `tests/knowledge/test_verified_evidence_package.py`: build `PersistedEvidenceIdentityMaterialV1` with exact string tenant/scope, deterministic immutable document/chunk version UUIDs derived from the fixture key, integer document/chunk versions, and `evidence_text_hash(text)`; mint through `mint_canonical_evidence_identity`; then project through `EvidenceRefV1.from_canonical_identity` while retaining retrieval diagnostics such as rank and score outside identity material. Make `_row` spread the complete `ref.to_canonical_identity()` projection before adding content/current-policy metadata, so every exact identity field is present and overrides remain limited to the intended status fault. Add `FakeCanonicalRetriever.get_current_canonical_evidence_rows_by_keys` with the same tenant-filtered behavior as the existing canonical method, and retain call recording needed by the test. In the combined test keep `invalid_hash` as the package status, change the pre-exact `tenant_mismatch` expectation to bounded `evidence_unavailable`, and retain stale/hash/malformed/rejected-list/no-leak assertions. Do not modify `src/knowledge/service.py`, `src/agent/rag_context/builder.py`, or `src/agent/nodes/rag_context_build.py`; do not restore `EvidenceRefV1.build` compatibility fallback in the production path. Per D-19, use only the MOCA `uv run` test entrypoint.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_rag_context_build.py tests/knowledge/test_verified_evidence_package.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check tests/agent/test_nodes/test_rag_context_build.py</automated>
  </verify>
  <acceptance_criteria>The complete changed node module and canonical verified-package regression file pass; scoped Ruff passes; `git diff -- tests/agent/test_nodes/test_rag_context_build.py` contains only canonical fixture/assertion migration and no production fallback.</acceptance_criteria>
  <done>UAT gap 1 is closed by canonical fixture parity, and combined invalid evidence once again proves the intended invalid_hash precedence under exact current-row validation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test candidate -> exact current-evidence validator | Fixture-supplied IDs, tenant/scope, versions, and hashes are untrusted until the canonical owner and exact current-row seam agree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T64.2-01 | Spoofing/Tampering | RAG node canonical evidence fixture | mitigate | Mint refs from complete persisted material and return full immutable rows; keep forged/wrong-scope/hash mutations as explicit negative inputs. |
| T64.2-08 | Information disclosure | Combined wrong-scope diagnostics | mitigate | Assert bounded `evidence_unavailable` and verify rejected IDs never appear in prompt/verifier projections. |
</threat_model>

<source_audit>

| SOURCE | ID | Coverage | Plan | Status |
|---|---|---|---|---|
| GOAL | Phase 64.2 | Canonically identified evidence remains trustworthy at the RAG agent boundary | 10 | COVERED |
| REQ | SC-64.2-1 | Only validated canonical retrieval observations can reach authoritative projection | 10 | COVERED |
| REQ | SC-64.2-2 | One exact tenant/scope/immutable identity validation path rejects forged and mismatched candidates | 10, 11 | COVERED |
| REQ | SC-64.2-3..5 | Immutable replay and memory identity/lifecycle are already covered by Plans 01-09 and are not implicated by either diagnosed UAT gap | 01-09 | COVERED / UNCHANGED |
| RESEARCH | Canonical fixture owner | Reuse owner-minted refs, full immutable rows, and exact current-row retrieval rather than local aliases | 10 | COVERED |
| CONTEXT | D-01..04 | Promotion/authority behavior remains owned by Plans 05/07/09; this fixture preserves the fail-closed boundary | 05, 07, 09, 10 | COVERED |
| CONTEXT | D-05, D-06 | Canonical owner and complete immutable identity are used by the repaired fixture | 10, 11 | COVERED |
| CONTEXT | D-07..12 | Compatibility stays explicit, immutable replay stays unchanged, and wrong-scope diagnostics remain generic | 01, 02, 06, 09, 10, 11 | COVERED |
| CONTEXT | D-13..17 | Memory identity/provenance/lifecycle remain covered by Plans 03/07/08/09 and are outside these fixture gaps | 03, 07, 08, 09 | COVERED / UNCHANGED |
| CONTEXT | D-18 | Separate RAG and approval fixture ownership domains use separate plans | 10, 11 | COVERED |
| CONTEXT | D-19 | Exact unit/integration gates plus one serial repository-wide gate are assigned | 10, 11 | COVERED |
| CONTEXT | D-20 | No target-contract or production implementation delta is introduced | 10, 11 | COVERED |
| CONTEXT | Deferred ideas | Retrieval quality, PII governance, operation dispatch, LLM gateway, and broad service decomposition remain excluded | NONE | EXCLUDED AS DEFERRED |

</source_audit>

<verification>
- The task's exact automated command passes using `UV_CACHE_DIR=/tmp/uv-cache uv run ...`; bare pytest/python is invalid evidence.
- `git diff --check` passes.
- Plan 11, which depends on this plan, owns the repository-wide full-suite gate exactly once after both fixture repairs land.
</verification>

<success_criteria>
- The stale fixture reaches the same exact-current validator seam as production and the canonical service tests.
- The combined package status is `invalid_hash`, with bounded `evidence_unavailable`, `latest_version_invalid`, `text_hash_mismatch`, and `candidate_ref_invalid` diagnostics.
- No production file changes and no legacy/current compatibility fallback are present.
</success_criteria>

<output>After completion, create `.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-10-SUMMARY.md`.</output>
