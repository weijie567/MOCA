---
phase: "64.2"
slug: evidence-identity-immutable-replay-and-memory-provenance
status: verified
threats_total: 8
threats_closed: 8
threats_open: 0
threats_accepted: 0
threats_deferred: 0
unregistered_flags: 0
asvs_level: 1
created: 2026-08-06
updated: 2026-08-06
---

# Phase 64.2 — Security

> 本文档只核验 Phase 64.2 Plans 01–11 已登记的威胁及其 mitigation，不是无边界的新漏洞扫描。Plans 10/11 仅修复测试夹具并重跑回归，不构成新的生产攻击面。

## Audit Scope And Disposition Counts

| Metric | Count |
|---|---:|
| Unique registered threats | 8 |
| Mitigated / closed | 8 |
| Open | 0 |
| Accepted | 0 |
| Deferred registered threats | 0 |
| Unregistered summary flags | 0 |

Plans 01–09 的重复 threat ID 按 T64.2-01..08 合并核验；Plan 09 的范围写法 `01..03`、`05..06` 不重复计数。Plans 09–11 的 `## Threat Flags` 均明确为 `None`，Plans 10/11 只验证既有边界。

## Trust Boundaries

| Boundary | Description | Data Crossing |
|---|---|---|
| Caller / retrieval / API -> canonical evidence owner | 调用方字段在可信 immutable repository 下重算后才可成为 canonical evidence | tenant、exact policy scope、immutable IDs/versions、text hash |
| Mutable ingestion / legacy heads -> immutable history and cutover | rollout-first lock、单调 sequence、dual-write、watermark reconciliation 与 zero-gap CAS 控制 cutover | current heads、immutable versions、rollout epoch、quarantine state |
| Production event chain -> replay snapshot | 新写只传 typed canonical refs；append owner 重算并原子写 snapshot/dependency | canonical ref、retained locator/hash、lifecycle、retention |
| Tool / retrieval observation -> CWC verified fact | typed authority/status/completeness/scope/freshness/ref gate 决定 promote/observe/reject | tool result、business refs、policy evidence refs、bounded reasons |
| CWC / reviewer / migration -> reviewed CaseMemory | 只接收 promoted sources；source authority 与 contextual-only memory authority 分离 | source refs/status/authority、reviewer metadata、resolved/unresolved provenance |
| Concurrent memory writer / reviewer / tombstone -> durable lifecycle | 七段 exact identity claim、row lock、lifecycle CAS、event/tombstone/lineage 同事务 | candidate/source hashes、expected version、terminal state、lineage |

## Threat Register

| Threat ID | Category | Components / Boundaries | Disposition | Mitigation / Confirmed Evidence | Status |
|---|---|---|---|---|---|
| T64.2-01 | Spoofing / Tampering | caller, retrieval, approval, CWC evidence -> canonical authority | mitigate | `src/knowledge/evidence_identity.py:168-235` 从 persisted material 重算并逐字段比较；`src/repositories/evidence_version_repo.py:632-708` 将 exact resolve 与 legacy resolve 分离；`src/approvals/service.py:1061-1107` 在可信 tenant 下调用 repository exact resolver；`src/memory/case_working_context_lifecycle.py:55-80` 在提升前解析 retained row。负向证据：`tests/knowledge/test_evidence_identity.py:135-187`、`tests/approvals/test_phase64_2_evidence_validation.py:352-452`、`tests/agent/test_case_working_context_lifecycle.py:1055-1148`。 | closed |
| T64.2-02 | Tampering | mutable head / re-ingestion / backfill / production event -> historical replay | mitigate | migration 025 用 RESTRICT FK、retention/immutable triggers 与 guarded downgrade 保护 retained rows（`src/db/migrations/versions/025_phase64_2_immutable_evidence.py:235-284,332-477`）；writer 只追加 immutable versions 并投影同一 sequence（`src/repositories/evidence_version_repo.py:461-630`）；migration 026 unresolved 时保留 quarantine 且抛错使 revision 停留 025（`src/db/migrations/versions/026_phase64_2_evidence_cutover.py:40-119`）；`ReplayService.append_event` 是 snapshot/dependency owner，读取时精确重验 retained row（`src/replay/service.py:83-103,171-284,424-483`）。证据：`tests/knowledge/test_evidence_cutover.py:100-490`、`tests/replay/test_production_evidence_binding.py:306-543`、`tests/integration/test_phase64_2_integrity_matrix.py:241-751`。 | closed |
| T64.2-03 | Elevation of Privilege / Tampering | ambiguous legacy evidence or rollout fixture -> canonical / approval / replay authority | mitigate | legacy alias 只有唯一 persisted mapping 才能返回 `legacy_resolved`，否则保持 `legacy_unresolved`（`src/knowledge/evidence_identity.py:238-330`）；exact repository API 显式拒绝 legacy-resolved input（`src/repositories/evidence_version_repo.py:632-693`）；旧 replay JSON 只能从 tenant-bound existing event 的只读 adapter 进入，任何歧义/缺失仍 unresolved（`src/replay/service.py:485-544`）。Plan 11 只在 approval fixture 局部补 enabled rollout singleton，没有生产绕过。证据：`tests/knowledge/test_evidence_identity.py:189-233`、`tests/replay/test_production_evidence_binding.py:544-611`、`tests/test_approval_integration.py` 的最终绿门禁。 | closed |
| T64.2-04 | Tampering | normalization / provenance migration -> stored memory identity | mitigate | legacy `nfkc_casefold_legacy` 与 new-write `nfc_selective_v2` 使用不同 namespace（`src/memory/identity.py:25-36,125-171`）；stored row 只有在 content/source hashes 与某 profile 精确匹配时才可解释，否则失败（`src/memory/identity.py:445-506`）；migration 027 对无法证明完整 provenance 的旧行只写单独的 unresolved envelope（`src/db/migrations/versions/027_phase64_2_memory_provenance.py:175-210`），两种 envelope 由 discriminator 分离（`src/memory/schemas.py:319-413`）。证据：`tests/memory/test_memory_identity.py:298-326`、`tests/memory/test_case_memory_provenance.py:152-193,584-618`。 | closed |
| T64.2-05 | Tampering / Repudiation | local serializers and concurrent submit/review -> event/store/dedupe/lifecycle | mitigate | `MemoryCandidateIdentityV1` 与所有 typed builders 由 `src/memory/identity.py:109-122,243-369` 单一拥有；CaseMemory claim 对完整七段 tuple 建数据库 UNIQUE 且 owner FK 为 RESTRICT（`src/db/models.py:852-920`）；review 在 locked claim 下比较 row/claim expected lifecycle version、完整 payload 与 persisted event，随后同步更新 provenance/claim/event（`src/memory/case_memory.py:949-1060`）。证据：`tests/memory/test_memory_identity.py:327-415,504-702`、`tests/memory/test_case_memory_concurrency.py:132-345`。 | closed |
| T64.2-06 | Elevation of Privilege | delayed writer -> rejected/expired/superseded/deleted/tombstoned identity | mitigate | terminal exact claim 永不释放；旧 identity 的后续提交只返回 `identity_conflict` 且不返回 winner（`src/memory/case_memory.py:905-947`）。delete/tombstone 在同一 locked-claim transaction 内同步 terminalize row/claim、创建 tombstone、写 event（`src/memory/case_memory.py:1315-1390`）；claim owner FK `ondelete=RESTRICT`（`src/db/models.py:872-877`）。证据：`tests/memory/test_case_memory_lifecycle.py:527-744`、`tests/memory/test_case_memory_concurrency.py:347-475`。 | closed |
| T64.2-07 | Elevation of Privilege | observation / source normalization / human review -> verified or memory authority | mitigate | promotion schema 强制只有 successful、complete、fresh、valid-scope、validated `business_fact|policy_evidence` 可 promote；contextual/unknown 永远 observe/reject（`src/memory/fact_promotion.py:65-141,170-255`）。policy refs 还需生产 exact resolver 全集成功（`src/memory/case_working_context_lifecycle.py:592-621,684-742`）；mixed malformed container 在 typed validation 前即 fail closed（同文件 `1039-1047`）。CaseMemory source authority 只接受 business/policy，memory authority 固定 contextual-only，review 只增补 reviewer 字段（`src/memory/schemas.py:281-380`、`src/memory/case_memory.py:1029-1048`）。证据：`tests/agent/test_case_working_context_lifecycle.py:405-521,813-980`、`tests/memory/test_case_memory_provenance.py:195-328,537-582`。 | closed |
| T64.2-08 | Information Disclosure | evidence / replay / review failures -> cross-tenant or cross-scope caller | mitigate | canonical identity 对外失败只有 `evidence_unavailable`、对内才保留原因（`src/knowledge/evidence_identity.py:46-49,142-165,391-399`）；CWC active payload 移除 `internal_reason_code`（`src/memory/case_working_context_lifecycle.py:1139-1143`）；replay 先 tenant/owner 鉴权且 evidence resolution failure 投影 generic not-found（`src/api/routers/traces.py:76-108`）；memory action 将 cross-tenant/not-found 与 stale conflict 投影为 bounded 404/409（`src/api/routers/memory.py:726-743`）。证据：`tests/knowledge/test_evidence_identity.py:235-261`、`tests/replay/test_production_evidence_binding.py:613`、`tests/memory/test_case_memory_provenance.py:620-706`、`tests/integration/test_phase64_2_integrity_matrix.py:914`。 | closed |

## Summary Threat Flags

- Plans 01–08 的 summaries 没有登记新增未建模攻击面；其偏差均映射回 T64.2-01..08 并由对应测试覆盖。
- Plan 09 `## Threat Flags`: None；working-state 修复关闭既有 evidence-integrity gap。
- Plan 10 `## Threat Flags`: None；test-only canonical RAG fixture repair。
- Plan 11 `## Threat Flags`: None；test-only approval rollout fixture + graph/full-suite gate。
- **Unregistered flags:** 0。

## Verification Evidence

- 最终 post-gap repository self-UAT：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q --tb=short` -> `4462 passed, 4 skipped, 155 warnings in 2023.84s`；单一串行进程，exit 0。
- Repository Ruff：`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` -> `All checks passed!`。
- 最终 code review：82-file scope clean，0 critical / 0 warning / 0 info；review-focused security/correctness aggregate `210 passed`，scoped Ruff 与 diff check 通过。
- Phase 64.2 focused closeout aggregate：`204 passed, 15 warnings`；覆盖 T64.2-01..08、两条真实跨系统链、PostgreSQL cutover/concurrency、typed append、exact scope、retained replay 与 authority/no-resurrection matrices。
- 本次 security audit 复用了上述新鲜验证结果并逐项 `rg`/源码核对；未运行新的无边界扫描，也未修改实现或测试。

## Accepted Risks Log

No accepted risks.

## Residual And Named Future Scope

下列是已明确边界的兼容/目标范围，不是未缓解的 Phase 64.2 注册威胁，因此 `threats_deferred` 仍为 0：

1. Pre-Phase-64.2 persisted legacy evidence JSON 仍可经 existing-event-only read adapter 尝试唯一解析；歧义/缺失保持 `legacy_unresolved`，不返回 verified content。
2. Pre-027 CaseMemory 不能证明完整 identity/provenance 的行保持 `LegacyUnresolvedCaseMemoryProvenanceV1`，不进入 review、retrieval、claim 或 idempotent matching。
3. 当前 evidence scope 仅实现 `scope_type="tenant_policy"`、`scope_id=str(tenant_id)`；global/merchant/custom scope 必须由 post-Phase 17 Policy Scope 的新版本契约承接，禁止静默推断。
4. Phase 68 负责通用跨子系统 lifecycle registry；Phase 70 负责 retrieval quality / PII governance。本 phase 只收敛 CaseMemory exact lifecycle 与 identity/provenance trust boundary。

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Accepted | Deferred | Run By |
|---|---:|---:|---:|---:|---:|---|
| 2026-08-06 | 8 | 8 | 0 | 0 | 0 | Codex / gsd-security-auditor |

## Sign-Off

- [x] All registered threats have disposition `mitigate`.
- [x] Each unique T64.2-01..08 mitigation is confirmed against current implementation and executable tests.
- [x] Summary threat flags from Plans 09–11 are incorporated; no unregistered flag exists.
- [x] No accepted risk requires an Accepted Risks Log entry.
- [x] `threats_open: 0` and `threats_deferred: 0` confirmed.
- [x] `status: verified` set in frontmatter.

**Approval:** verified 2026-08-06 — 8/8 registered threats closed; no open, accepted, deferred, or unregistered threat.
