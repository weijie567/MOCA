---
phase: 64.2-evidence-identity-immutable-replay-and-memory-provenance
plan: "04"
name: Approval Snapshot Canonical Evidence Validation
type: execute
wave: 3
depends_on: ["64.2-02"]
files_modified:
  - src/approvals/service.py
  - tests/approvals/test_phase64_2_evidence_validation.py
autonomous: true
requirements: [SC-64.2-2]
must_haves:
  truths:
    - "Approval create, replacement revision, and attach-info snapshot paths validate evidence against Plan 02's tenant-scoped immutable repository before hashing or persistence."
    - "Repository-recomputed canonical evidence_refs are the sole source persisted as snapshot evidence and ApprovalRequest.verified_evidence_refs; caller-supplied verified_evidence_refs are only an exact-equality assertion and never an independent persistence source."
    - "An existing snapshot is reusable only when its stored evidence projection exactly equals the repository-produced canonical refs for the create command; a valid snapshot ref/hash/action binding alone is insufficient."
    - 'Approval validation fixes expected evidence scope to exact strings scope_type="tenant_policy" and scope_id=str(tenant_id), and rejects same-tenant request-scope substitution without inventing merchant policy scope.'
    - "Pydantic shape validation and canonical projection alone never establish evidence trust; forged, mismatched, mixed, legacy-ambiguous, or cross-tenant evidence in either input fails atomically with generic external semantics."
  artifacts:
    - {path: src/approvals/service.py, provides: "Approval-owned canonical evidence validation gate", contains: "_validate_canonical_snapshot_evidence"}
    - {path: tests/approvals/test_phase64_2_evidence_validation.py, provides: "Create/revision/attach-info evidence and verified-evidence trust-boundary matrix", contains: "test_create_rejects_existing_snapshot_with_divergent_evidence_without_new_rows"}
  key_links:
    - {from: src/approvals/service.py, to: src/repositories/evidence_version_repo.py, via: "tenant-scoped exact immutable recomputation before snapshot hash/persist", pattern: "resolve_exact"}
    - {from: src/approvals/service.py, to: src/approvals/snapshot_service.py, via: "one repository-produced canonical list feeds proposed-action projection, snapshot evidence, and persisted verified_evidence_refs", pattern: "persist_action_safety_snapshot"}
---

<objective>Close the approval snapshot trust boundary after evidence cutover. Every initial or replacement approval snapshot must validate canonical immutable evidence at the repository boundary before canonical projection, hashing, revision creation, or persistence.</objective>
<execution_context>@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md @/Users/ming/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md @docs/contract-spec.md @.planning/ROADMAP.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-CONTEXT.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-RESEARCH.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-VALIDATION.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-02-SUMMARY.md @src/approvals/service.py @src/approvals/snapshot_service.py @src/approvals/snapshots.py @src/knowledge/service.py @src/repositories/evidence_version_repo.py</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Validate canonical repository evidence before initial approval snapshot creation</name>
  <read_first>@src/approvals/service.py @src/approvals/snapshot_service.py @src/approvals/snapshots.py @src/knowledge/schemas.py @src/knowledge/service.py @src/repositories/evidence_version_repo.py @tests/approvals/test_service_transitions.py @tests/approvals/test_snapshots.py</read_first>
  <files>src/approvals/service.py, tests/approvals/test_phase64_2_evidence_validation.py</files>
  <action>Add one approval-service helper `_validate_canonical_snapshot_evidence(tenant_id, evidence_refs)` that parses only as input, then calls Plan 02's exact immutable resolver with expected `scope_type="tenant_policy"` and `scope_id=str(tenant_id)` for every ref, recomputes scope/canonical ID/document version/chunk version/text hash, and returns owner-produced `EvidenceRefV1[]` in canonical snapshot order. Initial create must call it before either creating or reusing a snapshot; request merchant identity is not evidence scope and cannot be substituted into the ref. Set a canonical proposed-action copy's `evidence_refs`, the snapshot `evidence_refs`, and the request repository's `verified_evidence_refs` from that returned list only; remove `command.verified_evidence_refs or command.evidence_refs` as a persistence choice. Preserve the live production contract by treating a non-empty `command.verified_evidence_refs` only as an exact set-equality assertion after canonical projection: it must contain every and only the canonical refs from `command.evidence_refs` with identical tenant, exact serialized scope strings, immutable document/chunk IDs and versions, evidence ID, text hash, retrieval config, rank, and retrieval timestamp. Empty `verified_evidence_refs` means derive it from the canonical list. When an existing safety snapshot is supplied, also require its stored evidence projection to equal that same list. Reject the whole request before any snapshot/request/level/assignment/event row is flushed when either list is forged, missing/extra/mismatched, mixed valid+invalid, compatibility-only/unresolved, cross-tenant, or same-tenant cross-scope. Map all failures to the existing generic approval validation error while retaining stable internal reason codes. Test valid empty and exact-equal verified inputs plus forged ID/hash/version/scope, request-scope substitution, missing/extra, mixed, cross-tenant, and same-tenant cross-scope `verified_evidence_refs`; compare serialized and stored `scope_type="tenant_policy"` and `scope_id=str(tenant_id)` exactly, and assert zero rows in every approval-owned table after each negative that begins from an empty database. Add `test_create_rejects_existing_snapshot_with_divergent_evidence_without_new_rows`: precreate a snapshot whose ref, immutable hash, action hash, tenant/run/scope, and business-fact binding are all valid and internally consistent, but whose stored evidence is a different repository-valid immutable ref from the create command's repository-produced canonical refs. Record snapshot/request/level/assignment/event row counts, with the snapshot baseline non-zero; call create using that existing snapshot ref/hash plus canonical `evidence_refs` and exactly equal `verified_evidence_refs`; require the same generic atomic rejection and assert every post-call count equals its pre-call baseline rather than zero.</action>
  <verify><automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/approvals/test_phase64_2_evidence_validation.py tests/approvals/test_service_transitions.py tests/approvals/test_snapshots.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/approvals/service.py tests/approvals/test_phase64_2_evidence_validation.py</automated></verify>
  <acceptance_criteria>Initial create has no `command.verified_evidence_refs or command.evidence_refs` persistence fallback: proposed action, new/reused snapshot evidence, and persisted `verified_evidence_refs` equal the repository-produced canonical projection with exact strings `scope_type="tenant_policy"` and `scope_id=str(tenant_id)`. Empty or exactly equal verified input succeeds; forged, mismatched, missing/extra, mixed, legacy-ambiguous, cross-tenant, request-scope, and same-tenant cross-scope input returns the same external error and leaves snapshot/request/level/assignment/event row counts unchanged. `test_create_rejects_existing_snapshot_with_divergent_evidence_without_new_rows` starts with one valid pre-existing snapshot whose stored evidence differs from the command's canonical refs, receives the generic error, and proves every approval-owned table count is unchanged from the captured non-zero baseline.</acceptance_criteria>
  <done>Approval creation can no longer mint a trusted snapshot from caller-shaped evidence alone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route revision replacement and attach-info evidence through the same gate</name>
  <read_first>@src/approvals/service.py @src/approvals/snapshot_service.py @src/repositories/evidence_version_repo.py @tests/approvals/test_needs_info_resume.py @tests/approvals/test_service_transitions.py @tests/approvals/test_phase64_2_evidence_validation.py</read_first>
  <files>src/approvals/service.py, tests/approvals/test_phase64_2_evidence_validation.py</files>
  <action>Replace `_replacement_action_and_evidence` behavior that trusts `EvidenceRefV1.model_validate` plus local projection. Every edit/revision replacement and `attach_info` path that creates or rebinds snapshot evidence must call `_validate_canonical_snapshot_evidence` under the trusted request tenant before calculating the new snapshot hash or superseding the active revision. Return and propagate one repository-produced canonical list for replacement proposed-action evidence, snapshot evidence, and the verified-evidence binding consumed by the subsequent re-risk/revision path. If `verified_evidence_refs` appears independently in `edited_action`, `proposed_action`, or `info_payload`, apply the same exact-equality assertion as Task 1 and never persist or forward it as a separate source. A failed validation must leave the old active revision executable state unchanged, create no replacement revision/snapshot/event, and reveal no cross-tenant existence. Add direct revision and attach-info matrices for forged, mismatched, missing/extra, mixed, and cross-tenant verified refs as well as forged evidence ID, changed text hash, wrong document/chunk immutable version, and ambiguous legacy alias; include a valid changed-evidence case proving all replacement bindings use the repository-produced ref/hash.</action>
  <verify><automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/approvals/test_phase64_2_evidence_validation.py tests/approvals/test_needs_info_resume.py tests/approvals/test_service_transitions.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/approvals/service.py tests/approvals/test_phase64_2_evidence_validation.py</automated></verify>
  <acceptance_criteria>Create, direct revision, and attach-info all invoke one repository-backed gate and cannot forward an independent verified-evidence list; every forged/mismatched/missing/extra/mixed/legacy/cross-tenant case is atomic and generic, while a valid revision's proposed action, snapshot, verified-evidence binding, and hash derive from the same recomputed canonical refs.</acceptance_criteria>
  <done>All approval snapshot creation and replacement surfaces share the canonical repository trust boundary.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Boundary | Disposition | Mitigation |
|---|---|---|---|---|
| T64.2-01 | Spoofing | caller/API evidence -> approval snapshot | mitigate | Tenant-scoped immutable row recomputation before hash/persist. |
| T64.2-03 | Elevation of Privilege | legacy/ambiguous ref -> approval authority | mitigate | Canonical-only validation; compatibility-only/unresolved refs rejected. |
| T64.2-08 | Information Disclosure | cross-tenant evidence -> approval API | mitigate | Trusted request tenant and one generic external validation result. |
</threat_model>
<verification>Run both task commands. Tests must assert unchanged snapshot/request/level/assignment/event row counts after every evidence or verified-evidence negative; schema validation, local projection, and `verified_evidence_refs or evidence_refs` fallback are insufficient.</verification>
<success_criteria>SC-64.2-2 includes approval snapshots: create, revision, and attach-info use one repository-produced canonical list for snapshot evidence and verified-evidence persistence, with exact-equality assertions on any independently supplied verified refs.</success_criteria>
<output>After completion, create `64.2-04-SUMMARY.md`.</output>
