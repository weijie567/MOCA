---
phase: 64.2-evidence-identity-immutable-replay-and-memory-provenance
plan: "07"
name: Reviewed Case-Memory Canonical Provenance
type: execute
wave: 4
depends_on: ["64.2-03", "64.2-05"]
files_modified:
  - src/memory/schemas.py
  - src/memory/case_precedent.py
  - src/memory/case_memory.py
  - src/db/models.py
  - src/db/migrations/versions/027_phase64_2_memory_provenance.py
  - src/api/schemas/memory.py
  - src/api/routers/memory.py
  - tests/memory/test_case_memory_provenance.py
autonomous: true
requirements: [SC-64.2-1, SC-64.2-4]
must_haves:
  truths:
    - "Reviewed case-memory preserves the Plan 05 authoritative CWC source contract and the Plan 03 canonical candidate identity without placeholders or reduced policy refs."
    - "Original authority is stored per source, separately from memory_authority_class=contextual_only; review changes neither."
    - "Rejected or observed CWC material remains only in CWC observation/audit storage and never enters CaseMemory content, caveats, provenance, source authorities, or refs."
    - "Legacy provenance uncertainty uses a separate typed unresolved envelope and is excluded from promotion, review, retrieval, and idempotent matching."
    - "Survivor-to-many deduplication lineage has a normalized deterministic association rather than lossy scalar two-way pointers."
  artifacts:
    - {path: src/memory/schemas.py, provides: "Resolved and explicitly unresolved reviewed-memory provenance envelopes", contains: "LegacyUnresolvedCaseMemoryProvenanceV1"}
    - {path: src/db/migrations/versions/027_phase64_2_memory_provenance.py, provides: "Additive provenance/identity/lifecycle foundation", contains: "provenance_json"}
    - {path: tests/memory/test_case_memory_provenance.py, provides: "Projection/persistence/review/API provenance matrix", contains: "contextual_only"}
  key_links:
    - {from: src/memory/case_precedent.py, to: src/memory/schemas.py, via: "typed provenance built from full promoted CWC facts", pattern: "CaseMemoryProvenanceV1"}
    - {from: src/memory/case_memory.py, to: src/db/models.py, via: "validated envelope persisted atomically", pattern: "provenance_json"}
---

<objective>Project, persist, review, and expose complete canonical provenance for reviewed case-memory after the authoritative CWC and shared identity contracts exist. Original per-source authority remains distinct from contextual memory authority. Lifecycle uniqueness/CAS is reserved for Plan 08.</objective>
<execution_context>@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md @/Users/ming/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md @docs/contract-spec.md @.planning/ROADMAP.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-CONTEXT.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-RESEARCH.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-VALIDATION.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-03-SUMMARY.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-05-SUMMARY.md @src/memory/case_precedent.py @src/memory/case_memory.py @src/db/models.py @src/api/routers/memory.py</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Project and persist complete canonical reviewed-memory provenance</name>
  <read_first>@src/memory/case_working_context_schemas.py @src/memory/schemas.py @src/memory/case_precedent.py @src/memory/case_memory.py @src/db/models.py @src/db/migrations/versions/013_long_term_case_memory.py @tests/memory/test_case_precedent_generation.py</read_first>
  <files>src/memory/schemas.py, src/memory/case_precedent.py, src/memory/case_memory.py, src/db/models.py, src/db/migrations/versions/027_phase64_2_memory_provenance.py, tests/memory/test_case_memory_provenance.py</files>
  <action>Add strict `CaseMemorySourceAuthorityV1` entries containing source kind, complete source ref, original `source_status`, and original `source_authority_class=business_fact|policy_evidence`; Plan 05 contextual_only/unknown observations are not valid source-authority entries. Add `CaseMemoryProvenanceV1` with trusted tenant, stable scope type/id, `resolution_status=canonical|legacy_resolved`, `memory_authority_class=contextual_only`, ordered `source_authorities[]`, source run/event/CWC revision, full tenant_policy-scoped canonical `EvidenceRefV1[]`, typed business fact refs, identity profile plus candidate/content/source hashes, nullable review decision/reviewer/time/reason, and correction/supersession lineage. Add a separate discriminated `LegacyUnresolvedCaseMemoryProvenanceV1` with `schema_version=case_memory_provenance_legacy_unresolved.v1`, `resolution_status=legacy_unresolved`, trusted tenant and row ID, only literally preserved legacy hashes/refs, and stable unresolved reasons; it must not accept fabricated scope, authority, reviewer, canonical evidence, or identity fields. Closed-case projection may consume only Plan 05 `promote` facts and must copy each fact's original business_fact/policy_evidence authority/status into its matching source entry. Rejected or observed CWC entries stay solely in CWC observation/audit records: they must never be copied into CaseMemory summary/excerpt/applicability/outcome/caveats, provenance, source_authorities, business refs, or evidence refs. Delete reduced `_project_policy_refs` and placeholder/default scope/status. Add CaseMemory columns `identity_algorithm_version`, `candidate_hash`, `identity_resolution_status`, typed `provenance_json`, positive `lifecycle_version`, and nullable direct correction/supersession IDs. Add normalized `CaseMemoryLineageLink`/`case_memory_lineage_links` with tenant, survivor ID, related ID, relation, and deterministic ordinal, unique pair/relation constraints, distinct-node check, and restrictive FKs for survivor-to-many migration lineage. Migration 027 marks a row `legacy_resolved` only when stored values prove complete identity/provenance; all others receive the separate unresolved envelope. Add explicit `test_rejected_cwc_observation_never_enters_case_memory` covering every CaseMemory content/provenance/ref field and schema tests proving resolved and unresolved envelopes cannot impersonate each other.</action>
  <verify><automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_case_memory_provenance.py tests/memory/test_case_precedent_generation.py tests/memory/test_case_memory_retrieval.py tests/agent/test_memory_evidence_boundary.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/memory/schemas.py src/memory/case_precedent.py src/memory/case_memory.py src/db/models.py src/db/migrations/versions/027_phase64_2_memory_provenance.py tests/memory/test_case_memory_provenance.py</automated></verify>
  <acceptance_criteria>New candidates persist exact tenant/scope/source/run/CWC/full evidence/identity fields, business_fact/policy_evidence source authority, and separate contextual-only memory authority; `test_rejected_cwc_observation_never_enters_case_memory` proves rejected observations appear in no CaseMemory field; unresolved rows use only `LegacyUnresolvedCaseMemoryProvenanceV1`; normalized lineage supports one survivor with many related rows.</acceptance_criteria>
  <done>Reviewed case-memory has canonical provenance founded on Plans 03 and 05 without conflating source and memory authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve reviewer provenance and expose bounded no-leak API projections</name>
  <read_first>@src/memory/schemas.py @src/memory/case_memory.py @src/api/schemas/memory.py @src/api/routers/memory.py @tests/test_memory_review_api.py @tests/memory/test_case_memory_provenance.py</read_first>
  <files>src/memory/case_memory.py, src/api/schemas/memory.py, src/api/routers/memory.py, tests/memory/test_case_memory_provenance.py</files>
  <action>Validate tenant/scope/source/hash binding and require resolved `CaseMemoryProvenanceV1` before insert or review. A central resolution validator must exclude `LegacyUnresolvedCaseMemoryProvenanceV1` from candidate promotion, pending-review lists, approve/reject/correct actions, published retrieval, provenance expansion, and idempotent winner matching; bounded detail may expose only `resolution_status=legacy_unresolved` and safe reasons. On approve/reject, add the exact decision, reviewer user ID, reviewed time, reason, and lifecycle version to resolved provenance without replacing any `source_authorities[]` entry or `memory_authority_class`. Pending/detail/action API schemas must return canonical scope, bounded per-source status/authority/ref metadata, `memory_authority_class=contextual_only`, identity version/resolution, candidate hash, lifecycle version, review decision, and direct plus multi-valued lineage; exclude raw content bodies, CWC rejected observations, embeddings, and internal mismatch reasons. Review changes visibility only: original source authority and contextual-only memory authority remain byte-identical. Cross-tenant, forged, and unresolved action requests return generic 404. Leave CAS/expiry/concurrency mechanics for Plan 08 but keep request/response fields ready for `expected_lifecycle_version`.</action>
  <verify><automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_case_memory_provenance.py tests/test_memory_review_api.py tests/memory/test_case_precedent_generation.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/memory/case_memory.py src/api/schemas/memory.py src/api/routers/memory.py tests/memory/test_case_memory_provenance.py</automated></verify>
  <acceptance_criteria>Review metadata is additive; every original source status/authority remains unchanged; memory authority stays contextual-only; rejected CWC observations remain absent; unresolved envelopes cannot be listed/reviewed/retrieved/matched; API refs and multi-lineage are complete but bounded; cross-tenant/forged/unresolved actions expose no existence detail.</acceptance_criteria>
  <done>Projection, persistence, review, and API surfaces preserve one complete reviewed-memory provenance envelope.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Boundary | Disposition | Mitigation |
|---|---|---|---|---|
| T64.2-07 | Elevation of Privilege | CWC/review -> memory authority | mitigate | Preserve original per-source authority separately; review cannot change it or contextual-only memory authority. |
| T64.2-04 | Tampering | identity migration -> legacy provenance | mitigate | Stored profile and explicit unresolved legacy state; no fabricated backfill. |
| T64.2-08 | Information Disclosure | review API -> tenant/source data | mitigate | Tenant predicates, bounded refs, generic external not-found. |
</threat_model>
<verification>Run both task commands. Verify full refs/source authority derive only from Plan 05 promoted facts and identity values derive from Plan 03 output; rejected observations and unresolved envelopes must have explicit zero-entry assertions across every authoritative CaseMemory path.</verification>
<success_criteria>SC-64.2-4 provenance requirements hold from CWC projection through stored reviewed memory and API response.</success_criteria>
<output>After completion, create `64.2-07-SUMMARY.md`.</output>
