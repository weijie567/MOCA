---
phase: 64.2-evidence-identity-immutable-replay-and-memory-provenance
plan: "10"
subsystem: testing
tags: [rag, evidence-identity, canonical-fixtures, fail-closed-validation]

requires:
  - phase: 64.2-09
    provides: "Exact current-evidence validation and the post-review UAT regression diagnosis"
provides:
  - "Owner-minted canonical RAG node fixture with deterministic immutable document and chunk version identities"
  - "Full canonical current-row fake seam preserving invalid_hash precedence and bounded wrong-scope diagnostics"
affects: [64.2-11, rag-context, evidence-validation]

tech-stack:
  added: []
  patterns: [owner-minted canonical test refs, complete immutable fake rows, explicit exact-current seam assertions]

key-files:
  created:
    - .planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-10-SUMMARY.md
  modified:
    - tests/agent/test_nodes/test_rag_context_build.py

key-decisions:
  - "Use deterministic UUIDv5 document/chunk version identifiers derived from tenant and fixture keys, then mint through the canonical identity owner."
  - "Keep wrong-scope output bounded as evidence_unavailable while preserving invalid_hash precedence and exact current/canonical retriever call evidence."
  - "Repair only the test fixture; no production fallback or compatibility alias is introduced."

patterns-established:
  - "RAG boundary fixtures spread ref.to_canonical_identity() into fake rows and implement both canonical and current-canonical tenant-filtered APIs."

requirements-completed: [SC-64.2-1, SC-64.2-2]

duration: 3min
completed: 2026-08-06
---

# Phase 64.2 Plan 10: Canonical RAG Combined-Status Fixture Repair Summary

**The combined RAG fail-closed regression now exercises owner-minted immutable evidence and the exact current-row seam, restoring `invalid_hash` precedence without weakening production validation.**

## Performance

- **Duration:** 3 min
- **Started:** 2026-08-06T04:14:40Z
- **Completed:** 2026-08-06T04:17:11Z
- **Tasks:** 1
- **Files modified:** 1 test module, plus this summary

## Accomplishments

- Replaced reduced `EvidenceRefV1.build` fixtures with complete persisted material, deterministic immutable UUIDv5 identifiers, canonical owner minting, and `EvidenceRefV1.from_canonical_identity` projection.
- Made fake rows carry every canonical identity field and made both canonical retrieval APIs tenant-filtered, with exact current/canonical call order asserted by the combined test.
- Restored the intended combined result: `invalid_hash`, plus bounded `evidence_unavailable`, `latest_version_invalid`, `text_hash_mismatch`, and `candidate_ref_invalid` diagnostics; rejected identities remain absent from ordinary projections.

## Task Commits

1. **Task 1: Migrate the combined-status node fixture to exact canonical identity** — `4830c9f` (test)

## Files Created/Modified

- `tests/agent/test_nodes/test_rag_context_build.py` — Canonical identity fixture, full immutable rows, exact current-row fake API, and bounded combined-status assertions.
- `.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-10-SUMMARY.md` — Plan execution and validation evidence.

## Decisions Made

- Derived immutable test UUIDs from tenant/doc/chunk/version material through UUIDv5 so the fixture remains deterministic without using caller-selected authority IDs.
- Passed the matching canonical content for wrong-scope and stale candidates; only the intended current-version and content-hash faults are overridden.
- Asserted both exact-current lookup and subsequent canonical detail lookup so future fallback drift cannot silently satisfy the regression.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. The expected pre-change RED reproduced as `no_evidence`; it was already diagnosed and recorded by the Phase 64.2 UAT/debug workflow.

## TDD Gate Compliance

- **RED:** The existing committed regression was reproduced before editing: `1 failed` with actual `no_evidence` versus expected `invalid_hash`.
- **GREEN:** The fixture-only task commit `4830c9f` made the exact node pass, followed by the complete plan validation (`9 passed`).
- A separate new RED commit was not created because Plan 10 repairs the already-committed failing fixture and the task changes were intentionally kept atomic.

## Verification

- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_rag_context_build.py tests/knowledge/test_verified_evidence_package.py -q --tb=short` → `9 passed, 1 warning in 0.04s`.
- `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check tests/agent/test_nodes/test_rag_context_build.py` → `All checks passed!`.
- `git diff --check` passed.
- Task commit changes only `tests/agent/test_nodes/test_rag_context_build.py`; no production file or compatibility fallback changed.

## Known Stubs

None. Empty mappings in the module are explicit fail-closed assertions, not unwired application data.

## Threat Flags

None. The test-only change verifies T64.2-01 and T64.2-08 at the existing trust boundary and introduces no endpoint, schema, authentication, network, or file-access surface.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- UAT gap 1 is closed and Plan 11 can repair the independently owned approval fixture, then run the single serial repository-wide gate.
- Global `STATE.md`, `ROADMAP.md`, `REQUIREMENTS.md`, and autopilot checkpoint updates remain owned by the parent phase orchestrator and were intentionally not modified here.

## Self-Check: PASSED

- `tests/agent/test_nodes/test_rag_context_build.py` exists and is present in task commit `4830c9f`.
- The canonical exact-current regression and verified-package module pass together; scoped Ruff and diff checks are green.
- No production file was modified, and unrelated checkpoint/review iteration artifacts remain untouched and unstaged.

---
*Phase: 64.2-evidence-identity-immutable-replay-and-memory-provenance*
*Completed: 2026-08-06*
