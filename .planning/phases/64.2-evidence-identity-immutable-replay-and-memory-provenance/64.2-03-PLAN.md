---
phase: 64.2-evidence-identity-immutable-replay-and-memory-provenance
plan: "03"
name: Shared Memory Candidate Identity Owner
type: execute
wave: 1
depends_on: []
files_modified:
  - src/memory/identity.py
  - src/memory/service.py
  - src/agent/nodes/memory_write.py
  - src/memory/long_term.py
  - src/memory/case_memory.py
  - src/memory/case_working_context_service.py
  - tests/memory/test_memory_identity.py
  - tests/agent/test_memory_write_node.py
autonomous: true
requirements: [SC-64.2-4]
must_haves:
  truths:
    - "src/memory/identity.py solely owns normalization, content hash, source normalization/hash, and complete candidate hash construction."
    - "Session node/service/event, long-term, case-memory, and CWC writers consume owner-produced typed identity results without local serializers."
    - "Legacy NFKC/casefold hashes remain versioned legacy values; new normalization never reinterprets an old hash in place."
  artifacts:
    - {path: src/memory/identity.py, provides: "Typed profile-aware identity owner and all memory-type builders", contains: "MemoryCandidateIdentityV1"}
    - {path: tests/memory/test_memory_identity.py, provides: "Cross-builder identity goldens", contains: "test_all_memory_builders_use_the_shared_owner"}
  key_links:
    - {from: src/agent/nodes/memory_write.py, to: src/memory/service.py, via: "node consumes the durable service identity", pattern: "candidate_hash"}
    - {from: src/memory/long_term.py, to: src/memory/identity.py, via: "typed owner wrapper", pattern: "build_long_term_memory_candidate_identity"}
    - {from: src/memory/case_working_context_service.py, to: src/memory/identity.py, via: "typed owner wrapper", pattern: "build_case_working_context_candidate_identity"}
---

<objective>Make one shared, version-aware owner produce complete memory candidate identity for every current builder. This plan does not add reviewed provenance persistence or lifecycle constraints; Plans 07 and 08 consume its output.</objective>
<execution_context>@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md @/Users/ming/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md @docs/contract-spec.md @.planning/ROADMAP.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-CONTEXT.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-RESEARCH.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-VALIDATION.md @src/common/canonical_hash.py @src/memory/identity.py @src/memory/service.py @src/agent/nodes/memory_write.py @src/memory/long_term.py @src/memory/case_memory.py @src/memory/case_working_context_service.py</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Publish one typed profile-aware candidate identity owner</name>
  <read_first>@docs/contract-spec.md @src/common/canonical_hash.py @src/memory/identity.py @tests/memory/test_memory_identity.py</read_first>
  <files>src/memory/identity.py, tests/memory/test_memory_identity.py</files>
  <action>Add frozen `MemoryCandidateIdentityV1` containing `identity_profile`, normalized source ref, `content_hash`, `source_identity_hash`, and `candidate_hash`. Keep `nfkc_casefold_legacy` as read-only verification for stored legacy values. New writes use `nfc_selective_v2`: Unicode NFC, trim, collapse whitespace, lowercase only registered enum-like fields, and preserve user/business proper-noun case. Include profile/version in the hash namespace. Publish typed wrappers `build_session_memory_candidate_identity`, `build_long_term_memory_candidate_identity`, `build_case_memory_candidate_identity`, and `build_case_working_context_candidate_identity`; all normalization/source material and complete content envelopes are defined in this module. Reject unknown content fields and incomplete source discriminators. Golden tests prove profile separation, proper-noun preservation, deterministic full-envelope hashes, and no legacy reinterpretation.</action>
  <verify><automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_memory_identity.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/memory/identity.py tests/memory/test_memory_identity.py</automated></verify>
  <acceptance_criteria>All four typed builders return `MemoryCandidateIdentityV1`; legacy and v2 goldens differ without rewriting old hashes; proper nouns preserve case; unknown/incomplete inputs fail closed.</acceptance_criteria>
  <done>The sole owner defines every identity input, profile, normalization rule, and output hash.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove session node/service identity drift</name>
  <read_first>@src/memory/identity.py @src/memory/service.py @src/agent/nodes/memory_write.py @tests/memory/test_memory_identity.py @tests/agent/test_memory_write_node.py @tests/memory/test_session_memory_service.py</read_first>
  <files>src/memory/service.py, src/agent/nodes/memory_write.py, tests/memory/test_memory_identity.py, tests/agent/test_memory_write_node.py</files>
  <action>Move the complete session write envelope—tenant/user/thread/run, versioned slots and refs, expected version, PII classification, decision and reason—into `build_session_memory_candidate_identity`. Delete `_session_write_identity` from the service and `_session_candidate_identity` from the node. The service must compute once, persist the same `candidate_hash` on `MemoryWriteEvent`, and return the typed identity on every written/skipped/conflict result. The node must project that durable result and must not recompute a reduced envelope. Assert byte-identical candidate/content/source hashes across node result, service result, write event, retry, and conflict paths.</action>
  <verify><automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_memory_identity.py tests/agent/test_memory_write_node.py tests/memory/test_session_memory_service.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/memory/service.py src/agent/nodes/memory_write.py tests/memory/test_memory_identity.py tests/agent/test_memory_write_node.py</automated></verify>
  <acceptance_criteria>No session-local serializer remains; one owner-produced identity is identical across node/service/event for success, skip, retry, and conflict outcomes.</acceptance_criteria>
  <done>Session identity is computed once and consumed consistently by every surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate long-term, case-memory, and CWC builders to the shared owner</name>
  <read_first>@src/memory/identity.py @src/memory/long_term.py @src/memory/case_memory.py @src/memory/case_working_context_service.py @tests/memory/test_memory_identity.py @tests/memory/test_memory_write_service.py @tests/memory/test_case_memory_retrieval.py @tests/memory/test_case_working_context_service.py</read_first>
  <files>src/memory/long_term.py, src/memory/case_memory.py, src/memory/case_working_context_service.py, tests/memory/test_memory_identity.py</files>
  <action>Replace `_candidate_identity`, `_candidate_hash_for_memory`, `_candidate_hash`, and equivalent local content/source serializers in long-term, case-memory, and CWC paths with their typed owner wrappers. Store/dedupe/event/review callers must consume the returned `candidate_hash`, `content_hash`, `source_identity_hash`, normalized source ref, and identity profile rather than rebuilding any component. Existing stored legacy values validate only under their stored legacy profile. Add cross-builder tests that monkeypatch the owner and prove each production path calls its named wrapper exactly once per candidate and persists/returns the exact owner result.</action>
  <verify><automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_memory_identity.py tests/memory/test_memory_write_service.py tests/memory/test_case_memory_retrieval.py tests/memory/test_case_working_context_service.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/memory/long_term.py src/memory/case_memory.py src/memory/case_working_context_service.py tests/memory/test_memory_identity.py</automated></verify>
  <acceptance_criteria>All five named caller surfaces (node, service, long-term, case-memory, CWC) use only owner wrappers; store/event/dedupe values equal the returned typed identity; legacy rows are never rehashed as v2.</acceptance_criteria>
  <done>Every current memory candidate builder consumes the single owner with parity tests.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Boundary | Disposition | Mitigation |
|---|---|---|---|---|
| T64.2-04 | Tampering | normalization rollout -> existing hashes | mitigate | Versioned profiles and legacy-only verification. |
| T64.2-05 | Repudiation | caller-local serializer -> event/store identity | mitigate | One typed owner result reused by node, service, events, stores, and dedupe. |
| T64.2-07 | Elevation of Privilege | source normalization -> candidate identity | mitigate | Complete typed source discriminators and fail-closed unknown fields. |
</threat_model>
<verification>Run all three task commands. Plan 09 will add an AST ownership guard; these behavioral tests must already prove all enumerated current builders consume the shared owner.</verification>
<success_criteria>SC-64.2-4 identity parity holds across node, service, event, long-term, case-memory, and CWC surfaces.</success_criteria>
<output>After completion, create `64.2-03-SUMMARY.md`.</output>
