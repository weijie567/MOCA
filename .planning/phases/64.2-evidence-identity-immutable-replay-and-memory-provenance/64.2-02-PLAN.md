---
phase: 64.2-evidence-identity-immutable-replay-and-memory-provenance
plan: "02"
name: Dual-Write Evidence Cutover Watermarked Backfill And Canonical Retrieval
type: execute
wave: 2
depends_on: ["64.2-01"]
files_modified:
  - src/repositories/evidence_version_repo.py
  - src/repositories/policy_chunk_repo.py
  - src/rag/ingestion.py
  - src/knowledge/retrieval.py
  - src/knowledge/service.py
  - src/db/migrations/versions/026_phase64_2_evidence_cutover.py
  - tests/rag/test_ingestion_safety.py
  - tests/knowledge/test_evidence_cutover.py
autonomous: true
requirements: [SC-64.2-2, SC-64.2-3]
must_haves:
  truths:
    - "Dual-write is installed and observable before any legacy head is backfilled."
    - "Every ingestion writer and the final read activator acquire the same singleton rollout-row lock and validate the same CAS epoch under one documented lock order, closing the final backfill/cutover race."
    - "Every successful ingestion, including an unchanged document, allocates exactly one database-native sequence; unchanged ingestion reuses its immutable binding but advances the current projection to that new sequence."
    - "Canonical current retrieval reads switch atomically only after reconciliation reports zero new-write gaps; operational rollback disables reads while retaining dual-write, quarantines gaps, reconciles, and CAS re-enables."
    - 'Every canonical/current/historical resolver enforces exact strings scope_type="tenant_policy" and scope_id=str(tenant_id); legacy resolution remains an explicit compatibility API.'
  artifacts:
    - {path: src/repositories/evidence_version_repo.py, provides: "Immutable append/exact resolve and rollout-state owner", contains: "EvidenceVersionRepository"}
    - {path: src/db/migrations/versions/026_phase64_2_evidence_cutover.py, provides: "Sequence-watermarked provable backfill and reconciliation", contains: "backfill_watermark_sequence"}
    - {path: tests/knowledge/test_evidence_cutover.py, provides: "Shared-lock ingestion, unchanged-sequence, atomic cutover, and operational rollback contract", contains: "test_writer_and_cutover_share_rollout_lock_epoch"}
  key_links:
    - {from: src/rag/ingestion.py, to: src/repositories/evidence_version_repo.py, via: "same-transaction dual-write before current-head projection", pattern: "append_immutable_version"}
    - {from: src/knowledge/service.py, to: src/repositories/evidence_version_repo.py, via: "separate canonical-current and exact-historical resolution", pattern: "resolve_exact"}
---

<objective>Install immutable dual-write first, then run a watermarked/reconciled backfill, and only then cut current retrieval to canonical rows. Compatibility aliases remain a visible, fail-closed adapter; ranking and Phase 70 retrieval-quality behavior do not change.</objective>
<execution_context>@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md @/Users/ming/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md @docs/contract-spec.md @.planning/ROADMAP.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-CONTEXT.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-RESEARCH.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-VALIDATION.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-01-SUMMARY.md @src/rag/ingestion.py @src/repositories/policy_chunk_repo.py @src/knowledge/retrieval.py @src/knowledge/service.py</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Install same-transaction immutable dual-write before backfill</name>
  <read_first>@src/rag/ingestion.py @src/repositories/policy_chunk_repo.py @src/repositories/policy_document_repo.py @src/repositories/document_block_repo.py @src/db/models.py @tests/rag/test_ingestion_safety.py @tests/knowledge/test_immutable_evidence_migration.py</read_first>
  <files>src/repositories/evidence_version_repo.py, src/repositories/policy_chunk_repo.py, src/rag/ingestion.py, tests/rag/test_ingestion_safety.py, tests/knowledge/test_evidence_cutover.py</files>
  <action>Create `EvidenceVersionRepository` as the only immutable append/exact-resolution and rollout-state owner. Document and enforce one lock order for both writers and cutover: acquire the singleton `evidence_identity_rollouts` row with `SELECT ... FOR UPDATE` first; read and validate the caller's expected `rollout_version` plus permitted dual-write/canonical-read state; only then lock affected `PolicyDocument`/current-head rows in deterministic `(tenant_id, doc_key, id)` order; never acquire those locks in reverse. While holding that rollout lock, every ingestion that will commit successfully—first, changed, correction, or unchanged—must allocate exactly one value from `evidence_ingestion_write_seq`, and no successful ingestion may allocate zero or multiple values. Compute the fingerprint before mutating current heads. First/changed/corrected ingestion writes exact string scope `scope_type="tenant_policy"` and `scope_id=str(tenant_id)`, flushes one immutable document version and one immutable chunk version per produced chunk, and writes the same new sequence to the current projections before commit. Unchanged ingestion proves and reuses the existing immutable document/chunk binding, creates no duplicate immutable version, but still writes its one newly allocated sequence to every affected current projection before commit; its sequence parity is the current projection/ingestion result sequence plus unchanged immutable FK/identity/hash binding, not a new immutable row. If rollout epoch/state validation fails, retry from the rollout lock or fail before any head/immutable mutation; current and immutable changes roll back together. The dual-write health/activation path uses the same rollout lock and CAS epoch. Cover first, unchanged, changed, correction linkage, failed writes, concurrent change, monotonic sequence ordering, stale epoch/CAS, exact serialized/stored/hashed scope strings, and zero backfill while dual-write becomes active.</action>
  <verify><automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/rag/test_ingestion_safety.py tests/knowledge/test_evidence_cutover.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/repositories/evidence_version_repo.py src/repositories/policy_chunk_repo.py src/rag/ingestion.py tests/rag/test_ingestion_safety.py tests/knowledge/test_evidence_cutover.py</automated></verify>
  <acceptance_criteria>Dual-write activation precedes any backfill marker; writer and activator use rollout-first lock order and reject stale epochs; every successful ingestion allocates exactly one monotonic sequence; unchanged ingestion advances the current projection sequence while preserving the exact immutable FK/hash binding and row count; stored and hashed scope equals `scope_type="tenant_policy"`, `scope_id=str(tenant_id)`; failures leave neither head nor immutable partial state.</acceptance_criteria>
  <done>The live writer path is dual-writing transactionally and observable before legacy migration begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backfill a snapshot and reconcile every intervening write</name>
  <read_first>@src/db/migrations/versions/025_phase64_2_immutable_evidence.py @src/db/models.py @src/repositories/evidence_version_repo.py @src/rag/ingestion.py @tests/knowledge/test_immutable_evidence_migration.py @tests/knowledge/test_evidence_cutover.py</read_first>
  <files>src/db/migrations/versions/026_phase64_2_evidence_cutover.py, src/repositories/evidence_version_repo.py, tests/knowledge/test_evidence_cutover.py</files>
  <action>Migration 026 must abort unless `dual_write_enabled_at` is non-null and the dual-write health proof is current. Acquire the singleton rollout row with `SELECT ... FOR UPDATE` before any current-head lock, validate the expected `rollout_version`, and reserve `backfill_watermark_sequence` from `evidence_ingestion_write_seq` before scanning current heads. Backfill only rows whose tenant, exact string scope `scope_type="tenant_policy"` and `scope_id=str(tenant_id)`, document version/fingerprint, unique logical chunk, and recomputed text hash prove one mapping; record missing/ambiguous rows as `legacy_unresolved` and never infer merchant scope or authority. Assert unchanged ingestions both immediately before and after the watermark: each allocates exactly one distinct sequence, advances its current projection to that sequence, reuses the identical immutable FK/identity/hash without a new immutable row, and is counted reconciled by binding parity. During the PostgreSQL test, pause the scan after the sequence watermark, run changed and unchanged ingestion through the real writer from a second session, resume, then reconcile every current projection sequence beyond the watermark and every visible head missing its exact immutable fingerprint/version/hash. For final activation, retain the same rollout-row lock continuously across the last reconciliation scan, zero-gap assertion, and CAS update to `canonical_reads_enabled=true`; no unlock/relock window is permitted. Add named two-session `test_writer_and_cutover_share_rollout_lock_epoch`: when the activator pauses after its final zero-gap check, a writer must block before sequence allocation; after activation commits, the writer rereads the new epoch/state, allocates exactly one sequence, dual-writes/projects, and commits with no gap. The inverse ordering must show the activator waits for a writer already holding the rollout lock and includes that committed sequence before enabling. Quarantine unresolved mismatches, store `reconciled_through_sequence` and exact canonical/resolved/unresolved counts, and CAS the enabled timestamp/next rollout version in the zero-gap transaction. Downgrade must refuse when immutable history, dependencies, snapshots, or later canonical refs exist.</action>
  <verify><automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/knowledge/test_evidence_cutover.py tests/knowledge/test_immutable_evidence_migration.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/repositories/evidence_version_repo.py src/db/migrations/versions/026_phase64_2_evidence_cutover.py tests/knowledge/test_evidence_cutover.py</automated></verify>
  <acceptance_criteria>The named two-session boundary test proves both lock interleavings and one shared rollout epoch; before/after-watermark unchanged writes each allocate one sequence, advance current projections, preserve immutable binding/hash/row count, and reconcile exactly once; ambiguous legacy rows remain unresolved; rollout CAS rejects stale owners; zero-gap verification and canonical-read activation are one continuously locked transaction.</acceptance_criteria>
  <done>Watermarked backfill and reconciliation close the writer window without guessing legacy identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Cut current retrieval to canonical rows and retain explicit compatibility reads</name>
  <read_first>@src/knowledge/evidence_identity.py @src/knowledge/schemas.py @src/knowledge/retrieval.py @src/knowledge/service.py @src/repositories/evidence_version_repo.py @src/repositories/policy_chunk_repo.py @tests/knowledge/test_evidence_projection.py @tests/knowledge/test_provenance_lookup.py @tests/knowledge/test_tenant_scope.py</read_first>
  <files>src/repositories/policy_chunk_repo.py, src/knowledge/retrieval.py, src/knowledge/service.py, tests/knowledge/test_evidence_cutover.py</files>
  <action>Remove local `doc_key/chunk_id@version` construction from retrieval and `PolicyChunkRepository`. Current search hits must join their persisted immutable row and ask the canonical owner to mint `EvidenceRefV1` with exact string scope `scope_type="tenant_policy"` and `scope_id=str(tenant_id)`; request/resolver expected scope must match those serialized values exactly, and merchant context remains an eligibility filter rather than evidence identity. Split service behavior into `validate_current_evidence(...)`, which enforces current eligibility/freshness/accepted scope, and `resolve_immutable_evidence(...)`, which returns an exact historical row regardless of current eligibility but never ignores identity scope. Read canonical rows only while rollout `canonical_reads_enabled=true`; current retrieval fails closed if disabled, quarantined, or missing an immutable binding. Add operational `disable_canonical_reads(expected_rollout_version, reason)` that follows the rollout-first lock order, locks/CAS-updates the rollout row, retains dual-write, records quarantine/audit state, and permits no silent mutable/legacy fallback. Reconcile quarantined gaps through the same sequence owner and re-enable only through the locked zero-gap CAS transition from Task 2. Keep `resolve_legacy_alias(...)` as the only compatibility entry and upgrade only a unique tenant/scope/doc/version/chunk/hash match. Cross-tenant, same-tenant cross-scope, forged, mismatched, missing, and ambiguous inputs share generic external semantics with distinct internal audit reasons. Test disable-under-load, continued dual-write, fail-closed reads, repair to zero gaps, stale-CAS rejection, re-enable, and destructive-downgrade refusal. Preserve ranking, effective-date filtering, and approval-snapshot use of `EvidenceRefV1`.</action>
  <verify><automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/knowledge/test_evidence_cutover.py tests/knowledge/test_evidence_projection.py tests/knowledge/test_provenance_lookup.py tests/knowledge/test_tenant_scope.py tests/knowledge/test_service.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/repositories/policy_chunk_repo.py src/knowledge/retrieval.py src/knowledge/service.py tests/knowledge/test_evidence_cutover.py</automated></verify>
  <acceptance_criteria>New/current retrieval uses only repository-backed refs whose serialized scope is exactly `scope_type="tenant_policy"`, `scope_id=str(tenant_id)`; request/cross-scope negatives fail generically; superseded evidence can fail current eligibility yet resolve historically; disabling reads never disables dual-write or falls back; only zero-gap CAS re-enables; destructive downgrade remains blocked.</acceptance_criteria>
  <done>Canonical-only current reads are enabled after reconciliation and historical/legacy paths remain separate and fail closed.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Boundary | Disposition | Mitigation |
|---|---|---|---|---|
| T64.2-01 | Spoofing | retrieval input -> immutable repository | mitigate | Tenant-scoped row lookup and canonical recomputation. |
| T64.2-02 | Tampering | re-ingestion/backfill -> historical evidence | mitigate | Dual-write before backfill, append-only rows, watermark reconciliation. |
| T64.2-03 | Elevation of Privilege | ambiguous legacy alias -> verified package | mitigate | Unique/provable mapping only; unresolved remains non-authoritative. |
| T64.2-08 | Information Disclosure | cross-scope lookup -> API/service | mitigate | Generic external failure and internal-only reasons. |
</threat_model>
<verification>Execute tasks in order. The named two-session writer/cutover test must prove the shared rollout lock, common CAS epoch, rollout-first lock order, both boundary interleavings, and before/after-watermark unchanged-ingestion sequence/binding parity. The disable-dual-write-stays-on/reconcile/re-enable rollback test is also blocking; timestamp-only or sequential substitutes are insufficient.</verification>
<success_criteria>Every successful exact-scope write is immutable or reuses a proven immutable binding before legacy migration, allocates exactly one monotonic sequence, and participates in the same rollout lock/CAS epoch as cutover; canonical reads activate or reactivate only from a continuously locked zero-gap rollout state while operational rollback remains fail closed.</success_criteria>
<output>After completion, create `64.2-02-SUMMARY.md`.</output>
