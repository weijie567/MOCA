# Phase 64.2 External Plan Reviews

## Review environment

- Reviewer: Claude Code 2.1.222, authenticated with OAuth.
- Mode: independent read-only review, split into three bounded scopes after the all-in-one prompt exceeded the CLI prompt limit and the unrestricted repository-read attempt did not return within ten minutes.
- Safety: `--safe-mode --tools Read --permission-mode dontAsk`; no repository writes or shell tools were available to the reviewer.
- Baseline: commit `f7949fa` after rebasing the phase branch onto `origin/main` (`bc55922`).

## Scope A — evidence identity, migration, approval, replay

**Verdict: NEEDS_REPAIR**

1. **BLOCKER — Scope binding is not part of the canonical identity or all trust-boundary checks.** `EvidenceRefV1` names trusted tenant identity but no stable merchant/scope binding; same-tenant cross-scope evidence could be accepted. Repair: include accepted scope/merchant in identity/hash/constraints/resolution or prove document identity authoritatively encodes scope, and add cross-scope negatives.
2. **BLOCKER — Production event binding has an unresolved type/granularity mismatch.** Plan 06 passes raw `ToolResultV2.policy_evidence_refs` as `evidence_snapshot_refs` while `ReplayEvidenceSnapshotV1` is a full enriched snapshot. Repair: define a canonical-ref input followed by one append-time snapshot constructor, or emit full snapshots; test all persisted snapshot fields.
3. **WARNING — Final backfill/cutover is not explicitly serialized with concurrent writes.** A writer can race the final zero-gap check and activation. Repair: database-native monotonic watermark plus rollout-row lock/CAS and an ingestion race test.
4. **WARNING — Tombstone replayability and retention are asserted but not specified.** Repair: preserve immutable material/locator for the audit-retention window, prevent destructive purge while snapshots depend on it, and test tombstone/expiry replay.

## Scope B — CWC and memory provenance/lifecycle

**Verdict: NEEDS_REPAIR**

5. **BLOCKER — Rejected observations can enter reviewed case memory.** Plan 07 says rejected observations become bounded caveats, conflicting with D-02. Repair: retain them only in CWC observation/audit data and add a negative test.
6. **BLOCKER — `contextual_only` and `unknown` authority are not explicitly non-promotable.** Repair: only `business_fact` and `policy_evidence` may produce `promote`; add stable observe/reject reasons and negative tests.
7. **WARNING — Terminal identity resurrection is not durably specified.** The active-only unique index excludes terminal rows. Repair: retain a durable terminal identity claim/tombstone key or dedicated claim table under the same transaction; test all terminal states.
8. **WARNING — Legacy-unresolved rows conflict with a strict provenance envelope.** Repair: use a separate unresolved envelope or nullable fields plus a resolution-state validator; exclude unresolved rows from authoritative paths.
9. **WARNING — Duplicate backfill two-way lineage has unclear survivor-to-many cardinality.** Repair: define an association table or multi-valued deterministic lineage and test groups larger than two.

## Scope C — cross-plan closure

**Verdict: NEEDS_REPAIR**

10. **BLOCKER — Migration 026 cannot be exercised by the stated clean-upgrade gate.** Plan 02 requires migration 026 to abort until dual-write is activated, while Plan 09 requests an unstaged 024→028 upgrade. Repair: automate the staged 025/deploy+activate/026+reconcile/027/028 sequence.
11. **BLOCKER — D-18 dependency-graph and overlap validation is asserted but not implemented by Plan 09.** Repair: parse Plans 01–09 and assert existence, dependency validity/order, and safe shared-file overlap.
12. **WARNING — Rollback covers schema downgrade but not operational cutover rollback.** Repair: test disabling canonical reads while keeping dual-write active, reconciling/quarantining, and re-enabling only at zero gaps.

## Codex adjudication

Completed. See `64.2-PLAN-REVIEW-DECISIONS.md` for evidence-backed dispositions and repair status.

## Repair re-review — loop 2

**Verdict: NEEDS_REPAIR**

1. **BLOCKER — Final cutover locking did not explicitly serialize writers.** The rollout row was locked by activation, but ingestion transactions were not required to join the same barrier. Repair: one rollout-first lock order and shared CAS epoch for writers and activator, plus both two-session boundary interleavings.
2. **WARNING — Unchanged-ingestion sequence semantics were underspecified.** Repair: every successful ingestion allocates exactly one sequence; unchanged ingestion reuses the immutable binding and advances the current projection sequence, with before/after-watermark parity tests.
3. **WARNING — Legacy replay input was not fully separated from append-owned snapshot construction.** Repair: new append accepts only typed `canonical_evidence_refs`; `evidence_refs_json` is read/migration-only for persisted legacy rows, and raw/reduced append attempts create no event/snapshot/dependency rows.
4. **WARNING — Scope ID representation was inconsistent across plans.** Repair: every policy-evidence boundary uses exact runtime strings `scope_type="tenant_policy"` and `scope_id=str(tenant_id)` and compares serialized/stored/hashed values exactly.

Claude separately confirmed that CLAUDE-01..12 and GSD-15/16 were otherwise closed; the nine-plan DAG, 22-row validation parity, D-01..D-20, SC-64.2-1..5, and uv-only gates were intact.

## Repair re-review — loop 3

**Verdict: PASS**

- CLAUDE-R2-01 through CLAUDE-R2-04 are fully closed.
- Shared writer/activator rollout locking, unchanged-ingestion sequence semantics, typed-only append input, and exact scope-string representation are concrete in Plan 02/06/09 and VALIDATION.
- Task/validation command parity and key-link mechanisms pass regression review.
- **No blockers or warnings.**
