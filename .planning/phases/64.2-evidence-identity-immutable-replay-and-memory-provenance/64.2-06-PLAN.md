---
phase: 64.2-evidence-identity-immutable-replay-and-memory-provenance
plan: "06"
name: Production Evidence Event Binding And Exact Replay Resolution
type: execute
wave: 3
depends_on: ["64.2-02"]
files_modified:
  - src/agent/nodes/investigate.py
  - src/agent/events.py
  - src/replay/decision_events.py
  - src/replay/schemas.py
  - src/replay/service.py
  - src/api/routers/traces.py
  - tests/replay/test_production_evidence_binding.py
autonomous: true
requirements: [SC-64.2-2, SC-64.2-3]
must_haves:
  truths:
    - "The real investigate -> agent event -> decision event path and every new ReplayService.append_event call accept only typed EvidenceRefV1 canonical_evidence_refs; append_event is the sole builder of full ReplayEvidenceSnapshotV1 records."
    - "evidence_refs_json is read-only compatibility data for persisted pre-Phase-64.2 events or an explicit migration adapter; it is never a new append input."
    - "Replay projects and resolves the exact immutable original version rather than looking up mutable current heads."
    - "Expiry or tombstone changes lifecycle visibility but retained immutable content/hash/locator remains replayable during audit retention; snapshot dependencies block destructive purge."
    - "Forged, mixed, unresolved, missing, and cross-scope bindings fail atomically and do not leak existence."
  artifacts:
    - {path: src/replay/schemas.py, provides: "Typed exact evidence snapshot binding", contains: "ReplayEvidenceSnapshotV1"}
    - {path: tests/replay/test_production_evidence_binding.py, provides: "Real emitter-to-API evidence binding test", contains: "test_investigate_event_replays_exact_original_evidence"}
  key_links:
    - {from: src/agent/nodes/investigate.py, to: src/agent/events.py, via: "terminal ToolResultV2 policy refs forwarded as canonical ref input", pattern: "canonical_evidence_refs"}
    - {from: src/agent/events.py, to: src/replay/decision_events.py, via: "typed canonical ref forwarding without reduced dict", pattern: "canonical_evidence_refs"}
    - {from: src/replay/service.py, to: src/repositories/evidence_version_repo.py, via: "append validation and exact resolution", pattern: "resolve_exact"}
---

<objective>Wire canonical evidence through the actual production event-emission path, persist an exact immutable binding, and resolve it through Replay V3/API after lifecycle changes. Direct `ReplayService.append_event` tests alone are not sufficient.</objective>
<execution_context>@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md @/Users/ming/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md @docs/contract-spec.md @.planning/ROADMAP.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-CONTEXT.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-RESEARCH.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-VALIDATION.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-02-SUMMARY.md @src/agent/nodes/investigate.py @src/agent/events.py @src/replay/decision_events.py @src/replay/schemas.py @src/replay/service.py @src/api/routers/traces.py</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Carry canonical refs through the real production emitter chain</name>
  <read_first>@src/agent/nodes/investigate.py @src/agent/events.py @src/replay/decision_events.py @src/replay/schemas.py @src/replay/service.py @src/knowledge/schemas.py @tests/agent/test_nodes/test_investigate.py @tests/agent/test_events.py @tests/replay/test_decision_events.py</read_first>
  <files>src/agent/nodes/investigate.py, src/agent/events.py, src/replay/decision_events.py, src/replay/schemas.py, src/replay/service.py, tests/replay/test_production_evidence_binding.py</files>
  <action>Add strict `ReplayEvidenceSnapshotV1` containing one full canonical `EvidenceRefV1`, exact strings `scope_type="tenant_policy"` and `scope_id=str(tenant_id)`, immutable document/chunk row IDs and versions, canonical identity hash, capture-time lifecycle status, retained-content hash/locator reference, and compatibility provenance. Production boundaries do not accept this enriched type. On terminal investigate events, pass actual typed `ToolResultV2.policy_evidence_refs` as the clearly named `canonical_evidence_refs: list[EvidenceRefV1]`; started/no-evidence events pass an empty typed list. Extend both configurable `event_emitter` and direct-session fallback, then add that exact typed argument to `src.agent.events.emit_event` and `src.replay.decision_events.emit_decision_event`, forwarding it unchanged to `ReplayService.append_event`. Change every new-write signature to `append_event(..., canonical_evidence_refs: list[EvidenceRefV1] | None = None, ...)`; remove `evidence_refs_json` as an accepted append argument rather than treating it as an alias. `append_event` must be the single owner of `_build_replay_evidence_snapshots`: resolve each canonical ref through `EvidenceVersionRepository` under event tenant and expected `scope_type="tenant_policy"`, `scope_id=str(tenant_id)`, recompute every serialized scope/ID/hash/version field, build the full snapshots, persist exact JSON, and insert normalized `EvidenceSnapshotDependency` rows in the same transaction. Reject mixed valid/forged/cross-scope batches atomically. Do not expose an emitter `evidence_snapshot_refs` input, place bindings in `resource_refs`, reconstruct from redacted payload, or accept raw/reduced dicts. Add a named negative that attempts the old raw/reduced `evidence_refs_json` write surface and proves no event, snapshot JSON, or dependency row is created. Tests must compare the entire stored snapshot projection field-for-field—including exact serialized/stored/hashed scope strings—with the append-time builder output, not only nested evidence ID.</action>
  <verify><automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay/test_production_evidence_binding.py tests/agent/test_nodes/test_investigate.py tests/agent/test_events.py tests/replay/test_decision_events.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/nodes/investigate.py src/agent/events.py src/replay/decision_events.py src/replay/schemas.py src/replay/service.py tests/replay/test_production_evidence_binding.py</automated></verify>
  <acceptance_criteria>The test invokes real `investigate`, observes identical typed `canonical_evidence_refs` at every emitter layer, proves append_event alone constructs snapshots, compares all stored snapshot/dependency fields and exact `scope_type="tenant_policy"`/`scope_id=str(tenant_id)` strings for configurable and direct paths, and proves forged, same-tenant cross-scope, or raw/reduced legacy append input causes zero event/snapshot/dependency rows.</acceptance_criteria>
  <done>Production tool events persist exact canonical evidence bindings end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Project and resolve original immutable evidence through Replay V3 and API</name>
  <read_first>@src/replay/schemas.py @src/replay/service.py @src/api/routers/traces.py @src/repositories/evidence_version_repo.py @tests/replay/test_replay_service.py @tests/replay/test_replay_migration_contract.py @tests/replay/test_replay_api.py @tests/replay/test_replay_redaction_retention.py</read_first>
  <files>src/replay/schemas.py, src/replay/service.py, src/api/routers/traces.py, tests/replay/test_production_evidence_binding.py</files>
  <action>Add `evidence_snapshot_refs: list[ReplayEvidenceSnapshotV1]` to `ReplayEventV3` and the minimal decision-event projection as an optional typed output field, preserving existing schema compatibility while keeping emitter/new-append input named `canonical_evidence_refs`. `ReplayService.project_event` must project the stored full snapshots. `get_replay` must resolve each exact immutable tenant/scope/ID/hash under the trusted run tenant and exact strings `scope_type="tenant_policy"`, `scope_id=str(tenant_id)`, and return allowlisted original retained content locator/hash plus current lifecycle status `current|superseded|corrected|archived|expired|tombstoned`; it must never query mutable `PolicyDocument`/`PolicyChunk` by logical alias for historical truth. Expiry/tombstone must not blank or replace immutable content/hash/locator before the event/dependency audit `retention_until`; a destructive purge or downgrade must fail while a live dependency exists. Add tests that re-ingest, expire, and tombstone the source after the production event and still recover exactly the original retained content/hash/locator and show updated lifecycle during retention. Only `evidence_refs_json` already persisted on a pre-Phase-64.2 event, or rows explicitly processed by a separately named migration adapter that reads an existing event ID, may resolve through Plan 02's compatibility resolver and retain `legacy_resolved`; neither projection/read compatibility path may call new-event append or accept caller JSON. Ambiguous/missing legacy refs remain `legacy_unresolved` with no verified content. Preserve replay redaction and owner/admin authorization. Return generic 404/unresolved external responses for cross-tenant run, same-tenant cross-scope target, missing target, forged binding, hash mismatch, or ambiguity while retaining internal reasons.</action>
  <verify><automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay/test_production_evidence_binding.py tests/replay/test_replay_service.py tests/replay/test_replay_migration_contract.py tests/replay/test_replay_api.py tests/replay/test_replay_redaction_retention.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/replay/schemas.py src/replay/service.py src/api/routers/traces.py tests/replay/test_production_evidence_binding.py</automated></verify>
  <acceptance_criteria>A run created through the production emitter resolves the identical full snapshot, original retained content/locator/ID/version/hash, and exact strings `scope_type="tenant_policy"`, `scope_id=str(tenant_id)` after changed re-ingestion, correction, supersession, archival, expiry, and tombstone during audit retention; legacy JSON is reachable only from pre-existing rows or the explicit read/migration adapter and cannot append; live dependencies reject purge/downgrade; mutable-current substitution and cross-scope leakage are absent.</acceptance_criteria>
  <done>Replay V3/API reconstruct exact original evidence from production-bound immutable snapshots.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Boundary | Disposition | Mitigation |
|---|---|---|---|---|
| T64.2-02 | Tampering | production result/current head -> replay | mitigate | Append-time exact binding through the real emitter chain; immutable-only resolution. |
| T64.2-03 | Elevation of Privilege | ambiguous legacy ref -> verified replay | mitigate | Explicit unresolved compatibility state; never infer authority. |
| T64.2-08 | Information Disclosure | replay lookup/API -> cross-scope target | mitigate | Trusted run tenant predicates, generic failures, bounded projection. |
</threat_model>
<verification>Both tasks must exercise `investigate`; a direct append-only E2E is insufficient. Unit coverage may call append_event only with typed `canonical_evidence_refs` to prove it is the sole snapshot builder. Production coverage must compare the full stored snapshot and dependency rows, and the old raw/reduced legacy input negative must prove zero event/snapshot/dependency rows.</verification>
<success_criteria>SC-64.2-3 is proven from the production emitter to exact replay resolution and API output.</success_criteria>
<output>After completion, create `64.2-06-SUMMARY.md`.</output>
