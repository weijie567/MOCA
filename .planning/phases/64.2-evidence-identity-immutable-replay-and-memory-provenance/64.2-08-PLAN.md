---
phase: 64.2-evidence-identity-immutable-replay-and-memory-provenance
plan: "08"
name: Exact-Identity Memory Lifecycle And Concurrency
type: execute
wave: 5
depends_on: ["64.2-03", "64.2-07"]
files_modified:
  - src/db/models.py
  - src/db/migrations/versions/028_phase64_2_memory_lifecycle.py
  - src/memory/case_memory.py
  - src/memory/tombstones.py
  - src/api/schemas/memory.py
  - src/api/routers/memory.py
  - tests/memory/test_case_memory_lifecycle.py
  - tests/memory/test_case_memory_concurrency.py
autonomous: true
requirements: [SC-64.2-4, SC-64.2-5]
must_haves:
  truths:
    - "Idempotency applies only when identity profile, candidate hash, content hash, source identity, tenant, and scope all match exactly."
    - "Distinct source identities are never returned as idempotent winners or merged by content-only uniqueness."
    - "A durable full-key identity claim survives rejected, expired, superseded, deleted, and tombstoned CaseMemory rows; active-only indexes are not the no-resurrection authority."
    - "Pending expiry, review, rejection, correction, supersession, deletion, and tombstone transitions update lifecycle row, durable claim, lineage, and event atomically so delayed writers cannot revive terminal identity."
    - "Duplicate groups larger than two retain deterministic survivor-to-many lineage through the normalized association table."
  artifacts:
    - {path: src/db/migrations/versions/028_phase64_2_memory_lifecycle.py, provides: "Durable exact identity claims, lifecycle constraints, and survivor-to-many lineage", contains: "case_memory_identity_claims"}
    - {path: tests/memory/test_case_memory_concurrency.py, provides: "Real PostgreSQL race matrix", contains: "test_source_distinct_race_is_not_idempotent"}
  key_links:
    - {from: src/memory/case_memory.py, to: src/db/models.py, via: "exact candidate claim and lifecycle CAS", pattern: "expected_lifecycle_version"}
    - {from: src/api/routers/memory.py, to: src/memory/case_memory.py, via: "versioned review/correction request", pattern: "expected_lifecycle_version"}
---

<objective>Enforce exact full-identity idempotency plus deterministic case-memory lifecycle transitions with database constraints, CAS, and real PostgreSQL concurrency. This plan does not redesign the broader Phase 68 lifecycle registry.</objective>
<execution_context>@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md @/Users/ming/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md @docs/contract-spec.md @.planning/ROADMAP.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-CONTEXT.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-RESEARCH.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-VALIDATION.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-03-SUMMARY.md @.planning/phases/64.2-evidence-identity-immutable-replay-and-memory-provenance/64.2-07-SUMMARY.md @src/db/models.py @src/memory/case_memory.py @src/memory/tombstones.py @src/api/routers/memory.py</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Establish durable exact-identity claims and deterministic duplicate lineage</name>
  <read_first>@src/db/models.py @src/db/migrations/versions/013_long_term_case_memory.py @src/db/migrations/versions/027_phase64_2_memory_provenance.py @src/memory/case_memory.py @tests/memory/test_case_memory_retrieval.py @tests/memory/test_case_memory_lifecycle.py</read_first>
  <files>src/db/models.py, src/db/migrations/versions/028_phase64_2_memory_lifecycle.py, tests/memory/test_case_memory_lifecycle.py</files>
  <action>Migration 028 must dry-run and classify duplicates by the complete canonical tuple `(identity_algorithm_version, tenant_id, scope_type, scope_id, candidate_hash, content_hash, source_identity_hash)`. Create `CaseMemoryIdentityClaim`/`case_memory_identity_claims` with all seven non-null key components, `owner_case_memory_id`, `claim_state=active|terminal`, nullable terminal status/reason/time, positive lifecycle version, and a UNIQUE constraint over the complete tuple. Claims have no soft-delete/expiry path and remain durable after rejected, pending-expired, superseded, deleted, or tombstoned memory rows. Backfill claims only for canonical/legacy_resolved provenance; unresolved envelopes receive no claim and remain non-authoritative. For byte-identical duplicate groups, retain the deterministic oldest row/claim owner, mark every later row superseded with an audit reason, and insert one deterministic `case_memory_lineage_links` row per related duplicate with stable ordinal. Keep an active partial index only as a query optimization, never as the no-resurrection authority; do not create content-hash-only uniqueness. Add positive lifecycle/claim versions, lineage consistency, terminal-state checks, restrictive FKs, and downgrade guards. Test a group of at least four exact duplicates: one survivor, three distinct association rows in deterministic order, one durable claim, no lossy scalar overwrite; source-distinct equal content remains separate; unresolved rows receive neither claim nor authoritative lineage.</action>
  <verify><automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_case_memory_lifecycle.py tests/memory/test_case_memory_retrieval.py tests/knowledge/test_immutable_evidence_migration.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/db/models.py src/db/migrations/versions/028_phase64_2_memory_lifecycle.py tests/memory/test_case_memory_lifecycle.py</automated></verify>
  <acceptance_criteria>Only exact full identities deduplicate; a 4-row group yields one deterministic survivor, three lineage links, and one durable full-key claim; source-distinct content remains distinct; unresolved legacy receives no claim; terminal claims survive; downgrade cannot erase claim or lineage history.</acceptance_criteria>
  <done>Database constraints enforce exact candidate identity without conflating source-distinct rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement CAS review, expiry, correction, and tombstone transitions</name>
  <read_first>@src/memory/case_memory.py @src/memory/tombstones.py @src/memory/schemas.py @src/api/schemas/memory.py @src/api/routers/memory.py @tests/memory/test_memory_tombstones.py @tests/test_memory_review_api.py @tests/memory/test_case_memory_lifecycle.py</read_first>
  <files>src/memory/case_memory.py, src/memory/tombstones.py, src/api/schemas/memory.py, src/api/routers/memory.py, tests/memory/test_case_memory_lifecycle.py</files>
  <action>Make durable full-key claim acquisition/lock, existing MemoryTombstone check, expired-pending retirement, CaseMemory insert, lineage, and event persistence one transaction. `list_pending_review` must require resolved `CaseMemoryProvenanceV1`, an active matching claim, and `expires_at IS NULL OR expires_at > now`; unresolved envelopes cannot participate. Review/correction requests require `expected_lifecycle_version`; execute one tenant/id/status/version/non-expired conditional update and increment lifecycle row plus claim version exactly once. An identical retry returns the persisted decision/event only after comparing the complete canonical identity and decision payload. A different source identity, candidate hash, decision, stale version, unresolved envelope, or existing terminal claim returns generic conflict and never returns the winner as idempotent. Expiry uses existing superseded row status plus `pending_review_expired` and terminalizes the claim. Rejection, ordinary supersession, deletion, and tombstone likewise retain and terminalize the full-key claim; none deletes/releases it. Correction atomically terminalizes the old claim, supersedes the old row, acquires a distinct new owner-produced claim, inserts the corrected row, and stores direct plus association lineage. The MemoryTombstone write and durable claim transition share the transaction so delayed writers cannot revive terminal identity.</action>
  <verify><automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_case_memory_lifecycle.py tests/memory/test_memory_tombstones.py tests/memory/test_memory_write_service.py tests/test_memory_review_api.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/memory/case_memory.py src/memory/tombstones.py src/api/schemas/memory.py src/api/routers/memory.py tests/memory/test_case_memory_lifecycle.py</automated></verify>
  <acceptance_criteria>Expired/unresolved items are not pending or approvable; exact retries are idempotent only against an active matching claim; source-distinct/stale/competing decisions conflict; rejected/expired/superseded/deleted/tombstoned claims remain durable; correction lineage is complete; no terminal identity is released or resurrected.</acceptance_criteria>
  <done>All owned lifecycle transitions are transactional, versioned, provenance-preserving, and fail closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove exact-identity and lifecycle races in real PostgreSQL</name>
  <read_first>@tests/conftest.py @tests/memory/test_session_memory_concurrency.py @src/memory/case_memory.py @src/memory/tombstones.py @tests/memory/test_case_memory_concurrency.py</read_first>
  <files>tests/memory/test_case_memory_concurrency.py</files>
  <action>Add two-session PostgreSQL barriers for: identical exact candidate submissions; equal content with distinct source identity; identical review retry; approve versus reject; review versus expiry; delayed submit after rejection; delayed submit after expiry/supersession; delayed submit after deletion; delayed submit versus tombstone; and correction versus duplicate submit. Assert exact memory/claim/lineage/event cardinality, one terminal winner where applicable, monotonic lifecycle and claim versions, unchanged reviewer/source provenance, distinct source rows or explicit conflict (never idempotent merge), retained terminal claims for every terminal state, and zero resurrected rows. Include the four-row migration group lineage assertions from Task 1 under PostgreSQL. Do not substitute SQLite, mocks, or sequential calls.</action>
  <verify><automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_case_memory_concurrency.py tests/memory/test_case_memory_lifecycle.py tests/test_memory_review_api.py -q --tb=short &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run ruff check tests/memory/test_case_memory_concurrency.py tests/memory/test_case_memory_lifecycle.py tests/test_memory_review_api.py</automated></verify>
  <acceptance_criteria>All named PostgreSQL races have deterministic memory/claim/lineage/event cardinality; exact active retries alone are idempotent; distinct source identity is never conflated; rejected, expired, superseded, deleted, and tombstoned identities retain terminal claims and cannot be revived.</acceptance_criteria>
  <done>The PostgreSQL race matrix proves exact full-identity idempotency and CAS lifecycle behavior.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Boundary | Disposition | Mitigation |
|---|---|---|---|---|
| T64.2-05 | Tampering/Repudiation | concurrent submit/review -> durable state | mitigate | Full-key durable unique claim, lifecycle CAS, lineage/event cardinality, payload equality. |
| T64.2-06 | Elevation of Privilege | delayed writer -> terminal/tombstoned identity | mitigate | Same-transaction retained terminal claim and tombstone transition. |
| T64.2-08 | Information Disclosure | stale/cross-tenant review -> API | mitigate | Tenant predicate and generic 404/409 responses. |
</threat_model>
<verification>Run all task commands against PostgreSQL. An unavailable local PostgreSQL fixture is a validation incident to record, not permission to replace the gate.</verification>
<success_criteria>SC-64.2-5 holds under real concurrency without content-only or source-distinct conflation.</success_criteria>
<output>After completion, create `64.2-08-SUMMARY.md`.</output>
