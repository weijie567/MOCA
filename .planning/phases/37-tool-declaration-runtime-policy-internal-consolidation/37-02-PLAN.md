---
phase: 37-tool-declaration-runtime-policy-internal-consolidation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tools/runtime.py
  - tests/tools/test_tool_platform.py
  - tests/replay/test_tool_policy_events.py
autonomous: true
requirements: [TPH-04]
must_haves:
  truths:
    - "ToolRuntime failure exits produce error result, projection, runtime-auth decision event id, and tuple return through one shared _fail helper."
    - "Failure helper output remains prompt/replay safe and does not leak raw args, raw tool output, input schema, required permission, or caller allowlist."
    - "Input schema validation still runs before runtime_auth so unvalidated args never enter resource binding or decision events."
  artifacts:
    - path: "src/tools/runtime.py"
      provides: "ToolRuntime._fail helper and refactored failure branches"
    - path: "tests/tools/test_tool_platform.py"
      provides: "runtime helper structural/behavior regression"
    - path: "tests/replay/test_tool_policy_events.py"
      provides: "runtime auth low-payload event regression"
  key_links:
    - from: "ToolRuntime.invoke"
      to: "ToolRuntime._fail"
      via: "await self._fail(...) for every failure branch"
      pattern: "await self\\._fail\\("
    - from: "ToolRuntime._fail"
      to: "ToolResultProjector.project + _emit_decision_event"
      via: "shared helper projection/event/tuple assembly"
      pattern: "_projector\\.project|_emit_decision_event"
---

<objective>
Consolidate `ToolRuntime.invoke` failure paths through one shared `_fail(...)` helper for TPH-04.

Purpose: the runtime currently repeats safe error construction, projection, decision-event emission, and tuple assembly across failure branches. The refactor must reduce that duplicated logic while preserving every current status/code/source mapping and low-payload event behavior.

Output: `src/tools/runtime.py` has a private async `_fail(...)` helper used by all runtime failure exits, with focused tests proving structure and safe behavior.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/37-tool-declaration-runtime-policy-internal-consolidation/37-RESEARCH.md
@.planning/phases/37-tool-declaration-runtime-policy-internal-consolidation/37-PATTERNS.md
@.planning/phases/37-tool-declaration-runtime-policy-internal-consolidation/37-VALIDATION.md
@AGENTS.md
@docs/contract-spec.md
@src/tools/runtime.py
@src/tools/manager_results.py
@src/tools/projection.py
@src/tools/contracts.py
@tests/tools/test_tool_platform.py
@tests/agent/test_tools/test_unified_tool_manager.py
@tests/replay/test_tool_policy_events.py

<interfaces>
Preserve `ToolRuntime.invoke(...)` return shape:
```python
tuple[ToolResultV2, ToolPolicyDecision, str | None, ToolResultProjectionV1]
```

Preserve `safe_result(...)` status/source surface:
```python
status: Literal["not_found", "permission_denied", "unavailable", "invalid_request", "invalid_response", "error"]
source: Literal["caller", "tool", "adapter", "policy"]
```

Preserve `_emit_decision_event(...)` redacted payload keys:
```python
"decision_stage", "tool_name", "decision", "reason_codes",
"policy_version", "data_classification", "runtime_available"
```
</interfaces>

<constraints>
- Keep descriptor lookup before input validation, and input validation before `runtime_auth`.
- Keep current failure mappings: `TOOL_NOT_FOUND`, `INVALID_TOOL_INPUT`, policy denial mapping from `_safe_denial_result`, `TOOL_UNAVAILABLE`, `EXECUTOR_ERROR`, and `INVALID_EXECUTOR_RESPONSE`.
- Do not include raw `args`, raw executor output, `input_schema`, `required_permission`, `caller_allowlist`, descriptors, or internal registry rows in `redacted_payload`.
- Do not change `ToolResultV2`, `ToolCallContext`, `ToolPolicyDecision`, `ToolViewV1`, or `ToolInvocationOutcome` fields.
- Do not edit `docs/contract-spec.md`.
- Do not implement Phase 38 real output schemas; preserve the current generic output validation behavior.
</constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add runtime helper and event payload regression tests</name>
  <files>tests/tools/test_tool_platform.py, tests/replay/test_tool_policy_events.py</files>
  <read_first>
    - tests/tools/test_tool_platform.py
    - tests/replay/test_tool_policy_events.py
    - src/tools/runtime.py
    - src/tools/manager_results.py
    - src/tools/projection.py
    - .planning/phases/37-tool-declaration-runtime-policy-internal-consolidation/37-VALIDATION.md
  </read_first>
  <action>
    Add tests before implementation for TPH-04:
    1. In `tests/tools/test_tool_platform.py`, add `test_tool_runtime_failure_paths_use_shared_fail_helper`. Use `inspect.getsource(ToolRuntime.invoke)` and `inspect.getsource(ToolRuntime)` to assert `async def _fail(` exists and `ToolRuntime.invoke` contains `await self._fail(` for the runtime failure paths. The structural assertion should check a stable lower bound of at least seven helper calls, matching the current failure exits: missing descriptor, invalid input, policy denied, executor unavailable, executor exception, non-`ToolResultV2` return, and output schema failure.
    2. In `tests/tools/test_tool_platform.py`, add or extend a behavior test proving a failure result still has a `ToolResultProjectionV1` projection through `ToolPlatform.invoke(...)`, and raw sentinel input such as `"RAW-RUNTIME-SENTINEL"` does not appear in `outcome.model_dump_json()` for invalid input or missing tool.
    3. In `tests/replay/test_tool_policy_events.py`, add `test_tool_runtime_event_payload_source_omits_raw_descriptor_and_args`. Inspect `ToolRuntime._emit_decision_event` and assert forbidden payload/source strings are absent from the redacted payload construction: `raw_args`, `raw_tool_output`, `input_schema`, `required_permission`, `caller_allowlist`, `arguments`, and `data`.
  </action>
  <verify>
    <automated>uv run pytest tests/tools/test_tool_platform.py tests/replay/test_tool_policy_events.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/tools/test_tool_platform.py` contains `test_tool_runtime_failure_paths_use_shared_fail_helper`.
    - `tests/tools/test_tool_platform.py` checks for `await self._fail(` in `ToolRuntime.invoke`.
    - `tests/replay/test_tool_policy_events.py` contains `test_tool_runtime_event_payload_source_omits_raw_descriptor_and_args`.
    - The tests use `uv run pytest ...` in documentation/comments if command examples are added; no bare `pytest` command is introduced.
  </acceptance_criteria>
  <done>Runtime helper and event payload regression tests exist and fail against the pre-refactor duplicated runtime branches.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor all ToolRuntime failure exits through _fail</name>
  <files>src/tools/runtime.py</files>
  <read_first>
    - src/tools/runtime.py
    - src/tools/manager_results.py
    - src/tools/projection.py
    - src/tools/contracts.py
    - tests/tools/test_tool_platform.py
    - tests/agent/test_tools/test_unified_tool_manager.py
    - tests/replay/test_tool_policy_events.py
  </read_first>
  <action>
    Add a private async helper on `ToolRuntime`, near `_safe_denial_result` / `_emit_decision_event`:
    `async def _fail(self, *, tool_name: str, ctx: ToolCallContext, decision: ToolPolicyDecision, session: AsyncSession | None, result: ToolResultV2 | None = None, status: Literal[...] | None = None, summary: str | None = None, code: str | None = None, source: Literal["caller", "tool", "adapter", "policy"] = "caller") -> tuple[ToolResultV2, ToolPolicyDecision, str | None, ToolResultProjectionV1]`.

    The helper must:
    - use the supplied `result` when present, otherwise call `safe_result(status, summary, code=code, source=source)`;
    - call `self._projector.project(tool_name=tool_name, result=error_result, tool_call_id=ctx.tool_call_id)`;
    - call `await self._emit_decision_event(decision=decision, ctx=ctx, session=session)`;
    - return `(error_result, decision, event_id, projection)`.

    Refactor these `ToolRuntime.invoke` failure branches to call `await self._fail(...)`: descriptor missing, input schema validation failure, runtime policy denial, executor unavailable, executor exception, executor returns non-`ToolResultV2`, and output schema validation failure.

    Preserve `_safe_denial_result(decision)` for policy denials by passing its result into `_fail(result=...)`. Preserve the success path unchanged: successful executor results are projected, decision events are emitted, and `(tool_result, decision, event_id, projection)` is returned outside `_fail`.
  </action>
  <verify>
    <automated>uv run pytest tests/tools/test_tool_platform.py tests/agent/test_tools/test_unified_tool_manager.py tests/replay/test_tool_policy_events.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/tools/runtime.py` contains `async def _fail(`.
    - `ToolRuntime.invoke` contains at least seven `await self._fail(` calls.
    - `ToolRuntime.invoke` still calls `validate_json_value(args, descriptor.input_schema)` before `self._policy_engine.runtime_auth(`.
    - `_emit_decision_event` redacted payload still contains only decision metadata and does not include raw args, raw output, descriptor schema, required permission, or caller allowlist.
    - Existing tests for `test_unknown_tool_returns_not_found`, `test_invalid_input_returns_invalid_request`, `test_malformed_executor_return_becomes_invalid_response`, `test_output_schema_failure_returns_invalid_response_without_raw_data`, and `test_manager_generated_errors_have_no_raw_payload_or_secret` pass.
  </acceptance_criteria>
  <done>Every current `ToolRuntime.invoke` failure exit routes through `_fail(...)`, preserves existing status/code/source behavior, and focused runtime/replay tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller args -> runtime auth | Only schema-validated args may enter resource binding and runtime auth decisions. |
| executor/runtime -> prompt/replay | Runtime failures must pass through safe result construction, projection, and low-payload event emission. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-02 | Information Disclosure | `ToolRuntime._fail`, `ToolResultProjector` | mitigate | Route failures through `_fail(...)`, use `safe_result(...)` / `_safe_denial_result(...)`, always project through `ToolResultProjector`, and add raw-sentinel regression tests. |
| T-37-03 | Information Disclosure | `ToolRuntime._emit_decision_event` | mitigate | Keep runtime auth event payload limited to decision metadata; add tests forbidding raw args, raw tool output, `input_schema`, `required_permission`, and `caller_allowlist`. |
</threat_model>

<verification>
Run these exact commands after the plan tasks:

```bash
uv run pytest tests/tools/test_tool_platform.py tests/agent/test_tools/test_unified_tool_manager.py tests/replay/test_tool_policy_events.py -q
uv run ruff check src/tools/runtime.py tests/tools/test_tool_platform.py tests/replay/test_tool_policy_events.py
```
</verification>

<success_criteria>
- TPH-04 runtime failure-helper consolidation is implemented without contract field changes.
- Every failure exit in `ToolRuntime.invoke` goes through `_fail(...)`.
- Runtime auth event payloads remain low-payload and redacted.
- Current failure behavior and result statuses remain green under the existing tool-manager/tool-platform tests.
</success_criteria>

<must_haves>
- Truth: runtime failures assemble `(ToolResultV2, ToolPolicyDecision, event_id, ToolResultProjectionV1)` through `_fail(...)`.
- Artifact: `src/tools/runtime.py` contains `ToolRuntime._fail`.
- Key link: `_fail(...)` calls `ToolResultProjector.project` and `_emit_decision_event`.
- Regression: `uv run pytest tests/tools/test_tool_platform.py tests/agent/test_tools/test_unified_tool_manager.py tests/replay/test_tool_policy_events.py -q` passes.
</must_haves>

<source_audit>
GOAL: Phase 37 success criterion 2 is covered by this plan.
REQ: TPH-04 runtime helper portion is covered by this plan.
RESEARCH: all current failure branches, safe result mapping, event redaction, and input-validation-before-runtime-auth ordering are covered.
CONTEXT: user constraints banning raw leak, contract shape changes, spec edits, real output schemas, and bare pytest commands are covered.
</source_audit>

<output>
After completion, create `.planning/phases/37-tool-declaration-runtime-policy-internal-consolidation/37-02-SUMMARY.md`.
</output>
