---
phase: 37-tool-declaration-runtime-policy-internal-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tools/catalog.py
  - src/tools/manager.py
  - tests/tools/test_catalog.py
  - tests/agent/test_tools/test_unified_tool_manager.py
autonomous: true
requirements: [TPH-03]
must_haves:
  truths:
    - "Adding or changing a tool declaration requires editing one catalog registry row/table, not separate schema and manager name lists."
    - "catalog._IDENTIFIER_SCHEMAS and manager.INVESTIGATE_TOOL_NAMES are derived from catalog declarations or checked against catalog-derived descriptors."
    - "UnifiedToolManager remains a compatibility adapter and does not become a policy/runtime owner."
  artifacts:
    - path: "src/tools/catalog.py"
      provides: "single-source tool declaration rows and derived ToolDescriptor instances"
    - path: "src/tools/manager.py"
      provides: "catalog-derived investigate descriptor filtering"
    - path: "tests/tools/test_catalog.py"
      provides: "registry/schema/name drift guard"
    - path: "tests/agent/test_tools/test_unified_tool_manager.py"
      provides: "manager compatibility drift guard"
  key_links:
    - from: "src/tools/catalog.py"
      to: "src/tools/manager.py"
      via: "ToolCatalog().descriptors() / catalog-derived helper"
      pattern: "ToolCatalog\\(\\)\\.descriptors|investigate_tool_names"
    - from: "tests/agent/test_tools/test_unified_tool_manager.py"
      to: "src/tools/catalog.py"
      via: "expected investigate set derived from catalog descriptors"
      pattern: "ToolCatalog\\(\\)\\.descriptors"
---

<objective>
Create the Phase 37 registry single source and drift guard for TPH-03.

Purpose: `ToolCatalog` is the normative declaration source in `docs/contract-spec.md` §12.6, so the implementation must stop requiring separate hand edits to `_IDENTIFIER_SCHEMAS` and `INVESTIGATE_TOOL_NAMES` when a tool changes.

Output: `src/tools/catalog.py` has one internal declaration row/table that feeds descriptor creation, `src/tools/manager.py` derives investigate visibility from catalog descriptors, and focused tests fail on registry/name/schema drift.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/37-tool-declaration-runtime-policy-internal-consolidation/37-RESEARCH.md
@.planning/phases/37-tool-declaration-runtime-policy-internal-consolidation/37-PATTERNS.md
@.planning/phases/37-tool-declaration-runtime-policy-internal-consolidation/37-VALIDATION.md
@AGENTS.md
@docs/contract-spec.md
@src/tools/catalog.py
@src/tools/manager.py
@src/tools/contracts.py
@tests/tools/test_catalog.py
@tests/agent/test_tools/test_unified_tool_manager.py

<interfaces>
From `src/tools/catalog.py`, preserve the external descriptor field surface exactly:
```python
class ToolDescriptor(BaseModel):
    model_config = ConfigDict(extra="forbid")
    name: str
    description: str = ""
    kind: Literal["read", "retrieval", "write"]
    input_schema: dict[str, Any]
    output_schema: dict[str, Any]
    risk_level: Literal["read", "retrieval", "write"]
    side_effect: Literal["none", "read_only", "retrieval", "write"]
    required_permission: str
    caller_allowlist: list[str]
    event_family: Literal["tool_call_*", "rag_retrieval_*", "action"] | None
    resource_type: str | None
    executor: Literal["business", "knowledge", "memory", "action"] | None = None
    exposure: Literal["planner_visible", "node_only", "internal"] = "planner_visible"
    requires_approval: bool = False
    requires_safety_snapshot: bool = False
    requires_idempotency_key: bool = False
```

From `src/tools/manager.py`, preserve the adapter boundary:
```python
async def invoke(self, name: str, args: dict[str, Any], ctx: ToolCallContext) -> ToolResultV2:
    outcome = await self._platform.invoke(name, args, ctx, session=None)
    return outcome.tool_result
```
</interfaces>

<constraints>
- Do not add, remove, or rename fields on `ToolResultV2`, `ToolCallContext`, `ToolPolicyDecision`, `ToolViewV1`, or `ToolInvocationOutcome`.
- Do not redefine, widen, or rename §8.0 identity/scope fields on `ToolCallContext`.
- Do not edit `docs/contract-spec.md`; Phase 39 owns spec reconciliation.
- Do not implement real per-tool `output_schema`; keep the Phase 37 generic `{"type": "object"}` behavior stable for every descriptor.
- Do not add new tools, executors, policy gates, rate limits, cost-budget gates, or domain ownership enforcement.
- Use only MOCA-approved verification entrypoints: `uv run ...`, `UV_CACHE_DIR=/tmp/uv-cache uv run ...`, `.venv/bin/...`; never bare `pytest` or bare `python -m pytest`.
</constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add registry and manager drift tests</name>
  <files>tests/tools/test_catalog.py, tests/agent/test_tools/test_unified_tool_manager.py</files>
  <read_first>
    - tests/tools/test_catalog.py
    - tests/agent/test_tools/test_unified_tool_manager.py
    - src/tools/catalog.py
    - src/tools/manager.py
    - .planning/phases/37-tool-declaration-runtime-policy-internal-consolidation/37-VALIDATION.md
  </read_first>
  <action>
    Add tests before implementation for TPH-03:
    1. In `tests/tools/test_catalog.py`, add `test_catalog_registry_derives_identifier_schemas_without_drift`. It must compare `_IDENTIFIER_SCHEMAS` to `{descriptor.name: descriptor.input_schema for descriptor in ToolCatalog().descriptors()}` if `_IDENTIFIER_SCHEMAS` remains exported as a private compatibility surface, and it must assert every default descriptor still has `descriptor.output_schema == {"type": "object"}`.
    2. In `tests/tools/test_catalog.py`, extend the existing single-source test or add a new test that derives the investigate names from `ToolCatalog().descriptors()` using `caller_allowlist`, `kind != "write"`, and `exposure == "planner_visible"`; do not introduce a second hand-maintained tool-name set in the test.
    3. In `tests/agent/test_tools/test_unified_tool_manager.py`, replace the module-level `INVESTIGATE_TOOLS` expected set with a helper such as `_catalog_investigate_tool_names()` that derives from `ToolCatalog().descriptors()` using the same production criteria: `"investigate" in descriptor.caller_allowlist`, `descriptor.kind != "write"`, and `descriptor.exposure == "planner_visible"`.
    4. Preserve the explicit behavioral assertion that `"create_coupon_grant_draft"` is excluded from investigate descriptors and preserve `test_declared_future_search_sop_returns_unavailable`.
  </action>
  <verify>
    <automated>uv run pytest tests/tools/test_catalog.py tests/agent/test_tools/test_unified_tool_manager.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/tools/test_catalog.py` contains `test_catalog_registry_derives_identifier_schemas_without_drift`.
    - `tests/tools/test_catalog.py` contains the exact assertion target `{"type": "object"}` for Phase 37 generic `output_schema` preservation.
    - `tests/agent/test_tools/test_unified_tool_manager.py` no longer defines a literal `INVESTIGATE_TOOLS = {` block.
    - `tests/agent/test_tools/test_unified_tool_manager.py` derives expected investigate names from `ToolCatalog().descriptors()`.
    - The command `uv run pytest tests/tools/test_catalog.py tests/agent/test_tools/test_unified_tool_manager.py -q` fails before Task 2 if production still has drift, and passes after Task 2.
  </acceptance_criteria>
  <done>Registry and manager drift tests exist, derive expectations from `ToolCatalog().descriptors()`, and are ready to fail before the production refactor.</done>
</task>

<task type="auto">
  <name>Task 2: Implement single-source declaration rows and catalog-derived investigate filtering</name>
  <files>src/tools/catalog.py, src/tools/manager.py</files>
  <read_first>
    - src/tools/catalog.py
    - src/tools/manager.py
    - src/tools/contracts.py
    - docs/contract-spec.md §8.0 and §12.5/§12.6
    - tests/tools/test_catalog.py
    - tests/agent/test_tools/test_unified_tool_manager.py
  </read_first>
  <action>
    In `src/tools/catalog.py`, introduce one internal frozen registry row/table, for example `@dataclass(frozen=True) class _ToolDeclaration` and `_TOOL_DECLARATIONS: tuple[_ToolDeclaration, ...]`. Each row must include the current declaration values for all nine existing tools: `name`, `description`, `kind`, `input_schema`, `side_effect`, `caller_allowlist`, `event_family`, `resource_type`, `executor`, `exposure`, `requires_approval`, `requires_safety_snapshot`, and `requires_idempotency_key`.

    Refactor `_descriptor(...)` so it accepts a `_ToolDeclaration` row and creates `ToolDescriptor` from that row. Keep `required_permission=f"tool:{declaration.name}"`, `risk_level=declaration.kind`, and `output_schema=_GENERIC_OBJECT_SCHEMA`. Replace `_default_descriptors()` with `[ _descriptor(declaration) for declaration in _TOOL_DECLARATIONS ]`.

    If `_IDENTIFIER_SCHEMAS` is retained, make it derived only: `_IDENTIFIER_SCHEMAS = {declaration.name: declaration.input_schema for declaration in _TOOL_DECLARATIONS}`. It must not be another hand-maintained map. Add a catalog helper such as `investigate_tool_names(descriptors: Iterable[ToolDescriptor] | None = None) -> frozenset[str]` that derives names from descriptor attributes, not literals.

    In `src/tools/manager.py`, remove the literal `INVESTIGATE_TOOL_NAMES = {"get_order", ...}` set. If keeping the exported constant for compatibility, define it as `INVESTIGATE_TOOL_NAMES = investigate_tool_names()` imported from `src.tools.catalog`. In `UnifiedToolManager.descriptors("investigate")`, filter directly on the descriptor attributes (`caller_allowlist`, `kind`, and `exposure`) so custom descriptors do not depend on the default catalog constant.

    Preserve `UnifiedToolManager.invoke(...)` delegation to `ToolPlatform.invoke(...)`; do not add policy/runtime/projection logic to `manager.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/tools/test_catalog.py tests/agent/test_tools/test_unified_tool_manager.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/tools/catalog.py` contains `_ToolDeclaration`.
    - `src/tools/catalog.py` contains `_TOOL_DECLARATIONS`.
    - `src/tools/catalog.py` contains `_IDENTIFIER_SCHEMAS = {declaration.name: declaration.input_schema for declaration in _TOOL_DECLARATIONS}` if `_IDENTIFIER_SCHEMAS` remains present.
    - `src/tools/catalog.py` keeps `_GENERIC_OBJECT_SCHEMA: dict[str, Any] = {"type": "object"}` and does not add real per-tool output schemas.
    - `src/tools/manager.py` has no literal tool-name set for `INVESTIGATE_TOOL_NAMES`; `rg -n '"get_order"|"search_sop"|INVESTIGATE_TOOL_NAMES = \{' src/tools/manager.py` returns no matches.
    - `UnifiedToolManager.descriptors("investigate")` filters using `descriptor.caller_allowlist`, `descriptor.kind != "write"`, and `descriptor.exposure == "planner_visible"`.
    - `test_declared_future_search_sop_returns_unavailable` still passes.
  </acceptance_criteria>
  <done>Catalog declarations are row/table-driven, manager investigate filtering is catalog-derived, generic output schemas remain unchanged, and focused registry/manager tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog -> planner visibility | Catalog descriptors are projected through manager/platform into planner-visible tool lists. |
| catalog -> manager compatibility | Legacy manager descriptor filtering must not become an independent authorization source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering / Elevation of Privilege | `src/tools/catalog.py`, `src/tools/manager.py` | mitigate | Derive `_IDENTIFIER_SCHEMAS` and `INVESTIGATE_TOOL_NAMES` from the catalog declaration rows and add drift tests that compare manager investigate names to catalog-derived descriptors. |
</threat_model>

<verification>
Run these exact commands after the plan tasks:

```bash
uv run pytest tests/tools/test_catalog.py tests/agent/test_tools/test_unified_tool_manager.py -q
uv run ruff check src/tools/catalog.py src/tools/manager.py tests/tools/test_catalog.py tests/agent/test_tools/test_unified_tool_manager.py
```
</verification>

<success_criteria>
- TPH-03 is implemented without adding, removing, or renaming external contract fields.
- `ToolCatalog` declaration data is the single source for descriptor-level schema/name metadata.
- Manager investigate descriptors are derived from descriptor attributes and exclude write/node-only tools.
- Registry drift tests fail on duplicated hand-maintained schema/name lists and pass after implementation.
</success_criteria>

<must_haves>
- Truth: adding or changing a tool declaration requires editing one catalog registry row/table.
- Artifact: `src/tools/catalog.py` contains `_TOOL_DECLARATIONS` and derived descriptors.
- Artifact: `src/tools/manager.py` no longer contains a literal investigate tool-name set.
- Key link: manager investigate descriptors are derived from `ToolCatalog().descriptors()` or equivalent descriptor objects.
- Regression: `uv run pytest tests/tools/test_catalog.py tests/agent/test_tools/test_unified_tool_manager.py -q` passes.
</must_haves>

<source_audit>
GOAL: Phase 37 success criterion 1 is covered by this plan.
REQ: TPH-03 is covered by this plan.
RESEARCH: registry row/table, derived `_IDENTIFIER_SCHEMAS`, derived investigate names, generic output schema preservation, and `search_sop` unavailable behavior are covered.
CONTEXT: user constraints banning external contract field changes, spec edits, real output schemas, new tools/executors/gates, and bare pytest commands are covered.
</source_audit>

<output>
After completion, create `.planning/phases/37-tool-declaration-runtime-policy-internal-consolidation/37-01-SUMMARY.md`.
</output>
