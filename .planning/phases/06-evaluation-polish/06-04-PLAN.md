---
phase: 06-evaluation-polish
plan: 04
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - docs/demo-walkthrough.md
  - docs/evaluation.md
  - docs/architecture.md
  - docs/security-and-permission.md
autonomous: true
requirements: [EVAL-07, INFR-07]
must_haves:
  truths:
    - "docs/demo-walkthrough.md contains 6-7 annotated demo scenarios with curl examples and interview talking points"
    - "docs/evaluation.md documents golden set design, metrics, thresholds, and CI logic"
    - "docs/architecture.md describes system architecture and agent workflow in detail"
    - "docs/security-and-permission.md covers RBAC, approval flow, audit trail, and risk boundaries"
  artifacts:
    - path: "docs/demo-walkthrough.md"
      provides: "10-minute demo flow with interview talking points"
      contains: "curl"
    - path: "docs/evaluation.md"
      provides: "Evaluation methodology documentation"
      contains: "Hit@5"
    - path: "docs/architecture.md"
      provides: "System and agent architecture detail"
      contains: "LangGraph"
    - path: "docs/security-and-permission.md"
      provides: "Security model documentation"
      contains: "risk_rules"
  key_links:
    - from: "README.md"
      to: "docs/demo-walkthrough.md"
      via: "hyperlink from 10-Minute Demo section"
      pattern: "demo-walkthrough"
    - from: "README.md"
      to: "docs/evaluation.md"
      via: "hyperlink from Evaluation Summary section"
      pattern: "evaluation.md"
---

<objective>
Create the four documentation files that provide depth behind the README's overview: demo walkthrough with interview talking points, evaluation methodology, architecture detail, and security model.

Purpose: Supports EVAL-07 (latency/cost documentation) and INFR-07 (evaluation framework documentation). Provides the interview-ready depth layer that README links to, enabling a 10-minute demo walkthrough and technical deep-dive on demand.

Output: `docs/demo-walkthrough.md`, `docs/evaluation.md`, `docs/architecture.md`, `docs/security-and-permission.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-evaluation-polish/06-CONTEXT.md
@.planning/phases/06-evaluation-polish/06-RESEARCH.md
@.planning/phases/06-evaluation-polish/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/demo-walkthrough.md with interview talking points</name>
  <files>docs/demo-walkthrough.md</files>
  <read_first>
    - scripts/demo_phase6.sh
    - scripts/seed_demo.py
    - src/moca/api/routes/agent.py
  </read_first>
  <action>
Create `docs/demo-walkthrough.md` per D-05a and D-06a.

**IMPORTANT: `scripts/demo_phase6.sh` is the canonical source for curl commands.** This walkthrough adds ONLY:
- Interview talking points (what to say to the interviewer at each step)
- Expected response highlights (key fields to point out)
- Pacing guidance (time allocation per section)

Do NOT duplicate curl commands verbatim. Instead, reference the script and show only the query/endpoint for context, then focus on the annotation layer.

**Structure:**

```markdown
# MOCA 10-Minute Demo Walkthrough

## Overview

This walkthrough demonstrates MOCA's core capabilities in a 10-minute interview format.
Pacing: 1 min problem statement, 2 min architecture, 4 min live demo, 2 min trace/eval, 1 min summary.

> **Note:** All curl commands are in `scripts/demo_phase6.sh`. Run `bash scripts/demo_phase6.sh` for the automated version. This document adds interview context and talking points.

## Deterministic Demo vs Live Smoke Test

- The demo script uses FakeLLM by default — it validates workflow determinism (graph routing, approval logic, permission checks)
- To demo with a real LLM: `export DASHSCOPE_API_KEY=... && bash scripts/demo_phase6.sh`
- `seed_demo.py --reset` guarantees the same business data every time

## Prerequisites

- Docker Compose running: `make up`
- Demo data seeded: `make seed`
- jq installed for JSON formatting

## Demo Accounts

| Username | Role | Scopes | Purpose |
|----------|------|--------|---------|
| cs_zhang | support_agent | agent:chat, orders:read, refunds:read | Submit queries |
| mgr_li | manager | approvals:review, agent:chat | Approve/reject |

Password for all: `moca2024`
```

**For each of the 7 scenarios, include:**

1. **Section header** with scenario number
2. **Interview talking point** (1-2 sentences explaining what this demonstrates and why it matters — in a callout block)
3. **curl command** (copy-pasteable)
4. **Expected response** (key fields highlighted, not full JSON)
5. **What to point out** (specific fields that show the system's intelligence)

**Scenarios (matching D-06c):**

1. **Authentication** — Get tokens for both roles
   - Talking point: "RBAC with OAuth2 scopes ensures each role can only access appropriate endpoints"

2. **Policy QA** — "退款超时规则是什么？"
   - Talking point: "The agent retrieves evidence from the knowledge base and cites specific policy documents — no hallucination"
   - Point out: evidence_refs with doc_id/chunk_id, citation in response

3. **Refund Troubleshooting** — "订单ORD-xxx退款进度如何？"
   - Talking point: "The agent calls structured tools (get_order, get_refund_case) to load business context before answering"
   - Point out: tools_called in trace, order data in context

4. **Compensation Suggestion (High-Risk)** — "客户要求补偿600元"
   - Talking point: "Risk assessment is deterministic — rules/risk_rules.yaml defines thresholds. Above 200 yuan triggers mandatory approval"
   - Point out: risk_level=high, approval_required=true, approval_request_id

5. **Permission Denied** — Support agent tries to approve
   - Talking point: "Defense in depth — even if someone gets the approval ID, the system checks role permissions"
   - Point out: 403 status, missing_scopes or role error

6. **Approval Rejected** — Manager rejects with reason
   - Talking point: "The graph resumes after human decision via LangGraph Command(resume=...) — rejection stops execution and records the reason"
   - Point out: decision=reject, reason preserved in audit

7. **Trace Query** — View execution trace
   - Talking point: "Full observability — every node traversed, tool called, and decision made is recorded and queryable"
   - Point out: nodes_executed, tools_called, evidence_count, risk_level, final_status

**Closing section:**
- "What This Demonstrates" summary (5 bullet points mapping to system capabilities)
- "Running the Automated Version": `bash scripts/demo_phase6.sh`
- "Evaluation Metrics": link to docs/evaluation.md

**Language:** English (matching README convention). Curl payloads contain Chinese queries (matching demo data).
  </action>
  <verify>
    <automated>test -f docs/demo-walkthrough.md && grep -q "curl" docs/demo-walkthrough.md && grep -q "Talking point\|talking point\|Interview\|interview" docs/demo-walkthrough.md && grep -c "Scenario\|##.*[1-7]" docs/demo-walkthrough.md | awk '{if ($1 >= 7) print "OK: 7+ scenario sections"; else {print "FAIL"; exit 1}}' && grep -q "moca2024" docs/demo-walkthrough.md && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/demo-walkthrough.md` exists
    - Contains 7 demo scenarios with curl commands
    - Each scenario has an interview talking point
    - References demo accounts cs_zhang and mgr_li
    - Contains expected response highlights for each scenario
    - Links to docs/evaluation.md
    - Mentions `scripts/demo_phase6.sh` for automated execution
  </acceptance_criteria>
  <done>Demo walkthrough created with 7 annotated scenarios and interview talking points</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/evaluation.md, docs/architecture.md, docs/security-and-permission.md</name>
  <files>docs/evaluation.md, docs/architecture.md, docs/security-and-permission.md</files>
  <read_first>
    - .planning/phases/06-evaluation-polish/06-CONTEXT.md
    - .planning/phases/06-evaluation-polish/06-RESEARCH.md
    - rules/risk_rules.yaml
    - src/moca/agent/graph.py
    - src/moca/api/routes/auth.py
  </read_first>
  <action>
**Create `docs/evaluation.md` per D-05b:**

Sections:
1. **Overview** — Two-layer evaluation: RAG retrieval quality + Agent end-to-end behavior
2. **Why FakeLLM (Deterministic Evaluation)**
   - CI/demo default to FakeLLM — validates workflow determinism (graph routing, tool selection, approval logic), NOT model quality
   - FakeLLM produces structured outputs that drive actual LangGraph node transitions
   - This proves the scoring harness, golden set schema, and graph routing logic are correct
   - Live smoke test (`make eval-live`) validates real LLM integration — optional, local-only, not a Phase 6 pass/fail condition
   - `seed_demo.py --reset` guarantees deterministic business data for reproducible evaluation
3. **Golden Set Design**
   - RAG: 14 cases, 5 categories (refund_rule, sop, faq, boundary, fallback)
   - Agent: 35 cases, 10 categories (list all from D-01d)
   - Safety-critical categories require 100% pass rate
   - Matching rules: see `evaluation/golden/MATCHING_RULES.md` for Chinese normalization
4. **Metrics**
   - RAG: Hit@5 (chunk intersection with top-5 results), Fallback Accuracy
   - Agent: Intent Accuracy, Tool Selection Accuracy, Task Completion Rate, Citation Rate, Safety Critical Pass Rate, Average Latency, Token Cost
   - **CI mode:** latency and token cost report as `null`/`0` (not meaningful with FakeLLM)
   - **Live mode:** latency and token cost contain real measurements
5. **Thresholds (per D-03)**
   - Table: metric | threshold | rationale
   - RAG Hit@5 >= 85%, Agent Intent >= 90%, Tools >= 85%, Citation >= 85%, Safety == 100%
6. **Running Evaluations**
   - `make eval` — full evaluation (CI-compatible, uses FakeLLM)
   - `make eval-rag` — RAG only (requires DB with embedded chunks)
   - `make eval-agent` — Agent only (CI mode with FakeLLM)
   - `make eval-live` — Live mode (requires DASHSCOPE_API_KEY) — optional smoke test
   - `make eval-baseline` — Save current results as baseline
7. **Report Format**
   - JSON source of truth at `evaluation/reports/latest.json` — includes `run_mode` field (`ci_deterministic` or `live`)
   - Markdown rendered FROM JSON at `evaluation/reports/latest.md` — for human consumption only
   - Baseline comparison via `evaluation/reports/baseline.json` — missing baseline = warning, not failure
   - CI pass/fail determined ONLY by JSON report `overall_status` field and exit code
8. **CI Integration**
   - CI runs lint + unit tests only (per D-07b)
   - Eval scripts available locally; exit code 0/1 for pass/fail
   - No eval in CI because it requires DB with seeded data
   - Unit tests are pure (no DB/Redis dependency) — `--ignore=tests/integration` excludes DB-dependent tests
   - `make eval-live` — Live mode (requires DASHSCOPE_API_KEY)
   - `make eval-baseline` — Save current results as baseline
6. **CI Integration**
   - CI runs lint + unit tests only (per D-07b)
   - Eval scripts available locally; exit code 0/1 for pass/fail
   - No eval in CI because it requires DB with seeded data
7. **Report Format**
   - JSON source of truth at `evaluation/reports/latest.json`
   - Markdown rendered from JSON at `evaluation/reports/latest.md`
   - Baseline comparison via `evaluation/reports/baseline.json`

**Create `docs/architecture.md` per D-05c:**

Sections:
1. **System Overview** — Mermaid diagram (same as README system architecture)
2. **Technology Stack**
   - Table: layer | technology | purpose
   - API: FastAPI, Auth: JWT+OAuth2, Agent: LangGraph, LLM: GLM-4/DashScope, DB: PostgreSQL+pgvector, Cache: Redis, Frontend: Next.js, Embeddings: DashScope text-embedding-v3
3. **Agent Workflow**
   - Mermaid diagram (same as README agent workflow)
   - Node descriptions: what each of the 10 nodes does (1-2 sentences each)
   - Routing logic: route_after_risk and route_after_approval
4. **Data Flow**
   - Request lifecycle: HTTP → Auth → Agent → Tools/RAG → Risk → [Approval] → Response
   - Trace persistence: AgentRun + AgentStep records
5. **Key Design Decisions**
   - Deterministic risk assessment (rules, not LLM)
   - Citation validation (field matching, not LLM judge)
   - FakeLLM for CI isolation
   - Scoped checkpointer threads (tenant:user:thread)
   - No-evidence fallback (skip LLM when insufficient evidence)

**Create `docs/security-and-permission.md` per D-05d:**

Sections:
1. **Authentication** — JWT tokens via /api/v1/auth/token, OAuth2 password flow
2. **Role-Based Access Control**
   - Table: role | scopes | description
   - Roles: admin, support_agent, manager, merchant
   - Scopes: agent:chat, orders:read, refunds:read, knowledge:read, approvals:review
3. **Approval Workflow**
   - High-risk threshold from rules/risk_rules.yaml
   - LangGraph interrupt() mechanism
   - Approval API: who can approve (admin, manager), self-approval prevention
   - Resume via Command(resume=...)
4. **Risk Rules Configuration**
   - Reference rules/risk_rules.yaml
   - HR-01: compensation > 200 yuan
   - HR-02: refund override
   - HR-03: policy exception
   - LR-01: standard refund within policy
5. **Audit Trail**
   - Every agent run produces: AgentRun (run_id, input, output, status) + AgentStep (per-node trace)
   - Approval decisions recorded with approver, timestamp, reason
   - Trace queryable via GET /api/v1/traces/{run_id}
6. **Tenant Isolation**
   - Application-layer tenant_id filtering on all queries
   - Checkpointer thread scoping: tenant_id:user_id:thread_id
   - Cross-tenant access returns 404 (not 403, to prevent enumeration)

**All docs in English.** Technical but accessible. No marketing language. Include code snippets or config examples where they add clarity.
  </action>
  <verify>
    <automated>test -f docs/evaluation.md && test -f docs/architecture.md && test -f docs/security-and-permission.md && grep -q "Hit@5" docs/evaluation.md && grep -q "Golden Set" docs/evaluation.md && grep -q "make eval" docs/evaluation.md && grep -q "LangGraph" docs/architecture.md && grep -q "mermaid" docs/architecture.md && grep -q "risk_rules" docs/security-and-permission.md && grep -q "OAuth2\|JWT" docs/security-and-permission.md && grep -q "approval" docs/security-and-permission.md && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/evaluation.md` exists with sections: Overview, Golden Set Design, Metrics, Thresholds, Running Evaluations, CI Integration, Report Format
    - `docs/evaluation.md` contains threshold values: Hit@5 >= 85%, Intent >= 90%, Safety == 100%
    - `docs/architecture.md` exists with Mermaid diagrams and node descriptions
    - `docs/architecture.md` lists all 10 graph nodes with descriptions
    - `docs/security-and-permission.md` exists with RBAC table, risk rules, and audit trail sections
    - `docs/security-and-permission.md` references rules/risk_rules.yaml with HR-01, HR-02, HR-03
    - All files are in English
    - No placeholder text like "TODO" or "TBD"
  </acceptance_criteria>
  <done>Three documentation files created covering evaluation methodology, architecture, and security model</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/ → readers | Documentation is public; must not contain secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Information Disclosure | docs/*.md | mitigate | Documentation references only public demo credentials (moca2024), architecture patterns, and configuration file names. No API keys, connection strings, or internal URLs. |
| T-06-12 | Information Disclosure | docs/security-and-permission.md | accept | Security documentation describes the permission model publicly. This is standard for open-source projects and helps reviewers understand the design. No implementation vulnerabilities are exposed. |
</threat_model>

<verification>
1. All four docs/ files exist and contain substantive content
2. docs/demo-walkthrough.md has 7 scenarios with curl + talking points
3. docs/evaluation.md documents all metrics and thresholds from D-03
4. docs/architecture.md has Mermaid diagrams and node descriptions
5. docs/security-and-permission.md covers RBAC, approval, audit, risk rules
6. No secrets or real credentials in any documentation file
</verification>

<success_criteria>
- All four documentation files are complete with no placeholder content
- Demo walkthrough is followable by someone unfamiliar with the project
- Evaluation doc matches the actual eval script behavior from Plan 02
- Architecture doc accurately reflects the implemented system
- Security doc accurately reflects the implemented permission model
</success_criteria>

<output>
After completion, create `.planning/phases/06-evaluation-polish/06-04-SUMMARY.md`
</output>
