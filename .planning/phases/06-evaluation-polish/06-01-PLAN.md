---
phase: 06-evaluation-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - evaluation/golden/rag_cases.jsonl
  - evaluation/golden/agent_cases.jsonl
  - evaluation/golden/MATCHING_RULES.md
  - scripts/validate_golden_seeds.py
  - eval/golden_rag_queries.jsonl
  - evals/golden_set_phase3.json
autonomous: true
requirements: [EVAL-03, EVAL-04, EVAL-06]
must_haves:
  truths:
    - "RAG golden set exists at evaluation/golden/rag_cases.jsonl with 14 cases"
    - "Agent golden set exists at evaluation/golden/agent_cases.jsonl with 30-35 cases in JSONL format"
    - "Agent golden set covers all 10 categories from D-01d"
    - "Safety-critical categories (approval_required, permission_denied, approval_approved, approval_rejected) each have at least 4/4/3/3 cases"
    - "Each agent case has fields per D-01e: expected_intent, expected_tools, expected_status, expected_approval_required, expected_permission_result, expected_evidence_doc_keys, expected_response_contains, must_not_contain"
  artifacts:
    - path: "evaluation/golden/rag_cases.jsonl"
      provides: "RAG evaluation golden set"
      contains: "expected_chunk_ids"
    - path: "evaluation/golden/agent_cases.jsonl"
      provides: "Agent evaluation golden set with 10 categories"
      contains: "approval_required"
  key_links:
    - from: "evaluation/golden/agent_cases.jsonl"
      to: "scripts/seed_demo.py"
      via: "order_ids and case_ids reference seeded data"
      pattern: "ORD-"
---

<objective>
Migrate existing golden sets to `evaluation/golden/` and expand the agent golden set from 15 to 30-35 cases covering all 10 required categories.

Purpose: Establish the evaluation data foundation that eval scripts (Plan 02) will score against. The expanded golden set enables measurement of citation accuracy (EVAL-03), tool selection accuracy (EVAL-04), and task completion rate (EVAL-06).

Output: `evaluation/golden/rag_cases.jsonl` (migrated), `evaluation/golden/agent_cases.jsonl` (expanded to 35 cases in JSONL format)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-evaluation-polish/06-CONTEXT.md
@.planning/phases/06-evaluation-polish/06-RESEARCH.md
@.planning/phases/06-evaluation-polish/06-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate RAG golden set to evaluation/golden/</name>
  <files>evaluation/golden/rag_cases.jsonl</files>
  <read_first>
    - eval/golden_rag_queries.jsonl
  </read_first>
  <action>
Create directory `evaluation/golden/`. Copy `eval/golden_rag_queries.jsonl` to `evaluation/golden/rag_cases.jsonl` with no content changes. The file is 14 cases in JSONL format with fields: query, expected_doc_ids, expected_chunk_ids, category, difficulty, should_fallback. Verify the copy has the same line count as the original.
  </action>
  <verify>
    <automated>diff <(wc -l < eval/golden_rag_queries.jsonl) <(wc -l < evaluation/golden/rag_cases.jsonl) && python -c "import json; lines=[json.loads(l) for l in open('evaluation/golden/rag_cases.jsonl') if l.strip()]; assert len(lines)==14, f'Expected 14, got {len(lines)}'; print('OK: 14 RAG cases')"</automated>
  </verify>
  <acceptance_criteria>
    - `evaluation/golden/rag_cases.jsonl` exists with exactly 14 lines of valid JSON
    - Each line contains keys: query, expected_doc_ids, expected_chunk_ids, category
    - Content is byte-identical to `eval/golden_rag_queries.jsonl`
  </acceptance_criteria>
  <done>RAG golden set migrated to new path with zero content changes</done>
</task>

<task type="auto">
  <name>Task 2: Expand agent golden set to 35 cases across 10 categories</name>
  <files>evaluation/golden/agent_cases.jsonl</files>
  <read_first>
    - evals/golden_set_phase3.json
    - rules/risk_rules.yaml
    - scripts/seed_demo.py
  </read_first>
  <action>
Convert `evals/golden_set_phase3.json` (JSON array, 15 cases) to JSONL format and expand to 35 cases at `evaluation/golden/agent_cases.jsonl`.

**Existing 15 cases — migrate with these changes:**
- Rename category `policy_qa` → `normal_policy_qa`
- Rename category `insufficient_evidence` → `low_confidence_no_evidence` (for out-of-domain) or `missing_context` (for missing business data)
- Add new fields with appropriate defaults: `expected_approval_required` (false for existing non-approval cases), `expected_permission_result` ("granted"), `expected_evidence_doc_keys` ([] if not applicable), `expected_response_contains` ([]), `must_not_contain` ([])

**Add 20 new cases (IDs GS-16 through GS-35) across these categories:**

| Category | Cases to add | IDs |
|----------|-------------|-----|
| normal_policy_qa | 1 more (total 4) | GS-16 |
| refund_troubleshooting | 1 more (total 4) | GS-17 |
| compensation_suggestion | 2 more (total 4) | GS-18, GS-19 |
| approval_required | 4 new | GS-20, GS-21, GS-22, GS-23 |
| permission_denied | 4 new | GS-24, GS-25, GS-26, GS-27 |
| approval_approved | 3 new | GS-28, GS-29, GS-30 |
| approval_rejected | 3 new | GS-31, GS-32, GS-33 |
| tool_failure_or_not_found | 3 new (includes 1 from cross_turn rename) | GS-34, GS-35 |

**Category-specific field values:**

`approval_required` cases:
- expected_intent: "compensation_suggestion"
- expected_tools: ["get_order"] or ["get_order", "get_refund_case"]
- expected_status: "completed"
- expected_approval_required: true
- expected_permission_result: "granted"
- Queries reference HR-01 (compensation > 200), HR-02 (refund override), HR-03 (policy exception) from risk_rules.yaml
- expected_response_contains: ["审批"] or ["approval"]
- must_not_contain: ["已执行", "已发放"]

`permission_denied` cases:
- expected_intent: varies
- expected_tools: []
- expected_status: "error"
- expected_approval_required: false
- expected_permission_result: "denied_scope" or "denied_role"
- Scenarios: missing agent:chat scope, wrong role for approval, self-approval attempt, expired token
- expected_response_contains: ["权限不足"] or ["403"]
- must_not_contain: ["成功"]

`approval_approved` cases:
- expected_intent: "compensation_suggestion"
- expected_tools: ["get_order", "create_coupon_grant_draft"]
- expected_status: "completed"
- expected_approval_required: true
- expected_permission_result: "granted"
- expected_response_contains: ["已批准"] or ["补偿"]
- must_not_contain: ["驳回"]

`approval_rejected` cases:
- expected_intent: "compensation_suggestion"
- expected_tools: ["get_order"]
- expected_status: "completed"
- expected_approval_required: true
- expected_permission_result: "granted"
- expected_response_contains: ["驳回"] or ["拒绝"]
- must_not_contain: ["已执行", "已发放"]

`tool_failure_or_not_found` cases:
- expected_intent: "refund_troubleshooting" or "compensation_suggestion"
- expected_tools: ["get_order"]
- expected_status: "error" or "completed"
- expected_approval_required: false
- Queries reference non-existent order IDs (e.g., "ORD-NONEXIST-001")
- expected_response_contains: ["未找到"] or ["不存在"]
- must_not_contain: ["退款规则"]

**All queries must be in Chinese.** Use realistic merchant support scenarios. Reference order IDs that exist in `scripts/seed_demo.py` output for valid cases, and deliberately non-existent IDs for failure cases.

Each line is one JSON object. No trailing comma. UTF-8 encoding.
  </action>
  <verify>
    <automated>python -c "
import json, sys
lines = [json.loads(l) for l in open('evaluation/golden/agent_cases.jsonl', encoding='utf-8') if l.strip()]
assert len(lines) >= 30, f'Expected >=30 cases, got {len(lines)}'
cats = {}
required_fields = ['id','category','query','expected_intent','expected_tools_called','expected_approval_required','expected_permission_result']
for c in lines:
    for f in required_fields:
        assert f in c, f'Case {c.get(\"id\")} missing field {f}'
    cats.setdefault(c['category'], []).append(c['id'])
expected_cats = ['normal_policy_qa','refund_troubleshooting','compensation_suggestion','approval_required','permission_denied','approval_approved','approval_rejected','missing_context','low_confidence_no_evidence','tool_failure_or_not_found']
for ec in expected_cats:
    assert ec in cats, f'Missing category: {ec}'
safety = ['approval_required','permission_denied','approval_approved','approval_rejected']
for sc in safety:
    assert len(cats[sc]) >= 3, f'{sc} has only {len(cats[sc])} cases, need >=3'
print(f'OK: {len(lines)} cases across {len(cats)} categories')
for k,v in sorted(cats.items()):
    print(f'  {k}: {len(v)}')
" && uv run python scripts/validate_golden_seeds.py && echo "SEED CROSS-VALIDATION PASSED"
    </automated>
  </verify>
  <acceptance_criteria>
    - `evaluation/golden/agent_cases.jsonl` contains 30-35 lines of valid JSON
    - All 10 categories from D-01d are present
    - Each case has fields: id, category, query, expected_intent, expected_tools_called, expected_approval_required, expected_permission_result, expected_evidence_doc_keys, expected_response_contains, must_not_contain
    - approval_required category has >= 4 cases
    - permission_denied category has >= 4 cases
    - approval_approved category has >= 3 cases
    - approval_rejected category has >= 3 cases
    - All queries are in Chinese
    - File is valid JSONL (one JSON object per line)
  </acceptance_criteria>
  <done>Agent golden set expanded to 35 cases covering all 10 categories with full D-01e field schema</done>
</task>

<task type="auto">
  <name>Task 3: Validate golden set seed ID references</name>
  <files>evaluation/golden/agent_cases.jsonl</files>
  <read_first>
    - scripts/seed_demo.py
    - evaluation/golden/agent_cases.jsonl
  </read_first>
  <action>
Create a validation script `scripts/validate_golden_seeds.py` that:

1. **Extracts all deterministic seed IDs from `scripts/seed_demo.py`** by parsing the script for:
   - Demo users: `cs_zhang`, `mgr_li`, `admin_wang` (usernames + roles + scopes)
   - Demo orders: all `ORD-*` IDs generated by the seed script
   - Refund cases: all `RFC-*` IDs
   - Tickets: all `TKT-*` IDs
   - Policy documents: all `doc_id` values (e.g., `DOC-REFUND-001`)
   - Policy chunks: all `chunk_id` values referenced in RAG golden set

2. **Reads `evaluation/golden/agent_cases.jsonl`** and extracts all referenced IDs from:
   - `query` field (regex for `ORD-\w+`, `RFC-\w+`, `TKT-\w+`)
   - `expected_evidence_doc_keys` field

3. **Cross-validates:**
   - Every `ORD-*` / `RFC-*` / `TKT-*` in golden set queries (for non-failure cases) MUST exist in seed output
   - Every `doc_key` in `expected_evidence_doc_keys` MUST exist in seed policy documents
   - Cases with category `tool_failure_or_not_found` are EXEMPT (they deliberately use non-existent IDs)

4. **Fail fast with clear error:**
   ```
   SEED VALIDATION FAILED:
   - GS-17 references ORD-NONEXIST-001 but category is refund_troubleshooting (not tool_failure)
   - GS-28 references DOC-MISSING-001 in expected_evidence_doc_keys but doc not in seed
   
   Fix: Update golden set IDs to match scripts/seed_demo.py output, or run: make seed
   ```

5. **Exit 0 if all valid, exit 1 if any mismatch.**

The script is run as part of Task 2's verify step AND independently via `uv run python scripts/validate_golden_seeds.py`.

**Deterministic seed IDs that MUST be documented in the script header:**
- Users: cs_zhang (support_agent), mgr_li (manager), admin_wang (admin)
- Password: moca2024 (all demo accounts)
- Orders: read from seed_demo.py `ORDER_IDS` list or equivalent
- Refund cases: read from seed_demo.py
- Policy docs: read from knowledge/ directory filenames or seed ingestion output
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('scripts/validate_golden_seeds.py').read()); print('syntax OK')" && uv run python scripts/validate_golden_seeds.py && echo "SEED VALIDATION PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/validate_golden_seeds.py` exists and parses without syntax errors
    - Script extracts seed IDs from seed_demo.py
    - Script cross-validates golden set references against seed IDs
    - tool_failure_or_not_found cases are exempt from ID existence checks
    - Exit 0 when all references valid, exit 1 with specific error messages when invalid
    - Error messages name the case ID, the invalid reference, and the expected fix
  </acceptance_criteria>
  <done>Seed ID validation script ensures golden set never silently references non-existent data</done>
</task>

<task type="auto">
  <name>Task 4: Document Chinese normalization and matching rules</name>
  <files>evaluation/golden/MATCHING_RULES.md</files>
  <read_first>
    - evaluation/golden/agent_cases.jsonl
    - src/moca/agent/nodes/classify_intent.py
  </read_first>
  <action>
Create `evaluation/golden/MATCHING_RULES.md` documenting the Chinese query normalization and matching rules used by the golden set and eval scripts:

```markdown
# Golden Set Matching Rules

## Purpose

Defines how Chinese queries in the golden set map to agent intents and routing paths.
These rules serve both FakeLLM deterministic routing and eval case scoring.

## Intent Routing Keywords (Chinese → Intent)

| Intent | Trigger Keywords (any match) | Examples |
|--------|------------------------------|----------|
| normal_policy_qa | 规则, 政策, 规定, 标准, 流程, 怎么办, 什么情况 | "退款超时规则是什么？" |
| refund_troubleshooting | 退款, 退货, 进度, 卡在, 状态, 为什么没退 | "订单ORD-xxx退款进度如何？" |
| compensation_suggestion | 补偿, 赔付, 赔偿, 补贴, 给.*元, 要求.*块 | "客户要求补偿600元" |

## Risk Level Keywords (Chinese → Risk Assessment)

| Risk Level | Trigger Patterns | Threshold |
|------------|-----------------|-----------|
| high | 补偿/赔付/赔偿 + 金额 > 200 | HR-01: amount > 200 yuan |
| high | 退款.*覆盖/override/超出政策 | HR-02: refund override |
| high | 例外/特批/政策豁免 | HR-03: policy exception |
| low | 补偿/赔付 + 金额 <= 200 | LR-01: within policy |

## Approval Decision Keywords

| Decision | Trigger Keywords |
|----------|-----------------|
| approval_required | 审批, 需要批准, 等待审核 |
| approval_approved | 已批准, 批准通过, 同意补偿 |
| approval_rejected | 驳回, 拒绝, 不同意, 主管不同意, 审批被驳回 |
| permission_denied | 权限不足, 无权, 403, 没有权限 |

## Amount Extraction Normalization

Chinese amount expressions that should all resolve to the same numeric value:
- "补偿600元" → 600
- "赔付600" → 600
- "给用户600块" → 600

The eval script uses regex: `(\d+)\s*(元|块|¥)?` to extract amounts from queries.
Chinese numerals (一二三...百千万) are out of scope for Phase 6 — all golden set cases use Arabic numerals only.

## Response Content Matching

`expected_response_contains` and `must_not_contain` use **substring matching** with these rules:
- Case-insensitive for ASCII characters
- Exact match for Chinese characters (no synonym expansion)
- Whitespace-normalized (consecutive whitespace collapsed to single space)
- Matching is applied to the `final_response` text field only

## FakeLLM Deterministic Routing Contract

In CI mode, FakeLLM responses are constructed to trigger specific graph routing paths:
- Intent classification node: returns `expected_intent` from the case
- Risk assessment node: returns `approval_required` based on amount threshold rules above
- Tool selection: returns tool calls matching `expected_tools_called`
- Final response: includes strings from `expected_response_contains`

This means CI mode validates the scoring harness + golden set schema + graph routing logic,
NOT the actual LLM's language understanding ability.
```
  </action>
  <verify>
    <automated>test -f evaluation/golden/MATCHING_RULES.md && grep -q "Intent Routing Keywords" evaluation/golden/MATCHING_RULES.md && grep -q "FakeLLM Deterministic Routing" evaluation/golden/MATCHING_RULES.md && grep -q "Amount Extraction" evaluation/golden/MATCHING_RULES.md && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - `evaluation/golden/MATCHING_RULES.md` exists
    - Contains intent routing keyword table (Chinese → intent mapping)
    - Contains risk level keyword table with HR-01/02/03 thresholds
    - Contains approval decision keyword mapping
    - Contains amount extraction normalization rules
    - Contains response content matching rules (substring, case rules)
    - Contains FakeLLM deterministic routing contract explanation
  </acceptance_criteria>
  <done>Chinese normalization and matching rules documented for golden set and eval scripts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| golden set → eval scripts | Golden set data is trusted input to scoring; corruption would produce false pass/fail |
| seed_demo.py → golden set | Golden set references seed data IDs; mismatch causes eval failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | evaluation/golden/*.jsonl | mitigate | Golden sets are committed to git; any change is visible in diff. Eval scripts validate JSON schema before scoring. |
| T-06-02 | Information Disclosure | evaluation/golden/agent_cases.jsonl | accept | Golden set contains only synthetic Chinese queries and expected field values; no real PII or credentials. |
</threat_model>

<verification>
1. `evaluation/golden/rag_cases.jsonl` has 14 valid JSONL lines
2. `evaluation/golden/agent_cases.jsonl` has 30-35 valid JSONL lines
3. All 10 categories present in agent golden set
4. Safety-critical categories have minimum case counts
5. All required fields present on every case
</verification>

<success_criteria>
- Golden set data files exist at new paths and pass schema validation
- Agent golden set covers all 10 categories with appropriate case counts
- All case IDs are unique and sequential
- Queries reference valid seed data IDs (for success cases) or deliberately invalid IDs (for failure cases)
</success_criteria>

<output>
After completion, create `.planning/phases/06-evaluation-polish/06-01-SUMMARY.md`
</output>
