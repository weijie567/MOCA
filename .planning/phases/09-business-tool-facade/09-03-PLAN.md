---
phase: 09-business-tool-facade
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/business_tools/adapters.py
  - tests/business_tools/test_adapters.py
autonomous: true
requirements: [TOOL-02]
must_haves:
  truths:
    - "Existing get_order.py, get_refund_case.py, get_ticket.py, and authz.py are imported in place and never relocated."
    - "Every raw status/error code maps deterministically to ToolResultV2 and every result sets required latency_ms."
    - "Invalid-response paths discard raw payloads; business reads populate business_fact_refs and leave policy_evidence_refs empty."
    - "get_logistics and get_merchant_risk are register-but-unavailable."
  artifacts:
    - path: "src/business_tools/adapters.py"
      provides: "Raw business-tool to ToolResultV2 adapters and unavailable adapters"
      contains: "business_fact_refs"
    - path: "tests/business_tools/test_adapters.py"
      provides: "Status mapping, provenance, unavailable, and no-raw-leak tests"
      contains: "invalid_response"
  key_links:
    - from: "src/business_tools/adapters.py"
      to: "src/agent/tools/get_order.py"
      via: "import-in-place raw read function"
      pattern: "from src.agent.tools.get_order import get_order"
    - from: "src/business_tools/adapters.py"
      to: "src/business_tools/schemas.py"
      via: "ToolResultV2 and BusinessFactRefV1 construction"
      pattern: "BusinessFactRefV1"
---

<objective>
Normalize existing raw business reads into safe typed ToolResultV2 envelopes, with deterministic status mapping and business provenance.

Purpose: satisfy TOOL-02's no-raw-invalid-payload guarantee while preserving the proven tenant-scoped raw tools in place.
Output: adapter module and mapping/no-leak tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-business-tool-facade/09-CONTEXT.md
@.planning/phases/09-business-tool-facade/09-RESEARCH.md
@.planning/phases/09-business-tool-facade/09-PATTERNS.md

Source-of-truth spec:
- docs/contract-spec.md §8.0 — scope/no-widening semantics.
- docs/contract-spec.md §12.5 — ToolResultV2 status, provenance, invalid-response, and no-leak rules.
- docs/contract-spec.md §12.6 — descriptor/adapter validation seam.

Cross-cutting carries: SCF-1 context fields are trusted; SCF-2 canonical caller naming is consumed by registry, not adapters; SCF-3 write execution is absent here; SCF-4 all results use `audit_ref=None`.

<interfaces>
<!-- Raw result envelope and error codes. Source: src/agent/tools/get_order.py:12-26,29-92 [VERIFIED] -->
Raw business reads return `{"status","data","error"}`; order emits VALIDATION_ERROR, ORDER_NOT_FOUND, FORBIDDEN, DB_TIMEOUT, DB_ERROR and enforces merchant access before returning data.

<!-- Existing adapter forwarding/input schemas. Source: src/agent/tools/adapters.py:14-51 [VERIFIED] -->
Reuse the three business input schemas/signature pattern; do not re-own `SearchPolicyInput`/`search_policy_adapter` at lines 26-31 and 54-65.

<!-- Authorization helper remains in place. Source: src/agent/tools/authz.py:11-36 [VERIFIED] -->
`merchant_can_access` is the existing per-resource tenant/merchant authorization helper; import/reuse it through the raw tools, do not relocate it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement deterministic raw-to-ToolResultV2 adapters</name>
  <files>src/business_tools/adapters.py</files>
  <read_first>
    - src/agent/tools/adapters.py:14-51 (business input schemas and forwarding analog)
    - src/agent/tools/get_order.py:12-92, src/agent/tools/get_refund_case.py, src/agent/tools/get_ticket.py (raw shapes/codes)
    - src/agent/tools/authz.py:11-36 (existing scope enforcement; reuse in place)
    - .planning/phases/09-business-tool-facade/09-RESEARCH.md §3.9 and §6
    - docs/contract-spec.md §12.5
  </read_first>
  <behavior>
    - success→success; FORBIDDEN→permission_denied/source caller; *_NOT_FOUND→not_found/source upstream; DB_TIMEOUT→timeout/source adapter/retryable true; VALIDATION_ERROR→invalid_request/source caller; output validation failure→invalid_response/source adapter; DB_ERROR/other→error.
    - Success returns only projected summary/data fields and one BusinessFactRefV1 with tenant/resource/source/retrieval metadata.
    - Invalid response returns data=None and never carries the raw payload.
    - Every result, including unavailable/error, sets latency_ms to measured elapsed milliseconds or 0.
  </behavior>
  <action>
Create `src/business_tools/adapters.py`. Import `get_order`, `get_refund_case`, and `get_ticket` from their existing `src.agent.tools.*` files; do not move or duplicate those raw functions or `authz.py`. Define/reuse `GetOrderInput`, `GetRefundCaseInput`, and `GetTicketInput` and wrappers for each business read.

Use the exact RESEARCH §3.9 mapping:

| Raw error_code | status | ToolError.source | retryable |
|---|---|---|---|
| success | success | none | false |
| FORBIDDEN | permission_denied | caller | false |
| ORDER_NOT_FOUND / REFUND_CASE_NOT_FOUND / TICKET_NOT_FOUND | not_found | upstream | false |
| DB_TIMEOUT | timeout | adapter | true |
| VALIDATION_ERROR | invalid_request | caller | false |
| output-schema validation failure | invalid_response | adapter | false |
| DB_ERROR / other | error | adapter or upstream | raw retryable value |

For success, construct safe projected `data`/`summary`, `source_system` (`"demo_orders_db"`, `"demo_refund_cases_db"`, or `"demo_tickets_db"`), `policy_evidence_refs=[]`, and one `BusinessFactRefV1(resource_type=..., resource_id=..., tenant_id=ctx.tenant_id, source_system=..., retrieved_at=now, data_freshness_at=None where unavailable)`. For all outcomes set `audit_ref=None` and required integer `latency_ms`. For invalid_response explicitly use `data=None` and a safe message with no repr/stringification of raw data. Implement `get_logistics_adapter` and `get_merchant_risk_adapter` returning `status="unavailable"`, `data=None`, empty refs, `latency_ms=0`.
  </action>
  <verify>
    <automated>python -m py_compile src/business_tools/adapters.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'from src.agent.tools.get_(order|refund_case|ticket) import' src/business_tools/adapters.py` returns all three import-in-place lines.
    - `rg -n 'permission_denied|not_found|timeout|invalid_request|invalid_response|unavailable' src/business_tools/adapters.py` returns all mapped statuses.
    - `rg -n 'business_fact_refs|policy_evidence_refs|latency_ms' src/business_tools/adapters.py` returns all required envelope/provenance fields.
    - `rg -n 'status="unavailable"|status = "unavailable"' src/business_tools/adapters.py` returns unavailable adapter results.
    - `python -m py_compile src/business_tools/adapters.py` exits 0.
  </acceptance_criteria>
  <done>All business raw outcomes map to typed safe envelopes, provenance is correct, and unavailable tools are explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Add mapping, provenance, and no-raw-leak tests</name>
  <files>tests/business_tools/test_adapters.py</files>
  <read_first>
    - tests/agent/test_tools/test_tool_adapters.py:30-39 (raw-tool monkeypatch analog)
    - tests/knowledge/test_facade_status.py:62-119 (parametrized mapping/timeout analog)
    - tests/agent/test_tools/test_registry.py:288 (no-raw-text assertion analog)
    - src/business_tools/adapters.py (Task 1 output)
    - .planning/phases/09-business-tool-facade/09-RESEARCH.md §6
  </read_first>
  <behavior>
    - Parametrized raw-code mapping asserts expected status and ToolError.source.
    - Invalid_response serialization does not contain a unique raw upstream sentinel.
    - Successful reads populate the correct BusinessFactRefV1 resource_type and leave policy_evidence_refs empty.
    - Logistics and merchant risk return unavailable; all outcomes expose integer latency_ms.
  </behavior>
  <action>
Create at least five tests. Monkeypatch raw functions with `AsyncMock` returning concrete raw envelopes. Parametrize `(FORBIDDEN, ORDER_NOT_FOUND, DB_TIMEOUT, VALIDATION_ERROR, DB_ERROR)` with expected status/source. For no-leak, return an invalid success payload containing sentinel `RAW-UPSTREAM-SECRET-09` and assert both `result.data is None` and `"RAW-UPSTREAM-SECRET-09" not in str(result.model_dump())`. Test a valid order success produces resource_type `"order"`, the requested resource_id, `ctx.tenant_id`, nonempty business_fact_refs, and `policy_evidence_refs == []`. Test unavailable adapters and assert `isinstance(result.latency_ms, int)` throughout.
  </action>
  <verify>
    <automated>pytest tests/business_tools/test_adapters.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c 'def test_' tests/business_tools/test_adapters.py` returns at least 5.
    - `rg -n 'RAW-UPSTREAM-SECRET-09.*not in|not in.*RAW-UPSTREAM-SECRET-09' tests/business_tools/test_adapters.py` returns the serialized no-leak assertion.
    - `rg -n 'business_fact_refs|policy_evidence_refs' tests/business_tools/test_adapters.py` returns provenance assertions.
    - `pytest tests/business_tools/test_adapters.py -x -q` exits 0.
  </acceptance_criteria>
  <done>Tests prove deterministic mapping, required latency, business provenance, unavailable outcomes, and no raw invalid payload leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upstream demo DB/raw tool → adapter | Raw dictionaries and unsafe/invalid payloads enter the typed facade boundary. |
| Adapter → graph/facade consumer | Only validated projected data, safe summaries, statuses, and provenance may cross. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Information Disclosure | invalid-response adapter path | mitigate | Set data=None, omit raw values from safe error/summary, and assert serialized result excludes sentinel. |
| T-09-05A | Tampering / Elevation of Privilege | business provenance | mitigate | Build BusinessFactRefV1 from trusted context/resource mapping and never emit policy EvidenceRefV1. |
</threat_model>

<verification>
- `pytest tests/business_tools/test_adapters.py -x -q` is green.
- Existing raw tools/authz remain in their original paths and are imported in place.
- Every constructed ToolResultV2 sets latency_ms and audit_ref=None.
</verification>

<success_criteria>
- TOOL-02 outcomes map correctly, invalid payloads cannot escape, and business provenance is populated.
- Logistics/merchant-risk absence is explicit rather than silently omitted.
- Search-policy ownership remains outside this adapter module.
</success_criteria>

<output>
After completion, create `.planning/phases/09-business-tool-facade/09-03-SUMMARY.md`.
</output>
