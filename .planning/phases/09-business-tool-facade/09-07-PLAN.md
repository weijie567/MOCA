---
phase: 09-business-tool-facade
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/nodes/retrieve_policy_evidence.py
  - tests/agent/test_nodes/test_retrieve_policy_evidence.py
  - tests/agent/test_graph.py
autonomous: true
requirements: [TOOL-02]
gap_closure: true
must_haves:
  truths:
    - "Structured trusted merchant_scope is projected compatibly into KnowledgeContext without being dropped or widened."
    - "A merchant_ids list from trusted config reaches live PolicyKnowledgeService.search unchanged."
    - "Malformed or missing structured merchant scope fails closed instead of becoming unrestricted None."
  artifacts:
    - path: "src/agent/nodes/retrieve_policy_evidence.py"
      provides: "Compatible structured merchant-scope projection for KnowledgeContext"
      contains: "merchant_ids"
    - path: "tests/agent/test_nodes/test_retrieve_policy_evidence.py"
      provides: "Focused scope preservation and fail-closed regressions"
      contains: "merchant_scope"
    - path: "tests/agent/test_graph.py"
      provides: "Graph-path assertion that structured scope reaches KnowledgeContext"
      contains: "knowledge_search"
  key_links:
    - from: "configurable.merchant_scope.merchant_ids"
      to: "KnowledgeContext.merchant_scope"
      via: "explicit compatible projection in retrieve_policy_evidence"
      pattern: "merchant_ids"
    - from: "src/agent/nodes/retrieve_policy_evidence.py"
      to: "src/knowledge/service.py"
      via: "PolicyKnowledgeService.search(request, context)"
      pattern: "PolicyKnowledgeService"
---

<objective>
Preserve structured merchant authorization scope across the live policy-retrieval path without changing Phase 8 PolicyKnowledgeService ownership.

Purpose: prevent trusted merchant scope from being silently dropped or widened when constructing KnowledgeContext.
Output: compatible projection helper and focused plus graph-level propagation tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-business-tool-facade/09-CONTEXT.md
@.planning/phases/09-business-tool-facade/09-RESEARCH.md
@.planning/phases/09-business-tool-facade/09-VERIFICATION.md
@.planning/phases/09-business-tool-facade/09-REVIEW.md
@.planning/phases/09-business-tool-facade/09-05-SUMMARY.md

<interfaces>
Router-owned trusted shape: `merchant_scope: {"merchant_ids": list[str], ...}`.
Phase 8 consumer shape: `KnowledgeContext.merchant_scope: list[str] | None`.

Add a small pure projection helper in `retrieve_policy_evidence.py`, for example:
`def _knowledge_merchant_scope(value: object) -> list[str]:`
It returns a copied validated string list from structured `merchant_ids` or a legacy list; missing/malformed values return `[]` to fail closed. It must never return `None` for an invalid trusted projection.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project structured merchant scope into KnowledgeContext without widening</name>
  <files>src/agent/nodes/retrieve_policy_evidence.py, tests/agent/test_nodes/test_retrieve_policy_evidence.py</files>
  <read_first>
    - src/agent/nodes/retrieve_policy_evidence.py
    - src/knowledge/schemas.py
    - src/knowledge/service.py
    - tests/agent/test_nodes/test_retrieve_policy_evidence.py
    - tests/knowledge/test_tenant_scope.py
    - .planning/ROADMAP.md (Phase 8/9 facade ownership)
    - .planning/REQUIREMENTS.md (TOOL-02 scope contract)
    - .planning/phases/09-business-tool-facade/09-CONTEXT.md (policy ownership lock)
    - .planning/phases/09-business-tool-facade/09-RESEARCH.md (structured MerchantScopeV1 projection constraints)
    - .planning/phases/09-business-tool-facade/09-PATTERNS.md (scope-check and test patterns)
    - .planning/phases/09-business-tool-facade/09-VERIFICATION.md (merchant-scope gap)
    - .planning/phases/09-business-tool-facade/09-REVIEW.md (WR-01)
  </read_first>
  <behavior>
    - `{"merchant_ids": ["merchant-1"]}` becomes `KnowledgeContext.merchant_scope == ["merchant-1"]`.
    - Legacy list projection remains compatible.
    - Missing, malformed, non-string, or empty structured merchant IDs become `[]`, not unrestricted `None`.
    - Other structured dimensions are not misinterpreted as merchant IDs.
  </behavior>
  <action>
Replace the current `isinstance(merchant_scope, list) else None` branch with a pure compatible projection helper. For a dict, read only `merchant_ids`; for a legacy list, validate it directly. Require every ID to be a non-empty string, return a copied list, and fail closed with `[]` for absent/malformed values. Do not infer merchant IDs from active slots, user text, categories, or risk levels. Keep `PolicyKnowledgeService(LegacyRagKnowledgeAdapter(session)).search(request, context)` unchanged because policy knowledge remains Phase 8-owned. Add focused tests that capture the `KnowledgeContext` passed to `PolicyKnowledgeService.search` for structured, legacy, and malformed scope inputs.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_retrieve_policy_evidence.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'merchant_ids|_knowledge_merchant_scope' src/agent/nodes/retrieve_policy_evidence.py` returns the explicit projection helper and use.
    - `rg -n 'merchant_scope = None' src/agent/nodes/retrieve_policy_evidence.py` returns zero matches.
    - `rg -n 'merchant_scope.*\\[\\]|structured|malformed' tests/agent/test_nodes/test_retrieve_policy_evidence.py` returns fail-closed and structured-scope assertions.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_retrieve_policy_evidence.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>Live policy retrieval preserves structured merchant IDs and fails closed on incompatible scope shapes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove structured merchant scope reaches KnowledgeContext across the graph path</name>
  <files>tests/agent/test_graph.py</files>
  <read_first>
    - tests/agent/test_graph.py
    - src/agent/nodes/retrieve_policy_evidence.py
    - src/knowledge/schemas.py
    - .planning/ROADMAP.md (authoritative Phase 9 goal)
    - .planning/REQUIREMENTS.md (TOOL-02 scope contract)
    - .planning/phases/09-business-tool-facade/09-CONTEXT.md (Phase 8 policy ownership lock)
    - .planning/phases/09-business-tool-facade/09-RESEARCH.md (scope projection and policy ownership)
    - .planning/phases/09-business-tool-facade/09-PATTERNS.md (graph test seam)
    - .planning/phases/09-business-tool-facade/09-VERIFICATION.md (required graph/router integration proof)
    - .planning/phases/09-business-tool-facade/09-REVIEW.md (WR-01)
  </read_first>
  <behavior>
    - A graph invocation configured with `{"merchant_ids": ["merchant-graph"]}` calls `PolicyKnowledgeService.search` with `KnowledgeContext.merchant_scope == ["merchant-graph"]`.
    - The graph continues using PolicyKnowledgeService for retrieval and BusinessToolService for business reads.
  </behavior>
  <action>
Extend the existing graph fake-dependency seam so the policy-search AsyncMock call arguments can be inspected after invocation. Add a graph test using structured configurable scope `{"merchant_ids": ["merchant-graph"]}` and assert the second argument passed to `knowledge_search` is a `KnowledgeContext` carrying exactly `["merchant-graph"]`. Keep the existing service-boundary monkeypatches: business reads at BusinessToolService and policy retrieval at PolicyKnowledgeService. Do not route policy retrieval through BusinessToolService.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_graph.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'merchant-graph|KnowledgeContext|merchant_scope' tests/agent/test_graph.py` returns the graph-path propagation assertion.
    - `rg -n 'PolicyKnowledgeService.*search|knowledge_search' tests/agent/test_graph.py` confirms the Phase 8-owned retrieval seam remains tested.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_graph.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>The graph path proves structured merchant ID scope reaches KnowledgeContext unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Trusted run config → retrieval node | Structured merchant authorization scope crosses into a compatibility projection. |
| Retrieval node → PolicyKnowledgeService | Projected merchant IDs become KnowledgeContext authorization input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07-01 | Elevation of Privilege | Merchant-scope projection | mitigate | Invalid/missing structured scope maps to `[]`, never unrestricted `None`; focused tests cover malformed inputs. |
| T-09-07-02 | Information Disclosure | Merchant-scope dropping/widening | mitigate | Extract only validated `merchant_ids` and graph-test exact propagation to KnowledgeContext. |
| T-09-07-03 | Tampering | User/LLM-provided merchant IDs | mitigate | Read scope only from trusted configurable input; never derive it from slots or request text. |
| T-09-07-04 | Elevation of Privilege / confused deputy | Token/role scope intersection | accept | Plan 09-06 closes and API-tests this separate upstream authorization issue. |
| T-09-07-05 | Elevation of Privilege | Policy retrieval ownership boundary | mitigate | Preserve direct PolicyKnowledgeService call and assert it in graph tests; no BusinessToolService policy adapter is introduced. |
</threat_model>

<verification>
Run focused tests after each task, then run the full Phase 9 regression command:
`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/business_tools/ tests/agent/test_nodes/test_load_business_context.py tests/agent/test_nodes/test_retrieve_policy_evidence.py tests/agent/test_graph.py tests/test_agent_runs_api.py tests/integration/test_auth.py -q --tb=short`
</verification>

<success_criteria>
- Structured `merchant_scope.merchant_ids` reaches KnowledgeContext exactly.
- Missing or malformed scope fails closed with an empty list and cannot widen to unrestricted `None`.
- Policy retrieval remains owned and executed by Phase 8 PolicyKnowledgeService.
- The full Phase 9 regression command passes.
</success_criteria>

<output>
After completion, create `.planning/phases/09-business-tool-facade/09-07-SUMMARY.md`.
</output>
