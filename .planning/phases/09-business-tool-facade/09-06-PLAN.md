---
phase: 09-business-tool-facade
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/auth/permissions.py
  - src/api/routers/agent_runs.py
  - tests/integration/test_auth.py
  - tests/test_agent_runs_api.py
autonomous: true
requirements: [TOOL-01, TOOL-02]
gap_closure: true
must_haves:
  truths:
    - "Verified JWT scopes survive authentication and remain available only in trusted request context."
    - "Tool permissions are derived from the intersection of verified token scopes and the current database role allowlist."
    - "A support-role token containing only agent:chat receives no business or retrieval tool permissions."
  artifacts:
    - path: "src/auth/permissions.py"
      provides: "Authenticated request-state projection of verified JWT scopes"
      contains: "verified_token_scopes"
    - path: "src/api/routers/agent_runs.py"
      provides: "Role/token scope intersection before tool-permission mapping"
      contains: "SCOPE_TO_TOOL_PERMISSION"
    - path: "tests/test_agent_runs_api.py"
      provides: "Live SSE router regression proving restricted-token no-widening"
      contains: "agent:chat"
  key_links:
    - from: "src/auth/permissions.py"
      to: "src/api/routers/agent_runs.py"
      via: "request.state.verified_token_scopes"
      pattern: "verified_token_scopes"
    - from: "src/api/routers/agent_runs.py"
      to: "configurable.permissions"
      via: "token-scope and ROLE_SCOPES intersection mapped through SCOPE_TO_TOOL_PERMISSION"
      pattern: "SCOPE_TO_TOOL_PERMISSION"
---

<objective>
Close the verified-token confused-deputy gap by preserving authenticated JWT scopes and intersecting them with current database-role scopes before granting tool permissions.

Purpose: make the live ToolCallContext authorization projection faithful to both token least privilege and current role authorization.
Output: trusted request-scope preservation, intersection-based router projection, and API-level no-widening regressions.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-business-tool-facade/09-CONTEXT.md
@.planning/phases/09-business-tool-facade/09-RESEARCH.md
@.planning/phases/09-business-tool-facade/09-VERIFICATION.md
@.planning/phases/09-business-tool-facade/09-REVIEW.md
@.planning/phases/09-business-tool-facade/09-05-SUMMARY.md

<interfaces>
`get_current_user(...) -> User` remains the dependency contract used across routers. Preserve that return type; attach the normalized verified scope set to `request.state.verified_token_scopes`.

Change `_trusted_tool_config` to:
`def _trusted_tool_config(user: User, token_scopes: Iterable[str], trace_id: str | None) -> dict[str, Any]`

The granted scope set is exactly:
`set(token_scopes) & set(ROLE_SCOPES.get(user.role, []))`
Only intersected scopes present in `SCOPE_TO_TOOL_PERMISSION` become `tool:*` permissions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve verified JWT scopes in trusted request context</name>
  <files>src/auth/permissions.py, tests/integration/test_auth.py</files>
  <read_first>
    - src/auth/permissions.py
    - src/auth/jwt.py
    - tests/integration/test_auth.py
    - .planning/ROADMAP.md (authoritative Phase 9 goal)
    - .planning/REQUIREMENTS.md (TOOL-01 and TOOL-02)
    - .planning/phases/09-business-tool-facade/09-CONTEXT.md (trusted ToolCallContext and ownership boundary)
    - .planning/phases/09-business-tool-facade/09-RESEARCH.md (trusted-context and permission constraints)
    - .planning/phases/09-business-tool-facade/09-PATTERNS.md (test and trusted-config patterns)
    - .planning/phases/09-business-tool-facade/09-VERIFICATION.md (verified-token widening gap)
    - .planning/phases/09-business-tool-facade/09-REVIEW.md (CR-01)
  </read_first>
  <behavior>
    - A valid token's normalized scopes are preserved after signature, subject, tenant, active-user, and required-endpoint-scope checks succeed.
    - Invalid or insufficient-scope tokens never populate trusted verified scopes.
    - `get_current_user` continues returning the database `User`; token claims are not attached to the mutable ORM entity.
  </behavior>
  <action>
Add `request: Request` to `get_current_user` and, only after all authentication and required-scope checks pass, assign an immutable normalized value such as `frozenset(token_scopes)` to `request.state.verified_token_scopes`. Validate the JWT `scopes` claim as a collection of strings before preservation; malformed scope claims must fail authentication rather than being iterated as arbitrary text. Keep the database user lookup and `User` return contract unchanged so all existing `Security(get_current_user, ...)` consumers remain compatible. Add focused integration coverage proving a valid `agent:chat`-only token preserves exactly `{"agent:chat"}` and that a rejected token cannot produce a trusted scope projection.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/integration/test_auth.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'request.state.verified_token_scopes' src/auth/permissions.py` returns the trusted request-state assignment.
    - `rg -n 'frozenset' src/auth/permissions.py` confirms the preserved scopes are an immutable projection.
    - `rg -n 'setattr\\(user|user\\..*scopes' src/auth/permissions.py` returns zero matches.
    - `rg -n 'agent:chat.*verified_token_scopes|verified_token_scopes.*agent:chat' tests/integration/test_auth.py` returns a restricted-token preservation assertion.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/integration/test_auth.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>Verified JWT scopes survive authentication in trusted request context without changing the authenticated User contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Intersect verified token scopes with current role scopes for live tool config</name>
  <files>src/api/routers/agent_runs.py, tests/test_agent_runs_api.py</files>
  <read_first>
    - src/api/routers/agent_runs.py
    - src/auth/permissions.py
    - src/auth/jwt.py
    - tests/test_agent_runs_api.py
    - .planning/ROADMAP.md (authoritative Phase 9 goal)
    - .planning/REQUIREMENTS.md (TOOL-01 and TOOL-02)
    - .planning/phases/09-business-tool-facade/09-CONTEXT.md (trusted context decision)
    - .planning/phases/09-business-tool-facade/09-RESEARCH.md (permission mapping and no-widening constraints)
    - .planning/phases/09-business-tool-facade/09-PATTERNS.md (router trusted-config pattern)
    - .planning/phases/09-business-tool-facade/09-VERIFICATION.md (permission gap)
    - .planning/phases/09-business-tool-facade/09-REVIEW.md (CR-01 exact fix)
  </read_first>
  <behavior>
    - Token scope cannot grant a permission absent from the current DB role.
    - DB role cannot grant a permission absent from the verified token.
    - A support-role `agent:chat`-only token streams a run with `configurable.permissions == []`.
    - A support token with `agent:chat` and `orders:read` receives exactly `tool:get_order`.
  </behavior>
  <action>
Change `_trusted_tool_config` to accept the verified token scopes explicitly and compute `trusted_scopes = set(token_scopes) & set(ROLE_SCOPES.get(user.role, []))`; map only that intersection through `SCOPE_TO_TOOL_PERMISSION`. In `stream_agent_run_events`, read the scope collection from `request.state.verified_token_scopes`; fail closed with an empty collection if the trusted projection is unexpectedly absent. Do not decode the bearer token again, do not derive permissions from request text or AgentState, and do not fall back to all role scopes. Keep merchant-scope and trace projection behavior unchanged. Add API-level tests with a capture graph: issue a support-role token with only `agent:chat`, stream a pending run, and assert the graph config receives no tool permissions; add the positive intersection case for `agent:chat + orders:read`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_agent_runs_api.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'token_scopes|ROLE_SCOPES|SCOPE_TO_TOOL_PERMISSION' src/api/routers/agent_runs.py` shows explicit intersection inputs and mapping.
    - `rg -n 'trusted_scopes = ROLE_SCOPES' src/api/routers/agent_runs.py` returns zero matches.
    - `rg -n 'verified_token_scopes' src/api/routers/agent_runs.py` shows the trusted request-state consumer.
    - `rg -n 'agent:chat.*permissions|permissions.*== \\[\\]|tool:get_order' tests/test_agent_runs_api.py` returns restricted-token denial and positive intersection assertions.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_agent_runs_api.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>Live agent-run tool permissions are the exact token/role intersection, with agent:chat-only support tokens receiving none.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JWT → authentication dependency | Untrusted bearer claims become verified scopes only after cryptographic and principal checks. |
| Authenticated request → agent-run config | Verified token scopes and current DB role are intersected before tool permissions enter trusted config. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | Elevation of Privilege / confused deputy | `get_current_user` → `_trusted_tool_config` | mitigate | Preserve verified scopes in request state and API-test that `agent:chat`-only support token yields `permissions=[]`. |
| T-09-06-02 | Elevation of Privilege | Role/token scope intersection | mitigate | Grant only `token_scopes ∩ ROLE_SCOPES[current_db_role]`; test token-only and role-only widening are impossible. |
| T-09-06-03 | Tampering | JWT `scopes` claim | mitigate | Reject malformed scope claims and never trust user/LLM/AgentState permission fields. |
| T-09-06-04 | Information Disclosure | Merchant scope projection | accept | Merchant projection is unchanged in this plan and is covered by Plan 09-07 propagation tests. |
| T-09-06-05 | Elevation of Privilege | Policy retrieval ownership boundary | accept | This plan changes authorization projection only; Plan 09-08 locks Phase 8 PolicyKnowledgeService ownership. |
</threat_model>

<verification>
Run focused tests after each task, then run the full Phase 9 regression command:
`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/business_tools/ tests/agent/test_nodes/test_load_business_context.py tests/agent/test_nodes/test_retrieve_policy_evidence.py tests/agent/test_graph.py tests/test_agent_runs_api.py tests/integration/test_auth.py -q --tb=short`
</verification>

<success_criteria>
- Verified token scopes are preserved in trusted request context without mutating the User ORM model.
- Tool permissions equal the verified-token/current-role intersection mapped through the fixed scope table.
- An API-level regression proves an `agent:chat`-only support token receives no tool permissions.
- The full Phase 9 regression command passes.
</success_criteria>

<output>
After completion, create `.planning/phases/09-business-tool-facade/09-06-SUMMARY.md`.
</output>
