---
phase: 09-business-tool-facade
plan: 05
type: execute
wave: 4
depends_on: ["09-04"]
files_modified:
  - src/agent/nodes/load_business_context.py
  - src/agent/tools/registry.py
  - src/agent/tools/contracts.py
  - tests/agent/test_nodes/test_load_business_context.py
  - tests/agent/test_tools/test_registry.py
  - tests/agent/test_tools/test_tool_contracts.py
  - tests/agent/test_graph.py
autonomous: true
requirements: [TOOL-01, TOOL-03]
must_haves:
  truths:
    - "load_business_context no longer imports or calls concrete get_order/get_refund_case/get_ticket functions; it calls BusinessToolService.fetch_context."
    - "The migrated node injects caller_node=\"investigate\" from trusted state/config and preserves the existing state-write shape."
    - "Old investigator whitelist and two-state result are retired from the live business-read path; search_policy remains untouched."
    - "Rollback remains a single-node read-switch revert."
  artifacts:
    - path: "src/agent/nodes/load_business_context.py"
      provides: "Live graph read-switch to BusinessToolService"
      contains: "BusinessToolService"
    - path: "src/agent/tools/registry.py"
      provides: "Retired obsolete investigator-whitelist business semantics"
      contains: "search_policy"
    - path: "tests/agent/test_graph.py"
      provides: "Facade-boundary graph regression monkeypatch"
      contains: "BusinessToolService"
  key_links:
    - from: "src/agent/nodes/load_business_context.py"
      to: "src/business_tools/service.py"
      via: "BusinessToolService.fetch_context"
      pattern: "fetch_context"
    - from: "tests/agent/test_graph.py"
      to: "src/agent/nodes/load_business_context.py"
      via: "facade-boundary monkeypatch and business_context parity assertion"
      pattern: "business_context"
---

<objective>
Switch the live business-context node from direct raw-tool calls to BusinessToolService and retire obsolete prior-line business registry/contracts without touching policy search ownership.

Purpose: complete TOOL-01's read-switch and TOOL-03 cleanup while preserving downstream state shape and a simple rollback seam.
Output: migrated node, retired legacy business semantics, and updated node/tool/graph regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-business-tool-facade/09-CONTEXT.md
@.planning/phases/09-business-tool-facade/09-RESEARCH.md
@.planning/phases/09-business-tool-facade/09-PATTERNS.md

Source-of-truth spec:
- docs/contract-spec.md §8.0 — trusted state/config identity and scope.
- docs/contract-spec.md §8.4 — BusinessToolService.fetch_context signature.
- docs/contract-spec.md §9.4 — target business-context/investigate node contract.
- docs/contract-spec.md §12.4 — investigate allowlist.
- docs/contract-spec.md §12.5 and §12.6 — v2 result/registry rules and write block.

Cross-cutting carries: SCF-1 constructs context only from trusted state/config; SCF-2 injects `caller_node="investigate"` despite physical node name; SCF-3 leaves write execution outside facade; SCF-4 introduces no audit persistence.

<interfaces>
<!-- Direct-call read path and state-write shape to replace/preserve. Source: src/agent/nodes/load_business_context.py:31-78 [VERIFIED] -->
The live node imports concrete tools at lines 9-11, calls them at lines 52/60/68, and returns exactly `business_context`, `tool_results`, `last_business_context_refs`, and `trace_steps` at lines 74-78.

<!-- Obsolete registry semantics. Source: src/agent/tools/registry.py:28-32,149-224,253-269 [VERIFIED] -->
Legacy investigator constants/methods/caller branches and policy-evidence extraction are mixed into the old business registry path and must no longer govern live business reads.

<!-- Obsolete two-state contracts. Source: src/agent/tools/contracts.py:15-22,69-75 [VERIFIED] -->
`ToolInvocationContext` has five fields and `ToolExecutionResult` only success/error; neither may remain the live business-read contract.

<!-- Graph monkeypatch seam. Source: tests/agent/test_graph.py:155-157,193-197,228-233 [VERIFIED] -->
Graph tests currently patch node-module concrete tool names and assert the existing business_context shape; retarget patches to the facade boundary while preserving assertions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate load_business_context to BusinessToolService.fetch_context</name>
  <files>src/agent/nodes/load_business_context.py</files>
  <read_first>
    - src/agent/nodes/load_business_context.py:1-79 (full direct-call node and preserved state-write shape)
    - src/business_tools/service.py (Plan 09-04 output)
    - src/business_tools/schemas.py (ToolCallContext exact fields)
    - .planning/phases/09-business-tool-facade/09-RESEARCH.md D4, D6, §6
    - docs/contract-spec.md §8.0, §8.4, §9.4
  </read_first>
  <behavior>
    - Direct imports/calls to get_order/get_refund_case/get_ticket are absent.
    - ToolCallContext is constructed from trusted state/config, with canonical caller_node="investigate", minted request_id/tool_call_id, and permissions/merchant_scope from trusted config.
    - Existing intent/identifier conditional-load guard and extracted_slots/active_slots selection are preserved.
    - Return keys and trace node name remain unchanged so downstream graph behavior and rollback seam remain stable.
  </behavior>
  <action>
Replace the three direct imports and calls with imports of `BusinessToolService` and `ToolCallContext`, plus the registry/service construction seam used by the application. Construct ToolCallContext only from trusted inputs: `tenant_id`, `user_id`, `role` from state; `permissions`, `merchant_scope`, `session_id`, and session/service dependencies from `config["configurable"]`; `thread_id`, `run_id`, `trace_id` from state/config; mint `request_id` and `tool_call_id`; set `caller_node="investigate"`, `attempt=1`, `max_attempts=1`, `deadline_at=None`, `idempotency_key=None`, `policy_snapshot_ref=None`.

Preserve the current `should_load_context` guard and slot selection. When loading, call `await service.fetch_context(slots, intent, ctx)`. Map the returned BusinessContextV1 back to the existing node return keys exactly: `business_context` receives `context.facts`; `tool_results` receives serialized typed results; `last_business_context_refs` preserves loaded-at/identifier/ref information; `trace_steps` preserves node `"load_business_context"` and called-tool list. The rollback is reverting this node only. Do not relocate raw tools and do not touch `search_policy`.
  </action>
  <verify>
    <automated>python -m py_compile src/agent/nodes/load_business_context.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'from src.agent.tools.get_(order|refund_case|ticket) import|await get_(order|refund_case|ticket)' src/agent/nodes/load_business_context.py` returns zero matches.
    - `rg -n 'BusinessToolService|fetch_context|ToolCallContext|caller_node="investigate"' src/agent/nodes/load_business_context.py` returns all facade/context markers.
    - `rg -n '"business_context"|"tool_results"|"last_business_context_refs"|"trace_steps"' src/agent/nodes/load_business_context.py` returns all preserved state-write keys.
    - `python -m py_compile src/agent/nodes/load_business_context.py` exits 0.
  </acceptance_criteria>
  <done>The live node uses only the facade for business reads and preserves downstream state/rollback behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Retire obsolete prior-line business registry and contract semantics</name>
  <files>src/agent/tools/registry.py, src/agent/tools/contracts.py</files>
  <read_first>
    - src/agent/tools/registry.py:1-269 (legacy whitelist, caller branches, two-state conversion, policy extraction)
    - src/agent/tools/contracts.py:1-75 (old ToolInvocationContext and ToolExecutionResult)
    - .planning/phases/09-business-tool-facade/09-CONTEXT.md locked relationship decision
    - .planning/phases/09-business-tool-facade/09-RESEARCH.md §2.1, D1-D3
    - docs/contract-spec.md §12.5 and §12.6
  </read_first>
  <behavior>
    - `INVESTIGATOR_TOOL_NAMES`, `allowed_in_investigator`, and investigator `_caller_can_invoke` business semantics are absent.
    - Old ToolInvocationContext/ToolExecutionResult are no longer imported by any live business node.
    - Policy search remains owned by Phase 8 and is not deleted or migrated into BusinessToolService.
    - Minimal-diff shim/deletion choice keeps non-business compatibility only where tests/live imports require it.
  </behavior>
  <action>
Reduce the obsolete prior-line registry/contracts to the minimal compatibility surface needed outside the migrated business path. Remove `INVESTIGATOR_TOOL_NAMES`, `allowed_in_investigator`, investigator/load_business_context caller branches, and business-path `_evidence_refs_from_data`. Ensure no live node imports `ToolInvocationContext` or `ToolExecutionResult`; remove or clearly isolate those old two-state contracts so they cannot be mistaken for the business facade contract. Do not edit `src/agent/tools/get_order.py`, `get_refund_case.py`, `get_ticket.py`, `authz.py`, or `search_policy.py`; the raw reads remain import-in-place and policy ownership stays Phase 8.
  </action>
  <verify>
    <automated>python -m py_compile src/agent/tools/registry.py src/agent/tools/contracts.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'INVESTIGATOR_TOOL_NAMES|allowed_in_investigator' src/agent/tools/registry.py src/agent/tools/contracts.py` returns zero matches.
    - `rg -n 'ToolInvocationContext|ToolExecutionResult' src/agent/nodes` returns zero matches.
    - `rg -n 'search_policy' src/agent/tools/registry.py src/agent/tools/search_policy.py` confirms policy search remains present.
    - `python -m py_compile src/agent/tools/registry.py src/agent/tools/contracts.py` exits 0.
  </acceptance_criteria>
  <done>Legacy business whitelist/two-state semantics are retired from live reads without disturbing raw tools or policy search.</done>
</task>

<task type="auto">
  <name>Task 3: Retarget affected tests and add read-switch parity regression</name>
  <files>tests/agent/test_nodes/test_load_business_context.py, tests/agent/test_tools/test_registry.py, tests/agent/test_tools/test_tool_contracts.py, tests/agent/test_graph.py</files>
  <read_first>
    - tests/agent/test_graph.py:155-157,193-197,228-233 (concrete-tool monkeypatch seam and parity assertions)
    - tests/agent/test_tools/test_registry.py (old registry-bound tests)
    - tests/agent/test_tools/test_tool_contracts.py (old contract-bound tests)
    - tests/agent/test_nodes/ (locate or create the node-level load_business_context test)
    - .planning/phases/09-business-tool-facade/09-PATTERNS.md §load_business_context and §Test Patterns
  </read_first>
  <behavior>
    - Node/graph tests patch BusinessToolService/facade boundary, not concrete raw functions.
    - Existing successful business_context shape remains equivalent after the read-switch.
    - Tests no longer assert obsolete whitelist/two-state business behavior.
    - Full agent node/tool plus business_tools suites pass.
  </behavior>
  <action>
Create or update the node test to inject/mock `BusinessToolService.fetch_context` and assert the returned state keys and successful business_context match the existing passing case. Retarget `tests/agent/test_graph.py` lines 155-157 from module-level `get_order/get_refund_case/get_ticket` monkeypatches to the facade boundary while retaining the existing business_context assertions around lines 193-197 and 228-233. Rewrite/remove only tests bound exclusively to obsolete business whitelist/ToolInvocationContext/ToolExecutionResult semantics; rely on `tests/business_tools/` for v2 registry/contracts. Add a regression assertion that the migrated node contains no raw invalid sentinel when facade returns invalid_response.
  </action>
  <verify>
    <automated>pytest tests/agent/test_nodes/ tests/agent/test_tools/ tests/agent/test_graph.py tests/business_tools/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'monkeypatch.setattr\\(load_business_context_module, "get_(order|refund_case|ticket)"' tests/agent/test_graph.py` returns zero matches.
    - `rg -n 'BusinessToolService|fetch_context' tests/agent/test_graph.py tests/agent/test_nodes/test_load_business_context.py` returns facade-boundary tests.
    - `rg -n 'business_context' tests/agent/test_nodes/test_load_business_context.py tests/agent/test_graph.py` returns parity assertions.
    - `pytest tests/agent/test_nodes/ tests/agent/test_tools/ tests/agent/test_graph.py tests/business_tools/ -x -q` exits 0.
  </acceptance_criteria>
  <done>Tests follow the new facade boundary, prove behavior parity/no-leak, and no longer enforce obsolete business semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AgentState/config → ToolCallContext | Trusted identity/scope/permissions are assembled at the migrated live node. |
| Graph node → BusinessToolService | The read-switch replaces direct raw-tool access with the typed facade boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Information Disclosure / Elevation of Privilege | read-switch migration | mitigate | Construct context from trusted state/config, route through facade scope/no-leak gates, and add graph/node parity/no-raw regression tests. |
| T-09-09 | Tampering | legacy registry coexistence | mitigate | Remove obsolete whitelist/two-state business semantics and grep live nodes for zero old-contract imports. |
</threat_model>

<verification>
- `pytest tests/agent/test_nodes/ tests/agent/test_tools/ tests/agent/test_graph.py tests/business_tools/ -x -q` is green.
- Live node has zero direct concrete business-tool imports/calls.
- Search policy and raw read/authz files remain untouched and in place.
</verification>

<success_criteria>
- TOOL-01: live business reads uniformly use BusinessToolService with trusted ToolCallContext.
- TOOL-03: obsolete/write-capable prior-line business semantics are not the live path; write execution remains outside the facade.
- State-write shape and per-node rollback seam are preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/09-business-tool-facade/09-05-SUMMARY.md`.
</output>
