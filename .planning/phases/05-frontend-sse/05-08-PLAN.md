---
phase: 05-frontend-sse
plan: 08
type: execute
wave: 3
depends_on: [02, 03, 04]
files_modified:
  - frontend/src/components/details/DetailsPanel.tsx
  - frontend/src/components/ui/tabs.tsx
  - frontend/src/components/ui/textarea.tsx
  - frontend/src/components/ui/scroll-area.tsx
  - frontend/tailwind.config.ts
autonomous: true
gap_closure: true
requirements:
  - FRNT-03

must_haves:
  truths:
    - "Frontend lint passes for the files touched by the Phase 5 verification/review blockers"
  artifacts:
    - path: "frontend/src/components/details/DetailsPanel.tsx"
      provides: "Approval tab auto-selection without synchronous setState inside an effect"
      contains: "selectedTab"
    - path: "frontend/tailwind.config.ts"
      provides: "ESM plugin import for Tailwind animate"
      contains: "tailwindcssAnimate"
  key_links:
    - from: "frontend/src/components/details/DetailsPanel.tsx"
      to: "frontend/src/components/ui/tabs.tsx"
      via: "selectedTab value and activeValue props"
      pattern: "selectedTab"
---

<objective>
Close the frontend lint blocker from `05-REVIEW.md`.

Purpose: Phase 5 cannot pass the required frontend lint verification while current UI primitives and DetailsPanel contain lint violations. This plan fixes those violations without changing visual behavior.
Output: lint-clean DetailsPanel tab selection, UI primitive type definitions, and Tailwind ESM plugin import.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-frontend-sse/05-CONTEXT.md
@.planning/phases/05-frontend-sse/05-UI-SPEC.md
@.planning/phases/05-frontend-sse/05-REVIEW.md
@.planning/phases/05-frontend-sse/05-PATTERNS.md
@.planning/phases/05-frontend-sse/05-03-SUMMARY.md

<pattern_analogs>
- `05-PATTERNS.md` marks `frontend/src/components/details/DetailsPanel.tsx` and `frontend/src/components/ui/*` as frontend-only files; preserve the existing Phase 5 shadcn/Radix-style component contracts while resolving lint findings.
- `05-PATTERNS.md` maps no backend analog for these UI primitives, so the source of truth is the existing frontend component API and `05-UI-SPEC.md` tab/approval behavior.
</pattern_analogs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve frontend lint blockers from review</name>
  <files>frontend/src/components/details/DetailsPanel.tsx, frontend/src/components/ui/tabs.tsx, frontend/src/components/ui/textarea.tsx, frontend/src/components/ui/scroll-area.tsx, frontend/tailwind.config.ts</files>
  <read_first>
    - .planning/phases/05-frontend-sse/05-REVIEW.md — IN-01 lists the lint failure categories.
    - .planning/phases/05-frontend-sse/05-PATTERNS.md — use frontend-only component notes and preserve existing shadcn-style primitive APIs.
    - frontend/src/components/details/DetailsPanel.tsx — remove synchronous setState from effect.
    - frontend/src/components/ui/tabs.tsx — fix unused destructured parameters.
    - frontend/src/components/ui/textarea.tsx and frontend/src/components/ui/scroll-area.tsx — replace empty interfaces.
    - frontend/tailwind.config.ts — replace CommonJS `require()`.
  </read_first>
  <action>
Fix the lint categories without changing visual behavior:

- In `DetailsPanel.tsx`, remove the `useEffect` that calls `setActiveTab('approval')`. Use:
  `const selectedTab = status === 'waiting_approval' ? 'approval' : activeTab`
  for `Tabs value`, `TabsTrigger activeValue`, and `TabsContent activeValue`. Keep `setActiveTab` for user-triggered tab changes.
- In `tabs.tsx`, use the destructured `value` and `onValueChange` parameters explicitly with `void value; void onValueChange;` inside `Tabs(...)`, or otherwise implement them so ESLint no-unused-vars passes.
- In `textarea.tsx`, replace `export interface TextareaProps extends React.TextareaHTMLAttributes<HTMLTextAreaElement> {}` with:
  `export type TextareaProps = React.TextareaHTMLAttributes<HTMLTextAreaElement>`.
- In `scroll-area.tsx`, replace `export interface ScrollAreaProps extends React.HTMLAttributes<HTMLDivElement> {}` with:
  `export type ScrollAreaProps = React.HTMLAttributes<HTMLDivElement>`.
- In `tailwind.config.ts`, import the plugin at top:
  `import tailwindcssAnimate from 'tailwindcss-animate'`
  and set `plugins: [tailwindcssAnimate]`.
  </action>
  <acceptance_criteria>
    - `! rg -n "setActiveTab\\('approval'\\)" frontend/src/components/details/DetailsPanel.tsx` succeeds.
    - `rg -n "selectedTab" frontend/src/components/details/DetailsPanel.tsx` returns matches.
    - `! rg -n "interface (TextareaProps|ScrollAreaProps).*\\{\\}" frontend/src/components/ui/textarea.tsx frontend/src/components/ui/scroll-area.tsx` succeeds.
    - `! rg -n "require\\(" frontend/tailwind.config.ts` succeeds.
    - `cd frontend && npm run lint` passes.
    - `cd frontend && npm run build` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run lint</automated>
    <automated>cd frontend && npm run build</automated>
    <automated>docker compose config --quiet</automated>
  </verify>
  <done>`npm run lint` no longer fails on the Phase 5 review blockers, and build/compose validation still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> UI tab state | User interaction changes local tab state and approval visibility only. |
| Build tooling -> frontend bundle | Tailwind config is executed by local build tooling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | T | `DetailsPanel` tab state | mitigate | Preserve approval auto-selection with derived `selectedTab` while keeping user-triggered `setActiveTab` behavior. |
| T-05-08-02 | D | Frontend lint/build gate | mitigate | Run `npm run lint`, `npm run build`, and `docker compose config --quiet` after cleanup. |
| T-05-08-03 | E | Tailwind plugin config | accept | No new plugin is introduced; convert the existing `tailwindcss-animate` usage from `require()` to ESM import. |
</threat_model>

<verification>
Run these after the task:

```bash
cd frontend && npm run lint
cd frontend && npm run build
docker compose config --quiet
```
</verification>

<success_criteria>
- Review lint blocker IN-01 is closed.
- UI behavior remains aligned with `05-UI-SPEC.md`, especially approval tab auto-selection.
- `npm run lint`, `npm run build`, and `docker compose config --quiet` pass.
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-sse/05-08-SUMMARY.md`.
</output>
