---
phase: 05-frontend-sse
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tailwind.config.ts
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/lib/api.ts
  - frontend/src/lib/sse.ts
  - frontend/src/hooks/useAuth.ts
  - frontend/src/types/events.ts
  - frontend/components.json
  - frontend/Dockerfile
autonomous: true
requirements:
  - FRNT-01
  - FRNT-02

must_haves:
  truths:
    - "frontend/ 目录包含完整 Vite + React + TypeScript 项目"
    - "shadcn/ui 已初始化，dark theme 为默认"
    - "Tailwind CSS 配置包含 UI-SPEC 中定义的 color tokens"
    - "API client 封装 Bearer token auth"
    - "SSE client 使用 @microsoft/fetch-event-source 并支持 Authorization header"
    - "useAuth hook 实现 demo role switching（support_agent / manager / admin）"
    - "TypeScript 类型定义覆盖所有 SSE event types"
  artifacts:
    - path: "frontend/package.json"
      provides: "React + Vite + shadcn + Tailwind + fetch-event-source 依赖"
      contains: "@microsoft/fetch-event-source"
    - path: "frontend/src/lib/sse.ts"
      provides: "SSE client with auth and reconnection"
      contains: "fetchEventSource"
    - path: "frontend/src/hooks/useAuth.ts"
      provides: "Demo auth hook with role switching"
      contains: "demo-token"
    - path: "frontend/src/types/events.ts"
      provides: "SSE event type definitions"
      contains: "SseEvent"
---

<objective>
搭建前端项目脚手架：Vite + React + TypeScript + shadcn/ui + Tailwind CSS，配置 API client、SSE client、auth hook 和类型定义。

Purpose: 为 wave 2 的 UI 组件开发提供完整的项目基础设施。
Output: frontend/ 目录包含可运行的 dev server，所有基础设施代码就绪。
</objective>

<context>
@.planning/phases/05-frontend-sse/05-CONTEXT.md
@.planning/phases/05-frontend-sse/05-UI-SPEC.md
@.planning/phases/05-frontend-sse/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 初始化 Vite + React + TypeScript 项目</name>
  <files>frontend/package.json, frontend/vite.config.ts, frontend/tsconfig.json, frontend/index.html, frontend/src/main.tsx</files>
  <read_first>
    - .planning/phases/05-frontend-sse/05-CONTEXT.md — D-01 确认 React + Vite + TypeScript
    - .planning/phases/05-frontend-sse/05-UI-SPEC.md — 确认 dark theme 和 Inter 字体
  </read_first>
  <action>
在项目根目录执行：
```bash
cd /Users/ming/projects/MOCA
npm create vite@latest frontend -- --template react-ts
cd frontend
npm install
```

修改 vite.config.ts 添加 API proxy：
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
      },
    },
  },
})
```

修改 tsconfig.json 添加 path alias：
```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

修改 index.html 的 title 为 "MOCA Agent Console"，添加 Inter 字体 link：
```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
```
  </action>
  <acceptance_criteria>
    - frontend/package.json 包含 "react" 和 "vite"
    - frontend/vite.config.ts 包含 "proxy" 和 "/api"
    - frontend/index.html 包含 "MOCA Agent Console"
    - frontend/tsconfig.json 包含 "@/*"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 安装 Tailwind CSS + shadcn/ui + 依赖</name>
  <files>frontend/tailwind.config.ts, frontend/components.json, frontend/src/main.tsx</files>
  <read_first>
    - .planning/phases/05-frontend-sse/05-UI-SPEC.md — color tokens, spacing scale, typography
    - .planning/phases/05-frontend-sse/05-CONTEXT.md — D-02 确认 shadcn/ui 组件列表
  </read_first>
  <action>
```bash
cd /Users/ming/projects/MOCA/frontend
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p --ts
npm install @microsoft/fetch-event-source lucide-react class-variance-authority clsx tailwind-merge
npx shadcn@latest init --defaults --force
```

shadcn init 后修改 tailwind.config.ts 确保包含 UI-SPEC color tokens：
```typescript
import type { Config } from "tailwindcss"

const config: Config = {
  darkMode: "class",
  content: ["./index.html", "./src/**/*.{ts,tsx}"],
  theme: {
    extend: {
      colors: {
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        muted: { DEFAULT: "hsl(var(--muted))", foreground: "hsl(var(--muted-foreground))" },
        accent: { DEFAULT: "hsl(var(--accent))", foreground: "hsl(var(--accent-foreground))" },
        destructive: { DEFAULT: "hsl(var(--destructive))", foreground: "hsl(var(--destructive-foreground))" },
        card: { DEFAULT: "hsl(var(--card))", foreground: "hsl(var(--card-foreground))" },
        border: "hsl(var(--border))",
        status: {
          running: "hsl(217 91% 60%)",
          completed: "hsl(142 71% 45%)",
          waiting: "hsl(45 93% 47%)",
          rejected: "hsl(0 72% 51%)",
          degraded: "hsl(25 95% 53%)",
          failed: "hsl(0 72% 51%)",
          disconnected: "hsl(0 0% 45%)",
        },
      },
      fontFamily: {
        sans: ["Inter", "system-ui", "sans-serif"],
      },
      fontSize: {
        label: ["12px", { lineHeight: "1.33" }],
        body: ["14px", { lineHeight: "1.5" }],
        heading: ["16px", { lineHeight: "1.25" }],
        display: ["20px", { lineHeight: "1.2" }],
      },
    },
  },
  plugins: [require("tailwindcss-animate")],
}
export default config
```

安装 shadcn 组件（D-02 列表）：
```bash
npx shadcn@latest add button card badge tabs dialog textarea scroll-area toast select separator skeleton
```

确保 src/index.css 包含 dark theme CSS variables（shadcn init 默认生成）。在 index.html 的 `<html>` 标签添加 `class="dark"`。
  </action>
  <acceptance_criteria>
    - frontend/package.json 包含 "@microsoft/fetch-event-source"
    - frontend/package.json 包含 "tailwindcss"
    - frontend/package.json 包含 "lucide-react"
    - frontend/tailwind.config.ts 包含 "status" color tokens
    - frontend/components.json 存在
    - frontend/src/components/ui/button.tsx 存在
    - frontend/index.html 包含 'class="dark"'
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 创建 API client 和 SSE client</name>
  <files>frontend/src/lib/api.ts, frontend/src/lib/sse.ts</files>
  <read_first>
    - .planning/phases/05-frontend-sse/05-CONTEXT.md — D-11 SSE auth, D-12 disconnection strategy, D-13 API endpoints
  </read_first>
  <action>
创建 frontend/src/lib/api.ts：
```typescript
const API_BASE = '/api/v1'

let authToken: string | null = null

export function setAuthToken(token: string | null) {
  authToken = token
}

export function getAuthToken(): string | null {
  return authToken
}

export async function apiFetch<T = unknown>(
  path: string,
  options: RequestInit = {}
): Promise<{ success: boolean; data: T; error?: { code: string; message: string } }> {
  const headers: Record<string, string> = {
    'Content-Type': 'application/json',
    ...(options.headers as Record<string, string> || {}),
  }
  if (authToken) {
    headers['Authorization'] = `Bearer ${authToken}`
  }

  const res = await fetch(`${API_BASE}${path}`, { ...options, headers })
  const json = await res.json()
  return json
}

export async function createRun(query: string, threadId: string) {
  return apiFetch<{ run_id: string; status: string }>('/agent-runs', {
    method: 'POST',
    body: JSON.stringify({ query, thread_id: threadId }),
  })
}

export async function getRunStatus(runId: string) {
  return apiFetch<{ run_id: string; final_status: string; final_response: string | null }>(`/agent-runs/${runId}`)
}

export async function getRunEvidence(runId: string) {
  return apiFetch<{ evidence: Array<{ doc_key: string; title: string; confidence: number }> }>(`/agent-runs/${runId}/evidence`)
}

export async function getRunTrace(runId: string) {
  return apiFetch<{ run_id: string; steps: Array<unknown>; timeline: Array<unknown> }>(`/agent-runs/${runId}/trace`)
}

export async function decideApproval(approvalId: string, decision: 'approve' | 'reject', reason: string) {
  return apiFetch(`/approvals/${approvalId}/decide`, {
    method: 'POST',
    body: JSON.stringify({ decision, reason }),
  })
}

export async function getDemoToken(username: string) {
  return apiFetch<{ access_token: string }>('/auth/demo-token', {
    method: 'POST',
    body: JSON.stringify({ username }),
  })
}
```

创建 frontend/src/lib/sse.ts：
```typescript
import { fetchEventSource } from '@microsoft/fetch-event-source'
import { getAuthToken } from './api'
import type { SseEvent } from '@/types/events'

export interface SseCallbacks {
  onEvent: (event: SseEvent) => void
  onError: (error: Error) => void
  onClose: () => void
}

export function connectToRunEvents(
  runId: string,
  callbacks: SseCallbacks
): AbortController {
  const controller = new AbortController()

  fetchEventSource(`/api/v1/agent-runs/${runId}/events`, {
    method: 'GET',
    headers: {
      Authorization: `Bearer ${getAuthToken()}`,
    },
    signal: controller.signal,
    onmessage(msg) {
      if (!msg.data) return
      try {
        const event: SseEvent = JSON.parse(msg.data)
        callbacks.onEvent(event)
      } catch {
        // ignore malformed events
      }
    },
    onerror(err) {
      callbacks.onError(err instanceof Error ? err : new Error(String(err)))
      callbacks.onClose()
      throw err // stop retrying
    },
    onclose() {
      callbacks.onClose()
    },
    openWhenHidden: true,
  })

  return controller
}
```
  </action>
  <acceptance_criteria>
    - frontend/src/lib/api.ts 包含 "Bearer" 和 "agent-runs"
    - frontend/src/lib/sse.ts 包含 "fetchEventSource" 和 "Authorization"
    - frontend/src/lib/api.ts 包含 "createRun" 和 "getDemoToken" 和 "decideApproval"
    - frontend/src/lib/sse.ts 包含 "connectToRunEvents"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: 创建 useAuth hook 和 SSE event types</name>
  <files>frontend/src/hooks/useAuth.ts, frontend/src/types/events.ts</files>
  <read_first>
    - .planning/phases/05-frontend-sse/05-CONTEXT.md — D-20, D-21, D-22 demo role switcher; D-08, D-09 SSE event schema
  </read_first>
  <action>
创建 frontend/src/types/events.ts：
```typescript
export type EventType =
  | 'run_started'
  | 'step_started'
  | 'step_completed'
  | 'approval_required'
  | 'final_response'
  | 'error'

export type RunStatus =
  | 'running'
  | 'completed'
  | 'waiting_approval'
  | 'rejected'
  | 'degraded'
  | 'failed'
  | 'disconnected'

export interface SseEvent {
  event_type: EventType
  run_id: string
  step_index: number
  node_name?: string
  status: RunStatus | string
  message: string
  timestamp: string
  payload: SsePayload
}

export interface SsePayload {
  evidence_count?: number
  tool_name?: string
  risk_level?: string
  short_summary?: string
  approval_id?: string
  proposed_action?: Record<string, unknown>
  final_response?: string
  error_code?: string
  error_message?: string
}

export type DemoRole = 'support_agent' | 'manager' | 'admin'

export const DEMO_USERS: Record<DemoRole, { username: string; label: string }> = {
  support_agent: { username: 'cs_wang', label: 'Support Agent' },
  manager: { username: 'mgr_zhang', label: 'Approver' },
  admin: { username: 'admin_li', label: 'Admin' },
}
```

创建 frontend/src/hooks/useAuth.ts：
```typescript
import { useState, useCallback, useEffect } from 'react'
import { getDemoToken, setAuthToken } from '@/lib/api'
import type { DemoRole } from '@/types/events'
import { DEMO_USERS } from '@/types/events'

export function useAuth() {
  const [role, setRole] = useState<DemoRole>('support_agent')
  const [isAuthenticated, setIsAuthenticated] = useState(false)
  const [isLoading, setIsLoading] = useState(false)

  const switchRole = useCallback(async (newRole: DemoRole) => {
    setIsLoading(true)
    try {
      const user = DEMO_USERS[newRole]
      const res = await getDemoToken(user.username)
      if (res.success && res.data) {
        setAuthToken(res.data.access_token)
        setRole(newRole)
        setIsAuthenticated(true)
      }
    } finally {
      setIsLoading(false)
    }
  }, [])

  useEffect(() => {
    switchRole('support_agent')
  }, [switchRole])

  return { role, isAuthenticated, isLoading, switchRole }
}
```
  </action>
  <acceptance_criteria>
    - frontend/src/types/events.ts 包含 "SseEvent" 和 "EventType" 和 "DemoRole"
    - frontend/src/types/events.ts 包含 "DEMO_USERS"
    - frontend/src/hooks/useAuth.ts 包含 "switchRole" 和 "demo-token"
    - frontend/src/hooks/useAuth.ts 包含 "support_agent"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: 创建 App.tsx 骨架和 Dockerfile</name>
  <files>frontend/src/App.tsx, frontend/Dockerfile</files>
  <read_first>
    - .planning/phases/05-frontend-sse/05-CONTEXT.md — D-16 三列布局, D-18 SPA
    - .planning/phases/05-frontend-sse/05-UI-SPEC.md — Layout Contract
  </read_first>
  <action>
创建 frontend/src/App.tsx（骨架，wave 2 填充组件）：
```tsx
import { useAuth } from '@/hooks/useAuth'

export default function App() {
  const { role, isAuthenticated, isLoading, switchRole } = useAuth()

  if (!isAuthenticated || isLoading) {
    return (
      <div className="flex h-screen items-center justify-center bg-background text-foreground">
        <p className="text-body">正在连接...</p>
      </div>
    )
  }

  return (
    <div className="flex h-screen flex-col bg-background text-foreground">
      {/* Top Bar — wave 2 */}
      <header className="flex h-12 items-center justify-between border-b border-border px-4">
        <h1 className="text-heading font-semibold">MOCA Agent Console</h1>
        <div className="flex items-center gap-2">
          <span className="text-label text-muted-foreground">Demo Mode</span>
          <span className="text-label font-semibold text-accent">{role}</span>
        </div>
      </header>

      {/* Three-column layout — wave 2 fills with real components */}
      <main className="flex flex-1 overflow-hidden">
        {/* Left: Chat (30%) */}
        <section className="flex w-[30%] flex-col border-r border-border p-4">
          <p className="text-muted-foreground">Chat Panel (wave 2)</p>
        </section>

        {/* Center: Timeline (35%) */}
        <section className="flex w-[35%] flex-col border-r border-border p-4">
          <p className="text-muted-foreground">Agent Timeline (wave 2)</p>
        </section>

        {/* Right: Details (35%) */}
        <section className="flex w-[35%] flex-col p-4">
          <p className="text-muted-foreground">Details Panel (wave 2)</p>
        </section>
      </main>
    </div>
  )
}
```

创建 frontend/Dockerfile：
```dockerfile
FROM node:20-alpine AS base
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci
COPY . .

FROM base AS dev
EXPOSE 3000
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
```
  </action>
  <acceptance_criteria>
    - frontend/src/App.tsx 包含 "MOCA Agent Console" 和 "useAuth"
    - frontend/src/App.tsx 包含 "w-[30%]" 和 "w-[35%]"
    - frontend/Dockerfile 包含 "node:20-alpine" 和 "npm run dev"
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
cd /Users/ming/projects/MOCA/frontend
# 1. 项目结构
test -f package.json && test -f vite.config.ts && test -f tailwind.config.ts && echo "structure ok"

# 2. 依赖
grep "@microsoft/fetch-event-source" package.json
grep "tailwindcss" package.json
grep "lucide-react" package.json

# 3. TypeScript 编译
npx tsc --noEmit

# 4. Dev server 启动（验证无崩溃）
timeout 10 npm run dev || true
```
</verification>

<success_criteria>
- frontend/ 目录包含完整 Vite + React + TypeScript 项目
- shadcn/ui 初始化完成，dark theme 默认
- Tailwind 配置包含 status color tokens
- API client 和 SSE client 可编译
- useAuth hook 实现 demo role switching
- TypeScript 类型覆盖所有 SSE event types
- Dockerfile 可构建
</success_criteria>
