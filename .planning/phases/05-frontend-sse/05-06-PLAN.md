---
phase: 05-frontend-sse
plan: 06
type: execute
wave: 3
depends_on: [02, 03, 04]
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/lib/sse.ts
  - frontend/src/hooks/useAuth.ts
  - frontend/src/types/events.ts
  - frontend/src/App.tsx
  - frontend/src/components/chat/ChatPanel.tsx
  - frontend/src/components/chat/ChatInput.tsx
  - frontend/vite.config.ts
autonomous: true
gap_closure: true
requirements:
  - AGNT-07
  - FRNT-01
  - FRNT-03

must_haves:
  truths:
    - "Demo role switching obtains real JWTs for cs_zhang, mgr_li, and admin_user before protected actions are enabled"
    - "Frontend SSE event types exactly match backend emissions: run_started, step_started, step_completed, final_response, approval_required, error"
    - "Docker compose Vite proxy sends /api requests from the frontend container to http://api:8000"
  artifacts:
    - path: "frontend/src/hooks/useAuth.ts"
      provides: "Seeded demo role username map without placeholder bearer tokens"
      contains: "cs_zhang"
    - path: "frontend/src/types/events.ts"
      provides: "Frontend event contract aligned with backend SSE event names"
      contains: "step_started"
    - path: "frontend/vite.config.ts"
      provides: "VITE_API_URL-driven dev-server proxy target"
      contains: "process.env.VITE_API_URL"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/lib/api.ts"
      via: "getDemoToken(username) success before enabling ChatPanel submit"
      pattern: "authReady"
    - from: "frontend/src/lib/sse.ts"
      to: "frontend/src/lib/api.ts"
      via: "apiUrl(`/agent-runs/${runId}/events`)"
      pattern: "apiUrl"
    - from: "frontend/vite.config.ts"
      to: "docker-compose.yml frontend.environment.VITE_API_URL"
      via: "proxy target uses process.env.VITE_API_URL"
      pattern: "VITE_API_URL"
---

<objective>
Close the frontend demo auth, SSE contract, and Docker proxy gaps from `05-VERIFICATION.md`.

Purpose: The default demo must obtain real seeded-user JWTs, the TypeScript SSE union must match backend events, and the dev-server proxy must route `/api` to the API container when running under compose.
Output: real demo auth gating, aligned event type contract, shared API path builder for REST/SSE, and env-driven Vite proxy.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-frontend-sse/05-CONTEXT.md
@.planning/phases/05-frontend-sse/05-UI-SPEC.md
@.planning/phases/05-frontend-sse/05-VERIFICATION.md
@.planning/phases/05-frontend-sse/05-REVIEW.md
@.planning/phases/05-frontend-sse/05-PATTERNS.md
@.planning/phases/05-frontend-sse/05-02-SUMMARY.md
@.planning/phases/05-frontend-sse/05-03-SUMMARY.md
@.planning/phases/05-frontend-sse/05-04-SUMMARY.md

<interfaces>
From `src/api/routers/agent_runs.py`:
```python
_sse_event(event_type=...)
# Backend emits exactly:
# run_started, step_started, step_completed, final_response, approval_required, error
```

From `scripts/seed_demo.py`:
```python
("demo_admin", "demo", "admin_user", ..., "admin", None)
("demo_support_1", "demo", "cs_zhang", ..., "support", None)
("demo_manager_1", "demo", "mgr_li", ..., "manager", None)
```
</interfaces>

<pattern_analogs>
- `05-PATTERNS.md` maps `frontend/src/lib/api.ts` to backend auth/router contracts; client helpers should preserve the backend `ApiResponse` envelope and bearer-token expectations.
- `05-PATTERNS.md` marks `frontend/src/lib/sse.ts`, `frontend/src/App.tsx`, and `frontend/src/components/chat/*` as frontend-only stack files; use the established Phase 5 React hook/component patterns from `05-02-SUMMARY.md` and `05-03-SUMMARY.md`.
- `05-PATTERNS.md` maps `frontend/src/types/events.ts` to `src/api/schemas/agent.py` as a schema analog; keep event names tied to backend emitted values rather than adding un-emitted names.
</pattern_analogs>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix demo auth, API routing, and SSE event contracts</name>
  <files>frontend/src/lib/api.ts, frontend/src/lib/sse.ts, frontend/src/hooks/useAuth.ts, frontend/src/types/events.ts, frontend/src/App.tsx, frontend/src/components/chat/ChatPanel.tsx, frontend/src/components/chat/ChatInput.tsx, frontend/vite.config.ts</files>
  <read_first>
    - frontend/src/lib/api.ts — update `API_BASE`, `apiFetch`, and exported helpers while preserving the existing ApiResponse envelope.
    - frontend/src/lib/sse.ts — update SSE URL construction to reuse API path behavior.
    - frontend/src/hooks/useAuth.ts — replace nonexistent demo users and remove placeholder bearer-token installation.
    - frontend/src/App.tsx — surface demo token failures before enabling ChatPanel submit.
    - frontend/src/components/chat/ChatPanel.tsx and frontend/src/components/chat/ChatInput.tsx — disable submit while auth is not ready.
    - frontend/src/types/events.ts — align frontend event contract with backend event names.
    - frontend/vite.config.ts and docker-compose.yml — proxy target must honor `VITE_API_URL=http://api:8000`.
    - .planning/phases/05-frontend-sse/05-PATTERNS.md — use frontend client/component analog notes and the `src/api/schemas/agent.py` schema analog for type contracts.
    - .planning/phases/05-frontend-sse/05-VERIFICATION.md — gaps 1, 4, and 6 are the source of truth.
  </read_first>
  <action>
Make these concrete changes:

- In `frontend/src/hooks/useAuth.ts`, set:
  `support_agent: 'cs_zhang'`, `manager: 'mgr_li'`, `admin: 'admin_user'`.
- Remove the `useEffect` that calls `setAuthToken(\`demo-token:${...}\`)`; no frontend file may install a `demo-token:*` placeholder bearer token.
- In `frontend/src/App.tsx`, add `authReady` and `authError` state. On each `username` change:
  `setAuthToken(null)`, `setAuthReady(false)`, `setAuthError(null)`, call `getDemoToken(username)`, and only set `setAuthToken(result.data.access_token); setAuthReady(true)` on `result.success === true`.
- In the same effect, handle failed API results and promise rejection by setting `authError` to `result.error?.message ?? 'Demo token 获取失败，无法执行受保护操作'` and leaving `authReady=false`.
- Pass `authReady` and `authError` into `ChatPanel`; in `ChatInput`, disable the submit button when `disabled || !authReady || !query.trim()`. Show the auth error above the input in `ChatPanel` so protected run actions are visibly unavailable.
- In `frontend/src/types/events.ts`, replace `SseEventType` with exactly:
  `'run_started' | 'step_started' | 'step_completed' | 'final_response' | 'approval_required' | 'error'`.
- In `frontend/src/lib/api.ts`, keep browser calls relative by default: `const API_BASE = '/api/v1'`. Add `export function apiUrl(path: string) { return `${API_BASE}${path}` }` and use it in `apiFetch`.
- In `frontend/src/lib/sse.ts`, replace hardcoded `/api/v1/agent-runs/...` with `apiUrl(`/agent-runs/${runId}/events`)`.
- In `frontend/vite.config.ts`, define `const apiProxyTarget = process.env.VITE_API_URL || 'http://localhost:8000'` and set `server.proxy['/api'].target = apiProxyTarget`. Keep `changeOrigin: true`, `port: 3000`, and the existing alias.
  </action>
  <acceptance_criteria>
    - `rg -n "cs_zhang|mgr_li|admin_user" frontend/src/hooks/useAuth.ts` returns matches.
    - `! rg -n "demo-token:" frontend/src frontend/vite.config.ts` succeeds.
    - `rg -n "authReady|authError|Demo token 获取失败" frontend/src/App.tsx frontend/src/components/chat/ChatPanel.tsx frontend/src/components/chat/ChatInput.tsx` returns matches.
    - `rg -n "step_started|step_completed|event_type: SseEventType" frontend/src/types/events.ts` returns matches, and `! rg -n "node_started|node_completed|run_completed|run_failed|tool_called|evidence_retrieved" frontend/src/types/events.ts` succeeds.
    - `rg -n "process\\.env\\.VITE_API_URL|apiProxyTarget|target: apiProxyTarget" frontend/vite.config.ts` returns matches.
    - `cd frontend && npm run build` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>Default demo role switching obtains real seeded-user JWTs, does not install placeholder tokens, disables protected submit until auth succeeds, and Docker proxy routing uses `VITE_API_URL`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> FastAPI REST/SSE | Untrusted UI actions and bearer tokens cross into protected run APIs. |
| Demo role switcher -> auth endpoint | User-selected demo role maps to seeded usernames and requests JWTs. |
| Vite dev server -> API container | `/api` proxy target is controlled by `VITE_API_URL` in Docker compose. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | S/E | Demo auth/RBAC | mitigate | Map roles only to seeded users `cs_zhang`, `mgr_li`, `admin_user`; disable protected submit until real JWT fetch succeeds. |
| T-05-06-02 | I/S | Token handling | mitigate | Remove all `demo-token:*` placeholder bearer tokens; only `access_token` from `/auth/demo-token` is installed with `setAuthToken`. |
| T-05-06-03 | T/D | SSE duplicate execution | transfer | Backend mitigation is in Plan 05-05; frontend aligns event types and treats non-2xx stream setup as a visible failure path. |
| T-05-06-04 | I | Docker/API routing exposure | mitigate | Vite proxy reads `process.env.VITE_API_URL` so compose uses `http://api:8000`; browser clients keep relative `/api/v1` paths and do not expose internal service names. |
</threat_model>

<verification>
Run these after the task:

```bash
cd frontend && npm run build
rg -n "cs_zhang|mgr_li|admin_user|step_started|step_completed|process\\.env\\.VITE_API_URL" frontend/src frontend/vite.config.ts
```
</verification>

<success_criteria>
- Verification gaps 1, 4, and 6 are closed.
- Default demo frontend cannot start protected run actions until a real seeded-user JWT is installed.
- Frontend SSE event names match backend emissions.
- Compose dev-server proxy target is driven by `VITE_API_URL`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-sse/05-06-SUMMARY.md`.
</output>
