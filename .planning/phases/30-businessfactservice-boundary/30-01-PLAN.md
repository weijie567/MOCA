---
phase: 30-businessfactservice-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/business/schemas.py
  - src/business/service.py
  - src/business/__init__.py
  - src/tools/executors/business.py
  - src/tools/projection.py
  - tests/business/test_schemas.py
  - tests/business/test_service.py
  - tests/business/test_adapters.py
  - tests/tools/test_tool_platform.py
  - tests/agent/test_nodes/test_investigate.py
  - tests/agent/rag_context/test_authority_boundaries.py
  - tests/agent/test_policy_retrieval_ownership.py
autonomous: true
requirements: [APF-08]
must_haves:
  truths:
    - "Order, refund case, ticket, logistics, and merchant-risk reads return service-owned BusinessFactResultV1 semantics before any ToolResultV2 wrapping."
    - "BusinessFactRefV1 is emitted only after BusinessFactService domain scope proof succeeds."
    - "ToolPlatform order/refund/ticket requires_domain_scope_check markers are enforced by BusinessFactService before graph facts, prompt summaries, or refs are produced."
    - "Denied, stale, unavailable, invalid, and unsupported business reads expose generic safe errors with no resource existence leak."
    - "Memory, RAG, LLM/model context, prompt summaries, and raw repository rows cannot substitute for missing or denied current business facts."
  artifacts:
    - path: "src/business/schemas.py"
      provides: "BusinessFactResultV1 strict domain result contract and BusinessContextV1 compatibility aggregation schema"
      contains: "class BusinessFactResultV1"
    - path: "src/business/service.py"
      provides: "BusinessFactService authoritative public methods and BusinessToolService compatibility adapter"
      contains: "class BusinessFactService"
    - path: "src/tools/executors/business.py"
      provides: "ToolPlatform business executor delegation to BusinessFactService"
      contains: "BusinessFactService"
    - path: "src/tools/projection.py"
      provides: "Projection only carries service-approved business refs"
      contains: "result.business_fact_refs"
    - path: "tests/business/test_schemas.py"
      provides: "BusinessFactResultV1 and BusinessFactRefV1 contract tests"
      contains: "BusinessFactResultV1"
    - path: "tests/business/test_service.py"
      provides: "BusinessFactService no-leak, scope, unavailable, stale, and compatibility tests"
      contains: "BusinessFactService"
    - path: "tests/tools/test_tool_platform.py"
      provides: "ToolPlatform requires_domain_scope_check enforcement through service-approved results"
      contains: "requires_domain_scope_check"
    - path: "tests/agent/test_nodes/test_investigate.py"
      provides: "Graph accumulation tests for denied/stale/unavailable service results"
      contains: "service-approved"
    - path: "tests/agent/rag_context/test_authority_boundaries.py"
      provides: "Memory/RAG/model substitution negative tests for business facts"
      contains: "business_fact_ref_required"
  key_links:
    - from: "src/tools/platform.py"
      to: "src/tools/executors/business.py"
      via: "ToolPlatform.with_defaults(session) executor registry"
      pattern: "BusinessToolExecutor\\(session\\)"
    - from: "src/tools/executors/business.py"
      to: "src/business/service.py"
      via: "BusinessToolExecutor.execute delegates to BusinessFactService"
      pattern: "BusinessFactService"
    - from: "src/business/service.py"
      to: "src/business/adapters.py"
      via: "private adapter calls for order/refund/ticket data-backed reads"
      pattern: "get_order_adapter|get_refund_case_adapter|get_ticket_adapter"
    - from: "src/tools/policy.py"
      to: "src/business/service.py"
      via: "requires_domain_scope_check identifiers resolved by BusinessFactService before refs"
      pattern: "requires_domain_scope_check"
    - from: "src/tools/projection.py"
      to: "src/agent/nodes/investigate.py"
      via: "ToolInvocationOutcome.projection consumed instead of raw ToolResultV2.data"
      pattern: "projection\\.normalized_result"
---

<objective>
Make current business facts available only through stable domain service public methods and the `BusinessFactResultV1` / `BusinessFactRefV1` contracts.

Purpose: Close APF-08 by moving public business-fact authority from tool-facing `BusinessToolService` / raw demo integration seams into `BusinessFactService`, while preserving Phase 29 ToolPlatform gates and Phase 29.5 merchant-bound scope semantics.

Output: One autonomous implementation plan that adds the domain result contract, creates the service public boundary, adapts ToolPlatform business reads to service-approved results, and adds no-leak authority-boundary tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-businessfactservice-boundary/30-CONTEXT.md
@.planning/phases/30-businessfactservice-boundary/30-RESEARCH.md
@.planning/phases/30-businessfactservice-boundary/30-VALIDATION.md
@.planning/phases/30-businessfactservice-boundary/30-PATTERNS.md
@docs/contract-spec.md
@docs/target-agent-platform-architecture-plan.md
@docs/eval-test-plan.md
@.planning/phases/29-tool-platform-boundary/29-CONTEXT.md
@.planning/phases/29-tool-platform-boundary/29-03-SUMMARY.md
@.planning/phases/29-tool-platform-boundary/29-04-SUMMARY.md
@.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-CONTEXT.md
@.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-02-SUMMARY.md
@.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-03-SUMMARY.md
@.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-06-SUMMARY.md
@.planning/todos/deferred/2026-06-27-merchant-scope-businessfactservice.md
@src/business/schemas.py
@src/business/service.py
@src/business/adapters.py
@src/tools/contracts.py
@src/tools/policy.py
@src/tools/executors/business.py
@src/tools/projection.py
@src/agent/nodes/investigate.py

<interfaces>
Key contracts already present:

```python
class BusinessFactRefV1(BaseModel):
    schema_version: Literal["business_fact_ref.v1"] = "business_fact_ref.v1"
    tenant_id: str
    source_system: str
    resource_type: Literal["order", "refund_case", "ticket", "logistics", "merchant_risk"]
    resource_id: str
    resource_version: str | None
    data_freshness_at: datetime | None
    retrieved_at: datetime
```

```python
class ToolResultV2(BaseModel):
    status: Literal[
        "success", "partial_success", "not_found", "permission_denied", "timeout",
        "unavailable", "conflict", "invalid_request", "invalid_response", "error"
    ]
    data: dict[str, Any] | None
    summary: str
    source_system: str
    data_freshness_at: datetime | None
    policy_evidence_refs: list[EvidenceRefV1] = Field(default_factory=list)
    business_fact_refs: list[BusinessFactRefV1] = Field(default_factory=list)
    error: ToolError | None = None
```

Normative Phase 30 target from `docs/contract-spec.md` section 8.4:

```python
class BusinessFactResultV1(BaseModel):
    schema_version: Literal["business_fact_result.v1"] = "business_fact_result.v1"
    tenant_id: str
    status: Literal["ok", "partial", "not_found", "permission_denied", "stale", "unavailable", "invalid_request"]
    fact: dict[str, Any] | None
    business_fact_refs: list[BusinessFactRefV1]
    resource_version: str | None = None
    data_freshness_at: datetime | None = None
    source_system: str
    scope_check_result: Literal["allowed", "denied", "not_applicable", "unknown"]
    missing_required_facts: list[str]
    safe_errors: list[ToolError]
```

Current ToolPolicy marker:

```python
_DOMAIN_SCOPE_CHECK_IDENTIFIERS = {"order_id", "order_no", "refund_id", "refund_case_no", "ticket_id"}
# _build_resource_binding(...) sets binding["requires_domain_scope_check"] = True
```
</interfaces>

No database schema push is part of this phase: no ORM model, migration, collection, global, Prisma, Drizzle, Supabase, entity, or migration file is in Phase 30 scope.
</context>

<source_audit>
| Source | ID | Feature / Requirement | Plan Task | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | Phase 30 | Stable BusinessFactService public methods and `BusinessFactResultV1` / `BusinessFactRefV1` contracts | 1, 2, 3 | COVERED | Goal-backward must-haves above map the service, executor, projection, and tests. |
| REQ | APF-08 | Business fact reads expose domain result/ref contracts; graph/tool cannot substitute memory/RAG/LLM/raw rows | 1, 2, 3 | COVERED | `requirements: [APF-08]` and all tasks include APF-08 verify commands. |
| RESEARCH | schema | Add strict `BusinessFactResultV1` with status, explicit nullable freshness/version, scope check, missing facts, safe errors | 1 | COVERED | Follows `30-RESEARCH.md` validation map 30-01-01. |
| RESEARCH | service | Add `BusinessFactService` public methods for order/refund/ticket/logistics/merchant-risk and `fetch_context` | 1 | COVERED | Implements data-backed order/refund/ticket reads and typed unavailable logistics/merchant-risk reads. |
| RESEARCH | tool-platform | Delegate `BusinessToolExecutor` through service-approved results; enforce `requires_domain_scope_check` | 2 | COVERED | Keeps ToolRuntime auth separate from domain scope proof. |
| RESEARCH | projection | Keep prompts/graph/business_context free of raw payloads and denied refs | 3 | COVERED | Removes data-derived business refs unless the result envelope has service-approved refs. |
| RESEARCH | authority | Add memory/RAG/LLM/raw-row substitution negative tests | 3 | COVERED | Uses existing authority-boundary files. |
| RESEARCH | validation | Run focused business/tool/agent suites and ruff | 1, 2, 3 | COVERED | Plan-level verification uses `30-VALIDATION.md` focused command. |
| RESEARCH | security | Mitigate T-30-01 through T-30-05 | 1, 2, 3 | COVERED | Threat model below maps all five threat refs. |
| CONTEXT | D-01 | `BusinessFactService` authoritative domain service under `src/business/` | 1, 2 | COVERED | Task 1 creates it; Task 2 delegates compatibility adapter to it. |
| CONTEXT | D-02 | Public methods cover `fetch_context`, order/refund/ticket/logistics/merchant-risk | 1 | COVERED | Unsupported logistics/risk return typed unavailable results with no facts/refs. |
| CONTEXT | D-03 | Repositories stay helpers; graph/tool consumers do not use raw rows as authority | 3 | COVERED | Static/boundary tests reject graph/executor raw repository authority. |
| CONTEXT | D-04 | Shared projection/ref emission owned by service, API semantics preserved | 1 | COVERED | No API route rewrite; service owns shared business-fact projection. |
| CONTEXT | D-05 | Dedicated `BusinessFactResultV1`, not `ToolResultV2` as domain contract | 1 | COVERED | Task 1 adds schema and service returns it. |
| CONTEXT | D-06 | `BusinessFactRefV1` distinct from `EvidenceRefV1` | 1, 3 | COVERED | Schema and authority tests pin separation. |
| CONTEXT | D-07 | `BusinessContextV1` aggregates safe facts/refs/missing/errors only | 1 | COVERED | Task 1 updates aggregation through service results. |
| CONTEXT | D-08 | `resource_version` / `data_freshness_at` exist and can be explicit null | 1 | COVERED | Schema tests pin presence and null values. |
| CONTEXT | D-09 | Scope proof before facts/refs for order/refund/ticket identifiers | 1, 2 | COVERED | Service final projection emits refs only after allowed result. |
| CONTEXT | D-10 | Resolve ToolPlatform `requires_domain_scope_check` marker | 2 | COVERED | ToolPlatform tests assert marker plus service-approved output. |
| CONTEXT | D-11 | Merchant-bound role/admin/unknown-role semantics | 1 | COVERED | Service tests cover support/manager/merchant/admin/missing/unknown. |
| CONTEXT | D-12 | No-leak service/tool permission denial | 1, 2, 3 | COVERED | Generic denial message, no refs/facts/prompt resource details. |
| CONTEXT | D-13 | Cross-tenant fail-closed no raw ids or adapter errors | 1 | COVERED | Service tests assert safe `permission_denied` / unavailable surfaces. |
| CONTEXT | D-14 | `BusinessToolExecutor` delegates reads to `BusinessFactService` | 2 | COVERED | Executor constructor/wiring updated. |
| CONTEXT | D-15 | ToolPlatform owns runtime auth; service owns domain proof/projection | 2 | COVERED | Task action preserves ToolRuntime gate order. |
| CONTEXT | D-16 | Projector/business_context/last refs free of denied facts/refs | 2, 3 | COVERED | Tool and graph tests assert empty refs/prompt summaries. |
| CONTEXT | D-17 | New tests target ToolPlatform -> executor -> service chain | 2 | COVERED | `tests/tools/test_tool_platform.py` adds chain coverage. |
| CONTEXT | D-18 | Start with RED tests for schema/service/no-leak/domain marker | 1, 2, 3 | COVERED | All tasks are marked `tdd="true"`. |
| CONTEXT | D-19 | Regression matrix for allow/deny/admin/invalid/unavailable/unsupported | 1, 2 | COVERED | Service tests cover all listed cases. |
| CONTEXT | D-20 | Authority-boundary tests for RAG/memory/LLM/prompt/raw rows | 3 | COVERED | Task 3 targets exact files. |
| CONTEXT | D-21 | Preserve Phase 29/29.5 behavior with focused suites | 1, 2, 3 | COVERED | Plan verification runs full Phase 30 focused suite including raw tool tests. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add BusinessFactResultV1 and BusinessFactService public contract</name>
  <files>src/business/schemas.py, src/business/service.py, src/business/__init__.py, tests/business/test_schemas.py, tests/business/test_service.py</files>
  <read_first>
- docs/contract-spec.md
- docs/target-agent-platform-architecture-plan.md
- .planning/phases/30-businessfactservice-boundary/30-CONTEXT.md
- .planning/phases/30-businessfactservice-boundary/30-RESEARCH.md
- .planning/phases/30-businessfactservice-boundary/30-VALIDATION.md
- .planning/phases/30-businessfactservice-boundary/30-PATTERNS.md
- .planning/phases/29.5-merchant-scope-role-model-alignment/29.5-02-SUMMARY.md
- .planning/phases/29.5-merchant-scope-role-model-alignment/29.5-03-SUMMARY.md
- .planning/todos/deferred/2026-06-27-merchant-scope-businessfactservice.md
- src/business/schemas.py
- src/business/service.py
- src/business/adapters.py
- src/business/__init__.py
- src/tools/contracts.py
- src/integrations/demo_business/authz.py
- tests/business/test_schemas.py
- tests/business/test_service.py
  </read_first>
  <behavior>
- Test `BusinessFactResultV1` accepts statuses `ok`, `partial`, `not_found`, `permission_denied`, `stale`, `unavailable`, and `invalid_request`.
- Test `BusinessFactResultV1` rejects unknown fields, requires `tenant_id`, `fact`, `business_fact_refs`, `source_system`, `scope_check_result`, `missing_required_facts`, and `safe_errors`, and preserves explicit `resource_version=None` and `data_freshness_at=None`.
- Test `BusinessFactRefV1` still fails `EvidenceRefV1.model_validate(...)`.
- Test `BusinessFactService.get_order`, `get_refund_case`, and `get_ticket` return `BusinessFactResultV1` with `status="ok"` and one matching `BusinessFactRefV1` for same-merchant/admin allowed reads.
- Test same-tenant cross-merchant, missing merchant binding, and unknown role reads return `status="permission_denied"`, `scope_check_result="denied"`, `fact is None`, `business_fact_refs == []`, and generic safe message `Business resource unavailable for this request`.
- Test cross-tenant reads fail closed with no fact, no refs, no raw id, and only the service's generic not-found or permission-denied safe surface.
- Test `get_logistics` and `get_merchant_risk` return `status="unavailable"`, no fact, no refs, and safe error code `BUSINESS_FACT_UNAVAILABLE`.
  </behavior>
  <action>
Implement D-01, D-02, D-04, D-05, D-06, D-07, D-08, D-09, D-11, D-12, D-13, D-18, and D-19.

In `src/business/schemas.py`, add strict Pydantic `BusinessFactResultV1` exactly matching `docs/contract-spec.md` section 8.4:
- `schema_version: Literal["business_fact_result.v1"] = "business_fact_result.v1"`
- `tenant_id: str`
- `status: Literal["ok", "partial", "not_found", "permission_denied", "stale", "unavailable", "invalid_request"]`
- `fact: dict[str, Any] | None`
- `business_fact_refs: list[BusinessFactRefV1]`
- `resource_version: str | None = None`
- `data_freshness_at: datetime | None = None`
- `source_system: str`
- `scope_check_result: Literal["allowed", "denied", "not_applicable", "unknown"]`
- `missing_required_facts: list[str]`
- `safe_errors: list[ToolError]`

Keep `BusinessFactRefV1` imported from `src.tools.contracts`; do not define a second business ref schema. Export `BusinessFactResultV1` and preserve existing `BusinessContextV1` compatibility fields. If `BusinessContextV1` needs adjustment, keep it strict, prompt-safe, and free of policy `EvidenceRefV1` values.

In `src/business/service.py`, add `BusinessFactService` as the authoritative current-business-fact boundary. Give it `with_default_registry(session)`, `has_tool(name)`, `fetch_context(slots, intent, ctx)`, `get_order(order_no, ctx)`, `get_refund_case(refund_case_no, ctx)`, `get_ticket(ticket_id, ctx)`, `get_logistics(tracking_no, ctx)`, and `get_merchant_risk(merchant_id, ctx)`. It may reuse existing adapter functions as private implementation helpers, but the public service methods must return `BusinessFactResultV1`.

Use deterministic result mapping:
- Adapter `ToolResultV2.status == "success"` -> `BusinessFactResultV1.status="ok"`, `scope_check_result="allowed"`, `fact=result.data`, refs copied from the adapter result, explicit nullable `resource_version` / `data_freshness_at` from the first ref when present or `None`.
- `partial_success` -> `status="partial"`.
- `not_found` -> `status="not_found"` with no fact/refs and safe error preserved only if it does not include raw ids.
- `permission_denied` -> `status="permission_denied"`, `scope_check_result="denied"`, no fact, no refs, and one safe `ToolError(code="BUSINESS_FACT_PERMISSION_DENIED", safe_message="Business resource unavailable for this request", retryable=False, source="caller")`.
- `timeout`, `unavailable`, `error`, `invalid_response`, and `conflict` -> `status="unavailable"`, no fact/refs, `scope_check_result="unknown"`, and safe code `BUSINESS_FACT_UNAVAILABLE` unless a more specific safe code is needed for tests.
- `invalid_request` -> `status="invalid_request"`, no fact/refs, safe code `BUSINESS_FACT_INVALID_REQUEST`.
- Explicit stale test doubles -> `status="stale"`, no fact/refs unless the stale fact is only used in a non-action-safe internal test; action-bound wrapping in Task 2 must fail closed.

For `get_logistics` and `get_merchant_risk`, return typed unavailable results with `source_system="business_fact_service"`, `scope_check_result="not_applicable"` for logistics and `"unknown"` for merchant risk unless explicit `merchant_id` scope denial is detected before the unavailable result. Do not emit facts, refs, or success-like summaries for these reads.

Update `BusinessContextV1` aggregation in `BusinessFactService.fetch_context(...)` so it derives reads from slots using the existing mapping (`order_id -> order`, `refund_case_id -> refund_case`, `ticket_id -> ticket`) and aggregates only `BusinessFactResultV1.status in {"ok", "partial"}` values with non-empty service-approved facts/refs. Denied, stale, unavailable, invalid, or not-found results must add missing required facts and safe errors, not facts or refs. Keep `BusinessToolService` present in this task only as a compatibility class; full delegation is Task 2.

Update `src/business/__init__.py` to export both `BusinessFactService` and `BusinessToolService`, plus `BusinessFactResultV1` if local package exports are used.
  </action>
  <acceptance_criteria>
- `rg -n "class BusinessFactResultV1" src/business/schemas.py` finds the schema.
- `rg -n "business_fact_result\\.v1|scope_check_result|missing_required_facts|safe_errors" src/business/schemas.py` finds all normative fields.
- `rg -n "class BusinessFactService|def fetch_context|def get_order|def get_refund_case|def get_ticket|def get_logistics|def get_merchant_risk" src/business/service.py` finds all public methods.
- `rg -n "Business resource unavailable for this request|BUSINESS_FACT_PERMISSION_DENIED|BUSINESS_FACT_UNAVAILABLE" src/business/service.py tests/business/test_service.py` finds pinned no-leak messages/codes.
- `rg -n "BusinessFactResultV1" tests/business/test_schemas.py tests/business/test_service.py` finds schema and service tests.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/business/test_schemas.py tests/business/test_service.py -q --tb=short</automated>
  </verify>
  <done>`BusinessFactResultV1` exists with the normative contract; `BusinessFactService` public methods return typed service results for supported and unsupported reads; service denials/unavailable/stale paths emit no facts or refs and use generic safe errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route ToolPlatform business reads through service-approved results</name>
  <files>src/business/service.py, src/tools/executors/business.py, tests/business/test_service.py, tests/business/test_adapters.py, tests/tools/test_tool_platform.py</files>
  <read_first>
- docs/contract-spec.md
- docs/target-agent-platform-architecture-plan.md
- .planning/phases/30-businessfactservice-boundary/30-CONTEXT.md
- .planning/phases/30-businessfactservice-boundary/30-RESEARCH.md
- .planning/phases/30-businessfactservice-boundary/30-VALIDATION.md
- .planning/phases/30-businessfactservice-boundary/30-PATTERNS.md
- .planning/phases/29-tool-platform-boundary/29-03-SUMMARY.md
- .planning/phases/29-tool-platform-boundary/29-04-SUMMARY.md
- .planning/phases/29.5-merchant-scope-role-model-alignment/29.5-03-SUMMARY.md
- src/business/service.py
- src/business/adapters.py
- src/tools/contracts.py
- src/tools/catalog.py
- src/tools/policy.py
- src/tools/runtime.py
- src/tools/platform.py
- src/tools/executors/business.py
- tests/business/test_service.py
- tests/business/test_adapters.py
- tests/tools/test_tool_platform.py
  </read_first>
  <behavior>
- Test `BusinessToolExecutor(session).service` is a `BusinessFactService` or a compatibility adapter that delegates to `BusinessFactService`.
- Test `BusinessToolExecutor.execute("get_order", ...)` returns `ToolResultV2` converted from a service-approved `BusinessFactResultV1`.
- Test a same-merchant allowed ToolPlatform `get_order` call has `outcome.policy_decision.resource_scope_binding["requires_domain_scope_check"] is True`, `status=="success"`, and exactly one `BusinessFactRefV1`.
- Test a same-tenant cross-merchant ToolPlatform `get_order` call with the same marker returns `status=="permission_denied"`, no `business_fact_refs`, `data is None`, and no denied order number in `summary`, `error.safe_message`, projection, or serialized outcome.
- Test `get_logistics` and `get_merchant_risk` are handled through the business executor and return safe unavailable outputs with no facts/refs when service support is unavailable.
- Test stale `BusinessFactResultV1` conversion produces fail-closed `ToolResultV2.status=="unavailable"` with no `data` or refs for action-bound callers.
  </behavior>
  <action>
Implement D-01, D-02, D-09, D-10, D-12, D-14, D-15, D-16, D-17, D-18, D-19, and D-21.

Update `src/tools/executors/business.py` so the default constructor uses the new service boundary. The executor remains thin:
- `has_tool(name)` delegates to the service.
- `execute(name, args, ctx)` calls a service method or service dispatch method and returns a `ToolResultV2` wrapper derived from a `BusinessFactResultV1`.
- It must not import raw demo integration modules or repositories.

In `src/business/service.py`, make `BusinessToolService` a compatibility adapter over `BusinessFactService` instead of the place where new authority logic accumulates. Preserve public methods currently used by tests and old callers:
- `BusinessToolService.with_default_registry(session)`
- `BusinessToolService.with_default_adapters(session)`
- `BusinessToolService.has_tool(name)`
- `BusinessToolService.invoke_tool(name, args, ctx) -> ToolResultV2`
- `BusinessToolService.fetch_context(slots, intent, ctx) -> BusinessContextV1`

Implement a deterministic wrapper from `BusinessFactResultV1` to `ToolResultV2` at the service/executor boundary:
- `ok` -> `ToolResultV2.status="success"` with `data=fact`, service-approved `business_fact_refs`, `policy_evidence_refs=[]`, and a bounded summary such as `Business fact read succeeded`.
- `partial` -> `ToolResultV2.status="partial_success"`.
- `not_found` -> `ToolResultV2.status="not_found"`, no data/refs, generic safe message.
- `permission_denied` -> `ToolResultV2.status="permission_denied"`, no data/refs, `ToolError(code="BUSINESS_FACT_PERMISSION_DENIED", safe_message="Business resource unavailable for this request", retryable=False, source="caller")`.
- `stale` -> `ToolResultV2.status="unavailable"`, no data/refs, `ToolError(code="BUSINESS_FACT_STALE", safe_message="Business resource unavailable for this request", retryable=False, source="adapter")`.
- `unavailable` -> `ToolResultV2.status="unavailable"`, no data/refs, safe code `BUSINESS_FACT_UNAVAILABLE`.
- `invalid_request` -> `ToolResultV2.status="invalid_request"`, no data/refs, safe code `BUSINESS_FACT_INVALID_REQUEST`.

Do not move descriptor permission checks, caller allowlist, side-effect checks, schema validation, approval checks, or idempotency checks into `BusinessFactService`; those stay in `ToolRuntime` / `ToolPolicyEngine` per D-15 and Phase 29 summaries. `BusinessFactService` owns domain scope proof, final fact/ref projection, freshness/status mapping, and no-leak semantics.

Add or update tests in `tests/tools/test_tool_platform.py` so the ToolPlatform -> BusinessToolExecutor -> BusinessFactService chain proves the `requires_domain_scope_check` marker cannot remain metadata-only. Use the current `ToolPolicyEngine` resource binding for `order_no`, `refund_case_no`, and `ticket_id`; the assertion must show that service denial prevents facts/refs even when runtime auth allowed dispatch.

Keep raw adapter tests in `tests/business/test_adapters.py` aligned with the new service boundary: adapters may still validate/project raw demo responses privately, but public authority tests should assert service-approved results rather than treating adapter-created refs as graph authority.
  </action>
  <acceptance_criteria>
- `rg -n "BusinessFactService" src/tools/executors/business.py src/business/service.py tests/tools/test_tool_platform.py` finds executor/service integration and tests.
- `rg -n "requires_domain_scope_check" tests/tools/test_tool_platform.py` finds a ToolPlatform chain test, not only a policy-unit test.
- `rg -n "BUSINESS_FACT_STALE|Business resource unavailable for this request" src/business/service.py tests/business/test_service.py tests/tools/test_tool_platform.py` finds stale/unavailable fail-closed coverage.
- `! rg -n "src\\.integrations\\.demo_business|src\\.repositories" src/tools/executors/business.py` succeeds; the business executor does not import raw integrations/repositories.
- `rg -n "get_logistics|get_merchant_risk" tests/business/test_service.py tests/tools/test_tool_platform.py` finds unsupported business read tests.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/business/test_service.py tests/business/test_adapters.py tests/tools/test_tool_platform.py -q --tb=short</automated>
  </verify>
  <done>ToolPlatform business reads wrap only service-approved domain results; `requires_domain_scope_check` is enforced through BusinessFactService; denied/stale/unavailable/unsupported reads produce no facts, refs, or prompt-useful resource details.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close graph, projection, and authority-substitution boundaries</name>
  <files>src/tools/projection.py, tests/agent/test_nodes/test_investigate.py, tests/agent/rag_context/test_authority_boundaries.py, tests/agent/test_policy_retrieval_ownership.py</files>
  <read_first>
- docs/contract-spec.md
- docs/target-agent-platform-architecture-plan.md
- docs/eval-test-plan.md
- .planning/phases/30-businessfactservice-boundary/30-CONTEXT.md
- .planning/phases/30-businessfactservice-boundary/30-RESEARCH.md
- .planning/phases/30-businessfactservice-boundary/30-VALIDATION.md
- .planning/phases/30-businessfactservice-boundary/30-PATTERNS.md
- .planning/phases/29-tool-platform-boundary/29-04-SUMMARY.md
- .planning/phases/29.5-merchant-scope-role-model-alignment/29.5-06-SUMMARY.md
- src/tools/projection.py
- src/agent/nodes/investigate.py
- tests/agent/test_nodes/test_investigate.py
- tests/agent/rag_context/test_authority_boundaries.py
- tests/agent/test_policy_retrieval_ownership.py
  </read_first>
  <behavior>
- Test `ToolResultProjector` does not produce `business_fact_refs`, `resource_refs`, or prompt business refs from `result.data` fields such as `order_no`, `refund_case_no`, `ticket_id`, `tracking_no`, or `merchant_id` when `result.business_fact_refs == []`.
- Test projector still carries envelope `result.business_fact_refs` when present.
- Test `investigate` denied/stale/unavailable business results do not populate `business_context.facts`, `business_context.business_fact_refs`, `last_business_context_refs.business_fact_refs`, prompt summaries, or claim dependencies with caller-supplied denied ids.
- Test memory, RAG/policy evidence, model knowledge, prompt summaries, and raw repository-row-shaped dicts cannot satisfy a `business_fact_claim` without `BusinessFactRefV1`.
- Test graph/tool boundary source inspection shows `src.agent.nodes.investigate` does not import `BusinessFactService`, `BusinessToolService`, raw demo integrations, or business repositories; it must use ToolPlatform projections only.
  </behavior>
  <action>
Implement D-03, D-06, D-12, D-16, D-18, D-20, and D-21.

In `src/tools/projection.py`, remove business-ref authority derived only from raw-ish `ToolResultV2.data`. The projector may continue to expose safe scalar values such as `status` or `summary`, but `business_fact_refs`, prompt `business_fact_refs`, and prompt `resource_refs` for business facts must come from `result.business_fact_refs` after BusinessFactService scope proof. If `_extract_business_fact_refs(data)` remains, it must not feed graph/prompt business refs; it can be deleted or restricted to non-authority debug counts that never enter prompt or normalized authority fields. Keep policy evidence refs sourced only from `result.policy_evidence_refs`.

In tests, add explicit denial/stale/unavailable graph cases using fake `ToolInvocationOutcome` / `ToolResultV2` values:
- `permission_denied` result with caller-supplied `ORD-DENIED-30` in args must not serialize that id into prompt summaries, `business_context`, `last_business_context_refs`, or `claim_dependency_map`.
- `unavailable` result with safe error `BUSINESS_FACT_UNAVAILABLE` must leave facts/refs empty.
- service stale converted to `ToolResultV2.status="unavailable"` and safe error `BUSINESS_FACT_STALE` must leave facts/refs empty and route as fail-closed investigation output.

Extend authority-boundary tests so a `business_fact_claim` with policy evidence, case/session memory, model knowledge, prompt summary text, or a raw repository-row-shaped dict but no `BusinessFactRefV1` is not supported. The expected reason-code set must include `business_fact_ref_required`; do not implement Phase 33 claim verification expansion beyond the negative boundary cases already available.

Extend ownership tests so graph/tool code cannot substitute raw repository rows:
- inspect `src.agent.nodes.investigate` and assert it exposes/uses `ToolPlatform` but not `BusinessFactService`, `BusinessToolService`, `src.integrations.demo_business`, `OrderRepository`, `RefundRepository`, or `TicketRepository`.
- inspect `src.tools.executors.business` and assert it imports `BusinessFactService` / compatibility service only, not raw demo integrations or repositories.

Do not implement Phase 31 memory isolation, Phase 33 full RAG claim verification, Phase 34 approval/action binding, Phase 35 replay/eval broad gates, Phase 36 database constraints/RLS, physical microservices, or real external execution.
  </action>
  <acceptance_criteria>
- `rg -n "_extract_business_fact_refs\\(|result\\.business_fact_refs|business_fact_refs" src/tools/projection.py` shows business refs are sourced from `result.business_fact_refs`, not raw data-only inference.
- `rg -n "ORD-DENIED-30|BUSINESS_FACT_STALE|BUSINESS_FACT_UNAVAILABLE|service-approved" tests/agent/test_nodes/test_investigate.py` finds graph no-leak/fail-closed tests.
- `rg -n "raw repository|model knowledge|prompt summar|business_fact_ref_required" tests/agent/rag_context/test_authority_boundaries.py tests/agent/test_policy_retrieval_ownership.py` finds authority-substitution tests.
- `rg -n "BusinessFactService|BusinessToolService|OrderRepository|RefundRepository|TicketRepository|integrations\\.demo_business" tests/agent/test_policy_retrieval_ownership.py` finds static boundary assertions.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/agent/test_nodes/test_investigate.py tests/agent/rag_context/test_authority_boundaries.py tests/agent/test_policy_retrieval_ownership.py -q --tb=short</automated>
  </verify>
  <done>Graph and prompt projections consume only service-approved business refs, and tests prove memory/RAG/LLM/prompt/raw rows cannot substitute current business facts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| ToolRuntime -> BusinessToolExecutor | ToolPlatform has authorized descriptor/caller/schema/scope metadata; domain ownership proof still begins here. |
| BusinessFactService -> demo business adapters/repositories | Private implementation seam that can see raw rows/payloads but must return strict no-leak domain results. |
| BusinessFactService -> ToolResultV2 wrapper | Domain facts are converted to graph/tool envelopes; denied/stale/unavailable paths must not gain refs or prompt summaries. |
| ToolResultProjector -> investigate graph state/prompt summaries | Only projected, service-approved refs and safe errors may enter graph state or prompt-facing records. |
| Memory/RAG/LLM/context -> business fact claims | Contextual systems cannot satisfy current business fact authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-30-01 | Information Disclosure | `BusinessFactService.get_order/get_refund_case/get_ticket` | mitigate | Generic `permission_denied` safe message `Business resource unavailable for this request`, no facts, no refs, and no caller-supplied denied id in service/tool/graph/prompt outputs. |
| T-30-02 | Information Disclosure | `src/business/adapters.py`, `src/tools/projection.py`, `src/agent/nodes/investigate.py` | mitigate | Strict `BusinessFactResultV1` projection, adapter raw payload discard, projector refs from result envelope only, graph consumes projection instead of raw `ToolResultV2.data`. |
| T-30-03 | Tampering / Information Disclosure | authority-boundary claim and graph tests | mitigate | Business fact claims require `BusinessFactRefV1`; memory, RAG/policy evidence, LLM/model knowledge, prompt summaries, and raw rows are negative-only inputs. |
| T-30-04 | Elevation of Privilege | `ToolPolicyEngine.resource_scope_binding` + `BusinessToolExecutor` | mitigate | Tests assert `requires_domain_scope_check` is present for order/refund/ticket ids and final facts/refs are emitted only after BusinessFactService approval. |
| T-30-05 | Tampering / Repudiation | `get_logistics`, `get_merchant_risk` | mitigate | Unsupported reads return typed unavailable domain/tool results with no facts, no refs, and safe code `BUSINESS_FACT_UNAVAILABLE`; prompt summaries cannot imply data exists. |
</threat_model>

<verification>
Run after all tasks:

```bash
uv run pytest tests/business/test_service.py tests/business/test_adapters.py tests/business/test_schemas.py tests/tools/test_tool_platform.py tests/agent/test_nodes/test_investigate.py tests/agent/test_tools/test_get_order.py tests/agent/test_tools/test_get_refund_case.py tests/agent/test_tools/test_get_ticket.py tests/agent/rag_context/test_authority_boundaries.py tests/agent/test_policy_retrieval_ownership.py -q --tb=short
uv run ruff check src/business/schemas.py src/business/service.py src/business/__init__.py src/tools/executors/business.py src/tools/projection.py tests/business/test_schemas.py tests/business/test_service.py tests/business/test_adapters.py tests/tools/test_tool_platform.py tests/agent/test_nodes/test_investigate.py tests/agent/rag_context/test_authority_boundaries.py tests/agent/test_policy_retrieval_ownership.py
git diff --check
```
</verification>

<success_criteria>
1. APF-08 appears in the summary `requirements-completed` field after execution.
2. `BusinessFactResultV1` exists in `src/business/schemas.py` with the normative status enum, freshness/version fields, `scope_check_result`, `missing_required_facts`, and `safe_errors`.
3. `BusinessFactService` public methods exist for `fetch_context`, `get_order`, `get_refund_case`, `get_ticket`, `get_logistics`, and `get_merchant_risk`.
4. Data-backed order/refund/ticket reads emit `BusinessFactRefV1` only after service-approved scope proof; denied, missing, stale, unavailable, invalid, and unsupported reads emit no facts or refs.
5. `BusinessToolExecutor` delegates through `BusinessFactService`; ToolPlatform runtime auth still owns descriptor/caller/permission/schema/side-effect gates.
6. `ToolPolicyEngine` `requires_domain_scope_check` markers for order/refund/ticket identifiers are covered by tests proving service enforcement.
7. `ToolResultProjector`, `investigate`, `business_context`, prompt summaries, and `last_business_context_refs` do not derive business refs from raw data or denied results.
8. Tests prove memory, RAG/policy evidence, LLM/model knowledge, prompt summaries, and raw repository rows cannot substitute missing or denied current business facts.
9. Phase 31 memory isolation, Phase 33 full claim verification, Phase 34 approval/action target binding, Phase 35 replay/eval broad hardening, Phase 36+ DB/RLS work, physical microservices, and real external execution remain untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/30-businessfactservice-boundary/30-01-SUMMARY.md` using the summary template and set `requirements-completed: [APF-08]`.
</output>
