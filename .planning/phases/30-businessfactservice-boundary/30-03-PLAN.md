---
phase: 30-businessfactservice-boundary
plan: 03
type: execute
wave: 3
depends_on: ["30-02"]
files_modified:
  - src/tools/projection.py
  - tests/agent/test_nodes/test_investigate.py
  - tests/agent/rag_context/test_authority_boundaries.py
  - tests/agent/test_policy_retrieval_ownership.py
autonomous: true
requirements: [APF-08]
must_haves:
  truths:
    - "ToolResultProjector does not infer authoritative business refs from raw result.data."
    - "Investigate graph state, prompt summaries, business_context, last_business_context_refs, and claim dependencies include only service-approved business refs."
    - "Memory, RAG/policy evidence, LLM/model knowledge, prompt summaries, and raw repository-row-shaped data cannot substitute missing or denied current business facts."
  artifacts:
    - path: "src/tools/projection.py"
      provides: "Projection business refs sourced from ToolResultV2 envelope only"
      contains: "result.business_fact_refs"
    - path: "tests/agent/test_nodes/test_investigate.py"
      provides: "Graph no-leak tests for denied/stale/unavailable business results"
      contains: "BUSINESS_FACT_STALE"
    - path: "tests/agent/rag_context/test_authority_boundaries.py"
      provides: "Authority-substitution negative tests for memory/RAG/LLM/prompt summaries"
      contains: "business_fact_ref_required"
    - path: "tests/agent/test_policy_retrieval_ownership.py"
      provides: "Static ownership tests for graph/executor boundaries and ref separation"
      contains: "BusinessFactService"
  key_links:
    - from: "src/tools/projection.py"
      to: "src/agent/nodes/investigate.py"
      via: "ToolInvocationOutcome.projection consumed by investigate without raw data authority"
      pattern: "projection\\.normalized_result"
    - from: "src/agent/nodes/investigate.py"
      to: "src/tools/platform.py"
      via: "graph invokes ToolPlatform rather than business service or repositories"
      pattern: "ToolPlatform"
    - from: "tests/agent/rag_context/test_authority_boundaries.py"
      to: "docs/contract-spec.md"
      via: "business fact claims require BusinessFactRefV1"
      pattern: "business_fact_ref_required"
---

<objective>
Close the graph, projection, and authority-substitution boundaries for APF-08 and run the final focused Phase 30 verification.

Purpose: After Plan 02 routes business reads through the service, this plan prevents graph/prompt projection from recreating business authority from raw data and proves memory/RAG/LLM/prompt/raw rows cannot substitute current business facts.

Output: Projector hardening, graph no-leak tests, authority-boundary tests, and final focused verification command.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-businessfactservice-boundary/30-CONTEXT.md
@.planning/phases/30-businessfactservice-boundary/30-RESEARCH.md
@.planning/phases/30-businessfactservice-boundary/30-VALIDATION.md
@.planning/phases/30-businessfactservice-boundary/30-PATTERNS.md
@.planning/phases/30-businessfactservice-boundary/30-01-SUMMARY.md
@.planning/phases/30-businessfactservice-boundary/30-02-SUMMARY.md
@docs/contract-spec.md
@docs/target-agent-platform-architecture-plan.md
@docs/eval-test-plan.md
@.planning/phases/29-tool-platform-boundary/29-04-SUMMARY.md
@.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-06-SUMMARY.md
@src/tools/projection.py
@src/agent/nodes/investigate.py
@src/tools/executors/business.py
@tests/agent/test_nodes/test_investigate.py
@tests/agent/rag_context/test_authority_boundaries.py
@tests/agent/test_policy_retrieval_ownership.py

`src/agent/nodes/investigate.py` is a required reference file for this plan, but it is not a planned source edit unless tests prove graph source behavior must change. The intended source edit is `src/tools/projection.py`.
</context>

<source_audit>
| Source | ID | Requirement / Decision | Plan Coverage | Status |
| --- | --- | --- | --- | --- |
| GOAL | Phase 30 | Graph/tool code cannot substitute non-service data for business facts | Tasks 1-3 | COVERED |
| REQ | APF-08 | Memory/RAG/LLM/raw repository rows cannot substitute current business facts | Tasks 2-3 | COVERED |
| CONTEXT | D-03 | Repositories remain helpers; graph/tool consumers do not use raw rows as authority | Task 3 | COVERED |
| CONTEXT | D-06 | BusinessFactRefV1 distinct from EvidenceRefV1 | Task 3 | COVERED |
| CONTEXT | D-12 | No-leak service/tool denial reaches graph/prompt outputs | Task 2 | COVERED |
| CONTEXT | D-16 | Projector, business_context, and last refs free of denied facts/refs | Tasks 1-2 | COVERED |
| CONTEXT | D-18 | RED tests include no-leak/domain-boundary behavior | Tasks 1-3 | COVERED |
| CONTEXT | D-20 | Authority-boundary tests for RAG/memory/LLM/prompt/raw rows | Task 3 | COVERED |
| CONTEXT | D-21 | Focused tests cover business, tools, investigate, raw tool, and authority files | Verification | COVERED |
| RESEARCH | projection | Remove data-only business-ref authority from ToolResultProjector | Task 1 | COVERED |
| RESEARCH | stale/unavailable | Fail closed for stale/unavailable action-bound paths | Task 2 | COVERED |
| DEFERRED | Phase 31+ | Full memory isolation, full claim verifier expansion, approval/action binding, replay/eval broad gates | Excluded from action text | EXCLUDED |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Source projection business refs only from service-approved envelopes</name>
  <files>src/tools/projection.py, tests/agent/test_nodes/test_investigate.py</files>
  <read_first>
- .planning/phases/30-businessfactservice-boundary/30-02-SUMMARY.md
- docs/contract-spec.md
- .planning/phases/30-businessfactservice-boundary/30-CONTEXT.md
- .planning/phases/30-businessfactservice-boundary/30-RESEARCH.md
- .planning/phases/30-businessfactservice-boundary/30-PATTERNS.md
- src/tools/projection.py
- src/agent/nodes/investigate.py
- tests/agent/test_nodes/test_investigate.py
  </read_first>
  <behavior>
- Test `ToolResultProjector` does not produce `business_fact_refs`, prompt `business_fact_refs`, or prompt `resource_refs` from `result.data` keys such as `order_no`, `refund_case_no`, `ticket_id`, `tracking_no`, or `merchant_id` when `result.business_fact_refs == []`.
- Test `ToolResultProjector` still carries envelope `result.business_fact_refs` when present.
- Test raw sentinels remain stripped from normalized, prompt, text, and debug projections.
  </behavior>
  <action>
Implement D-03, D-12, D-16, D-18, and D-20.

In `src/tools/projection.py`, remove authoritative business-ref extraction from `ToolResultV2.data`. `normalized_result["business_fact_refs"]`, prompt `business_fact_refs`, and prompt `resource_refs` for business facts must come from `result.business_fact_refs` only. Safe scalar fields such as status or bounded summary may remain. If `_extract_business_fact_refs(data)` remains for diagnostic counts, it must not feed graph, prompt, resource, or normalized authority fields.

Do not edit `src/agent/nodes/investigate.py` for this task unless the projector-only change cannot satisfy the tests. If an investigate source edit is unavoidable, keep it limited to consuming `ToolInvocationOutcome.projection` and never import `BusinessFactService`, `BusinessToolService`, raw demo integrations, or repositories.
  </action>
  <acceptance_criteria>
- `rg -n "_extract_business_fact_refs\\(|result\\.business_fact_refs|business_fact_refs" src/tools/projection.py` shows business refs are sourced from `result.business_fact_refs`, not data-only inference.
- `rg -n "resource_refs|business_fact_refs" tests/agent/test_nodes/test_investigate.py` finds projector envelope-vs-data tests.
- `! rg -n "BusinessFactService|BusinessToolService|src\\.integrations\\.demo_business|OrderRepository|RefundRepository|TicketRepository" src/agent/nodes/investigate.py` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/agent/test_nodes/test_investigate.py -q --tb=short</automated>
  </verify>
  <done>Projection no longer turns raw data-shaped business identifiers into authoritative business refs; envelope refs still project correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove investigate no-leak behavior for denied, stale, and unavailable business results</name>
  <files>tests/agent/test_nodes/test_investigate.py</files>
  <read_first>
- .planning/phases/30-businessfactservice-boundary/30-02-SUMMARY.md
- docs/contract-spec.md
- .planning/phases/30-businessfactservice-boundary/30-CONTEXT.md
- .planning/phases/30-businessfactservice-boundary/30-RESEARCH.md
- .planning/phases/30-businessfactservice-boundary/30-PATTERNS.md
- src/agent/nodes/investigate.py
- src/tools/projection.py
- tests/agent/test_nodes/test_investigate.py
  </read_first>
  <behavior>
- Test a `permission_denied` business result with caller-supplied `ORD-DENIED-30` in args does not serialize that id into prompt summaries, `business_context`, `last_business_context_refs`, or `claim_dependency_map`.
- Test `unavailable` with `BUSINESS_FACT_UNAVAILABLE` leaves facts and business refs empty.
- Test stale service result converted to `ToolResultV2.status=="unavailable"` with safe code `BUSINESS_FACT_STALE` leaves facts/refs empty and fails closed.
  </behavior>
  <action>
Implement D-12, D-16, D-18, D-20, and D-21.

Add focused graph tests using the existing fake platform / fake manager patterns in `tests/agent/test_nodes/test_investigate.py`. Keep graph assertions on the projected outputs already returned by `investigate(...)`: `business_context`, `tool_results`, `last_business_context_refs`, `claim_dependency_map`, and `termination_reason`.

Do not implement Phase 31 memory isolation, Phase 33 full claim verification, Phase 34 approval/action binding, Phase 35 replay/eval broad gates, Phase 36 database constraints/RLS, physical microservices, or real external execution.
  </action>
  <acceptance_criteria>
- `rg -n "ORD-DENIED-30|BUSINESS_FACT_STALE|BUSINESS_FACT_UNAVAILABLE" tests/agent/test_nodes/test_investigate.py` finds graph no-leak/fail-closed tests.
- `rg -n "last_business_context_refs|claim_dependency_map|prompt_summary" tests/agent/test_nodes/test_investigate.py` finds assertions on all graph/prompt surfaces.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/agent/test_nodes/test_investigate.py -q --tb=short</automated>
  </verify>
  <done>Investigate graph outputs stay free of denied/stale/unavailable business facts and refs, including prompt and dependency surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add authority-boundary tests and run focused Phase 30 gate</name>
  <files>tests/agent/rag_context/test_authority_boundaries.py, tests/agent/test_policy_retrieval_ownership.py</files>
  <read_first>
- .planning/phases/30-businessfactservice-boundary/30-02-SUMMARY.md
- docs/contract-spec.md
- docs/target-agent-platform-architecture-plan.md
- .planning/phases/30-businessfactservice-boundary/30-CONTEXT.md
- .planning/phases/30-businessfactservice-boundary/30-RESEARCH.md
- .planning/phases/30-businessfactservice-boundary/30-VALIDATION.md
- .planning/phases/30-businessfactservice-boundary/30-PATTERNS.md
- src/agent/nodes/investigate.py
- src/tools/executors/business.py
- tests/agent/rag_context/test_authority_boundaries.py
- tests/agent/test_policy_retrieval_ownership.py
  </read_first>
  <behavior>
- Test memory, RAG/policy evidence, model knowledge, prompt summaries, and raw repository-row-shaped dicts cannot support a `business_fact_claim` without `BusinessFactRefV1`.
- Test expected reason codes include `business_fact_ref_required`.
- Test `src.agent.nodes.investigate` exposes/uses `ToolPlatform` but does not import `BusinessFactService`, `BusinessToolService`, raw demo integrations, `OrderRepository`, `RefundRepository`, or `TicketRepository`.
- Test `src.tools.executors.business` imports the service boundary and does not import raw demo integrations or repositories.
  </behavior>
  <action>
Implement D-03, D-06, D-20, and D-21.

Extend authority-boundary tests without building Phase 33 claim verification. Use the existing `MaterialClaimVerifier` negative paths and add only the APF-08 negative cases needed to prove non-authoritative sources cannot substitute business facts. Keep expected behavior fail-closed and reason-code based.

Extend ownership tests using `inspect.getsource(...)` or module attribute checks. `investigate` must remain graph/tool-platform orchestration only; it must not directly import business service facades, raw demo integrations, or repositories. `BusinessToolExecutor` may import the service boundary, but must not import raw integrations or repositories.

After tests pass, run the full Phase 30 focused gate from `30-VALIDATION.md`.
  </action>
  <acceptance_criteria>
- `rg -n "raw repository|model knowledge|prompt summar|business_fact_ref_required" tests/agent/rag_context/test_authority_boundaries.py tests/agent/test_policy_retrieval_ownership.py` finds authority-substitution assertions.
- `rg -n "BusinessFactService|BusinessToolService|OrderRepository|RefundRepository|TicketRepository|integrations\\.demo_business" tests/agent/test_policy_retrieval_ownership.py` finds static boundary assertions.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/agent/rag_context/test_authority_boundaries.py tests/agent/test_policy_retrieval_ownership.py -q --tb=short && uv run pytest tests/business/test_service.py tests/business/test_adapters.py tests/business/test_schemas.py tests/tools/test_tool_platform.py tests/agent/test_nodes/test_investigate.py tests/agent/test_tools/test_get_order.py tests/agent/test_tools/test_get_refund_case.py tests/agent/test_tools/test_get_ticket.py tests/agent/rag_context/test_authority_boundaries.py tests/agent/test_policy_retrieval_ownership.py -q --tb=short</automated>
  </verify>
  <done>Authority-boundary tests prove APF-08 substitution bans, static ownership boundaries hold, and the full Phase 30 focused suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| ToolResultV2 envelope -> ToolResultProjector | Only service-approved envelope refs may become business ref projections. |
| ToolInvocationOutcome -> investigate graph state | Graph consumes projection and result envelope without raw data authority. |
| Memory/RAG/LLM/prompt/raw rows -> business fact claims | Contextual or policy systems cannot satisfy current-business-fact authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-30-09 | Information Disclosure | `src/tools/projection.py` | mitigate | Remove data-only business-ref/resource-ref projection; refs come from `result.business_fact_refs` only. |
| T-30-10 | Information Disclosure | `investigate` graph state and prompt summaries | mitigate | Tests assert denied/stale/unavailable ids do not appear in facts, refs, prompt summaries, or dependencies. |
| T-30-11 | Tampering | material business fact claims | mitigate | Tests assert memory, RAG, LLM/model knowledge, prompt summaries, and raw rows cannot satisfy business fact claims without `BusinessFactRefV1`. |
| T-30-12 | Elevation of Privilege | graph and executor source boundaries | mitigate | Static tests prevent graph direct business service/repository imports and executor raw integration/repository imports. |
</threat_model>

<verification>
Final Phase 30 focused verification:

```bash
uv run pytest tests/business/test_service.py tests/business/test_adapters.py tests/business/test_schemas.py tests/tools/test_tool_platform.py tests/agent/test_nodes/test_investigate.py tests/agent/test_tools/test_get_order.py tests/agent/test_tools/test_get_refund_case.py tests/agent/test_tools/test_get_ticket.py tests/agent/rag_context/test_authority_boundaries.py tests/agent/test_policy_retrieval_ownership.py -q --tb=short
uv run ruff check src/business/schemas.py src/business/service.py src/business/__init__.py src/tools/executors/business.py src/tools/projection.py tests/business/test_schemas.py tests/business/test_service.py tests/business/test_adapters.py tests/tools/test_tool_platform.py tests/agent/test_nodes/test_investigate.py tests/agent/rag_context/test_authority_boundaries.py tests/agent/test_policy_retrieval_ownership.py
git diff --check
```
</verification>

<success_criteria>
1. `ToolResultProjector` derives business refs and prompt resource refs only from service-approved `ToolResultV2.business_fact_refs`.
2. `investigate` graph state and prompt summaries remain free of denied/stale/unavailable business ids, facts, and refs.
3. Tests prove memory, RAG/policy evidence, LLM/model knowledge, prompt summaries, and raw repository-row-shaped data cannot substitute current business facts.
4. Static ownership tests prove graph nodes do not import business services or repositories, while the business executor does not import raw repositories/integrations.
5. The final Phase 30 focused suite, ruff check, and `git diff --check` pass.
6. Phase 31 memory isolation, Phase 33 full claim verification, Phase 34 approval/action target binding, Phase 35 replay/eval broad hardening, Phase 36+ DB/RLS work, physical microservices, and real external execution remain untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/30-businessfactservice-boundary/30-03-SUMMARY.md` using the summary template and set `requirements-completed: [APF-08]`.
</output>
