---
phase: 47-case-precedent-repositioning-and-closed-case-candidate-gener
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/contract-spec.md
  - docs/current-implementation-map.md
  - docs/architecture-overview.md
  - .planning/MEMORY-REDESIGN-DECISIONS.md
  - src/memory/schemas.py
  - src/memory/policy.py
  - tests/memory/test_phase47_case_precedent_alignment.py
  - tests/memory/test_memory_policy.py
autonomous: true
requirements: [MEM-04]
requirements_addressed: [MEM-04]
must_haves:
  truths:
    - "case_memories is documented and test-locked as reviewed closed-case precedent, not active working state"
    - "closed-case CWC candidates enter the existing case-memory review workflow as needs_review"
    - "metadata/text retrieval remains first-class and embeddings remain optional"
    - "projection boundaries forbid policy body text, raw tool payloads, authority bodies, replay/debug blobs, and sensitive raw PII"
    - "case_memories, long_term_memories, case_working_contexts, and conversation_threads.case_id are not renamed, dropped, or replaced"
    - "DEFER-3 remains named as Phase 48 scope and is not implemented"
  artifacts:
    - path: "tests/memory/test_phase47_case_precedent_alignment.py"
      provides: "Phase 47 static semantic, red-line, and verification-entrypoint guards"
      contains: "closed_case_cwc_candidate"
    - path: "src/memory/schemas.py"
      provides: "CaseMemorySourceType includes closed_case_cwc_candidate"
      contains: "\"closed_case_cwc_candidate\""
    - path: "src/memory/policy.py"
      provides: "closed_case_cwc_candidate is review-required and not auto-approved"
      contains: "REVIEW_REQUIRED_CASE_SOURCE_TYPES"
  key_links:
    - from: "src/memory/policy.py"
      to: "src/memory/case_memory.py"
      via: "case_memory_policy_decision used by submit_case_memory_candidate"
      pattern: "case_memory_policy_decision"
    - from: "tests/memory/test_phase47_case_precedent_alignment.py"
      to: "src/api/services/agent_run_memory.py"
      via: "static guard that completed agent runs do not trigger closed-case candidates"
      pattern: "final_status.*completed"
---

<objective>
Lock the Phase 47 contract and policy foundation before any closed-case projection service exists.

Purpose: `case_memories` must keep its table/model identity while becoming executable as reviewed closed-case precedent. The new source type must default to review-required so later candidate generation cannot accidentally publish precedents.
Output: docs/static tests for the semantic boundary, protected table red lines, approved pytest entrypoint guard, DEFER-3 carry-forward, and the `closed_case_cwc_candidate` source type classified as review-required only.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/MEMORY-REDESIGN-DECISIONS.md
@.planning/phases/47-case-precedent-repositioning-and-closed-case-candidate-gener/47-CONTEXT.md
@.planning/phases/47-case-precedent-repositioning-and-closed-case-candidate-gener/47-RESEARCH.md
@.planning/phases/47-case-precedent-repositioning-and-closed-case-candidate-gener/47-VALIDATION.md
@.planning/phases/47-case-precedent-repositioning-and-closed-case-candidate-gener/47-PATTERNS.md
@.planning/phases/46-session-context-repositioning/46-03-SUMMARY.md
@docs/contract-spec.md
@docs/current-implementation-map.md
@docs/architecture-overview.md
@src/memory/schemas.py
@src/memory/policy.py
@tests/memory/test_memory_policy.py
@tests/memory/test_phase45_contract_alignment.py
@tests/memory/test_phase46_session_context_alignment.py

<interfaces>
From `src/memory/schemas.py`:
```python
class MemorySourceRefV1(BaseModel):
    model_config = ConfigDict(extra="forbid")
    source_type: str
    run_id: str | None = None
    event_id: str | None = None
    conversation_message_id: str | None = None
    tool_result_id: str | None = None
    agent_run_id: str | None = None
    business_object_type: str | None = None
    business_object_id: str | None = None
    policy_version: str | None = None
    outcome_id: str | None = None

CaseMemorySourceType = Literal[
    "explicit_admin_preference",
    "human_reviewed",
    "deterministic_tool_result",
    "confirmed_business_outcome",
    "approved_approval_state",
    "llm_candidate",
    "semantic_episode_candidate",
    "summary_candidate",
    "cross_case_pattern_candidate",
    "behavior_inference",
]
```

From `src/memory/policy.py`:
```python
AUTO_APPROVED_CASE_SOURCE_TYPES = frozenset({"explicit_admin_preference", "human_reviewed"})
REVIEW_REQUIRED_CASE_SOURCE_TYPES = frozenset({
    "deterministic_tool_result",
    "confirmed_business_outcome",
    "approved_approval_state",
    "llm_candidate",
    "semantic_episode_candidate",
    "summary_candidate",
    "cross_case_pattern_candidate",
    "behavior_inference",
})
```
</interfaces>

<source_audit>
SOURCE | ID | Feature / Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | Phase 47 | Reposition `case_memories` as reviewed closed-case precedent and add governed CWC-to-review candidate path | 47-01, 47-02, 47-03, 47-04 | COVERED | Contract/policy foundation, service projection, governed write lifecycle, retrieval/docs validation.
REQ | MEM-04 | Reviewed precedent semantics, governed candidate generation, metadata-first retrieval, needs_review/audit behavior, no destructive rename | 47-01, 47-02, 47-03, 47-04 | COVERED | Every plan includes MEM-04.
RESEARCH | Dedicated source type | Add `closed_case_cwc_candidate` or justify alternative | 47-01 | COVERED | Dedicated source type chosen per default expectation.
RESEARCH | Trusted close seam | Do not infer closure from completed AgentRun or add public close endpoint | 47-01, 47-02 | COVERED | Static guard plus internal service seam.
RESEARCH | Deterministic projection | Allowlisted CWC projection with raw payload/authority/PII blocks | 47-02, 47-03 | COVERED | Service projection and write tests.
RESEARCH | Existing review pipeline | Use `CaseMemoryService.submit_case_memory_candidate(...)`, pending review, review events | 47-03 | COVERED | No second queue/store.
RESEARCH | Metadata-first retrieval | Retrieval works without embeddings for merchant and exact case scope | 47-04 | COVERED | Query embedding remains optional.
CONTEXT | D-47-01 | Preserve table identity; CWC is active state and case_memories is reviewed precedent | 47-01, 47-04 | COVERED | Docs/static tests.
CONTEXT | D-47-02 | Candidate generation may be automatic only to needs_review; retrieval invisible until approval | 47-01, 47-03 | COVERED | Source policy and review/retrieval tests.
CONTEXT | D-47-03 | Reuse existing case memory review pipeline; no second queue/table/store | 47-01, 47-03 | COVERED | Policy and write lifecycle.
CONTEXT | D-47-04 | Trusted close trigger; no AgentRun.completed inference; explicit status allowlist | 47-01, 47-02 | COVERED | Static and service tests.
CONTEXT | D-47-05 | Source CWC is finalized contextual-only snapshot; skip if missing/blocked | 47-02, 47-03 | COVERED | Service skip/projection tests.
CONTEXT | D-47-06 | Projection deterministic and allowlisted; claims/facts separate; forbidden payloads blocked | 47-02, 47-03 | COVERED | Projection and static forbidden-token tests.
CONTEXT | D-47-07 | Retrieval scope separate from source identity; merchant scope or exact case fallback; no ToolCallContext case id | 47-02, 47-04 | COVERED | Scope resolution and tool contract tests.
CONTEXT | D-47-08 | Metadata-first retrieval; embeddings optional only | 47-04 | COVERED | Retrieval tests use `query_embedding=None`.
CONTEXT | D-47-09 | Idempotent writes and audit using existing identity hashes/events | 47-03 | COVERED | Source_ref event/outcome identity and duplicate tests.
CONTEXT | D-47-10 | MOCA-approved pytest entrypoint | 47-01, 47-02, 47-03, 47-04 | COVERED | All automated pytest commands use `UV_CACHE_DIR=/tmp/uv-cache uv run pytest`.
DEFERRED | Phase 48 | DEFER-3 explicit long-term preference memory | none | EXCLUDED | Carried by name only.
</source_audit>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock case precedent semantics and static red lines</name>
  <read_first>
    - docs/contract-spec.md Section 13.4 and 13.4a
    - docs/current-implementation-map.md memory rows for `search_case_memory`
    - docs/architecture-overview.md memory layer bullets
    - .planning/MEMORY-REDESIGN-DECISIONS.md DEFER-2 and DEFER-3
    - .planning/phases/47-case-precedent-repositioning-and-closed-case-candidate-gener/47-CONTEXT.md decisions D-47-01 through D-47-10
    - tests/memory/test_phase45_contract_alignment.py
    - tests/memory/test_phase46_session_context_alignment.py
  </read_first>
  <files>docs/contract-spec.md, docs/current-implementation-map.md, docs/architecture-overview.md, .planning/MEMORY-REDESIGN-DECISIONS.md, tests/memory/test_phase47_case_precedent_alignment.py</files>
  <action>
    Update `docs/contract-spec.md` near Section 13.4 / 13.4a with these exact Phase 47 semantics: `case_memories` and `case_memory` are reviewed closed-case precedent, NOT active case state; active current-case state remains `case_working_contexts`; finalized CWC may be projected only into a `CaseMemoryWriteCandidate` submitted through `CaseMemoryService.submit_case_memory_candidate(...)`; generated closed-case candidates default to `needs_review` and are invisible to `retrieve_reviewed(...)`, `reviewed_memory_context`, and `search_case_memory` until approved; source CWC remains `authority_class = contextual_only`; generated case-memory candidates are not policy evidence, current business fact authority, approval authorization, action authorization, action outcome truth, audit truth, or replay truth; metadata/text retrieval remains first-class and vector retrieval is optional only when `query_embedding` is supplied. In `docs/current-implementation-map.md` and `docs/architecture-overview.md`, add the same planner-facing fact: `search_case_memory` uses reviewed case memory via `MemoryToolExecutor -> CaseMemoryService.retrieve_reviewed(...)`; it must not use active CWC or legacy session-derived precedent. In `.planning/MEMORY-REDESIGN-DECISIONS.md`, add a Phase 47 planning trace that DEFER-2 is now planned as reviewed case precedent generation and `DEFER-3 -> Phase 48` remains named and unimplemented.

    Create `tests/memory/test_phase47_case_precedent_alignment.py` by copying the static-test style from Phase 45/46. Include tests that:
    1. read `docs/contract-spec.md` and assert it contains `reviewed closed-case precedent`, `NOT active case state`, `CaseMemoryService.submit_case_memory_candidate`, `needs_review`, `metadata/text retrieval`, and `query_embedding`;
    2. read `src/db/models.py` and assert the protected strings `__tablename__ = "case_memories"`, `__tablename__ = "long_term_memories"`, `__tablename__ = "case_working_contexts"`, and `case_id: Mapped[str | None]` or `case_id` on `ConversationThread` still exist;
    3. create two separate static guards for protected storage safety. The implementation-surface guard must scan only `src/db/models.py` and Phase 47 migration files under `alembic/versions/` whose filename or content identifies the Phase 47 case-precedent work; it must exclude `.planning/**`, docs, and the alignment test file itself before applying destructive-operation regexes. The planning-artifact guard must scan Phase 47 PLAN/VALIDATION prose only for unsafe imperative action language after stripping fenced code blocks, interface/source-audit/threat-model sections, pattern-definition lines, and prohibition language such as `do not` / `must not`; do not embed raw Alembic destructive-operation examples in that planning-prose guard.
    4. scan `src/api/services/agent_run_memory.py` and `src/memory/case_working_context_lifecycle.py` and assert they do not contain `generate_closed_case_precedent_candidate`, `ClosedCasePrecedentService`, or `closed_case_cwc_candidate`;
    5. scan Phase 47 PLAN/VALIDATION files for pytest commands and assert every command starts with exactly `UV_CACHE_DIR=/tmp/uv-cache uv run pytest`;
    6. assert `.planning/MEMORY-REDESIGN-DECISIONS.md` still contains `DEFER-3 -> Phase 48`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_phase47_case_precedent_alignment.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "reviewed closed-case precedent" docs/contract-spec.md` matches.
    - `grep -n "CaseMemoryService.submit_case_memory_candidate" docs/contract-spec.md` matches.
    - `grep -n "DEFER-3 -> Phase 48" .planning/MEMORY-REDESIGN-DECISIONS.md` matches.
    - `grep -n "generate_closed_case_precedent_candidate" src/api/services/agent_run_memory.py src/memory/case_working_context_lifecycle.py` returns no matches.
    - `grep -n "no_destructive_schema_ops_in_implementation_surfaces" tests/memory/test_phase47_case_precedent_alignment.py` matches.
    - `grep -n "plan_text_has_no_unsafe_storage_action_instructions" tests/memory/test_phase47_case_precedent_alignment.py` matches.
    - The automated pytest command exits 0.
  </acceptance_criteria>
  <done>Case precedent semantics, protected table identity, no-AgentRun-close inference, approved pytest entrypoint, and DEFER-3 carry-forward are executable static checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add review-required closed-case source type</name>
  <read_first>
    - src/memory/schemas.py `MemorySourceRefV1`, `CaseMemorySourceType`, `CaseMemoryWriteCandidate`
    - src/memory/policy.py `AUTO_APPROVED_CASE_SOURCE_TYPES`, `REVIEW_REQUIRED_CASE_SOURCE_TYPES`, `case_memory_policy_decision`
    - src/memory/identity.py `ALLOWED_SOURCE_REF_KEYS` and `_SOURCE_IDENTITY_DISCRIMINATORS`
    - tests/memory/test_memory_policy.py existing case source policy tests
    - tests/memory/test_phase47_case_precedent_alignment.py from Task 1
  </read_first>
  <files>src/memory/schemas.py, src/memory/policy.py, tests/memory/test_memory_policy.py, tests/memory/test_phase47_case_precedent_alignment.py</files>
  <behavior>
    - Test 1: `CaseMemoryWriteCandidate(..., source_type="closed_case_cwc_candidate")` validates.
    - Test 2: `case_memory_review_status_for_source("closed_case_cwc_candidate") == "needs_review"`.
    - Test 3: `case_memory_policy_decision("closed_case_cwc_candidate").decision == "needs_review"` and `.review_status == "needs_review"`.
    - Test 4: `closed_case_cwc_candidate` is not in `AUTO_APPROVED_CASE_SOURCE_TYPES`.
    - Test 5: `MemorySourceRefV1` and `src/memory/identity.py::ALLOWED_SOURCE_REF_KEYS` are not extended for `cwc_version`, `closed_at`, or arbitrary new keys.
  </behavior>
  <action>
    Add the literal string `"closed_case_cwc_candidate"` to `CaseMemorySourceType` in `src/memory/schemas.py`. Add the same string to `REVIEW_REQUIRED_CASE_SOURCE_TYPES` in `src/memory/policy.py` and do not add it to `AUTO_APPROVED_CASE_SOURCE_TYPES`. Extend `tests/memory/test_memory_policy.py` so the existing case-memory source review assertions include `closed_case_cwc_candidate` as `needs_review`, and add an assertion that it is absent from `AUTO_APPROVED_CASE_SOURCE_TYPES`. Extend `tests/memory/test_phase47_case_precedent_alignment.py` with a source-ref stability guard asserting `MemorySourceRefV1.model_fields` and `ALLOWED_SOURCE_REF_KEYS` contain only the existing allowed keys `source_type`, `run_id`, `event_id`, `conversation_message_id`, `tool_result_id`, `agent_run_id`, `business_object_type`, `business_object_id`, `policy_version`, and `outcome_id`; Phase 47 must encode close/CWC identity through existing `event_id` and `outcome_id`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_phase47_case_precedent_alignment.py tests/memory/test_memory_policy.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"closed_case_cwc_candidate"' src/memory/schemas.py src/memory/policy.py tests/memory/test_memory_policy.py` matches.
    - `grep -n "AUTO_APPROVED_CASE_SOURCE_TYPES" -A8 src/memory/policy.py` does not show `closed_case_cwc_candidate`.
    - `grep -n "cwc_version\\|closed_at" src/memory/schemas.py src/memory/identity.py` returns no matches.
    - The automated pytest command exits 0.
  </acceptance_criteria>
  <done>`closed_case_cwc_candidate` exists as a case-memory source type, is review-required only, and does not require a source-ref schema/identity extension.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/plans -> executor | Contract text and static tests shape later runtime implementation. |
| closed-case source type -> case memory policy | New provenance enters `CaseMemoryService.submit_case_memory_candidate(...)` policy classification. |
| phase artifacts -> verification | PLAN/VALIDATION commands must use the MOCA-approved test entrypoint to avoid invalid local verification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-03 | T/I | `src/memory/policy.py`, `src/memory/schemas.py` | mitigate | Add `closed_case_cwc_candidate` to review-required source types only; tests assert it is not auto-approved. |
| T-47-04 | I | `docs/contract-spec.md`, static tests | mitigate | Contract text and tests forbid policy body text, raw tool payloads, authority bodies, replay/debug blobs, and sensitive raw PII in generated precedent. |
| T-47-06 | E/T | `docs/contract-spec.md` CWC/case-memory boundary | mitigate | State CWC remains contextual-only and generated precedent is not policy/action/current-state/audit/replay authority. |
| T-47-01 | S/E | `src/api/services/agent_run_memory.py` and CWC finalizer | mitigate | Static test forbids Phase 47 candidate generation from completed AgentRun finalization; later service uses trusted internal close seam. |
| T-47-05 | R/T | source-ref identity | mitigate | Keep `MemorySourceRefV1` allowed keys fixed; later plans use `event_id` and `outcome_id` for idempotent source identity. |
</threat_model>

<verification>
Run the plan gate:
```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_phase47_case_precedent_alignment.py tests/memory/test_memory_policy.py -q
```
</verification>

<success_criteria>
- `case_memories` semantics are documented and statically locked as reviewed closed-case precedent, not active CWC state.
- `closed_case_cwc_candidate` validates as a `CaseMemorySourceType`, is `needs_review`, and is not auto-approved.
- Phase 47 static tests protect table identity, no completed-run close inference, DEFER-3 carry-forward, and approved pytest entrypoints.
- No Alembic migration is introduced by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/47-case-precedent-repositioning-and-closed-case-candidate-gener/47-01-SUMMARY.md`.
</output>
