---
phase: 47-case-precedent-repositioning-and-closed-case-candidate-gener
plan: 03
type: execute
wave: 3
depends_on: [47-02]
files_modified:
  - src/memory/case_precedent.py
  - tests/memory/test_case_precedent_generation.py
  - tests/memory/test_case_memory_retrieval.py
  - tests/test_memory_review_api.py
autonomous: true
requirements: [MEM-04]
requirements_addressed: [MEM-04]
must_haves:
  truths:
    - "case_memories is documented and test-locked as reviewed closed-case precedent, not active working state"
    - "closed-case CWC candidates enter the existing case-memory review workflow as needs_review"
    - "metadata/text retrieval remains first-class and embeddings remain optional"
    - "projection boundaries forbid policy body text, raw tool payloads, authority bodies, replay/debug blobs, and sensitive raw PII"
    - "case_memories, long_term_memories, case_working_contexts, and conversation_threads.case_id are not renamed, dropped, or replaced"
    - "DEFER-3 remains named as Phase 48 scope and is not implemented"
  artifacts:
    - path: "src/memory/case_precedent.py"
      provides: "CaseMemoryService-submitted closed-case candidates with source identity"
      contains: "submit_case_memory_candidate"
    - path: "tests/memory/test_case_precedent_generation.py"
      provides: "needs_review, audit, duplicate, PII, and pending/retrieval lifecycle tests"
      contains: "duplicate_active"
  key_links:
    - from: "src/memory/case_precedent.py"
      to: "src/memory/case_memory.py"
      via: "CaseMemoryService.submit_case_memory_candidate"
      pattern: "submit_case_memory_candidate"
    - from: "src/memory/case_precedent.py"
      to: "src/memory/schemas.py"
      via: "MemorySourceRefV1 allowed event_id/outcome_id source identity"
      pattern: "event_id|outcome_id"
---

<objective>
Route accepted closed-case projections through the existing governed case-memory write and review lifecycle.

Purpose: The candidate service must not directly insert `CaseMemory` rows or create a parallel review/audit path. It must submit a `CaseMemoryWriteCandidate` with allowed source-ref identity, produce `needs_review`, dedupe repeated close events/CWC versions, and keep candidates pending-review visible but reviewed-retrieval invisible until approval.
Output: write lifecycle implementation plus integration tests for `needs_review`, audit/write event behavior, duplicate/idempotency behavior, PII skip behavior, pending review visibility, and approval-gated retrieval.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/47-case-precedent-repositioning-and-closed-case-candidate-gener/47-CONTEXT.md
@.planning/phases/47-case-precedent-repositioning-and-closed-case-candidate-gener/47-RESEARCH.md
@.planning/phases/47-case-precedent-repositioning-and-closed-case-candidate-gener/47-VALIDATION.md
@.planning/phases/47-case-precedent-repositioning-and-closed-case-candidate-gener/47-PATTERNS.md
@.planning/phases/47-case-precedent-repositioning-and-closed-case-candidate-gener/47-02-SUMMARY.md
@src/memory/case_precedent.py
@src/memory/case_memory.py
@src/memory/identity.py
@src/memory/schemas.py
@tests/memory/test_case_memory_retrieval.py
@tests/test_memory_review_api.py

<interfaces>
Existing service methods:
```python
class CaseMemoryService:
    async def submit_case_memory_candidate(self, candidate: CaseMemoryWriteCandidate) -> CaseMemoryWriteResult: ...
    async def list_pending_review(self, *, tenant_id: uuid.UUID, limit: int = 50) -> list[CaseMemory]: ...
    async def approve_case_memory(self, decision: CaseMemoryReviewDecision) -> MemoryWriteEvent: ...
    async def retrieve_reviewed(self, request: CaseMemorySearchRequest) -> CaseMemorySearchResult: ...
```

Existing source identity allowed keys:
```python
ALLOWED_SOURCE_REF_KEYS = frozenset({
    "source_type",
    "run_id",
    "event_id",
    "conversation_message_id",
    "tool_result_id",
    "agent_run_id",
    "business_object_type",
    "business_object_id",
    "policy_version",
    "outcome_id",
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Submit terminal projections through CaseMemoryService</name>
  <read_first>
    - src/memory/case_precedent.py from Plan 47-02
    - src/memory/case_memory.py `CaseMemoryService.submit_case_memory_candidate`
    - src/memory/schemas.py `MemorySourceRefV1`, `CaseMemoryWriteCandidate`, `CaseMemoryWriteResult`
    - src/memory/identity.py `ALLOWED_SOURCE_REF_KEYS`, `_SOURCE_IDENTITY_DISCRIMINATORS`
    - tests/memory/test_case_memory_retrieval.py existing submit/retrieve/approve assertions
    - tests/memory/test_case_precedent_generation.py from Plan 47-02
  </read_first>
  <files>src/memory/case_precedent.py, tests/memory/test_case_precedent_generation.py</files>
  <behavior>
    - Test 1: terminal close with active CWC writes exactly one `case_memories` row through `CaseMemoryService.submit_case_memory_candidate(...)`.
    - Test 2: written row has `source_type == "closed_case_cwc_candidate"` and `review_status == "needs_review"`.
    - Test 3: generated `source_ref_json` uses only allowed keys and includes `event_id=f"refund-case-close:{case_id}:{close_event_id}"`, `business_object_type="refund_case"`, `business_object_id=str(case_id)`, `agent_run_id=str(run_id)`, `run_id=str(run_id)`, and `outcome_id=f"cwc:{cwc_row.id}:v{cwc_row.version}"`.
    - Test 4: generated candidate has `embedding is None`.
    - Test 5: the service result exposes the existing write event id from `CaseMemoryWriteResult.event_id`.
  </behavior>
  <action>
    Finish `ClosedCasePrecedentService.generate_closed_case_precedent_candidate(...)` so accepted terminal projections call only `self.case_memory_service.submit_case_memory_candidate(candidate)` for persistence. Do not instantiate `CaseMemory`, do not call `CaseMemoryRepository.insert_case_memory(...)` directly, and do not emit a custom write event outside the case-memory service.

    Build `MemorySourceRefV1` with exactly these allowed keys when values are available: `source_type="closed_case_cwc_candidate"`, `run_id=str(request.run_id)`, `agent_run_id=str(request.run_id)`, `event_id=f"refund-case-close:{request.case_id}:{request.close_event_id}"`, `business_object_type="refund_case"`, `business_object_id=str(request.case_id)`, `outcome_id=f"cwc:{cwc_row.id}:v{cwc_row.version}"`, and `policy_version=<first policy ref version if present>`. Do not add `cwc_version`, `closed_at`, `close_source`, or other source-ref keys. Map `CaseMemoryWriteResult.status == "needs_review"` to `ClosedCasePrecedentGenerationResult(status="needs_review", reason_code=result.reason_code, memory_id=result.memory_id, review_status=result.review_status, event_id=result.event_id, scope_type=candidate.scope_type, scope_id=candidate.scope_id)`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_case_precedent_generation.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "submit_case_memory_candidate" src/memory/case_precedent.py` matches.
    - `grep -n "insert_case_memory" src/memory/case_precedent.py` returns no matches.
    - `grep -n "refund-case-close" src/memory/case_precedent.py tests/memory/test_case_precedent_generation.py` matches.
    - `grep -n "outcome_id" src/memory/case_precedent.py tests/memory/test_case_precedent_generation.py` matches.
    - The automated pytest command exits 0.
  </acceptance_criteria>
  <done>Terminal closed-case projections persist only through the existing case-memory service and produce review-required candidates with allowed source identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove idempotency, duplicate handling, and PII skip behavior</name>
  <read_first>
    - src/memory/case_precedent.py
    - src/memory/case_memory.py duplicate, tombstone, and PII skip branches
    - src/memory/policy.py `is_blocked_memory_write_pii_classification`
    - tests/memory/test_case_memory_retrieval.py duplicate and PII tests
    - tests/memory/test_case_precedent_generation.py current closed-case tests
  </read_first>
  <files>src/memory/case_precedent.py, tests/memory/test_case_precedent_generation.py</files>
  <behavior>
    - Test 1: invoking the service twice with the same tenant/source case/CWC version/close event returns first `status="needs_review"` and second `status="skipped"` with duplicate reason from existing duplicate handling.
    - Test 2: invoking with a different close event or different active CWC version does not collide through source identity unless content hash already matches; test expected reason clearly.
    - Test 3: CWC row `pii_classification="sensitive"` or `"prohibited"` routes through existing case-memory service skip behavior and returns `status="skipped"`, `reason_code="pii_blocked"`, and no case-memory row.
    - Test 4: missing active CWC and case-not-found skips do not insert a row.
  </behavior>
  <action>
    Reuse existing `CaseMemoryService` duplicate and PII logic rather than adding a new duplicate table or new audit table. If the current projection short-circuits PII before service submission, change it so a minimal prompt-safe blocked candidate with `pii_classification` copied from the CWC row is submitted to `submit_case_memory_candidate(...)`; its text fields must use non-sensitive fixed text such as `Closed refund case precedent candidate blocked by PII classification.` so the existing service emits the skip event without persisting a row. Keep missing CWC, case not found, and non-terminal status as local `status="skipped"` results with reason codes because no candidate identity exists to submit. Add tests that inspect resulting `memory_write_events` only for paths where `CaseMemoryService` emits them, and do not create any second audit table.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_case_precedent_generation.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "duplicate" tests/memory/test_case_precedent_generation.py` matches closed-case generation tests.
    - `grep -n "pii_blocked" tests/memory/test_case_precedent_generation.py` matches.
    - `grep -n "MemoryWriteEvent(" src/memory/case_precedent.py` returns no matches.
    - `grep -n "second review queue\\|parallel store" src/memory/case_precedent.py` returns no matches.
    - The automated pytest command exits 0.
  </acceptance_criteria>
  <done>Repeated close events/CWC source identities dedupe through existing service behavior, and blocked PII uses the existing skip/event path without persisting a case-memory row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove pending-review visibility and reviewed-retrieval invisibility until approval</name>
  <read_first>
    - tests/memory/test_case_precedent_generation.py
    - tests/memory/test_case_memory_retrieval.py existing review-before-retrieval tests
    - tests/test_memory_review_api.py existing pending review API tests
    - src/memory/case_memory.py `list_pending_review`, `approve_case_memory`, `retrieve_reviewed`
    - src/api/routers/memory.py existing review routes
  </read_first>
  <files>tests/memory/test_case_precedent_generation.py, tests/memory/test_case_memory_retrieval.py, tests/test_memory_review_api.py</files>
  <behavior>
    - Test 1: generated closed-case candidate appears in `CaseMemoryService.list_pending_review(...)` with `review_status == "needs_review"`.
    - Test 2: the same generated candidate is not returned by `CaseMemoryService.retrieve_reviewed(...)` while it is `needs_review`.
    - Test 3: after `CaseMemoryService.approve_case_memory(...)`, the generated candidate is returned by `retrieve_reviewed(...)` for its scope and text query.
    - Test 4: existing memory review API pending/review actions continue to work for `case_memory` rows generated from `closed_case_cwc_candidate`; no new endpoint or review queue is added.
  </behavior>
  <action>
    Add integration coverage around the existing service/API surfaces. Prefer placing the full generation-to-approval scenario in `tests/memory/test_case_precedent_generation.py`; add targeted assertions to `tests/memory/test_case_memory_retrieval.py` or `tests/test_memory_review_api.py` only where an existing route/service fixture already makes the behavior cheaper to prove. Do not modify `src/api/routers/memory.py` unless a test shows closed-case generated rows are not visible through the existing pending review route. Do not add any public refund-case close endpoint.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_case_precedent_generation.py tests/memory/test_case_memory_retrieval.py tests/test_memory_review_api.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "list_pending_review" tests/memory/test_case_precedent_generation.py tests/test_memory_review_api.py` matches.
    - `grep -n "approve_case_memory" tests/memory/test_case_precedent_generation.py tests/memory/test_case_memory_retrieval.py` matches.
    - `grep -n "retrieve_reviewed" tests/memory/test_case_precedent_generation.py tests/memory/test_case_memory_retrieval.py` matches.
    - `grep -n "post.*close\\|close endpoint" src/api/routers/refund_cases.py` returns no Phase 47 close-route matches.
    - The automated pytest command exits 0.
  </acceptance_criteria>
  <done>Generated closed-case candidates use the existing pending review and approval path and remain invisible to reviewed retrieval until approval.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| projection service -> case memory service | Candidate persistence crosses into governed review/audit/dedupe policy. |
| generated candidate -> pending review API | Candidate is visible to reviewers but must remain unpublished. |
| pending row -> reviewed retrieval | Approval is the only publication transition. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-03 | T/I | `ClosedCasePrecedentService`, `CaseMemoryService.submit_case_memory_candidate` | mitigate | Persist only through existing service with `source_type="closed_case_cwc_candidate"`; tests prove `needs_review` and retrieval exclusion until approval. |
| T-47-05 | R/T | source identity and duplicate handling | mitigate | Use allowed `event_id` and `outcome_id`; reuse existing source/content identity duplicate checks and write events. |
| T-47-04 | I | PII-blocked CWC rows | mitigate | Route blocked PII through existing service skip path with fixed non-sensitive blocked-candidate text and no persisted row. |
| T-47-02 | I | reviewed retrieval tenant/scope filters | mitigate | Tests retrieve through existing `retrieve_reviewed(...)`; candidates remain tenant/scope-bound and invisible cross-scope. |
| T-47-06 | E/T | candidate caveats and source refs | mitigate | Candidate caveats from Plan 47-02 persist; no review/API route treats precedent as authority. |
</threat_model>

<verification>
Run the governed lifecycle gate:
```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_case_precedent_generation.py tests/memory/test_case_memory_retrieval.py tests/test_memory_review_api.py -q
```
</verification>

<success_criteria>
- Terminal closed-case projection writes through `CaseMemoryService.submit_case_memory_candidate(...)` only.
- Generated candidates default to `needs_review` and emit/preserve existing observable write-event behavior.
- Duplicate close event / same CWC source identity dedupes through existing duplicate handling.
- PII-blocked CWC candidates skip without a persisted case-memory row.
- Pending-review visibility and reviewed-retrieval invisibility are both test-proven until approval.
</success_criteria>

<output>
After completion, create `.planning/phases/47-case-precedent-repositioning-and-closed-case-candidate-gener/47-03-SUMMARY.md`.
</output>
