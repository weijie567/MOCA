---
phase: 47-case-precedent-repositioning-and-closed-case-candidate-gener
plan: 02
type: execute
wave: 2
depends_on: [47-01]
files_modified:
  - src/memory/case_precedent.py
  - src/repositories/refund_repo.py
  - tests/memory/test_case_precedent_generation.py
  - tests/memory/test_phase47_case_precedent_alignment.py
autonomous: true
requirements: [MEM-04]
requirements_addressed: [MEM-04]
must_haves:
  truths:
    - "case_memories is documented and test-locked as reviewed closed-case precedent, not active working state"
    - "closed-case CWC candidates enter the existing case-memory review workflow as needs_review"
    - "metadata/text retrieval remains first-class and embeddings remain optional"
    - "projection boundaries forbid policy body text, raw tool payloads, authority bodies, replay/debug blobs, and sensitive raw PII"
    - "case_memories, long_term_memories, case_working_contexts, and conversation_threads.case_id are not renamed, dropped, or replaced"
    - "DEFER-3 remains named as Phase 48 scope and is not implemented"
  artifacts:
    - path: "src/memory/case_precedent.py"
      provides: "trusted closed-case CWC-to-case-memory candidate service seam"
      contains: "TERMINAL_REFUND_CASE_STATUSES"
    - path: "src/repositories/refund_repo.py"
      provides: "tenant-bound RefundCase lookup with order merchant for scope resolution"
      contains: "get_by_id_with_order"
    - path: "tests/memory/test_case_precedent_generation.py"
      provides: "closed-case projection, skip, scope, and payload tests"
      contains: "ClosedCasePrecedentService"
  key_links:
    - from: "src/memory/case_precedent.py"
      to: "src/memory/case_working_context.py"
      via: "CaseWorkingContextRepository.read_active and hydrate_content"
      pattern: "read_active|hydrate_content"
    - from: "src/memory/case_precedent.py"
      to: "src/repositories/refund_repo.py"
      via: "tenant-bound RefundCase -> Order.merchant_id resolution"
      pattern: "get_by_id_with_order"
---

<objective>
Create the internal trusted closed-case candidate-generation seam and deterministic allowlisted projection.

Purpose: Phase 47 must not invent a public close endpoint or infer closure from completed agent runs. The service boundary should accept explicit trusted close inputs, load the active CWC snapshot, resolve merchant or exact case scope, and build a prompt-safe `CaseMemoryWriteCandidate` without writing directly to `case_memories`.
Output: `src/memory/case_precedent.py`, tenant-bound refund-case merchant lookup, and focused tests for terminal status allowlist, CWC read/hydration, conservative scope fallback, projection allowlist, skip reasons, and static forbidden payload/import guards.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/MEMORY-REDESIGN-DECISIONS.md
@.planning/phases/47-case-precedent-repositioning-and-closed-case-candidate-gener/47-CONTEXT.md
@.planning/phases/47-case-precedent-repositioning-and-closed-case-candidate-gener/47-RESEARCH.md
@.planning/phases/47-case-precedent-repositioning-and-closed-case-candidate-gener/47-VALIDATION.md
@.planning/phases/47-case-precedent-repositioning-and-closed-case-candidate-gener/47-PATTERNS.md
@.planning/phases/45-memory-lifecycle-wiring-for-case-working-context/45-04-SUMMARY.md
@.planning/phases/46-session-context-repositioning/46-03-SUMMARY.md
@src/memory/case_working_context.py
@src/memory/case_working_context_schemas.py
@src/memory/schemas.py
@src/memory/case_memory.py
@src/repositories/refund_repo.py
@src/db/models.py

<interfaces>
Existing CWC read surface:
```python
class CaseWorkingContextRepository:
    async def read_active(self, *, tenant_id: uuid.UUID, case_id: uuid.UUID) -> CaseWorkingContext | None: ...

def hydrate_content(row: CaseWorkingContext) -> CaseWorkingContextContentV1: ...
```

Existing case-memory candidate shape:
```python
class CaseMemoryWriteCandidate(BaseModel):
    tenant_id: uuid.UUID
    run_id: uuid.UUID
    scope_type: CaseMemoryScopeType
    scope_id: str
    case_type: str
    summary: str
    excerpt: str
    applicability: str | None = None
    outcome: str | None = None
    caveats: str | None = None
    source_type: CaseMemorySourceType
    source_ref: MemorySourceRefV1 | None = None
    policy_family: str | None = None
    policy_version: str | None = None
    policy_refs: list[dict[str, Any]] = Field(default_factory=list)
    embedding: list[float] | None = None
    pii_classification: CaseMemoryPiiClassification = "none"
```

RefundCase / Order model facts:
```python
class RefundCase(TimestampMixin, Base):
    __tablename__ = "refund_cases"
    order_id: Mapped[uuid.UUID] = mapped_column(... ForeignKey("orders.id") ...)
    status: Mapped[str] = mapped_column(String(32), nullable=False)
    order: Mapped["Order"] = relationship(back_populates="refund_cases")

class Order(TimestampMixin, Base):
    __tablename__ = "orders"
    merchant_id: Mapped[uuid.UUID] = mapped_column(... nullable=False ...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add trusted close seam, terminal allowlist, and scope resolution</name>
  <read_first>
    - src/memory/case_precedent.py if it already exists
    - src/repositories/refund_repo.py current `RefundRepository`
    - src/db/models.py `RefundCase` and `Order` relationships
    - src/memory/case_working_context.py `CaseWorkingContextRepository.read_active` and `hydrate_content`
    - tests/memory/test_case_precedent_generation.py if it already exists
    - tests/memory/test_case_memory_retrieval.py fixture/candidate style
    - .planning/phases/47-case-precedent-repositioning-and-closed-case-candidate-gener/47-CONTEXT.md D-47-04, D-47-05, D-47-07
  </read_first>
  <files>src/memory/case_precedent.py, src/repositories/refund_repo.py, tests/memory/test_case_precedent_generation.py</files>
  <behavior>
    - Test 1: `TERMINAL_REFUND_CASE_STATUSES == frozenset({"closed", "refunded", "rejected"})`.
    - Test 2: `generate_closed_case_precedent_candidate(...)` with `closed_status="open"`, `"reviewing"`, or `"unknown_status"` returns `status="skipped"` and `reason_code="non_terminal_status"` without calling case-memory submission.
    - Test 3: terminal status with missing `RefundCase` returns `status="skipped"` and `reason_code="case_not_found"`.
    - Test 4: terminal status with no active CWC row returns `status="skipped"` and `reason_code="missing_active_cwc"`.
    - Test 5: when `RefundCase -> Order.merchant_id` resolves, the projected candidate uses `scope_type="merchant"` and `scope_id=str(order.merchant_id)`.
    - Test 6: when merchant cannot resolve, the projected candidate uses exact fallback `scope_type="case"` and `scope_id=str(case_id)`; it never uses tenant-wide fallback.
  </behavior>
  <action>
    Add `src/memory/case_precedent.py` with `TERMINAL_REFUND_CASE_STATUSES = frozenset({"closed", "refunded", "rejected"})`, a frozen dataclass `ClosedCasePrecedentGenerationInput` with fields `tenant_id: uuid.UUID`, `case_id: uuid.UUID`, `run_id: uuid.UUID`, `closed_status: str`, `close_event_id: str`, `closed_at: datetime`, and `close_source: str = "trusted_internal"`, and a frozen dataclass `ClosedCasePrecedentGenerationResult` with fields `status: Literal["needs_review", "skipped", "error"]`, `reason_code: str`, `memory_id: uuid.UUID | None = None`, `review_status: str | None = None`, `event_id: uuid.UUID | None = None`, `scope_type: str | None = None`, and `scope_id: str | None = None`. Add class `ClosedCasePrecedentService` with constructor dependencies for `session: AsyncSession`, `case_memory_service: CaseMemoryService | None = None`, `cwc_repository: CaseWorkingContextRepository | None = None`, and `refund_repository: RefundRepository | None = None`.

    Implement `async def generate_closed_case_precedent_candidate(self, request: ClosedCasePrecedentGenerationInput) -> ClosedCasePrecedentGenerationResult`. It must skip before CWC projection if `request.closed_status not in TERMINAL_REFUND_CASE_STATUSES` with `reason_code="non_terminal_status"`. It must load the refund case through a new `RefundRepository.get_by_id_with_order(case_id: UUID, tenant_id: UUID) -> RefundCase | None` helper that uses `selectinload(RefundCase.order)` or an equivalent tenant-bound join. It must call `CaseWorkingContextRepository.read_active(tenant_id=request.tenant_id, case_id=request.case_id)` and `hydrate_content(row)` only after terminal status and case lookup pass. It must not call `CaseMemoryService.submit_case_memory_candidate(...)` yet if Task 2 needs to finish projection first, but it must expose a pure candidate projection helper that tests can inspect.

    Add scope resolution helper `_resolve_precedent_scope(refund_case: RefundCase | None, *, case_id: uuid.UUID) -> tuple[str, str]`: return `("merchant", str(refund_case.order.merchant_id))` when the order and merchant id are present; otherwise return `("case", str(case_id))`. Do not ever return `("tenant", ...)` as fallback. Do not modify `src/api/routers/refund_cases.py`; Phase 47 has no public close endpoint.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_case_precedent_generation.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TERMINAL_REFUND_CASE_STATUSES = frozenset({\"closed\", \"refunded\", \"rejected\"})" src/memory/case_precedent.py` matches.
    - `grep -n "def get_by_id_with_order" src/repositories/refund_repo.py` matches.
    - `grep -n "non_terminal_status" tests/memory/test_case_precedent_generation.py` matches.
    - `grep -n '"tenant"' src/memory/case_precedent.py` does not show a fallback scope branch.
    - The automated pytest command exits 0.
  </acceptance_criteria>
  <done>The internal trusted close seam exists, non-terminal/unknown statuses skip, active CWC is read only after terminal case validation, and merchant-or-exact-case scope resolution is test-covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic allowlisted CWC projection</name>
  <read_first>
    - src/memory/case_precedent.py from Task 1
    - src/memory/case_working_context_schemas.py `CaseWorkingContextContentV1` and nested field models
    - src/memory/schemas.py `CaseMemoryWriteCandidate` and `MemorySourceRefV1`
    - src/memory/case_working_context_lifecycle.py deterministic projection helpers and PII patterns
    - tests/agent/test_case_working_context_lifecycle.py prompt-safe projection tests
    - tests/memory/test_phase47_case_precedent_alignment.py from Plan 47-01
  </read_first>
  <files>src/memory/case_precedent.py, tests/memory/test_case_precedent_generation.py, tests/memory/test_phase47_case_precedent_alignment.py</files>
  <behavior>
    - Test 1: projection keeps CWC `claims` text separate from `verified_facts` text and labels them as `Customer claim:` and `Verified fact:`.
    - Test 2: projection copies policy refs only as dictionaries with `doc_id`, `chunk_id`, and `version`; it never copies policy body text.
    - Test 3: projection output contains fixed caveat text `Contextual precedent only; not policy evidence, current business fact authority, approval authorization, action authorization, action outcome truth, audit truth, or replay truth.`
    - Test 4: projection excludes raw payload markers `raw_payload`, `raw_tool`, `policy_body`, `approval_authority`, `action_authority`, `replay_blob`, and `debug_blob` from `summary`, `excerpt`, `applicability`, `outcome`, `caveats`, and `policy_refs`.
    - Test 5: CWC rows with `pii_classification in {"sensitive", "prohibited"}` do not produce a persisted candidate row; they are prepared for service skip behavior with `reason_code="pii_blocked"`.
  </behavior>
  <action>
    Implement a pure projection helper in `src/memory/case_precedent.py`, for example `_project_closed_case_candidate(...) -> CaseMemoryWriteCandidate | ClosedCasePrecedentGenerationResult`, that constructs `CaseMemoryWriteCandidate` with `source_type="closed_case_cwc_candidate"`, `embedding=None`, and `pii_classification` copied from the active CWC row. Use these deterministic field rules:
    - `case_type` is `content.issue_type` when non-empty, otherwise `"refund"`.
    - `summary` starts with `Closed refund case precedent:` and includes the issue type or refund reason only as bounded text.
    - `excerpt` may include bounded lines for `Customer request:`, `Customer claim:`, `Verified fact:`, `Action taken:`, `Agent recommendation:`, `Staff decision:`, and `Commitment:` using CWC summaries only.
    - `applicability` is a bounded sentence beginning `Use only as reviewed precedent for similar refund cases`.
    - `outcome` includes trusted close metadata from `closed_status` and `closed_at` plus CWC staff decision/commitment summaries when present.
    - `caveats` is exactly `Contextual precedent only; not policy evidence, current business fact authority, approval authorization, action authorization, action outcome truth, audit truth, or replay truth.`
    - `policy_refs` is a list of dictionaries shaped only as `{"doc_id": ref.doc_id, "chunk_id": ref.chunk_id, "version": ref.version}`.
    - `policy_family` is the first policy ref `doc_id` when present; `policy_version` is the first policy ref `version` when present.

    Add a local `_bounded_text(value: str | None, limit: int) -> str | None` helper and use it before assigning candidate text fields. Do not import or construct `EvidenceRefV1`, `BusinessFactRefV1`, `ApprovalRequest`, `ApprovalDecision`, `ActionDraft`, `ReplayEvent`, `ReplayTruth`, tool result raw payload DTOs, or policy body objects. Extend `tests/memory/test_phase47_case_precedent_alignment.py` to scan `src/memory/case_precedent.py` for those forbidden import/token strings while allowing `CaseMemoryWriteCandidate`, `MemorySourceRefV1`, and CWC schema imports.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_case_precedent_generation.py tests/memory/test_phase47_case_precedent_alignment.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Contextual precedent only; not policy evidence" src/memory/case_precedent.py` matches.
    - `grep -n "Customer claim:" src/memory/case_precedent.py` matches.
    - `grep -n "Verified fact:" src/memory/case_precedent.py` matches.
    - `grep -n "EvidenceRefV1\\|ApprovalRequest\\|ActionDraft\\|ReplayEvent" src/memory/case_precedent.py` returns no matches.
    - The automated pytest command exits 0.
  </acceptance_criteria>
  <done>The closed-case projection is deterministic, prompt-safe, claim/fact separated, CWC-PII aware, and free of forbidden authority/raw payload imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| trusted close caller -> internal service seam | Caller provides explicit terminal status, case id, run id, event id, and closed_at; no public endpoint is introduced. |
| business case row -> retrieval scope | RefundCase/Order lookup resolves merchant scope or conservative exact case fallback. |
| CWC snapshot -> case-memory candidate | Contextual-only CWC content is allowlisted into prompt-safe precedent text and refs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | S/E | `ClosedCasePrecedentService.generate_closed_case_precedent_candidate` | mitigate | Explicit `TERMINAL_REFUND_CASE_STATUSES` allowlist; `open`, `reviewing`, and unknown statuses skip with `non_terminal_status`; no public close endpoint. |
| T-47-02 | I | `_resolve_precedent_scope`, `RefundRepository.get_by_id_with_order` | mitigate | Tenant-bound case lookup; merchant scope only from `RefundCase -> Order.merchant_id`; unresolved merchant falls back to exact case scope and never tenant-wide reusable scope. |
| T-47-04 | I | `_project_closed_case_candidate` | mitigate | Deterministic allowlist projection, bounded text, ref-only policy refs, and static forbidden token/import tests. |
| T-47-06 | T/E | claim/fact projection | mitigate | Label claims and verified facts separately; fixed caveat states precedent is not policy/action/current-state/audit/replay authority. |
| T-47-03 | I/T | future candidate write path | mitigate | Build only `CaseMemoryWriteCandidate` with `source_type="closed_case_cwc_candidate"`; Plan 47-03 routes it through review-required service submission. |
</threat_model>

<verification>
Run the service-foundation gate:
```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_case_precedent_generation.py tests/memory/test_phase47_case_precedent_alignment.py -q
```
</verification>

<success_criteria>
- A trusted internal close seam exists and does not modify refund-case API routes or agent-run finalization.
- Terminal status allowlist is exactly `closed`, `refunded`, and `rejected`; `open`, `reviewing`, and unknown statuses skip.
- Active CWC is read and hydrated by tenant/case; missing CWC or case skips without creating a case-memory row.
- Merchant-scope storage is chosen when merchant resolves; exact case scope is used as conservative fallback; tenant-wide fallback is absent.
- Projection is deterministic, prompt-safe, claim/fact separated, and bounded.
</success_criteria>

<output>
After completion, create `.planning/phases/47-case-precedent-repositioning-and-closed-case-candidate-gener/47-02-SUMMARY.md`.
</output>
