---
phase: 28-decision-event-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/replay/decision_events.py
  - src/replay/validators.py
  - src/replay/service.py
  - src/replay/__init__.py
  - src/agent/events.py
  - src/agent/nodes/memory_write.py
  - tests/replay/test_decision_events.py
  - tests/agent/test_events.py
  - tests/agent/test_memory_write_node.py
  - tests/replay/test_sequence_allocator.py
autonomous: true
requirements:
  - APF-05
must_haves:
  truths:
    - "DecisionEventEnvelopeV1 validates exactly the existing minimal_event_envelope.v1 top-level envelope and does not create a second replay event format."
    - "Decision event writes persist through ReplayService.append_event with run_id, tenant_id, thread_id, trace_id, operation_id, actor, resource_refs, redaction_policy_version, and redacted_payload."
    - "Reason codes are stored as redacted_payload.reason_codes, use first-seen de-duplication, and reject non-snake-case or empty values."
    - "Policy/model/tool versions are stored under redacted_payload.versions and never as top-level envelope keys."
    - "Redaction guards reject unsafe raw prompt/tool/business/PII/secret keys in both redacted_payload and resource_refs."
    - "src.agent.events.emit_event remains usable as a compatibility wrapper and delegates to the replay-owned emitter."
    - "Existing replay sequence allocation remains monotonic and collision-safe across current writer surfaces."
  artifacts:
    - path: "src/replay/decision_events.py"
      provides: "DecisionEventEnvelopeV1, emit_decision_event, and reason/version normalization helpers"
      exports: ["DecisionEventEnvelopeV1", "emit_decision_event"]
    - path: "src/replay/validators.py"
      provides: "Shared redacted payload and resource ref forbidden-key guards"
      exports: ["guard_redacted_payload", "guard_resource_refs"]
    - path: "src/replay/service.py"
      provides: "ReplayService persistence path with resource_refs guard and typed minimal projection validation"
      exports: ["ReplayService"]
    - path: "src/agent/events.py"
      provides: "Legacy minimal event compatibility wrapper routed through replay-owned emitter"
      exports: ["emit_event", "allocate_sequence", "classify_event_family"]
    - path: "src/agent/nodes/memory_write.py"
      provides: "Narrow compatibility update so memory_write lifecycle events pass one operation_id through the wrapper"
    - path: "tests/replay/test_decision_events.py"
      provides: "Contract tests for schema strictness, emitter identity, normalization, redaction, and service payload extension"
    - path: "tests/agent/test_events.py"
      provides: "Compatibility wrapper tests, including legacy reason_code conversion"
    - path: "tests/agent/test_memory_write_node.py"
      provides: "Focused memory_write operation_id regression coverage without memory domain payload migration"
    - path: "tests/replay/test_sequence_allocator.py"
      provides: "Ordering regression coverage including the replay-owned decision emitter"
  key_links:
    - from: "src/replay/decision_events.py"
      to: "src/replay/service.py"
      via: "emit_decision_event calls ReplayService(session).append_event"
      pattern: 'ReplayService\(session\)\.append_event'
    - from: "src/replay/service.py"
      to: "src/replay/decision_events.py"
      via: "project_minimal_event validates through DecisionEventEnvelopeV1"
      pattern: 'DecisionEventEnvelopeV1\.model_validate'
    - from: "src/agent/events.py"
      to: "src/replay/decision_events.py"
      via: "emit_event delegates to emit_decision_event"
      pattern: 'emit_decision_event'
    - from: "src/replay/service.py"
      to: "src/replay/validators.py"
      via: "append_event guards resource_refs before persistence"
      pattern: 'guard_resource_refs\(resource_refs\)'
---

<objective>
Create the Phase 28 decision event foundation for APF-05.

Purpose: Later platform services need one replay-owned event envelope and emitter contract for service decisions, with trusted identity, reason-code normalization, version placement, redaction/resource-ref safety, and allocator compatibility already enforced.

Output: `DecisionEventEnvelopeV1`, `emit_decision_event(...)`, shared resource-ref guarding, replay service projection validation, an `src.agent.events.emit_event` compatibility wrapper, and focused contract tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-decision-event-foundation/28-CONTEXT.md
@.planning/phases/28-decision-event-foundation/28-RESEARCH.md
@.planning/phases/28-decision-event-foundation/28-VALIDATION.md
@.planning/phases/28-decision-event-foundation/28-PATTERNS.md
@.planning/phases/26-architecture-contract-baseline/26-01-SUMMARY.md
@.planning/phases/27-trustedcontextfactory-and-projections/27-02-SUMMARY.md
@.planning/phases/27-trustedcontextfactory-and-projections/27-03-SUMMARY.md
@docs/contract-spec.md
@docs/target-agent-platform-architecture-plan.md
@src/replay/service.py
@src/replay/schemas.py
@src/replay/validators.py
@src/agent/events.py
@src/platform/context_projections.py
@src/platform/trusted_context.py
@tests/agent/test_events.py
@tests/replay/test_replay_service.py
@tests/replay/test_sequence_allocator.py
@tests/platform/test_context_projections.py

<interfaces>
Key existing contracts the executor must preserve:

From `docs/contract-spec.md` section 17.2, the minimal event envelope top-level fields are exactly:
`schema_version`, `event_id`, `sequence`, `operation_id`, `run_id`, `tenant_id`, `thread_id`, `trace_id`, `event_type`, `occurred_at`, `actor`, `resource_refs`, `redaction_policy_version`, `redacted_payload`.

From `src/replay/service.py`:
```python
async def append_event(
    self,
    *,
    run_id: uuid.UUID | str,
    tenant_id: uuid.UUID | str,
    thread_id: str,
    event_type: str,
    actor: dict[str, Any],
    resource_refs: dict[str, Any],
    redacted_payload: dict[str, Any],
    trace_id: str | None = None,
    operation_id: uuid.UUID | str | None = None,
    schema_version: str = "replay_event.v3",
    redaction_policy_version: str = "redaction.v1",
    iteration: int | None = None,
) -> dict[str, Any]:
    ...
```

From `src/replay/service.py`, `project_minimal_event(...)` currently returns the existing minimal envelope dict:
```python
{
    "schema_version": event.schema_version,
    "event_id": event.event_id,
    "sequence": event.sequence,
    "operation_id": event.operation_id,
    "run_id": event.run_id,
    "tenant_id": event.tenant_id,
    "thread_id": event.thread_id,
    "trace_id": event.trace_id,
    "event_type": event.event_type,
    "occurred_at": event.occurred_at,
    "actor": event.actor,
    "resource_refs": event.resource_refs,
    "redaction_policy_version": event.redaction_policy_version,
    "redacted_payload": event.redacted_payload,
}
```

From `src/platform/context_projections.py`:
```python
class ReplayContext(ProjectionMetadata):
    schema_version: Literal["replay_context.v1"] = "replay_context.v1"
    tenant_id: str
    user_id: str
    role: str
    session_id: str | None = None
    thread_id: str
    run_id: str
    trace_id: str | None = None
    locale: str | None = None
```

From `src/agent/events.py`, preserve this wrapper call shape by adding only optional keyword-only compatibility fields:
```python
async def emit_event(
    session: AsyncSession,
    *,
    run_id: uuid.UUID | str,
    tenant_id: uuid.UUID | str,
    thread_id: str,
    event_type: str,
    actor: dict[str, Any],
    resource_refs: dict[str, Any],
    redacted_payload: dict[str, Any],
    trace_id: str | None = None,
    operation_id: uuid.UUID | str | None = None,
    iteration: int | None = None,
    redaction_policy_version: str = "redaction.v1",
) -> dict[str, Any]:
    ...
```
</interfaces>
</context>

<source_audit>
| Source | ID | Required item | Plan | Status | Notes |
|--------|----|---------------|------|--------|-------|
| GOAL | phase-28 | Establish minimal decision event envelope, reason-code convention, redaction policy, and emitter foundation | 28-01 | COVERED | Tasks 1-3 implement tests, replay-owned facade, wrapper, and gates. |
| REQ | APF-05 | Minimal DecisionEventEnvelopeV1 / emitter records reason codes, versions, redaction policy, and run/tenant/trace identity | 28-01 | COVERED | Frontmatter requirements includes APF-05; all tasks verify APF-05. |
| CONTEXT | D-01 | Add explicit DecisionEventEnvelopeV1 schema | 28-01 | COVERED | Tasks 1-2. |
| CONTEXT | D-02 | Place schema in src/replay/decision_events.py | 28-01 | COVERED | Task 2. |
| CONTEXT | D-03 | Strict schema_version, required fields, extra forbid, registered event type, operation_id condition | 28-01 | COVERED | Tasks 1-2. |
| CONTEXT | D-04 | Do not implement ReplayEventV3 parent/attempt pairing | 28-01 | COVERED | Task 2 acceptance forbids adding V3 pairing to minimal emitter. |
| CONTEXT | D-05 | No DB schema change; reuse agent_trace_events | 28-01 | COVERED | Task 2 acceptance forbids src/db model/migration edits. |
| CONTEXT | D-06 | Add emit_decision_event in replay boundary calling ReplayService.append_event | 28-01 | COVERED | Task 2. |
| CONTEXT | D-07 | Keep src.agent.events.emit_event compatibility wrapper routed through replay emitter | 28-01 | COVERED | Task 3. |
| CONTEXT | D-08 | Prefer ReplayContext trusted projection for identity | 28-01 | COVERED | Tasks 1-2. |
| CONTEXT | D-09 | Missing identity fails closed | 28-01 | COVERED | Tasks 1-2. |
| CONTEXT | D-10 | Migrate thin wrapper/key path only | 28-01 | COVERED | Task 3 forbids broad writer rewrites. |
| CONTEXT | D-11 | Standardize reason_codes list | 28-01 | COVERED | Tasks 1-2. |
| CONTEXT | D-12 | Legacy reason_code converted by compatibility wrapper | 28-01 | COVERED | Tasks 1 and 3. |
| CONTEXT | D-13 | First-seen reason_code de-duplication | 28-01 | COVERED | Tasks 1-2. |
| CONTEXT | D-14 | Versions under redacted_payload.versions | 28-01 | COVERED | Tasks 1-2. |
| CONTEXT | D-15 | Non-empty snake_case reason codes; no global allowlist | 28-01 | COVERED | Tasks 1-2. |
| CONTEXT | D-16 | Tighten common helpers and focused key-path tests | 28-01 | COVERED | Tasks 1-2. |
| CONTEXT | D-17 | resource_refs limited to stable typed refs/hashes/ids | 28-01 | COVERED | Tasks 1-2. |
| CONTEXT | D-18 | No raw business/tool/prompt/PII/secrets in resource_refs | 28-01 | COVERED | Tasks 1-2. |
| CONTEXT | D-19 | Redaction guard checks payload and refs | 28-01 | COVERED | Tasks 1-2. |
| CONTEXT | D-20 | Tests emphasize strictness, leakage, wrapper compatibility, allocator regression | 28-01 | COVERED | Tasks 1 and 3. |
| RESEARCH | R-01 | Implement replay-owned decision_events.py facade over ReplayService.append_event | 28-01 | COVERED | Task 2. |
| RESEARCH | R-02 | Add resource_refs guard; preserve advisory-lock allocator | 28-01 | COVERED | Tasks 1-2, final sequence gate. |
| RESEARCH | R-03 | Avoid DB migration and broad service-specific event payload migration | 28-01 | COVERED | Tasks 2-3. |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 decision-event contract tests</name>
  <files>tests/replay/test_decision_events.py, tests/agent/test_events.py, tests/agent/test_memory_write_node.py, tests/replay/test_sequence_allocator.py</files>
  <read_first>
    - `.planning/phases/28-decision-event-foundation/28-CONTEXT.md`
    - `.planning/phases/28-decision-event-foundation/28-VALIDATION.md`
    - `.planning/phases/28-decision-event-foundation/28-PATTERNS.md`
    - `docs/contract-spec.md` section 17.2
    - `src/replay/service.py`
    - `src/replay/schemas.py`
    - `src/replay/validators.py`
    - `src/agent/events.py`
    - `src/platform/context_projections.py`
    - `tests/replay/test_decision_events.py` (create if missing)
    - `tests/agent/test_events.py`
    - `tests/agent/test_memory_write_node.py`
    - `tests/replay/test_sequence_allocator.py`
    - `tests/replay/test_replay_service.py`
    - `tests/platform/test_context_projections.py`
  </read_first>
  <behavior>
    - Test strict schema: `DecisionEventEnvelopeV1` accepts exactly the 14 top-level fields from `contract-spec.md` section 17.2 and rejects extra top-level keys including `policy_version`, `model_version`, `tool_version`, `reason_code`, and `reason_codes`.
    - Test required fields and registry: missing `run_id`, `tenant_id`, `thread_id`, `actor`, `resource_refs`, `redaction_policy_version`, or `redacted_payload` fails validation; unregistered event types fail validation.
    - Test conditional operation id: `node_started`, `tool_call_started`, `rag_retrieval_completed`, `llm_call_failed`, and `memory_write_started` require `operation_id`; `run_status_changed`, `approval_requested`, `approval_decided`, `approval_expired`, `approval_resumed`, and demo-only `action_draft_created` may omit it in Phase 28.
    - Test trusted identity: `emit_decision_event(...)` accepts a `ReplayContext` and uses its `run_id`, `tenant_id`, `thread_id`, and `trace_id`; missing required identity in the replay-owned entrypoint raises `ValueError` and persists no row.
    - Test normalization: legacy `reason_code="scope_denied"` plus `reason_codes=["missing_permission", "scope_denied"]` stores `redacted_payload["reason_codes"] == ["scope_denied", "missing_permission"]`.
    - Test invalid reason codes: `""`, `"ScopeDenied"`, `"scope-denied"`, and `"scope denied"` raise `ValueError`; no global allowlist rejects unknown valid snake_case like `"phase_29_future_reason"`.
    - Test versions: `policy_version`, `model_version`, and `tool_version` from `ReplayContext` or explicit versions land under `redacted_payload["versions"]`; no version fields exist at the top level.
    - Test redaction: unsafe keys including `raw_prompt`, `raw_args`, `raw_payload`, `raw_tool_output`, `secret`, `credentials`, and `pii` are rejected in both `redacted_payload` and `resource_refs`.
    - Test service payload extension: service metadata such as `service_name`, `operation_name`, `decision_type`, `latency_ms`, and `error_code` is allowed only inside `redacted_payload`, never as top-level envelope fields.
    - Test ordering: extend `tests/replay/test_sequence_allocator.py` so the new `emit_decision_event(...)` facade participates in the same per-run sequence allocator as `emit_event`, `ReplayService.append_event`, and `RunLifecycleService`.
    - Update existing wrapper/allocator tests that emit `node_*`, `tool_call_*`, `rag_retrieval_*`, `llm_call_*`, or `memory_write_*` events so operation lifecycle cases pass an explicit `operation_id`; keep run status, approval lifecycle, and demo-only `action_draft_created` coverage without `operation_id`.
    - Add focused `tests/agent/test_memory_write_node.py` regressions proving a normal memory write emits `memory_write_started` plus terminal memory event with the same non-null `operation_id`, and the failure fallback emits `memory_write_failed` with a non-null `operation_id`.
  </behavior>
  <action>Create the Wave 0 tests before production implementation. Add `tests/replay/test_decision_events.py` with the exact test cases named above, using existing `_create_run(...)` and async DB patterns from `tests/replay/test_replay_service.py` and `tests/agent/test_events.py`. Update `tests/agent/test_events.py` so the wrapper delegation spy expects `src.agent.events.emit_event` to call `emit_decision_event(...)`, add a legacy `reason_code` conversion assertion, and refresh existing helper calls so operation lifecycle events pass explicit operation ids. Update `tests/agent/test_memory_write_node.py` with focused normal-write and failure-path regressions asserting memory lifecycle events carry non-null operation ids and the normal started/terminal pair shares one id. Update `tests/replay/test_sequence_allocator.py` to import `emit_decision_event` from `src.replay.decision_events`, include one decision-emitter write in `test_sequence_allocator_covers_pre_lifecycle_writer_surfaces`, and pass explicit operation ids for existing node/tool/memory lifecycle writes. This implements D-01, D-03, D-08, D-09, D-11 through D-20 as executable RED coverage before implementation.</action>
  <acceptance_criteria>
    - `tests/replay/test_decision_events.py` contains `DecisionEventEnvelopeV1`, `emit_decision_event`, `test_decision_event_envelope_rejects_extra_top_level_service_metadata`, `test_emit_decision_event_missing_required_identity_fails_closed`, `test_reason_code_compatibility_normalizes_first_seen_reason_codes`, `test_resource_refs_redaction_guard_rejects_unsafe_keys`, and `test_project_minimal_event_matches_decision_event_envelope`.
    - `tests/agent/test_events.py` contains `emit_decision_event`, `reason_code="scope_denied"`, and `reason_codes`.
    - `tests/replay/test_sequence_allocator.py` contains `emit_decision_event` and asserts a contiguous sequence list that includes the new decision emitter write.
    - `tests/agent/test_memory_write_node.py` contains assertions that normal and failure memory lifecycle event emissions carry non-null `operation_id`, and that two trace events for one normal write share the same `operation_id`.
    - `rg -n "policy_version|model_version|tool_version|reason_code|reason_codes" tests/replay/test_decision_events.py` shows tests asserting these fields are forbidden at top level and allowed only under `redacted_payload`.
    - The RED verification command runs the new `tests/replay/test_decision_events.py`, exits non-zero before Task 2, and matches an expected missing `src.replay.decision_events` module or planned-symbol import failure so unrelated dirty-worktree failures cannot satisfy the RED gate.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set +e; output=$(uv run pytest tests/replay/test_decision_events.py -q 2>&1); status=$?; printf "%s\n" "$output"; test "$status" -ne 0 && printf "%s\n" "$output" | rg "No module named .src\\.replay\\.decision_events.|cannot import name .(DecisionEventEnvelopeV1|emit_decision_event). from .src\\.replay\\.decision_events"'</automated>
  </verify>
  <done>Wave 0 tests are present, specific to APF-05, and fail for missing/unimplemented Phase 28 production behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement replay-owned decision event facade</name>
  <files>src/replay/decision_events.py, src/replay/validators.py, src/replay/service.py, src/replay/__init__.py</files>
  <read_first>
    - `.planning/phases/28-decision-event-foundation/28-CONTEXT.md`
    - `.planning/phases/28-decision-event-foundation/28-RESEARCH.md`
    - `.planning/phases/28-decision-event-foundation/28-PATTERNS.md`
    - `docs/contract-spec.md` sections 0.2, 8.0, 17.2, and 18.4
    - `docs/target-agent-platform-architecture-plan.md` section 14
    - `src/replay/decision_events.py` (create if missing)
    - `src/replay/validators.py`
    - `src/replay/service.py`
    - `src/replay/schemas.py`
    - `src/replay/__init__.py`
    - `src/platform/context_projections.py`
    - `src/platform/trusted_context.py`
    - `tests/replay/test_decision_events.py`
    - `tests/replay/test_sequence_allocator.py`
    - `tests/platform/test_context_projections.py`
  </read_first>
  <behavior>
    - `DecisionEventEnvelopeV1.model_validate(payload)` returns a strict model for only the minimal envelope fields and raises `pydantic.ValidationError` on extras.
    - `emit_decision_event(session, replay_context=ReplayContext(...), ...)` persists through `ReplayService.append_event(..., schema_version="minimal_event_envelope.v1")` and returns a validated `DecisionEventEnvelopeV1` dict.
    - `emit_decision_event(...)` may accept legacy explicit `run_id`, `tenant_id`, `thread_id`, and optional `trace_id`, but when `ReplayContext` is absent and any required identity field is missing it raises `ValueError` before calling `ReplayService.append_event`.
    - `redacted_payload["reason_codes"]` is always a `list[str]` when a reason is provided; duplicate values are removed in first-seen order.
    - `redacted_payload["versions"]` contains only non-empty `policy_version`, `model_version`, and `tool_version` values; `redaction_policy_version` remains the envelope field.
    - `guard_resource_refs(resource_refs)` recursively rejects the same forbidden unsafe keys as `guard_redacted_payload(...)` using path root `resource_refs`.
  </behavior>
  <action>Create `src/replay/decision_events.py` per D-01, D-02, D-03, D-06, D-08, D-09, D-11 through D-19. Define `DecisionEventEnvelopeV1` with `model_config = ConfigDict(extra="forbid")`, `schema_version: Literal["minimal_event_envelope.v1"] = "minimal_event_envelope.v1"`, `event_id: UUID`, `sequence: int = Field(gt=0)`, `operation_id: UUID | None = None`, `run_id: UUID`, `tenant_id: UUID`, `thread_id: str = Field(min_length=1)`, `trace_id: str | None = None`, `event_type: str`, `occurred_at: datetime`, `actor: dict[str, Any]`, `resource_refs: dict[str, Any]`, `redaction_policy_version: str = Field(min_length=1)`, and `redacted_payload: dict[str, Any]`. Validate `event_type` with `validate_event_type(...)`; require `operation_id` for event types starting with `node_`, `tool_call_`, `rag_retrieval_`, `llm_call_`, or `memory_write_`; do not require `operation_id` for `run_status_changed`, `approval_*` lifecycle events, or demo-only `action_draft_created` in Phase 28 per D-04 and D-10. Implement `emit_decision_event(...)` with optional `replay_context: ReplayContext | None`, optional legacy `run_id`, `tenant_id`, `thread_id`, `trace_id`, keyword-only `reason_code: str | None`, `reason_codes: list[str] | None`, and `versions: dict[str, str] | None`; prefer `ReplayContext` identity when present. Normalize reasons with regex `^[a-z][a-z0-9_]*$`, first-seen de-duplication, and no global allowlist per D-15. Merge `ReplayContext.policy_version`, `ReplayContext.model_version`, `ReplayContext.tool_version`, and explicit `versions` into `redacted_payload["versions"]`. Use a lazy import of `ReplayService` inside `emit_decision_event(...)` to avoid circular imports, call `ReplayService(session).append_event(..., schema_version="minimal_event_envelope.v1")`, and validate the returned dict with `DecisionEventEnvelopeV1.model_validate(...).model_dump(mode="python")`. Add `guard_resource_refs(...)` to `src/replay/validators.py`, call it in `ReplayService.append_event(...)` before row construction, and update `ReplayService.project_minimal_event(...)` to validate its projection through `DecisionEventEnvelopeV1`. Export `DecisionEventEnvelopeV1`, `emit_decision_event`, and `guard_resource_refs` from `src/replay/__init__.py`. Do not edit `src/db/models.py` or `src/db/migrations/**` per D-05.</action>
  <acceptance_criteria>
    - `src/replay/decision_events.py` contains `class DecisionEventEnvelopeV1`, `schema_version: Literal["minimal_event_envelope.v1"]`, `model_config = ConfigDict(extra="forbid")`, `async def emit_decision_event`, `ReplayContext`, `reason_code`, `reason_codes`, `versions`, and `REASON_CODE_PATTERN`.
    - `src/replay/decision_events.py` contains the regex string `^[a-z][a-z0-9_]*$` and does not contain any global allowlist variable named `REASON_CODE_ALLOWLIST`.
    - `src/replay/validators.py` contains `def guard_resource_refs` and `walk(resource_refs, "resource_refs")`.
    - `src/replay/service.py` contains `guard_resource_refs(resource_refs)` and `DecisionEventEnvelopeV1.model_validate`.
    - `src/replay/__init__.py` contains `DecisionEventEnvelopeV1`, `emit_decision_event`, and `guard_resource_refs` in imports and `__all__`.
    - `git diff --name-only -- src/db/models.py src/db/migrations` prints no paths.
    - `rg -n "parent_operation_id|attempt" src/replay/decision_events.py` shows no required minimal-envelope validation for V3 parent/attempt pairing.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/replay/test_decision_events.py tests/replay/test_sequence_allocator.py tests/platform/test_context_projections.py -q</automated>
  </verify>
  <done>Replay-owned decision event schema, emitter, normalization, resource-ref guard, and minimal projection validation exist and satisfy APF-05 without a DB schema migration or a parallel event format.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route legacy event wrapper, patch memory key path, and run final gates</name>
  <files>src/agent/events.py, src/agent/nodes/memory_write.py, tests/agent/test_events.py, tests/agent/test_memory_write_node.py</files>
  <read_first>
    - `.planning/phases/28-decision-event-foundation/28-CONTEXT.md`
    - `.planning/phases/28-decision-event-foundation/28-VALIDATION.md`
    - `src/agent/events.py`
    - `src/agent/nodes/memory_write.py`
    - `src/replay/decision_events.py`
    - `src/replay/service.py`
    - `src/replay/validators.py`
    - `src/agent/nodes/investigate.py`
    - `src/agent/nodes/memory_write.py`
    - `src/approvals/events.py`
    - `src/actions/service.py`
    - `tests/agent/test_events.py`
    - `tests/replay/test_decision_events.py`
    - `tests/replay/test_sequence_allocator.py`
  </read_first>
  <behavior>
    - Existing callers of `src.agent.events.emit_event(...)` keep using the same required arguments and receive the same minimal envelope dict shape.
    - The wrapper delegates to `src.replay.decision_events.emit_decision_event(...)` instead of directly calling `ReplayService(session).append_event(...)`.
    - The wrapper accepts optional legacy `reason_code` and `reason_codes` keyword-only arguments and passes them to the replay-owned emitter for normalization.
    - The wrapper keeps `MINIMAL_EVENT_TYPES`, `EVENT_RETENTION_CLASSIFICATION`, `SCHEMA_VERSION`, `FORBIDDEN_REDACTED_PAYLOAD_KEYS`, `allocate_sequence(...)`, and `classify_event_family(...)` usable for current imports.
    - `src.agent.nodes.memory_write` makes only a narrow event-helper compatibility change: one generated memory write `operation_id` is passed to started, terminal, and outer failure-fallback memory events. It does not change memory policy, candidate construction, storage semantics, or redacted payload shape.
    - No broad rewrite of `src.agent.nodes.memory_write`, `src.agent.nodes.investigate`, `src.approvals.events`, or `src.actions.service` is performed in this phase.
  </behavior>
  <action>Update `src/agent/events.py` per D-07, D-10, D-12, and D-20. Replace the direct `ReplayService(session).append_event(..., schema_version=SCHEMA_VERSION)` call inside `emit_event(...)` with `await emit_decision_event(session, run_id=run_id, tenant_id=tenant_id, thread_id=thread_id, trace_id=trace_id, event_type=event_type, actor=actor, resource_refs=resource_refs, redacted_payload=redacted_payload, operation_id=operation_id, iteration=iteration, redaction_policy_version=redaction_policy_version, reason_code=reason_code, reason_codes=reason_codes)`. Keep `ReplayService` imported only for `allocate_sequence(...)` unless the module no longer needs it elsewhere. Add optional keyword-only parameters `reason_code: str | None = None` and `reason_codes: list[str] | None = None` at the end of `emit_event(...)` so existing positional/keyword callers remain compatible. Update `src/agent/nodes/memory_write.py` narrowly: generate one `memory_operation_id = uuid.uuid4()` in `memory_write(...)` after a session is available and before the `asyncio.wait_for(...)` call, pass it into `_write_with_service(...)` and the outer exception fallback `_emit_memory_event(...)`, add an optional `operation_id` parameter to `_write_with_service(...)` and `_emit_memory_event(...)`, and forward it to `emit_event(...)` for `memory_write_started`, `memory_write_completed`, and `memory_write_failed`. Update `tests/agent/test_events.py` from the old `ReplayService` spy to an `emit_decision_event` spy and assert `schema_version == "minimal_event_envelope.v1"`, `iteration == 3`, and legacy `reason_code` conversion to `redacted_payload["reason_codes"]`. Update `tests/agent/test_memory_write_node.py` for normal and failure memory operation-id regressions. Do not broaden service-specific top-level envelope keys; service metadata remains in `redacted_payload`, `redacted_payload.versions`, or typed `resource_refs`.</action>
  <acceptance_criteria>
    - `src/agent/events.py` contains `from src.replay.decision_events import emit_decision_event`.
    - `src/agent/events.py` contains `reason_code: str | None = None` and `reason_codes: list[str] | None = None` in `emit_event(...)`.
    - `rg -n "ReplayService\\(session\\)\\.append_event" src/agent/events.py` prints no matches.
    - `tests/agent/test_events.py` contains `monkeypatch.setattr(events_module, "emit_decision_event"` and `reason_code="scope_denied"`.
    - `src/agent/nodes/memory_write.py` forwards `operation_id=` to `emit_event(...)` and does not change memory policy, candidate construction, or storage calls.
    - `tests/agent/test_memory_write_node.py` asserts normal memory write trace events share one non-null operation id and failure memory event emissions carry a non-null operation id.
    - `rg -n "service_name|operation_name|decision_type|policy_version|model_version|tool_version|reason_codes" src/agent/events.py` shows no new top-level envelope field construction.
    - Capture a pre-task baseline with `git status --short -- src/agent/nodes/investigate.py src/approvals/events.py src/actions/service.py`, then confirm Task 3 introduces no new changes to those read-only writer files beyond that baseline.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/replay/test_decision_events.py tests/agent/test_events.py tests/agent/test_memory_write_node.py -q</automated>
    <automated>uv run pytest tests/replay/test_sequence_allocator.py tests/platform/test_context_projections.py -q</automated>
    <automated>uv run pytest tests/replay tests/agent/test_events.py tests/agent/test_memory_write_node.py tests/platform/test_context_projections.py -q</automated>
  </verify>
  <done>The legacy wrapper remains compatible, delegates to the replay-owned decision emitter, and the final targeted suite passes with ordering, redaction, missing identity, normalization, and projection coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TrustedContextFactory -> ReplayContext -> emit_decision_event | Trusted API/auth/run identity crosses into replay audit truth. User or LLM payload fields must not override run_id, tenant_id, thread_id, or trace_id. |
| Service writer -> redacted_payload/resource_refs -> AgentTraceEvent | Service-provided event metadata crosses into durable audit storage. Raw prompts, raw tools, raw business payloads, PII, and secrets must be rejected. |
| Wrapper compatibility -> replay-owned emitter | Legacy callers enter the new contract. Compatibility must not create a second envelope or widen top-level fields. |
| Concurrent writers -> ReplayService allocator -> agent_trace_events | Multiple services append events for one run. The shared allocator and DB unique constraint preserve monotonic ordering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Spoofing | `src/replay/decision_events.py::emit_decision_event` | mitigate | Prefer `ReplayContext` identity from Phase 27 projection; require `run_id`, `tenant_id`, and `thread_id` before append; missing required identity raises `ValueError` and persists no row. |
| T-28-02 | Information Disclosure | `src/replay/validators.py`, `src/replay/service.py` | mitigate | Apply recursive forbidden-key guards to both `redacted_payload` and `resource_refs` before persistence; test keys `raw_prompt`, `raw_args`, `raw_payload`, `raw_tool_output`, `secret`, `credentials`, and `pii`. |
| T-28-03 | Tampering | `DecisionEventEnvelopeV1`, `validate_event_type` | mitigate | Use Pydantic `ConfigDict(extra="forbid")`, registered event type validation, and tests rejecting unregistered events and extra top-level keys. |
| T-28-04 | Tampering / Repudiation | `ReplayService.append_event`, sequence allocator | mitigate | Preserve `pg_advisory_xact_lock`, UUIDv5 event id from `(run_id, sequence)`, and unique `(run_id, sequence)` behavior; add `emit_decision_event` to sequence allocator regression coverage. |
| T-28-05 | Repudiation | Minimal envelope field set | mitigate | Validate `project_minimal_event(...)` through `DecisionEventEnvelopeV1`; tests forbid top-level `policy_version`, `model_version`, `tool_version`, `reason_code`, `reason_codes`, and service-specific metadata. |
</threat_model>

<verification>
Run these in order after all tasks:

1. `uv run pytest tests/replay/test_decision_events.py tests/agent/test_events.py tests/agent/test_memory_write_node.py -q`
2. `uv run pytest tests/replay/test_sequence_allocator.py tests/platform/test_context_projections.py -q`
3. `uv run pytest tests/replay tests/agent/test_events.py tests/agent/test_memory_write_node.py tests/platform/test_context_projections.py -q`
4. `git diff --name-only -- src/db/models.py src/db/migrations` must print no paths.
5. `rg -n "DecisionEventEnvelopeV1|emit_decision_event|guard_resource_refs|operation_id" src/replay src/agent/events.py src/agent/nodes/memory_write.py tests/replay/test_decision_events.py tests/agent/test_events.py tests/agent/test_memory_write_node.py` must show the new contract, emitter, guard, wrapper, and memory operation-id references.
</verification>

<success_criteria>
- `DecisionEventEnvelopeV1` maps to the existing `minimal_event_envelope.v1` projection and validates exactly the contract-spec section 17.2 top-level field set.
- `emit_decision_event(...)` records trusted run/tenant/thread/trace identity, operation id when required, actor, resource refs, redaction policy, reason codes, and policy/model/tool versions through `ReplayService.append_event(...)`.
- `src.agent.events.emit_event(...)` remains a compatibility wrapper and routes through the replay-owned emitter.
- Redaction guard coverage rejects unsafe keys in `redacted_payload` and `resource_refs`.
- Contract tests cover ordering, redaction, missing identity, missing required fields, operation-id condition, reason/version normalization, wrapper compatibility, and service-level payload extension.
- No DB schema migration, no `ReplayEventV3` parent/attempt pairing implementation, and no service-specific top-level envelope widening are introduced in Phase 28.
</success_criteria>

<output>
After completion, create `.planning/phases/28-decision-event-foundation/28-01-SUMMARY.md`.
</output>
