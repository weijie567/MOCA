---
phase: 39-contract-spec-12-5-12-6-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/contract-spec.md
autonomous: true
requirements:
  - TPH-02
must_haves:
  truths:
    - "`docs/contract-spec.md` §12.5 documents implemented `ToolCallContext` local/action-safety fields without redefining §8.0 identity projections."
    - "`docs/contract-spec.md` §12.6 documents implemented `ToolDescriptor` metadata, `event_family=\"action\"`, and `ToolPolicyDecision` availability fields."
    - "Commit `4dcb673` has been rechecked before editing and is confirmed not to have modified §12.5/§12.6."
    - "The implementation diff is docs-only unless a later blocker is explicitly justified."
    - "The spec change has validation evidence and a dual-AI review/adjudication record before TPH-02 is treated as complete."
  artifacts:
    - path: "docs/contract-spec.md"
      provides: "Normative §8.0 projection note, §12.5 ToolCallContext, and §12.6 tool platform contract reconciliation"
      contains: "effective_at"
    - path: ".planning/phases/39-contract-spec-12-5-12-6-reconciliation/39-01-SUMMARY.md"
      provides: "Execution evidence, validation results, and dual-AI review/adjudication record"
      contains: "Dual-AI Review Gate"
  key_links:
    - from: "docs/contract-spec.md §12.5"
      to: "src/tools/contracts.py::ToolCallContext"
      via: "field parity for tool-call-local fields"
      pattern: "effective_at: str \\| None = None"
    - from: "docs/contract-spec.md §12.6"
      to: "src/tools/catalog.py::ToolDescriptor"
      via: "descriptor metadata and event_family optionality"
      pattern: "event_family: Literal\\[\"tool_call_\\*\", \"rag_retrieval_\\*\", \"action\"\\] \\| None"
    - from: "docs/contract-spec.md §12.6"
      to: "src/tools/contracts.py::ToolPolicyDecision"
      via: "runtime availability metadata"
      pattern: "runtime_available: bool \\| None = None"
---

<objective>
Reconcile `docs/contract-spec.md` §12.5 and §12.6 with the already-implemented tool contract fields for TPH-02.

Purpose: Phase 37/38 finalized the catalog/runtime/policy implementation shape; Phase 39 makes the normative spec catch up without changing production code or weakening §8.0 `TrustedContext` identity ownership.
Output: A docs-only spec patch with pre-edit commit evidence, structural/test validation, and a recorded dual-AI review/adjudication gate.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-contract-spec-12-5-12-6-reconciliation/39-RESEARCH.md
@.planning/phases/39-contract-spec-12-5-12-6-reconciliation/39-VALIDATION.md
@.planning/phases/39-contract-spec-12-5-12-6-reconciliation/39-PATTERNS.md
@.planning/phases/37-tool-declaration-runtime-policy-internal-consolidation/37-01-SUMMARY.md
@.planning/phases/37-tool-declaration-runtime-policy-internal-consolidation/37-02-SUMMARY.md
@.planning/phases/37-tool-declaration-runtime-policy-internal-consolidation/37-03-SUMMARY.md
@.planning/phases/38-output-schema-declaration-runtime-output-validation-enforcem/38-VERIFICATION.md
@.planning/phases/38-output-schema-declaration-runtime-output-validation-enforcem/38-REVIEW.md
@docs/contract-spec.md
@src/tools/contracts.py
@src/tools/catalog.py
@src/tools/policy.py
@src/tools/runtime.py
@tests/architecture/test_trusted_context_boundaries.py
@tests/tools/test_catalog.py
@tests/tools/test_tool_platform.py

<interfaces>
Implemented source contracts to reflect in the spec:

```python
class ToolCallContext(BaseModel):
    deadline_at: datetime | None = None
    effective_at: str | None = None
    attempt: int = 1
    max_attempts: int = 1
    idempotency_key: str | None = None
    approval_ref: str | None = None
    safety_snapshot_ref: str | None = None
    policy_snapshot_ref: str | None = None
```

```python
class ToolDescriptor(BaseModel):
    name: str
    description: str = ""
    kind: Literal["read", "retrieval", "write"]
    input_schema: dict[str, Any]
    output_schema: dict[str, Any]
    risk_level: Literal["read", "retrieval", "write"]
    side_effect: Literal["none", "read_only", "retrieval", "write"]
    required_permission: str
    caller_allowlist: list[str]
    event_family: Literal["tool_call_*", "rag_retrieval_*", "action"] | None
    resource_type: str | None
    executor: Literal["business", "knowledge", "memory", "action"] | None = None
    exposure: Literal["planner_visible", "node_only", "internal"] = "planner_visible"
    requires_approval: bool = False
    requires_safety_snapshot: bool = False
    requires_idempotency_key: bool = False
```

```python
class ToolPolicyDecision(BaseModel):
    resource_scope_binding: dict[str, Any] | None = None
    runtime_available: bool | None = None
    availability_summary: str | None = None
```
</interfaces>

<plan_granularity>
One plan is sufficient and required: Phase 39 has one requirement (`TPH-02`), one implementation ownership domain (`docs/contract-spec.md` as the normative spec), one wave, and no production-code boundary to split. Source/test files are read-only evidence and validation surfaces. Splitting would add coordination overhead without reducing file conflict or service-boundary risk.
</plan_granularity>

<source_audit>
SOURCE | ID | Feature / Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | Phase 39 | §12.5/§12.6 match implemented contract fields without redefining §8.0 identity fields | 39-01 | COVERED | Tasks 1-3 cover evidence, docs edit, validation/review.
REQ | TPH-02 | Add implemented-but-unspecified fields and protect §8.0 identity projections | 39-01 | COVERED | `requirements: [TPH-02]`; Task 2 has exact target lines.
RESEARCH | Single docs-only plan | Prefer one spec-only plan; avoid production code changes | 39-01 | COVERED | Frontmatter `files_modified` is `docs/contract-spec.md` only.
RESEARCH | Commit `4dcb673` check | Re-check prior memory commit before editing | 39-01 | COVERED | Task 1 runs `git show` and no-§12.5/§12.6 hunk check.
RESEARCH | Optional §8.0 note | Minimal local-field list update only | 39-01 | COVERED | Task 2 allows only local-field additions to the ToolCallContext projection note.
RESEARCH | Event family optionality | Ground `| None` choice in `src/tools/catalog.py` | 39-01 | COVERED | Task 2 requires `Literal[..., "action"] | None`.
RESEARCH | Validation commands | Use `uv run pytest`, structural `rg`, `git diff --check` | 39-01 | COVERED | Task 3 uses exact commands from `39-VALIDATION.md`.
CONTEXT | n/a | No phase `CONTEXT.md` exists | 39-01 | COVERED | No locked discuss-phase D-XX decisions to map.
</source_audit>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconfirm pre-edit commit and implementation evidence</name>
  <files>docs/contract-spec.md</files>
  <read_first>
    - AGENTS.md
    - .planning/STATE.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/39-contract-spec-12-5-12-6-reconciliation/39-RESEARCH.md
    - .planning/phases/39-contract-spec-12-5-12-6-reconciliation/39-VALIDATION.md
    - docs/contract-spec.md
    - src/tools/contracts.py
    - src/tools/catalog.py
    - src/tools/policy.py
    - src/tools/runtime.py
  </read_first>
  <action>
    Do not edit files in this task. Re-run the required pre-edit evidence checks:
    `git show --stat --oneline 4dcb673 -- docs/contract-spec.md`;
    `git show --unified=80 4dcb673 -- docs/contract-spec.md`;
    `uv run python -c 'from src.tools.contracts import ToolCallContext, ToolPolicyDecision; from src.tools.catalog import ToolDescriptor; print("ToolCallContext", sorted(ToolCallContext.model_fields)); print("ToolDescriptor", sorted(ToolDescriptor.model_fields)); print("ToolPolicyDecision", sorted(ToolPolicyDecision.model_fields))'`.
    Confirm and record in the eventual summary that commit `4dcb673` touches the `memory_write_events` hunk only and does not show a §12.5/§12.6 hunk or any `ToolCallContext`, `ToolDescriptor`, or `ToolPolicyDecision` edit. Confirm current on-disk §12.5/§12.6 still lack the TPH-02 additions before Task 2 edits.
  </action>
  <verify>
    <automated>git show --stat --oneline 4dcb673 -- docs/contract-spec.md && git show --unified=80 4dcb673 -- docs/contract-spec.md</automated>
    <automated>bash -lc 'if git show --unified=80 4dcb673 -- docs/contract-spec.md | rg -n "ToolCallContext|ToolDescriptor|ToolPolicyDecision|### 12\\.5|### 12\\.6"; then exit 1; else exit 0; fi'</automated>
    <automated>uv run python -c 'from src.tools.contracts import ToolCallContext, ToolPolicyDecision; from src.tools.catalog import ToolDescriptor; print("ToolCallContext", sorted(ToolCallContext.model_fields)); print("ToolDescriptor", sorted(ToolDescriptor.model_fields)); print("ToolPolicyDecision", sorted(ToolPolicyDecision.model_fields))'</automated>
  </verify>
  <acceptance_criteria>
    - `git show --unified=80 4dcb673 -- docs/contract-spec.md | rg -n "memory_write_events"` finds the touched hunk.
    - The no-§12.5/§12.6 `bash -lc` command above exits 0.
    - The field inventory output includes `effective_at`, `approval_ref`, `safety_snapshot_ref`, `executor`, `exposure`, `requires_approval`, `requires_safety_snapshot`, `requires_idempotency_key`, `runtime_available`, and `availability_summary`.
    - No production or docs edit is made by this task.
  </acceptance_criteria>
  <done>Pre-edit evidence is fresh and the executor has confirmed the current implementation fields that the spec must document.</done>
</task>

<task type="auto">
  <name>Task 2: Patch §8.0 note, §12.5, and §12.6 to the exact implemented contract state</name>
  <files>docs/contract-spec.md</files>
  <read_first>
    - docs/contract-spec.md
    - src/tools/contracts.py
    - src/tools/catalog.py
    - src/tools/policy.py
    - src/tools/runtime.py
    - tests/architecture/test_trusted_context_boundaries.py
    - tests/tools/test_catalog.py
    - tests/tools/test_tool_platform.py
    - .planning/phases/39-contract-spec-12-5-12-6-reconciliation/39-PATTERNS.md
    - .planning/phases/39-contract-spec-12-5-12-6-reconciliation/39-RESEARCH.md
  </read_first>
  <action>
    Edit only `docs/contract-spec.md`.
    In §8.0 projection table, if the `ToolCallContext` note still lists local fields without the new action-safety/local fields, make the minimal note-only addition so the tool-call-local list includes `effective_at`, `approval_ref`, and `safety_snapshot_ref`. Do not change the protected projection field list: `tenant_id`, `user_id`, `role`, `permissions`, `merchant_scope`, `session_id`, `thread_id`, `run_id`, `trace_id`.
    In §12.5 `ToolCallContext`, preserve the paragraph saying identity/scope/permission fields are §8.0 `TrustedContext` projections and add exactly these local/action-safety fields in the code block:
    `effective_at: str | None = None` after `deadline_at`;
    `approval_ref: str | None = None` and `safety_snapshot_ref: str | None = None` after `idempotency_key`.
    Do not widen or rename `merchant_scope`, `permissions`, or any other protected identity/scope field in §12.5.
    In §12.6 `ToolDescriptor`, update the code block to include implemented descriptor fields. Add `description: str = ""` for current descriptor parity, update `event_family` to `Literal["tool_call_*", "rag_retrieval_*", "action"] | None`, and add:
    `executor: Literal["business", "knowledge", "memory", "action"] | None = None`;
    `exposure: Literal["planner_visible", "node_only", "internal"] = "planner_visible"`;
    `requires_approval: bool = False`;
    `requires_safety_snapshot: bool = False`;
    `requires_idempotency_key: bool = False`.
    The `| None` on `event_family` and `executor` is required because `src/tools/catalog.py::ToolDescriptor` implements both fields as optional; do not add a default to `event_family` because the implementation field has no default.
    In §12.6 `ToolPolicyDecision`, add `runtime_available: bool | None = None` and `availability_summary: str | None = None` after `resource_scope_binding`.
    Do not add production-code changes, new tests, new tools, new policy gates, action output semantics, or `ToolResultV2` envelope fields.
  </action>
  <verify>
    <automated>rg -n "effective_at|approval_ref|safety_snapshot_ref" docs/contract-spec.md</automated>
    <automated>rg -n "executor|exposure|requires_approval|requires_safety_snapshot|requires_idempotency_key" docs/contract-spec.md</automated>
    <automated>rg -n "event_family: Literal\\[.*action|runtime_available|availability_summary" docs/contract-spec.md</automated>
    <automated>uv run pytest tests/architecture/test_trusted_context_boundaries.py -q</automated>
    <automated>git diff --name-only -- docs/contract-spec.md src/tools/contracts.py src/tools/catalog.py src/tools/policy.py src/tools/runtime.py tests</automated>
  </verify>
  <acceptance_criteria>
    - §12.5 contains `effective_at: str | None = None`, `approval_ref: str | None = None`, and `safety_snapshot_ref: str | None = None`.
    - §12.6 contains `description: str = ""`, `event_family: Literal["tool_call_*", "rag_retrieval_*", "action"] | None`, `executor: Literal["business", "knowledge", "memory", "action"] | None = None`, `exposure: Literal["planner_visible", "node_only", "internal"] = "planner_visible"`, `requires_approval: bool = False`, `requires_safety_snapshot: bool = False`, `requires_idempotency_key: bool = False`, `runtime_available: bool | None = None`, and `availability_summary: str | None = None`.
    - `git diff --name-only -- docs/contract-spec.md src/tools/contracts.py src/tools/catalog.py src/tools/policy.py src/tools/runtime.py tests` prints `docs/contract-spec.md` only.
    - `git diff -- docs/contract-spec.md | rg -n '^[+-]\\s+(tenant_id|user_id|role|permissions|merchant_scope|session_id|thread_id|run_id|trace_id):'` has no matches.
    - The exact §12.5 paragraph still states that the nine identity/scope/permission fields are §8.0 canonical `TrustedContext` projections and are not redefined in §12.5.
  </acceptance_criteria>
  <done>The normative spec reflects the implemented local/action-safety, descriptor, action event-family, and availability metadata fields while preserving §8.0 identity ownership.</done>
</task>

<task type="auto">
  <name>Task 3: Run structural validation, focused tests, and review gate record</name>
  <files>docs/contract-spec.md, .planning/LOCAL-VALIDATION-ISSUES.md</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/39-contract-spec-12-5-12-6-reconciliation/39-VALIDATION.md
    - .planning/phases/39-contract-spec-12-5-12-6-reconciliation/39-RESEARCH.md
    - .planning/phases/38-output-schema-declaration-runtime-output-validation-enforcem/38-VERIFICATION.md
    - .planning/phases/38-output-schema-declaration-runtime-output-validation-enforcem/38-REVIEW.md
    - docs/contract-spec.md
    - tests/architecture/test_trusted_context_boundaries.py
    - tests/tools/test_catalog.py
    - tests/tools/test_tool_platform.py
    - .planning/LOCAL-VALIDATION-ISSUES.md
  </read_first>
  <action>
    Run the exact Phase 39 structural checks from `39-VALIDATION.md` plus the focused and full validation suites using MOCA-approved entrypoints only. Never use bare `pytest` or bare `python -m pytest`.
    Run:
    `rg -n "effective_at|approval_ref|safety_snapshot_ref" docs/contract-spec.md`;
    `rg -n "executor|exposure|requires_approval|requires_safety_snapshot|requires_idempotency_key" docs/contract-spec.md`;
    `rg -n "event_family: Literal\\[.*action|runtime_available|availability_summary" docs/contract-spec.md`;
    `git show --stat --oneline 4dcb673 -- docs/contract-spec.md`;
    `git show --unified=80 4dcb673 -- docs/contract-spec.md`;
    `git diff --name-only -- docs/contract-spec.md src/tools/contracts.py src/tools/catalog.py src/tools/policy.py src/tools/runtime.py tests`;
    `git diff --check`;
    `uv run pytest tests/architecture/test_trusted_context_boundaries.py tests/tools/test_catalog.py::test_action_descriptor_is_node_only_and_requires_idempotency tests/tools/test_tool_platform.py::test_tool_policy_decision_is_not_an_event_envelope tests/tools/test_tool_platform.py::test_runtime_auth_gate_sequence_is_declarative_and_ordered tests/tools/test_tool_platform.py::test_runtime_auth_declarative_gates_preserve_multi_denial_reason_order -q`;
    `uv run pytest tests/architecture/test_trusted_context_boundaries.py tests/tools/test_catalog.py tests/tools/test_tool_platform.py -q`.
    Before writing the summary, include a `Dual-AI Review Gate` section. It must record: GSD-native plan/checker or structural validation result, independent cross-review result on the docs diff, and adjudication for every review issue with repository evidence. If the current runtime cannot obtain the required external/second-AI review, stop and report the missing review as a blocker instead of marking TPH-02 complete.
    If any local validation fails because of environment/tooling rather than the spec diff, append a Chinese entry to `.planning/LOCAL-VALIDATION-ISSUES.md` with: problem symptom, reproduction/detection command, key evidence, current root-cause judgment, handling done, remaining issue, and next entry point. Do not stage or include `.planning/LOCAL-VALIDATION-ISSUES.md` unless that new entry is necessary and intentionally part of the Phase 39 evidence.
  </action>
  <verify>
    <automated>rg -n "effective_at|approval_ref|safety_snapshot_ref" docs/contract-spec.md && rg -n "executor|exposure|requires_approval|requires_safety_snapshot|requires_idempotency_key" docs/contract-spec.md && rg -n "event_family: Literal\\[.*action|runtime_available|availability_summary" docs/contract-spec.md</automated>
    <automated>git show --stat --oneline 4dcb673 -- docs/contract-spec.md && git show --unified=80 4dcb673 -- docs/contract-spec.md</automated>
    <automated>git diff --name-only -- docs/contract-spec.md src/tools/contracts.py src/tools/catalog.py src/tools/policy.py src/tools/runtime.py tests && git diff --check</automated>
    <automated>uv run pytest tests/architecture/test_trusted_context_boundaries.py tests/tools/test_catalog.py::test_action_descriptor_is_node_only_and_requires_idempotency tests/tools/test_tool_platform.py::test_tool_policy_decision_is_not_an_event_envelope tests/tools/test_tool_platform.py::test_runtime_auth_gate_sequence_is_declarative_and_ordered tests/tools/test_tool_platform.py::test_runtime_auth_declarative_gates_preserve_multi_denial_reason_order -q</automated>
    <automated>uv run pytest tests/architecture/test_trusted_context_boundaries.py tests/tools/test_catalog.py tests/tools/test_tool_platform.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All required `rg` structural checks find the target fields in `docs/contract-spec.md`.
    - `git diff --name-only -- docs/contract-spec.md src/tools/contracts.py src/tools/catalog.py src/tools/policy.py src/tools/runtime.py tests` prints only `docs/contract-spec.md` unless the summary records an explicit justified blocker.
    - `git diff --check` passes.
    - Both `uv run pytest` commands in this task pass, or any failure is recorded in `.planning/LOCAL-VALIDATION-ISSUES.md` only when it is an environment/tooling issue and not a product/spec mismatch.
    - The summary contains `Dual-AI Review Gate`, `4dcb673`, `docs-only diff`, and `TPH-02`.
    - `rg -n "bare pytest|python -m pytest" .planning/phases/39-contract-spec-12-5-12-6-reconciliation/39-01-SUMMARY.md` has no matches after the summary is written.
  </acceptance_criteria>
  <done>Phase 39 has structural proof, focused test proof, docs-only diff proof, and a review/adjudication record before completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| §8.0 TrustedContext -> §12.5 ToolCallContext | Trusted identity/scope fields are projected into tool context and must not be re-owned by §12.5. |
| ToolDescriptor -> ToolView prompt surface | Descriptor metadata includes internal executor/exposure/safety fields that must not become prompt-visible fields. |
| ToolPolicyDecision -> runtime authorization | Runtime availability metadata must clarify availability without making planner visibility equivalent to runtime authorization. |
| Action safety refs -> write tool invocation | Approval/safety/idempotency references must be documented as local action-safety inputs, not identity or permission substitutes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Spoofing / Elevation of Privilege | `docs/contract-spec.md` §12.5 | mitigate | Preserve the existing §8.0 projection statement and forbid protected identity/scope field redefinition, widening, or renaming. |
| T-39-02 | Information Disclosure | `ToolDescriptor` metadata in §12.6 | mitigate | Document descriptor metadata as catalog/runtime contract while preserving `ToolView` as the prompt-safe surface; validate with `test_tool_policy_decision_is_not_an_event_envelope` and catalog metadata tests. |
| T-39-03 | Tampering / Elevation of Privilege | `ToolPolicyDecision.runtime_available` / `availability_summary` | mitigate | Add availability fields and keep planner visibility distinct from runtime authorization; validate runtime gate tests. |
| T-39-04 | Tampering / Repudiation | `approval_ref`, `safety_snapshot_ref`, `idempotency_key` | mitigate | Document action-safety refs as local fields consumed by runtime gates and verify ordered action-safety gate behavior. |
| T-39-05 | Repudiation | Commit/spec reconciliation evidence | mitigate | Require `4dcb673` pre-edit evidence and docs-only diff proof in the summary. |
</threat_model>

<verification>
Overall Phase 39 verification:
- `git show --stat --oneline 4dcb673 -- docs/contract-spec.md` and `git show --unified=80 4dcb673 -- docs/contract-spec.md` are recorded, with no §12.5/§12.6 hunk.
- Required §12.5/§12.6 fields are found by the three structural `rg` checks from `39-VALIDATION.md`.
- `git diff --name-only -- docs/contract-spec.md src/tools/contracts.py src/tools/catalog.py src/tools/policy.py src/tools/runtime.py tests` shows `docs/contract-spec.md` only.
- `git diff --check` passes.
- `uv run pytest tests/architecture/test_trusted_context_boundaries.py tests/tools/test_catalog.py::test_action_descriptor_is_node_only_and_requires_idempotency tests/tools/test_tool_platform.py::test_tool_policy_decision_is_not_an_event_envelope tests/tools/test_tool_platform.py::test_runtime_auth_gate_sequence_is_declarative_and_ordered tests/tools/test_tool_platform.py::test_runtime_auth_declarative_gates_preserve_multi_denial_reason_order -q` passes.
- `uv run pytest tests/architecture/test_trusted_context_boundaries.py tests/tools/test_catalog.py tests/tools/test_tool_platform.py -q` passes.
- The final summary records the dual-AI review/adjudication gate.
</verification>

<success_criteria>
- §12.5 includes `effective_at`, `approval_ref`, and `safety_snapshot_ref` as tool-call-local/action-safety fields.
- §12.6 includes descriptor `description`, `executor`, `exposure`, `requires_approval`, `requires_safety_snapshot`, `requires_idempotency_key`, `event_family` value `action` with implementation optionality, and policy availability fields.
- The nine §8.0 protected identity/scope/permission projection fields are not redefined, widened, or renamed.
- No production-code or test file changes are present in the implementation diff.
- TPH-02 is covered by a summary with validation and review evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/39-contract-spec-12-5-12-6-reconciliation/39-01-SUMMARY.md`.
</output>
