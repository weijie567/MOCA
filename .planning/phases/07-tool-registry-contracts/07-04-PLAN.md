---
phase: 07-tool-registry-contracts
plan: 04
type: execute
wave: 4
depends_on:
  - 07-01
  - 07-02
  - 07-03
files_modified:
  - src/agent/schemas.py
  - src/agent/state.py
  - tests/agent/test_tools/test_tool_contracts.py
  - tests/agent/test_graph.py
  - tests/test_agent_runs_api.py
autonomous: true
requirements:
  - STATE-01
  - STATE-02
  - STATE-03
  - STATE-04
  - TEST-01
must_haves:
  truths:
    - Downstream phases have a typed InvestigationResult contract to consume.
    - Current runtime can ignore new investigation state fields with no behavior change.
    - Existing graph and API outputs remain unchanged after Phase 7 state/schema additions.
  artifacts:
    - src/agent/schemas.py provides strict InvestigationResult validation.
    - src/agent/state.py provides dormant optional investigation state keys.
    - tests/agent/test_tools/test_tool_contracts.py proves schema/state validity and rejection rules.
    - tests/agent/test_graph.py and tests/test_agent_runs_api.py prove backward-compatible runtime behavior.
  key_links:
    - src/agent/state.py -> src/agent/schemas.py via investigation_result type usage or compatible schema reference strategy chosen during implementation.
    - tests/agent/test_graph.py -> current graph path via unchanged execution assertions guarding against accidental routing or edge changes.
    - tests/test_agent_runs_api.py -> public API response surface via assertions that dormant investigation fields are not exposed.
---

<objective>
Add the dormant investigation schema and optional state fields, then prove current graph and API behavior remain unchanged.

Purpose: finish the Phase 7 contract surface for downstream phases while preserving v1.0 runtime behavior, graph topology, and public API responses.
Output: `InvestigationResult` in `src/agent/schemas.py`, dormant optional investigation fields in `src/agent/state.py`, contract coverage updates, and targeted regression assertions in graph/API tests.
</objective>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| typed investigation schema -> future downstream consumers | Downstream phases need a strict evolvable structure instead of free-form investigator output. |
| new dormant state keys -> current graph/API/runtime | Added fields must remain optional and unused so current runtime paths do not change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | T | `InvestigationResult` contract | mitigate | Add a strict versioned schema in `src/agent/schemas.py` per D-02, D-04, D-05, D-06, and D-07 so invalid confidence or stop-reason values fail validation. |
| T-07-11 | D | current graph/runtime compatibility | mitigate | Add dormant optional keys only to `AgentState`; do not wire them into graph nodes, routing, recommendation generation, or public API serialization per D-03 and the Phase 7 boundary. |
| T-07-12 | I | public API response surface | mitigate | Prove with targeted regression tests that new state/schema fields stay absent from existing API responses unless a later phase explicitly exposes them. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add versioned investigation schema and dormant optional state fields</name>
  <files>src/agent/schemas.py, src/agent/state.py</files>
  <read_first>
    @/Users/ming/projects/MOCA/.planning/phases/07-tool-registry-contracts/07-SPEC.md
    @/Users/ming/projects/MOCA/.planning/phases/07-tool-registry-contracts/07-CONTEXT.md
    @/Users/ming/projects/MOCA/.planning/phases/07-tool-registry-contracts/07-RESEARCH.md
    @/Users/ming/projects/MOCA/.planning/phases/07-tool-registry-contracts/07-PATTERNS.md
    @/Users/ming/projects/MOCA/src/agent/schemas.py
    @/Users/ming/projects/MOCA/src/agent/state.py
  </read_first>
  <action>Implement D-02 and D-07 by adding a strict, versioned `InvestigationResult` schema to `src/agent/schemas.py`, using the existing `BaseModel + Literal + Field(...)` style in `src/agent/schemas.py` as the exact analog. Include typed fields for `schema_version: Literal["v1"] = "v1"`, `facts`, `evidence_refs`, `missing_info`, `candidate_action`, `confidence`, `stop_reason`, and `safety_notes`. Extend `AgentState` in `src/agent/state.py`, following its existing `TypedDict(total=False)` style, with optional dormant fields `investigation_result`, `investigation_steps`, `investigation_trigger_reason`, and `investigation_path`. Keep the fields dormant: do not change graph edges, routing, `generate_recommendation`, investigator execution, API serialization, or DB/schema state.</action>
  <acceptance_criteria>`InvestigationResult` validates only the approved shape and version, `AgentState` accepts the new investigation keys as optional, and existing runtime consumers do not need to populate or read them.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/agent/test_tools/test_tool_contracts.py -q</automated>
  </verify>
  <done>
    - `src/agent/schemas.py` contains `class InvestigationResult`.
    - `src/agent/schemas.py` contains `schema_version` with literal/default value `v1`.
    - `src/agent/state.py` contains `investigation_result`, `investigation_steps`, `investigation_trigger_reason`, and `investigation_path`.
    - `uv run pytest tests/agent/test_tools/test_tool_contracts.py -q` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend contract tests for schema/state validity and invalid cases</name>
  <files>tests/agent/test_tools/test_tool_contracts.py</files>
  <read_first>
    @/Users/ming/projects/MOCA/.planning/phases/07-tool-registry-contracts/07-VALIDATION.md
    @/Users/ming/projects/MOCA/.planning/phases/07-tool-registry-contracts/07-PATTERNS.md
    @/Users/ming/projects/MOCA/src/agent/schemas.py
    @/Users/ming/projects/MOCA/src/agent/state.py
    @/Users/ming/projects/MOCA/src/agent/tools/contracts.py
  </read_first>
  <action>Extend the contract test file to cover STATE-01 through STATE-03: validate a well-formed `InvestigationResult`, reject invalid confidence bounds or unsupported `stop_reason` values, and assert the new investigation-related `AgentState` keys are optional rather than required. Keep this focused on schema/state validation, not runtime graph behavior.</action>
  <acceptance_criteria>The tests prove the investigation schema is strict and evolvable, and the state additions are backward-compatible optional fields.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/agent/test_tools/test_tool_contracts.py -q</automated>
  </verify>
  <done>
    - `tests/agent/test_tools/test_tool_contracts.py` contains a valid `InvestigationResult` test.
    - The test file contains invalid confidence and invalid `stop_reason` rejection tests.
    - The test file confirms new `AgentState` investigation keys are optional.
    - `uv run pytest tests/agent/test_tools/test_tool_contracts.py -q` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove graph and API regressions remain unchanged with dormant fields present</name>
  <files>tests/agent/test_graph.py, tests/test_agent_runs_api.py</files>
  <read_first>
    @/Users/ming/projects/MOCA/.planning/phases/07-tool-registry-contracts/07-SPEC.md
    @/Users/ming/projects/MOCA/.planning/phases/07-tool-registry-contracts/07-VALIDATION.md
    @/Users/ming/projects/MOCA/.planning/phases/07-tool-registry-contracts/07-PATTERNS.md
    @/Users/ming/projects/MOCA/src/agent/graph.py
    @/Users/ming/projects/MOCA/tests/agent/test_graph.py
    @/Users/ming/projects/MOCA/tests/test_agent_runs_api.py
  </read_first>
  <action>Add or tighten targeted regression assertions per STATE-04 and TEST-01 so existing graph execution and public API response tests prove the dormant investigation fields do not alter current outputs. Keep the coverage narrow and compatibility-focused: no new investigator routing, no graph edge updates, and no public response expansion. Where existing tests already cover the path, add assertions that the new investigation fields are absent from v1.0 response surfaces unless explicitly introduced by a later phase.</action>
  <acceptance_criteria>Regression tests stay green while demonstrating that Phase 7 added only dormant internal contract/state support and preserved current graph/API behavior unchanged.</acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/agent/test_graph.py tests/agent/test_nodes/test_retrieve_policy_evidence.py tests/test_agent_runs_api.py -q</automated>
  </verify>
  <done>
    - `tests/agent/test_graph.py` still validates the existing graph path without investigator routing.
    - `tests/test_agent_runs_api.py` asserts existing public response surfaces do not expose dormant investigation fields.
    - No plan task modifies `src/agent/graph.py` or API response schemas.
    - `uv run pytest tests/agent/test_graph.py tests/test_agent_runs_api.py -q` exits 0.
  </done>
</task>

</tasks>

<verification>
Run `uv run pytest tests/agent/test_tools/test_tool_contracts.py tests/agent/test_graph.py tests/agent/test_nodes/test_retrieve_policy_evidence.py tests/test_agent_runs_api.py -q`.
This plan is complete only when the new fields remain optional and current graph/API behavior is unchanged.
</verification>

<success_criteria>
- `InvestigationResult` exists in `src/agent/schemas.py` as a strict versioned schema.
- `AgentState` contains optional dormant investigation fields only.
- Existing graph edges, routing, recommendation behavior, and public API responses remain unchanged.
- Regression tests prove backward compatibility rather than relying on comments or assumptions.
</success_criteria>

<must_haves>
## Truths
- Downstream phases have a typed `InvestigationResult` contract to consume.
- Current runtime can ignore new investigation state fields with no behavior change.
- Existing graph and API outputs remain unchanged after Phase 7 state/schema additions.

## Artifacts
- `src/agent/schemas.py` provides strict `InvestigationResult` validation.
- `src/agent/state.py` provides dormant optional investigation state keys.
- `tests/agent/test_tools/test_tool_contracts.py` proves schema/state validity and rejection rules.
- `tests/agent/test_graph.py` and `tests/test_agent_runs_api.py` prove backward-compatible runtime behavior.

## Key Links
- `src/agent/state.py` -> `src/agent/schemas.py` via `investigation_result` type usage or compatible schema reference strategy chosen during implementation.
- `tests/agent/test_graph.py` -> current graph path via unchanged execution assertions guarding against accidental routing or edge changes.
- `tests/test_agent_runs_api.py` -> public API response surface via assertions that dormant investigation fields are not exposed.
</must_haves>
