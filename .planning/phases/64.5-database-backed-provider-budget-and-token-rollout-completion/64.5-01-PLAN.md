---
phase: 64.5-database-backed-provider-budget-and-token-rollout-completion
plan: "01"
name: Migration 032 Provider Authority Schema
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/migrations/versions/032_phase64_5_provider_execution_authority.py
  - src/db/models.py
  - tests/knowledge/test_provider_execution_authority_migration.py
autonomous: true
requirements:
  - SC-64.4-5
  - SC-64.4-6
must_haves:
  truths:
    - Migration 032 is an exact guarded child of 031 and creates one shared authority root per fresh tenant/run/candidate, with purpose and envelopes owned by child reservations and a DB promotion prerequisite.
    - Authority-family rows are create-only, reservations are bounded by DB constraints, and a non-empty authority schema cannot be downgraded silently.
    - Activation history is the sole event identity; selected cutover and restore may reuse one selected lineage while rollback has no selected lineage.
  artifacts:
    - path: src/db/migrations/versions/032_phase64_5_provider_execution_authority.py
      provides: guarded schema upgrade and downgrade
    - path: src/db/models.py
      provides: ORM parity for all migration-032 tables and constraints
    - path: tests/knowledge/test_provider_execution_authority_migration.py
      provides: real-Postgres migration, constraint, trigger, and downgrade coverage
  key_links:
    - from: src/db/models.py
      to: src/db/migrations/versions/032_phase64_5_provider_execution_authority.py
      via: identical names, types, foreign keys, uniqueness, checks, and indexes
---

<objective>
Create migration 032 and ORM parity for database-backed provider authority before any runtime path can use it.

Purpose: make the database the only durable authority, budget, result, and activation-lineage source of truth.
Output: migration, ORM mappings, and real-Postgres contract tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-CONTEXT.md
@.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-RESEARCH.md
@.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-PATTERNS.md
@src/db/migrations/versions/030_phase64_4_token_corpora.py
@src/db/migrations/versions/031_phase64_4_policy_corpus_cow.py
@tests/knowledge/test_token_corpus_migration.py
@tests/knowledge/test_policy_corpus_cow_migration.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the migration-032 schema and exact ORM parity</name>
  <read_first>
    <file>.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-CONTEXT.md</file>
    <file>.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-RESEARCH.md</file>
    <file>src/db/migrations/versions/030_phase64_4_token_corpora.py</file>
    <file>src/db/migrations/versions/031_phase64_4_policy_corpus_cow.py</file>
    <file>src/db/models.py</file>
    <file>tests/knowledge/test_policy_corpus_cow_migration.py</file>
  </read_first>
  <files>
    <file>tests/knowledge/test_provider_execution_authority_migration.py</file>
    <file>src/db/migrations/versions/032_phase64_5_provider_execution_authority.py</file>
    <file>src/db/models.py</file>
  </files>
  <action>
First add and immediately run the narrow RED node `test_migration032_declares_head_and_orm_parity`; it must fail because 032 and its mappings do not exist. Then create guarded revision 032 with `down_revision` exactly equal to revision 031.

Create plural tables `provider_execution_promotions`, `provider_execution_authorities`, `provider_execution_reservations`, `provider_execution_results`, and `activation_receipt_lineages`. Promotion is one create-only DB gate for scope `phase64.5.reviewed-provider-execution` and stores only independently verifiable, non-self-referential values: exact protected-code C0/C1 commits and tree hashes, canonical C0→C1 diff hash, the four standard REVIEW/SECURITY artifact SHA-256 values, the four phase-specific attestation SHA-256 values, C0/C1 gate-report SHA-256 values, promotion-request hash, and DB time. Do not store claimed stage/scope/reviewed-commit fields invented for standard artifacts, reviewer identity as authority, or any hash that includes the promotion row itself. `provider_execution_authorities` is the single shared root for a fresh `(tenant_id, run_token, candidate_id)` and has a non-null FK to promotion; reservations own purpose/envelope and results remain one-to-one.

Enforce one promotion scope row and append-only promotion, plus exactly one authority root with unique `(tenant_id, run_token)` and `(tenant_id, candidate_id)`. Both provider purposes share that root. Enforce reservation/result limits as above. Activation lineage uses unique `activation_history_id` only; rollback lineage is all NULL and selected events may reuse refs. Install UPDATE/DELETE guards on all five tables. Add the candidate partial unique index and exact ORM parity; preserve every V1 byte/loader.

Decision ownership: D-01 makes the DB family authoritative; D-02 requires one logical root shared by both purposes while reservation rows bind purpose/subject/ordinal/envelope; D-03 puts concurrency arbitration in uniqueness; D-25 requires guarded child 032 with real migration parity; D-26 requires additive storage with V1 bytes/loaders unchanged; D-27 owns the frozen first serial boundary; D-28 owns the immediate single-node RED.
  </action>
  <verify>
    <automated>make format &amp;&amp; make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/knowledge/test_provider_execution_authority_migration.py::test_migration032_declares_head_and_orm_parity -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`032_phase64_5_provider_execution_authority.py` declares revision 032 with `down_revision` equal to 031.</criterion>
    <criterion>The five migration tables and ORM mappings have identical columns, keys, checks, unique constraints, indexes, and foreign-key deletion behavior.</criterion>
    <criterion>No authority root can exist without a committed immutable promotion row for the one production scope.</criterion>
    <criterion>One authority root accepts both `reviewed_build` and `canonical_ab` child reservations; inserting a second root for the same tenant/run or tenant/candidate fails.</criterion>
    <criterion>Activation history ID is the only unique event reference; cutover/restore may reuse selected lineage and rollback requires all selected-lineage columns NULL.</criterion>
    <criterion>The schema has no mutable file path, environment flag, caller UUID, or process clock that can independently authorize provider work.</criterion>
    <criterion>The focused migration/ORM test and full lint pass after `make format`.</criterion>
  </acceptance_criteria>
  <done>Migration 032 and model parity encode the complete DB authority family and bounded ordinal model.</done>
</task>

<task type="auto">
  <name>Task 2: Prove real-Postgres constraints, immutability, and guarded downgrade</name>
  <read_first>
    <file>src/db/migrations/versions/032_phase64_5_provider_execution_authority.py</file>
    <file>src/db/models.py</file>
    <file>tests/knowledge/test_token_corpus_migration.py</file>
    <file>tests/knowledge/test_policy_corpus_cow_migration.py</file>
    <file>tests/knowledge/test_provider_execution_authority_migration.py</file>
  </read_first>
  <files>
    <file>tests/knowledge/test_provider_execution_authority_migration.py</file>
    <file>src/db/migrations/versions/032_phase64_5_provider_execution_authority.py</file>
  </files>
  <action>
Add the narrow RED node `test_migration032_nonempty_downgrade_is_guarded` and run it before implementation. Extend the repository-standard UPDATE/DELETE guard to promotion, authority, reservation, result, and activation lineage. Downgrade succeeds only when all five are empty; otherwise it atomically refuses.

Extend the real-Postgres test to assert tables/constraints/indexes/triggers; duplicate promotion scope, run/candidate/reservation/result rejection; missing-promotion authority rejection; UPDATE/DELETE rejection; empty downgrade; and non-empty refusal preserving rows.

Add explicit real-Postgres rows proving that one shared authority root can hold a `reviewed_build` reservation and a `canonical_ab` reservation; selected cutover and selected restore can reference the same authority/reservation/result/selection; `rollback_prior` accepts only an all-NULL selected lineage; duplicate activation-history linkage fails; and no uniqueness constraint exists on reused selected-lineage references. Decision ownership: D-04 requires append-only DB-to-file truth, D-21 permits exact selected-lineage reuse while rollback has none, D-23 makes history authoritative for receipts, D-25 owns upgrade/downgrade tests, and D-28 owns the immediate downgrade RED.
  </action>
  <verify>
    <automated>make format &amp;&amp; make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/knowledge/test_provider_execution_authority_migration.py -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Real PostgreSQL rejects duplicate budget ordinals and duplicate terminal results without application cooperation.</criterion>
    <criterion>Real PostgreSQL accepts both purposes below one root and accepts selected-lineage reuse across cutover/restore, while rejecting non-NULL rollback lineage.</criterion>
    <criterion>UPDATE and DELETE against promotion and every authority-family table fail through database triggers.</criterion>
    <criterion>Downgrade is reversible when empty and refuses atomically when any authority-family row exists.</criterion>
    <criterion>The complete migration-032 test file and full lint pass after formatting.</criterion>
  </acceptance_criteria>
  <done>The schema is concurrency-safe at its uniqueness boundary, append-only, and operationally downgrade-safe.</done>
</task>

</tasks>

<threat_model>
- Forged or copied caller identifiers cannot grant authority because the schema requires exact DB-bound lineage and later repository validation.
- Concurrent double reservation is contained by database uniqueness, not an in-process lock.
- Evidence deletion or rewriting is blocked by append-only triggers.
- Destructive rollback is refused whenever durable authority evidence exists.
</threat_model>

<validation_mapping>
- SC-64.4-5: migration, DB uniqueness, immutability, and guarded downgrade establish durable authority and capped-reservation primitives.
- SC-64.4-6: activation-lineage storage makes later cutover/rollback/restore receipts DB-linkable.
</validation_mapping>

<verification>
Run only the named migration tests with the project interpreter; run `make format` before full `make lint`. Do not run provider-backed commands.
</verification>

<success_criteria>
- Revision 032 upgrades cleanly from 031 and has exact ORM parity.
- Authority, reservation, result, and activation-lineage records are immutable and uniquely bounded.
- Empty downgrade works; non-empty downgrade refuses without data loss.
</success_criteria>

<output>
After completion, create `.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-01-SUMMARY.md` with schema names, exact constraints, test commands/results, and deviations.
</output>
