---
phase: 64.5-database-backed-provider-budget-and-token-rollout-completion
plan: "04"
subsystem: rag
tags: [postgresql, provider-authority, canonical-ab, request-envelope, review-attestation]

requires:
  - phase: 64.5-03
    provides: shared PostgreSQL provider authority, purpose reservations, immutable results, and projections
provides:
  - Source-derived canonical A/B envelope with 126 query requests and 16 ingestion batches
  - Shared-root canonical_ab reservation, immediate recheck, terminal DB lineage, and selected-pass-only authorization
  - Create-only review attestations, sole promotion mutation, and read-only live-state checker modes
affects: [64.5-05, 64.5-06, 64.5-07, canonical-ab, provider-execution]

tech-stack:
  added: []
  patterns:
    - Source-derived provider call-site envelope
    - Reserve-commit-recheck-before-construction
    - Embedded review bytes with strict create-only attestations

key-files:
  created:
    - scripts/check_phase64_5_gate.py
    - tests/architecture/test_phase64_5_gate.py
  modified:
    - src/rag/evaluation/token_chunk_ab.py
    - scripts/eval_rag_token_chunk_ab.py
    - tests/eval/test_rag_token_chunk_ab.py
    - tests/architecture/test_rag_chunking_boundaries.py

key-decisions:
  - "The canonical envelope follows the real rewrite-aware production call graph: 126 query requests plus 16 ingestion batches, for a maximum of 142 provider requests."
  - "Canonical A/B reuses the reviewed-build authority root and reserves purpose canonical_ab; no second authority root or issuance command exists."
  - "Production run-ab remains earliest hard-disabled until independent review and promotion complete."

patterns-established:
  - "Canonical A/B: require current promotion and exact shared root, commit reservation, immediately recheck, then construct the provider."
  - "Review gate: trust embedded actual standard-artifact/gate bytes and real frontmatter; only promote-reviewed-execution mutates promotion state."

requirements-completed: []

duration: 58min
completed: 2026-08-13
---

# Phase 64.5 Plan 04: Canonical A-B Authority and Request Envelope Summary

**Rewrite-aware 142-request canonical A/B envelope, shared PostgreSQL authority lineage, and fail-closed review promotion checker behind the unchanged production hard stop**

## Performance

- **Duration:** 58 min
- **Started:** 2026-08-12T22:50:31Z
- **Completed:** 2026-08-12T23:48:24Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- Derived every canonical A/B provider call site from the production call graph: two roles, three formats, 18 original questions, three deterministic rewrite expansions, and exact offline ingestion batches.
- Bound canonical A/B to the existing shared DB authority through a committed `canonical_ab` reservation, immediate dispatch recheck, bounded result persistence, immutable terminal lineage, and selected-pass-only selection/authorization.
- Added strict create-only C0/C1 review attestations, exact git transition validation, one named promotion mutator with read-back, and read-only promotion/candidate/selected-pass checker modes.
- Kept production `run-ab` hard-disabled before path, DB, artifact, reservation, or provider side effects; no live provider was called.

## Task Commits

Each task was committed atomically:

1. **Task 1: Seal the real canonical A/B request envelope** - `998fcf4a` (feat)
2. **Task 2: Integrate A/B authority, result lineage, promotion checker, and read-only verifiers behind disable** - `9ea42fe3` (feat)

Root planning adjudication evidence: `4b410be7` (`docs(64.5): correct canonical A-B request budget`). This is supporting deviation evidence, not a Plan 04 task commit.

## Files Created/Modified

- `src/rag/evaluation/token_chunk_ab.py` - Source-derived envelope, canonical A/B subject/result mapping, shared-root execution service, and DB-backed selected-pass authorization.
- `scripts/eval_rag_token_chunk_ab.py` - Shared authority ID/ordinal wiring and provider construction after DB recheck while retaining the earliest hard-disable.
- `scripts/check_phase64_5_gate.py` - Strict attestation, promotion, status, candidate, and selected-pass project entry point.
- `tests/eval/test_rag_token_chunk_ab.py` - Rewrite-aware envelope, terminal retry closure, persisted lineage, and zero-side-effect hard-disable tests.
- `tests/architecture/test_rag_chunking_boundaries.py` - Production provider-constructor and authority-boundary inventory.
- `tests/architecture/test_phase64_5_gate.py` - Adversarial attestation, git transition, sole mutation, and read-only checker tests.

## Decisions Made

- Count requests from the production retrieval/rewrite loops rather than a hand-maintained constant: `2 × 3 × (18 + 3) = 126` query calls, plus 16 ingestion batches.
- Put purpose-specific envelope and subject identity on a `canonical_ab` reservation under the reviewed-build root; files and projections remain non-authoritative.
- Preserve SDK retry zero and one outer attempt per reservation; only an unchanged committed allowlisted result may qualify for ordinal two.
- Treat standard REVIEW/SECURITY frontmatter as it actually exists (`status: clean`, `threats_open: 0`) and keep stage/kind/agent facts in the root-sealed attestation.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected the incomplete 108-query envelope to the production rewrite-aware 126-query call graph**

- **Found during:** Task 1 source/call-graph verification
- **Issue:** The initial calculation counted only 18 original questions per role/format and omitted three deterministic query-rewrite provider calls, producing an unsafe 108-query ceiling.
- **Fix:** Enumerated original and rewrite call sites directly from the production rewrite plan, including ordered IDs and input hashes. The sealed maximum is now 126 query requests plus 16 exact ingestion batches, or 142 total.
- **Files modified:** `src/rag/evaluation/token_chunk_ab.py`, `tests/eval/test_rag_token_chunk_ab.py`
- **Verification:** The corrected focused test first failed against the 108 implementation, then the final scoped suite passed with exact call-site cardinality/order/hash assertions.
- **Committed in:** `998fcf4a`; planning adjudication recorded in root commit `4b410be7`

---

**Total deviations:** 1 auto-fixed (1 Rule 1 bug)
**Impact on plan:** The correction makes the reservation envelope cover the real production call graph without changing rewrite or retrieval semantics.

## Issues Encountered

- CLI rewiring exposed stale file-budget argv/source-order assertions; they were updated to the shared DB authority and recheck-before-provider contract.
- A pre-commit stub scan found unimplemented `candidate` and `selected-pass` checker branches. They were connected to the existing typed promotion/projection reads without DB mutation or provider access.
- The first summary self-check command reused zsh's special `path` variable and invalidated its later commit lookups; no repository state changed, and the self-check was rerun with explicit commands.
- Final verification: `make format` passed; full `make lint` passed across 560 files; the exact Plan 04 suites passed `102 passed` with one pre-existing LangChain deprecation warning.

## User Setup Required

None - no live provider or external configuration was used.

## Next Phase Readiness

- The canonical runtime is authority-bound and test-proven but remains intentionally unreachable through the production `run-ab` command.
- Plans 05-07 can review/promote the protected code, issue and consume the one shared authority, and use the read-only live verifiers.

---
*Phase: 64.5-database-backed-provider-budget-and-token-rollout-completion*
*Completed: 2026-08-13*

## Self-Check: PASSED

- Summary and all six implementation/test files exist.
- Task commits `998fcf4a` and `9ea42fe3`, plus root adjudication evidence `4b410be7`, are present in repository history.
