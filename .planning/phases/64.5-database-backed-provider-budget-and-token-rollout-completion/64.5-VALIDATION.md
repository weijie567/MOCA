---
phase: 64.5
slug: database-backed-provider-budget-and-token-rollout-completion
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-12
---

# Phase 64.5 — Validation Strategy

> Per-phase validation contract for database authority, provider dispatch, live evaluation, and reversible rollout.

## Test Infrastructure

| Property | Value |
|---|---|
| **Framework** | pytest 9.0.3, pytest-asyncio 1.3.0, SQLAlchemy/asyncpg against real PostgreSQL |
| **Config files** | `pyproject.toml`, `tests/conftest.py`, `docker-compose.yml` |
| **Quick run command** | `UV_CACHE_DIR=/tmp/uv-cache uv run pytest <exact-test-node> -x` |
| **Plan gate** | `make format`, full `make lint`, then the plan's exact scoped project-entry suite |
| **Final suite command** | `UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q` |
| **Estimated runtime** | Focused 1–180 seconds; final suite about 40 minutes from Phase 64.4 evidence |

Bare `pytest` and bare `python -m pytest` are invalid verification in MOCA.

## Sampling Rate

- After each minimal Wave-0 test: run the exact node immediately and preserve the intended RED reason before adding more cases.
- After every Python task: run `make format`, full `make lint`, then that task's exact scoped suite.
- After each plan: rerun the plan union and `git diff --check` before SUMMARY/self-check.
- Plans 01–05: use independent PostgreSQL sessions for migration, contention, crash, replay, and before-provider assertions; SQLite/mock-only evidence is insufficient.
- Plan 05: run an interim deep code review and security audit before production dispatch is enabled; any open blocker stops the chain.
- Plans 06–08: live gates are strictly serial and never CI-parallelized.
- Before final verification/PR: full `make lint` and full project-entry pytest must pass.

## Per-Plan Verification Map

The planner must expand this into one row per final executable task without changing the eight-plan serial boundary.

| Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command / Gate | File Exists | Status |
|---|---:|---|---|---|---|---|---|---|
| 64.5-01 | 1 | SC-64.4-5 | T-64.5-01/T-64.5-07 | Migration 032 adds immutable authority tables; empty downgrade works and nonempty downgrade refuses | real PG migration | `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/knowledge/test_provider_execution_authority_migration.py -x` | ❌ W0 | ⬜ pending |
| 64.5-02 | 2 | SC-64.4-5 | T-64.5-01..05 | One committed ordinal wins; crash remains spent; exact drift/expiry/retry matrix fails before provider | real PG concurrency | `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/rag/test_provider_execution_authority.py -x` | ❌ W0 | ⬜ pending |
| 64.5-03 | 3 | SC-64.4-5 | T-64.5-02/T-64.5-06 | Reviewed candidate build consumes DB authority while dispatch remains hard-disabled and legacy remains unreachable | integration/architecture | `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/rag/test_provider_execution_authority.py tests/rag/test_policy_reindex.py tests/architecture/test_rag_chunking_boundaries.py -x` | partial | ⬜ pending |
| 64.5-04 | 4 | SC-64.4-6 | T-64.5-03..06 | A/B request envelope is exact, cap-two is closed, result/reconciliation cannot replay provider | integration | `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/rag/test_provider_execution_authority.py tests/eval/test_rag_token_chunk_ab.py tests/eval/test_rag_retrieval_round_isolation.py -x` | partial | ⬜ pending |
| 64.5-05 | 5 | SC-64.4-5, SC-64.4-6 | T-64.5-01..07 | Zero bypass; only reviewed build and canonical A/B become reachable after clean review/security/full regression | architecture/security | Full `make lint`, full project-entry pytest, interim `$gsd-code-review`, interim `$gsd-secure-phase`, then exact dispatch tests | partial | ⬜ pending |
| 64.5-06 | 6 | SC-64.4-5 | T-64.5-03..06 | New authority/parity/lease and one inactive candidate only; prior character corpus remains active | deterministic preflight + live | Focused authority/reindex suite, then reviewed fresh-authority and build commands | partial | ⬜ pending |
| 64.5-07 | 7 | SC-64.4-6 | T-64.5-03..06 | Canonical full-provider A/B persists one truthful terminal result; only fresh `selected_pass` continues | deterministic preflight + live | Focused authority/A-B/report suite, then one DB-reserved canonical live run (second only under locked transient matrix) | partial | ⬜ pending |
| 64.5-08 | 8 | SC-64.4-5, SC-64.4-6 | T-64.5-03/T-64.5-05 | Exact cutover→rollback→restore, three DB-derived receipts, final selected visibility, closeout and full regression | real PG/integration/live | `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/rag/test_activation_receipt.py tests/rag/test_phase64_5_closeout.py -x`, live drill, then full gates | ❌ W0 closeout | ⬜ pending |

## Wave 0 Requirements

- [ ] `tests/knowledge/test_provider_execution_authority_migration.py` — head/ORM parity, constraints, upgrade, empty downgrade, nonempty refusal.
- [ ] `tests/rag/test_provider_execution_authority.py` — two-session collision, committed-crash consumption, closed ordinal-two matrix, full identity/envelope/expiry drift, result reconciliation without provider.
- [ ] `tests/rag/test_phase64_5_closeout.py` — wrong final pointer minimal RED, then exact three-event/receipt/selection/visibility closeout checks.
- [ ] Extend `tests/rag/test_policy_reindex.py`, `tests/eval/test_rag_token_chunk_ab.py`, and architecture guards only as each integration seam is introduced.
- [ ] Each plan runs its smallest missing-contract RED immediately; it does not prebuild a large adversarial matrix before observing that RED.

## Manual / Controlled Live Verifications

| Behavior | Requirement | Why Controlled | Gate |
|---|---|---|---|
| Interim code/security approval before dispatch | SC-64.4-5/6 | GSD review artifacts and adjudication are workflow evidence, not ordinary unit behavior | Plans 01–04 complete; migrations/concurrency/bypass/full gates green; review and security have zero open blockers |
| Fresh parity and candidate build | SC-64.4-5 | Uses real DashScope credentials and a mutable evaluation-only DB | Fresh run/candidate/absolute lease only; sealed hashes and active baseline rechecked; each document reservation committed first |
| Canonical full-provider A/B | SC-64.4-6 | External provider cost and truthful live metrics cannot be mocked | Exact DB authority and request envelope; at most two ordinals; nonpass/exhaustion stops |
| Cutover, rollback, restore | SC-64.4-5/6 | Intentionally changes the evaluation-only tenant's active pointer | Fresh `selected_pass` only; contiguous CAS lineage; post-commit reads; three receipts; final selected pointer |

## Validation Sign-Off

- [ ] Every final plan task has a unique map row and valid project-entry automated command or explicitly controlled live gate.
- [ ] No three consecutive tasks lack automated verification.
- [ ] Real PostgreSQL authority tests cover concurrency, crash, replay, upgrade, and guarded downgrade.
- [ ] Before-provider negative tests prove DB/root/artifact/CLI/environment/helper bypasses construct zero provider clients.
- [ ] Interim code review/security artifacts are clean before dispatch enablement.
- [ ] Live evidence is fresh and serial; no Phase 64.4 authority is reused.
- [ ] Full lint and full project-entry pytest pass before final closeout/PR.
- [ ] `nyquist_compliant: true` and `wave_0_complete: true` are set only after all final task rows and Wave-0 tests are green.

**Approval:** pending plan review
