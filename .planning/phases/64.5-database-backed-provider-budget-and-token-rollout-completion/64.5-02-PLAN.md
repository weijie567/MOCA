---
phase: 64.5-database-backed-provider-budget-and-token-rollout-completion
plan: "02"
name: Database Authority Domain and Repository
type: execute
wave: 2
depends_on:
  - "64.5-01"
files_modified:
  - src/repositories/provider_execution_authority_repo.py
  - src/rag/provider_execution_authority.py
  - tests/rag/test_provider_execution_authority.py
autonomous: true
requirements:
  - SC-64.4-5
  - SC-64.4-6
must_haves:
  truths:
    - Only one exact, unexpired, DB-committed authority root exists for a fresh tenant/run/candidate, and it cannot issue/reserve/recheck without a current DB promotion.
    - Reservations are committed before provider work, never recycled after a crash, and ordinal 2 is limited to a recorded allowlisted result.
    - Reconciliation can repair file projections from DB state but can never create authority or restore budget.
  artifacts:
    - path: src/rag/provider_execution_authority.py
      provides: immutable requests, views, enums, validation, and orchestration service
    - path: src/repositories/provider_execution_authority_repo.py
      provides: DB-time exact-match issue/reserve/result/reconcile operations
    - path: tests/rag/test_provider_execution_authority.py
      provides: real-Postgres concurrency, crash, expiry, and replay proofs
  key_links:
    - from: src/rag/provider_execution_authority.py
      to: src/repositories/provider_execution_authority_repo.py
      via: typed service calls with an independent transaction per durable transition
---

<objective>
Build the typed domain and repository that make committed PostgreSQL state the sole provider-execution authority.

Purpose: turn migration-032 primitives into exact-match, crash-consuming, concurrency-safe operations.
Output: authority DTO/service, repository, and real-Postgres behavioral tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-01-SUMMARY.md
@.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-CONTEXT.md
@src/repositories/policy_corpus_repo.py
@src/repositories/rag_evaluation_round_repo.py
@src/rag/evaluation/retrieval_rounds.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement exact DB-time authority issuance and committed reservation</name>
  <read_first>
    <file>.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-01-SUMMARY.md</file>
    <file>src/db/models.py</file>
    <file>src/repositories/policy_corpus_repo.py</file>
    <file>src/repositories/rag_evaluation_round_repo.py</file>
    <file>src/rag/evaluation/retrieval_rounds.py</file>
  </read_first>
  <files>
    <file>tests/rag/test_provider_execution_authority.py</file>
    <file>src/rag/provider_execution_authority.py</file>
    <file>src/repositories/provider_execution_authority_repo.py</file>
  </files>
  <action>
First add and immediately run the real-Postgres RED node `test_concurrent_same_subject_reservation_has_exactly_one_winner`. Define immutable DTOs/enums with explicit cardinality: `ProviderExecutionAuthorityRequestV1`/view issue one shared tenant/run/candidate root and contain only shared config/source/evidence/parity/provider/lease/expiry bindings; `ProviderExecutionReservationRequestV1`/view contain `authority_id`, purpose (`reviewed_build` or `canonical_ab`), subject, ordinal, and the purpose-specific canonical request envelope; terminal result DTOs are one-to-one with a reservation. The envelope carries version, exact ordered call sites, per-site maximum attempts, maximum request count, provider/model/dimensions, and canonical hash.

Define `ExecutionPromotionRequestV1`/view and `promote_reviewed_execution`/`require_current_promotion`. The request contains only checker-validated C0/C1 commits/trees/diff, four standard artifact SHAs, four attestation SHAs, two gate-report SHAs, and canonical request hash. The repository rechecks shapes/existence/hash consistency, locks the singleton scope, inserts once with DB time, and never treats agent-name strings or file possession as DB authority. No update/replacement/idempotence across different bytes is allowed.

`issue_authority_root`, `reserve_and_commit`, and `recheck_dispatch` must call `require_current_promotion` before authority/reservation/provider construction. Current code identity is computed internally from the repository/project entry (never accepted as CLI/env/flag authority) and must equal promotion C1 protected tree/commit; missing, stale, foreign, mismatched, or dirty protected code fails before reservation. Then apply existing locks, `clock_timestamp()`, equality refusal, root/purpose-envelope validation, and short commit. Surface typed `promotion_missing`, `promotion_stale`, `promotion_mismatch` in addition to existing failures.

Decision ownership: D-01 assigns sole authority to committed DB rows/locks; D-02 is implemented as one root-to-many purpose reservations with envelopes on reservations; D-03 owns DB uniqueness and lock order; D-05 requires commit before construction; D-10/D-11 require minimum prerequisite expiry and `clock_timestamp()` with equality refused; D-12 requires complete root-and-reservation drift checks.
  </action>
  <verify>
    <automated>make format &amp;&amp; make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/rag/test_provider_execution_authority.py::test_concurrent_same_subject_reservation_has_exactly_one_winner -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Two independent PostgreSQL sessions racing for the same ordinal produce one committed reservation and one typed loser.</criterion>
    <criterion>Equality at expiry is refused using `clock_timestamp()` obtained inside the reservation transaction.</criterion>
    <criterion>Every locked identity/binding field is checked before reservation insertion; a UUID or copied projection alone cannot pass.</criterion>
    <criterion>Reservation commit completes before the service returns control to a provider caller.</criterion>
    <criterion>`issue_authority_root` yields exactly one root per tenant/run/candidate; build and canonical-A/B reservation requests both reference it and carry separate envelopes.</criterion>
    <criterion>Missing/stale/foreign/mismatched promotion or protected code drift refuses authority issue, reservation, and dispatch recheck before provider construction.</criterion>
  </acceptance_criteria>
  <done>Exact DB authority can issue and commit one bounded reservation under the repository-standard lock order.</done>
</task>

<task type="auto">
  <name>Task 2: Add crash consumption, closed retries, results, and DB-to-file reconciliation</name>
  <read_first>
    <file>src/rag/provider_execution_authority.py</file>
    <file>src/repositories/provider_execution_authority_repo.py</file>
    <file>tests/rag/test_provider_execution_authority.py</file>
    <file>src/repositories/rag_evaluation_round_repo.py</file>
    <file>src/repositories/policy_corpus_repo.py</file>
  </read_first>
  <files>
    <file>tests/rag/test_provider_execution_authority.py</file>
    <file>src/rag/provider_execution_authority.py</file>
    <file>src/repositories/provider_execution_authority_repo.py</file>
  </files>
  <action>
Add and immediately run the RED node `test_committed_reservation_without_result_consumes_ordinal_one`. Implement append-only result recording with one result per reservation and actual request count bounded by the sealed envelope. Use the closed terminal code set `success`, `provider_unavailable`, `transient_execution_error`, `quality_fail`, `safety_fail`, `configuration_error`, `parity_error`, `source_drift`, `response_error`, `projection_error`, and `unknown_error`.

Ordinal 1 is consumed at reservation commit even if the process dies before provider/result/candidate writes. Allow ordinal 2 only when ordinal 1 has a committed result of `provider_unavailable` or `transient_execution_error`, all original bindings and the envelope are byte-for-byte unchanged, and the caller explicitly requests retry; absence of a result is never retryable. Reject ordinal 3 at the database/domain boundary. Add idempotent result lookup/record behavior and a DB-to-file projection reconciliation operation that can recreate a missing projection from committed rows but never mint authority, edit a reservation/result, or refund a consumed ordinal.

Cover copied projections, missing DB rows, stale source active ID/epoch, changed manifest/parity/envelope, equality-at-expiry, crash before result, terminal quality/safety/configuration failures, ordinal-2 concurrency, ordinal 3, and reconciliation in real-Postgres tests.

Explicitly test one root with both purposes, promotion singleton/immutability, exact successful promotion, and missing/stale/foreign/commit/tree/diff/artifact-SHA/attestation-SHA/gate-report-SHA/dirty-tree refusal before reservation. Phase-specific checker—not repository schema—inpects attestation agent strings/roles and evidence bytes. D-04/D-06/D-07/D-08 own DB truth/caps; D-14/D-15/D-16 own reviewed promotion and stale refusal; D-28 owns immediate RED.
  </action>
  <verify>
    <automated>make format &amp;&amp; make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/rag/test_provider_execution_authority.py -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>A committed no-result reservation permanently consumes its ordinal and does not qualify for ordinal 2.</criterion>
    <criterion>Only a committed unavailable/transient ordinal-1 result permits one unchanged ordinal-2 reservation; all terminal quality/safety/configuration outcomes stop.</criterion>
    <criterion>No authority subject can reserve ordinal 3, including under concurrent sessions.</criterion>
    <criterion>Reconciliation is one-way DB-to-file and cannot authorize calls or restore budget.</criterion>
    <criterion>The full authority test module and full lint pass after formatting.</criterion>
  </acceptance_criteria>
  <done>The authority service has durable crash semantics, closed retries, append-only results, and non-authoritative projections.</done>
</task>

</tasks>

<threat_model>
- TOCTOU is reduced by rechecking all bindings under the same lock that inserts the reservation.
- Process death cannot create free retries because budget is consumed at committed reservation, not result write.
- Forged, stale, or copied files cannot grant authority because reconciliation only projects committed DB rows.
- Lock ordering and uniqueness prevent concurrent double spend without hiding work through `SKIP LOCKED`.
</threat_model>

<validation_mapping>
- SC-64.4-5: DB-time exact authority, capped committed reservations, closed retry rules, and crash consumption are the core live-execution boundary.
- SC-64.4-6: immutable result and authority identities supply the selected lineage later required by activation receipts.
</validation_mapping>

<verification>
Use real-Postgres tests through `UV_CACHE_DIR=/tmp/uv-cache uv run pytest`; format before full lint. Do not invoke a provider or a live CLI.
</verification>

<success_criteria>
- Exact-match authority and reservation operations use trusted DB time and committed short transactions.
- Crash, replay, copied projection, expiry, and concurrent double-spend cases fail closed.
- Result/reconciliation APIs preserve immutable DB supremacy.
</success_criteria>

<output>
After completion, create `.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-02-SUMMARY.md` with public signatures, lock order, result codes, retry semantics, and verification results.
</output>
