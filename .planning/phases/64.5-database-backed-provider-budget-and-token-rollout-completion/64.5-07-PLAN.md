---
phase: 64.5-database-backed-provider-budget-and-token-rollout-completion
plan: "07"
name: Fresh Canonical Live A-B Selected Pass
type: execute
wave: 7
depends_on:
  - "64.5-06"
files_modified:
  - evaluation/reports/rag_provider_execution_authority/v2/
  - evaluation/reports/rag_token_chunk_ab/v1/runs/
  - evaluation/reports/rag_token_chunk_ab/v1/recovery-authorizations/
  - .planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-07-LIVE-VERIFY.json
autonomous: false
requirements:
  - SC-64.4-5
  - SC-64.4-6
must_haves:
  truths:
    - Canonical A/B reuses the exact Plan-06 shared authority root and adds only purpose `canonical_ab` reservations under unchanged sealed inputs and fixed request caps.
    - Only a persisted full-provider `selected_pass` linked to the exact DB reservation/result may create activation authorization.
    - Any nonpass, ambiguous crash, expired authority, exhausted cap, or prerequisite drift is an honest terminal stop without tuning or another run.
  artifacts:
    - path: evaluation/reports/rag_token_chunk_ab/v1/runs/
      provides: immutable fresh terminal full-provider A/B report and selection projection
    - path: evaluation/reports/rag_provider_execution_authority/v2/
      provides: canonical A/B authority/reservation/result projection
  key_links:
    - from: evaluation/reports/rag_token_chunk_ab/v1/runs/
      to: evaluation/reports/rag_provider_execution_authority/v2/
      via: exact selected-pass run, reservation, result, candidate, incumbent, and envelope hashes
---

<objective>
Run the canonical live full-provider A/B once under fresh DB authority and continue only on selected-pass.

Purpose: produce honest immutable selection evidence without retuning or reusing Phase 64.4 authority.
Output: terminal A/B DB result and, only for selected-pass, selection/activation authorization.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-06-SUMMARY.md
@src/rag/provider_execution_authority.py
@src/rag/evaluation/token_chunk_ab.py
@scripts/eval_rag_token_chunk_ab.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze canonical A-B inputs under the existing shared authority root</name>
  <read_first>
    <file>.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-06-SUMMARY.md</file>
    <file>src/rag/provider_execution_authority.py</file>
    <file>src/rag/evaluation/token_chunk_ab.py</file>
    <file>scripts/eval_rag_token_chunk_ab.py</file>
    <file>scripts/check_phase64_5_gate.py</file>
  </read_first>
  <files>
    <file>evaluation/reports/rag_provider_execution_authority/v2/</file>
    <file>evaluation/reports/rag_token_chunk_ab/v1/runs/</file>
    <file>.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-07-LIVE-VERIFY.json</file>
  </files>
  <action>
Run format/lint, persisted-lineage/envelope focused tests, and the read-only candidate checker before live work. Reread DB to prove Plan-06 candidate complete/validated/inactive, character active, and source/evidence/parity/lease/config fresh. Freeze canonical manifest, Gold, baseline, roles, three formats, 18 base questions/54 per-role format-expanded cases, fractions, thresholds, provider/model/dimensions, source-derived current 108 query calls, exact ingestion batches, SDK retries 0, and one outer attempt; do not edit any value.

Do not generate another authority or root. Reuse the exact authority ID recorded by strict `64.5-06-LIVE-VERIFY.json`; the authority keeps the same fresh run token and the reservation subject identifies this canonical comparison. Before reserving, run read-only `selected-pass --stage preflight --expect-no-ab-reservation` to prove exactly one root, completed reviewed-build lineage, zero A/B reservations/results, exact candidate/incumbent/pointers, and sufficient remaining expiry for the fixed envelope. Failure stops without renewal/new root/new run/substitution/tuning. D-02 owns shared-root reuse; D-09 forbids historic/second authority; D-10/D-12 expiry/drift; D-18 locked evaluation; D-29 bounded reads; D-30 honest stop.
  </action>
  <verify>
    <automated>make format &amp;&amp; make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_rag_token_chunk_ab.py::test_persisted_selected_pass_binds_db_result_lineage tests/eval/test_rag_token_chunk_ab.py::test_canonical_ab_envelope_enumerates_every_provider_call_site -q &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/check_phase64_5_gate.py selected-pass --stage preflight --identity-file .planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-06-LIVE-VERIFY.json --require-current-promotion --expect-no-ab-reservation --output .planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-07-LIVE-VERIFY.json</automated>
  </verify>
  <acceptance_criteria>
    <criterion>No new authority/root exists; exact Plan-06 shared root has zero canonical-A/B reservations before Task 2.</criterion>
    <criterion>The sealed matrix/thresholds/fractions/hashes and computed request envelope are unchanged and recorded before reservation.</criterion>
    <criterion>No Phase 64.4 A/B run, reservation, recovery budget, selection, or authorization is reused.</criterion>
    <criterion>Any failed/freshness precondition stops before provider reservation.</criterion>
  </acceptance_criteria>
  <done>The fresh shared root remains exact and has an unspent `canonical_ab` purpose budget ready for the serial live run.</done>
</task>

<task type="auto">
  <name>Task 2: Execute canonical A-B and require persisted selected-pass</name>
  <read_first>
    <file>.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-06-SUMMARY.md</file>
    <file>src/rag/provider_execution_authority.py</file>
    <file>src/rag/evaluation/token_chunk_ab.py</file>
    <file>scripts/eval_rag_token_chunk_ab.py</file>
    <file>scripts/check_phase64_5_gate.py</file>
    <file>evaluation/reports/rag_provider_execution_authority/v2/</file>
  </read_first>
  <files>
    <file>evaluation/reports/rag_provider_execution_authority/v2/</file>
    <file>evaluation/reports/rag_token_chunk_ab/v1/runs/</file>
    <file>evaluation/reports/rag_token_chunk_ab/v1/recovery-authorizations/</file>
    <file>.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-07-LIVE-VERIFY.json</file>
  </files>
  <action>
Invoke canonical `run-ab` once using the authority ID from strict `64.5-06-LIVE-VERIFY.json`; it reserves purpose `canonical_ab` beneath that root. Reread reservation/result before follow-up. Only committed `provider_unavailable`/`transient_execution_error` may use ordinal 2 unchanged; no-result crash stops. Never use ordinal 3, a second root/run, changed thresholds/fractions/data, or parallel execution.

Require DB result/report agreement, then run read-only `selected-pass --stage terminal --require-selected-pass`. It proves one shared root, reviewed-build counts, one/two allowed A/B reservations, terminal result, requests within the source-derived envelope, unchanged character-active/token-inactive pointers, and selection/authorization linked to that exact result. Tests alone are not post-live proof.

Continue only when DB and checker are exactly `selected_pass`. Every other terminal state writes truthful summary/verifier `status: checkpoint/incomplete`, then the executor MUST stop incomplete so Plan 08 cannot dispatch. A nonpass never satisfies done/success. D-07/D-08/D-20 own caps/envelope/retry; D-19 selected-pass only; D-27 serial live; D-30 no tuning/extra run.
  </action>
  <verify>
    <automated>make format &amp;&amp; make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_rag_token_chunk_ab.py -q &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/check_phase64_5_gate.py selected-pass --stage terminal --identity-file .planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-06-LIVE-VERIFY.json --require-current-promotion --require-selected-pass --output .planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-07-LIVE-VERIFY.json</automated>
  </verify>
  <acceptance_criteria>
    <criterion>At most two complete A/B reservations exist, with ordinal 2 only for an unchanged allowlisted committed ordinal-1 result.</criterion>
    <criterion>Actual requests are within the exact envelope and report/result hashes agree with DB lineage.</criterion>
    <criterion>Activation authorization exists if and only if terminal result is the fresh full-provider `selected_pass`.</criterion>
    <criterion>The read-only checker proves exact shared-root/reservation/result/report/selection/pointer live state and exits nonzero for every nonpass.</criterion>
    <criterion>Character remains active and token remains inactive; every nonpass stops with no tuning or further live run.</criterion>
  </acceptance_criteria>
  <done>A fresh full-provider `selected_pass` lineage exists and the terminal read-only checker passes; every other result leaves this task incomplete and stops execution.</done>
</task>

</tasks>

<threat_model>
- Immutable DB bindings prevent swapped candidate/incumbent/data/config between issuance and dispatch.
- Envelope and closed retry semantics prevent hidden request expansion or repeated full runs.
- File selection/authorization possession cannot replace the exact committed selected-pass result.
- Nonpassing outcomes cannot be tuned, relabeled, or retried into activation eligibility.
</threat_model>

<validation_mapping>
- SC-64.4-5: canonical live evaluation remains serial/bounded and leaves the current corpus untouched on every failure.
- SC-64.4-6: fresh full-provider selected-pass is the sole source of activation authorization and selected lineage.
</validation_mapping>

<verification>
This plan is controlled and serial. Automated commands use the project interpreter; live execution occurs once under the fixed DB envelope. A non-selected-pass result ends execution before Plan 08.
</verification>

<success_criteria>
- Exact Plan-06 shared DB authority root governs both reviewed build and canonical-A/B reservations.
- The run is within caps and has an immutable terminal DB/result projection.
- Only selected-pass permits Plan 08; all other outcomes stop honestly.
</success_criteria>

<output>
Create `64.5-07-SUMMARY.md` with exact fresh identities/hashes, sealed matrix/envelope, reservation/result/request counts, terminal state, pointer states, and explicit selected-pass or honest-stop verdict.
</output>
