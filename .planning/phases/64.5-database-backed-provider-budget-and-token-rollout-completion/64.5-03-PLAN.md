---
phase: 64.5-database-backed-provider-budget-and-token-rollout-completion
plan: "03"
name: Reviewed Candidate Build Behind Hard Disable
type: execute
wave: 3
depends_on:
  - "64.5-02"
files_modified:
  - src/rag/policy_reindex.py
  - scripts/reindex_policies.py
  - tests/rag/test_policy_reindex.py
  - tests/rag/test_provider_execution_authority.py
autonomous: true
requirements:
  - SC-64.4-5
  - SC-64.4-6
must_haves:
  truths:
    - The reviewed candidate-build service reaches provider construction only after an exact reservation has committed and all bindings are rechecked with fresh DB time.
    - Provider work runs outside the reservation transaction and every terminal path records immutable DB truth before projecting files.
    - Both `build-next-reviewed` and legacy `build-next` remain unreachable at the production CLI through this plan.
  artifacts:
    - path: src/rag/policy_reindex.py
      provides: authority-bound reviewed-build orchestration
    - path: scripts/reindex_policies.py
      provides: authority issuance surface plus still-disabled provider commands
    - path: tests/rag/test_policy_reindex.py
      provides: reachability, before-provider negatives, crash, and hard-disable coverage
  key_links:
    - from: src/rag/policy_reindex.py
      to: src/rag/provider_execution_authority.py
      via: reserve-and-commit then immediate dispatch recheck
---

<objective>
Integrate database authority into the reviewed token-candidate build while preserving the production hard-disable.

Purpose: finish and adversarially test the candidate provider path before it can be enabled.
Output: authority-bound service/CLI wiring and focused fail-closed tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-02-SUMMARY.md
@.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-CONTEXT.md
@src/rag/provider_execution_authority.py
@src/rag/policy_reindex.py
@scripts/reindex_policies.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Put the reviewed-build service behind committed DB reservations</name>
  <read_first>
    <file>.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-02-SUMMARY.md</file>
    <file>src/rag/provider_execution_authority.py</file>
    <file>src/repositories/provider_execution_authority_repo.py</file>
    <file>src/rag/policy_reindex.py</file>
    <file>tests/rag/test_policy_reindex.py</file>
  </read_first>
  <files>
    <file>tests/rag/test_policy_reindex.py</file>
    <file>src/rag/policy_reindex.py</file>
    <file>tests/rag/test_provider_execution_authority.py</file>
  </files>
  <action>
Add and immediately run the narrow RED node `test_reviewed_build_reaches_provider_only_with_committed_db_authority`. Refactor only the reviewed path into: require current DB promotion/current protected C1 code identity; deterministic preflight; reserve/commit; lock/recheck promotion plus candidate/source/root/envelope with fresh DB time; construct provider; release transaction; bounded network work; terminal result/candidate progress; DB-derived projection. Promotion refusal must precede reservation and factory access.

The per-document envelope must seal the ordered input hashes/count, batch size 10, `ceil(input_count / 10)` provider batches, provider/model/dimensions, SDK retries 0, and exactly one reviewed outer attempt. Preserve the existing candidate lifecycle, lease fencing, source snapshot, inactive visibility, ordered cursor, and validation rules. A committed reservation with a crash before result/candidate write remains spent and is recovered only through repository reconciliation. Do not alter V1 artifact bytes/loaders and do not add a flag, environment escape, alternate root, or test-only provider construction seam.

Every document reservation must reference the one shared authority root and set `purpose=reviewed_build`; the purpose-specific document envelope belongs only on that reservation. Decision ownership: D-02 owns shared-root/purpose-reservation separation; D-05 owns reserve-commit-before-construction; D-06 owns crash consumption; D-08 owns exact batching/retry envelope; D-10/D-11/D-12 own minimum expiry, DB time, and immediate drift recheck; D-17 owns inactive candidate visibility; D-28 owns the immediate RED.
  </action>
  <verify>
    <automated>make format &amp;&amp; make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/rag/test_policy_reindex.py::test_reviewed_build_reaches_provider_only_with_committed_db_authority -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>The provider factory is not called until a matching reservation is committed and the immediate DB-time recheck succeeds.</criterion>
    <criterion>No/mismatched/stale promotion stops before reservation and provider factory, even with an otherwise valid authority/projection.</criterion>
    <criterion>The provider call occurs outside the reservation transaction with SDK retries zero and one envelope-bound outer attempt.</criterion>
    <criterion>Candidate progress/result projection follows committed DB truth and never activates the candidate.</criterion>
    <criterion>The focused service test and full lint pass after formatting.</criterion>
  </acceptance_criteria>
  <done>The reviewed build implementation is DB-authorized, bounded, crash-consuming, and still internal.</done>
</task>

<task type="auto">
  <name>Task 2: Add safe authority issuance, negatives, and retain both CLI hard stops</name>
  <read_first>
    <file>src/rag/policy_reindex.py</file>
    <file>scripts/reindex_policies.py</file>
    <file>tests/rag/test_policy_reindex.py</file>
    <file>tests/rag/test_provider_execution_authority.py</file>
    <file>src/rag/policy_reindex_artifacts.py</file>
  </read_first>
  <files>
    <file>tests/rag/test_policy_reindex.py</file>
    <file>scripts/reindex_policies.py</file>
    <file>src/rag/policy_reindex.py</file>
  </files>
  <action>
Add and immediately run the RED node `test_copied_reviewed_projection_without_db_row_stops_before_provider`. Add a non-provider `issue-provider-authority` CLI operation that derives the shared root bindings from current committed candidate/source/parity/manifest/config state, asks the authority service to issue the sole fresh tenant/run/candidate row, and writes only a create-only V2 projection after commit. It must not accept a purpose or purpose envelope and must reject any conflicting second root. It may return an authority ID for later lookup, but possession of that ID must never bypass exact DB checks.

Wire `build-next-reviewed` to the Task-1 service, but retain the earliest `_main` refusal for both reviewed and legacy commands through Plan 03. Extend negatives with missing/stale/foreign promotion and protected-tree drift, plus existing copied/drift/crash cases. Legacy remains permanently zero-side-effect disabled.

Decision ownership: D-01/D-04 make the issued file a non-authoritative projection; D-09 forbids importing Phase 64.4 identity/budget; D-13 retains both hard stops; D-15 forbids alternate command/root/env/helper seams and permanently disables legacy build; D-26 preserves V1 meaning; D-28 owns the copied-file immediate RED.
  </action>
  <verify>
    <automated>make format &amp;&amp; make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/rag/test_policy_reindex.py tests/rag/test_provider_execution_authority.py -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>`issue-provider-authority` performs no provider work, accepts no purpose/envelope, creates the sole shared root, and emits only a projection of committed DB truth.</criterion>
    <criterion>`build-next-reviewed` and `build-next` both fail at the earliest CLI gate with zero DB/root/artifact/reservation/provider side effects.</criterion>
    <criterion>Every stale, copied, expired, drifted, crashed, or terminal-failure case stops before an unauthorized provider call.</criterion>
    <criterion>The focused reindex/authority suites and full lint pass after formatting.</criterion>
  </acceptance_criteria>
  <done>The complete reviewed path is implemented and tested but remains production-unreachable pending Plan 05.</done>
</task>

</tasks>

<threat_model>
- A copied descriptor or known authority UUID is non-authoritative without the exact current committed row and bindings.
- A source/pointer/parity/lease/config change between issuance and dispatch fails the immediate recheck.
- A process crash after reservation cannot repeat a document call for free.
- Legacy and alternate CLI surfaces remain closed while implementation review is pending.
</threat_model>

<validation_mapping>
- SC-64.4-5: reviewed builds remain ordered, inactive, resumable from DB truth, capped to two committed ordinals per document, and preserve the current corpus on failure.
- SC-64.4-6: the fresh candidate and immutable result lineage created here can later be selected and activated, but this plan grants no activation authority.
</validation_mapping>

<verification>
Run only mocked/fake-provider and real-Postgres focused tests through the project interpreter; never call a live provider. Run `make format` before full `make lint`.
</verification>

<success_criteria>
- Reviewed build dispatch is centralized behind committed DB authority and a fresh exact recheck.
- Provider cost is fully envelope-bounded and crash reservations are spent.
- Production reviewed and legacy build commands remain hard-disabled with proven zero side effects.
</success_criteria>

<output>
After completion, create `.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-03-SUMMARY.md` listing CLI/service signatures, disable placement, envelope formula, negatives, and verification results.
</output>
