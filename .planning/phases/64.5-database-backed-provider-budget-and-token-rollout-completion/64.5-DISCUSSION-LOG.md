# Phase 64.5: Database-Backed Provider Budget And Token Rollout Completion - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md; this log preserves the alternatives considered.

**Date:** 2026-08-12
**Phase:** 64.5-database-backed-provider-budget-and-token-rollout-completion
**Mode:** `--auto` — all gray areas selected; recommended conservative option selected in one pass
**Areas discussed:** Global database authority, crash/concurrency/retries, fresh authority/expiry, provider dispatch review gate, A/B terminal decision, reversible activation/closeout, migration/compatibility

---

## Global database authority

| Option | Description | Selected |
|---|---|---|
| Database authority | PostgreSQL constraints, locks, and committed reservations are the sole provider permission across all processes and worktrees. | ✓ |
| Hybrid database and files | Database records a budget, but file paths/ordinals remain part of authorization. | |
| Hardened files | Continue filesystem pinning, no-follow, and create-only artifacts as production authority. | |

**Auto-selected choice:** Database authority. Phase 64.4 proved that pathname-local safety cannot provide globally non-forgeable execution authority.

## Crash, concurrency, and retries

| Option | Description | Selected |
|---|---|---|
| Consume on durable reservation | Commit a unique ordinal before provider construction; crashes consume it; retry only through a closed second-ordinal matrix. | ✓ |
| Reclaim timed-out reservations | Make ambiguous reservations available after a timeout or lease handoff. | |
| Application counter | Count attempts in process and reconcile later. | |

**Auto-selected choice:** Consume on durable reservation. It prevents external provider work from being repeated after a local rollback or crash.

## Fresh authority and expiry

| Option | Description | Selected |
|---|---|---|
| Fresh authority only | New run, candidate, parity proof, absolute lease, and DB budget; old artifacts remain historical. | ✓ |
| Migrate old authority | Import retained Phase 64.4 candidate and file ordinals into the new database ledger. | |
| Renew old lease | Extend the expired descriptor and continue its prior run. | |

**Auto-selected choice:** Fresh authority only. This follows the Phase 64.5 roadmap boundary and avoids silently reinterpreting expired evidence.

## Provider dispatch review gate

| Option | Description | Selected |
|---|---|---|
| Reviewed interim gate | Keep production disabled until a dedicated deep code review, security audit, and negative suite approve the DB boundary. | ✓ |
| Enable after tests | Re-enable after implementation tests, then rely on the final phase review. | |
| Runtime feature flag | Add a CLI/environment override while the authority is being reviewed. | |

**Auto-selected choice:** Reviewed interim gate. The roadmap explicitly requires review and security proof before production dispatch becomes reachable.

## A/B terminal decision

| Option | Description | Selected |
|---|---|---|
| `selected_pass` only | Preserve the sealed dataset, exact gates, and full-provider requirement; nonpassing outcomes never activate. | ✓ |
| Best available candidate | Select token-aware if it is directionally better despite a failed hard gate. | |
| Operator override | Allow a manual decision to bypass report status. | |

**Auto-selected choice:** `selected_pass` only. Thresholds, source data, and candidate configuration stay frozen before live observation.

## Reversible activation and closeout

| Option | Description | Selected |
|---|---|---|
| Three-step drill | Cut over to selected, roll back to prior, restore selected, then verify three receipts and final visibility. | ✓ |
| Cutover only | Activate selected and rely on unit tests for rollback. | |
| Dry-run proof only | Do not change the pointer; validate the activation request offline. | |

**Auto-selected choice:** Three-step drill. It directly proves SC-64.4-5's rollback safety and no-mixed-corpus requirement.

## Migration and compatibility

| Option | Description | Selected |
|---|---|---|
| Additive fresh schema | Add DB authority from head 031, import no old authority, preserve V1 bytes, and refuse unsafe downgrade. | ✓ |
| Import file budgets | Convert retained Phase 64.4 file manifests and attempts into authoritative rows. | |
| Replace V1 meaning | Update old artifact schemas/loaders so they become DB-backed authorization. | |

**Auto-selected choice:** Additive fresh schema. Historical evidence remains verifiable without acquiring new authority.

## the agent's Discretion

- Exact normalized table/module names, indexes, safe DTO names, and projection schema versions.
- Whether authority/reservation/result records share one table family or bounded purpose-specific tables, provided there is one logical gate.

## Deferred Ideas

- General provider/model/retry policy: Phase 69.
- Parser, OCR, tokenizer, chunker, retrieval, reranker, and quality-gate tuning: outside Phase 64.5.
- General multi-tenant rollout scheduling or UI: future phase.
