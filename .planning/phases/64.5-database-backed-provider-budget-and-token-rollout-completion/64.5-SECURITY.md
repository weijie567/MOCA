---
phase: 64.5-database-backed-provider-budget-and-token-rollout-completion
status: verified
scope: final-bounded-security-audit-plans-01-07
phase_complete: false
rollout_status: incomplete
security_enforcement: true
asvs_level: 1
block_on: open
threats_total: 7
threats_closed: 7
threats_open: 0
accepted_risks: 0
unregistered_flags: 0
created: 2026-08-13
updated: 2026-08-13
audited_head: d7bd593fdaa7e030846cd8111e27075cb735442d
audited_tree: b097aab250b4a273e0fb6c9e226b990e28dd03c5
audited_protected_head: 37aac6504db878e73286455bf8816b1636b23396
audited_protected_tree: f59a362782699f3b11a285d7bdc0ec7e5d01adf6
---

# Phase 64.5 — Final Bounded Security Audit

## Verdict

**SECURED for the implemented Plans 01–07 security boundary: 7/7 consolidated threats CLOSED, 0 OPEN.**

This is not a rollout-completion verdict. The only canonical A/B execution ended in the prescribed honest-terminal nonpass branch, no persisted `selected_pass` exists, Plan 08 was not dispatched, and SC-64.4-5 / SC-64.4-6 remain open. Those are explicit product-scope and outcome gaps, not security vulnerabilities in the implemented Plans 01–07 controls.

The exact current repository identity is `d7bd593fdaa7e030846cd8111e27075cb735442d`, tree `b097aab250b4a273e0fb6c9e226b990e28dd03c5`. Its protected implementation is exact commit `37aac6504db878e73286455bf8816b1636b23396`, tree `f59a362782699f3b11a285d7bdc0ec7e5d01adf6`. The committed `37aac650..d7bd593f` delta contains only `64.5-REVIEW.md` and `64.5-REVIEW-FIX.md`; the protected committed diff is empty and the protected worktree/index are clean.

All 28 prose threat-model rows in Plans 01–07 are implicit `mitigate` dispositions and are accounted for below. Plans contain no accepted or transferred risk. Plan 08's four planned threat rows were also read and classified, but remain explicitly out of implemented scope because its selected-pass prerequisite was not met.

## Implemented and Live-Evidence Boundary

| Control / outcome | Bounded evidence | Result |
|---|---|---|
| Sole reviewed promotion | Promotion `243bafb3-0c86-4482-8e1a-602b24546171` binds protected C1 `5eee5288` / tree `57cf3fc4`; one active promotion-candidate manifest exists for that identity. | CLOSED for the historical authorized execution |
| Shared DB authority | Authority `92c9188b-769c-430d-b333-30abf5931dd1` binds run `d5198cc0-fa07-4061-bfe7-7c73f05453a5`, candidate `63fde3fc-25e2-4414-adff-a3722e44124c`, and incumbent character corpus `55d651e5-634f-4b64-b057-350b22054007`. | CLOSED |
| Reviewed-build budget | Exactly three ordinal-1 reviewed reservations/results: actual/max requests `2/2`, `2/2`, and `3/3`; total actual requests 7; no ordinal 2. Candidate reached complete v7/cursor 3 and remained inactive. | CLOSED |
| Canonical A/B budget | Exactly one ordinal-1 reservation/result, fixed maximum 142, actual requests 0, committed code `source_drift`; no ordinal 2. | CLOSED, honest terminal |
| Selected lineage | Canonical result has `selection_id`, `selection_decision_hash`, `activation_authorization_hash`, and `output_candidate_id` all null. `64.5-07-LIVE-VERIFY.json` has `selected_result_id: null`. | CLOSED fail-safe |
| Active visibility | Recorded final state keeps the character corpus active and the complete token candidate inactive. | CLOSED fail-safe |
| Activation / Plan 08 | No Phase 64.5 selection, DB activation authorization, recovery authorization, activation event, Plan-08 summary, or closeout verification artifact exists. | Not executed; rollout incomplete |

The immutable canonical projection is
`evaluation/reports/rag_token_chunk_ab/v1/provider-authority/canonical-ab.ordinal-1.v2.json`
(SHA-256 `5eb3f77296a99b88d95457e2e53669a1a7f8ed6039ab8f138fe395b879c1a5b7`).
It binds the source-drift result `a17f70e7-1ae9-4eff-87a3-ee2ff4de35d3`, result hash
`sha256:d2cb99b7c32bff90f678301d2472b9187cefe18a21f4001bfc8f04a8fe6d210e`,
and request count `0/142`. The terminal run reports `execution_error`; its diagnostic is
`shared_preflight / candidate_pair_invalid`, provider availability `not_checked`,
request classification `not_attempted`, and request count 0.

## Latest Schema-Descendant Fix

The exact protected fix replaces unsafe exact Alembic-head equality with a fail-closed capability-and-ancestry proof:

- `_database_prerequisites()` still requires the Phase-64.3 relation and pgvector, and accepts only a single non-empty repository-known Alembic descendant of revision 029.
- `_phase64_4_schema_available()` still requires all five Phase-64.4 relations/capabilities and accepts only a repository-known descendant of revision 031.
- The SQL head read returns a revision only when `alembic_version` contains exactly one row. Empty or multiple heads become `NULL` and fail.
- Missing capabilities, pre-floor revisions, non-string/empty heads, unknown future revisions, Alembic resolution errors, and filesystem/config errors all return false.
- The helper and both consumers are within `PROTECTED_PROVIDER_EXECUTION_GRAPH`; this fix therefore cannot ride the old promotion. The existing promotion binds `5eee5288`, while `5eee5288..37aac650` changes the two protected evaluation scripts. Current promotion validation consequently fails stale before any new reservation or provider dispatch.

The fix changes no authority, reservation, result, candidate, pointer, selection, activation, or live artifact. It does not reinterpret or replace the committed source-drift result, refund ordinal 1, authorize ordinal 2, or make Plan 08 eligible. Historical evidence remains historical; the corrected predicate applies only to a future independently authorized execution path.

Evidence: `scripts/eval_rag_format_parity.py:772-821`,
`scripts/eval_rag_token_chunk_ab.py:876-910`,
`src/rag/provider_execution_authority.py:34-48,120-175`, and
`src/repositories/provider_execution_authority_repo.py:556-597`.

## Threat Verification

| Threat ID | Category | Disposition | Status | Verification evidence |
|---|---|---|---|---|
| T-64.5-01 | Spoofing / Elevation of Privilege | mitigate | CLOSED | Migration 032 permits one promotion scope and authority roots reference the committed promotion. Attestation gate identity, top-level identity, candidate identity, exact protected diff, and current protected equivalence are bound before the sole promotion mutation. Service and repository require current promotion for issue, reserve, and dispatch. Current schema-fix drift makes the old promotion stale rather than reusable. |
| T-64.5-02 | Spoofing / Tampering | mitigate | CLOSED | One tenant/run/candidate root binds candidate, incumbent, source manifest/epoch, parity, config, provider/model/dimensions, expiry, and promotion. Reservation and dispatch lock and recheck exact bindings. The recorded live projection shows one shared root used by both purposes; swapped files/UUIDs cannot mint rows or execution rights. |
| T-64.5-03 | Tampering / Repudiation | mitigate | CLOSED | Reservation commits before provider construction, uniqueness/locks contain concurrency, and committed ordinals are permanently spent. Ordinal 2 requires an exact immutable ordinal-1 result in the closed retry set `provider_unavailable` or positive-count `transient_execution_error`; `source_drift` is excluded. The actual live chain has only ordinal 1 for all four subjects. |
| T-64.5-04 | Denial of Service / Tampering | mitigate | CLOSED | Reviewed documents seal ordered batch sites and exact limits; live counts are 2/2, 2/2, and 3/3. Canonical A/B seals 126 query plus 16 ingestion sites, one attempt per site, maximum 142, SDK retries zero, and one outer attempt. DB results enforce `actual_request_count <= request_limit`; the terminal used 0/142. |
| T-64.5-05 | Tampering / Elevation of Privilege | mitigate | CLOSED | Non-selected reports forbid selection/authorization lineage. Only a complete full-provider `selected_pass` with exact run, round-owner, reservation/result, report, and 142-request lineage can persist selection/activation authorization. The real source-drift result contains only null selected fields, and the read-only checkpoint records no selected result. |
| T-64.5-06 | Elevation of Privilege / Tampering | mitigate | CLOSED | Strict attestations bind embedded gate commit/tree to top-level reviewed identity; copied evidence cannot replace identity. Reviewed build and canonical A/B require promotion before root/artifact/dataset/envelope preflight, then service/repository recheck after committed reservation and before provider construction. Legacy `build-next` remains the earliest unconditional hard stop; there is no live/override/self-attestation bypass. |
| T-64.5-07 | Tampering / Repudiation | mitigate | CLOSED | All five migration-032 authority tables reject UPDATE/DELETE and non-empty downgrade takes exclusive locks then refuses. DB projections are create-only/reconcilable outputs, not authority. The historical source-drift result, ordinal-1 reservation, run, and diagnostic remain unchanged by the schema fix; no replacement selection or activation evidence exists. |

### Key Code Evidence

- Promotion/current identity: `src/rag/provider_execution_authority.py:120-175,441-463`;
  `src/repositories/provider_execution_authority_repo.py:556-597`.
- Reservation/retry/dispatch: `src/repositories/provider_execution_authority_repo.py:208-337`.
- Result cap and immutability: `src/repositories/provider_execution_authority_repo.py:339-430`;
  `src/db/migrations/versions/032_phase64_5_provider_execution_authority.py:351-418,528-546`.
- Reviewed ordering: `scripts/reindex_policies.py:977-1008`;
  `src/rag/policy_reindex.py:263-315`.
- Canonical ordering and lineage: `scripts/eval_rag_token_chunk_ab.py:1097-1195`;
  `src/rag/evaluation/token_chunk_ab.py:916-1072`.
- Selected-pass checker: `scripts/check_phase64_5_gate.py:524-590`.

## Declared Threat-Row Accounting

Every implemented-plan row is classified as `mitigate`; no row is skipped.

| Plan | Declared rows | Consolidated mapping | Result |
|---:|---|---|---|
| 01 | Forged/copied caller IDs; concurrent double reservation; evidence rewrite/deletion; destructive non-empty rollback | T-01/T-02; T-03; T-07; T-07 | 4/4 CLOSED |
| 02 | Locked TOCTOU recheck; crash-spent budget; forged/stale/copied files; lock ordering/double spend | T-02/T-03; T-03; T-01/T-02; T-03 | 4/4 CLOSED |
| 03 | Copied descriptor/known UUID; source/pointer/parity/lease/config drift; crash-spent document; legacy/alternate CLI | T-02; T-02/T-03; T-03; T-06 | 4/4 CLOSED |
| 04 | Hidden batching/retries; file/flag/root/helper substitution; crash/ambiguous A/B; nonpass relabel/retry | T-04; T-01/T-02/T-06; T-03; T-05 | 4/4 CLOSED |
| 05 | Explicit root checkpoint; no executor self-attestation; strings not authority; CLI cannot bypass promotion | T-06; T-01/T-06; T-01/T-06; T-01/T-02/T-06 | 4/4 CLOSED |
| 06 | No historic identity reuse; serial uniqueness; expiry/drift/crash honest stop; inactive-only visibility | T-01/T-02; T-03/T-04; T-02/T-03; T-05 | 4/4 CLOSED |
| 07 | Immutable candidate/incumbent/data/config; envelope/closed retry; files cannot replace DB selected-pass; nonpass cannot tune/relabel/retry | T-02; T-03/T-04; T-05/T-06; T-03/T-05 | 4/4 CLOSED |

## Plan 08 Explicit Scope Boundary

Plan 08 was read but not executed because Plan 07 produced no selected pass. Its four rows retain planned `mitigate` disposition and are **OUT OF IMPLEMENTED SCOPE**, not open findings:

| Planned threat | Classification | Current status |
|---|---|---|
| Copied receipts cannot authorize CAS | mitigate (planned) | Plan 08 not dispatched; no selected event/receipt exists |
| Pointer drift/mixed visibility during cutover | mitigate (planned) | No Phase 64.5 cutover occurred; character remains active |
| Rollback cannot target arbitrary corpus | mitigate (planned) | No Phase 64.5 rollback occurred |
| Closeout cannot hide nonpass/stale authority/regression/security/extra recovery | mitigate (planned) | No closeout is claimed; this audit explicitly records nonpass and incomplete rollout |

Accordingly, this document does not claim V2 receipt implementation, cutover→rollback→restore, selected final pointer, Plan-08 closeout, or phase/requirement completion.

## Threat Flags and Accepted Risks

No Summary 01–07 contains an explicit `## Threat Flags` section. Summary 03 states that its new CLI/file surfaces map to the registered threat model. The Plan-06 verifier blind spots and Plan-07 schema compatibility defect were recorded as execution/architecture issues, not unregistered threat flags; the former was corroborated with exact DB-derived evidence during the controlled run, and the latter is fixed fail-closed at the protected head without changing historical state.

Unregistered flags: none. Accepted risks: none. Transferred risks: none.

## Verification Gates

Root-provided final gates for the exact current fix/evidence state:

- `make format`: PASS, 560 files unchanged.
- Full `make lint`: PASS.
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q`: exit 0,
  **4970 passed, 4 skipped, 176 warnings in 2703.31s (45:03)**.
- Warnings are existing LangGraph, RunnableConfig, Alembic, and four AsyncMock-unawaited categories; there were no failures.
- The bounded fix report additionally records minimum RED `2 failed, 1 warning`, minimum GREEN `2 passed`, and direct focused suites `197 passed, 1 warning`.

This security auditor did not rerun tests or invoke a database, provider, promotion, reservation, live command, or external service. Gate results are root-owned evidence; implementation and committed artifacts were inspected read-only.

## Audit Constraints

- Invocation: `$gsd-secure-phase 64.5`.
- Canonical task: `/root/phase64_5_c1_security`.
- Actual role: `gsd-security-auditor`.
- Only this standard `64.5-SECURITY.md` artifact was modified.
- No source, test, DB, provider, live, promotion, candidate, selection, activation, or commit mutation was performed by this auditor.
- Project `.claude/skills/` and `.agents/skills/` directories are absent; no additional project skill rules applied.

## Sign-Off

- [x] Plans 01–07 threat registers fully classified and verified.
- [x] All seven consolidated threats closed; `threats_open: 0`.
- [x] Exact current HEAD/tree and protected fix HEAD/tree recorded.
- [x] Promotion, shared authority, DB budgets, immutable nonpass, caps, ordering, hard-disable, and selected-pass-only lineage verified.
- [x] Schema-descendant fix rejects unknown/pre-floor/empty/multiple/missing-capability states and makes the old promotion stale.
- [x] Historical `source_drift` and ordinal 1 remain immutable; no ordinal 2, selection, activation, or pointer move.
- [x] Final format/lint/full-suite gates passed.
- [x] Plan 08 and rollout completion explicitly not claimed.
- [ ] Fresh selected-pass, Plan-08 activation drill/receipts/closeout, SC-64.4-5/6 closure, and phase rollout completion.

**Approval:** Security controls for implemented Plans 01–07 are verified at current HEAD
`d7bd593fdaa7e030846cd8111e27075cb735442d` / tree
`b097aab250b4a273e0fb6c9e226b990e28dd03c5`, with exact protected implementation
`37aac6504db878e73286455bf8816b1636b23396` / tree
`f59a362782699f3b11a285d7bdc0ec7e5d01adf6`.
Security is secured; rollout remains incomplete and unactivated.
