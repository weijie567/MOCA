---
phase: 64.5-database-backed-provider-budget-and-token-rollout-completion
status: verified
scope: current-c0-equivalent-protected-baseline-plans-01-05
phase_complete: false
security_enforcement: true
asvs_level: 1
block_on: open
threats_total: 7
threats_closed: 7
threats_open: 0
accepted_risks: 0
unregistered_flags: 0
created: 2026-08-13
updated: 2026-08-13
audited_head: 5eee5288efdf38a8959a0b646627b2e2fc748c1c
audited_tree: 57cf3fc4939428cbc870832e07024eed6034927d
---

# Phase 64.5 — Current C0-Equivalent Protected Baseline Security Audit

> This is a fresh current-baseline threat-mitigation verdict for the complete implemented protected runtime in Plans 01–05, including reviewed-route ordering and both attestation-identity repairs. It does not claim DB promotion, provider execution, Plans 06–08 implementation, or completion of SC-64.4-5/6.

## Verdict

At exact protected HEAD `5eee5288efdf38a8959a0b646627b2e2fc748c1c`, tree `57cf3fc4939428cbc870832e07024eed6034927d`, the current protected baseline has **7/7 consolidated threats closed and 0 open threats**. All 16 prose threat-model rows in Plans 01–04 plus all four Plan 05 rows are classified below. The plans contain no `accept` or `transfer` language, so every row has the implicit disposition `mitigate`.

“Current C0-equivalent” is the baseline role for the next reviewed transition; it does not rename historical commits or remove implemented Plan 05 routing. Exact `5eee5288` remains the immutable audit identity. Current repository HEAD `4490eadc265c932f062b5b1db68684c8aa00b035` is an evidence-only descendant: it is ancestral from `5eee5288`, has an empty committed diff over all six protected pathspecs, and the current protected worktree is clean. It is therefore only a current-equivalence witness, not a substitute reviewed identity.

The exact-`36ea6391` T-64.5-06 finding is closed. Seal and strict load now share `_require_gate_report_protected_identity()`: embedded gate commit/tree must exist and be exact 40-hex Git object IDs; seal verifies the gate-reviewed commit/tree and current protected equivalence before writing those same values; strict load rehashes the embedded gate and then requires its identity to equal the attestation top-level identity before Git validation (`scripts/check_phase64_5_gate.py:180-280,649-707`). A copied older passing gate paired with a rewritten current top-level identity now fails with `attestation_gate_identity_mismatch`. `promote-reviewed-execution` calls this strict validation before building a promotion request or invoking the sole DB mutation (`:1088-1114`). Candidate/top-level attestation equality, exact transition diff, and current protected identity remain unchanged (`:399-445`).

The three iteration-2 review findings are closed at the audited HEAD. T-64.5-06 now uses one broad, stable protected runtime pathspec (`src` plus the exact provider/checker scripts), shared by repository and checker dirty/diff gates; a recursive local-import architecture guard has no exclusions and proves the dispatch/checker closure is covered. T-64.5-03/T-64.5-05 now bind the canonical reservation and terminal lineage to one expected run ID, require exact run-derived round owners, and require a completed full-provider outcome to record the exact sealed maximum of 142 requests before any terminal/selection/result/projection write. Transient retry evidence must satisfy `0 < diagnostic.provider_request_count <= actual_request_count`, while domain, repository write/read, and ordinal-2 authorization independently reject a transient zero-count predecessor.

The implementation diff from the prior audited snapshot (`4822d410`) through the final C0 baseline (`27f0932e`) changes only the protected-graph definition, canonical A/B run/completeness validation, transient-result validation, and the CLI's explicit expected-run handoff. Migration 032, promotion/authority binding, reviewed-build ordering, envelope enumeration, provider retry configuration, hard-disable placement, append-only guards, and guarded downgrade were not weakened.

The post-clean delta from that 7/7 baseline to historical audited HEAD `049963d5` is narrower: in protected implementation code it changes only the checker, normalizing YAML-native `date`/`datetime` frontmatter values through the existing canonical-JSON encoder at both seal and strict-load comparison. The raw standard-artifact and gate-report bytes, base64 payloads, SHA-256 checks, exact clean/zero-open gates, role/task/invocation and git identity checks, create-only writer, and repository/no-follow path protections are unchanged. The other two delta files are one regression test and the local validation ledger. All seven registered threats remained closed at that historical snapshot; the later exact-`36ea6391` finding and its `5eee5288` closure are recorded separately below.

The evidence-commit repair at `a5d5e8f6` separates immutable reviewed audit identity from current protected-code equivalence: the reviewed commit must still resolve to its recorded tree and be ancestral to current HEAD, while planning-only descendants are allowed only when the protected worktree is clean and the reviewed-to-current protected diff is empty. The exact target `995afd9f` closes the bounded re-review's CR race in that proof. A single domain helper now pins HEAD for ancestry and committed-diff checks, samples protected dirty state after the diff, rechecks HEAD, then samples dirty and HEAD again. A protected dirty mutation injected after the committed diff is rejected; a commit/checkout race changes a post-sample HEAD and is rejected. Checker and repository consume this one helper rather than maintaining independent command sequences.

Reviewed commit/tree remain the audit and immutable promotion-row identity; the pinned current HEAD is only an equivalence witness. Planning/evidence-only commits remain permitted, while missing/foreign/non-ancestor history, a forged reviewed tree, committed protected drift, dirty protected drift, or observed HEAD movement fail closed. The old `049963d5` attestations remain absent and cannot be reused because both `a5d5e8f6` and `995afd9f` change protected identity code. The fresh C0 attestations embed artifacts auditing `995afd9f` / tree `5976e077` and bind the protected-equivalent evidence carrier `b5f00942` / tree `28bd8f94`, as permitted by the audited equivalence rule.

Fresh C1 commit `3b8ca36b8cabb11b695f5dc72c45df04181e2b8e`, tree `4c6bd3f28be83602b0d5b71a6739b8dd1f694729`, routes only `build-next-reviewed` and canonical `run-ab` toward the existing DB-authorized services. The ordering review findings are closed: reviewed build requires current immutable promotion before secure-root canonicalization or descriptor/state reads; canonical A/B requires it before recovery-root, candidate, dataset, input, or envelope preflight. Both services independently require promotion again, commit the purpose-specific bounded reservation, and recheck promotion/root/envelope immediately before provider construction. Missing, stale, foreign/mismatched promotion therefore reaches no root/artifact/dataset/envelope preflight, reservation, or provider construction.

Legacy `build-next` remains the first post-parse unconditional zero-side-effect hard stop. No CLI/environment override, alternate root, sanctioned executor self-attestation command, DB promotion, reservation, provider call, or Plan 06 live side effect was introduced. `issue-recovery-budget` remains a separate non-provider branch and does not make `run-ab` reachable without promotion. Gate-reviewed identity, attestation top-level identity, candidate identity, and current protected equivalence are now all separately and exactly bound, so an evidence carrier cannot replace reviewed identity.

The active-directory candidate remains `evaluation/reports/rag_provider_execution_authority/v2/promotion-candidates/3b8ca36b8cabb11b695f5dc72c45df04181e2b8e.json`; it binds reviewed route commit `3b8ca36b` / tree `4c6bd3f2`. Exact audited HEAD `5eee5288` changes the protected checker, so that candidate is correctly non-current and cannot authorize promotion. The prior `e16fefc8` candidate remains under sibling `superseded-promotion-candidates/`; the non-recursive unique loader still excludes it. A replacement candidate and fresh root-sealed attestations for the repaired protected identity are future evidence work and cannot be counted as present in this audit.

This verdict is therefore **SECURED for the current C0-equivalent protected baseline at exact `5eee5288`**. The DB promotion barrier, route ordering, reservation/provider caps, legacy hard-disable, and complete reviewed-identity chain are closed. The historical candidate remains stale for this protected checker revision; a new exact reviewed transition, root-owned evidence, and replacement candidate remain prerequisites to any sole Plan 06 promotion command. Live execution, activation receipts, and closeout remain excluded.

## Scope Boundary

Audited implementation and tests:

- migration 032 and ORM parity;
- provider execution promotion/authority/reservation/result repository and service;
- reviewed candidate-build orchestration and `reindex_policies.py` CLI;
- canonical A/B orchestration, embedding retry wrapper, and A/B CLI;
- Phase 64.5 review-attestation/promotion checker;
- the corresponding migration, authority, reindex, A/B, architecture, and gate tests;
- both C0 review-fix iterations: the prior six findings plus iteration 2's protected-closure, run/completeness, and transient-count fixes;
- the post-clean `049963d5` attestation replay delta only: checker frontmatter canonicalization, its focused regression test, and the validation-ledger entry;
- the evidence-commit current-equivalence and CR race closure through exact HEAD `995afd9f`: the shared domain helper, checker/repository adapters, focused race regression, and required planning ledgers;
- Plan 05 C1 route opening plus WR-01/WR-02 ordering repair: the two provider CLI dispatch paths, promotion-before-preflight tests, and updated provider-ownership architecture inventory;
- active-directory candidate `3b8ca36b…json`, superseded candidate namespace, exact historical C0/C1 transition, and unique-loader boundary;
- protected deltas `3b8ca36b..36ea6391..5eee5288`: reviewed-identity sealing, strict-load embedded/top-level identity binding, focused architecture assertions, and the required planning ledgers.

Plans 06–08 are excluded. Their DB promotion, fresh live authority/candidate/A-B execution, activation receipts, and closeout are neither marked open nor claimed implemented here. Plan 04's checker/promotion boundary and Plan 05's unpromoted route candidate are in scope.

## Reviewed-Identity Fix Verification at Exact HEAD

| Control | Exact-HEAD result | Evidence |
|---|---|---|
| Gate report supplies reviewed identity at seal | CLOSED | `seal_review_attestation()` requires a passing gate report, extracts exact 40-hex commit/tree, verifies commit→tree, and requires current protected equivalence before writing those values into the attestation (`scripts/check_phase64_5_gate.py:180-247`). |
| Missing / foreign / stale reviewed identity | CLOSED at seal/current-equivalence boundary | Missing or malformed fields return `gate_report_protected_identity_missing`; missing/foreign Git objects return `attestation_git_identity_missing`; wrong trees return `attestation_git_tree_mismatch`; non-ancestor, committed protected drift, dirty drift, or HEAD movement returns `gate_report_not_current` / `protected_code_dirty` through the shared helper (`scripts/check_phase64_5_gate.py:211-219,663-707`; `src/rag/provider_execution_authority.py:120-175`). |
| Evidence-only carrier does not replace reviewed identity | CLOSED at seal | Seal records the gate-reviewed commit/tree rather than current carrier HEAD, while current equivalence separately permits only an ancestral, clean, protected-empty descendant. The focused carrier assertion is present at `tests/architecture/test_phase64_5_gate.py:325-351`. |
| Strict load rebinds embedded gate identity | CLOSED | Exact HEAD rehashes embedded gate bytes, parses the required commit/tree through the same strict helper, and compares the pair with the attestation top-level identity before Git validation (`scripts/check_phase64_5_gate.py:250-280,663-682`). |
| Candidate/attestation exact equality | CLOSED | Promotion request construction still exactly compares candidate C0/C1 commit/tree with code/security attestation top-level identities, same-stage gate hashes, C0 attestation/artifact hashes, transition diff, and current protected identity (`scripts/check_phase64_5_gate.py:399-445`). No equality was removed or weakened. |
| Self-attestation / copied-evidence bypass | CLOSED for the declared boundary | The focused negative rewrites only the top-level identity over copied passing gate bytes and now requires `attestation_gate_identity_mismatch` (`tests/architecture/test_phase64_5_gate.py:353-381`). The CLI calls `validate_review_attestations()` before request construction and the DB mutation (`scripts/check_phase64_5_gate.py:1096-1114`). Root-owned actual-agent independence remains the explicit procedural boundary declared by Plan 05; task/role strings are evidence, not cryptographic identity or DB authority. |
| Existing candidate and live state | FAIL-CLOSED / not executed | The existing `3b8ca36b` candidate is stale against exact protected checker HEAD `5eee5288`; no valid replacement candidate or fresh attestation exists for this exact HEAD. This auditor did not query or mutate DB/provider/live state. |

## Fresh C1 Ordering Verification

| Control | Verification evidence |
|---|---|
| Reviewed promotion precedes all artifact preflight | `scripts/reindex_policies.py:977-1008` preserves the legacy refusal first, then constructs one authority service and awaits `require_current_promotion()` before calling the decorated reviewed command. The decorator/root, descriptor, and state work therefore cannot precede promotion. The same service is injected downstream. Parameterized missing/stale/mismatch tests require zero root, descriptor, state, reservation, and provider calls (`tests/rag/test_policy_reindex.py:109-164`). |
| Canonical promotion precedes all input preflight | `scripts/eval_rag_token_chunk_ab.py:1094-1127` leaves only non-provider `issue-recovery-budget` before the gate; `run-ab` awaits current promotion before root canonicalization, descriptor/state, dataset, questions, inputs, and envelope construction. Parameterized missing/stale/mismatch tests require every preflight, reservation, and provider counter to remain zero (`tests/eval/test_rag_token_chunk_ab.py:1220-1299`). |
| Service and dispatch rechecks retained | Reviewed service still requires promotion, performs candidate/root preflight, commits a reservation, rechecks dispatch, then constructs provider (`src/rag/policy_reindex.py:263-312`). Canonical service independently repeats promotion, exact shared-root binding, committed reservation, dispatch recheck, then provider construction (`src/rag/evaluation/token_chunk_ab.py:916-963`). C1 does not modify either service or the repository gate. |
| Legacy/override boundary | `build-next` returns `live_provider_execution_disabled` immediately after parsing and before DB/root/artifact/authority/provider (`scripts/reindex_policies.py:977-980`; `tests/rag/test_policy_reindex.py:80-106`). Parser/dispatch inspection rejects allow/enable/live/override/provider switches and environment authorization (`tests/rag/test_policy_reindex.py:167-181`). |
| One root and caps unchanged | C1 protected production changes are limited to the two CLI scripts. Authority, reservation, terminal-result, reviewed-envelope, canonical 126+16=142 envelope, SDK retry zero, and one-outer-attempt code are unchanged. Both purposes still use the same authority root and separate child envelopes (`tests/rag/test_provider_execution_authority.py:636-684`). |
| Active/superseded candidate isolation | `_candidate_from_directory` still canonicalizes the explicitly supplied directory and loads only direct non-symlink `*.json` files, requiring exactly one (`scripts/check_phase64_5_gate.py:1016-1028`). The old `e16fefc8…json` file remains in sibling `superseded-promotion-candidates/` and is not recursively visible. |
| Candidate/current identity | C0 carrier `b5f00942` / tree `28bd8f94` remains an ancestor of reviewed-route C1 `3b8ca36b` / tree `4c6bd3f2`; the existing candidate and its hashes remain byte-consistent for that historical transition. Exact HEAD `5eee5288` / tree `57cf3fc4` changes protected checker code, so the existing candidate is deliberately stale and must fail `promotion_candidate_not_current`. |
| No promotion/provider/live side effect | Repository evidence records the Plan 06 preflight stopping before migration, promotion, reservation, provider, or live artifact creation. This auditor did not query or mutate external DB/provider/live state. |

## Post-Clean Attestation Delta Confirmation

| Preserved control | Bounded confirmation at `049963d5` |
|---|---|
| Artifact and gate bytes/hash | Seal still reads the original bytes and stores their exact base64 plus SHA-256 (`scripts/check_phase64_5_gate.py:207-235`). Strict load still decodes and verifies both hashes before frontmatter replay (`:255-266`). Canonicalization never replaces or re-hashes normalized content as artifact evidence. |
| Clean code review / zero-open security review | Both seal and strict load still call `_require_standard_artifact` after parsing; it still requires exact `status == "clean"` for code and exact non-boolean integer `threats_open == 0` for security (`:207-209,267-270,641-646`). Date canonicalization cannot satisfy either predicate accidentally. Gate JSON still requires the exact schema and `result == "pass"` (`:649-660`). |
| Role, canonical task, invocation, and stage set | Frozen/extra-forbid attestation validation still binds kind to the exact agent role, requires the kind-specific `$gsd-*` workflow prefix, requires an absolute canonical task name, rejects duplicate task names, and requires the exact code/security stage set (`:61-96,292-308`). None of these fields passes through frontmatter canonicalization. |
| Git and protected-code identity | Seal rejects a dirty protected graph, binds the gate-reviewed identity, and requires current protected equivalence; strict load binds embedded/top-level identity before verifying reviewed commit→tree. Stage/current validation preserves that reviewed identity while the shared race-closed helper proves the reviewed graph equivalent to a pinned current HEAD (`scripts/check_phase64_5_gate.py:198-219,250-280,303-328,685-707`; `src/rag/provider_execution_authority.py:109-176`). The helper and checker remain inside the protected `src`/checker pathspec. |
| Create-only and path safety | Repository-bounded resolved input paths, repository-bounded output paths, no-follow output components, output-symlink refusal, descriptor-pinned parents, exclusive temporary creation, hard-link create-only publication, parent-swap detection, and conflict refusal are unchanged (`scripts/check_phase64_5_gate.py:739-1000`). |

The helper itself is a deterministic `json.loads(canonical_json_bytes(value))` round trip (`scripts/check_phase64_5_gate.py:615-618`). With the unchanged unique-key `yaml.SafeLoader`, ordinary JSON scalars retain their types, YAML dates become ISO strings, and unsupported YAML-native objects fail serialization rather than bypassing a gate.

## Race-Closed Current-Equivalence Confirmation

| Control | Bounded confirmation at `995afd9f` |
|---|---|
| Immutable reviewed identity | `require_current_protected_code_equivalence` resolves `reviewed_commit^{tree}` and compares it with the recorded reviewed tree. It stores current HEAD separately as `pinned_head`; neither checker nor repository replaces the reviewed commit/tree in attestations, candidates, requests, or promotion rows (`src/rag/provider_execution_authority.py:109-176`; `scripts/check_phase64_5_gate.py:385-451`; `src/repositories/provider_execution_authority_repo.py:87-131`). |
| Planning-only descendants | An evidence commit may follow review only when reviewed commit is an ancestor of `pinned_head`, the binary/full-index diff over the shared protected graph is empty, and both dirty samples are empty. Non-protected `.planning` changes therefore do not silently redefine runtime identity (`src/rag/provider_execution_authority.py:130-168`). |
| Foreign/non-ancestor and committed drift | The helper runs `merge-base --is-ancestor reviewed pinned_head` with `check=True`, then diffs reviewed→pinned HEAD over `PROTECTED_PROVIDER_EXECUTION_GRAPH`. Missing/foreign/non-ancestor history becomes `protected_code_not_current`; any committed protected bytes do likewise (`src/rag/provider_execution_authority.py:130-153,169-176`). |
| Reported status→diff race | The committed diff is computed before `dirty_after_diff`, so a dirty mutation appearing after diff is observed by the first status sample. The fault-injection regression performs exactly that mutation and requires `ProtectedCodeIdentityError` (`src/rag/provider_execution_authority.py:136-155`; `tests/architecture/test_phase64_5_gate.py:434-467`). |
| HEAD/commit race | Ancestry and committed diff use one `pinned_head`. HEAD is sampled after the first dirty check and again after the final dirty check; either value differing from the pinned witness rejects the proof. A concurrent protected commit cannot combine ancestry/diff from one HEAD with acceptance of another observed HEAD (`src/rag/provider_execution_authority.py:132-176`). |
| Checker/repository equivalence | Checker `_require_current_protected_identity` and repository `_require_current_protected_identity` call the same domain helper. Checker preserves `protected_code_dirty` versus its caller-specific non-current reason; repository maps any helper refusal to the context-specific `promotion_mismatch` or `promotion_stale`. The disclosed codes differ intentionally, but acceptance is identical (`scripts/check_phase64_5_gate.py:685-707`; `src/repositories/provider_execution_authority_repo.py:523-597`). |
| Failure disclosure | Git/OS/decode failures are collapsed into `ProtectedCodeIdentityError("protected_code_not_current")`; checker converts that to a fixed `GateRefusal`, repository converts it to a typed authority error, and the CLI emits only the fixed gate envelope plus reason code. Git stderr, artifact/provider bytes, and internal paths are not surfaced (`src/rag/provider_execution_authority.py:109-176`; `scripts/check_phase64_5_gate.py:1142-1176`). |

## Trust Boundaries

| Boundary | Security property verified |
|---|---|
| Review/gate evidence → promotion row | Embedded standard-artifact and gate bytes, strict frontmatter/result parsing, distinct role/stage/task metadata, exact git transition, create-only attestations, and one immutable DB promotion |
| Protected checkout → current promotion | One shared broad runtime pathspec (`src` plus exact provider/checker scripts) drives dirty detection and C0→C1 diff identity; a recursive local-import guard has no exclusions. The promotion retains exact reviewed C1 commit/tree while the shared helper proves ancestry and protected equivalence against one pinned, rechecked HEAD. |
| PostgreSQL promotion → authority root | One immutable scope row; current promotion is required before authority issue, reservation, and dispatch recheck |
| Authority root → purpose reservations | One tenant/run/candidate root is shared by `reviewed_build` and `canonical_ab`; purpose, subject, ordinal, and exact envelope live on children |
| Reservation → provider construction | Reservation commits independently, then promotion/root/envelope/DB-time are rechecked before provider construction; provider work is outside the reservation transaction |
| Provider execution → terminal result | One immutable result per reservation; request count is bounded by the sealed limit; only one narrowly proven transient class can authorize ordinal two |
| DB graph → file projection | Projections and review artifacts are create-only/reconcilable outputs; files cannot mint DB authority or refund ordinals |
| Production CLI → provider-capable service | C1 routes only reviewed build and canonical A/B through current promotion before all authority-sensitive preflight, then committed reservation and dispatch recheck; legacy build remains the earliest zero-side-effect refusal |

## Consolidated Threat Register

`64.5-VALIDATION.md` refers to `T-64.5-01..07` without defining a separate register. These seven IDs consolidate every prose row from Plans 01–04, and the four Plan 05 rows map to the same established promotion/attestation/dispatch boundary; the row-accounting table below preserves all 20 statements.

| Threat ID | Category | Component | Disposition | Status | Verification evidence |
|---|---|---|---|---|---|
| T-64.5-01 | Spoofing / Elevation of Privilege | Promotion and DB authority | mitigate | CLOSED | Migration 032 permits one checked promotion scope and requires every authority root to reference it (`src/db/migrations/versions/032_phase64_5_provider_execution_authority.py:104-170,173-220`). Promotion keeps exact candidate/top-level attestation C0/C1 commit/tree/diff identities, locks the singleton, exactly replays identical request bytes, and rejects replacements (`src/repositories/provider_execution_authority_repo.py:87-131,523-554`; `scripts/check_phase64_5_gate.py:399-445`). Current C1 equivalence separately requires reviewed tree integrity, ancestry to pinned HEAD, empty committed protected diff, two clean protected samples, and stable HEAD (`src/rag/provider_execution_authority.py:109-176`). Strict-loaded gate identity is now bound to those top-level fields before the immutable DB request is built. |
| T-64.5-02 | Spoofing / Tampering | Shared root, exact bindings, replay, and drift | mitigate | CLOSED | DB uniqueness enforces one root per tenant/run and tenant/candidate while reservations own purpose/envelope (`032...py:239-348`). Issue, reserve, and dispatch lock and revalidate current promotion, rollout, manifest, evidence, source, candidate, expiry, root, and envelope (`provider_execution_authority_repo.py:135-328,571-713`). Reviewed build and A/B independently match the supplied ID to the shared root (`src/rag/policy_reindex.py:1611-1643`; `scripts/eval_rag_token_chunk_ab.py:749-780`), and the two-purpose root/recheck test is `tests/rag/test_provider_execution_authority.py:636-684`. |
| T-64.5-03 | Tampering / Repudiation | Reservation, concurrency, crash, and retry | mitigate | CLOSED | `reserve_and_commit` uses committed tenant/row locks and uniqueness, spends ordinal one at insert, requires an exact immutable allowlisted predecessor for ordinal two, refuses ordinal three, and now rejects even a validation-bypassed transient predecessor whose actual count is zero (`src/repositories/provider_execution_authority_repo.py:206-294`). Both provider services reserve, immediately recheck, and only then construct (`src/rag/policy_reindex.py:263-315`; `src/rag/evaluation/token_chunk_ab.py:937-963`). Transient diagnostics must be positive and supported by the DB actual count before persistence (`token_chunk_ab.py:880-895,998-1027`); domain, repository write/read, and retry gates independently fail closed (`src/rag/provider_execution_authority.py:300-332`; `provider_execution_authority_repo.py:245-273,337-348,833-842`). Negatives cover model validation and bypassed/raw zero-count evidence with no result and no ordinal two (`tests/rag/test_provider_execution_authority.py:345-351,463-527`; `tests/eval/test_rag_token_chunk_ab.py:1664-1700`). |
| T-64.5-04 | Denial of Service / Tampering | Canonical request envelope and retries | mitigate | CLOSED | Canonical A/B derives 126 ordered original/rewrite query sites plus 16 exact ingestion batches, seals one attempt per site, and requires the total to equal 142 (`src/rag/evaluation/token_chunk_ab.py:301-344,347-512`; `tests/eval/test_rag_token_chunk_ab.py:317-385`). Reviewed documents seal ordered input hashes and `ceil(N/10)` batch sites (`src/rag/policy_reindex.py:1589-1681`). The SDK client uses `max_retries=0`, wrappers receive one outer attempt, and DB results cannot exceed the reserved limit (`src/rag/embedder.py:73-159`; migration `:351-418`). |
| T-64.5-05 | Tampering / Elevation of Privilege | Terminal result and selected lineage | mitigate | CLOSED (C1 implemented scope) | Results remain one-to-one, append-only, closed-code, and request-bound (`032...py:351-418,528-546`). The canonical subject is now `v2` and includes the expected run ID; terminal validation requires the same report run ID and both run-derived round owners (`src/rag/evaluation/token_chunk_ab.py:732-817`). Completed `full_provider` selected/failing reports must record exactly the envelope maximum—142 in the sealed current call graph—before typed-result, lineage, or any persistence callback (`token_chunk_ab.py:980-1027`; CLI handoff `scripts/eval_rag_token_chunk_ab.py:1175-1191`). Tests prove run-ID/round-owner replay and zero/short completed counts produce no terminal, selection, authorization, result, or projection write (`tests/eval/test_rag_token_chunk_ab.py:493-515,725-887`). Activation execution/receipts remain Plan 08 scope. |
| T-64.5-06 | Elevation of Privilege / Tampering | Review attestations, protected code, and dispatch bypass | mitigate | CLOSED | Seal and strict load share exact gate identity parsing; strict load binds embedded gate commit/tree to attestation top-level identity before Git/promotion use (`scripts/check_phase64_5_gate.py:180-280,663-707`). Forged copied-gate/current-identity evidence fails before request construction or DB mutation (`tests/architecture/test_phase64_5_gate.py:353-381`; CLI ordering `scripts/check_phase64_5_gate.py:1096-1114`). The broad protected graph, shared race-closed current-equivalence helper, candidate equality, promotion-before-preflight routing, service rechecks, legacy disable, and non-recursive candidate loader remain intact. |
| T-64.5-07 | Tampering / Repudiation | Append-only evidence and guarded downgrade | mitigate | CLOSED | All five migration-032 tables receive UPDATE/DELETE rejection triggers (`032...py:528-546`). Downgrade first takes ACCESS EXCLUSIVE locks across the family and refuses if any row exists (`:69-101`). Current static/real-PG contract tests assert trigger installation, mutation refusal, selected-lineage reuse/rollback NULL shape, and preservation after a refused downgrade (`tests/knowledge/test_provider_execution_authority_migration.py:605-689,693-927,977-1021`). Migration and ORM files are unchanged from the prior audit. |

## Declared Plan-Row Accounting

| Plan | Prose threat row | Consolidated threat | Disposition | Audit result |
|---:|---|---|---|---|
| 01 | Forged/copied caller identifiers cannot grant authority | T-64.5-01 / T-64.5-02 | mitigate | CLOSED |
| 01 | Concurrent double reservation is DB-contained | T-64.5-03 | mitigate | CLOSED |
| 01 | Evidence deletion/rewriting is blocked | T-64.5-07 | mitigate | CLOSED |
| 01 | Nonempty destructive rollback is refused | T-64.5-07 | mitigate | CLOSED |
| 02 | TOCTOU is reduced by locked exact rechecks | T-64.5-02 / T-64.5-03 | mitigate | CLOSED |
| 02 | Process death cannot create free retries | T-64.5-03 | mitigate | CLOSED |
| 02 | Forged/stale/copied files cannot grant authority | T-64.5-01 / T-64.5-02 | mitigate | CLOSED |
| 02 | Lock ordering/uniqueness prevents double spend | T-64.5-03 | mitigate | CLOSED |
| 03 | Copied descriptor/known UUID remains non-authoritative | T-64.5-02 | mitigate | CLOSED |
| 03 | Source/pointer/parity/lease/config drift fails dispatch recheck | T-64.5-02 | mitigate | CLOSED |
| 03 | Crash after reservation cannot repeat a document for free | T-64.5-03 | mitigate | CLOSED |
| 03 | Legacy and alternate CLI surfaces remain closed | T-64.5-06 | mitigate | CLOSED |
| 04 | Hidden batching/retry multiplication cannot exceed the envelope | T-64.5-04 | mitigate | CLOSED |
| 04 | Files/flags/alternate roots/helper calls cannot replace DB authority | T-64.5-01 / T-64.5-02 / T-64.5-06 | mitigate | CLOSED |
| 04 | Crash/ambiguous A/B consumes budget | T-64.5-03 | mitigate | CLOSED |
| 04 | Nonpassing result cannot be relabeled/retried into activation authority | T-64.5-05 | mitigate | CLOSED for the implemented result/selection boundary |
| 05 | Root commands/checkpoints make nondefault orchestration explicit | T-64.5-06 | mitigate | CLOSED for the C1 candidate boundary |
| 05 | Executors cannot self-attest; root seals actual returned review facts | T-64.5-01 / T-64.5-06 | mitigate | CLOSED; seal and strict load bind the same embedded reviewed identity, while root owns actual-agent independence |
| 05 | Attestation strings are evidence, not DB authority | T-64.5-01 / T-64.5-06 | mitigate | CLOSED |
| 05 | CLI reachability cannot bypass immutable DB promotion | T-64.5-01 / T-64.5-02 / T-64.5-06 | mitigate | CLOSED; promotion is first authority-sensitive operation |

## Iteration 2 Review-Fix Cross-Check

| Review finding | Threat mapping | Current audit result |
|---|---|---|
| CR-01 protected graph not transitively closed | T-64.5-06 | CLOSED by the broad shared pathspec, recursive local-import closure, zero exclusions, common repository/checker consumers, and all-gate dirty refusal |
| WR-01 selected-pass replay without current run / complete execution | T-64.5-03 / T-64.5-05 | CLOSED by `canonical_ab_subject.v2` run binding, exact report and round-owner equality, and exact 142-request completed full-provider check before all persistence |
| WR-02 transient diagnostic not tied to DB count | T-64.5-03 / T-64.5-05 | CLOSED by diagnostic-to-actual count binding plus independent domain, repository write/read, and ordinal-2 zero-count refusal |

The six findings from review iteration 1 remain closed by the controls documented in the prior audit. The target diff does not remove or relax those controls.

## Threat Flags

No unregistered threat flags were found. Summaries 01–05 contain no explicit `## Threat Flags` section; Summary 03 explicitly states that its new CLI/file surfaces were covered by the plan threat model. The exact-`36ea6391` strict-loader reviewed-identity gap mapped to T-64.5-06 and is closed at `5eee5288` by embedded/top-level equality before promotion. The prior C1 ordering WR-01/WR-02 remains closed by promotion-before-all-preflight ordering. Candidate staleness at the repaired checker HEAD is a correct fail-closed consequence of protected drift, while candidate supersession remains inside the existing immutable attestation/promotion boundary. Earlier CR-01 maps to T-64.5-06, and run/count WR-01/WR-02 map to T-64.5-03/T-64.5-05.

## Accepted Risks Log

No accepted risks. No Plans 01–05 threat uses `accept` or `transfer` disposition.

## Audit Constraints and Evidence Quality

- Target identity was independently confirmed from Git as exact HEAD `5eee5288efdf38a8959a0b646627b2e2fc748c1c`, tree `57cf3fc4939428cbc870832e07024eed6034927d`.
- Current repository HEAD `4490eadc265c932f062b5b1db68684c8aa00b035`, tree `04d019dcc5ef7dfd0220800b265a8cd462850f80`, is a descendant of the exact baseline. Direct Git checks found no committed or dirty change in `PROTECTED_PROVIDER_EXECUTION_GRAPH`; it is accepted only as a current-equivalence witness.
- C0 carrier `b5f009429ae06327b433c006953a7694b262d0fc` / tree `28bd8f9498eca0ae304177b3b1c474efa647ae3d` remains an ancestor. The historical candidate binds C0 to route commit `3b8ca36b` / tree `4c6bd3f2`; exact HEAD has a non-empty protected delta limited to `scripts/check_phase64_5_gate.py`, so the historical candidate is not current.
- The protected identity is now six stable Git pathspec entries: broad `src` plus five exact script paths. Direct source inspection confirmed repository and checker use the same tuple for dirty and C0→C1 diff gates; the architecture guard recursively covers local imports with no exclusions.
- This auditor modified only this existing SECURITY artifact. Concurrent REVIEW/REVIEW-FIX documentation edits were excluded; exact-HEAD implementation conclusions were taken from `git show 5eee5288:<path>`.
- This auditor ran no pytest, lint, formatting, migration, database, provider, live CLI, or live-state command. Closure uses current source, current test assertions, and direct call-chain inspection.
- Prior root evidence remains format PASS, lint PASS, ordering/ownership focused `166 passed`, and full production coverage `4965 passed, 4 skipped` with the sole stale inventory assertion subsequently repaired and focused-passed. The reviewed-identity seal fix ledger records `4 passed, 1 warning`; the exact strict-loader negative records RED `1 failed, 1 warning` and GREEN `1 passed, 1 warning`. This auditor did not rerun any command; closure is based on exact source, the targeted assertion, and promotion call ordering.
- No live promotion, authority, reservation, result, candidate, pointer, projection, or provider state was inspected or mutated.
- Project `.claude/skills/` and `.agents/skills/` directories are absent, so no additional project skill rules applied.

## Security Audit Trail

| Audit date | Scope / identity | Threats | Closed | Open | Run by | Invocation |
|---|---|---:|---:|---:|---|---|
| 2026-08-13 | C0 Plans 01–04 at `7147e75a` / tree `2279f33c` | 7 | 6 | 1 | Codex `gsd-security-auditor` | `$gsd-secure-phase 64.5` |
| 2026-08-13 | Fresh C0 re-audit at `4822d410` / tree `2dafbc17` | 7 | 7 | 0 | `/root/phase64_5_c0_security_rerun`, `gsd-security-auditor` | `$gsd-secure-phase 64.5` |
| 2026-08-13 | Final bounded C0 closeout at `27f0932e` / tree `d2317f48` | 7 | 7 | 0 | `/root/phase64_5_c0_final_security`, `gsd-security-auditor` | `$gsd-secure-phase 64.5` |
| 2026-08-13 | Post-clean attestation delta at `049963d5` / tree `3296ead5` | 7 | 7 | 0 | `/root/phase64_5_c0_attestation_delta_security`, `gsd-security-auditor` | `$gsd-secure-phase 64.5` |
| 2026-08-13 | Race-closed evidence equivalence at `995afd9f` / tree `5976e077` | 7 | 7 | 0 | `/root/phase64_5_c0_evidence_commit_security`, `gsd-security-auditor` | `$gsd-secure-phase 64.5` |
| 2026-08-13 | Ordering-fixed C1 at `3b8ca36b` / tree `4c6bd3f2` | 7 | 7 | 0 | `/root/phase64_5_c1_security`, `gsd-security-auditor` | `$gsd-secure-phase 64.5` |
| 2026-08-13 | Reviewed-identity fix at `36ea6391` / tree `9f98518e` | 7 | 6 | 1 | `/root/phase64_5_c1_security`, `gsd-security-auditor` | `$gsd-secure-phase 64.5` |
| 2026-08-13 | Strict attestation identity binding at `5eee5288` / tree `57cf3fc4` | 7 | 7 | 0 | `/root/phase64_5_c1_security`, `gsd-security-auditor` | `$gsd-secure-phase 64.5` |
| 2026-08-13 | Current C0-equivalent baseline at `5eee5288` / tree `57cf3fc4` | 7 | 7 | 0 | `/root/phase64_5_current_baseline_security`, `gsd-security-auditor` | `$gsd-secure-phase 64.5` |

## Sign-Off

- [x] All Plans 01–05 threat-model rows classified by disposition.
- [x] Summary threat flags, historical C0/C1 findings, and current-baseline equivalence incorporated.
- [x] Implemented controls verified from current source and direct call chains.
- [x] Iteration 2 protected-runtime closure, run/exact-142 binding, and transient-count/ordinal-2 fail-closed fixes verified.
- [x] T-64.5-01..07 checked against exact HEAD; T-64.5-06 is closed by strict-load reviewed-identity binding.
- [x] Post-clean YAML date/datetime replay canonicalization, bytes/hash, clean/zero-open, Git identity, create-only, and path controls remain present; embedded gate identity is exactly rebound to top-level attestation identity on strict load.
- [x] Shared current-equivalence helper checked: reviewed audit identity is unchanged; planning-only descendants remain valid; protected committed/dirty drift, foreign/non-ancestor history, and observed HEAD movement fail closed.
- [x] The reported status→diff/HEAD race is closed by pinned-HEAD committed checks followed by dirty/HEAD rechecks; checker and repository share acceptance semantics and disclose only typed reason codes.
- [x] Reviewed build promotion occurs before root/artifact/descriptor/state preflight; service and dispatch rechecks remain.
- [x] Canonical A/B promotion occurs before root/candidate/dataset/input/envelope preflight; service and dispatch rechecks remain.
- [x] Legacy build remains earliest zero-side-effect disabled; no CLI/environment override exists.
- [x] Shared root, reservation envelopes, 142-request cap, retry semantics, and terminal lineage are unchanged.
- [x] Historical candidate hashes and C0/route-C1 identities independently confirmed; the candidate is correctly stale at exact protected HEAD and the superseded candidate remains outside the non-recursive active loader root.
- [x] No sanctioned C1 self-attestation command, DB promotion, reservation, provider call, or Plan 06 live side effect is claimed or present in the bounded artifacts; copied gate/current identity substitution is rejected before promotion.
- [x] Plans 06–08 excluded without being mislabeled as vulnerabilities.
- [x] No accepted or transferred risk silently introduced.
- [x] Strict loader requires embedded gate-report commit/tree to equal the attestation top-level reviewed identity, with copied-gate/rewritten-identity regression coverage.
- [x] `threats_open: 0` confirmed.
- [x] `status: verified` set in frontmatter.
- [ ] Next exact reviewed transition, root-owned attestations, DB promotion, live execution, activation receipts, and full phase closeout complete (future Plans 06–08).

**Approval:** verified 2026-08-13 for the current C0-equivalent protected baseline at exact commit `5eee5288efdf38a8959a0b646627b2e2fc748c1c` / tree `57cf3fc4939428cbc870832e07024eed6034927d`. The declared Plans 01–05 attestation, promotion, ordering, cap, and provider-dispatch threats are closed. The historical candidate is stale for this baseline; a new exact reviewed transition and root-owned evidence remain mandatory before immutable DB promotion. This audit authorizes no promotion or live execution and makes no Plans 06–08 claim.
