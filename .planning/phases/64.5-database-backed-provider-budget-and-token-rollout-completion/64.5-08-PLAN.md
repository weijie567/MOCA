---
phase: 64.5-database-backed-provider-budget-and-token-rollout-completion
plan: "08"
name: Activation Drill DB Receipts and Closeout
type: execute
wave: 8
depends_on:
  - "64.5-07"
files_modified:
  - src/rag/activation_receipt.py
  - src/repositories/provider_execution_authority_repo.py
  - scripts/reindex_policies.py
  - tests/rag/test_activation_receipt.py
  - scripts/check_phase64_5_closeout.py
  - tests/rag/test_phase64_5_closeout.py
  - evaluation/reports/rag_token_chunk_ab/v1/activations/
  - docs/architecture/rag-and-grounding.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-VERIFICATION.md
  - .planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-SECURITY.md
autonomous: false
requirements:
  - SC-64.4-5
  - SC-64.4-6
must_haves:
  truths:
    - Selected cutover and restore require the exact fresh DB selected-pass lineage; rollback targets only the exact prior complete corpus.
    - Cutover, rollback, and restore are one contiguous three-event CAS sequence with committed DB lineage before create-only V2 receipt projection.
    - Closeout fails unless final pointer is selected, prior corpus stays complete/readable, receipts/history/DB lineage agree, and full regression/security evidence is clean.
  artifacts:
    - path: src/rag/activation_receipt.py
      provides: additive DB-linked V2 receipt chain with V1 unchanged
    - path: scripts/check_phase64_5_closeout.py
      provides: read-only fail-closed final pointer/history/lineage/evidence verifier
    - path: .planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-VERIFICATION.md
      provides: final requirement evidence and regression results
  key_links:
    - from: evaluation/reports/rag_token_chunk_ab/v1/activations/
      to: src/repositories/provider_execution_authority_repo.py
      via: receipt bytes derived after committed activation-history and DB lineage rows
---

<objective>
Perform exact selected cutover, prior rollback, selected restore; publish DB-derived receipts; and close Phase 64.5 only after full verification.

Purpose: demonstrate reversible activation without file authority or mixed visibility.
Output: three-event DB/receipt chain, selected final pointer, closeout verifier, documentation, and full evidence.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-07-SUMMARY.md
@src/rag/activation_receipt.py
@src/repositories/provider_execution_authority_repo.py
@scripts/reindex_policies.py
@docs/architecture/rag-and-grounding.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement additive DB-linked receipts and fail-closed closeout checks</name>
  <read_first>
    <file>.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-07-SUMMARY.md</file>
    <file>src/rag/activation_receipt.py</file>
    <file>src/repositories/provider_execution_authority_repo.py</file>
    <file>scripts/reindex_policies.py</file>
    <file>tests/rag/test_activation_receipt.py</file>
  </read_first>
  <files>
    <file>tests/rag/test_activation_receipt.py</file>
    <file>tests/rag/test_phase64_5_closeout.py</file>
    <file>src/rag/activation_receipt.py</file>
    <file>src/repositories/provider_execution_authority_repo.py</file>
    <file>scripts/reindex_policies.py</file>
    <file>scripts/check_phase64_5_closeout.py</file>
  </files>
  <action>
Add and immediately run only the minimal RED node `test_v2_receipt_requires_committed_db_selection_lineage`; after observing it fail, add the second narrow RED `test_closeout_refuses_wrong_final_pointer` before implementing its branch. Add V2 receipt DTO/loaders/persistence without changing any V1 schema, loader, path, or bytes. For selected cutover/restore, lock and recheck the exact fresh shared authority plus canonical-A/B reservation/result/selection, current source/evidence/pointer, and DB time immediately before pointer CAS. For rollback, require the exact prior corpus from cutover history to remain complete/readable; do not require or mint selection authority.

After each successful pointer CAS/history insert, append exactly one immutable activation-lineage row keyed uniquely only by `activation_history_id`; include event ordinal/reason, prior/current corpus/epoch, previous/current receipt hashes, and selected lineage. `selected_cutover` and `selected_restore` reuse the same authority/reservation/result/selection IDs/hashes and no uniqueness on those refs blocks reuse. Exact existing reason `rollback_prior` requires all authority/reservation/result/selection fields NULL. Only then project the receipt file. Reconciliation recreates identical bytes from DB but cannot authorize/mutate activation.

Create a read-only closeout CLI that refuses unless exactly three contiguous events are `selected_cutover`, `rollback_prior`, `selected_restore`; CAS endpoints join; final pointer selected; prior complete/readable; selected-pass report/selection/authorization/DB result match; selected lineage is identical on events 1/3 and wholly NULL on event 2; receipts validate; no stale/mixed authority; caps/security evidence hold. Include corruption, copied-file/no-row, wrong order/final pointer, non-NULL rollback lineage, mismatched restore lineage, incomplete prior, stale selection, and reconciliation tests. D-04/D-21/D-22/D-23/D-24/D-26/D-28 own DB-derived additive receipts, nullable rollback, exact sequence, closeout, V1 compatibility, and immediate RED.
  </action>
  <verify>
    <automated>make format &amp;&amp; make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/rag/test_activation_receipt.py tests/rag/test_phase64_5_closeout.py tests/rag/test_policy_reindex.py -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>V1 receipt bytes/loaders remain unchanged; V2 files are projections of committed DB history/lineage only.</criterion>
    <criterion>Selected cutover/restore fail before CAS without exact current selected-pass DB lineage; rollback accepts only the exact prior complete corpus.</criterion>
    <criterion>History ID alone is unique; selected cutover/restore reuse the identical selected lineage and `rollback_prior` stores all selected-lineage fields NULL.</criterion>
    <criterion>Closeout rejects every wrong pointer/order/hash/linkage/completeness/cap/security condition.</criterion>
    <criterion>Formatting, full lint, and focused receipt/closeout/activation suites pass.</criterion>
  </acceptance_criteria>
  <done>Activation and closeout code can prove the exact reversible DB-backed sequence without treating files as authority.</done>
</task>

<task type="auto">
  <name>Task 2: Run cutover rollback restore and complete full regression closeout</name>
  <read_first>
    <file>.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-07-SUMMARY.md</file>
    <file>src/rag/activation_receipt.py</file>
    <file>scripts/check_phase64_5_closeout.py</file>
    <file>scripts/reindex_policies.py</file>
    <file>.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-SECURITY.md</file>
  </read_first>
  <files>
    <file>evaluation/reports/rag_token_chunk_ab/v1/activations/</file>
    <file>docs/architecture/rag-and-grounding.md</file>
    <file>.planning/REQUIREMENTS.md</file>
    <file>.planning/ROADMAP.md</file>
    <file>.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-VERIFICATION.md</file>
    <file>.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-SECURITY.md</file>
  </files>
  <action>
Abort unless `64.5-07-SUMMARY.md`, `64.5-07-LIVE-VERIFY.json`, a fresh selected-pass check, and a fresh DB promotion read prove the exact result and current promoted C1. Snapshot pointers, then run only: prior→selected (`selected_cutover`) with shared-root selected-lineage recheck; selected→prior (`rollback_prior`) with prior complete/readable and NULL selected lineage; prior→selected (`selected_restore`) reusing/rechecking event-1 selected lineage. Require DB history/lineage, receipt, and live reread after each. Never extend/tune/renew/substitute.

Run closeout against DB/receipts; require final selected pointer, exact three joined events, reusable selected lineage on 1/3, NULL rollback lineage, readable prior, no mixed/stale authority, matching selected hashes, and fixed caps. Then run format, lint, full tests in that order. Any failure blocks docs and is recorded per AGENTS.md. D-19/D-21/D-22/D-23/D-24/D-30 own selected-pass prerequisite, exact CAS/receipt sequence, closeout, and no expansion.

Only after all gates pass, update architecture documentation with DB-only authority/envelope/retry/receipt semantics, finalize `64.5-SECURITY.md` with zero open findings and final-tree evidence, write `64.5-VERIFICATION.md` mapping SC-64.4-5/6 to exact DB rows/receipts/commands, check only SC-64.4-5/6 in REQUIREMENTS, and mark only the eight Phase-64.5 roadmap plans complete. Do not edit STATE or autopilot artifacts.
  </action>
  <verify>
    <automated>make format &amp;&amp; make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/check_phase64_5_closeout.py</automated>
  </verify>
  <acceptance_criteria>
    <criterion>DB history is exactly contiguous cutover→rollback→restore and final pointer is the selected token corpus.</criterion>
    <criterion>The DB reasons are exactly `selected_cutover`, `rollback_prior`, `selected_restore`; events 1/3 reuse selected lineage and event 2 has NULL lineage.</criterion>
    <criterion>All three create-only V2 receipts hash-chain to committed lineage; prior character corpus remains complete/readable.</criterion>
    <criterion>The immutable execution promotion remains exact/current through closeout and is cited by the selected authority/root evidence.</criterion>
    <criterion>Closeout, full lint, and full project test suite pass with project-approved commands.</criterion>
    <criterion>Security/verification/docs cite exact final evidence; only SC-64.4-5/6 and the eight Phase-64.5 roadmap items are closed.</criterion>
    <criterion>No STATE/autopilot edit, tuning, extra provider attempt, lease renewal, candidate replacement, or extended recovery chain occurs.</criterion>
  </acceptance_criteria>
  <done>Selected token corpus is restored active after a proven rollback, all DB/file lineage validates, and Phase 64.5 is honestly closed.</done>
</task>

</tasks>

<threat_model>
- Copied or valid-looking receipts cannot authorize CAS; selected events require current committed DB lineage.
- Pointer drift and mixed visibility are contained by fresh locks, expected-pointer/epoch CAS, and contiguous event verification.
- Rollback cannot target an arbitrary corpus; it is bound to the exact prior complete/readable history endpoint.
- Closeout cannot conceal a nonpass, stale authority, failed regression, open security finding, or extra recovery event.
</threat_model>

<validation_mapping>
- SC-64.4-5: exact CAS history, preserved prior corpus, no mixed visibility, and full regressions prove isolated resumable rollback safety.
- SC-64.4-6: fresh selected-pass cutover→rollback→restore with DB-linked receipts proves the reversible token rollout while retaining character fallback.
</validation_mapping>

<verification>
Plan 08 runs only after selected-pass. The live drill is one serial sequence. Final Python verification always runs `make format` before full `make lint`, and tests use only `UV_CACHE_DIR=/tmp/uv-cache uv run pytest`.
</verification>

<success_criteria>
- Exact three-event activation history and DB-derived receipt chain validate.
- Selected token corpus is final active; prior character corpus remains usable.
- Closeout/security/full regression evidence is clean and SC-64.4-5/6 are closed without unrelated state edits.
</success_criteria>

<output>
Create `64.5-08-SUMMARY.md` with exact event/history/lineage/receipt IDs and hashes, before/after pointers, closeout/full-suite results, documentation changes, and explicit no-tuning/no-extra-attempt attestation.
</output>
