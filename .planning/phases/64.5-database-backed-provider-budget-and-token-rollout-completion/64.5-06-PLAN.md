---
phase: 64.5-database-backed-provider-budget-and-token-rollout-completion
plan: "06"
name: Fresh Authority Parity and Live Inactive Candidate Build
type: execute
wave: 6
depends_on:
  - "64.5-05"
files_modified:
  - evaluation/reports/rag_embedding_tokenizer/v1/runs/
  - evaluation/reports/rag_provider_execution_authority/v2/
  - evaluation/reports/rag_provider_execution_authority/v2/promotions/
  - evaluation/reports/rag_token_chunk_ab/v1/candidates/
  - .planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-06-LIVE-VERIFY.json
autonomous: false
requirements:
  - SC-64.4-5
  - SC-64.4-6
must_haves:
  truths:
    - The sole reviewed-execution promotion is committed and read back from DB against clean standard C0/C1 code/security artifacts before any fresh live authority or reservation.
    - One wholly fresh run, parity proof, lease, candidate, and shared DB authority root is created exactly once after the review gate; no Phase 64.4 identity or expired budget is reused.
    - Exactly one inactive token candidate is built serially with at most two committed reservations per ordered document and no lease extension or extra candidate.
    - Every failed prerequisite, exhausted/expired authority, or terminal provider result stops honestly while character remains active.
  artifacts:
    - path: evaluation/reports/rag_provider_execution_authority/v2/
      provides: immutable projections of fresh committed authority/reservation/result rows
    - path: evaluation/reports/rag_token_chunk_ab/v1/candidates/
      provides: one fresh complete validated inactive token candidate
  key_links:
    - from: evaluation/reports/rag_token_chunk_ab/v1/candidates/
      to: evaluation/reports/rag_provider_execution_authority/v2/
      via: exact fresh run/candidate/document reservation and result IDs
---

<objective>
Create and build exactly one fresh inactive token candidate through the reviewed live provider path.

Purpose: replace expired/incomplete Phase 64.4 evidence without reusing or extending it.
Output: fresh parity/authority lineage and one validated inactive candidate while character remains active.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/autopilot/phase-64.5.md
@.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-05-SUMMARY.md
@.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-C0-CODE-REVIEW-ATTESTATION.json
@.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-C0-SECURITY-ATTESTATION.json
@.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-C1-CODE-REVIEW-ATTESTATION.json
@.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-C1-SECURITY-ATTESTATION.json
@src/rag/provider_execution_authority.py
@src/repositories/provider_execution_authority_repo.py
@scripts/check_embedding_tokenizer_parity.py
@scripts/reindex_policies.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pass preflight and issue one fresh reviewed-build authority</name>
  <read_first>
    <file>.planning/autopilot/phase-64.5.md</file>
    <file>.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-05-SUMMARY.md</file>
    <file>.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-C0-CODE-REVIEW-ATTESTATION.json</file>
    <file>.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-C0-SECURITY-ATTESTATION.json</file>
    <file>.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-C1-CODE-REVIEW-ATTESTATION.json</file>
    <file>.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-C1-SECURITY-ATTESTATION.json</file>
    <file>src/rag/provider_execution_authority.py</file>
    <file>src/db/models.py</file>
    <file>scripts/check_embedding_tokenizer_parity.py</file>
    <file>scripts/reindex_policies.py</file>
    <file>scripts/check_phase64_5_gate.py</file>
  </read_first>
  <files>
    <file>evaluation/reports/rag_embedding_tokenizer/v1/runs/</file>
    <file>evaluation/reports/rag_provider_execution_authority/v2/</file>
    <file>evaluation/reports/rag_provider_execution_authority/v2/promotions/</file>
    <file>evaluation/reports/rag_token_chunk_ab/v1/candidates/</file>
    <file>.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-06-LIVE-VERIFY.json</file>
  </files>
  <action>
The active Phase-64.5 Autopilot root may enter wave 6 only after it has actually run the post-wave-5 standard `$gsd-code-review` and `$gsd-secure-phase` agents, received their clean results, built the passing C1 gate report, called `seal-review-attestation` for each result, and recorded the boundary in `.planning/autopilot/phase-64.5.md`. The Plan 06 executor cannot create or repair any attestation.

Before any live identity, run the sole `promote-reviewed-execution` project-entry command. It must strict-load all four root-sealed C0/C1 attestations plus the unique C1 candidate manifest; independently rehash their embedded standard-artifact and gate-report bytes; parse the artifacts' real clean/threats-open-zero frontmatter; require four distinct canonical task-name strings with the expected code/security role and stage tuples; prove exact C0/C1 commits, trees, canonical diff, and unchanged protected current code; and reject every stale, foreign, duplicate, open, or mismatched input. In one transaction insert the singleton promotion containing only the verified C0/C1 commit/tree/diff values, four standard artifact SHAs, four attestation SHAs, both gate-report SHAs, promotion-request hash, and DB time. From a new session, `promotion-status --require-current` must read back those exact fields. No other command/API may create promotion, and neither user approval nor executor-authored evidence substitutes for the root-sealed attestations.

Only after promotion read-back, run fresh-lineage preflight. Confirm migration 032, pointer/epoch/evidence, credentials/network/OCR, source/config without secrets. Generate one new run token once; do not copy Phase 64.4 identity/evidence.

Before claim, compute a conservative bounded-build duration from the fixed ordered document count, each document's sealed maximum provider calls, two possible spent ordinals, plus validation overhead; require the candidate lease/parity/root-expiry remaining duration to exceed that bound with the repository-locked safety margin. If the repository maximum lease is 120 minutes, keep it; never increase or renew it. If it is insufficient, stop before claim/authority and create no live candidate.

Run parity once, claim one candidate with sufficient bounded lease, and call `issue-provider-authority` once; root must FK the current promotion. The read-only issued checker proves current promotion plus one fresh candidate/root, exact bindings, zero reservations/results, token inactive/character active. D-09 owns fresh identity; D-10/D-11 expiry/time; D-12 drift; D-14/D-16 exact clean reviews/promotion; D-17 one inactive candidate; D-29 bounded reads; D-30 honest stop.
  </action>
  <verify>
    <automated>make format &amp;&amp; make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_phase64_5_gate.py tests/rag/test_policy_reindex.py::test_reviewed_build_reaches_provider_only_with_committed_db_authority tests/rag/test_provider_execution_authority.py -q &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/check_phase64_5_gate.py promote-reviewed-execution --infer-unique-candidate-from evaluation/reports/rag_provider_execution_authority/v2/promotion-candidates --c0-code-attestation .planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-C0-CODE-REVIEW-ATTESTATION.json --c0-security-attestation .planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-C0-SECURITY-ATTESTATION.json --c1-code-attestation .planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-C1-CODE-REVIEW-ATTESTATION.json --c1-security-attestation .planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-C1-SECURITY-ATTESTATION.json --infer-current-protected-commit --projection-root evaluation/reports/rag_provider_execution_authority/v2/promotions &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/check_phase64_5_gate.py promotion-status --require-current --infer-unique-candidate-from evaluation/reports/rag_provider_execution_authority/v2/promotion-candidates &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/check_phase64_5_gate.py candidate --stage issued --infer-unique-phase-run --require-current-promotion --output .planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-06-LIVE-VERIFY.json</automated>
  </verify>
  <acceptance_criteria>
    <criterion>The run token, parity proof, candidate, lease, and authority are all created after Plan 05 and are absent from Phase 64.4 evidence.</criterion>
    <criterion>The four attestations were create-only sealed by the active root after four actual standard workflow agents returned; their canonical task-name strings are distinct and their role/stage tuples match C0/C1 code/security.</criterion>
    <criterion>The unique promotion row is inserted once before live authority and a new-session read-back matches exact C0/C1 commits/trees/diff, four standard-artifact SHAs, four attestation SHAs, both gate-report SHAs, request hash, and DB time.</criterion>
    <criterion>One inactive candidate and exactly one shared root exist with zero reservations/results for both purposes before Task 2.</criterion>
    <criterion>A conservative bounded-build duration plus locked safety margin fits every remaining prerequisite before claim; otherwise no live row is created.</criterion>
    <criterion>The read-only verifier emits exact IDs/counts/bindings/pointers and performs no mutation/provider work.</criterion>
    <criterion>Character remains the active corpus and all freshness uses DB/current-system time, never an operator timestamp.</criterion>
    <criterion>Any failed preflight causes an honest stop with no cap, lease, threshold, or candidate substitution.</criterion>
  </acceptance_criteria>
  <done>One fresh inactive candidate has exact unspent reviewed-build authority and all live prerequisites are green.</done>
</task>

<task type="auto">
  <name>Task 2: Build and validate the single candidate serially under fixed caps</name>
  <read_first>
    <file>.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-05-SUMMARY.md</file>
    <file>src/rag/provider_execution_authority.py</file>
    <file>src/rag/policy_reindex.py</file>
    <file>scripts/reindex_policies.py</file>
    <file>scripts/check_phase64_5_gate.py</file>
    <file>evaluation/reports/rag_provider_execution_authority/v2/</file>
  </read_first>
  <files>
    <file>evaluation/reports/rag_provider_execution_authority/v2/</file>
    <file>evaluation/reports/rag_token_chunk_ab/v1/candidates/</file>
    <file>.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-06-LIVE-VERIFY.json</file>
  </files>
  <action>
Using only the Task-1 run/candidate/authority identities, invoke `build-next-reviewed` serially for the next ordered document, then reread committed DB result/cursor state before the next invocation. For each document permit ordinal 1 and, only after a committed unchanged `provider_unavailable` or `transient_execution_error`, ordinal 2. A no-result crash, terminal result, ordinal exhaustion, expired prerequisite, drift, or changed envelope stops the plan; do not rerun the command, renew the lease, mint another authority/candidate, tune inputs, or increase attempts.

After all documents succeed, validate once and reconcile only DB→file. The complete checker requires the same current promotion, one shared root, reviewed-build counts/envelopes, complete validated inactive candidate, fixed source, and unchanged character pointer. Tests are preflight only. D-05..D-08 own capped execution; D-14/D-15 keep promotion current; D-17 one candidate; D-30 no rerun/renew/tuning.
  </action>
  <verify>
    <automated>make format &amp;&amp; make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/rag/test_policy_reindex.py tests/rag/test_provider_execution_authority.py -q &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/check_phase64_5_gate.py candidate --stage complete --identity-file .planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-06-LIVE-VERIFY.json --require-current-promotion --output .planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-06-LIVE-VERIFY.json</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Exactly one fresh candidate reaches complete and validated state; no second candidate or extended authority is created.</criterion>
    <criterion>Every document has one successful terminal DB result, at most two spent reservations, and actual requests within its sealed envelope.</criterion>
    <criterion>The candidate remains inactive and the original character corpus remains active with unchanged rollout visibility.</criterion>
    <criterion>The final read-only verifier proves exact live DB counts/states/pointers and records the shared authority ID for Plan 07.</criterion>
    <criterion>Any nonrecoverable/ambiguous outcome is truthfully persisted and stops without tuning or extra live work.</criterion>
  </acceptance_criteria>
  <done>One fresh validated token candidate exists under durable bounded authority, ready for canonical A/B but not activation.</done>
</task>

</tasks>

<threat_model>
- Historic IDs/files cannot authorize this run; every identity and prerequisite is freshly committed after the clean gate.
- Serial execution plus per-subject DB uniqueness prevents overlapping or hidden provider spend.
- Expiry, drift, crashes, and terminal errors stop instead of extending lease/budget or spawning replacement candidates.
- Inactive-only visibility preserves the known-good character corpus throughout build/validation.
</threat_model>

<validation_mapping>
- SC-64.4-5: proves one real candidate can build/resume under DB reservations without mixed visibility or loss of the active corpus.
- SC-64.4-6: produces the fresh validated candidate lineage required for a new canonical selected-pass decision.
</validation_mapping>

<verification>
Live operations are serial and non-autonomous. Automated tests use only the project interpreter. No threshold, sealed input, cap, lease renewal, extra candidate, or parallel provider command is permitted.
</verification>

<success_criteria>
- Fresh parity/run/candidate/authority lineage is complete and exact.
- One candidate is validated inactive under per-document caps.
- Character remains active and every stop condition is honored without tuning.
</success_criteria>

<output>
Create `64.5-06-SUMMARY.md` containing fresh IDs/hashes/expiries, commands/results, reservation/result counts by document, final inactive/active pointers, and an explicit no-reuse/no-tuning attestation.
</output>
