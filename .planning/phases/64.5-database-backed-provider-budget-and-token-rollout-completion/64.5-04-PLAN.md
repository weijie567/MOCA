---
phase: 64.5-database-backed-provider-budget-and-token-rollout-completion
plan: "04"
name: Canonical A-B Authority and Request Envelope Behind Hard Disable
type: execute
wave: 4
depends_on:
  - "64.5-03"
files_modified:
  - src/rag/evaluation/token_chunk_ab.py
  - src/rag/embedder.py
  - scripts/eval_rag_token_chunk_ab.py
  - tests/eval/test_rag_token_chunk_ab.py
  - tests/architecture/test_rag_chunking_boundaries.py
  - scripts/check_phase64_5_gate.py
  - tests/architecture/test_phase64_5_gate.py
autonomous: true
requirements:
  - SC-64.4-5
  - SC-64.4-6
must_haves:
  truths:
    - The canonical full-provider A/B seals an envelope enumerated from the real call graph and cannot exceed it through batching or retries.
    - Only a committed exact A/B reservation can reach provider construction, and only an immutable `selected_pass` result can create selection/activation authorization.
    - Canonical `run-ab` remains hard-disabled throughout this plan.
  artifacts:
    - path: src/rag/evaluation/token_chunk_ab.py
      provides: canonical envelope calculation, DB result lineage, and selected-pass gating
    - path: scripts/eval_rag_token_chunk_ab.py
      provides: shared-authority reservation plus still-disabled canonical runtime path
    - path: tests/eval/test_rag_token_chunk_ab.py
      provides: call-site cap, retry closure, reconciliation, and hard-disable proofs
    - path: scripts/check_phase64_5_gate.py
      provides: fail-closed standard-review, promotion, and read-only live-state project entry point
  key_links:
    - from: scripts/eval_rag_token_chunk_ab.py
      to: src/rag/provider_execution_authority.py
      via: committed canonical-ab reservation and immediate exact dispatch recheck
---

<objective>
Put the sole canonical full-provider A/B path behind DB authority and an exact request envelope while retaining its hard-disable.

Purpose: make A/B execution cost-bounded, crash-safe, and terminally honest before review permits reachability.
Output: envelope/runtime integration and adversarial focused tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-03-SUMMARY.md
@.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-CONTEXT.md
@src/rag/provider_execution_authority.py
@src/rag/evaluation/token_chunk_ab.py
@scripts/eval_rag_token_chunk_ab.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive and seal the canonical A-B provider request envelope</name>
  <read_first>
    <file>.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-03-SUMMARY.md</file>
    <file>src/rag/provider_execution_authority.py</file>
    <file>src/rag/evaluation/token_chunk_ab.py</file>
    <file>src/rag/embedder.py</file>
    <file>scripts/eval_rag_token_chunk_ab.py</file>
    <file>tests/eval/test_rag_token_chunk_ab.py</file>
  </read_first>
  <files>
    <file>tests/eval/test_rag_token_chunk_ab.py</file>
    <file>src/rag/evaluation/token_chunk_ab.py</file>
    <file>src/rag/embedder.py</file>
  </files>
  <action>
Add and immediately run the RED node `test_canonical_ab_envelope_enumerates_every_provider_call_site`. Derive the query ceiling from source, never by multiplying the already format-expanded `SEALED_TOTAL_CASE_COUNT`: `ordered_gold_questions(dataset)` currently yields 18 base ordered questions; `run_full_provider_ab` loops two roles; `run_rollback_only_retrieval_parity`/`run_retrieval_parity` loops the three `ROUND_FORMATS` and every base question once. Therefore the source-backed current query ceiling is `2 roles × 3 formats × 18 base questions = 108`; `SEALED_TOTAL_CASE_COUNT=54` is already `3 formats × 18` for one role. Lock 108 in the focused test only after the test independently derives those factors from the sealed dataset/constants and inspects/enumerates the real production loops.

Implement one canonical envelope calculator containing those 108 ordered query call sites plus candidate/incumbent ingestion batches computed offline before reservation as the sum of `ceil(exact_assembled_input_count / 10)` for every role/format/policy. Include call-site IDs/order, input hashes/counts, batch size, provider/model/dimensions, SDK retries 0, one outer attempt per reservation, total maximum requests, and canonical hash. The complete A/B envelope is purpose-specific and belongs on the shared root's `canonical_ab` reservation, never on the authority root.

Make the provider construction path consume that envelope rather than a hand-maintained constant. Configure `AsyncOpenAI` retries to zero and pass the embedding wrapper's outer-attempt setting explicitly as one; test the real wrapper/call sites so retry multiplication or a newly added provider call causes the envelope test to fail. Preserve the sealed source, Gold, baseline, fractions, thresholds, roles, formats, and metric definitions unchanged.

Decision ownership: D-02 puts the A/B purpose/envelope on a reservation beneath the shared root; D-08 owns source-derived request accounting and zero/one retry settings; D-18 preserves the sealed dataset/metrics/thresholds; D-28 owns the immediate call-graph RED.
  </action>
  <verify>
    <automated>make format &amp;&amp; make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_rag_token_chunk_ab.py::test_canonical_ab_envelope_enumerates_every_provider_call_site -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>The test proves the source-backed current query ceiling is 108 (`2 × 3 × 18`) and never multiplies the already format-expanded value 54 by formats again.</criterion>
    <criterion>The envelope adds exact offline-derived ingestion-batch calls for both roles/formats/policies and is stored on the `canonical_ab` reservation.</criterion>
    <criterion>The production constructors use SDK retries 0 and one explicit outer attempt; no implicit retry can exceed the sealed maximum.</criterion>
    <criterion>Adding or bypassing an A/B provider call site without updating the enumerator makes the focused test fail.</criterion>
    <criterion>No sealed dataset, fraction, threshold, role, format, or metric value changes.</criterion>
  </acceptance_criteria>
  <done>The canonical A/B request ceiling is derived from and enforced by the real production call graph.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate A-B reserve, result, selection, and reconciliation behind disable</name>
  <read_first>
    <file>src/rag/evaluation/token_chunk_ab.py</file>
    <file>src/rag/provider_execution_authority.py</file>
    <file>scripts/eval_rag_token_chunk_ab.py</file>
    <file>tests/eval/test_rag_token_chunk_ab.py</file>
    <file>src/rag/policy_reindex_artifacts.py</file>
  </read_first>
  <files>
    <file>tests/eval/test_rag_token_chunk_ab.py</file>
    <file>src/rag/evaluation/token_chunk_ab.py</file>
    <file>scripts/eval_rag_token_chunk_ab.py</file>
    <file>tests/architecture/test_rag_chunking_boundaries.py</file>
    <file>scripts/check_phase64_5_gate.py</file>
    <file>tests/architecture/test_phase64_5_gate.py</file>
  </files>
  <action>
Add and immediately run the RED node `test_terminal_ab_result_cannot_reserve_ordinal_two`. Do not add `issue-ab-authority` and do not create a second root. Make canonical `run-ab` accept the authority ID issued by `issue-provider-authority`, prove it is the exact shared tenant/run/candidate root, then reserve `purpose=canonical_ab` with exact fresh candidate/incumbent, sealed-source, Gold/baseline, and computed envelope bindings on the reservation.

Integrate canonical `run-ab` with the same sequence as reviewed build: require current DB promotion/current C1 protected identity before reserve; reserve/commit; immediate promotion/root/envelope/DB-time recheck; construct; bounded execution; result; projections. Only selected-pass creates selection/authorization; retry remains closed. Missing/stale/foreign/mismatched promotion or code drift stops before reservation/provider.

Keep the existing earliest `run-ab` hard-disable ahead of settings, paths, DB, artifact, reservation, and provider effects. Cover copied/missing projections, absent DB row, all drift dimensions, expiry equality, cap enforcement, crash/no-result, allowlisted retry, terminal no-retry, selected-pass-only authorization, reconciliation, and disabled zero-side-effects. Add `test_persisted_selected_pass_binds_db_result_lineage` and extend the static architecture inventory so every production provider constructor and dispatch helper is owned by the authority boundary; an alternate A/B/build command, helper, factory, or test-only seam must fail the guard.

Implement `check_phase64_5_gate.py` now so C0 review includes it. Do not require nonexistent metadata from standard REVIEW/SECURITY. Define exact attestation schema `phase64_5.review_attestation.v1`: stage (`c0`/`c1`) and kind (`code`/`security`) are attestation-owned; record active-root-supplied collaboration canonical task name, actual agent role, exact workflow invocation, captured protected-code HEAD/tree, standard artifact path/SHA and embedded canonical bytes plus its real parsed frontmatter, gate-report path/SHA and embedded bytes/result, and sealed-at time. It contains no self hash and makes no cryptographic identity claim.

Add create-only, no-DB/no-provider `seal-review-attestation`: the active autopilot root calls it only after the standard workflow agent returns. It verifies actual artifact bytes and real frontmatter (`status: clean`; security `threats_open: 0`), clean gate report, captured git commit/tree, then seals one of `64.5-C0-CODE-REVIEW-ATTESTATION.json`, `64.5-C0-SECURITY-ATTESTATION.json`, `64.5-C1-CODE-REVIEW-ATTESTATION.json`, or `64.5-C1-SECURITY-ATTESTATION.json`. `review-attestations` strict-loads all provided files, rehashes embedded artifact/gate bytes, reparses real frontmatter/results, verifies git commit/tree/diff, and requires four distinct canonical task-name strings with expected code/security roles/stages; root is responsible for actual agent independence. `promotion-candidate` binds C0 attestations and current C1 code without DB writes. Sole mutating `promote-reviewed-execution` validates all four attestations/current tree then writes/read-backs DB promotion. Other modes remain read-only. Add adversarial tests for fabricated standard-only fields, duplicate agent strings, wrong roles, byte/hash/frontmatter/gate/commit/tree/diff mismatch, self-created/unsealed files, and side effects.

Decision ownership: D-03/D-05/D-06/D-07 enforce committed capped reservations and spent crashes; D-12 rejects shared-root or A/B-reservation drift; D-13 retains the hard stop; D-19 permits authorization only from selected-pass; D-20 closes A/B retries and forbids tuning/recovery expansion.
  </action>
  <verify>
    <automated>make format &amp;&amp; make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_rag_token_chunk_ab.py tests/architecture/test_rag_chunking_boundaries.py tests/architecture/test_phase64_5_gate.py -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>A/B dispatch requires one committed exact DB reservation and an immediate fresh DB-time recheck.</criterion>
    <criterion>The checker/promotion entry point is implemented before C0 review; only `promote-reviewed-execution` may write promotion and all other checker modes are read-only.</criterion>
    <criterion>Checker trusts only actual standard artifact bytes/frontmatter plus phase attestations; it never assumes standard artifacts carry stage/scope/reviewed-commit/instance/evidence fields.</criterion>
    <criterion>Every production reservation/provider path requires an exact current immutable promotion row; files alone never pass.</criterion>
    <criterion>The A/B reservation uses the same authority root issued for reviewed build; no A/B-specific authority row or issuance command exists.</criterion>
    <criterion>Terminal nonpassing outcomes persist truthful DB evidence, create no selection/authorization, and cannot obtain ordinal 2.</criterion>
    <criterion>Only unchanged unavailable/transient evidence can obtain the sole second reservation; no-result crashes and ordinal 3 are refused.</criterion>
    <criterion>`run-ab` remains hard-disabled with zero side effects and the full focused suite/lint pass.</criterion>
  </acceptance_criteria>
  <done>The canonical A/B implementation is authority-bound and terminally honest but remains production-unreachable pending Plan 05.</done>
</task>

</tasks>

<threat_model>
- Hidden batching or retry multiplication cannot exceed the DB-sealed request envelope.
- File reports, selections, authorization UUIDs, flags, alternate roots, and helper calls cannot substitute for committed DB authority.
- Crash and ambiguous outcomes consume budget rather than silently rerunning a costly full A/B.
- A nonpassing result cannot be relabeled or retried into activation authority.
</threat_model>

<validation_mapping>
- SC-64.4-5: canonical A/B provider use is exact, capped, crash-safe, serial, and preserves current service on failure.
- SC-64.4-6: only a fresh full-provider `selected_pass` creates the immutable selected lineage needed for activation.
</validation_mapping>

<verification>
Run the focused suite with fake/mocked provider boundaries and real PostgreSQL only; never invoke the live provider. Run `make format` before full `make lint`.
</verification>

<success_criteria>
- The real call graph determines and obeys a canonical maximum request count.
- DB reservation/result rules govern every canonical A/B execution and retry.
- Only selected-pass can create authorization, and `run-ab` remains disabled pending independent review.
</success_criteria>

<output>
After completion, create `.planning/phases/64.5-database-backed-provider-budget-and-token-rollout-completion/64.5-04-SUMMARY.md` with envelope math/hash, dispatch order, retry/result matrix, hard-disable evidence, and verification results.
</output>
