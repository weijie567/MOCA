---
phase: 08
plan: 08-05
type: execute
wave: 4
depends_on:
  - 08-04
files_modified:
  - tests/knowledge/datasets/citation_membership_v1.json
  - tests/knowledge/test_citation_membership_eval.py
  - tests/knowledge/test_facade_integration.py
  - .planning/phases/08-knowledge-facade/08-EVAL-GATE.md
requirements:
  - KNOW-01
  - KNOW-02
  - KNOW-03
---

# 08-05 PLAN: Citation-Membership Eval Gate + Integration + Spec Sync

<objective>
Close Phase 8 with the BLOCKING citation-membership eval gate, an end-to-end facade integration test through the migrated nodes, and the eval-gate / spec-consistency record (D-D3, D-D4). This plan:

1. Creates a pinned citation-membership eval dataset (owner = Phase 8) with a stated version and content hash, and a runner test that BLOCKS on failure.
2. Adds an integration test proving the full retrieve → recommend → final path produces canonical EvidenceRefV1 with evidence_id membership and preserves strong/partial/no_evidence routing.
3. Writes `08-EVAL-GATE.md` recording: blocking status, dataset owner/version/hash, failure impact; AI-SPEC intentionally absent (with reason); and that semantic groundedness/support is a SEPARATE deferred eval with a named owner (not superseded by membership).
4. Verifies the Phase 7 baseline + migration-plan §19 are already consistent with Phase 8's direct cutover (no runtime read-switch); records any residual finding.

This plan adds tests + planning docs only. It must NOT modify `src/` (all implementation is in 08-01..04). The dataset is a test fixture, not a DB table.
</objective>

<traceability>
## Spec sections covered
- `docs/eval-test-plan.md` §20 — citation membership eval; semantic support as separate deferred eval; failure mode "no_evidence/failed membership still emits deterministic action" must be guarded.
- `.planning/REQUIREMENTS.md` lines 47, 49 — DEFERRED rows name owner/rationale/dependency/gate; eval gates name blocking status, dataset owner/version/hash, failure impact.
- `docs/migration-plan.md` §19 Phase 8 row — direct cutover, retained adapter rollback, no read-switch, no schema migration (already consistent — verify).
- `.planning/phases/07-contract-baseline/07-CONTRACT-BASELINE.md` — EvidenceRefV1/evidence_id DEFERRED_WITH_OWNER → Phase 8 (verify now satisfied / record sync).

## Spec consistency findings
- Verified: migration-plan §19 Phase 8 row already states "direct cutover；git revert 或 retained LegacyRagKnowledgeAdapter 回滚；不做 schema migration" — consistent with D-A3/D-D4, no runtime read-switch. Record CONSISTENT (no change needed) plus the consolidated Phase 8 findings (policy_version `v{version}`; tenant-over-global deferred; legacy citation validator retained; trace_id/merchant_scope plumbing → Phase 10) in `08-EVAL-GATE.md`.

## Schema/migration owner
- N/A with reason: no DB schema/migration. The eval dataset is a JSON test fixture under `tests/`, not a persisted table.

## Service/API owner
- N/A with reason: no API/service change; eval + integration tests only.

## State/router impact
- N/A with reason: no state/router change; integration test observes the already-migrated path from 08-04.

## Required tests
- `tests/knowledge/test_citation_membership_eval.py` — runs the pinned dataset through `validate_membership`; BLOCKING (a failed membership case fails the suite); asserts dataset hash matches the recorded value.
- `tests/knowledge/test_facade_integration.py` — end-to-end: facade → retrieve node → recommendation membership → final response, asserting EvidenceRefV1 and status routing.

## Acceptance criteria
- Task-level criteria below, plus both test files pass via `uv run pytest tests/knowledge -q`, and `08-EVAL-GATE.md` contains the required eval-gate metadata.

## Rollback/non-goals/deferred items
- Rollback: git revert; removing the dataset/tests does not touch runtime code.
- Non-goals: implementing semantic groundedness/support eval (separate deferred eval); modifying `src/`; modifying the docs' normative §19 (already consistent).
- Deferred: semantic claim-support eval — owner = separate deferred RAG groundedness eval (eval-test-plan §20); recorded with owner + acceptance gate in `08-EVAL-GATE.md`.
</traceability>

<threat_model>
ASVS L1; block on high.

| Threat | Impact | Mitigation in this plan | Blocking? |
| --- | --- | --- | --- |
| Membership eval treated as non-blocking | Citation regressions ship silently | The runner test fails (non-zero) on any membership failure; `08-EVAL-GATE.md` marks it BLOCKING with failure impact | high → yes |
| Dataset drifts without version/hash bump | Eval passes against a silently-changed dataset; Phase 13/replay can't trust it | Test asserts the dataset's recorded content hash; changing the dataset without updating the version+hash fails the test | high → yes |
| Membership silently stands in for semantic support | Unsupported-but-cited claims pass as grounded | `08-EVAL-GATE.md` records semantic support as a SEPARATE deferred eval with owner; integration test asserts membership-only semantics | high → yes |
| no_evidence/failed membership still emits a deterministic action | Unsafe recommendation on weak evidence | Integration test asserts no_evidence → insufficient draft and all-invalid citations → citation_invalid (no action draft) | high → yes |
</threat_model>

<must_haves>
- `tests/knowledge/datasets/citation_membership_v1.json` exists with a top-level `version` field (e.g. `"citation_membership.v1"`) and a list of cases, each with claims (`cited_evidence_ids`) + available `evidence_refs` (by `evidence_id`) + `expected_is_valid`. Cases MUST include: a valid-membership case, a missing-evidence_id case, an empty-citation case, and a same-chunk_id-different-evidence_id case.
- `tests/knowledge/test_citation_membership_eval.py` loads the dataset, asserts each case's `validate_membership` verdict equals `expected_is_valid`, and asserts the dataset's SHA-256 content hash equals a recorded constant (the gate's pinned hash). Failure → non-zero exit (BLOCKING).
- `tests/knowledge/test_facade_integration.py` exercises the migrated path (facade → retrieve_policy_evidence → generate_recommendation → final_response) with mocked retrieval, asserting: canonical EvidenceRefV1 in state; strong/partial/no_evidence routing preserved; membership validation by evidence_id; no_evidence/all-invalid → no action draft.
- `08-EVAL-GATE.md` records: eval name, BLOCKING status, dataset owner=Phase 8, dataset version, dataset content hash, failure impact; AI-SPEC intentionally absent + reason; semantic support as separate deferred eval + owner + acceptance gate; consolidated Spec Consistency Findings; confirmation that §19 / Phase 7 baseline are consistent (no read-switch).
- No `src/` file modified by this plan.
</must_haves>

<tasks>

<task id="1" type="execute">
  <title>Create pinned citation-membership eval dataset</title>
  <read_first>
  - `src/knowledge/citation.py`
  - `src/knowledge/schemas.py`
  - `tests/test_rag_eval.py`
  - `docs/eval-test-plan.md` (§20)
  </read_first>
  <action>
  Create `tests/knowledge/datasets/citation_membership_v1.json`. Top-level shape:

  ```json
  {
    "version": "citation_membership.v1",
    "description": "Phase 8 citation membership gate dataset. Membership = cited evidence_id present in evidence_refs. NOT semantic support.",
    "cases": [
      {
        "id": "valid_membership",
        "evidence_ids": ["policy_refund_timeout/chunk_001@v3", "policy_refund_timeout/chunk_002@v3"],
        "claims": [{"claim_id": "c1", "claim_text": "...", "cited_evidence_ids": ["policy_refund_timeout/chunk_001@v3"]}],
        "expected_is_valid": true
      },
      {
        "id": "missing_evidence_id",
        "evidence_ids": ["policy_refund_timeout/chunk_001@v3"],
        "claims": [{"claim_id": "c1", "claim_text": "...", "cited_evidence_ids": ["policy_refund_timeout/chunk_999@v3"]}],
        "expected_is_valid": false
      },
      {
        "id": "empty_citation",
        "evidence_ids": ["policy_refund_timeout/chunk_001@v3"],
        "claims": [{"claim_id": "c1", "claim_text": "...", "cited_evidence_ids": []}],
        "expected_is_valid": false
      },
      {
        "id": "same_chunk_id_diff_evidence_id",
        "evidence_ids": ["policy_a/chunk_001@v3"],
        "claims": [{"claim_id": "c1", "claim_text": "...", "cited_evidence_ids": ["policy_b/chunk_001@v3"]}],
        "expected_is_valid": false
      }
    ]
  }
  ```

  After writing the file, compute its SHA-256 and record it for task 2:

  ```bash
  uv run python -c "import hashlib,pathlib;print('sha256:'+hashlib.sha256(pathlib.Path('tests/knowledge/datasets/citation_membership_v1.json').read_bytes()).hexdigest())"
  ```

  Note: keep the file content stable/sorted so the hash is reproducible. The printed hash becomes the pinned constant in task 2 and `08-EVAL-GATE.md`.

  > On the placeholder (Codex review B3): the dataset file does not exist until this task runs, so a literal SHA-256 cannot be written into the plan ahead of time — that would be a fabricated hash. The planning-time contract is: dataset **owner = Phase 8**, **version = `citation_membership.v1`** (both fixed here), and the **hash is computed from the committed dataset bytes and pinned at execution** into both the runner constant (task 2) and `08-EVAL-GATE.md` (task 4). The hash is frozen and drift-guarded the moment the dataset is committed; it is not left unbounded.
  </action>
  <verify>
  The JSON parses and contains the four required case ids; the SHA-256 command prints a `sha256:` hash.
  </verify>
  <acceptance_criteria>
  - `tests/knowledge/datasets/citation_membership_v1.json` contains `"version": "citation_membership.v1"`.
  - It contains case ids `valid_membership`, `missing_evidence_id`, `empty_citation`, `same_chunk_id_diff_evidence_id`.
  - Each case has `claims` with `cited_evidence_ids` and an `expected_is_valid` boolean.
  - The SHA-256 of the file is computed and available for the runner.
  </acceptance_criteria>
</task>

<task id="2" type="execute">
  <title>Implement BLOCKING citation-membership eval runner</title>
  <read_first>
  - `tests/knowledge/datasets/citation_membership_v1.json`
  - `src/knowledge/citation.py`
  - `src/knowledge/schemas.py`
  </read_first>
  <action>
  Create `tests/knowledge/test_citation_membership_eval.py`:
  - Load the dataset JSON from `tests/knowledge/datasets/citation_membership_v1.json`.
  - Record the pinned hash as a module constant `DATASET_SHA256 = "sha256:<value from task 1>"`.
  - `test_dataset_hash_pinned`: recompute the file's SHA-256 and assert it equals `DATASET_SHA256` (guards silent dataset drift).
  - `test_membership_eval_gate` (parametrized over cases): for each case, build `EvidenceRefV1` stubs (or pass a minimal object exposing `.evidence_id`) for each id in `evidence_ids`, call `validate_membership(case["claims"], refs)`, and assert `result.is_valid == case["expected_is_valid"]`. Any mismatch fails the test (BLOCKING).
  - Add a module docstring stating this gate is BLOCKING and membership ≠ semantic support.

  Keep it pure/DB-free.
  </action>
  <verify>
  `uv run pytest tests/knowledge/test_citation_membership_eval.py -q` exits 0. Temporarily editing the dataset (then reverting) makes `test_dataset_hash_pinned` fail — confirm the guard works, then leave the dataset unchanged.
  </verify>
  <acceptance_criteria>
  - `tests/knowledge/test_citation_membership_eval.py` contains `DATASET_SHA256 = "sha256:` and asserts the recomputed hash equals it.
  - It parametrizes over the dataset cases and asserts `is_valid` matches `expected_is_valid`.
  - The module docstring states the gate is BLOCKING and membership is not semantic support.
  - `uv run pytest tests/knowledge/test_citation_membership_eval.py -q` exits 0.
  </acceptance_criteria>
</task>

<task id="3" type="execute">
  <title>End-to-end facade integration test</title>
  <read_first>
  - `src/agent/nodes/retrieve_policy_evidence.py`
  - `src/agent/nodes/generate_recommendation.py`
  - `src/agent/nodes/final_response.py`
  - `src/knowledge/service.py`
  - `tests/agent/conftest.py`
  - `tests/agent/test_graph.py`
  </read_first>
  <action>
  Create `tests/knowledge/test_facade_integration.py`. Mock the retrieval source (adapter/service) so the path is deterministic and DB-free, following `tests/agent/test_graph.py` / `tests/agent/conftest.py` patterns. Exercise the migrated nodes in sequence (call them directly with a constructed state + config, or via the graph if the existing test harness supports it):
  - strong_evidence: facade returns strong result → `retrieve_policy_evidence` puts EvidenceRefV1 refs (with `evidence_id`, `text_hash`) into state; `generate_recommendation` (LLM mocked) cites a present `evidence_id` → membership valid, ref retained; `final_response` renders citation without error.
  - partial_evidence: facade returns `partial_evidence` (0.55 ≤ best_score < 0.70) with refs above the gate → recommendation proceeds with the retained evidence (NOT routed to insufficient); status preserved as `partial_evidence` through the path. Confirms partial routing is not collapsed into strong or no_evidence.
  - no_evidence: facade returns no_evidence → `recommendation_draft.recommended_action == "insufficient_evidence"`; NO action draft / proposed action produced.
  - membership fail: recommendation cites an absent `evidence_id` → all-invalid → `citation_invalid`, confidence 0.0; no action proposed.
  - Assert membership-only semantics: a cited present evidence_id whose text does NOT support the claim still passes membership (the integration path does not perform semantic support).

  Reuse existing fixtures/mocks; do not hit the DB or a real LLM.
  </action>
  <verify>
  `uv run pytest tests/knowledge/test_facade_integration.py -q` exits 0.
  </verify>
  <acceptance_criteria>
  - The test drives retrieve → recommend → final_response with a mocked facade/LLM.
  - It asserts EvidenceRefV1 refs (with `evidence_id`) appear in state after retrieval.
  - It covers all three retrieval statuses: `strong_evidence`, `partial_evidence`, and `no_evidence` (partial proceeds with evidence; no_evidence → `insufficient_evidence` + no action draft).
  - It asserts membership-fail → `citation_invalid` (confidence 0.0) and no action draft.
  - It asserts membership passes on present evidence_id regardless of semantic support.
  - `uv run pytest tests/knowledge -q` exits 0.
  </acceptance_criteria>
</task>

<task id="4" type="execute">
  <title>Write 08-EVAL-GATE.md and record spec consistency</title>
  <read_first>
  - `tests/knowledge/datasets/citation_membership_v1.json`
  - `tests/knowledge/test_citation_membership_eval.py`
  - `docs/migration-plan.md` (§19 Phase 8 row)
  - `.planning/phases/07-contract-baseline/07-CONTRACT-BASELINE.md`
  - `.planning/phases/08-knowledge-facade/08-CONTEXT.md`
  </read_first>
  <action>
  Create `.planning/phases/08-knowledge-facade/08-EVAL-GATE.md` with these sections:

  `## Citation Membership Eval Gate`
  - Eval name: Citation membership.
  - Blocking status: **BLOCKING**.
  - Dataset owner: Phase 8.
  - Dataset version: `citation_membership.v1`.
  - Dataset content hash: `sha256:<value>` (paste from task 1/2).
  - Definition: cited `evidence_id ∈ evidence_refs`; deterministic; no LLM.
  - Failure impact: a no_evidence or failed-membership result MUST NOT emit a deterministic action recommendation; membership MUST NOT be counted as semantic claim support.

  `## AI-SPEC Disposition`
  - AI-SPEC intentionally ABSENT. Reason: Phase 8 is a refactor; framework (LangGraph + pgvector RAG) and the deterministic eval gate are already locked in `docs/contract-spec.md` §8.3 and `08-CONTEXT.md`. This is recorded so downstream review does not misflag the absence as a gap.

  `## Deferred: Semantic Groundedness/Support Eval`
  - Status: DEFERRED_WITH_OWNER.
  - Owner: separate deferred RAG groundedness/support eval (eval-test-plan §20).
  - Rationale: semantic support requires a separate eval or reviewed rule-based mapping; it is NOT inferred from membership.
  - Dependency: a labelled support dataset.
  - Acceptance gate: support eval passes on its own dataset; membership gate does not substitute for it.

  `## Spec Consistency Findings (Phase 8 consolidated)`
  Table with columns `Finding | Evidence | Handling | Owner | Status`:
  - `policy_version = v{version}` (not the date-like spec JSON example) | `src/db/models.py` PolicyDocument.version vs §8.3 example | pinned + golden test (08-01) | Phase 8 | COVERED
  - Ingestion did not bump version on content change (B1) | `src/rag/ingestion.py` overwrote `doc.content` without `version` bump | content-conditional bump in same transaction + tests (08-01 task 4) | Phase 8 | COVERED
  - Tenant-over-global deferred | PolicyDocument.tenant_id NOT NULL, no global scope, no P8 migration | deterministic tenant-scoped only; schema-and-query gate later | later policy-scope phase | DEFERRED_WITH_OWNER
  - `merchant_id` not applicable as a policy DB filter (B4) | no merchant column on policy tables; `search_similar` takes no merchant_id | facade validates merchant_id against merchant_scope as AUTH gate only; merchant-scoped policy query deferred | later policy-scope phase | DEFERRED_WITH_OWNER
  - Effective-time filter vs LIMIT ordering | `search_similar` ORDER BY+LIMIT has no effective predicate | adapter filters effective_date before top_k truncation (08-02) | Phase 8 | COVERED
  - `assess_risk_and_approval` only suppressed insufficient_evidence (B2) | node built proposed action for citation_invalid/retrieval_error | extend no-action set to all three states (08-04 task 5) | Phase 8 | COVERED
  - Observability consumers read legacy `data.evidence` / dedupe by chunk_id | `src/agent/trace.py`, `src/api/routers/agent_runs.py` | migrate to evidence_refs / evidence_id with legacy fallback (08-06) | Phase 8 | COVERED
  - Legacy `src/rag/citation_validator.py` retained as internal; new evidence_id validator in `src/knowledge/citation.py` | D-C3 | keep legacy unmodified | Phase 8 | COVERED
  - trace_id / merchant_scope not yet AgentState fields | `src/agent/state.py` | KnowledgeContext built from trusted sources; full convergence Phase 10 | Phase 10 | DEFERRED_WITH_OWNER
  - migration-plan §19 / Phase 7 baseline rollback | §19 Phase 8 row states direct cutover + retained-adapter rollback, no read-switch | CONSISTENT — no change needed | Phase 8 | COVERED

  `## Requirements Coverage`
  - KNOW-01 → 08-02 facade status + 08-04 node switch.
  - KNOW-02 → 08-01 EvidenceRefV1/projection/version-pin + 08-03 membership + 08-04 state/consumer migration + 08-06 observability.
  - KNOW-03 → 08-04 facade switch with git-revert/adapter rollback + B2 no-action gate; no persistence/schema; this gate's blocking eval.
  </action>
  <verify>
  `08-EVAL-GATE.md` exists and contains the dataset hash, BLOCKING, and the deferred semantic-support owner.
  </verify>
  <acceptance_criteria>
  - `.planning/phases/08-knowledge-facade/08-EVAL-GATE.md` contains `Blocking status: **BLOCKING**` (or equivalent BLOCKING marker).
  - It contains `Dataset version: citation_membership.v1` and a `sha256:` dataset hash.
  - It contains the AI-SPEC intentionally-absent reason.
  - It records semantic groundedness/support as DEFERRED_WITH_OWNER with a named owner and acceptance gate.
  - It contains the consolidated Spec Consistency Findings table including the `policy_version = v{version}` and tenant-over-global rows.
  - It maps KNOW-01/02/03 to the plans that cover them.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run `uv run pytest tests/knowledge -q` (all pass, including the BLOCKING eval). Run the full suite `uv run pytest -q`; note any pre-existing unrelated failures. Run `git status --short` and confirm this plan added only `tests/knowledge/` and `.planning/` files — no `src/` changes.
</verification>

<success_criteria>
- Citation-membership eval gate is BLOCKING with pinned dataset owner=Phase 8, version, and content hash (KNOW-02, D-D3).
- Integration test proves facade path: EvidenceRefV1 in state, status routing preserved, evidence_id membership, no unsafe action on no_evidence/invalid citations (KNOW-01/03).
- AI-SPEC intentionally-absent recorded with reason; semantic support is a separate deferred eval with a named owner (not superseded by membership).
- §19 / Phase 7 baseline confirmed consistent (no runtime read-switch); consolidated Spec Consistency Findings recorded.
- No `src/` modified by this plan.
</success_criteria>

## PLANNING COMPLETE
