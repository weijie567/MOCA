---
phase: 08
plan: 08-02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/knowledge/config.py
  - src/knowledge/adapters.py
  - src/knowledge/service.py
  - tests/knowledge/test_facade_status.py
  - tests/knowledge/test_effective_time.py
  - tests/knowledge/test_tenant_scope.py
requirements:
  - KNOW-01
  - KNOW-02
---

# 08-02 PLAN: PolicyKnowledgeService Facade + LegacyRagKnowledgeAdapter

<objective>
Implement the `PolicyKnowledgeService` facade (`src/knowledge/service.py`) and the `LegacyRagKnowledgeAdapter` (`src/knowledge/adapters.py`) that wraps the existing `Retriever` / `PolicyChunkRepository` / `EmbeddingService` and maps `PolicyChunk` rows into canonical `EvidenceRefV1` (from 08-01).

The facade exposes ONLY the evidence contract: `search(request: KnowledgeSearchRequest, context: KnowledgeContext) -> KnowledgeSearchResult`. It preserves the existing strong/partial/no_evidence outcomes, propagates explicit `effective_at` (no adapter wall-clock), validates `filters.merchant_id` against `context.merchant_scope`, and implements deterministic tenant-scoped retrieval (tenant-over-global is DEFERRED, see traceability).

This plan does NOT switch the node (08-04), does NOT implement the citation membership validator body (08-03 provides `validate_membership`; the facade leaves `citation_validation` at its default until the node populates claims), and does NOT modify `src/rag/*`, `src/agent/*`, `src/db/*`.

Critical adapter rule (RESEARCH GAP-2): `text_hash` MUST be computed over the FULL `chunk.content`, not the retriever's truncated `text[:300]`. The adapter therefore maps from the chunk object / retrieval result that carries full content, not from the truncating `search_policy` tool payload.
</objective>

<traceability>
## Spec sections covered
- `docs/contract-spec.md` §8.3 — facade signature, request/result contracts, knowledge rules (effective_at explicit, tenant-scoped, merchant_scope validation, no_evidence handling).
- `.planning/phases/08-knowledge-facade/08-RESEARCH.md` §4 (GAP-1/2/3), §5, §6.

## Spec consistency findings
- Tenant-over-global "tenant wins over global" rule (§8.3) is NOT a Phase 8 blocking exit. `PolicyDocument.tenant_id` is NOT NULL and there is no global-policy scope in the MVP schema, so Phase 8 implements deterministic tenant-scoped retrieval only; tenant-over-global is DEFERRED_WITH_OWNER (owner: later policy-scope phase; dependency: schema+query migration adding global scope; acceptance gate: schema-and-query tenant-over-global tests). Surfaced in `service.py` docstring; reconciled into docs at 08-05.
- `retrieval_config_version` / `rerank_config_version` literals did not exist in code (RESEARCH GAP-3). This plan introduces `src/knowledge/config.py` with `RETRIEVAL_CONFIG_VERSION = "retrieval.v3"` and `RERANK_CONFIG_VERSION = "rerank.v2"` as the single source, referenced by request defaults and every EvidenceRefV1.

## Schema/migration owner
- N/A with reason: no schema/table/column/migration change. Reuses existing `policy_documents` / `policy_chunks` via the existing repository.

## Service/API owner
- KnowledgeService facade owner = Phase 8 (this plan). No API route changes; the FastAPI/router layer is untouched (node wiring is 08-04).

## State/router impact
- N/A with reason: no AgentState/graph/router change in this plan.

## Required tests
- `tests/knowledge/test_facade_status.py` — strong/partial/no_evidence preserved through the facade; EvidenceRefV1 fields populated; full-text hash (not truncated) used.
- `tests/knowledge/test_effective_time.py` — explicit `effective_at` filters deterministically; adapter does not call wall-clock for the effective cutoff.
- `tests/knowledge/test_tenant_scope.py` — retrieval restricted to `context.tenant_id`; `filters.merchant_id` outside `merchant_scope` is rejected/ignored (not trusted).

## Acceptance criteria
- Task-level criteria below, plus all three test files pass via `uv run pytest tests/knowledge -q`.

## Rollback/non-goals/deferred items
- Rollback: git revert this plan; legacy `search_policy` + `Retriever` remain fully functional and the node still calls them until 08-04. The adapter retains the legacy path for rollback (D-A3).
- Non-goals: node switch (08-04), citation validator body (08-03), doc sync (08-05), query-rewrite enrichment beyond current behavior (deferred unless eval requires).
- Deferred: tenant-over-global precedence (owner recorded above).
</traceability>

<threat_model>
ASVS L1; block on high.

| Threat | Impact | Mitigation in this plan | Blocking? |
| --- | --- | --- | --- |
| Untrusted `filters.merchant_id` used as retrieval filter | Cross-merchant policy leakage / scope bypass | Facade validates `filters.merchant_id ∈ context.merchant_scope` before use; unauthorized id is rejected or dropped, never trusted | high → yes |
| Cross-tenant retrieval | Tenant data leakage | Retrieval keyed strictly on `context.tenant_id`; adapter passes trusted tenant, never request-supplied tenant alone | high → yes |
| `effective_at` taken from adapter wall-clock | Non-deterministic/incorrect time-travel evidence; replay divergence | `effective_at` is required on `KnowledgeContext`; adapter uses it for the cutoff and never calls `datetime.now()` for the effective filter | high → yes |
| `text_hash` over truncated text | Unstable evidence identity (see 08-01) | Adapter hashes full `chunk.content` | high → yes |
| Facade leaks pgvector/repo internals to caller | Tight coupling, contract bypass | Facade returns only `KnowledgeSearchResult`; no chunk/repo objects exposed | medium |
</threat_model>

<must_haves>
- `src/knowledge/config.py` defines `RETRIEVAL_CONFIG_VERSION` and `RERANK_CONFIG_VERSION` string literals.
- `LegacyRagKnowledgeAdapter` wraps the existing `Retriever`/`PolicyChunkRepository`/`EmbeddingService` and maps each retrieved chunk to an `EvidenceRefV1` via `EvidenceRefV1.build(...)` with: `tenant_id` from context, `doc_key` from `chunk.document.doc_key`, `chunk_id` from `chunk.chunk_id`, `policy_version = f"v{chunk.document.version}"`, `text` = FULL `chunk.content`, `rank` = 1-based final order, `score` = retrieval score, `retrieval_config_version` from config, `retrieved_at` from context-provided or explicit timestamp.
- Adapter computes `text_hash` over full `chunk.content` (assert in test).
- `PolicyKnowledgeService.search(request, context)` returns a `KnowledgeSearchResult` with `status` preserving strong/partial/no_evidence semantics (reuse existing thresholds), `best_score`, `threshold`, populated `evidence_refs`, and config versions.
- Facade validates `filters.merchant_id` against `context.merchant_scope`; unauthorized merchant id is not used as a filter.
- Retrieval is tenant-scoped using `context.tenant_id`.
- `effective_at` comes from `context.effective_at` (explicit); the effective cutoff path contains no `datetime.now()`/wall-clock call.
- `service.py` docstring records the tenant-over-global DEFERRED finding.
- Legacy `search_policy` and `Retriever` remain unmodified and importable (rollback path).
- No file under `src/rag/`, `src/agent/`, `src/db/` modified.
</must_haves>

<tasks>

<task id="1" type="execute">
  <title>Add knowledge config version literals</title>
  <read_first>
  - `src/rag/retriever.py`
  - `.planning/phases/08-knowledge-facade/08-RESEARCH.md`
  </read_first>
  <action>
  Create `src/knowledge/config.py`:

  ```python
  from __future__ import annotations

  # Single source for knowledge retrieval/rerank config version literals (RESEARCH GAP-3).
  RETRIEVAL_CONFIG_VERSION = "retrieval.v3"
  RERANK_CONFIG_VERSION = "rerank.v2"

  # Thresholds mirror src/rag/retriever.py STRONG/MIN so facade status matches legacy behavior.
  STRONG_EVIDENCE_THRESHOLD = 0.70
  MIN_SIMILARITY_THRESHOLD = 0.55
  ```

  These mirror the existing `src/rag/retriever.py` constants (STRONG=0.70, MIN=0.55) so the facade reproduces identical status boundaries without importing private retriever constants.
  </action>
  <verify>
  `uv run python -c "from src.knowledge.config import RETRIEVAL_CONFIG_VERSION, RERANK_CONFIG_VERSION; print(RETRIEVAL_CONFIG_VERSION, RERANK_CONFIG_VERSION)"` prints `retrieval.v3 rerank.v2`.
  </verify>
  <acceptance_criteria>
  - `src/knowledge/config.py` contains `RETRIEVAL_CONFIG_VERSION = "retrieval.v3"`.
  - `src/knowledge/config.py` contains `RERANK_CONFIG_VERSION = "rerank.v2"`.
  - `src/knowledge/config.py` contains `STRONG_EVIDENCE_THRESHOLD = 0.70` and `MIN_SIMILARITY_THRESHOLD = 0.55`.
  </acceptance_criteria>
</task>

<task id="2" type="execute">
  <title>Implement LegacyRagKnowledgeAdapter (full-text hash, rank, tenant scope)</title>
  <read_first>
  - `src/knowledge/schemas.py`
  - `src/knowledge/text_hash.py`
  - `src/knowledge/config.py`
  - `src/rag/retriever.py`
  - `src/rag/schemas.py`
  - `src/repositories/policy_chunk_repo.py`
  - `src/db/models.py`
  - `src/agent/tools/search_policy.py`
  </read_first>
  <action>
  Create `src/knowledge/adapters.py` with `LegacyRagKnowledgeAdapter`. It owns the legacy retrieval path and maps chunks to canonical `EvidenceRefV1`.

  Key requirements:
  - Constructor takes an `AsyncSession` (or the existing `PolicyChunkRepository` + `EmbeddingService`), matching how `search_policy` builds `Retriever(chunk_repo=PolicyChunkRepository(session), embedder=EmbeddingService())`.
  - Expose `async def retrieve(self, *, query, context: KnowledgeContext, max_results, doc_type=None, risk_level=None) -> tuple[str, list[EvidenceRefV1], float]` returning `(retrieval_status, evidence_refs, best_score)`.
  - Call the existing `Retriever.search(query, tenant_id=UUID(context.tenant_id), top_k=max_results, doc_type, risk_level)`. Because the retriever's `EvidenceItem.text` is truncated to 300 chars, the adapter MUST obtain full chunk content for hashing. Two acceptable approaches — pick one and document it:
    1. Extend the legacy path minimally by having the adapter call `PolicyChunkRepository.search_similar(...)` + the retriever's public rerank to get `(PolicyChunk, score)` pairs with full `chunk.content`; OR
    2. Add a non-truncating internal method on the adapter that re-runs ranking and returns chunks.
    DO NOT modify `src/rag/*` to change the existing truncation (that file stays legacy). If full content is only reachable via the chunk object, the adapter calls the repo/embedder directly (the retriever's ranking helpers `_rerank_candidates` are module-level and reusable).
  - For each result at 1-based `rank`, build:
    ```python
    EvidenceRefV1.build(
        tenant_id=context.tenant_id,
        doc_key=chunk.document.doc_key,
        chunk_id=chunk.chunk_id,
        policy_version=f"v{chunk.document.version}",
        text=chunk.content,            # FULL content, not truncated
        retrieved_at=context.effective_at if ... else explicit_retrieved_at,
        retrieval_config_version=RETRIEVAL_CONFIG_VERSION,
        score=score,
        rank=rank,
    )
    ```
    Use a single explicit `retrieved_at` timestamp passed in or derived once at the call boundary (not per-ref wall-clock); record it deterministically.
  - Effective-time filter: filter candidate chunks by `chunk.effective_date <= date(context.effective_at)` (or document effective_date) using `context.effective_at` ONLY. No `datetime.now()` in the effective path.
  - Tenant scope: pass `UUID(context.tenant_id)`; never use a request-supplied tenant.
  - Preserve status mapping using `STRONG_EVIDENCE_THRESHOLD` / `MIN_SIMILARITY_THRESHOLD` identical to legacy.
  - Keep a thin passthrough to legacy `search_policy` available for rollback (e.g. `legacy_search_policy` re-export or a method) without changing its behavior.
  </action>
  <verify>
  `uv run python -c "from src.knowledge.adapters import LegacyRagKnowledgeAdapter; print('ok')"` prints `ok`. Adapter file contains `chunk.content` (full) passed to `EvidenceRefV1.build` and `f\"v{chunk.document.version}\"`.
  </verify>
  <acceptance_criteria>
  - `src/knowledge/adapters.py` contains `class LegacyRagKnowledgeAdapter`.
  - `src/knowledge/adapters.py` calls `EvidenceRefV1.build(` with `text=chunk.content` (full content, NOT `[:300]` / `item.text`).
  - `src/knowledge/adapters.py` derives `policy_version=f"v{chunk.document.version}"`.
  - `src/knowledge/adapters.py` passes a 1-based `rank` to each EvidenceRefV1.
  - `src/knowledge/adapters.py` effective-time path contains no `datetime.now(` call; it uses `context.effective_at`.
  - `src/rag/retriever.py` and `src/rag/schemas.py` are NOT modified (no diff).
  </acceptance_criteria>
</task>

<task id="3" type="execute">
  <title>Implement PolicyKnowledgeService facade</title>
  <read_first>
  - `src/knowledge/schemas.py`
  - `src/knowledge/adapters.py`
  - `src/knowledge/config.py`
  - `docs/contract-spec.md` (§8.3 facade signature + knowledge rules)
  </read_first>
  <action>
  Create `src/knowledge/service.py` with `class PolicyKnowledgeService`. Module docstring MUST record the tenant-over-global DEFERRED_WITH_OWNER finding (owner: later policy-scope phase; dependency: schema+query migration; acceptance gate: schema-and-query tenant-over-global tests).

  ```python
  class PolicyKnowledgeService:
      def __init__(self, adapter: LegacyRagKnowledgeAdapter): ...

      async def search(self, request: KnowledgeSearchRequest, context: KnowledgeContext) -> KnowledgeSearchResult:
          ...
  ```

  Facade responsibilities:
  - Validate `request.filters.merchant_id`: if not None and `context.merchant_scope` is set and the id is not in `context.merchant_scope`, do NOT use it as a filter (drop it / treat as unauthorized). Never trust a model/user-supplied unauthorized merchant id.
  - Tenant scope: always use `context.tenant_id` (request `filters.tenant_id` must equal context; mismatch is treated as the trusted context value, not request override).
  - Call `adapter.retrieve(query=request.query, context=context, max_results=request.max_results, ...)`.
  - Map `(retrieval_status, evidence_refs, best_score)` into `KnowledgeSearchResult` with `threshold=MIN_SIMILARITY_THRESHOLD` (or STRONG per status semantics — match legacy gating), `retrieval_config_version` / `rerank_config_version` from config.
  - Leave `citation_validation` at its default `CitationValidationResult()` (the node populates claims via 08-03 after recommendation; the facade does not own claim text).
  - On adapter exception/timeout, return `status="error"` with an `error` dict mirroring the legacy `{error_code, message, retryable}` shape; `evidence_refs=[]`, `best_score=0.0`.
  - Do not expose chunk/repo objects in the return value.
  </action>
  <verify>
  `uv run python -c "from src.knowledge.service import PolicyKnowledgeService; print('ok')"` prints `ok`. File docstring contains the tenant-over-global DEFERRED finding.
  </verify>
  <acceptance_criteria>
  - `src/knowledge/service.py` contains `class PolicyKnowledgeService` with `async def search(self, request` and the `KnowledgeSearchRequest`/`KnowledgeContext` parameter types.
  - `src/knowledge/service.py` validates `merchant_id` against `context.merchant_scope` (contains both `merchant_id` and `merchant_scope`).
  - `src/knowledge/service.py` returns `KnowledgeSearchResult` and sets `retrieval_config_version` and `rerank_config_version`.
  - `src/knowledge/service.py` docstring contains `tenant-over-global` and `DEFERRED`.
  - `src/knowledge/service.py` does not return chunk or repository objects.
  </acceptance_criteria>
</task>

<task id="4" type="execute">
  <title>Facade contract tests: status, effective-time, tenant scope</title>
  <read_first>
  - `src/knowledge/service.py`
  - `src/knowledge/adapters.py`
  - `tests/knowledge/conftest.py`
  - `tests/test_retriever.py`
  - `tests/agent/test_tools/test_tool_contracts.py`
  - `tests/conftest.py`
  </read_first>
  <action>
  Use the existing test patterns (mock/stub the retriever or repository the way `tests/test_retriever.py` / tool-contract tests do; reuse fixtures from `tests/conftest.py` where available). Stub the adapter's retrieval source so tests are deterministic and DB-free where the existing suite already mocks embeddings/repo.

  `tests/knowledge/test_facade_status.py`:
  - strong_evidence: best_score ≥ 0.70 → result.status == "strong_evidence", evidence_refs non-empty, each ref has `schema_version == "evidence_ref.v1"`, populated `evidence_id`, `text_hash` starting `sha256:`, `rank` 1-based, `retrieval_config_version == "retrieval.v3"`.
  - partial_evidence: 0.55 ≤ best_score < 0.70 → status "partial_evidence".
  - no_evidence: best_score < 0.55 or empty → status "no_evidence", evidence_refs == [].
  - Full-text hash: feed a chunk whose full content > 300 chars; assert the ref's `text_hash` equals `evidence_text_hash(full_content)` and NOT `evidence_text_hash(full_content[:300])`.

  `tests/knowledge/test_effective_time.py`:
  - Two policy chunks with different `effective_date`; with `context.effective_at` between them, only the in-effect chunk is returned.
  - Assert determinism: same inputs → identical evidence_refs across two calls (no wall-clock leakage into identity).

  `tests/knowledge/test_tenant_scope.py`:
  - Retrieval is limited to `context.tenant_id` (a chunk from another tenant is never returned).
  - `request.filters.merchant_id` set to a value NOT in `context.merchant_scope` is not applied as a filter (result identical to no merchant filter, or rejected); a value within scope is applied.
  </action>
  <verify>
  `uv run pytest tests/knowledge/test_facade_status.py tests/knowledge/test_effective_time.py tests/knowledge/test_tenant_scope.py -q` exits 0.
  </verify>
  <acceptance_criteria>
  - `tests/knowledge/test_facade_status.py` asserts the three statuses (`strong_evidence`, `partial_evidence`, `no_evidence`).
  - `tests/knowledge/test_facade_status.py` asserts a ref `text_hash` equals the FULL-content hash and not the 300-char truncated hash.
  - `tests/knowledge/test_effective_time.py` asserts effective_at filtering and cross-call determinism.
  - `tests/knowledge/test_tenant_scope.py` asserts cross-tenant exclusion and unauthorized merchant_id is not used as a filter.
  - `uv run pytest tests/knowledge -q` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run `uv run pytest tests/knowledge -q` (all pass). Run `git status --short` and confirm only `src/knowledge/` and `tests/knowledge/` are touched — `src/rag/`, `src/agent/`, `src/db/` unchanged.
</verification>

<success_criteria>
- `PolicyKnowledgeService.search(request, context)` returns `knowledge_search_result.v2` preserving strong/partial/no_evidence (KNOW-01).
- `LegacyRagKnowledgeAdapter` produces canonical `EvidenceRefV1` with full-text hash, 1-based rank, `v{version}` policy_version, config versions (KNOW-02).
- `effective_at` explicit and deterministic; `merchant_id` validated against `merchant_scope`; tenant-scoped retrieval enforced.
- Tenant-over-global recorded as DEFERRED_WITH_OWNER in `service.py`.
- Legacy retriever/tool unchanged and available for rollback.
</success_criteria>

## PLANNING COMPLETE
