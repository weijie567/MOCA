---
phase: 15-replay-event-contract
plan: 06
type: execute
wave: 6
depends_on:
  - 15-05
files_modified:
  - src/actions/service.py
  - src/repositories/trace_repo.py
  - tests/replay/test_replay_redaction_retention.py
  - tests/replay/test_replay_api.py
  - tests/agent/test_tools/test_create_coupon_grant_draft.py
  - .planning/phases/15-replay-event-contract/15-COVERAGE.md
autonomous: true
requirements:
  - REPLAY-01
  - REPLAY-02
  - REPLAY-03
must_haves:
  truths:
    - "Replay-facing demo draft wording cannot be mistaken for external execution."
    - "Replay responses omit raw prompt/tool/action payloads, full final responses, secrets, credentials, and PII-heavy keys."
    - "Phase 15 coverage records every requirement, deferred owner, command result, and rollback fallback."
    - "Focused replay suite, existing trace/event/approval/action regressions, Alembic upgrade, ruff, and full pytest gate are recorded."
  artifacts:
    - path: ".planning/phases/15-replay-event-contract/15-COVERAGE.md"
      provides: "Final Phase 15 coverage, deferred owner, and verification record"
      contains: "REPLAY-01"
    - path: "tests/replay/test_replay_redaction_retention.py"
      provides: "Replay payload safety and retention contract tests"
      contains: "raw_payload"
  key_links:
    - from: "tests/replay/test_replay_redaction_retention.py"
      to: "src/replay/service.py"
      via: "replay projection omits raw payloads and marks retention metadata"
      pattern: "raw_payload"
    - from: ".planning/phases/15-replay-event-contract/15-COVERAGE.md"
      to: ".planning/phases/15-replay-event-contract/15-VALIDATION.md"
      via: "coverage records final command status for validation map"
      pattern: "REPLAY-01|REPLAY-02|REPLAY-03"
---

<objective>
Close replay-facing boundary cleanup and final Phase 15 verification evidence.

Purpose: ensure the implemented replay surface is safe, scoped to Phase 15, and ready for GSD verification without hiding Phase 16/17 deferred work.
Output: Phase 14 wording cleanup where needed, final redaction/retention regressions, `15-COVERAGE.md`, and recorded verification commands.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-replay-event-contract/15-CONTEXT.md
@.planning/phases/15-replay-event-contract/15-RESEARCH.md
@.planning/phases/15-replay-event-contract/15-VALIDATION.md
@docs/phase-13-17-architecture-plan.md
@docs/contract-spec.md
@src/actions/service.py
@src/repositories/trace_repo.py
@src/replay/service.py
@tests/replay/test_replay_redaction_retention.py
@tests/agent/test_tools/test_create_coupon_grant_draft.py
</context>

<architecture_alignment>
## Architecture Alignment

Boundary cleanup:
- Phase 15 may adjust replay-facing labels/projection for demo draft events so `action_draft_created` cannot be read as real external execution.
- Do not broadly remove compatibility fields such as `execute_action` or `action_result` unless a replay-facing expression blocks V3 clarity.
- Do not register `action_execution_*`.
- Do not create external execution tables, outbox, reconciliation, or compensation.

Final coverage:
- `15-COVERAGE.md` must map REPLAY-01, REPLAY-02, REPLAY-03 to plan files, tests, commands, and implementation artifacts.
- Every deferred item must name an owner phase: Phase 16 for memory identity/tombstones/review; Phase 17 for external execution/outbox/reconciliation/compensation/external workers; post-Phase 15 SLA Scanner Enablement phase for active SLA scanner enablement if still disabled.
- Record `/trace` rollback fallback explicitly.
</architecture_alignment>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Plan Status |
| --- | --- | --- |
| REQ | REPLAY-01 final lifecycle/replay coverage | COVERED by Plans 15-01, 15-04, 15-05, and 15-COVERAGE. |
| REQ | REPLAY-02 allocator/pairing coverage | COVERED by Plans 15-02 and 15-03. |
| REQ | REPLAY-03 redaction/retention/access/read-switch/fallback | COVERED by Plans 15-01, 15-02, 15-05, and 15-06. |
| CONTEXT | D-11 Phase 14 compatibility cleanup | COVERED narrowly by replay-facing cleanup tests. |
| DEFERRED | Phase 16 and Phase 17 scope | COVERED by owner-phase rows in `15-COVERAGE.md`. |
</source_audit>

<threat_model>
| Threat | Severity | Mitigation in this plan | Verification |
| --- | --- | --- | --- |
| Demo draft replay implies external execution | high | Safe labels and tests assert `external_side_effect=false` and no `action_execution_*` | replay/action draft tests |
| Final response/raw payload leaks through replay response | high | Redaction/retention API tests include response-body assertions | `tests/replay/test_replay_redaction_retention.py` |
| Deferred Phase 16/17 work is marked complete by accident | high | Coverage rows require owner phase and acceptance gate | read `15-COVERAGE.md` |
| Phase exits without command evidence | high | Coverage records PASS/FAIL/NOT_RUN for all final commands | final gate task |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clean replay-facing demo draft wording and safety assertions</name>
  <files>src/actions/service.py, src/repositories/trace_repo.py, tests/replay/test_replay_redaction_retention.py, tests/replay/test_replay_api.py, tests/agent/test_tools/test_create_coupon_grant_draft.py</files>
  <read_first>
    - .planning/phases/15-replay-event-contract/15-CONTEXT.md decision D-11.
    - .planning/phases/14-demo-action-executor-boundary/14-07-SUMMARY.md.
    - src/actions/service.py.
    - src/repositories/trace_repo.py.
    - src/replay/service.py.
    - tests/agent/test_tools/test_create_coupon_grant_draft.py.
  </read_first>
  <action>
    Add tests proving replay projection for `action_draft_created` contains `execution_mode="demo"`, `external_side_effect=false`, `draft_outcome.status="not_executed_demo"`, and does not contain `action_execution_started`, `action_execution_completed`, `action_execution_failed`, `external_dispatched`, or raw `ActionDraft.payload`. If `src/actions/service.py` or `src/repositories/trace_repo.py` still exposes replay-facing wording that says `executed`, `execution succeeded`, or `action_result.status == "success"` for demo drafts, change only that replay-facing expression to `draft_created`, `not_executed_demo`, or `external_side_effect=false`. Do not delete compatibility fields that are only used by legacy tests unless the replay response exposes them unsafely.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay/test_replay_redaction_retention.py tests/replay/test_replay_api.py tests/agent/test_tools/test_create_coupon_grant_draft.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/actions/service.py src/repositories/trace_repo.py tests/replay tests/agent/test_tools/test_create_coupon_grant_draft.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "external_side_effect.*false|not_executed_demo|execution_mode.*demo|action_draft_created" tests/replay tests/agent/test_tools/test_create_coupon_grant_draft.py src/actions/service.py src/repositories/trace_repo.py` returns matches.
    - `rg -n "action_execution_started|action_execution_completed|action_execution_failed|external_dispatched|external_side_effect.*true" src/replay src/actions src/repositories tests/replay` returns no Phase 15 production additions except negative test assertions.
    - Replay redaction tests assert raw `ActionDraft.payload` is not present in `/replay` JSON.
  </acceptance_criteria>
  <done>Replay-facing demo draft output is draft-only, contains safe outcome fields, and has tests proving no external execution event or raw action payload leaks.</done>
</task>

<task type="auto">
  <name>Task 2: Create Phase 15 coverage and deferred-owner record</name>
  <files>.planning/phases/15-replay-event-contract/15-COVERAGE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md.
    - .planning/ROADMAP.md Phase 15.
    - .planning/phases/15-replay-event-contract/15-CONTEXT.md.
    - .planning/phases/15-replay-event-contract/15-VALIDATION.md.
    - .planning/phases/15-replay-event-contract/15-01-PLAN.md.
    - .planning/phases/15-replay-event-contract/15-02-PLAN.md.
    - .planning/phases/15-replay-event-contract/15-03-PLAN.md.
    - .planning/phases/15-replay-event-contract/15-04-PLAN.md.
    - .planning/phases/15-replay-event-contract/15-05-PLAN.md.
  </read_first>
  <action>
    Create `.planning/phases/15-replay-event-contract/15-COVERAGE.md` with tables for requirement coverage, source-decision coverage, deferred owners, compatibility disposition, and verification command status. Requirement rows must include REPLAY-01, REPLAY-02, and REPLAY-03 with owning plan ids and test commands. Deferred rows must include: active SLA scanner enablement owned by the named post-Phase 15 SLA Scanner Enablement phase, Phase 16 memory identity/tombstones/review workflow, and Phase 17 external execution/outbox/reconciliation/compensation/`action_execution_*`/external worker allocator tests. Add a rollback/fallback row stating `/trace` remains legacy rollback fallback and `TraceRepository.build_timeline()` remains available. Use statuses `COVERED`, `PARTIAL`, `DEFERRED_WITH_OWNER`, `PASS`, `FAIL`, and `NOT_RUN`; do not use bare `TODO`.
  </action>
  <verify>
    <automated>rg -n "REPLAY-01|REPLAY-02|REPLAY-03|DEFERRED_WITH_OWNER|Phase 16|Phase 17|post-Phase 15 SLA Scanner Enablement|/trace|TraceRepository.build_timeline|PASS|FAIL|NOT_RUN" .planning/phases/15-replay-event-contract/15-COVERAGE.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "REPLAY-01|REPLAY-02|REPLAY-03" .planning/phases/15-replay-event-contract/15-COVERAGE.md` returns matches.
    - `rg -n "Phase 16|Phase 17|post-Phase 15 SLA Scanner Enablement|DEFERRED_WITH_OWNER" .planning/phases/15-replay-event-contract/15-COVERAGE.md` returns matches.
    - `rg -n "TraceRepository.build_timeline|/trace.*fallback|rollback fallback" .planning/phases/15-replay-event-contract/15-COVERAGE.md` returns matches.
    - `rg -n "TODO|later|future phase" .planning/phases/15-replay-event-contract/15-COVERAGE.md` returns no matches unless the same row names an explicit owner phase.
  </acceptance_criteria>
  <done>`15-COVERAGE.md` maps all Phase 15 requirements, source decisions, deferred owner phases, `/trace` fallback, and final verification commands.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 15 verification gate and record results</name>
  <files>.planning/phases/15-replay-event-contract/15-COVERAGE.md</files>
  <read_first>
    - .planning/phases/15-replay-event-contract/15-COVERAGE.md.
    - .planning/phases/15-replay-event-contract/15-VALIDATION.md.
    - pyproject.toml.
    - alembic.ini.
  </read_first>
  <action>
    Run and record the result of each command in `15-COVERAGE.md` as `PASS`, `FAIL`, or `NOT_RUN` with the exact command:
    - `UV_CACHE_DIR=/tmp/uv-cache uv run alembic upgrade head`
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay tests/agent/test_events.py tests/test_trace_api.py tests/approvals/test_events.py tests/approvals/test_needs_info_resume.py tests/approvals/test_sla_scanner.py tests/agent/test_tools/test_create_coupon_grant_draft.py -q --tb=short`
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q --tb=short`
    - `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests`
    If any command fails because the database is unavailable, record the environment blocker and exact failing command. Do not mark Phase 15 ready when a blocking command is `FAIL` or `NOT_RUN`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay tests/agent/test_events.py tests/test_trace_api.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `15-COVERAGE.md` records all four final commands exactly.
    - Every final command row has status `PASS`, `FAIL`, or `NOT_RUN`.
    - If any final command is `FAIL` or `NOT_RUN`, `15-COVERAGE.md` contains `Blocking follow-up`, an owner, and the exact command.
    - Focused replay/event/trace command exits 0 before Phase 15 is marked ready.
  </acceptance_criteria>
  <done>Final Phase 15 verification commands are recorded as PASS/FAIL/NOT_RUN with blocking follow-ups for any non-passing gate.</done>
</task>

</tasks>

<verification>
1. `UV_CACHE_DIR=/tmp/uv-cache uv run alembic upgrade head`
2. `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay tests/agent/test_events.py tests/test_trace_api.py tests/approvals/test_events.py tests/approvals/test_needs_info_resume.py tests/approvals/test_sla_scanner.py tests/agent/test_tools/test_create_coupon_grant_draft.py -q --tb=short`
3. `UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q --tb=short`
4. `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests`
</verification>

<success_criteria>
- Replay-facing demo draft output remains draft-only and cannot imply external execution.
- Redaction/retention regressions prove replay responses are safe.
- `15-COVERAGE.md` maps all REPLAY requirements and deferred owners.
- Final verification command statuses are recorded.
</success_criteria>
