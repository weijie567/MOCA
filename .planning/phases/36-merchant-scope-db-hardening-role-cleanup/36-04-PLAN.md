---
phase: 36-merchant-scope-db-hardening-role-cleanup
plan: 36-04
type: execute
wave: 3
depends_on:
  - 36-03
files_modified:
  - src/db/models.py
  - src/approvals/service.py
  - src/approvals/snapshots.py
  - src/approvals/snapshot_service.py
  - src/actions/service.py
  - tests/approvals/test_phase36_scope_consistency.py
  - tests/actions/test_phase34_action_draft_bindings.py
autonomous: true
requirements:
  - MSH-05
  - MSH-07
user_setup: []
must_haves:
  truths:
    - "ApprovalRequest target merchant cannot contradict its linked business-scoped AgentRun."
    - "ActionDraft target merchant cannot contradict its linked approval or business-scoped AgentRun."
    - "ActionSafetySnapshot carries target merchant binding or immutable scope proof for business-scoped action paths."
    - "Raw snapshot JSON is not used as standalone authorization proof."
  artifacts:
    - path: "src/approvals/snapshots.py"
      provides: "Snapshot contract includes target merchant binding material in immutable hash projection"
      contains: "target_merchant_id"
    - path: "src/db/models.py"
      provides: "ActionSafetySnapshot target merchant columns"
      contains: "ix_action_safety_snapshots_tenant_target_merchant"
    - path: "src/actions/service.py"
      provides: "Run/approval/action/snapshot consistency validation"
      contains: "RUN_SCOPE_BINDING_MISMATCH"
    - path: "src/approvals/service.py"
      provides: "ApprovalRequest creation and revision gates against linked AgentRun scope"
      contains: "approval_scope_mismatch"
    - path: "tests/approvals/test_phase36_scope_consistency.py"
      provides: "Cross-table contradiction tests"
      contains: "business_merchant"
  key_links:
    - from: "src/actions/service.py"
      to: "src/db/models.py"
      via: "loads AgentRun and ActionSafetySnapshot rows before draft creation"
      pattern: "AgentRun"
    - from: "src/approvals/service.py"
      to: "src/db/models.py"
      via: "loads AgentRun before ApprovalRequest insert or revision material rewrite"
      pattern: "AgentRun"
    - from: "src/approvals/snapshot_service.py"
      to: "src/approvals/snapshots.py"
      via: "persists canonical snapshot with target binding"
      pattern: "target_merchant_id"
---

<objective>
Harden approval, action draft, and action safety snapshot target merchant consistency against AgentRun scope.

Purpose: implement MSH-05 while preserving Phase 34 approval/action semantics and MSH-07 runtime boundaries. This plan does not redesign approval or action draft flows; it adds consistency checks and snapshot scope proof per D-16 and D-17.
Output: snapshot target scope fields, canonical consistency helpers, and focused contradiction tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-SPEC.md
@.planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-CONTEXT.md
@.planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-RESEARCH.md
@.planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-VALIDATION.md
@.planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-PATTERNS.md
@docs/contract-spec.md
@src/agent/run_scope.py
@src/db/models.py
@src/approvals/schemas.py
@src/approvals/service.py
@src/approvals/repository.py
@src/approvals/snapshots.py
@src/approvals/snapshot_service.py
@src/actions/service.py
@tests/actions/test_phase34_action_draft_bindings.py

<interfaces>
Existing target merchant binding:
```python
class TargetMerchantBindingV1(BaseModel):
    schema_version: Literal["target_merchant_binding.v1"]
    target_merchant_id: str
    source: Literal["business_fact_ref", "business_fact_result"]
    business_fact_ref: dict[str, Any]
```

Existing draft binding comparison:
```python
def _approval_phase34_binding_matches(approval: ApprovalRequest, requested: dict[str, Any]) -> bool: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind ActionSafetySnapshot to target merchant proof</name>
  <files>src/db/models.py, src/approvals/snapshots.py, src/approvals/snapshot_service.py, tests/approvals/test_phase36_scope_consistency.py</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-SPEC.md
    - .planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-CONTEXT.md
    - .planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-PATTERNS.md
    - docs/contract-spec.md sections 18.1 and 18.2
    - src/db/models.py
    - src/approvals/schemas.py
    - src/approvals/snapshots.py
    - src/approvals/snapshot_service.py
  </read_first>
  <behavior>
    - Test `ActionSafetySnapshot` model accepts `target_merchant_id`, `target_merchant_ref`, and `business_fact_refs`.
    - Test immutable hash projection includes target merchant binding fields for business-scoped snapshots.
    - Test persisted snapshot row stores target merchant fields and reload verifies the same values.
    - Test missing target merchant binding for action-bound snapshots fails before persistence.
  </behavior>
  <action>
    Implement D-17.

    Extend `src/approvals/snapshots.py::ActionSafetySnapshot` with:
    - `target_merchant_id: str | None = None`
    - `target_merchant_ref: TargetMerchantBindingV1 | None = None`
    - `business_fact_refs: list[BusinessFactRefV1] = Field(default_factory=list)`

    Update `snapshot_hash_projection(...)` so `target_merchant_id`, canonical `target_merchant_ref`, and canonical `business_fact_refs` are included in immutable hash material whenever `action_payload_hash is not None`. For no-action/read-only snapshots, allow null target fields but preserve the fields as explicit null/empty values in the projection.

    Update `build_action_safety_snapshot(...)` to accept `target_merchant_id`, `target_merchant_ref`, and `business_fact_refs`. If `action_payload_hash` is non-null and `target_merchant_id` or `target_merchant_ref` is missing, raise `ValueError("action-bound snapshot requires target merchant binding")`.

    Update `src/db/models.py::ActionSafetySnapshot` with nullable columns `target_merchant_id: String(128)`, `target_merchant_ref: JSONB`, and `business_fact_refs: JSONB`. Add `Index("ix_action_safety_snapshots_tenant_target_merchant", ActionSafetySnapshot.tenant_id, ActionSafetySnapshot.target_merchant_id)`. Plan 36-05 owns the Alembic migration.

    Update `src/approvals/snapshot_service.py` to pass and persist these fields and to verify reloaded row fields match the built snapshot. Do not treat raw `snapshot_json` as authorization proof without the typed fields and immutable hash checks.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/approvals/test_phase36_scope_consistency.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/db/models.py src/approvals/snapshots.py src/approvals/snapshot_service.py tests/approvals/test_phase36_scope_consistency.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "target_merchant_id|target_merchant_ref|business_fact_refs" src/approvals/snapshots.py src/approvals/snapshot_service.py src/db/models.py tests/approvals/test_phase36_scope_consistency.py` returns matches.
    - `rg -n "ix_action_safety_snapshots_tenant_target_merchant" src/db/models.py tests/approvals/test_phase36_scope_consistency.py` returns matches.
    - `rg -n "snapshot_json.*authorization|authorization.*snapshot_json" src/approvals src/actions` returns no production shortcut matches.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/approvals/test_phase36_scope_consistency.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>ActionSafetySnapshot carries typed target merchant proof and immutable hash material for business-scoped action paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject run/approval/action/snapshot target contradictions</name>
  <files>src/approvals/service.py, src/actions/service.py, tests/approvals/test_phase36_scope_consistency.py, tests/actions/test_phase34_action_draft_bindings.py</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-SPEC.md
    - .planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-CONTEXT.md
    - src/agent/run_scope.py
    - src/db/models.py
    - src/approvals/service.py
    - src/approvals/repository.py
    - src/actions/service.py
    - tests/actions/test_phase34_action_draft_bindings.py
    - tests/approvals/test_phase36_scope_consistency.py
  </read_first>
  <behavior>
    - Test business-scoped AgentRun target merchant mismatch rejects approval/action draft creation with safe error `RUN_SCOPE_BINDING_MISMATCH`.
    - Test `ApprovalService.create_request` rejects missing or mismatched approval target binding for a linked `business_merchant` AgentRun before `ApprovalRequest` is flushed.
    - Test approval replacement/revision paths that create or rewrite approval binding material reject missing or mismatched target binding for a linked `business_merchant` AgentRun.
    - Test approval/action/snapshot target mismatch rejects draft creation with `SNAPSHOT_BINDING_MISMATCH` or existing safe binding mismatch code.
    - Test policy-only, merchant-not-required, and unknown-legacy runs cannot be treated as merchant business runs.
    - Test exact matching run/approval/action/snapshot bindings still allow existing Phase 34 draft creation behavior.
  </behavior>
  <action>
    Implement D-16 and D-17 by extending, not replacing, Phase 34 validation.

    In `src/approvals/service.py`, protect approval creation before the repository flushes an `ApprovalRequest`:
    - Import/load `AgentRun` by `(tenant_id, run_id)` inside the existing nested transaction in `ApprovalService.create_request` before calling `repository.create_request_with_single_level`.
    - Add a small service helper such as `_assert_agent_run_scope_matches_approval(command_or_fields, run)` that raises `ApprovalTransitionError("approval_scope_mismatch", "Approval target merchant does not match run scope")` when `run.scope_classification == "business_merchant"` and `command.target_merchant_id` or canonical `command.target_merchant_ref` is missing, malformed, or mismatched with `run.target_merchant_id` / `run.target_merchant_ref`.
    - Raise the same safe error when an approval command includes a business target but the linked run is `policy_only`, `merchant_not_required`, `unknown_legacy`, missing, or tenant-mismatched. Do not insert an `ApprovalRequest`, `ApprovalLevel`, `ApprovalAssignment`, or approval event on failure.
    - Apply the same gate to replacement/revision paths in `ApprovalService` that create new safety snapshots or rewrite approval binding material, including `_supersede_from_info` and edit/info paths if they can change `target_merchant_id`, `target_merchant_ref`, `business_fact_refs`, or action payload binding. Keep `ApprovalRepository.create_request_with_single_level` as low-level persistence; do not move business validation into the repository unless existing repository patterns require it.

    Add tests in `tests/approvals/test_phase36_scope_consistency.py` or existing approval service tests proving:
    - Mismatched approval target for a `business_merchant` AgentRun raises `ApprovalTransitionError` with a stable safe code and no `ApprovalRequest` persists.
    - Missing approval target id/ref for a `business_merchant` AgentRun raises the same class of safe error and no `ApprovalRequest` persists.
    - Non-business/unknown runs cannot accept business-targeted approval roots.
    - Exact matching run/approval target binding still allows the existing Phase 34 approval request behavior.

    In `src/actions/service.py`, add a helper `_run_scope_matches_target(run: AgentRun | None, requested_target_merchant_id: str | None) -> tuple[bool, str | None]`.
    It must return:
    - `(True, None)` when `run.scope_classification == "business_merchant"` and `run.target_merchant_id == requested_target_merchant_id`.
    - `(False, "RUN_SCOPE_BINDING_MISMATCH")` when `run.scope_classification == "business_merchant"` and target ids differ or requested target is missing.
    - `(False, "RUN_SCOPE_NOT_BUSINESS_MERCHANT")` when an approval/action request includes a business target but run classification is `policy_only`, `merchant_not_required`, or `unknown_legacy`.
    - `(False, "RUN_SCOPE_MISSING")` when the run row cannot be loaded for a business-targeted draft.

    In `_validate_action_binding(...)`, after loading `ActionSafetySnapshot` and before creating/reusing an `ActionDraft`, load `AgentRun` by the same `tenant_id` and `run_id` and call `_run_scope_matches_target`. On false, return `_tool_error(code, "Action target merchant does not match run scope", retryable=False)` and do not insert a draft.

    Extend snapshot validation so `snapshot.target_merchant_id`, `snapshot.target_merchant_ref`, and `snapshot.business_fact_refs` match the requested canonical binding when `action_payload_hash` is non-null. Use existing `_canonical_target_merchant_ref(...)` and `_canonical_business_fact_refs(...)`; do not compare raw dict order.

    Keep existing `_approval_phase34_binding_matches(...)` behavior and add run/snapshot checks around it. Do not change approval manager API access semantics and do not widen run/trace/replay visibility.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/approvals/test_phase36_scope_consistency.py tests/actions/test_phase34_action_draft_bindings.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/approvals/service.py src/actions/service.py tests/approvals/test_phase36_scope_consistency.py tests/actions/test_phase34_action_draft_bindings.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "RUN_SCOPE_BINDING_MISMATCH|RUN_SCOPE_NOT_BUSINESS_MERCHANT|_run_scope_matches_target|scope_classification" src/actions/service.py tests/approvals/test_phase36_scope_consistency.py` returns matches.
    - `rg -n "approval_scope_mismatch|_assert_agent_run_scope_matches_approval|AgentRun|scope_classification" src/approvals/service.py tests/approvals/test_phase36_scope_consistency.py` returns matches.
    - `rg -n "no.*ApprovalRequest.*persist|assert_no.*approval|ApprovalRequest.*not.*persist" tests/approvals/test_phase36_scope_consistency.py` returns matches.
    - `rg -n "_canonical_target_merchant_ref|_canonical_business_fact_refs" src/actions/service.py` returns matches in snapshot/approval comparison code.
    - `rg -n "requested_by.*merchant|merchant_id.*requested_by|snapshot_json.*target" src/actions/service.py src/approvals/service.py` returns no shortcut matches.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/approvals/test_phase36_scope_consistency.py tests/actions/test_phase34_action_draft_bindings.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>Approval/action/snapshot target facts cannot contradict a linked business-scoped AgentRun, and non-business/unknown runs remain fail-closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AgentRun scope -> action draft validation | Durable run-scope facts constrain business-scoped approval/action roots. |
| snapshot contract -> persistence | Typed snapshot scope proof is persisted and hash-bound. |
| approval/action request -> draft persistence | Requested binding material crosses into durable draft creation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T36-05 | Tampering / Repudiation | `src/approvals/service.py`, `src/actions/service.py` | mitigate | Validate exact run, approval, action, and snapshot target merchant consistency before any approval request or draft insert/reuse. |
| T36-03 | Elevation of Privilege | snapshot scope proof | mitigate | Use typed target binding/hash material, not raw snapshot JSON or weak sources, as consistency proof. |
| T36-04 | Elevation of Privilege | approval/action side effects | mitigate | Consistency hardening does not modify run/status/evidence/trace/replay visibility guards. |
| T36-06 | Information Disclosure | non-business or unknown runs | mitigate | `policy_only`, `merchant_not_required`, and `unknown_legacy` runs reject business-targeted approval/action roots. |
</threat_model>

<verification>
Run focused verification:
`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/approvals/test_phase36_scope_consistency.py tests/actions/test_phase34_action_draft_bindings.py -q --tb=short`

Run lint:
`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/db/models.py src/approvals/service.py src/approvals/snapshots.py src/approvals/snapshot_service.py src/actions/service.py tests/approvals/test_phase36_scope_consistency.py tests/actions/test_phase34_action_draft_bindings.py`
</verification>

<success_criteria>
- ActionSafetySnapshot has hash-bound target merchant proof.
- ApprovalService rejects missing, malformed, or mismatched business-scoped ApprovalRequest target binding before persistence.
- Approval/action/snapshot target merchant fields match linked business-scoped AgentRun target merchant.
- Non-business and unknown legacy runs cannot be converted into business-scoped approval/action roots.
- Existing Phase 34 exact binding behavior still passes.
</success_criteria>

<output>
After completion, create `.planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-04-SUMMARY.md`.
</output>
