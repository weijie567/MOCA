---
phase: 36-merchant-scope-db-hardening-role-cleanup
plan: 36-01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/contract-spec.md
  - scripts/seed_demo.py
  - src/platform/trusted_context.py
  - src/auth/permissions.py
  - tests/platform/test_merchant_scope.py
  - tests/tools/test_merchant_scope_static.py
autonomous: true
requirements:
  - MSH-01
  - MSH-02
  - MSH-07
user_setup: []
must_haves:
  truths:
    - "Legacy merchant remains enabled only as deprecated support-equivalent merchant-bound compatibility."
    - "Active support, manager, and legacy merchant users without merchant binding receive deny-all business access."
    - "Admin remains the only human role that receives wildcard business merchant scope."
    - "Manager is never treated as tenant-wide supervisor in role or scope tests."
  artifacts:
    - path: "src/platform/trusted_context.py"
      provides: "Canonical role scope constants and active business-user binding helper"
      contains: "DEPRECATED_COMPATIBILITY_ROLES"
    - path: "src/auth/permissions.py"
      provides: "Route helper imports the canonical merchant-bound/admin role constants"
      contains: "MERCHANT_BOUND_ROLES"
    - path: "tests/platform/test_merchant_scope.py"
      provides: "Role matrix and missing-binding fail-closed tests"
      contains: "merchant"
    - path: "docs/contract-spec.md"
      provides: "Normative legacy merchant deprecation and active binding text"
      contains: "legacy"
  key_links:
    - from: "src/auth/permissions.py"
      to: "src/platform/trusted_context.py"
      via: "shared constants import"
      pattern: "from src.platform.trusted_context import .*MERCHANT_BOUND_ROLES"
    - from: "tests/platform/test_merchant_scope.py"
      to: "src/auth/permissions.py"
      via: "require_merchant_access denies missing merchant binding"
      pattern: "require_merchant_access"
---

<objective>
Harden the role registry/runtime role semantics before schema and migration work depends on them.

Purpose: implement MSH-01, the runtime side of MSH-02, and the no-regression role surface of MSH-07. This plan preserves `users.role` as the compatibility/runtime source per D-06 and does not introduce a `roles` / `user_roles` authority model.
Output: canonical role constants, deprecated legacy `merchant` documentation/seed treatment, and focused role-scope tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-SPEC.md
@.planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-CONTEXT.md
@.planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-RESEARCH.md
@.planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-VALIDATION.md
@.planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-PATTERNS.md
@docs/contract-spec.md
@src/platform/trusted_context.py
@src/auth/permissions.py
@scripts/seed_demo.py
@tests/platform/test_merchant_scope.py
@tests/tools/test_merchant_scope_static.py

<interfaces>
Existing role constants:
```python
MERCHANT_BOUND_ROLES = {"support", "manager", "merchant"}
PLATFORM_ADMIN_ROLES = {"admin"}
```

Existing deny-first route helper:
```python
def require_merchant_access(user: User, merchant_id: object, *, resource_name: str = "resource") -> None:
    role = str(user.role)
    if role in PLATFORM_ADMIN_ROLES:
        return
    if role not in MERCHANT_BOUND_ROLES:
        _raise_merchant_access_forbidden(resource_name)
    user_merchant_id = getattr(user, "merchant_id", None)
    if user_merchant_id is None or str(user_merchant_id) != str(merchant_id):
        _raise_merchant_access_forbidden(resource_name)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Centralize role semantics and deprecate legacy merchant</name>
  <files>docs/contract-spec.md, scripts/seed_demo.py, src/platform/trusted_context.py, src/auth/permissions.py, tests/platform/test_merchant_scope.py</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-SPEC.md
    - .planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-CONTEXT.md
    - .planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-PATTERNS.md
    - docs/contract-spec.md section 8.0.1
    - src/platform/trusted_context.py
    - src/auth/permissions.py
    - scripts/seed_demo.py
    - tests/platform/test_merchant_scope.py
  </read_first>
  <behavior>
    - Test `support`, `manager`, and legacy `merchant` are merchant-bound and never wildcard.
    - Test `admin` is the only human role deriving `merchant_ids=["*"]`.
    - Test unknown roles are deny-all for business data.
    - Test new seed role descriptions mark `merchant` as deprecated compatibility and prefer `support`, `manager`, and `admin` for non-legacy examples.
  </behavior>
  <action>
    Implement D-06 and D-07 without creating a new role authority model.

    In `src/platform/trusted_context.py`, keep `MERCHANT_BOUND_ROLES = {"support", "manager", "merchant"}` and `PLATFORM_ADMIN_ROLES = {"admin"}` as the runtime constants. Add `DEPRECATED_COMPATIBILITY_ROLES = {"merchant"}` and `ROLE_SCOPE_POLICY = {"support": "merchant_bound", "manager": "merchant_bound", "merchant": "deprecated_merchant_bound_compatibility", "admin": "platform_admin"}`. Add `def is_deprecated_compatibility_role(role: str) -> bool` returning true only for `"merchant"`.

    In `src/auth/permissions.py`, import `MERCHANT_BOUND_ROLES` and `PLATFORM_ADMIN_ROLES` from `src.platform.trusted_context` instead of maintaining a second literal copy. Keep `require_merchant_access(...)` behavior unchanged: admin returns, merchant-bound roles require exact same merchant id, missing merchant id denies, and unknown roles deny.

    Update `docs/contract-spec.md` section 8.0.1 to state the concrete Phase 36 implementation target: `merchant` stays a deprecated compatibility role, is support-equivalent for merchant-bound business data, must not be used as a recommended new role in seeds/examples, and never grants platform-wide or tenant-wide business scope.

    Update `scripts/seed_demo.py` role/user descriptions so `merchant` is described as deprecated compatibility. Keep one bound legacy merchant demo user only for compatibility regression; do not physically delete the role or migrate to `roles` / `user_roles` as authority.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/platform/test_merchant_scope.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/platform/trusted_context.py src/auth/permissions.py tests/platform/test_merchant_scope.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "DEPRECATED_COMPATIBILITY_ROLES|ROLE_SCOPE_POLICY|is_deprecated_compatibility_role" src/platform/trusted_context.py tests/platform/test_merchant_scope.py` returns matches.
    - `rg -n "MERCHANT_BOUND_ROLES =|PLATFORM_ADMIN_ROLES =" src/auth/permissions.py` returns no local constant assignments.
    - `rg -n "deprecated compatibility|support-equivalent|not a recommended new role" docs/contract-spec.md scripts/seed_demo.py` returns matches.
    - `rg -n "roles/|user_roles|UserRole" src/platform/trusted_context.py src/auth/permissions.py` returns no new role-authority implementation matches.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/platform/test_merchant_scope.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>Role constants have one runtime source, legacy `merchant` is documented/tested as deprecated compatibility, and no new role authority model is introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin active business-user binding fail-closed behavior</name>
  <files>src/platform/trusted_context.py, src/auth/permissions.py, tests/platform/test_merchant_scope.py, tests/tools/test_merchant_scope_static.py</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-SPEC.md
    - .planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-CONTEXT.md
    - docs/contract-spec.md section 8.0.1
    - src/platform/trusted_context.py
    - src/auth/permissions.py
    - tests/platform/test_merchant_scope.py
    - tests/tools/test_merchant_scope_static.py
  </read_first>
  <behavior>
    - Test active `support`, `manager`, and legacy `merchant` users without `merchant_id` produce `MerchantScopeV1(merchant_ids=[])`.
    - Test `require_merchant_access` returns 403 for active business users with missing or wrong merchant binding.
    - Test `server_merchant_scope={"merchant_ids":["*"]}` still raises for non-admin users.
    - Static test proves no production wildcard merchant scope is constructed outside `TrustedContextFactory` admin semantics.
  </behavior>
  <action>
    Implement D-08 and D-19 at the runtime boundary.

    Add `def requires_business_merchant_binding(role: str, *, is_active: bool = True) -> bool` in `src/platform/trusted_context.py`. It must return true only when `is_active is True` and `role in MERCHANT_BOUND_ROLES`. Use this helper inside `_base_merchant_scope_from_user(...)` only to make the active binding invariant explicit; do not change the deny-all result for missing merchant ids.

    Keep `MerchantScopeV1(merchant_ids=[])` as the fail-closed result for active merchant-bound users without a binding. Do not raise from `TrustedContextFactory` because current runtime behavior relies on deny-all projection; migration preflight in Plan 36-05 will block unsafe historical DB rows before hard constraints.

    Extend `tests/platform/test_merchant_scope.py` to cover active `support`, `manager`, and `merchant` users with `merchant_id=None`, inactive compatibility rows, non-admin wildcard override rejection, unknown-role deny-all, and manager not acting as tenant-wide supervisor.

    Extend `tests/tools/test_merchant_scope_static.py` or add Phase 36 cases to it so wildcard construction remains allowed only in `src/platform/trusted_context.py` admin derivation and no production source constructs business wildcard scope in graph nodes, memory, RAG, raw tool payload handling, approval resume, or action draft paths.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/platform/test_merchant_scope.py tests/tools/test_merchant_scope_static.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/platform/trusted_context.py src/auth/permissions.py tests/platform/test_merchant_scope.py tests/tools/test_merchant_scope_static.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "requires_business_merchant_binding|merchant_id=None|server_merchant_scope" src/platform/trusted_context.py tests/platform/test_merchant_scope.py` returns matches.
    - `rg -n "manager.*tenant-wide|tenant-wide.*manager|wildcard" tests/platform/test_merchant_scope.py tests/tools/test_merchant_scope_static.py` returns matches.
    - `rg -n "merchant_ids.*\\[\"\\*\"\\]" src | rg -v "src/platform/trusted_context.py"` returns no production wildcard construction matches.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/platform/test_merchant_scope.py tests/tools/test_merchant_scope_static.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>Active merchant-bound users without merchant binding fail closed at runtime, and wildcard business scope remains admin-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user row -> merchant scope | Database user facts become runtime business scope through `TrustedContextFactory`. |
| server override -> merchant scope | Server-supplied narrowing input can restrict but must not widen non-admin actors. |
| role registry -> API helper | Shared role constants determine business-data route authorization behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T36-01 | Elevation of Privilege | `TrustedContextFactory`, `require_merchant_access` | mitigate | Active merchant-bound roles with missing binding project deny-all and receive 403 in route helper tests. |
| T36-02 | Spoofing | role constants | mitigate | Use one canonical runtime role matrix; `merchant` is deprecated compatibility and unknown roles deny all. |
| T36-04 | Elevation of Privilege | run/trace/replay visibility | mitigate | Plan 36-01 does not modify run/trace/replay guards; tests assert role constants do not make manager tenant-wide. |
| T36-06 | Information Disclosure | policy-only / unknown legacy runs | transfer | Run classification is owned by Plan 36-03 and final no-widening verification by Plan 36-06. This plan preserves deny-first role scope only. |
</threat_model>

<verification>
Run focused verification:
`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/platform/test_merchant_scope.py tests/tools/test_merchant_scope_static.py -q --tb=short`

Run lint:
`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/platform/trusted_context.py src/auth/permissions.py tests/platform/test_merchant_scope.py tests/tools/test_merchant_scope_static.py`
</verification>

<success_criteria>
- Legacy `merchant` remains present, deprecated, support-equivalent, and non-wildcard.
- Active merchant-bound users without merchant binding receive deny-all business scope and route-level 403.
- Admin remains the only human wildcard business role.
- No new role authority table/model is introduced by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-01-SUMMARY.md`.
</output>
