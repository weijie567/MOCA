---
phase: 36-merchant-scope-db-hardening-role-cleanup
plan: 36-03
type: execute
wave: 2
depends_on:
  - 36-02
files_modified:
  - src/db/models.py
  - src/agent/run_scope.py
  - src/agent/state.py
  - src/agent/trace.py
  - src/api/schemas/agent_runs.py
  - src/api/routers/agent_runs.py
  - tests/agent/test_phase36_run_scope.py
  - tests/test_agent_runs_api.py
autonomous: true
requirements:
  - MSH-04
  - MSH-07
user_setup: []
must_haves:
  truths:
    - "AgentRun rows can store exactly one trusted target merchant for business-scoped runs."
    - "AgentRun scope classification uses only `business_merchant`, `policy_only`, `merchant_not_required`, or `unknown_legacy`."
    - "Target merchant projection/readiness metadata never overrides missing or contradictory AgentRun scope."
    - "AgentRun status/evidence/stream visibility remains owner/admin-only."
  artifacts:
    - path: "src/agent/run_scope.py"
      provides: "Run scope classification and safe target merchant extraction"
      contains: "business_merchant"
    - path: "src/db/models.py"
      provides: "AgentRun scope fields"
      contains: "scope_classification"
    - path: "src/agent/trace.py"
      provides: "AgentRun scope persistence on run create/update"
      contains: "classify_agent_run_scope"
    - path: "tests/agent/test_phase36_run_scope.py"
      provides: "Classifier and no-weak-source tests"
      contains: "unknown_legacy"
  key_links:
    - from: "src/agent/trace.py"
      to: "src/agent/run_scope.py"
      via: "classify final state before AgentRun flush"
      pattern: "classify_agent_run_scope"
    - from: "src/api/routers/agent_runs.py"
      to: "src/db/models.py"
      via: "response projection exposes safe scope fields without auth use"
      pattern: "scope_classification"
---

<objective>
Create the AgentRun target merchant binding model and runtime persistence path.

Purpose: implement MSH-04 while preserving MSH-07. This plan makes AgentRun the primary persisted run-level merchant fact per D-01, but it does not use the new fields to widen run/status/evidence/trace/replay access.
Output: run-scope helper, AgentRun ORM fields, runtime write/update support, and focused run-scope tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-SPEC.md
@.planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-CONTEXT.md
@.planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-RESEARCH.md
@.planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-VALIDATION.md
@.planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-PATTERNS.md
@docs/contract-spec.md
@src/db/models.py
@src/agent/merchant_context.py
@src/replay/proof_projection.py
@src/approvals/schemas.py
@src/business/schemas.py
@src/agent/trace.py
@src/api/routers/agent_runs.py
@src/api/schemas/agent_runs.py
@src/agent/state.py

<interfaces>
Existing run write helper:
```python
async def write_agent_run(..., final_status: str, final_response: str | None, ...) -> AgentRun:
    run = await session.get(AgentRun, run_uuid)
    if run is None:
        run = AgentRun(...)
```

Existing non-authorizing projection helpers:
```python
def project_target_merchant_context(state: Mapping[str, Any]) -> dict[str, Any]: ...
def project_replay_authorization_proof(state: Mapping[str, Any]) -> dict[str, Any]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define AgentRun scope helper and ORM fields</name>
  <files>src/db/models.py, src/agent/run_scope.py, src/agent/state.py, tests/agent/test_phase36_run_scope.py, tests/approvals/test_migration_contract.py</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-SPEC.md
    - .planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-CONTEXT.md
    - .planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-PATTERNS.md
    - docs/contract-spec.md section 8.0.1
    - src/db/models.py
    - src/agent/merchant_context.py
    - src/replay/proof_projection.py
    - src/approvals/schemas.py
    - src/business/schemas.py
    - src/agent/state.py
  </read_first>
  <behavior>
    - Test a valid single `TargetMerchantBindingV1` produces `scope_classification="business_merchant"` and that exact `target_merchant_id`.
    - Test no business indicators produces `scope_classification="policy_only"` or `"merchant_not_required"` with null target merchant.
    - Test missing target merchant on a business-scoped path produces `scope_classification="unknown_legacy"`.
    - Test mixed target merchant proof produces `scope_classification="unknown_legacy"` and no persisted target merchant.
    - Test forbidden weak sources do not classify a run as `business_merchant`.
  </behavior>
  <action>
    Implement D-01, D-02, D-03, and D-04.

    Add `src/agent/run_scope.py` with these exact literals:
    - `BUSINESS_MERCHANT = "business_merchant"`
    - `POLICY_ONLY = "policy_only"`
    - `MERCHANT_NOT_REQUIRED = "merchant_not_required"`
    - `UNKNOWN_LEGACY = "unknown_legacy"`
    - `AGENT_RUN_SCOPE_CLASSIFICATIONS = {BUSINESS_MERCHANT, POLICY_ONLY, MERCHANT_NOT_REQUIRED, UNKNOWN_LEGACY}`

    Add a strict dataclass or Pydantic model `AgentRunScopeFacts` with fields `scope_classification: str`, `target_merchant_id: str | None`, `target_merchant_ref: dict[str, Any] | None`, `scope_source: str`, and `scope_reason_codes: list[str]`.

    Add `def classify_agent_run_scope(state: Mapping[str, Any]) -> AgentRunScopeFacts`. It may trust only these sources:
    - `state["target_merchant_id"]` paired with a valid `TargetMerchantBindingV1` in `state["target_merchant_ref"]`.
    - `state["approval_plan"]["target_merchant_id"]` paired with `state["approval_plan"]["target_merchant_ref"]` when the same values are present in state.
    - `BusinessFactResultV1` objects or dictionaries that strictly validate, have `status in {"ok", "partial"}`, `scope_check_result == "allowed"`, and expose a single safe merchant id in their service-approved `fact["merchant_id"]` value.

    It must not read or trust `requested_by`, `user.merchant_id`, owner identity, thread id, prompt text, final response text, memory, RAG evidence, LLM output, raw tool payloads, `target_merchant_context`, or `replay_authorization_proof` as authority. Those projection helpers may be copied into `scope_reason_codes` only as non-authorizing metadata when the classification is `unknown_legacy`.

    Update `src/db/models.py::AgentRun` with nullable columns:
    - `target_merchant_id: Mapped[str | None] = mapped_column(String(128))`
    - `target_merchant_ref: Mapped[dict[str, Any] | None] = mapped_column(JSONB)`
    - `scope_classification: Mapped[str] = mapped_column(String(32), nullable=False, default="unknown_legacy")`
    - `scope_source: Mapped[str | None] = mapped_column(String(64))`
    - `scope_reason_codes: Mapped[list[str] | None] = mapped_column(JSONB)`

    Add `CheckConstraint("scope_classification IN ('business_merchant', 'policy_only', 'merchant_not_required', 'unknown_legacy')", name="ck_agent_runs_scope_classification")`, `Index("ix_agent_runs_tenant_target_merchant", AgentRun.tenant_id, AgentRun.target_merchant_id)`, and `Index("ix_agent_runs_tenant_scope_classification", AgentRun.tenant_id, AgentRun.scope_classification)`. Plan 36-05 owns the Alembic migration.

    Add `target_merchant_id`, `target_merchant_ref`, `scope_classification`, `scope_source`, and `scope_reason_codes` as optional `AgentState` keys in `src/agent/state.py`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_phase36_run_scope.py tests/approvals/test_migration_contract.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/db/models.py src/agent/run_scope.py src/agent/state.py tests/agent/test_phase36_run_scope.py tests/approvals/test_migration_contract.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "BUSINESS_MERCHANT|UNKNOWN_LEGACY|classify_agent_run_scope|AgentRunScopeFacts" src/agent/run_scope.py tests/agent/test_phase36_run_scope.py` returns matches.
    - `rg -n "target_merchant_id|scope_classification|ck_agent_runs_scope_classification|ix_agent_runs_tenant_target_merchant" src/db/models.py tests/approvals/test_migration_contract.py` returns matches.
    - `rg -n "requested_by|user\\.merchant_id|input_query|final_response|thread_id|target_merchant_context|replay_authorization_proof" src/agent/run_scope.py` returns no matches except explicit forbidden-source tests/comments.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_phase36_run_scope.py tests/approvals/test_migration_contract.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>AgentRun has explicit scope fields and a classifier that uses only authoritative target merchant proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist scope facts on run create/update without widening guards</name>
  <files>src/agent/trace.py, src/api/schemas/agent_runs.py, src/api/routers/agent_runs.py, tests/agent/test_phase36_run_scope.py, tests/test_agent_runs_api.py</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-SPEC.md
    - .planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-CONTEXT.md
    - src/agent/run_scope.py
    - src/agent/trace.py
    - src/api/routers/agent_runs.py
    - src/api/schemas/agent_runs.py
    - tests/agent/test_phase36_run_scope.py
    - tests/test_agent_runs_api.py
    - tests/replay/test_phase35_trace_replay_permissions.py
  </read_first>
  <behavior>
    - Test `write_agent_run(..., final_state=...)` or equivalent update persists `scope_classification` and target merchant fields.
    - Test update path changes scope from `unknown_legacy` to `business_merchant` only when authoritative proof is present.
    - Test `target_merchant_context` / `replay_authorization_proof` alone never changes `unknown_legacy` to `business_merchant`.
    - Test `_ensure_can_view_run` still uses only owner/admin guard and not target merchant scope.
  </behavior>
  <action>
    Update `src/agent/trace.py` so run write/update paths call `classify_agent_run_scope(final_state or state)` when a final or current graph state is available. Thread the optional state parameter through existing call sites that already have final graph state in `src/api/routers/agent_runs.py`; do not derive target merchant from `AgentRun.user_id`, `User.merchant_id`, `thread_id`, `input_query`, prompt text, memory, RAG, LLM output, or raw tool payload.

    When an `AgentRun` row is created, default persisted scope to `scope_classification="unknown_legacy"`, `target_merchant_id=None`, `target_merchant_ref=None`, `scope_source="run_scope_classifier"`, and a reason code containing `no_authoritative_scope_proof` unless classifier returns a trusted non-legacy classification.

    When an existing `AgentRun` row is updated, overwrite scope fields only with classifier output from authoritative proof in current state. If classifier returns `unknown_legacy`, preserve any existing `business_merchant` binding unless the current state has an explicit contradiction reason code `mixed_target_merchant_proof`; in that case fail closed by setting `unknown_legacy` and clearing target fields.

    Extend safe response schemas in `src/api/schemas/agent_runs.py` only to expose `target_merchant_id`, `scope_classification`, and `scope_source` if the existing route response already includes run status metadata. Do not add authorization logic based on these fields. Preserve `_ensure_can_view_run` and `_ensure_can_execute_run` exactly as owner/admin and owner-only guards.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_phase36_run_scope.py tests/test_agent_runs_api.py tests/replay/test_phase35_trace_replay_permissions.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/trace.py src/api/routers/agent_runs.py src/api/schemas/agent_runs.py tests/agent/test_phase36_run_scope.py tests/test_agent_runs_api.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "classify_agent_run_scope|scope_classification|target_merchant_id" src/agent/trace.py src/api/routers/agent_runs.py src/api/schemas/agent_runs.py tests/agent/test_phase36_run_scope.py` returns matches.
    - `rg -n "project_replay_authorization_proof|target_merchant_context|requested_by.*merchant|merchant_id.*requested_by" src/api/routers/agent_runs.py src/agent/trace.py` returns no matches in authorization or classifier-call code.
    - `rg -n "if run.user_id != user.id and user.role not in ADMIN_RUN_VISIBILITY_ROLES" src/api/routers/agent_runs.py` returns a match.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_phase36_run_scope.py tests/test_agent_runs_api.py tests/replay/test_phase35_trace_replay_permissions.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>Runtime writes AgentRun scope facts from authoritative proof, and current AgentRun visibility remains owner/admin-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graph state -> AgentRun row | Runtime state is classified into durable run-scope facts. |
| AgentRun row -> API projection | Persisted scope fields may be visible as status metadata but must not authorize access. |
| projection metadata -> classifier | Target merchant and replay proof projections are non-authorizing metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T36-03 | Tampering / Elevation of Privilege | `src/agent/run_scope.py` | mitigate | Classifier ignores owner, requested_by, thread id, prompt, memory, RAG, LLM, and raw tool payload sources. |
| T36-04 | Elevation of Privilege | `src/api/routers/agent_runs.py` | mitigate | Preserve owner/admin-only `_ensure_can_view_run`; tests forbid scope/proof fields in guards. |
| T36-06 | Information Disclosure | `AgentRun.scope_classification` | mitigate | Policy-only, merchant-not-required, and unknown legacy have null target merchant and remain fail-closed for business visibility. |
</threat_model>

<verification>
Run focused verification:
`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_phase36_run_scope.py tests/test_agent_runs_api.py tests/replay/test_phase35_trace_replay_permissions.py -q --tb=short`

Run lint:
`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/db/models.py src/agent/run_scope.py src/agent/state.py src/agent/trace.py src/api/routers/agent_runs.py src/api/schemas/agent_runs.py tests/agent/test_phase36_run_scope.py tests/test_agent_runs_api.py`
</verification>

<success_criteria>
- AgentRun can store target merchant id/ref and explicit scope classification.
- Business-scoped runs require exactly one trusted target merchant binding.
- Weak sources cannot classify a run as `business_merchant`.
- Owner/admin-only run/status/evidence/stream guards remain unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/36-merchant-scope-db-hardening-role-cleanup/36-03-SUMMARY.md`.
</output>
