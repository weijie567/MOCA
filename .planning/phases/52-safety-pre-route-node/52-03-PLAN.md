---
phase: 52-safety-pre-route-node
plan: "03"
type: execute
wave: 3
depends_on:
  - "52-02"
files_modified:
  - src/agent/graph_vocabulary.py
  - tests/agent/test_graph_vocabulary.py
  - docs/current-langgraph-architecture.md
  - .planning/ARCHITECTURE-DEBT.md
  - .planning/phases/52-safety-pre-route-node/52-VALIDATION.md
autonomous: true
requirements:
  - CAGM-03
must_haves:
  truths:
    - "`safety_pre_route` projects as a runtime canonical graph node in trace vocabulary."
    - "Any remaining `classify_intent` pre-route compatibility is documented with owner, reason, trace projection, validation, and Phase 53 deletion target."
    - "Phase 52 validation closes with focused tests, architecture guardrails, static side-effect checks, and Ruff through MOCA-approved entrypoints."
  artifacts:
    - path: "src/agent/graph_vocabulary.py"
      provides: "Post-Phase 52 graph vocabulary projection"
      contains: "safety_pre_route"
    - path: ".planning/ARCHITECTURE-DEBT.md"
      provides: "Chinese architecture-debt ledger entry for graph migration progress and remaining compatibility"
      contains: "Phase 52"
    - path: ".planning/phases/52-safety-pre-route-node/52-VALIDATION.md"
      provides: "Nyquist validation status and exact command evidence"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "src/agent/graph_vocabulary.py"
      to: "tests/agent/test_graph_vocabulary.py"
      via: "projection status tests"
      pattern: "safety_pre_route.*runtime"
    - from: ".planning/ARCHITECTURE-DEBT.md"
      to: ".planning/phases/52-safety-pre-route-node/52-VALIDATION.md"
      via: "closeout evidence and remaining Phase 53 compatibility"
      pattern: "Phase 53"
---

<objective>
Close Phase 52 compatibility, trace-vocabulary, documentation, architecture-debt, and final validation obligations.

Purpose: The runtime graph can preserve `classify_intent` safe-path compatibility only if the exact temporary surface is recorded and tests prove `safety_pre_route` is now the canonical active pre-route node.
Output: Updated vocabulary projection, compatibility ledger, current-architecture docs, architecture-debt record, and final Nyquist validation evidence.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<read_first>
@.planning/phases/52-safety-pre-route-node/52-CONTEXT.md
@.planning/phases/52-safety-pre-route-node/52-RESEARCH.md
@.planning/phases/52-safety-pre-route-node/52-PATTERNS.md
@.planning/phases/52-safety-pre-route-node/52-VALIDATION.md
@.planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
@.planning/phases/51-canonical-graph-baseline-guardrails-and-migration-matrix/51-03-SUMMARY.md
@src/agent/graph_vocabulary.py
@tests/agent/test_graph_vocabulary.py
@docs/current-langgraph-architecture.md
@.planning/ARCHITECTURE-DEBT.md
</read_first>

<context>
## Locked Decisions Implemented

- D-52-04: Preserved `classify_intent` compatibility must use Phase 50 metadata: legacy surface, canonical owner, reason, trace projection, validation, delete phase. Delete phase is Phase 53.
- D-52-11 / D-52-13: Trace projection treats the new node as canonical runtime `safety_pre_route`; any `classify_intent:pre_route` alias is temporary and covered by tests.
- D-52-12: `classification_trace.pre_route_decision` may remain as a migration artifact with owner and cleanup path.
- D-52-17: Final validation commands must use approved MOCA entrypoints.
- D-52-18: Phase 52 remains split by ownership boundaries; no one mega-plan.

## Compatibility Table Required by Phase 50

| Legacy surface | Canonical owner | Reason | Trace projection | Validation | Delete phase |
|----------------|-----------------|--------|------------------|------------|--------------|
| Safe-route continuation `safety_pre_route -> classify_intent` and `classify_intent` active graph node | `contextual_intent_resolve` / Phase 53 CAGM-04 | Phase 52 only extracts pre-route safety; session context before intent and contextual intent cutover are Phase 53 | `classify_intent` continues to project to `contextual_intent_resolve`; new `safety_pre_route` projects as runtime canonical | Architecture graph baseline + graph tests prove unsafe pre-route cases stop before `classify_intent` and safe cases use compatibility only | Phase 53 |
| `classification_trace.pre_route_decision` inside `classify_intent` | `safety_pre_route` for runtime pre-route ownership; Phase 53 removes classifier-owned duplicate | Safe-path compatibility may still need classifier trace parity until contextual intent cutover | `classify_intent:pre_route` remains a compatibility alias to `safety_pre_route`; `safety_pre_route` itself is runtime | `test_graph_vocabulary.py`, `test_safety_pre_route.py`, and classifier parity tests | Phase 53 |

## Interfaces

From `src/agent/graph_vocabulary.py`:
```python
_entry("classify_intent:pre_route", "safety_pre_route", "node", "compatibility_alias", True)
_entry("safety_pre_route", "safety_pre_route", "node", "compatibility_alias", True)
```

Post-Phase 52 target:
```python
_entry("safety_pre_route", "safety_pre_route", "node", "runtime", True)
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote safety_pre_route trace vocabulary and document compatibility</name>
  <files>src/agent/graph_vocabulary.py, tests/agent/test_graph_vocabulary.py, docs/current-langgraph-architecture.md, .planning/ARCHITECTURE-DEBT.md</files>
  <behavior>
    - `graph_vocabulary_entry("safety_pre_route", kind="node")` has `target_name == "safety_pre_route"`, `status == "runtime"`, and `runnable is True`.
    - `classify_intent:pre_route` remains only a temporary compatibility alias to `safety_pre_route`, with Phase 53 deletion documented.
    - Current architecture docs describe the implemented Phase 52 path `receive_request -> safety_pre_route -> classify_intent` as transitional, not final target graph completion.
    - `.planning/ARCHITECTURE-DEBT.md` gets a Chinese ledger entry based on real code/tests/planning evidence, marking Phase 52 safety pre-route extraction complete and naming remaining Phase 53 compatibility.
    - Docs/ledger/validation closeout include exact compatibility rows for both `safety_pre_route -> classify_intent` safe-path continuation and `classification_trace.pre_route_decision`, with owner, reason, trace projection, validation, and Phase 53 deletion target on the same row.
  </behavior>
  <action>Update graph vocabulary tests before changing projection status. Then update `graph_vocabulary.py` so new runtime traces for `safety_pre_route` project as runtime, while `classify_intent:pre_route` remains a compatibility alias with a clear Phase 53 cleanup note in docs/ledger. Record the two compatibility rows using stable labels `Safe-route continuation ...` and `classification_trace.pre_route_decision ...` so the verification commands prove each row carries owner, reason, trace projection, validation, and delete phase. Do not mark `classify_intent` itself runtime-canonical; it remains a Phase 53 compatibility surface. This implements D-52-04, D-52-11, D-52-12, and D-52-13.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_graph_vocabulary.py -q --tb=short</automated>
    <automated>rg -n '^\x7c Safe-route continuation .*safety_pre_route -> classify_intent.*classify_intent.*active graph node.*contextual_intent_resolve.*Phase 53 CAGM-04.*Architecture graph baseline.*graph tests.*Phase 53' docs/current-langgraph-architecture.md .planning/ARCHITECTURE-DEBT.md .planning/phases/52-safety-pre-route-node/52-VALIDATION.md</automated>
    <automated>rg -n '^\x7c \x60classification_trace\.pre_route_decision.*classify_intent.*safety_pre_route.*classify_intent:pre_route.*test_graph_vocabulary\.py.*test_safety_pre_route\.py.*classifier parity tests.*Phase 53' docs/current-langgraph-architecture.md .planning/ARCHITECTURE-DEBT.md .planning/phases/52-safety-pre-route-node/52-VALIDATION.md</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/graph_vocabulary.py tests/agent/test_graph_vocabulary.py</automated>
  </verify>
  <done>Trace vocabulary distinguishes runtime `safety_pre_route` from temporary classifier compatibility, and docs/validation/architecture-debt records include exact owner, reason, trace projection, validation, and Phase 53 deletion path.</done>
</task>

<task type="auto">
  <name>Task 2: Run final Phase 52 Nyquist validation and close validation artifact</name>
  <files>.planning/phases/52-safety-pre-route-node/52-VALIDATION.md</files>
  <action>Run the full Phase 52 validation gate from `52-VALIDATION.md` using approved entrypoints only. Record command results in `52-VALIDATION.md`, set `status: complete`, `wave_0_complete: true`, and `nyquist_compliant: true` only after all commands pass. Include explicit rows for pre/post parity, negative control tests, static graph guardrails, fail-closed router behavior, no authority fields, and no memory/tool/approval/action side effects. Also run a source scan proving no bare pytest / bare python -m pytest commands were added to Phase 52 artifacts.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture tests/agent/test_nodes/test_safety_pre_route.py tests/agent/test_nodes/test_classify_intent.py tests/agent/test_graph.py tests/agent/test_graph_vocabulary.py tests/test_graph_routing.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/graph.py src/agent/routing.py src/agent/intent_policy.py src/agent/state.py src/agent/nodes/receive_request.py src/agent/nodes/classify_intent.py src/agent/nodes/safety_pre_route.py src/agent/graph_vocabulary.py tests/architecture/graph_baseline.py tests/architecture/test_canonical_graph_baseline.py tests/agent/test_nodes/test_safety_pre_route.py tests/agent/test_nodes/test_classify_intent.py tests/agent/test_nodes/test_receive_request.py tests/agent/test_graph.py tests/agent/test_graph_vocabulary.py tests/test_graph_routing.py</automated>
    <automated>! rg -n '([<]automated[>][[:space:]]*(pytest([[:space:]]|$)|python -m pytest([[:space:]]|$))|^[[:space:]]*(pytest([[:space:]]|$)|python -m pytest([[:space:]]|$)))' .planning/phases/52-safety-pre-route-node/*.md</automated>
    <automated>git diff --check -- src/agent/graph.py src/agent/routing.py src/agent/intent_policy.py src/agent/state.py src/agent/nodes/receive_request.py src/agent/nodes/classify_intent.py src/agent/nodes/safety_pre_route.py src/agent/graph_vocabulary.py tests/architecture/graph_baseline.py tests/architecture/test_canonical_graph_baseline.py tests/agent/test_nodes/test_safety_pre_route.py tests/agent/test_nodes/test_classify_intent.py tests/agent/test_nodes/test_receive_request.py tests/agent/test_graph.py tests/agent/test_graph_vocabulary.py tests/test_graph_routing.py docs/current-langgraph-architecture.md .planning/ARCHITECTURE-DEBT.md .planning/phases/52-safety-pre-route-node/52-VALIDATION.md</automated>
  </verify>
  <done>Final focused suite, Ruff, bare-pytest scan, and whitespace check pass; `52-VALIDATION.md` records complete Nyquist coverage for CAGM-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime trace -> contract projection | Implementation node names become API/replay/eval-visible contract metadata. |
| temporary compatibility -> future cleanup | Preserved legacy surfaces can become permanent unless owner/delete phase is explicit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-04 | Repudiation | graph trace vocabulary | mitigate | Promote `safety_pre_route` to runtime in vocabulary and test projection fields. |
| T-52-09 | Tampering / Repudiation | `classify_intent` compatibility | mitigate | Record Phase 50 compatibility table fields and Phase 53 delete target in docs/ledger. |
| T-52-10 | Tampering | validation artifact | mitigate | Set `nyquist_compliant: true` only after exact commands pass; record command evidence. |
| T-52-14 | Elevation of Privilege | missed side-effect regression | mitigate | Final suite includes node, graph, router, architecture, and vocabulary tests plus Ruff/static scans. |

ASVS L1 posture: no accepted unresolved high-severity threat in this plan. Missing trace projection or undocumented legacy compatibility blocks completion.
</threat_model>

<verification>
Run after all tasks:

```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture tests/agent/test_nodes/test_safety_pre_route.py tests/agent/test_nodes/test_classify_intent.py tests/agent/test_graph.py tests/agent/test_graph_vocabulary.py tests/test_graph_routing.py -q --tb=short
UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/graph.py src/agent/routing.py src/agent/intent_policy.py src/agent/state.py src/agent/nodes/receive_request.py src/agent/nodes/classify_intent.py src/agent/nodes/safety_pre_route.py src/agent/graph_vocabulary.py tests/architecture/graph_baseline.py tests/architecture/test_canonical_graph_baseline.py tests/agent/test_nodes/test_safety_pre_route.py tests/agent/test_nodes/test_classify_intent.py tests/agent/test_nodes/test_receive_request.py tests/agent/test_graph.py tests/agent/test_graph_vocabulary.py tests/test_graph_routing.py
git diff --check -- src/agent/graph.py src/agent/routing.py src/agent/intent_policy.py src/agent/state.py src/agent/nodes/receive_request.py src/agent/nodes/classify_intent.py src/agent/nodes/safety_pre_route.py src/agent/graph_vocabulary.py tests/architecture/graph_baseline.py tests/architecture/test_canonical_graph_baseline.py tests/agent/test_nodes/test_safety_pre_route.py tests/agent/test_nodes/test_classify_intent.py tests/agent/test_nodes/test_receive_request.py tests/agent/test_graph.py tests/agent/test_graph_vocabulary.py tests/test_graph_routing.py docs/current-langgraph-architecture.md .planning/ARCHITECTURE-DEBT.md .planning/phases/52-safety-pre-route-node/52-VALIDATION.md
```
</verification>

<rollback_and_deviation>
- If final validation reveals `safety_pre_route` still hidden only in classifier trace, return to Plan 01/02; do not close `52-VALIDATION.md`.
- If docs/spec require a semantic change beyond Phase 52 compatibility, stop and run the MOCA dual-AI spec divergence workflow before editing normative contracts.
- If any validation result came from bare pytest or bare python -m pytest, mark it invalid and rerun with `UV_CACHE_DIR=/tmp/uv-cache uv run ...`.
</rollback_and_deviation>

<out_of_scope>
- Do not delete `classify_intent`, `route_after_intent`, or classifier `classification_trace.pre_route_decision`; Phase 53 owns deletion/cleanup.
- Do not remove remaining migration aliases or enforce exact canonical final graph; Phase 58 owns final no-debt cleanup.
- Do not create UI-SPEC, AI-SPEC, frontend tasks, new tools, new services, new migrations, or external action execution.
</out_of_scope>

<source_audit>
| Source | ID | Required item | Plan | Status |
|--------|----|---------------|------|--------|
| GOAL | Phase 52 | Explicit safety pre-route node with compatibility recorded and validation closed | 01,02,03 | COVERED |
| REQ | CAGM-03 | `safety_pre_route` owns request-risk pre-route before memory/investigation/approval/action | 01,02,03 | COVERED |
| CONTEXT | D-52-04, D-52-11..D-52-13 | Compatibility metadata, trace projection, Phase 53 cleanup path | 03 | COVERED |
| CONTEXT | D-52-17, D-52-18 | Approved test entrypoints and split plan granularity | 01,02,03 | COVERED |
| RESEARCH | Compatibility policy | Phase 50 table fields required for preserved `classify_intent` surfaces | 03 | COVERED |
| VALIDATION | 52-03-01, 52-03-02 | Static docs/ledger check, final suite, Ruff, approved pytest entrypoint scan, git diff check | 03 | COVERED |
</source_audit>

<success_criteria>
- `safety_pre_route` is a runtime graph vocabulary entry, while `classify_intent:pre_route` remains a named temporary compatibility alias.
- Phase 52 docs/ledger identify preserved compatibility surface, canonical owner, reason, trace projection, validation, and Phase 53 deletion target.
- `52-VALIDATION.md` is complete and records exact green commands for every Nyquist row.
- No command evidence uses bare pytest or bare python -m pytest.
</success_criteria>

<output>
After completion, create `.planning/phases/52-safety-pre-route-node/52-03-SUMMARY.md`.
</output>
