---
phase: 15.1-memory-foundation-v2
plan: 01
subsystem: memory
tags: [conversation-log, sqlalchemy, alembic, fastapi, pytest]

requires:
  - phase: 15-replay-event-contract
    provides: ReplayEventV3 trace/audit redaction and sequence foundation
provides:
  - Conversation/thread/message/tool/summary ORM models and Alembic schema
  - ConversationService append/read boundary with raw payload rejection
  - Chat route user/assistant conversation append wiring
  - Blocking schema rollout report with downgrade/re-upgrade verification
affects: [phase-15.1, phase-16-memory, replay, agent-chat]

tech-stack:
  added: []
  patterns:
    - SQLAlchemy ORM plus Alembic additive migration
    - Service-owned raw payload denylist before persistence
    - Disposable DB migration downgrade/re-upgrade contract test

key-files:
  created:
    - src/db/migrations/versions/011_memory_foundation_v2.py
    - src/conversation/__init__.py
    - src/conversation/schemas.py
    - src/conversation/repository.py
    - src/conversation/service.py
    - tests/conversation/test_models.py
    - tests/conversation/test_repository.py
    - tests/conversation/test_service.py
    - .planning/phases/15.1-memory-foundation-v2/15.1-MIGRATION-REPORT.md
  modified:
    - src/db/models.py
    - src/api/routers/agent.py

key-decisions:
  - "Conversation facts are stored in dedicated conversation/tool/summary tables while business tables, policy KB, approval/action tables, and replay remain authoritative for their own domains."
  - "ConversationService rejects raw prompt/tool payload, private reasoning, and approval/action authority keys before append."
  - "Phase 15.1 reserves nullable case_id only; long-term memory, case retrieval, tombstones, embeddings, and vector retrieval remain Phase 16-owned."
  - "Migration rollout uses main DB upgrade plus disposable DB downgrade/re-upgrade testing; rollback target is 010_replay_event_v3."

patterns-established:
  - "ConversationRepository owns tenant/thread scoped append and ordered list semantics."
  - "Conversation schema parity tests compare ORM metadata with Alembic-created columns."
  - "Chat route appends user message before graph invocation and assistant/status message before commit."

requirements-completed:
  - MEMORY-FOUNDATION-01
  - MEMORY-FOUNDATION-02
  - MEMORY-FOUNDATION-03

duration: 17 min
completed: 2026-06-17
---

# Phase 15.1 Plan 01: Conversation Memory Foundation Summary

**Conversation log foundation with safe message append, additive tool/summary schema, and verified Alembic rollout to `011_memory_foundation_v2`**

## Performance

- **Duration:** 17 min
- **Started:** 2026-06-17T04:46:09Z
- **Completed:** 2026-06-17T05:03:54Z
- **Tasks:** 4
- **Files modified:** 11

## Accomplishments

- Added five Phase 15.1 foundation tables in ORM and Alembic: `conversation_threads`, `conversation_messages`, `tool_calls`, `tool_results`, and `summaries`.
- Added `ConversationService` and `ConversationRepository` for tenant/thread scoped safe append/list behavior.
- Wired `/api/v1/agent/chat` to append user messages before graph invocation and assistant/status messages before commit.
- Verified main DB upgrade to `011_memory_foundation_v2 (head)` and disposable DB downgrade/re-upgrade back to head.

## Task Commits

1. **Task 1: Add conversation schema/model contract tests first** - `526737b` (test)
2. **Task 2: Add ORM models and Alembic migration for conversation/tool/summary tables** - `daf12e4` (feat)
3. **Task 3: Add ConversationService and append chat user/assistant messages** - `b9b625b` (feat)
4. **Task 4: [BLOCKING] Apply and verify memory foundation schema** - `a5291b9` (chore)

## Files Created/Modified

- `src/db/models.py` - Conversation, message, tool call/result, and summary ORM models.
- `src/db/migrations/versions/011_memory_foundation_v2.py` - Additive schema migration and downgrade to `010_replay_event_v3`.
- `src/conversation/schemas.py` - Strict Pydantic append/view schemas and forbidden key guard.
- `src/conversation/repository.py` - Tenant/thread scoped append/list repository.
- `src/conversation/service.py` - Safe user/assistant/tool-summary append service.
- `src/api/routers/agent.py` - Chat route conversation append wiring.
- `tests/conversation/test_models.py` - ORM/migration parity, raw-column exclusion, and downgrade round-trip tests.
- `tests/conversation/test_repository.py` - Tenant scope, ordering, and raw payload rejection tests.
- `tests/conversation/test_service.py` - Chat turn append and Phase 16 non-overlap tests.
- `.planning/phases/15.1-memory-foundation-v2/15.1-MIGRATION-REPORT.md` - Rollout, rollback, and verification report.

## Decisions Made

- Followed the plan's owner split: conversation rows store conversation facts and safe refs/summaries only, while replay remains the redacted audit timeline.
- Preserved existing `AgentRun.input_query` and `AgentRun.final_response` compatibility fields instead of moving run summary ownership.
- Treated `case_id` as a nullable future reservation only; no Phase 16 retrieval/write/tombstone/vector surface was introduced.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed migration parity test parser for helper-expanded columns**
- **Found during:** Task 2
- **Issue:** The RED parity test initially parsed `op.create_table(...)` with regex and missed helper-expanded timestamp/retention columns.
- **Fix:** Switched the test to AST parsing so helper-expanded Alembic columns are still compared against ORM metadata.
- **Files modified:** `tests/conversation/test_models.py`
- **Verification:** `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/conversation/test_models.py -q --tb=short`
- **Committed in:** `daf12e4`

### Verification Scope Adjustment

**Task 2 acceptance command over-matched pre-existing Phase 2 RAG schema**
- **Found during:** Task 2 acceptance verification
- **Issue:** The planned command `rg -n "case_memories|memory_tombstones|embedding|vector" src/db/models.py src/db/migrations/versions/011_memory_foundation_v2.py` cannot return no matches because `src/db/models.py` already contains pre-existing `PolicyChunk.embedding` using `pgvector`.
- **Resolution:** Did not remove or weaken the existing RAG schema. Verified the new migration and `src/conversation` introduce no Phase 16 long-term/case memory, tombstone, embedding, or vector retrieval surface.
- **Verification:** `tests/conversation/test_models.py::test_memory_foundation_migration_does_not_create_phase_16_memory_surfaces` and `tests/conversation/test_service.py::test_reserved_case_id_does_not_create_case_memory_retrieval`

**Total deviations:** 1 auto-fixed bug, 1 documented verification-scope adjustment.
**Impact on plan:** No scope creep. The adjustment preserves Phase 2 RAG behavior while enforcing the Phase 15.1/16 boundary on new code.

## Issues Encountered

- Running DB-backed pytest commands in parallel against the shared `moca_test` database caused transient duplicate type/deadlock errors. Re-ran DB suites sequentially; all passed.
- The plan-level no-vector grep was broader than the actual Phase 15.1 change surface, as documented above.

## Authentication Gates

None.

## Known Stubs

None - no intentional stubs or placeholder data sources were introduced.

## User Setup Required

None - no external service configuration required.

## Verification

- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/conversation/test_models.py tests/conversation/test_repository.py tests/conversation/test_service.py -q --tb=short` - PASS, 16 tests.
- `UV_CACHE_DIR=/tmp/uv-cache uv run alembic upgrade head` - PASS.
- `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/db/models.py src/db/migrations/versions/011_memory_foundation_v2.py src/conversation src/api/routers/agent.py tests/conversation` - PASS.
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/integration/test_auth.py tests/test_agent_runs_api.py -q --tb=short` - PASS, 29 tests.

## Next Phase Readiness

Ready for `15.1-02-PLAN.md`. Downstream tool-result layering can use the new `tool_calls` and `tool_results` tables plus `ConversationService` without relying on replay raw payloads.

## Self-Check: PASSED

- Verified key created files exist on disk.
- Verified task commits exist in git history: `526737b`, `daf12e4`, `b9b625b`, `a5291b9`.
- Verified no accidental tracked file deletions were introduced by task commits.

---
*Phase: 15.1-memory-foundation-v2*
*Completed: 2026-06-17*
