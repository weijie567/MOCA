---
phase: 15.1-memory-foundation-v2
plan: 04
subsystem: memory
tags: [thread-summary, conversation-log, prompt-context, pytest, sqlalchemy]

requires:
  - phase: 15.1-01
    provides: Conversation messages, tool results, and summaries schema/repository foundation
  - phase: 15.1-02
    provides: Prompt-safe tool result summaries and layered tool result persistence
  - phase: 15.1-03
    provides: Prompt-safe WorkingStateV1 projection boundary
provides:
  - Source-range-backed thread rolling summary derivation and persistence
  - Repository APIs for reading summary source messages and tool prompt summaries
  - Prompt context window reads for latest prior-turn summary plus bounded recent messages/tool summaries
  - Regression coverage proving session memory remains separate from thread summaries
affects: [phase-15.1, phase-16-memory, context-assembler, agent-chat]

tech-stack:
  added: []
  patterns:
    - Deterministic summary derivation from safe conversation/tool summary inputs
    - `summaries.summary_type='thread_rolling'` writes with source message/tool result provenance
    - Prompt context reads exclude same-run summaries from prior-turn context

key-files:
  created:
    - src/memory/thread_summary.py
    - tests/memory/test_thread_summary.py
  modified:
    - src/api/routers/agent.py
    - src/conversation/repository.py
    - src/conversation/service.py
    - tests/conversation/test_service.py
    - tests/memory/test_session_memory_service.py

key-decisions:
  - "ThreadRollingSummaryService owns thread rolling summary derivation and writes only `summaries` rows."
  - "Phase 12 `session_memories` remains the owner of slot continuity, session summary, unresolved questions, and last intent."
  - "Prompt context loading returns the latest committed prior-turn `thread_rolling` summary plus bounded recent conversation messages and tool prompt summaries."
  - "Phase 16 case memory, embeddings, tombstones, vector retrieval, and case precedent search remain unimplemented."

patterns-established:
  - "Summary rows record `source_start_message_id`, `source_end_message_id`, `source_message_ids_json`, and `source_tool_result_ids_json`."
  - "Thread summaries use message content plus safe tool summaries, never normalized raw tool payloads."
  - "Completed chat turns persist thread summaries after the assistant message insert in the same transaction."

requirements-completed:
  - MEMORY-FOUNDATION-03

duration: 21 min
completed: 2026-06-17
---

# Phase 15.1 Plan 04: Thread Rolling Summary Summary

**Source-range-backed thread rolling summaries with safe tool-summary inputs and prior-turn prompt context reads**

## Performance

- **Duration:** 21 min
- **Started:** 2026-06-17T06:56:02Z
- **Completed:** 2026-06-17T07:16:50Z
- **Tasks:** 4
- **Files modified:** 7

## Accomplishments

- Added `ThreadRollingSummaryService` with deterministic derivation from safe conversation messages and tool summaries.
- Added source-range and provenance persistence into `summaries` rows for `thread_rolling` summaries.
- Added `ConversationService.load_prompt_context()` for Plan 05 to read the latest prior-turn summary plus bounded recent messages/tool prompt summaries.
- Wired successful `/api/v1/agent/chat` turns to persist a thread summary after the assistant message in the same DB transaction.
- Added regression tests proving thread summaries do not mutate or replace Phase 12 `session_memories`.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add thread summary source-range and boundary tests** - `acb38f8` (test, RED)
2. **Task 2: Implement ThreadRollingSummaryService and repository support** - `63bf0cc` (feat, GREEN)
3. **Task 3: Prove session memory remains separate** - `272739b` (test)
4. **Task 4: Wire rolling summary production and context-window reads into normal conversation flow** - `5925c87` (feat)

## Files Created/Modified

- `src/memory/thread_summary.py` - Thread rolling summary service, source-range summary derivation, raw-key denylist, and summary hash generation.
- `tests/memory/test_thread_summary.py` - TDD source range, safe tool summary, open question/constraint, and session/case non-overlap tests.
- `src/conversation/repository.py` - Message/source-range, summary, recent message, and tool prompt summary repository reads plus summary insert.
- `src/conversation/service.py` - `PromptContextWindow` and prior-turn `load_prompt_context()` reads.
- `src/api/routers/agent.py` - Completed chat success path writes a thread rolling summary after assistant persistence.
- `tests/conversation/test_service.py` - First-turn context, completed-turn summary, and bounded context-window tests.
- `tests/memory/test_session_memory_service.py` - Regression that `load_session_memory()` ignores thread summary rows and session fields remain unchanged.

## Decisions Made

- Used deterministic local derivation rather than adding an LLM summarizer in this foundation plan.
- Kept summary writes in `src/memory/thread_summary.py` while conversation repository owns persistence access to conversation/tool/summary rows.
- Treated `case_current` as schema-reserved only; the service writes only `thread_rolling`.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- Running DB-backed pytest commands in parallel against the shared `moca_test` database caused transient DDL deadlock/duplicate table errors. Re-ran all affected verification commands serially; all passed.

## Authentication Gates

None.

## Known Stubs

None. Stub scan only found test fixture empty lists/`None` values and type defaults; no placeholder behavior or unwired UI/data source was introduced.

## Threat Flags

None. The plan adds prompt-context reads and chat summary wiring already covered by the plan threat model; no new network endpoint, auth path, file access pattern, schema boundary, or Phase 16 retrieval surface was introduced.

## Verification

- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_thread_summary.py tests/memory/test_session_memory_service.py tests/conversation/test_service.py -q --tb=short` - PASS, 22 tests.
- `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/memory/thread_summary.py src/conversation tests/memory/test_thread_summary.py tests/memory/test_session_memory_service.py` - PASS.
- `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/api/routers/agent.py tests/conversation/test_service.py` - PASS.
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_agent_runs_api.py::test_agent_chat_only_token_invokes_legacy_chat_with_no_tool_permissions -q --tb=short` - PASS, 1 test.

## User Setup Required

None - no external service configuration required.

## TDD Gate Compliance

- RED commit: `acb38f8` added failing thread summary tests; failure was the expected missing `src.memory.thread_summary` module.
- GREEN commit: `63bf0cc` implemented the service and repository support; target tests passed.

## Next Phase Readiness

Ready for `15.1-05-PLAN.md`. ContextAssembler can consume `ConversationService.load_prompt_context()` for prior-turn thread summary, recent safe conversation messages, and tool prompt summaries without using raw tool payloads or Phase 12 session memory as thread summary.

## Self-Check: PASSED

- Verified key created/modified files exist on disk.
- Verified task commits exist in git history: `acb38f8`, `63bf0cc`, `272739b`, `5925c87`.
- Verified no accidental tracked file deletions were introduced by task commits.

---
*Phase: 15.1-memory-foundation-v2*
*Completed: 2026-06-17*
