---
phase: 15.1-memory-foundation-v2
verified: 2026-06-17T08:49:04Z
status: passed
score: 8/8 must-haves verified
overrides_applied: 0
code_review_status: clean
---

# Phase 15.1: Memory Foundation V2 Verification Report

**Phase Goal:** Establish the pre-Phase 16 memory foundation: complete conversation log, tool call/result layering, prompt-safe WorkingStateV1, thread rolling summaries, ContextAssembler, token budgeting, and trace/audit/conversation ID alignment.
**Verified:** 2026-06-17T08:49:04Z
**Status:** passed
**Re-verification:** No - initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | Conversation log foundation is user-scoped and stores safe conversation facts without raw prompt/tool payloads. | VERIFIED | `ConversationThread` has `tenant_id`, `user_id`, `thread_id` and user-scoped active uniqueness in `src/db/models.py:645-683`; `012_thread_user_scope.py:22-30` migrates uniqueness to tenant/user/thread; repository reads scope by tenant/user/thread in `src/conversation/repository.py:25-40`; schemas reject raw/private/authority keys in `src/conversation/schemas.py:10-20`. |
| 2 | Tool call/result storage has layered raw refs/hashes, normalized records, summaries, and prompt-safe projections. | VERIFIED | `ToolCallRecord`/`ToolResultRecord` include argument summary/hash, raw result ref/hash, normalized JSON, summary, prompt summary, replay/audit refs in `src/db/models.py:728-814`; `ConversationService.append_tool_result()` stores normalized JSON but returns `ToolResultPromptSummary` in `src/conversation/service.py:147-217`; tests assert raw markers stay out of prompt summaries in `tests/tools/test_tool_result_storage.py`. |
| 3 | WorkingStateV1 is prompt-safe and does not become authoritative state. | VERIFIED | `WorkingStateV1` is a strict projection only, produced by `project_working_state(state)` in `src/agent/working_state.py:114-150`; unsafe runtime keys and raw/authority field names are denylisted in `src/agent/working_state.py:13-67`; no `working_memory` table was added. |
| 4 | Thread rolling summaries are source-range short-term summaries and do not mutate Phase 12 session memory. | VERIFIED | `ThreadRollingSummaryService` derives from conversation messages and tool summaries, records source message/tool result IDs, and writes `summary_type='thread_rolling'` through `src/memory/thread_summary.py:47-155` and `src/conversation/repository.py:315-351`; session memory separation is covered in `tests/memory/test_session_memory_service.py` and `tests/memory/test_thread_summary.py`. |
| 5 | ContextAssembler owns prompt assembly/token budgeting and respects reserved sections. | VERIFIED | `ContextAssembler.assemble()` builds explicit blocks and calls `TokenBudgetPolicy.apply()` in `src/agent/context/assembler.py:25-93`; protected block names include `system_prompt`, `current_user_message`, `safety_constraints`, `business_ids`, and `policy_refs` in `src/agent/context/budget.py:25-39`; live recommendation/risk/slot nodes consume `PromptAssembly.to_messages()`. |
| 6 | Conversation/tool/replay/audit refs align without weakening ReplayEventV3 redaction. | VERIFIED | Alignment test asserts shared tenant/thread/run/trace/tool/operation/message/replay/audit refs in `tests/replay/test_memory_foundation_alignment.py:74-225`; replay still rejects `raw_prompt`, `raw_tool_output`, `raw_payload`, `raw`, `data`, and `prompt` in `tests/replay/test_memory_foundation_alignment.py:228-265`; `ReplayService.append_event()` still calls `guard_redacted_payload()` in `src/replay/service.py:49-82`. |
| 7 | Phase 16 long-term/case memory/tombstones/embeddings/vector retrieval/user-managed memory and Phase 17 execution/outbox/reconciliation/compensation remain absent from Phase 15.1 implementation. | VERIFIED | Phase 15.1 code/memory migrations do not add `case_memories`, `memory_tombstones`, action outbox/execution tables, or memory vector retrieval; negative assertions are in `tests/replay/test_memory_foundation_alignment.py:285-310`. Pre-existing policy pgvector and transitional `search_case_memory` over `session_memories` are older surfaces, documented as not reviewed case memory in `src/memory/search.py:15-21`. |
| 8 | Code review status is clean and final verification evidence is plausible. | VERIFIED | `15.1-REVIEW.md` frontmatter status is `clean`; it records 0 critical/warning/info findings after fix commit `1f53308`. I re-ran focused pytest, ruff, schema drift, and Alembic current checks successfully. |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/db/models.py` | Conversation/thread/message/tool/summary ORM models | VERIFIED | Models and indexes exist; user-scoped thread uniqueness is present. |
| `src/db/migrations/versions/011_memory_foundation_v2.py` | Additive memory foundation schema | VERIFIED | Creates five Phase 15.1 tables; `012_thread_user_scope.py` closes user-scope uniqueness. |
| `src/conversation/service.py` | Safe append/load boundary and tool result persistence | VERIFIED | User/assistant/tool append, prompt context loading, layered tool result persistence. |
| `src/tools/contracts.py` | Prompt-safe tool storage/projection contracts | VERIFIED | `ToolResultStorageV1` and `ToolResultPromptSummary` exist. |
| `src/agent/working_state.py` | Strict prompt-safe `WorkingStateV1` projection | VERIFIED | Allowlisted projection from `AgentState`; unsafe keys omitted. |
| `src/memory/thread_summary.py` | Thread rolling summary derivation | VERIFIED | Uses source ranges and safe tool summaries. |
| `src/agent/context/*` | ContextAssembler, projectors, token budgeting | VERIFIED | Central assembly, explicit projectors, protected blocks. |
| `tests/replay/test_memory_foundation_alignment.py` | Cross-layer alignment/non-overlap regression | VERIFIED | Covers refs, replay redaction, layer separation, Phase 16/17 absence. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `src/api/routers/agent.py` | `src/conversation/service.py` | Chat appends user before graph, assistant after final response | WIRED | Lines 72-100 and 170-202 create service, append messages, pass `conversation_message_id`, and persist summary. |
| `src/agent/nodes/investigate.py` | `src/conversation/service.py` | Tool call/result persistence | WIRED | Lines 263-326 append call/result records when DB session and conversation message ref exist. |
| `src/conversation/service.py` | `src/agent/nodes/generate_recommendation.py`, `src/agent/nodes/assess_risk_and_approval.py` | `load_prompt_context()` before ContextAssembler | WIRED | Both nodes load user-scoped prompt context when a real session exists. |
| `src/agent/working_state.py` | `src/agent/context/assembler.py` | `WorkingStateV1` consumed by ContextAssembler | WIRED | `assemble()` requires `WorkingStateV1` and projectors render it explicitly. |
| `src/conversation/service.py` | `src/replay/service.py` / audit log | Refs only | WIRED | Tool rows carry `replay_event_id`/`audit_ref`; replay redaction remains owned by `src/replay`. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| Conversation log | `ConversationMessage` rows | `/api/v1/agent/chat` calls `append_user_message()` and `append_assistant_message()` | Yes, DB-backed rows via repository | FLOWING |
| Tool results | `ToolResultRecord` + `ToolResultPromptSummary` | `investigate()` tool loop and `ConversationService.append_tool_result()` | Yes, DB rows plus prompt-safe projection | FLOWING |
| Thread summaries | `ConversationSummary` rows | `ThreadRollingSummaryService.persist_thread_summary()` after assistant append | Yes, source-range summary rows | FLOWING |
| Prompt assembly | `PromptAssembly` | Recommendation/risk/slot nodes call `ContextAssembler.assemble()` | Yes, rendered via `to_messages()` | FLOWING |
| Replay/audit refs | `replay_event_id`, `audit_ref` | `ReplayService.append_event()` and `AuditRepository.record_conversation_reference()` | Yes, correlation rows and refs | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Focused memory foundation regressions | `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/conversation/test_repository.py tests/conversation/test_service.py tests/tools/test_tool_result_storage.py tests/agent/test_working_state.py tests/memory/test_thread_summary.py tests/replay/test_memory_foundation_alignment.py -q --tb=short` | 25 passed, 1 LangGraph deprecation warning | PASS |
| Focused lint | `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check ...` | All checks passed | PASS |
| Schema drift | `gsd-sdk query verify.schema-drift "15.1"` | `valid: true`, 0 issues, 6 checked | PASS |
| Alembic state | `UV_CACHE_DIR=/tmp/uv-cache uv run alembic current` | `012_thread_user_scope (head)` | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| MEMORY-FOUNDATION-01 | Plans 01, 02, 06 | Conversation and tool facts with run/thread/trace correlation without replacing business/policy/approval/action/replay authorities | SATISFIED | Conversation/tool tables, user-scoped service/repository, chat/investigate wiring, alignment tests. |
| MEMORY-FOUNDATION-02 | Plans 01, 02, 03, 05, 06 | Prompt-safe WorkingStateV1, four-layer tool result storage, ContextAssembler, token budgeting, no raw payloads or implicit dict/list stringification | SATISFIED | WorkingStateV1/projectors/ContextAssembler and raw-marker exclusion tests. |
| MEMORY-FOUNDATION-03 | Plans 01, 04, 05, 06 | Thread rolling summaries from safe source ranges, distinct from session memory, no Phase 16 case/long-term memory | SATISFIED | `ThreadRollingSummaryService`, source-range columns/tests, session-memory non-overlap tests, Phase 16 negative checks. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---:|---|---|---|
| `src/memory/search.py` | 15-21 | Pre-existing `search_case_memory` transitional session-memory search | Info | Not introduced by Phase 15.1; explicitly documented as not the target reviewed case-memory store. |
| `src/db/models.py` | 194 | Pre-existing policy `PolicyChunk.embedding` | Info | RAG/knowledge pgvector, not Phase 15.1 memory embedding; Plan 01 summary documented the broad grep adjustment. |

### Human Verification Required

None.

### Gaps Summary

No blockers found. One literal GSD artifact check looked for `append_user_message` in `tests/conversation/test_repository.py`; the intended repository/service coverage exists across `tests/conversation/test_repository.py` and `tests/conversation/test_service.py`, so this is a plan pattern mismatch rather than a goal gap.

---

_Verified: 2026-06-17T08:49:04Z_
_Verifier: Claude (gsd-verifier)_
