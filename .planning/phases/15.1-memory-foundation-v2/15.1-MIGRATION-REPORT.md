# Phase 15.1 Plan 01 Migration Report

## Owners

- schema_owner: `src/db/models.py` and `src/db/migrations/versions/011_memory_foundation_v2.py`
- read_switch_owner: `src/conversation/service.py` and `src/api/routers/agent.py`
- fallback_owner: disable or revert Phase 15.1 conversation append/load wiring; existing `AgentRun`, `AgentStep`, `agent_trace_events`, `session_memories`, approval/action tables, and replay remain canonical operational paths.
- rollback_owner: Alembic `downgrade()` in `src/db/migrations/versions/011_memory_foundation_v2.py`

## Rollback

- rollback_command: `UV_CACHE_DIR=/tmp/uv-cache uv run alembic downgrade 010_replay_event_v3`
- downgrade_round_trip_command: `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/conversation/test_models.py::test_memory_foundation_migration_downgrade_round_trip -q --tb=short`

## Verification Results

| Command | Result |
| --- | --- |
| `UV_CACHE_DIR=/tmp/uv-cache uv run alembic upgrade head` | PASS - upgraded main local DB to `011_memory_foundation_v2` |
| `UV_CACHE_DIR=/tmp/uv-cache uv run alembic current` | PASS - reported `011_memory_foundation_v2 (head)` |
| `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/conversation/test_models.py::test_memory_foundation_migration_downgrade_round_trip -q --tb=short` | PASS - disposable DB removed only Phase 15.1 tables on downgrade to `010_replay_event_v3` and recreated them on re-upgrade |
| `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/conversation/test_models.py tests/conversation/test_repository.py tests/conversation/test_service.py -q --tb=short` | PASS - 16 tests passed |

## Scope Notes

- The migration is additive and creates only `conversation_threads`, `conversation_messages`, `tool_calls`, `tool_results`, and `summaries`.
- `conversation_threads.case_id` and `summaries.case_id` are nullable reservations only.
- No Phase 16 long-term memory, case memory retrieval, tombstones, user-manageable memory, or vector retrieval surface is introduced by revision `011_memory_foundation_v2`.
