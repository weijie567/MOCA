---
phase: 15.1-memory-foundation-v2
plan: 06
subsystem: memory
tags: [replay-alignment, conversation-log, audit, tool-results, regression]

requires:
  - phase: 15.1-01
    provides: Conversation/tool/summary tables and ConversationService append boundary
  - phase: 15.1-02
    provides: Layered tool call/result persistence
  - phase: 15.1-03
    provides: WorkingStateV1 prompt-safe projection
  - phase: 15.1-04
    provides: Thread rolling summaries and prompt context reads
  - phase: 15.1-05
    provides: ContextAssembler and token budgeting
provides:
  - End-to-end memory foundation alignment tests for conversation/tool/replay/audit refs
  - Safe audit conversation reference helper
  - Tool call/result conversation_message_id and replay_event_id wiring
  - Final Phase 15.1 targeted regression evidence
  - Phase 16 and Phase 17 owner-named deferrals in 15.1-SUMMARY.md
affects: [phase-15.1, phase-16-memory, phase-17-external-action, replay, audit, context-assembler]

tech-stack:
  added: []
  patterns:
    - Safe cross-layer refs instead of raw payload sharing
    - Conversation tool persistence gated by real DB session plus conversation message ref
    - Audit metadata guard for conversation/tool correlation refs

key-files:
  created:
    - tests/replay/test_memory_foundation_alignment.py
    - .planning/phases/15.1-memory-foundation-v2/15.1-SUMMARY.md
  modified:
    - src/conversation/service.py
    - src/repositories/audit_repo.py
    - src/api/routers/agent.py
    - src/agent/nodes/investigate.py

key-decisions:
  - "Conversation/tool/replay/audit alignment is implemented with safe refs: conversation_message_id, tool_result_id, replay_event_id, audit_ref, run_id, trace_id, operation_id, and tool_call_id."
  - "ReplayService redaction guards were not weakened; approval/action authority-body exclusion remains owned by conversation/context boundaries."
  - "Investigate persists conversation tool rows only when a real DB session and conversation_message_id are present, preserving graph-only test paths as prompt-safe projections."
  - "Phase 16 owns long-term memory write gate, memory tombstones, user-manageable memory, case memory vector search, and case precedent retrieval; Phase 17 owns external action execution storage, outbox/execution reconciliation, and compensation workflow."

patterns-established:
  - "Alignment tests exercise real DB rows across conversation_messages, tool_calls, tool_results, agent_trace_events, and audit_logs."
  - "Audit conversation refs return opaque audit/conversation/{id} refs while metadata is recursively raw-key guarded."

requirements-completed:
  - MEMORY-FOUNDATION-01
  - MEMORY-FOUNDATION-02
  - MEMORY-FOUNDATION-03

duration: 20 min
completed: 2026-06-17
---

# Phase 15.1 Plan 06: Memory Foundation Alignment Summary

**Conversation, tool, replay, and audit refs now correlate without relaxing ReplayEventV3 redaction or implementing Phase 16/17 surfaces**

## Performance

- **Duration:** 20 min
- **Started:** 2026-06-17T07:40:55Z
- **Completed:** 2026-06-17T08:01:26Z
- **Tasks:** 3
- **Files modified:** 6

## Accomplishments

- Added alignment tests proving user/tool/assistant conversation records, tool call/result rows, replay events, and audit rows share tenant/thread/run/trace/tool/operation IDs.
- Added safe audit conversation-reference logging with raw payload/authority metadata rejection.
- Wired `conversation_message_id` from `/api/v1/agent/chat` into investigate tool persistence, while keeping graph-only runs on prompt-safe in-memory projections.
- Verified replay raw-key guards still reject `raw_prompt`, `raw_tool_output`, `raw_payload`, `raw`, `data`, and `prompt` without adding approval/action authority-body keys.
- Created the final phase summary artifact with required Phase 16/17 deferrals.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add memory foundation alignment tests** - `df58272` (test, RED)
2. **Task 2: Implement conversation/replay/audit correlation refs** - `df7b4f5` (feat, GREEN)
3. **Task 3: Run final targeted regression suite and record deferrals** - `dc925c0` (docs)

Additional auto-fix commit:

- **Task 3 blocking regression fix** - `04ec832` (fix)

## Files Created/Modified

- `tests/replay/test_memory_foundation_alignment.py` - End-to-end ID correlation, redaction, non-overlap, and Phase 16/17 negative tests.
- `src/conversation/service.py` - Accepts and persists `conversation_message_id` for tool call/result rows; keeps `replay_event_id` references only.
- `src/repositories/audit_repo.py` - Adds safe `record_conversation_reference()` helper with recursive metadata guard.
- `src/api/routers/agent.py` - Passes the appended user conversation message ref into graph config for investigate tool storage.
- `src/agent/nodes/investigate.py` - Passes conversation message refs to tool storage and gates DB persistence to real conversation-backed runs.
- `.planning/phases/15.1-memory-foundation-v2/15.1-SUMMARY.md` - Records final Phase 15.1 outcomes and required Phase 16/17 deferrals.

## Decisions Made

- Used refs, not raw payloads, for cross-layer traceability.
- Kept ReplayService redaction semantics unchanged; replay remains the redacted event stream.
- Treated direct graph invocations without conversation message refs as projection-only paths to avoid invalid FK writes and preserve non-API test ergonomics.
- Documented Phase 16 and Phase 17 owner boundaries in the phase-level summary rather than implementing deferred surfaces.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Removed accidental patch marker from new alignment test**
- **Found during:** Task 1 RED verification
- **Issue:** The first RED run failed at collection because a patch marker was left in the new test file.
- **Fix:** Removed the stray marker and re-ran the RED command.
- **Files modified:** `tests/replay/test_memory_foundation_alignment.py`
- **Verification:** `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay/test_memory_foundation_alignment.py -q --tb=short` then failed for the expected missing audit helper.
- **Committed in:** `df58272`

**2. [Rule 3 - Blocking] Gated investigate conversation persistence for non-conversation graph runs**
- **Found during:** Task 3 full targeted regression
- **Issue:** Graph-only tests passed `session=object()` or invoked graph runs without a persisted `AgentRun`/conversation message ref, causing tool row persistence to fail with missing `execute` or FK errors.
- **Fix:** Persist conversation tool rows only when a real DB session and `conversation_message_id` are present; otherwise keep prompt-safe in-memory projections.
- **Files modified:** `src/agent/nodes/investigate.py`
- **Verification:** The four failing graph/session-memory tests passed; full targeted pytest passed with 445 tests.
- **Committed in:** `04ec832`

---

**Total deviations:** 2 auto-fixed (1 bug, 1 blocking issue).
**Impact on plan:** Both fixes preserve the plan boundary. No ReplayService guard was weakened, no Phase 16/17 implementation surface was added, and API-backed conversation runs still persist aligned tool refs.

## Issues Encountered

- The first full targeted pytest run failed in four graph/session-memory tests due to conversation tool row persistence on non-conversation graph paths. Fixed in `04ec832` and re-ran the full targeted suite successfully.

## Authentication Gates

None.

## Known Stubs

None. Stub scan found only normal optional `None` type signatures and no placeholder/TODO/unwired data sources.

## Threat Flags

None. The plan added no new network endpoint, auth path, file access pattern, schema trust boundary, long-term/case memory retrieval surface, or external execution surface. The new audit helper is a planned safe correlation writer with recursive raw-key metadata guarding.

## User Setup Required

None - no external service configuration required.

## Verification

- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay/test_memory_foundation_alignment.py -q --tb=short` - RED failed as expected before implementation; final PASS, 4 tests.
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay/test_memory_foundation_alignment.py tests/conversation/test_service.py tests/tools/test_tool_result_storage.py -q --tb=short` - PASS, 13 tests.
- `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/conversation src/agent/trace.py src/replay/service.py src/repositories/audit_repo.py src/api/routers/agent.py tests/replay/test_memory_foundation_alignment.py` - PASS.
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/conversation tests/agent tests/tools tests/memory tests/replay -q` - PASS, 445 tests.
- `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/conversation src/agent/working_state.py src/agent/context src/agent/nodes/investigate.py src/agent/nodes/generate_recommendation.py src/agent/nodes/assess_risk_and_approval.py src/memory/thread_summary.py src/tools src/replay tests/conversation tests/agent tests/tools tests/memory tests/replay` - PASS.
- `rg -n "Phase 16 owns long-term memory write gate|memory tombstones|user-manageable memory|case memory vector search|case precedent retrieval|Phase 17 owns external action execution storage|outbox/execution reconciliation|compensation workflow" .planning/phases/15.1-memory-foundation-v2/15.1-SUMMARY.md` - PASS.

## TDD Gate Compliance

- RED commit: `df58272` added failing alignment tests; failure was the expected missing `AuditRepository.record_conversation_reference`.
- GREEN commit: `df7b4f5` implemented the correlation refs and safe audit helper; alignment and task-local suites passed.
- Refactor/fix commit: `04ec832` narrowed persistence gating after the full regression suite exposed graph-only path failures.

## Next Phase Readiness

Ready for verifier review of Phase 15.1. The overall phase is not marked complete here; verifier owns phase completion. Phase 16 can plan long-term/case memory using the safe refs and deferrals captured in `15.1-SUMMARY.md`.

## Self-Check: PASSED

- Verified key created files exist on disk: `tests/replay/test_memory_foundation_alignment.py`, `.planning/phases/15.1-memory-foundation-v2/15.1-SUMMARY.md`, `.planning/phases/15.1-memory-foundation-v2/15.1-06-SUMMARY.md`.
- Verified task commits exist in git history: `df58272`, `df7b4f5`, `04ec832`, `dc925c0`.
- Verified no accidental tracked file deletions were introduced by task commits.

---
*Phase: 15.1-memory-foundation-v2*
*Completed: 2026-06-17*
