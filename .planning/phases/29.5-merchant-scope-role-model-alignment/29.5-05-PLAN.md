---
phase: 29.5-merchant-scope-role-model-alignment
plan: 05
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - src/api/routers/approvals.py
  - src/api/routers/agent_runs.py
  - src/api/routers/traces.py
  - src/agent/nodes/investigate.py
  - tests/test_approval_api.py
  - tests/approvals/test_single_level_runtime.py
  - tests/approvals/test_hash_binding.py
  - tests/approvals/test_events.py
  - tests/approvals/test_needs_info_resume.py
  - tests/test_approval_integration.py
  - tests/test_agent_runs_api.py
  - tests/test_trace_api.py
  - tests/replay/test_replay_api.py
  - tests/agent/test_nodes/test_investigate.py
autonomous: true
requirements: [MER-01]
must_haves:
  truths:
    - "Manager approval review is admin-only/fail-closed during Phase 29.5."
    - "Approval resume no longer passes wildcard server_merchant_scope for non-admin humans."
    - "Business AgentRun, trace, and replay visibility is owner/admin-only until later target merchant binding phases."
    - "Investigate missing trusted context fails closed without wildcard merchant scope."
  artifacts:
    - path: "src/api/routers/approvals.py"
      provides: "Admin-only approval interim guard"
      contains: "APPROVAL_ROLES = {\"admin\"}"
    - path: "src/api/routers/agent_runs.py"
      provides: "Owner/admin run visibility"
      contains: "ADMIN_RUN_VISIBILITY_ROLES = {\"admin\"}"
    - path: "src/agent/nodes/investigate.py"
      provides: "Missing trusted context fail-closed behavior"
      contains: "MISSING_TRUSTED_CONTEXT"
  key_links:
    - from: "src/api/routers/approvals.py"
      to: "src/platform/trusted_context.py"
      via: "approval resume relies on server_tool_permissions only, not wildcard server_merchant_scope"
      pattern: "server_tool_permissions"
---

<objective>
Apply Phase 29.5 interim guards for approval review, run/trace/replay visibility, and investigate missing trusted context.

Purpose: Deferred target merchant binding must not leave old tenant-wide manager or wildcard graph paths active.
Output: Approval is admin-only interim, run/trace/replay are owner/admin-only interim, and investigate missing trusted context fails closed.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-CONTEXT.md
@.planning/todos/deferred/2026-06-27-merchant-scope-agentrun-replay.md
@.planning/todos/deferred/2026-06-27-merchant-scope-approval-action.md
@docs/contract-spec.md
@src/api/routers/approvals.py
@src/api/routers/agent_runs.py
@src/api/routers/traces.py
@src/agent/nodes/investigate.py
@tests/test_approval_api.py
@tests/approvals/test_single_level_runtime.py
@tests/approvals/test_hash_binding.py
@tests/approvals/test_events.py
@tests/approvals/test_needs_info_resume.py
@tests/test_approval_integration.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restrict approval review and remove wildcard resume</name>
  <files>src/api/routers/approvals.py, tests/test_approval_api.py, tests/approvals/test_single_level_runtime.py, tests/approvals/test_hash_binding.py, tests/approvals/test_events.py, tests/approvals/test_needs_info_resume.py, tests/test_approval_integration.py</files>
  <read_first>src/api/routers/approvals.py, tests/test_approval_api.py, tests/approvals/test_single_level_runtime.py, tests/approvals/test_hash_binding.py, tests/approvals/test_events.py, tests/approvals/test_needs_info_resume.py, tests/test_approval_integration.py, .planning/phases/29.5-merchant-scope-role-model-alignment/29.5-CONTEXT.md</read_first>
  <action>
    In `src/api/routers/approvals.py`, set `APPROVAL_ROLES = {"admin"}`. Do not add manager-specific branches or requested_by-to-merchant approximation. In approval resume graph config, remove any `server_merchant_scope={"merchant_ids":["*"]}` injection. Keep `server_tool_permissions` so approved/accepted decisions may inject action-draft permission without widening merchant scope.

    Update `tests/test_approval_api.py` so manager list/get/decide paths return 403 and admin allowed paths still succeed subject to existing approval state/version/hash rules. Add a regression that approved resume config contains server tool permission but no wildcard server merchant scope.

    Migrate broad approval flow tests that currently use `approval_manager` / role `"manager"` as the approval decider. In `tests/approvals/test_single_level_runtime.py`, `tests/approvals/test_hash_binding.py`, `tests/approvals/test_events.py`, `tests/approvals/test_needs_info_resume.py`, and `tests/test_approval_integration.py`, preserve approval workflow coverage by switching successful list/get/decide actors to `admin_user` / role `"admin"` where the test intent is approval lifecycle behavior. Only use manager in these files when the test explicitly asserts the Phase 29.5 interim 403 behavior; keep the primary manager-denial regression in `tests/test_approval_api.py`.
  </action>
  <acceptance_criteria>
    - `rg -n 'APPROVAL_ROLES = \\{"admin"\\}' src/api/routers/approvals.py` finds the admin-only role set.
    - `rg -n 'server_merchant_scope' src/api/routers/approvals.py` returns no match.
    - `rg -n 'manager.*403|403.*manager|APPROVAL_ROLES' tests/test_approval_api.py` finds manager denial coverage.
    - `rg -n 'admin_user|role=\"admin\"|admin' tests/approvals/test_single_level_runtime.py tests/approvals/test_hash_binding.py tests/approvals/test_events.py tests/approvals/test_needs_info_resume.py tests/test_approval_integration.py` finds admin decider coverage for broad approval flow tests.
  </acceptance_criteria>
  <verify>uv run pytest tests/test_approval_api.py tests/approvals/test_single_level_runtime.py tests/approvals/test_hash_binding.py tests/approvals/test_events.py tests/approvals/test_needs_info_resume.py tests/test_approval_integration.py -q</verify>
  <done>Approval interim is admin-only and resume does not widen merchant scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restrict run/trace/replay visibility and fail closed investigate fallback</name>
  <files>src/api/routers/agent_runs.py, src/api/routers/traces.py, src/agent/nodes/investigate.py, tests/test_agent_runs_api.py, tests/test_trace_api.py, tests/replay/test_replay_api.py, tests/agent/test_nodes/test_investigate.py</files>
  <read_first>src/api/routers/agent_runs.py, src/api/routers/traces.py, src/agent/nodes/investigate.py, tests/test_agent_runs_api.py, tests/test_trace_api.py, tests/replay/test_replay_api.py, tests/agent/test_nodes/test_investigate.py</read_first>
  <action>
    In `src/api/routers/agent_runs.py` and `src/api/routers/traces.py`, restrict non-owner visibility to admin only. Remove effective visibility membership for `manager`, `supervisor`, and `approval_manager`; either set `ADMIN_RUN_VISIBILITY_ROLES = {"admin"}` or inline `user.role == "admin"` checks. Do not relax owner access.

    In `src/agent/nodes/investigate.py`, remove the missing-trusted-context fallback that constructs `merchant_scope={"merchant_ids":["*"]}`. Missing or invalid trusted context must produce no visible business tools and no business tool execution, with a safe `MISSING_TRUSTED_CONTEXT`/fail-closed outcome.

    Note the execution order: the main investigate flow may already fail closed when `trusted_context is None` with `MISSING_TRUSTED_CONTEXT`. This task targets the earlier `_build_visibility_context` fallback used for `visible_tools()` projection: missing trusted context must not construct wildcard visibility context or project business tools. Do not delete the existing main fail-closed branch.

    Update tests so manager/ghost roles cannot view non-owner AgentRun status/evidence/trace/replay; admin and owner remain allowed. For negative-only ghost-role coverage, create or mutate same-tenant test users with actual `role="supervisor"` and `role="approval_manager"` values, distinct from existing fixture usernames, and assert both roles receive 403 for non-owner status/evidence/trace/replay. These strings are not Phase 29.5 product roles and must not appear in allow role sets. Update investigate tests to assert no wildcard fallback and no business tool execution when trusted context is missing.
  </action>
  <acceptance_criteria>
    - `rg -n 'ADMIN_RUN_VISIBILITY_ROLES = \\{"admin"\\}' src/api/routers/agent_runs.py src/api/routers/traces.py` finds owner/admin interim role sets, or equivalent inline admin-only checks are present.
    - `rg -n 'supervisor|approval_manager|manager' src/api/routers/agent_runs.py src/api/routers/traces.py` does not show these roles in visibility role sets.
    - `rg -n 'role=.*supervisor|role=.*approval_manager|supervisor.*403|approval_manager.*403' tests/test_agent_runs_api.py tests/test_trace_api.py tests/replay/test_replay_api.py` finds explicit ghost-role denial coverage using actual role values.
    - `rg -n 'merchant_scope=\\{"merchant_ids": \\["\\*"\\]\\}|merchant_ids.*\\["\\*"\\]' src/agent/nodes/investigate.py` returns no missing-context wildcard fallback.
    - `rg -n 'MISSING_TRUSTED_CONTEXT|unrecoverable_error|merchant_ids.*\\[\\]' tests/agent/test_nodes/test_investigate.py` finds fail-closed regression coverage.
  </acceptance_criteria>
  <verify>uv run pytest tests/test_agent_runs_api.py tests/test_trace_api.py tests/replay/test_replay_api.py tests/agent/test_nodes/test_investigate.py -q</verify>
  <done>Deferred manager same-merchant visibility is guarded by owner/admin-only interim behavior, and investigate no longer fabricates wildcard business scope.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/test_approval_api.py tests/approvals/test_single_level_runtime.py tests/approvals/test_hash_binding.py tests/approvals/test_events.py tests/approvals/test_needs_info_resume.py tests/test_approval_integration.py tests/test_agent_runs_api.py tests/test_trace_api.py tests/replay/test_replay_api.py tests/agent/test_nodes/test_investigate.py -q`
- [ ] `git diff --check`
</verification>

<success_criteria>
- Old manager tenant-wide approval/run/trace access is not left active.
- No approval or investigate path bypasses TrustedContextFactory with wildcard business scope.
</success_criteria>

<output>
After completion, create `.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-05-SUMMARY.md`.
</output>
