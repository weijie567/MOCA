---
phase: 29.5-merchant-scope-role-model-alignment
plan: 06
subsystem: final-verification
tags: [merchant-scope, static-guard, verification, trusted-context]

requires:
  - phase: 29.5-03
    provides: Business read merchant guards
  - phase: 29.5-04
    provides: Policy-only empty merchant scope behavior
  - phase: 29.5-05
    provides: Approval/run/trace/replay interim guards
provides:
  - Production wildcard merchant-scope static regression
  - Focused Phase 29.5 regression suite result
  - Whole-suite regression gate result
  - Whole-suite legacy seam cleanup
affects: [Phase 30 BusinessFactService Boundary, Phase 32 Intent Graph Migration, Phase 35 Replay and Eval Hardening]

tech-stack:
  added: []
  patterns: [AST static guard, ToolPlatform test seam, admin-only run visibility naming]

key-files:
  created:
    - tests/tools/test_merchant_scope_static.py
    - .planning/phases/29.5-merchant-scope-role-model-alignment/29.5-06-SUMMARY.md
  modified:
    - tests/agent/test_tools/test_unified_tool_manager.py
    - src/agent/intent_policy.py
    - tests/agent/test_intent_routing.py
    - src/agent/nodes/investigate.py
    - src/platform/context_projections.py
    - tests/agent/test_graph.py
    - tests/agent/test_policy_retrieval_ownership.py
    - tests/knowledge/test_facade_integration.py
    - tests/knowledge/test_phase21_boundaries.py
    - src/api/routers/agent_runs.py
    - src/api/routers/traces.py
    - .planning/phases/29.5-merchant-scope-role-model-alignment/29.5-05-PLAN.md

key-decisions:
  - "Static wildcard guard scans production `src/` only and allows admin wildcard derivation only in `TrustedContextFactory`."
  - "Legacy test helpers must not model ordinary `support` as wildcard merchant scoped."
  - "Whole-suite graph/knowledge tests now use the Phase 29 `ToolPlatform` seam and trusted context instead of old `UnifiedToolManager` fallback assumptions."
  - "`supervisor` and `approval_manager` are negative-only ghost-role test values, not Phase 29.5 product roles."

requirements-completed: [MER-01]

duration: 2h 35min
completed: 2026-06-27
---

# Phase 29.5 Plan 06: Static Guard and Final Verification Summary

**Plan 06 completed the final static and regression gates for Phase 29.5.**

## Performance

- **Duration:** 2h 35min
- **Completed:** 2026-06-27
- **Tasks:** 2
- **Files created:** 2
- **Files modified:** 12

## Accomplishments

- Added `tests/tools/test_merchant_scope_static.py`, an AST guard that fails on production wildcard merchant-scope construction outside `src/platform/trusted_context.py`.
- Removed a stale wildcard default from `tests/agent/test_tools/test_unified_tool_manager.py`; `support` helpers now default to `merchant-primary`.
- Fixed whole-suite regressions exposed by the final gate:
  - Compensation policy/status questions no longer get incorrectly promoted to `compensation_suggestion`.
  - Graph and knowledge facade tests now pass explicit trusted context and fake `ToolPlatform` boundaries.
  - Missing-trusted-context visibility fallback moved to `src/platform/context_projections.py`, so `investigate.py` no longer directly constructs service context.
  - Phase 21 static boundary allowlist now recognizes later Phase 22 test surfaces.
- Renamed `SUPERVISOR_ROLES` to `ADMIN_RUN_VISIBILITY_ROLES` in run/trace APIs and clarified Plan 05 text so ghost roles are explicitly negative-only test values.

## Task Commits

1. **Task 1: Static wildcard guard** - `d9f68a5` (test)
2. **Task 2: Remove stale wildcard test helper** - `8966b26` (test)
3. **Task 2: Intent precedence whole-suite fix** - `e54976c` (fix)
4. **Task 2: Trusted context whole-suite seam fixes** - `b119491` (fix)
5. **Task 2: Admin-only run visibility naming** - `6530c20` (chore)

## Verification

- `uv run pytest tests/tools/test_merchant_scope_static.py -q` passed (`1 passed`, 1 warning).
- Focused Phase 29.5 suite passed (`341 passed`, 6 warnings).
- Whole suite passed: `uv run pytest tests/ -q --tb=short` (`1590 passed`, 1 skipped, 42 warnings).
- Targeted previously failing files passed (`199 passed`, 32 warnings).
- Run/trace/replay/investigate focused regression passed after role-set rename (`96 passed`, 1 warning).
- `uv run ruff check ...` passed for changed source/test files.
- `git diff --check` passed.
- Acceptance grep passed for admin-only run visibility roles, no investigate wildcard fallback, raw business-read denial coverage, and no stale support/manager wildcard helper assertions.

## Deviations from Plan

### Auto-fixed Issues

1. **Whole-suite legacy ToolPlatform seam drift** — Final whole-suite gate exposed tests still wired to old manager fallback assumptions. These tests now use fake `ToolPlatform` facades and explicit trusted context.
2. **Intent precedence regression** — Whole-suite gate exposed compensation keyword over-promotion for policy/status questions. The deterministic precedence rule now requires action cues before selecting `compensation_suggestion`.
3. **Misleading run visibility role-set name** — User review flagged `SUPERVISOR_ROLES` as confusing. The constant was renamed to `ADMIN_RUN_VISIBILITY_ROLES`, with Plan 05 text clarified.

---

**Total deviations:** 3 auto-fixed.
**Impact on plan:** No product scope expansion. The fixes remove stale assumptions and preserve Phase 29.5's admin-only / merchant-bound interim model.

## Issues Encountered

- No environment blockers. The final whole-suite failures were ordinary regressions and were fixed before completion, so no new `.planning/LOCAL-VALIDATION-ISSUES.md` entry was required.

## User Setup Required

None.

## Next Phase Readiness

Phase 30 can now plan BusinessFactService domain ownership proof on top of Phase 29.5 merchant-bound trusted context and interim raw business-read guards.

---
*Phase: 29.5-merchant-scope-role-model-alignment*
*Completed: 2026-06-27*

## Self-Check: PASSED

- Static guard, focused suite, whole suite, ruff, and `git diff --check` all passed.
- Phase 29.5 deferred boundaries remain intact: BusinessFactService proof, Memory deepening, AgentRun/replay/trace target merchant binding, Approval/ActionDraft target merchant binding, and DB/RLS hardening remain routed to later phases.
