---
phase: 29.5-merchant-scope-role-model-alignment
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - src/api/routers/orders.py
  - src/api/routers/refund_cases.py
  - src/api/routers/tickets.py
  - src/integrations/demo_business/authz.py
  - src/integrations/demo_business/orders.py
  - src/integrations/demo_business/refunds.py
  - src/integrations/demo_business/tickets.py
  - tests/integration/test_orders.py
  - tests/integration/test_refund_cases.py
  - tests/integration/test_tickets.py
  - tests/agent/test_tools/test_get_order.py
  - tests/agent/test_tools/test_get_refund_case.py
  - tests/agent/test_tools/test_get_ticket.py
  - tests/business/test_service.py
autonomous: true
requirements: [MER-01]
must_haves:
  truths:
    - "Order, refund case, and ticket APIs fetch by tenant first, then apply merchant access helper."
    - "Order, refund case, and ticket tool/raw business reads enforce the same merchant-bound role semantics until Phase 30 BusinessFactService owns domain proof."
    - "Cross-tenant resources remain 404 and same-tenant out-of-merchant-scope resources are 403."
    - "support, manager, and legacy merchant users can access same-merchant business resources only."
  artifacts:
    - path: "src/api/routers/orders.py"
      provides: "Order route merchant helper enforcement"
      contains: "require_merchant_access"
    - path: "src/api/routers/refund_cases.py"
      provides: "Refund route merchant helper enforcement"
      contains: "require_merchant_access"
    - path: "src/api/routers/tickets.py"
      provides: "Ticket route merchant helper enforcement"
      contains: "require_merchant_access"
    - path: "src/integrations/demo_business/authz.py"
      provides: "Interim raw business tool merchant-bound guard"
      contains: "merchant_can_access"
  key_links:
    - from: "src/api/routers/orders.py"
      to: "src/auth/permissions.py"
      via: "route imports helper from Plan 02"
      pattern: "require_merchant_access"
---

<objective>
Migrate Order, RefundCase, and Ticket APIs to the unified merchant access helper.

Purpose: These are the Phase 29.5 business-data API surfaces where ordinary business users must be scoped to their merchant.
Output: Route checks, raw business/tool guards, and integration/tool tests prove same-merchant allow, same-tenant cross-merchant deny, missing merchant deny, admin cross-merchant allow, and cross-tenant 404/no-leak behavior.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-CONTEXT.md
@.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-PATTERNS.md
@.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-VALIDATION.md
@src/auth/permissions.py
@src/api/routers/orders.py
@src/api/routers/refund_cases.py
@src/api/routers/tickets.py
@src/business/service.py
@src/integrations/demo_business/authz.py
@src/integrations/demo_business/orders.py
@src/integrations/demo_business/refunds.py
@src/integrations/demo_business/tickets.py
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace route-local checks and close raw tool business-read seam</name>
  <files>src/api/routers/orders.py, src/api/routers/refund_cases.py, src/api/routers/tickets.py, src/integrations/demo_business/authz.py, src/integrations/demo_business/orders.py, src/integrations/demo_business/refunds.py, src/integrations/demo_business/tickets.py</files>
  <read_first>src/auth/permissions.py, src/api/routers/orders.py, src/api/routers/refund_cases.py, src/api/routers/tickets.py, src/business/service.py, src/integrations/demo_business/authz.py, src/integrations/demo_business/orders.py, src/integrations/demo_business/refunds.py, src/integrations/demo_business/tickets.py</read_first>
  <action>
    Import `require_merchant_access` in all three routers.

    In `orders.py`, keep `repository.get_with_hints(order_no, user.tenant_id)` and the 404 branch first. Then call `require_merchant_access(user, order.merchant_id, resource_name="orders")` before success audit/response.

    In `refund_cases.py`, keep tenant-first lookup and 404 first. Keep the existing `Order.merchant_id` derivation, then call `require_merchant_access(user, merchant_id, resource_name="refund cases")`. Remove the legacy `if user.role == "merchant"` branch.

    In `tickets.py`, keep tenant-first lookup and 404 first. Keep the existing `Order.merchant_id` derivation, then call `require_merchant_access(user, merchant_id, resource_name="tickets")`. Remove the legacy `if user.role == "merchant"` branch.

    Also migrate the current tool/raw business-read seam used by `BusinessToolService`. In `src/integrations/demo_business/authz.py`, replace legacy `role == "merchant"`-only logic with the Phase 29.5 role matrix: `admin` allows; `support`, `manager`, and legacy `merchant` require same tenant and `actor.merchant_id == target merchant_id`; missing `merchant_id` or unknown role denies. In refund and ticket raw integrations, derive the target merchant id after tenant-first lookup and call `merchant_can_access` for every role, not only `role == "merchant"`. `get_order` already calls `merchant_can_access`, but must inherit the tightened semantics. This is an interim guard only; Phase 30 still owns full BusinessFactService no-leak semantics and domain ownership proof.
  </action>
  <acceptance_criteria>
    - `rg -n 'require_merchant_access\\(user, order\\.merchant_id' src/api/routers/orders.py` finds the order call.
    - `rg -n 'require_merchant_access\\(user, merchant_id' src/api/routers/refund_cases.py src/api/routers/tickets.py` finds refund/ticket calls.
    - `rg -n 'user\\.role == "merchant"' src/api/routers/orders.py src/api/routers/refund_cases.py src/api/routers/tickets.py` returns no route-local legacy merchant branch.
    - `rg -n 'merchant_can_access' src/integrations/demo_business/authz.py src/integrations/demo_business/orders.py src/integrations/demo_business/refunds.py src/integrations/demo_business/tickets.py` shows all raw order/refund/ticket reads use the shared raw guard.
    - `rg -n 'role == "merchant"' src/integrations/demo_business/refunds.py src/integrations/demo_business/tickets.py` returns no raw refund/ticket legacy-only branch.
  </acceptance_criteria>
  <verify>uv run pytest tests/integration/test_orders.py tests/integration/test_refund_cases.py tests/integration/test_tickets.py tests/agent/test_tools/test_get_order.py tests/agent/test_tools/test_get_refund_case.py tests/agent/test_tools/test_get_ticket.py tests/business/test_service.py -q</verify>
  <done>Business-data routes and raw tool reads enforce merchant scope after tenant-first fetch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite business API and tool expectations</name>
  <files>tests/integration/test_orders.py, tests/integration/test_refund_cases.py, tests/integration/test_tickets.py, tests/agent/test_tools/test_get_order.py, tests/agent/test_tools/test_get_refund_case.py, tests/agent/test_tools/test_get_ticket.py, tests/business/test_service.py</files>
  <read_first>tests/conftest.py, tests/integration/test_orders.py, tests/integration/test_refund_cases.py, tests/integration/test_tickets.py, tests/agent/test_tools/test_get_order.py, tests/agent/test_tools/test_get_refund_case.py, tests/agent/test_tools/test_get_ticket.py, tests/business/test_service.py</read_first>
  <action>
    Update tests so:
    - Primary-merchant support, manager, and legacy merchant can read primary-merchant order/refund/ticket.
    - Primary-merchant support, manager, and legacy merchant get HTTP 403 for `second_order`, `second_refund_case`, and `second_ticket`.
    - Missing merchant-bound user cases get HTTP 403 when such fixture exists or by direct helper test from Plan 02.
    - Admin can read primary and second-merchant resources within the same tenant.
    - Other-tenant users still get HTTP 404 for demo-tenant resources.
    - Tool tests that intentionally need cross-merchant visibility use admin context; merchant-bound contexts use same-merchant fixtures.
    - Raw tool/business-path tests cover support and manager same-merchant allow, same-tenant second-merchant deny, admin same-tenant cross-merchant allow, merchant-bound user with missing `merchant_id` deny, and unknown role deny.
    - Denied tool/business-path reads must not emit `BusinessFactRefV1`, prompt-visible business facts, or successful raw summaries for the denied resource.
  </action>
  <acceptance_criteria>
    - `rg -n '403|HTTP_403_FORBIDDEN|status_code == 403' tests/integration/test_orders.py tests/integration/test_refund_cases.py tests/integration/test_tickets.py` finds same-tenant cross-merchant deny assertions.
    - `rg -n '404|HTTP_404_NOT_FOUND|status_code == 404' tests/integration/test_orders.py tests/integration/test_refund_cases.py tests/integration/test_tickets.py` finds cross-tenant not-found assertions.
    - `rg -n 'admin_user|admin' tests/agent/test_tools/test_get_order.py tests/agent/test_tools/test_get_refund_case.py tests/agent/test_tools/test_get_ticket.py` finds explicit admin contexts for cross-merchant tool tests if any remain.
    - `rg -n 'cs_other_merchant|manager_other_merchant|permission_denied|business_fact_refs.*\\[\\]' tests/agent/test_tools/test_get_order.py tests/agent/test_tools/test_get_refund_case.py tests/agent/test_tools/test_get_ticket.py tests/business/test_service.py` finds raw tool/business-path deny and no-business-ref assertions.
    - In `tests/business/test_service.py`, denied cross-merchant `fetch_context()` assertions check `context.facts == {}`, `context.business_fact_refs == []`, no `ToolResultPromptSummary.business_fact_refs`, and no prompt summary contains the denied resource id or a success summary for that resource.
  </acceptance_criteria>
  <verify>uv run pytest tests/integration/test_orders.py tests/integration/test_refund_cases.py tests/integration/test_tickets.py tests/agent/test_tools/test_get_order.py tests/agent/test_tools/test_get_refund_case.py tests/agent/test_tools/test_get_ticket.py tests/business/test_service.py -q</verify>
  <done>Tests encode the new API semantics and no longer rely on support/manager wildcard access.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/integration/test_orders.py tests/integration/test_refund_cases.py tests/integration/test_tickets.py -q`
- [ ] `uv run pytest tests/agent/test_tools/test_get_order.py tests/agent/test_tools/test_get_refund_case.py tests/agent/test_tools/test_get_ticket.py tests/business/test_service.py -q`
- [ ] `git diff --check`
</verification>

<success_criteria>
- API route behavior matches Phase 29.5 403/404 contract.
- Raw tool/business-read behavior does not bypass Phase 29.5 merchant-bound role semantics before Phase 30 BusinessFactService work.
- Business tool tests are not masking merchant-bound failures with old wildcard assumptions.
</success_criteria>

<output>
After completion, create `.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-03-SUMMARY.md`.
</output>
