---
phase: 29.5-merchant-scope-role-model-alignment
plan: 04
subsystem: knowledge
tags: [knowledge, merchant-scope, public-policy, memory, tool-executor]

requires:
  - phase: 29.5-02
    provides: Merchant-bound TrustedContext scope semantics
  - phase: 29.5-03
    provides: Business-data merchant scope guards separate from public policy retrieval
provides:
  - PolicyKnowledgeService policy-only retrieval that allows empty business merchant scope
  - Explicit merchant filter fail-closed tests for empty and wrong merchant scopes
  - Memory executor smoke tests for narrowed, empty, and wildcard merchant scopes
affects: [Phase 31 Memory Platform Boundary, Phase 33 RAG Context Build and Claim Verification]

tech-stack:
  added: []
  patterns: [explicit merchant filter authorization, policy-only public retrieval scope separation, memory no-wildcard smoke guard]

key-files:
  created:
    - .planning/phases/29.5-merchant-scope-role-model-alignment/29.5-04-SUMMARY.md
  modified:
    - src/knowledge/service.py
    - tests/knowledge/test_service.py
    - tests/agent/test_tools/test_unified_tool_manager.py

key-decisions:
  - "PolicyKnowledgeService treats `merchant_scope=[]` as valid for tenant public policy-only retrieval when no explicit merchant filter is requested."
  - "`merchant_scope=None` remains fail-closed because that is missing scope, not deny-all business scope."
  - "Memory executor implementation was left unchanged; smoke tests prove it adds narrowed merchant scope but does not add wildcard merchant scope."

patterns-established:
  - "KnowledgeService checks business merchant scope only when `request.filters.merchant_id is not None`."
  - "Phase 29.5 memory coverage is smoke-only; Phase 31 still owns full memory isolation and write policy hardening."

requirements-completed: [MER-01]

duration: 6min
completed: 2026-06-27
---

# Phase 29.5 Plan 04: Knowledge Public Policy Scope Summary

**Tenant public policy retrieval now stays available with empty business merchant scope while explicit merchant policy filters still fail closed.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-06-27T10:38:30Z
- **Completed:** 2026-06-27T10:44:36Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Removed the unconditional empty merchant-scope early return from `PolicyKnowledgeService.search(...)`.
- Preserved fail-closed behavior for missing scope (`merchant_scope=None`) and explicit merchant filters outside the caller scope.
- Added tests proving policy-only `merchant_scope=[]` calls the retriever, while explicit merchant filters with empty/wrong scope do not.
- Added memory smoke tests proving narrowed scope contributes `("merchant", "merchant-a")`, empty scope contributes no merchant scope, and wildcard scope is not converted into `("merchant", "*")`.

## Task Commits

1. **Task 1 RED: Policy-only empty-scope test** - `9e2c7d1` (test)
2. **Task 1 GREEN: KnowledgeService scope separation** - `8f52223` (fix)
3. **Task 2: Memory merchant scope smoke tests** - `50c0448` (test)
4. **Task 1 acceptance clarity** - `b87b000` (test)

## Files Created/Modified

- `src/knowledge/service.py` - Allows tenant public policy-only retrieval with `merchant_scope=[]` and checks merchant scope only for explicit merchant filters.
- `tests/knowledge/test_service.py` - Covers missing scope fail-closed, empty business scope policy-only allow, explicit merchant filter deny, and authorized merchant filters.
- `tests/agent/test_tools/test_unified_tool_manager.py` - Adds memory executor smoke coverage for narrowed, empty, and wildcard merchant scopes.

## Decisions Made

- `merchant_scope=[]` means deny-all for business data, but not deny-all for tenant public policy retrieval.
- `merchant_scope=None` remains invalid/missing context and returns no evidence without adapter execution.
- No memory executor source change was needed; the existing implementation already excludes `*` when constructing case memory request scopes.

## Deviations from Plan

### Auto-fixed Issues

None - plan executed within planned scope.

### Verification Note

The Plan 04 memory acceptance grep `merchant.*\*` is intentionally broad and matches existing wildcard fixtures plus the memory executor exclusion guard `merchant_id != "*"`. Manual inspection confirms no code path adds `("merchant", "*")` as an allowed memory request scope; the new smoke test asserts that directly.

---

**Total deviations:** 0 auto-fixed, 1 verification note.
**Impact on plan:** No scope change. The broad grep was interpreted against the actual matched lines.

## Issues Encountered

- The first memory smoke fake used invalid `CaseMemorySearchResult(status="not_found")`; the schema accepts `success` or `empty`. It was corrected to `empty`, and the executor returned the expected `not_found` tool result.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plan 05 can tighten approval/run/trace/replay/investigate fallback behavior knowing tenant public policy retrieval remains separate from business merchant scope and memory wildcard expansion has a smoke guard.

---
*Phase: 29.5-merchant-scope-role-model-alignment*
*Completed: 2026-06-27*

## Self-Check: PASSED

- `uv run pytest tests/knowledge/test_service.py tests/test_search_integration.py -q` passed (`18 passed`, 1 warning).
- `uv run pytest tests/agent/test_tools/test_unified_tool_manager.py -q` passed (`29 passed`, 1 warning).
- Acceptance grep passed for `merchant_id is not None`, policy-only empty-scope coverage, and memory smoke coverage; broad wildcard grep was manually inspected as described above.
- `uv run ruff check src/knowledge/service.py src/tools/executors/memory.py tests/knowledge/test_service.py tests/test_search_integration.py tests/agent/test_tools/test_unified_tool_manager.py` passed.
- `git diff --check` passed before summary creation.
