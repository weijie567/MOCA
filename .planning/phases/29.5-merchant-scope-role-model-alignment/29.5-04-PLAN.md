---
phase: 29.5-merchant-scope-role-model-alignment
plan: 04
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - src/knowledge/service.py
  - src/tools/executors/memory.py
  - tests/knowledge/test_service.py
  - tests/agent/test_tools/test_unified_tool_manager.py
autonomous: true
requirements: [MER-01]
must_haves:
  truths:
    - "Tenant public policy search is not denied solely because business merchant_scope is empty."
    - "Explicit merchant/business policy filters remain scope-checked and fail closed."
    - "Current memory executor smoke coverage proves narrowed merchant scope does not become wildcard memory scope."
  artifacts:
    - path: "src/knowledge/service.py"
      provides: "Policy-only empty-scope allow behavior"
      contains: "merchant_id is not None"
    - path: "tests/knowledge/test_service.py"
      provides: "Policy-only allow and explicit merchant filter deny tests"
      contains: "merchant_scope=[]"
  key_links:
    - from: "src/knowledge/service.py"
      to: "docs/contract-spec.md"
      via: "business scope is separate from tenant public policy scope"
      pattern: "merchant_id is not None"
---

<objective>
Separate tenant-public policy retrieval from business-data merchant scope and add memory scope smoke coverage.

Purpose: Tightening support/manager business merchant scope must not accidentally block tenant public policy search, but explicit merchant filters must still deny out-of-scope requests.
Output: KnowledgeService policy-only behavior and memory executor smoke tests reflect Phase 29.5 semantics.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-CONTEXT.md
@.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-VALIDATION.md
@.planning/todos/deferred/2026-06-27-merchant-scope-memory.md
@docs/contract-spec.md
@src/knowledge/service.py
@src/tools/executors/memory.py
@tests/knowledge/test_service.py
@tests/agent/test_tools/test_unified_tool_manager.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Allow policy-only search with empty business scope</name>
  <files>src/knowledge/service.py, tests/knowledge/test_service.py</files>
  <read_first>src/knowledge/service.py, tests/knowledge/test_service.py, docs/contract-spec.md</read_first>
  <action>
    In `src/knowledge/service.py`, remove the unconditional early return that treats empty `context.merchant_scope` as no evidence. Keep explicit merchant filter enforcement:
    - If `request.filters.merchant_id is not None`, require `context.merchant_scope` to contain `"*"` or that merchant id; otherwise return `_no_evidence_result()` before adapter execution.
    - If `request.filters.merchant_id is None`, allow the retriever to run even when `context.merchant_scope == []`, assuming the caller has already supplied authenticated tenant identity and knowledge permission.
    - Add a short comment that this path is for tenant public policy; future merchant-specific policy remains scope-checked through explicit filters.

    Update `tests/knowledge/test_service.py` to prove policy-only empty-scope search calls the retriever and explicit merchant filter with empty/wrong scope returns no evidence without calling the retriever.
  </action>
  <acceptance_criteria>
    - `rg -n 'if not merchant_scope:\\s*return self\\._no_evidence_result' src/knowledge/service.py` returns no match.
    - `rg -n 'merchant_id is not None|merchant_scope' src/knowledge/service.py` finds the explicit filter branch.
    - `rg -n 'merchant_scope=\\[\\]|policy-only|merchant_id' tests/knowledge/test_service.py` finds policy-only allow and explicit filter deny tests.
  </acceptance_criteria>
  <verify>uv run pytest tests/knowledge/test_service.py tests/test_search_integration.py -q</verify>
  <done>Policy-only tenant public search works with empty business scope while explicit merchant filters still fail closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add memory scope smoke regression</name>
  <files>src/tools/executors/memory.py, tests/agent/test_tools/test_unified_tool_manager.py</files>
  <read_first>src/tools/executors/memory.py, tests/agent/test_tools/test_unified_tool_manager.py, .planning/todos/deferred/2026-06-27-merchant-scope-memory.md</read_first>
  <action>
    Do not redesign memory in Phase 29.5. Add smoke tests around existing memory executor behavior:
    - `merchant_scope={"merchant_ids":["merchant-a"]}` contributes merchant scope `("merchant", "merchant-a")` or the existing equivalent request structure.
    - `merchant_scope={"merchant_ids":[]}` contributes no wildcard merchant scope.
    - `merchant_scope={"merchant_ids":["*"]}` is not converted into `("merchant", "*")` by current memory executor behavior.

    Only change `src/tools/executors/memory.py` if needed to preserve the existing no-wildcard behavior. Deeper memory isolation remains deferred to Phase 31.
  </action>
  <acceptance_criteria>
    - `rg -n 'merchant-a|merchant_ids.*\\[\\]|merchant_ids.*\\["\\*"\\]' tests/agent/test_tools/test_unified_tool_manager.py` finds smoke coverage.
    - `rg -n '\\("merchant", "\\*"\\)|merchant.*\\*' src/tools/executors/memory.py tests/agent/test_tools/test_unified_tool_manager.py` does not show code adding wildcard merchant memory scope as an allowed request scope.
  </acceptance_criteria>
  <verify>uv run pytest tests/agent/test_tools/test_unified_tool_manager.py -q</verify>
  <done>Memory executor behavior is covered enough for Phase 29.5 input-scope changes without pulling Phase 31 into scope.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/knowledge/test_service.py tests/test_search_integration.py -q`
- [ ] `uv run pytest tests/agent/test_tools/test_unified_tool_manager.py -q`
- [ ] `git diff --check`
</verification>

<success_criteria>
- Business merchant scope and tenant public policy scope are not conflated.
- Memory remains a deferred deeper boundary but has a Phase 29.5 smoke guard.
</success_criteria>

<output>
After completion, create `.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-04-SUMMARY.md`.
</output>
