---
phase: 29.5-merchant-scope-role-model-alignment
review: 29.5
type: code_review
status: clean
depth: standard
files_reviewed: 49
findings:
  critical: 0
  warning: 0
  info: 0
  total: 0
fixed_during_review:
  total: 1
reviewed_at: 2026-06-27
---

# Phase 29.5 Code Review

## Verdict

Clean after one review-time hardening fix. No open critical, warning, or info findings remain.

## Fixed During Review

- Service-layer approval policy still allowed `manager` through `src/approvals/policy.py` even after the API router was made admin-only. That could let a future internal `ApprovalService.decide()` caller bypass the Phase 29.5 interim approval guard. Fixed in commit `e590ee9` by setting service `APPROVAL_ROLES = {"admin"}`, migrating direct service success tests to admin, and adding a manager-denial regression in `tests/approvals/test_service_transitions.py`.

## Verification

- `uv run pytest tests/approvals tests/test_approval_models.py tests/test_approval_api.py tests/test_approval_integration.py tests/agent/test_tools/test_create_coupon_grant_draft.py -q` -> 165 passed, 6 warnings.
- `uv run ruff check src/approvals/policy.py tests/approvals/test_service_transitions.py tests/test_approval_models.py tests/agent/test_tools/test_create_coupon_grant_draft.py` -> passed.
- `uv run pytest tests/tools/test_merchant_scope_static.py -q` -> 1 passed, 1 warning.
- `uv run pytest tests/ -q --tb=short` -> 1591 passed, 1 skipped, 42 warnings.
- `git diff --check` -> passed.

## Residual Risk

- Phase 29.5 intentionally does not implement ApprovalRequest target merchant binding, ActionDraft merchant binding, BusinessFactService domain ownership proof, Memory hardening, or replay/trace same-merchant manager visibility. Those remain routed to Phase 30, Phase 31, Phase 32/35, Phase 34, and Phase 36+ deferred work.
- Whole-suite warnings are pre-existing dependency/test warnings observed during the final verification run; they did not block Phase 29.5 acceptance.
