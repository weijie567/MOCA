---
phase: 29.5-merchant-scope-role-model-alignment
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/platform/trusted_context.py
  - src/auth/permissions.py
  - tests/platform/test_trusted_context_factory.py
  - tests/platform/test_merchant_scope.py
autonomous: true
requirements: [MER-01]
must_haves:
  truths:
    - "TrustedContextFactory derives merchant-bound scope for support, manager, and legacy merchant users."
    - "Only admin derives wildcard business merchant scope as a human actor."
    - "Non-admin wildcard server_merchant_scope raises ValueError and server_tool_permissions never changes merchant scope."
  artifacts:
    - path: "src/platform/trusted_context.py"
      provides: "Trusted role-to-merchant-scope matrix"
      contains: "MERCHANT_BOUND_ROLES"
    - path: "src/auth/permissions.py"
      provides: "Unified route-level merchant helper"
      contains: "def require_merchant_access"
  key_links:
    - from: "src/platform/trusted_context.py"
      to: "tests/platform/test_trusted_context_factory.py"
      via: "role matrix and server override tests"
      pattern: "server_merchant_scope"
---

<objective>
Implement the trusted scope foundation and route-level helper for merchant-bound business access.

Purpose: All later route/tool plans must rely on one role matrix and one helper instead of per-route hand-written merchant checks.
Output: `TrustedContextFactory` implements Phase 29.5 merchant scope semantics and `src/auth/permissions.py` exports `require_merchant_access`.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-CONTEXT.md
@.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-PATTERNS.md
@.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-VALIDATION.md
@docs/contract-spec.md
@src/platform/trusted_context.py
@src/auth/permissions.py
@tests/platform/test_trusted_context_factory.py
@tests/platform/test_merchant_scope.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tighten TrustedContextFactory role matrix</name>
  <files>src/platform/trusted_context.py, tests/platform/test_trusted_context_factory.py</files>
  <read_first>src/platform/trusted_context.py, tests/platform/test_trusted_context_factory.py, docs/contract-spec.md</read_first>
  <action>
    In `src/platform/trusted_context.py`, define `MERCHANT_BOUND_ROLES = {"support", "manager", "merchant"}` and `PLATFORM_ADMIN_ROLES = {"admin"}`. Change merchant scope derivation so:
    - support/manager/merchant with `merchant_id` get exactly `[str(merchant_id)]`.
    - support/manager/merchant without `merchant_id` get `[]`.
    - admin gets `["*"]`.
    - unknown roles get `[]`.
    - non-admin human actor with `server_merchant_scope={"merchant_ids":["*"]}` raises `ValueError`.
    - non-admin server scope may only narrow to ids already in the derived base scope; it cannot add ids.
    - admin may keep wildcard or narrow to explicit ids.
    - `server_tool_permissions` remains independent and cannot mutate merchant scope.

    Rewrite `tests/platform/test_trusted_context_factory.py` so support/manager no longer assert wildcard. Add role matrix, missing merchant deny-all, unknown role deny-all, non-admin wildcard rejection, allowed narrowing, and server tool permission non-widening tests.
  </action>
  <acceptance_criteria>
    - `rg -n 'MERCHANT_BOUND_ROLES = \\{"support", "manager", "merchant"\\}|PLATFORM_ADMIN_ROLES = \\{"admin"\\}' src/platform/trusted_context.py` finds both constants.
    - `rg -n 'raise ValueError' src/platform/trusted_context.py` finds the server override rejection branch.
    - `rg -n 'assert .*merchant_scope.*\\["\\*"\\]' tests/platform/test_trusted_context_factory.py` returns only admin-specific assertions.
    - Tests cover a non-admin merchant-bound actor receiving `server_merchant_scope={"merchant_ids":["other-merchant"]}` as no-widening behavior: the factory must reject or fail closed, and must never return the added merchant id.
  </acceptance_criteria>
  <verify>uv run pytest tests/platform/test_trusted_context_factory.py tests/platform/test_trusted_context.py tests/architecture/test_trusted_context_boundaries.py -q</verify>
  <done>TrustedContextFactory implements the locked role matrix and rejects non-admin wildcard expansion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add route-level merchant access helper</name>
  <files>src/auth/permissions.py, tests/platform/test_merchant_scope.py</files>
  <read_first>src/auth/permissions.py, tests/platform/test_merchant_scope.py, docs/contract-spec.md</read_first>
  <action>
    In `src/auth/permissions.py`, add `MERCHANT_BOUND_ROLES`, `PLATFORM_ADMIN_ROLES`, and:

    ```python
    def require_merchant_access(user: User, merchant_id: object, *, resource_name: str = "resource") -> None:
        ...
    ```

    Behavior:
    - `admin` allows same-tenant business resources; the helper does not fetch or verify tenant.
    - support/manager/merchant require `user.merchant_id` and exact string equality with target `merchant_id`.
    - Missing user merchant id, target mismatch, or unknown role raises `HTTPException(status_code=403, detail={"code": FORBIDDEN, ...})` using existing API error shape.
    - The helper must not return booleans and must not perform DB queries.

    Add focused helper tests in `tests/platform/test_merchant_scope.py` or an adjacent existing platform test file.
  </action>
  <acceptance_criteria>
    - `rg -n 'def require_merchant_access|MERCHANT_BOUND_ROLES|PLATFORM_ADMIN_ROLES' src/auth/permissions.py` finds the helper and constants.
    - `rg -n 'HTTP_403_FORBIDDEN|status_code=403|FORBIDDEN' src/auth/permissions.py tests/platform/test_merchant_scope.py` finds fail-closed helper behavior and tests.
  </acceptance_criteria>
  <verify>uv run pytest tests/platform/test_merchant_scope.py -q</verify>
  <done>Routes can import one helper for merchant authorization without duplicating role checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `uv run pytest tests/platform/test_trusted_context_factory.py tests/platform/test_merchant_scope.py -q`
- [ ] `git diff --check`
</verification>

<success_criteria>
- Factory and helper implement the locked Phase 29.5 role model.
- No route migration is done in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-02-SUMMARY.md`.
</output>
