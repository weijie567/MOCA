---
phase: 29.5-merchant-scope-role-model-alignment
plan: 03
subsystem: api
tags: [merchant-scope, business-data, fastapi, raw-tools, business-facts]

requires:
  - phase: 29.5-02
    provides: TrustedContextFactory merchant scope matrix and require_merchant_access route helper
provides:
  - Order, refund case, and ticket API routes using tenant-first lookup followed by merchant access helper checks
  - Interim raw demo business-read guard for support, manager, legacy merchant, admin, and unknown roles
  - BusinessToolService denied read no-facts/no-business-ref test coverage before Phase 30
affects: [Phase 29.5 Plan 04, Phase 29.5 Plan 05, Phase 30 BusinessFactService, Phase 34 Approval ActionDraft]

tech-stack:
  added: []
  patterns: [tenant-first 404 then same-tenant merchant 403, shared raw business read guard, denied business read no-ref assertion]

key-files:
  created:
    - .planning/phases/29.5-merchant-scope-role-model-alignment/29.5-03-SUMMARY.md
  modified:
    - src/api/routers/orders.py
    - src/api/routers/refund_cases.py
    - src/api/routers/tickets.py
    - src/integrations/demo_business/authz.py
    - src/integrations/demo_business/refunds.py
    - src/integrations/demo_business/tickets.py
    - tests/integration/test_orders.py
    - tests/integration/test_refund_cases.py
    - tests/integration/test_tickets.py
    - tests/agent/test_tools/test_get_order.py
    - tests/agent/test_tools/test_get_refund_case.py
    - tests/agent/test_tools/test_get_ticket.py
    - tests/business/test_service.py

key-decisions:
  - "API routes keep tenant-first repository lookup and only call require_merchant_access after a resource is found."
  - "Raw demo business reads allow admin, require same merchant for support/manager/merchant, and deny unknown roles."
  - "Denied BusinessToolService fetch_context reads must remain insufficient with no facts, no BusinessFactRefV1, no prompt-summary refs, and no denied resource id in prompt text."

patterns-established:
  - "Business-data route pattern: tenant lookup -> 404 audit/response -> target merchant derivation -> require_merchant_access -> success audit/response."
  - "Interim raw tool guard pattern: shared merchant_can_access enforces the Phase 29.5 role matrix until Phase 30 owns domain proof."

requirements-completed: [MER-01]

duration: 34min
completed: 2026-06-27
---

# Phase 29.5 Plan 03: Business Data Merchant Guard Summary

**Order, refund, and ticket API/tool reads now fail closed for same-tenant cross-merchant business users while admin remains the only cross-merchant human role.**

## Performance

- **Duration:** 34 min
- **Started:** 2026-06-27T10:04:00Z
- **Completed:** 2026-06-27T10:38:10Z
- **Tasks:** 2
- **Files modified:** 13

## Accomplishments

- Replaced route-local legacy merchant-only branches with `require_merchant_access(...)` in order, refund case, and ticket API routes.
- Tightened `merchant_can_access(...)` for raw demo business reads: `admin` allows; `support`, `manager`, and legacy `merchant` require a matching `User.merchant_id`; missing merchant binding and unknown roles deny.
- Updated integration and raw tool tests to cover same-merchant allow, same-tenant cross-merchant 403/`FORBIDDEN`, admin cross-merchant allow, and cross-tenant 404.
- Added BusinessToolService coverage proving denied cross-merchant reads produce `insufficient` context with no facts, no business refs, no prompt-summary refs, and no denied resource id in prompt text.

## Task Commits

1. **Task 1/2 RED: Merchant business read tests** - `6656583` (test)
2. **Task 1 GREEN: Merchant-bound route/raw guards** - `c8adb5b` (feat)
3. **Task 2 hardening: Denied read no-leak assertions** - `3304b1b` (test)

## Files Created/Modified

- `src/api/routers/orders.py` - Calls `require_merchant_access(user, order.merchant_id, resource_name="orders")` after tenant-first 404.
- `src/api/routers/refund_cases.py` - Derives target merchant from order and calls the helper after tenant-first 404.
- `src/api/routers/tickets.py` - Derives target merchant from order and calls the helper after tenant-first 404.
- `src/integrations/demo_business/authz.py` - Implements the interim raw read role matrix and denies unknown roles.
- `src/integrations/demo_business/refunds.py` - Applies raw merchant guard for every role after target merchant derivation.
- `src/integrations/demo_business/tickets.py` - Applies raw merchant guard for every role after target merchant derivation.
- `tests/integration/test_orders.py` - Adds support/manager/merchant same-merchant allow, cross-merchant 403, admin allow, and cross-tenant 404 route coverage.
- `tests/integration/test_refund_cases.py` - Adds the same route coverage for refund cases.
- `tests/integration/test_tickets.py` - Adds the same route coverage for tickets.
- `tests/agent/test_tools/test_get_order.py` - Adds raw guard allow/deny/admin/missing-merchant/unknown-role coverage.
- `tests/agent/test_tools/test_get_refund_case.py` - Adds raw refund allow/deny/admin coverage and updates mock success to admin context.
- `tests/agent/test_tools/test_get_ticket.py` - Adds raw ticket allow/deny/admin coverage and updates mock success to admin context.
- `tests/business/test_service.py` - Adds denied `fetch_context()` no-facts/no-ref/no-prompt-leak assertions.

## Decisions Made

- Route 404 codes remain resource-specific (`ORDER_NOT_FOUND`, `REFUND_CASE_NOT_FOUND`, `TICKET_NOT_FOUND`) while preserving tenant-first 404 semantics.
- Raw mocked success tests without a real `User` now use `admin`, because merchant-bound roles require a DB-backed user merchant binding.
- Phase 30 still owns authoritative domain ownership proof and full BusinessFactService no-leak semantics; Plan 03 only closes the current raw seam.

## Deviations from Plan

None - plan executed within the planned API, raw integration, and test scope.

## Issues Encountered

- Initial RED tests expected generic `NOT_FOUND` for cross-tenant route checks, but the existing API contract returns resource-specific not-found codes. The tests were corrected before the RED commit so failures represented only the intended merchant-scope gap.
- One legacy raw order mock success test still used `support_agent`; it was updated to `admin` during GREEN because that test has no real DB user to prove a merchant-bound scope.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Plan 04 can validate knowledge/RAG public-policy scope without the business merchant scope leaking into public policy retrieval. Plan 05 can rely on business reads already failing closed for merchant-bound users while it tightens approvals, AgentRun, trace, replay, and investigate wildcard fallbacks.

---
*Phase: 29.5-merchant-scope-role-model-alignment*
*Completed: 2026-06-27*

## Self-Check: PASSED

- Task 1 acceptance grep passed for route helper usage, no route-local legacy branch, shared raw guard usage, and no refund/ticket raw `role == "merchant"` branch.
- Task 2 acceptance grep passed for 403/404 tests, admin tool contexts, denied no-business-ref assertions, and prompt summary no-leak assertions.
- `uv run pytest tests/integration/test_orders.py tests/integration/test_refund_cases.py tests/integration/test_tickets.py -q` passed (`26 passed`, 1 warning).
- `uv run pytest tests/agent/test_tools/test_get_order.py tests/agent/test_tools/test_get_refund_case.py tests/agent/test_tools/test_get_ticket.py tests/business/test_service.py -q` passed (`50 passed`, 1 warning).
- `uv run ruff check ...` passed for all Plan 03 source/test files.
- `git diff --check` passed before summary creation.
