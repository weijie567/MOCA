---
phase: 64.4-token-aware-policy-chunking-and-reindex-validation
plan: "17"
name: Canonical Live Recovery Manifest Issuance And Lease Gate
type: execute
wave: 17
depends_on: [64.4-16]
files_modified:
  - src/rag/evaluation/token_chunk_ab.py
  - scripts/eval_rag_token_chunk_ab.py
  - tests/eval/test_rag_token_chunk_ab.py
  - evaluation/reports/rag_token_chunk_ab/v1/recovery-budgets/README.md
  - evaluation/reports/rag_token_chunk_ab/v1/candidates/README.md
  - .planning/ARCHITECTURE-DEBT.md
  - .planning/LOCAL-VALIDATION-ISSUES.md
autonomous: true
requirements: [SC-64.4-5, SC-64.4-6]
must_haves:
  truths:
    - "One explicit production `issue-recovery-budget` project entry can create or byte-identically reconcile the sole canonical cap=2 manifest only from a strict-loaded complete candidate, exact fresh parity and matching live DB authority."
    - "Manifest issuance, every A-B reservation and normal full-provider entry capture one current `checked_at` per invocation and require `checked_at < descriptor.lease_expires_at` and `checked_at < identity.parity_expires_at` before manifest write, ordinal write or provider construction, including when a manifest already exists."
    - "The current Plan15/16 candidate is still building, so the reviewed issuance command refuses it with zero manifest/provider/reservation/A-B/pointer/DB side effects; Plan18 must finish the same candidate before issuance can succeed."
  artifacts:
    - {path: scripts/eval_rag_token_chunk_ab.py, provides: "Explicit canonical recovery-budget issuance command and shared pre-provider lease gate", contains: "issue-recovery-budget"}
    - {path: src/rag/evaluation/token_chunk_ab.py, provides: "Strict complete-candidate/parity issuance contract plus live-authority window forcing function", contains: "issue_canonical_recovery_budget_manifest"}
    - {path: evaluation/reports/rag_token_chunk_ab/v1/recovery-budgets/README.md, provides: "Production issuance, reconcile, refusal and A-B gate protocol", contains: "issue-recovery-budget"}
  key_links:
    - {from: scripts/eval_rag_token_chunk_ab.py, to: src/rag/evaluation/token_chunk_ab.py, via: "canonical root + exact candidate/parity/DB validation before manifest publication", pattern: "issue_canonical_recovery_budget_manifest"}
    - {from: src/rag/evaluation/token_chunk_ab.py, to: scripts/eval_rag_token_chunk_ab.py, via: "same checked_at lease/parity forcing function used by issuance, reservation and normal A-B startup", pattern: "recovery_authority_expired"}
---

<objective>Create and test the missing canonical manifest issuance boundary, then prove the current incomplete candidate cannot cross it.</objective>
<execution_context>@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @64.4-14-SUMMARY.md @64.4-15-SUMMARY.md @64.4-16-SUMMARY.md @64.4-RECOVERY-PLAN-DECISIONS.md @src/rag/evaluation/token_chunk_ab.py @scripts/eval_rag_token_chunk_ab.py</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement canonical manifest issuance and enforce lease/parity at every live entry</name>
  <read_first>@AGENTS.md @64.4-14-SUMMARY.md @64.4-15-SUMMARY.md @64.4-16-SUMMARY.md @src/rag/evaluation/token_chunk_ab.py @scripts/eval_rag_token_chunk_ab.py @tests/eval/test_rag_token_chunk_ab.py @evaluation/reports/rag_token_chunk_ab/v1/recovery-budgets/README.md @evaluation/reports/rag_token_chunk_ab/v1/candidates/README.md</read_first>
  <files>src/rag/evaluation/token_chunk_ab.py, scripts/eval_rag_token_chunk_ab.py, tests/eval/test_rag_token_chunk_ab.py, evaluation/reports/rag_token_chunk_ab/v1/recovery-budgets/README.md, evaluation/reports/rag_token_chunk_ab/v1/candidates/README.md, .planning/ARCHITECTURE-DEBT.md, .planning/LOCAL-VALIDATION-ISSUES.md</files>
  <behavior>
    - "`issue-recovery-budget` captures one non-operator-supplied UTC checked_at, accepts only the canonical resolved output root and canonical candidate-state path, strict-loads descriptor/state/parity, requires state complete and matching live incumbent/candidate/source/evidence DB authority, then writes or byte-identically reconciles the fixed Plan12 cap=2 manifest."
    - "The same forcing function rejects checked_at equal to or later than either absolute lease expiry or parity expiry before manifest/reservation/provider; backdated CLI timestamps cannot bypass it."
    - "Normal full-provider A-B rechecks the current lease/parity window before its existing-manifest fast path and again as part of reservation; copied/alternate roots, symlinks, stale manifest state, wrong DB row and any identity/hash mismatch refuse before provider construction."
    - "Deterministic tests and temporary PostgreSQL fixtures issue manifests only under temp roots or injected repository roots; Task1 creates no repository live manifest and performs no live provider/A-B/pointer operation."
  </behavior>
  <action>Write RED tests first. Add a domain-level `issue_canonical_recovery_budget_manifest(...)` contract that receives a caller-captured `checked_at`, repository/canonical root, exact candidate state, descriptor, parity and a source-backed live-authority proof; before any write require `checked_at < descriptor.lease_expires_at`, `checked_at < identity.lease_expires_at`, and `checked_at < identity.parity_expires_at`, state `complete`, exact candidate/config/parity/source/evidence identities, active incumbent equality, complete projection/proofs and sealed Plan10/Phase64.3 inputs. Build the existing fixed `rag_token_chunk_recovery_budget.v1`/cap=2 identity and make `write_recovery_budget_manifest_create_only` return success for byte-identical canonical replay only after parent fsync while divergent existing bytes fail closed. Add an `issue-recovery-budget` subcommand to `scripts/eval_rag_token_chunk_ab.py` with exact production command `UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/eval_rag_token_chunk_ab.py issue-recovery-budget --candidate-state <canonical-complete-state.json> --parity-report <retained-fresh-parity.json> --output-root evaluation/reports/rag_token_chunk_ab/v1`; the handler itself captures one `datetime.now(UTC)` and does not accept `--checked-at`/`--generated-at`. It must strict-load candidate/descriptor/parity, verify live DB through the same read-only `_validate_corpus_pair` authority, issue/reconcile the canonical manifest, and print only path-relative safe hash/status. Preserve the current normal full-provider argument form as the A-B command, but capture one current `checked_at` inside `main` rather than trusting `--generated-at` for authority. Require the lease/parity forcing function before loading/accepting an existing manifest, in `reserve_recovery_attempt`, and immediately before `reserve_then_create_provider`; a previously valid manifest never extends authority. Add exact tests for complete success/reconcile, incomplete refusal, equality-at-expiry, expired lease, expired parity, copied/alternate/symlink roots, state/descriptor/parity/DB mismatch, pre-existing manifest fast path and refusal before provider factory/reservation. Update both READMEs and append verified Chinese ledgers. No live command/provider/manifest in Task1. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/eval/test_rag_token_chunk_ab.py tests/eval/test_rag_retrieval_round_isolation.py tests/eval/test_rag_format_parity_report.py</automated></verify>
  <acceptance_criteria>
    - CLI help exposes `issue-recovery-budget`; the subcommand has no user-controlled authority timestamp and writes only the canonical fixed manifest after complete candidate/parity/live-DB proof.
    - Byte-identical manifest replay fsyncs its parent and returns the same SHA; conflicting bytes retain a safe create-conflict refusal.
    - Tests prove issuance, existing-manifest A-B startup and reservation all reject at `checked_at >= lease_expires_at` or `checked_at >= parity_expires_at` before ordinal or provider construction.
    - Tests prove copied/alternate/symlink roots and every candidate/descriptor/parity/source/evidence/DB mismatch leave manifest/attempt directories absent and provider-factory count zero.
    - `make format`, full `make lint` and the automated test gate exit 0 without a repository live manifest or live DB mutation.
  </acceptance_criteria>
  <done>The repository has one explicit, tested canonical issuance command and a non-bypassable current-time lease/parity gate shared by issuance and all normal A-B paths.</done>
</task>
<task type="auto">
  <name>Task 2: Prove the current building candidate cannot issue canonical A-B authority</name>
  <read_first>@64.4-15-SUMMARY.md @64.4-16-SUMMARY.md @scripts/eval_rag_token_chunk_ab.py @evaluation/reports/rag_token_chunk_ab/v1/candidates/tenants/64300000-0000-4000-8000-000000000001/runs/f8e190f1-ad0e-4476-9f77-704735e3dc46/descriptor.json @evaluation/reports/rag_token_chunk_ab/v1/candidates/tenants/64300000-0000-4000-8000-000000000001/runs/f8e190f1-ad0e-4476-9f77-704735e3dc46/states/00000002.json @evaluation/reports/rag_token_chunk_ab/v1/recovery-budgets/</read_first>
  <files>.planning/ARCHITECTURE-DEBT.md, .planning/LOCAL-VALIDATION-ISSUES.md</files>
  <action>Capture a read-only before snapshot of the retained descriptor/state/build budget, candidate DB row, active pointer/epoch/history/current view, canonical recovery-budget manifest/attempts, runs/diagnostics/selections/authorizations/activations and provider-call counter. Require Plan16 preserved the same run `f8e190f1-ad0e-4476-9f77-704735e3dc46` and corpus `64932871-4488-4b8b-b438-a02791a1151f` at exact `building/v2/index0` with zero candidate attempts/results and no canonical A-B manifest. While the absolute lease/parity window remains valid, run the exact production entry `UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/eval_rag_token_chunk_ab.py issue-recovery-budget --candidate-state evaluation/reports/rag_token_chunk_ab/v1/candidates/tenants/64300000-0000-4000-8000-000000000001/runs/f8e190f1-ad0e-4476-9f77-704735e3dc46/states/00000002.json --parity-report evaluation/reports/rag_embedding_tokenizer/v1/runs/925446ea470da4da9a0ac9aee81f9103bb4b07bd7292c761bd98a36edd749584/c760c106-7e85-440e-a56d-ed7e00eb2fb7.json --output-root evaluation/reports/rag_token_chunk_ab/v1`; never discover or substitute an alternate parity run by globbing. Require safe refusal `recovery_candidate_incomplete`. Re-run the snapshot and prove no manifest, reservation, provider, A-B, pointer/history/current-view or persistent DB side effect. If the lease/parity has expired first, require safe `recovery_authority_expired`, record the narrow-window exhaustion in both Chinese ledgers and stop the phase; do not renew, rebind or create a second candidate. Task2 must not build the candidate or issue a live manifest.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/eval/test_rag_token_chunk_ab.py tests/rag/test_policy_reindex.py tests/rag/test_policy_reindex_artifacts.py</automated></verify>
  <acceptance_criteria>
    - While authority is current, the exact production command exits with `recovery_candidate_incomplete` for building/v2/index0 and creates no canonical manifest or attempt.
    - If authority has expired, the command exits with `recovery_authority_expired` before completeness/manifest/provider boundaries and the phase stops without renewal or replacement authority.
    - Provider requests, build reservations/results, canonical A-B attempts, runs, diagnostics, selections, authorizations and activations remain unchanged.
    - Read-only DB comparison proves candidate row, active pointer/epoch/history/current view, source manifest and evidence rollout are unchanged.
    - Both Chinese ledgers record only the verified refusal/no-side-effect result and the remaining lease-window fact; Plan18 remains the sole owner of later build/issuance/A-B work.
  </acceptance_criteria>
  <done>The real incomplete candidate is proven unable to mint A-B authority with zero side effects, or expiry stops the phase truthfully before any live continuation.</done>
</task>
</tasks>

<threat_model>
| Threat | Boundary | Disposition | Mitigation |
|---|---|---|---|
| T64.4-02 Tampering | copied root/state -> new cap namespace | mitigate | Canonical resolved root/path, strict descriptor/state/parity rehash and fixed budget identity before write. |
| T64.4-05 Tampering | stale lease/parity/source/evidence -> manifest or ordinal | mitigate | One current checked_at and exact live DB authority gate at issuance, existing-manifest startup and reservation. |
| T64.4-18 Elevation | building candidate -> A-B authority/provider | mitigate | Complete-state/projection proofs precede manifest; live building refusal proves zero artifact/provider side effects. |
| T64.4-07 Disclosure | issuance/refusal -> output/ledgers | mitigate | Relative paths, hashes, status and safe reason only; no raw content, provider payload, credentials, DSN or absolute machine path. |
</threat_model>
<validation_mapping>
| Task | Gate |
|---|---|
| 64.4-17-01 | issuance/reconcile, current-time lease/parity forcing, canonical root/live DB and pre-provider refusal tests |
| 64.4-17-02 | live building-candidate issuance refusal plus zero manifest/provider/A-B/pointer/DB side-effect proof |
</validation_mapping>
<verification>Task1 is deterministic/temp-PostgreSQL only and must not issue the repository live manifest. Task2 runs only the issuance entry against the incomplete retained candidate; no provider-capable A-B invocation is allowed.</verification>
<success_criteria>SC-64.4-5/6 gain a machine-enforced canonical manifest boundary that cannot outlive lease/parity or be minted by the current incomplete candidate.</success_criteria>
<output>Create `64.4-17-SUMMARY.md`; never modify Plan15 SUMMARY, REVIEWS, prior live bytes or the retained candidate beyond Plan16's already authorized state reconciliation.</output>
