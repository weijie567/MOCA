---
phase: "64.4"
review_source: 64.4-REVIEWS.md
adjudicated_at: "2026-08-11"
status: accepted
review_loops_completed: 2
latest_external_verdict: clean
codex_independent_verdict: clean
accepted: 8
false_positive: 1
disagree: 1
defer: 0
---

# Phase 64.4 External Plan Review Decisions

Codex independently checked every Claude concern and suggestion against the live repository, CONTEXT, RESEARCH, VALIDATION, and the eleven plans. Accepted wording is implemented in the plan set; rejected suggestions retain the narrower verified ownership boundary.

| ID | External item | Decision | Repository-backed adjudication and repair |
|---|---|---|---|
| C-01 | HIGH: Plan04 could switch production before corpus isolation | **accepted** | Current `src/rag/ingestion.py` writes shared `policy_chunks`, while active corpus routing is not introduced until Plans05-06. Plan04 now keeps ordinary production on the named character-compatible default and exposes token assembly only through an explicit default-off mode; Plan06 owns active-config routing and retrieval cutover tests. |
| C-02 | MEDIUM: no canonical threat registry | **accepted** | The original RESEARCH table had descriptions but plans/VALIDATION used stable IDs. RESEARCH now defines canonical `T64.4-*` meanings, including reserved gaps; plan and validation references use that registry. |
| C-03 | MEDIUM: Plan06 production retrieval scope ambiguous | **accepted** | `src/knowledge/retrieval.py` already consumes repository APIs and does not own an arbitrary corpus selector. Plan06 now requires production repositories to resolve the tenant active pointer internally, permits explicit scope only for evaluation/reindex/history methods, and tests retrieval output across cutover without changing the production caller. |
| C-04 | MEDIUM: passing-candidate loop unbounded | **accepted** (refined) | The fixed D-23 profile must not be tuned after observing results. Plan10 caps full-provider attempts at three and permits retries only for a verified in-scope defect or transient unavailable condition. A genuine red candidate or three non-passing attempts stops with all immutable reports, prior pointer unchanged, and an explicit user/new-reviewed-config decision; the phase remains incomplete. |
| C-05 | LOW: REQUIREMENTS traceability stale | **accepted** | The existing generic ROADMAP-style identifiers collide with historical Phase64.3 labels. Phase64.4 now uses unique `SC-64.4-1..6` IDs in ROADMAP, plans, VALIDATION, and REQUIREMENTS; all six are mapped as Planned. Historical Phase64.3 artifacts are not silently rewritten. |
| C-06 | LOW: native tokenizer dependency/platform/license risk | **accepted** (refined) | Local provider research already resolved `tokenizers==0.23.1`, so dependency feasibility is not currently unverified. Plan01 nevertheless requires compatible repository-platform locks and wheel/sdist hashes before vendoring, and retains the tokenizer source, immutable revision, asset hash, and license. |
| C-07 | LOW: sealed hash field names drift | **accepted** | The Phase64.3 contract uses `manifest_hash` and `gold_hash`; Plan09 now uses those exact names while preserving the already-verified values. |
| S-01 | Explain why rollout/receipt machinery is required for a small corpus | **accepted** | Plan11 must state that generation/CAS/receipt machinery is driven by the inherited Phase64.2 immutable replay contract, not evaluation corpus size. |
| S-02 | Add post-cutover ordinary-ingestion integration test | **false_positive** (already covered) | Plan08 Task2 and `tests/rag/test_ingestion_continuity.py` already require normal ingestion before/after cutover, failed selection, rollback, and post-rollback source drift. Adding a duplicate owner would weaken rather than clarify the test boundary. |
| S-03 | Put the live 10/10 parity result into the offline count fixture | **disagree** | Offline golden counts must remain deterministic and provider-independent. Plan01 keeps safe probe labels/counts; Plan03 owns immutable live parity provenance, request classifications, and exact 10/10 evidence. Combining them would make an offline fixture a mutable live-run artifact. |

## Final Adjudication

The repaired plan preserves the accepted 11-plan/22-task granularity and strict dependency chain. No accepted item changes Phase64.4's locked 512/384/48 candidate, fixed quality thresholds, Phase64.2 identity/replay contract, or named scope exclusions. The plan must pass a fresh GSD checker and external review before implementation begins.

## Review Loop 2

- Fresh GSD plan-checker result: `## VERIFICATION PASSED`.
- Claude re-review result: no remaining actionable blocker or warning.
- Claude offered two optional LOW traceability clarifications. Codex accepted them as non-material text repairs: Plan04 now states that the character baseline DTO carries a real Plan01 tokenizer count/hash/named config for audit and A/B cost; Plan11 maps SC-64.4-3 to its actual Plan01/02/04 regression/convergence owners.
- Codex independently rechecked context/roadmap/requirements coverage, the strict 01→11 order, all task gates, corpus-free immutable identity, active/current versus history/evaluation scope, parity→selection→activation immutability, fixed three-attempt behavior, and named exclusions. No accepted issue remains.

The combined plan-review loop is clean and Phase64.4 is approved for execution.
