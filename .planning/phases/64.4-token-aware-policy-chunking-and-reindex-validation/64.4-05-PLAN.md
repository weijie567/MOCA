---
phase: 64.4-token-aware-policy-chunking-and-reindex-validation
plan: "05"
name: Corpus Schema Bootstrap And Source Identity
type: execute
wave: 5
depends_on: [64.4-04]
files_modified:
  - src/db/models.py
  - src/db/migrations/versions/030_phase64_4_token_corpora.py
  - src/repositories/policy_corpus_repo.py
  - src/repositories/document_block_repo.py
  - src/repositories/policy_chunk_repo.py
  - src/repositories/evidence_version_repo.py
  - src/rag/ingestion.py
  - tests/knowledge/test_token_corpus_migration.py
  - tests/knowledge/test_document_source_identity.py
  - tests/test_rag_migration.py
autonomous: true
requirements: [ROADMAP-SC-4, ROADMAP-SC-5]
must_haves:
  truths:
    - "Corpus rollout visibility is separate from immutable document/chunk identity through explicit projection/binding tables; corpus ID is never part of evidence identity or compatibility."
    - "Canonical document citation content and fingerprint derive from ordered ParsedBlocks/authoritative block snapshot, never joined chunks; chunk overlap/config cannot change document source identity."
    - "Legacy bootstrap is explicit character.v1, count-preserving and replay-preserving; shared current heads are not changed by candidate builds."
  artifacts:
    - {path: src/db/migrations/versions/030_phase64_4_token_corpora.py, provides: "Corpus, immutable versions, bindings, manifests, pointer/history bootstrap", contains: "character.v1"}
  key_links:
    - {from: src/repositories/policy_chunk_repo.py, to: src/db/models.py, via: "corpus projection references immutable chunk version", pattern: "corpus_chunk_binding"}
---

<objective>Add a count-safe corpus schema that preserves historical replay and separates source/document identity from chunk rollout configuration.</objective>
<execution_context>@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @64.4-04-SUMMARY.md @src/db/models.py @src/rag/ingestion.py @src/repositories/evidence_version_repo.py @docs/contract-spec.md</context>

<tasks>
<task type="auto">
  <name>Task 1: Add corpus, manifest, immutable-version and projection schema with safe bootstrap</name>
  <read_first>@src/db/models.py @src/db/migrations/versions/025_phase64_2_immutable_evidence.py @src/db/migrations/versions/029_phase64_3_rag_eval_rounds.py @src/repositories/policy_chunk_repo.py @tests/test_rag_migration.py</read_first>
  <files>src/db/models.py, src/db/migrations/versions/030_phase64_4_token_corpora.py, src/repositories/policy_corpus_repo.py, tests/knowledge/test_token_corpus_migration.py, tests/test_rag_migration.py</files>
  <action>Wave0 tests first. Add tenant corpus generation/config fingerprint, append-only source-manifest revisions, one rollout pointer/epoch, append-only activation history, current and immutable chunk audit fields for config fingerprint/exact-input hash/token count, ingestion-job count/usage audit fields, and explicit corpus-document/corpus-block/corpus-chunk projection bindings. Bootstrap all existing rows into one active named `character.v1` corpus using `CharacterCompatibilityAssembler` semantics and existing identifiers; require before/after per-tenant counts, no orphan/duplicate, exact current visibility and downgrade refusal after token rows. Corpus ID controls visibility only and is absent from immutable evidence/document/chunk identity hashes. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/knowledge/test_token_corpus_migration.py tests/test_rag_migration.py</automated></verify>
  <done>Migration creates explicit rollout projections and a truthful legacy bootstrap without rewriting history.</done>
</task>
<task type="auto">
  <name>Task 2: Version canonical source/document identity independently of chunks</name>
  <read_first>@src/rag/ingestion.py @src/repositories/document_block_repo.py @src/repositories/policy_chunk_repo.py @src/repositories/evidence_version_repo.py @tests/replay/test_production_evidence_binding.py</read_first>
  <files>src/rag/ingestion.py, src/repositories/document_block_repo.py, src/repositories/policy_chunk_repo.py, src/repositories/evidence_version_repo.py, tests/knowledge/test_document_source_identity.py</files>
  <action>Replace `_document_citation_text(chunks)` authority for new writes with versioned `canonical_document_content.v2` computed from ordered ParsedBlocks/authoritative block snapshot before chunking. Preserve legacy v1 fingerprints/readability explicitly; do not silently rewrite them. Wire the exact count/input-hash/config values carried by Plan04's DTO into the migration 030 current, immutable and ingestion-job audit columns in the same persistence transaction. Immutable document reuse compares tenant/source checksum plus canonical block content/provenance/schema and ignores chunk config/corpus. Chunk-version compatibility separately compares canonical citation/search/input/provenance plus tokenizer/model/assembly fingerprint and count, but ignores corpus ID. Tests prove the same source under character/token config reuses the document/evidence-document identity while compatible chunks can bind across corpora and incompatible chunk config creates a new chunk version. Candidate paths never change shared current head. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/knowledge/test_document_source_identity.py tests/replay/test_production_evidence_binding.py</automated></verify>
  <done>Document source identity is boundary-independent; chunk identity is configuration-aware; both remain replayable.</done>
</task>
</tasks>

<threat_model>
| Threat | Boundary | Mitigation |
|---|---|---|
| T64.4-01 Information Disclosure | inactive corpus -> current query | Explicit projection joined through single pointer. |
| T64.4-03 Tampering | rollout/config -> immutable identity | Corpus excluded from identity; document/chunk compatibility versioned separately. |
</threat_model>
<validation_mapping>
| Task | Gate |
|---|---|
| 64.4-05-01 | migration/bootstrap/count/visibility tests |
| 64.4-05-02 | document source identity and chunk compatibility/replay tests |
</validation_mapping>
<verification>Run migration against PostgreSQL and both task gates; no SQLite-only conclusion.</verification>
<success_criteria>D-14..D-16 and SC4 schema foundations are preserved without source identity drift.</success_criteria>
<output>Create `64.4-05-SUMMARY.md`.</output>
