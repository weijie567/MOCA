---
phase: 64.4-token-aware-policy-chunking-and-reindex-validation
plan: "01"
name: Pinned Tokenizer Contract And Assets
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/config.py
  - src/rag/embedding_tokenizer.py
  - src/rag/assets/embedding_tokenizer.v1.json
  - src/rag/assets/qwen3_embedding_0_6b/tokenizer.json
  - src/rag/assets/qwen3_embedding_0_6b/SOURCE.json
  - src/rag/assets/qwen3_embedding_0_6b/LICENSE
  - evaluation/golden/embedding_tokenizer_count_probes.v1.json
  - tests/rag/test_embedding_tokenizer.py
autonomous: true
requirements: [SC-64.4-1]
must_haves:
  truths:
    - "One immutable embedding_tokenizer.v1 contract pins model, dimensions, official tokenizer revision/hash/runtime, EOS behavior, 8192/10 provider limits, and 512/384/48 assembly values."
    - "Offline exact counting is deterministic, network-free, and fails closed on unknown contract, asset/runtime drift, count failure, or nondeterminism; it never silently falls back to characters."
  artifacts:
    - {path: src/rag/embedding_tokenizer.py, provides: "Frozen config/fingerprint and offline counter", contains: "EmbeddingTokenizerConfigV1"}
    - {path: src/rag/assets/qwen3_embedding_0_6b/tokenizer.json, provides: "Pinned official tokenizer asset", contains: "model"}
  key_links:
    - {from: src/rag/embedding_tokenizer.py, to: src/rag/assets/embedding_tokenizer.v1.json, via: "strict config load and canonical SHA-256", pattern: "config_fingerprint"}
---

<objective>Pin and test the deterministic offline tokenizer authority before any assembly, provider, or persistence path depends on it.</objective>
<execution_context>@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @.planning/ROADMAP.md @.planning/phases/64.4-token-aware-policy-chunking-and-reindex-validation/64.4-CONTEXT.md @.planning/phases/64.4-token-aware-policy-chunking-and-reindex-validation/64.4-RESEARCH.md @src/config.py @pyproject.toml</context>

<tasks>
<task type="auto">
  <name>Task 1: Vendor the exact asset and freeze the configuration</name>
  <read_first>@pyproject.toml @uv.lock @src/config.py @.planning/phases/64.4-token-aware-policy-chunking-and-reindex-validation/64.4-RESEARCH.md</read_first>
  <files>pyproject.toml, uv.lock, src/config.py, src/rag/assets/embedding_tokenizer.v1.json, src/rag/assets/qwen3_embedding_0_6b/tokenizer.json, src/rag/assets/qwen3_embedding_0_6b/SOURCE.json, src/rag/assets/qwen3_embedding_0_6b/LICENSE, evaluation/golden/embedding_tokenizer_count_probes.v1.json, tests/rag/test_embedding_tokenizer.py</files>
  <action>Write Wave-0 contract tests first. Resolve and pin `tokenizers==0.23.1` before vendoring; require compatible locked wheels for the repository's development/CI platforms and retain `uv.lock` sdist/wheel hashes, failing before asset changes if resolution is unsupported. Vendor the official Qwen asset at revision `97b0c614be4d77ee51c0cef4e5f07c00f9eb65b3`, size 11,423,705 and SHA-256 `def76fb086971c7867b829c23a26261e38d9d74e02139253b38aeb9df8b4b50a`; retain source/license and state that mapping to DashScope is empirical, not vendor-guaranteed. Freeze provider/model/dimensions, add_special_tokens=true, 8192/10, 512/384/48 and assembly schema. The fixture contains only safe labels and multilingual/table/URL/number/emoji/whitespace probes; live provider result provenance remains exclusively in Plan03 parity run artifacts. Finish Python-affecting dependency/config work by running `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/rag/test_embedding_tokenizer.py</automated></verify>
  <done>Asset provenance and immutable values are exact, tested, offline, and auditable.</done>
</task>
<task type="auto">
  <name>Task 2: Implement the deterministic fail-closed counter</name>
  <read_first>@src/rag/assets/embedding_tokenizer.v1.json @tests/rag/test_embedding_tokenizer.py @src/config.py</read_first>
  <files>src/rag/embedding_tokenizer.py, tests/rag/test_embedding_tokenizer.py</files>
  <action>Implement frozen `EmbeddingTokenizerConfigV1`, canonical sorted-key fingerprint, hash/runtime verification, and `EmbeddingTokenCounter.count(text)` using `encode(..., add_special_tokens=True)`. Verify repeat determinism and positive counts. Typed safe failures cover unknown contract, missing/changed asset, runtime mismatch, failed/invalid/nondeterministic count and parity unavailable/quarantine; no path, raw exception, download, character fallback, provider call, or persistence is allowed. Test EOS-inclusive counts and one-token special-token delta for all probes. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/rag/test_embedding_tokenizer.py</automated></verify>
  <done>The only token counter is deterministic, exact for the pinned contract, and fail-closed before side effects.</done>
</task>
</tasks>

<threat_model>
| Threat | Boundary | Mitigation |
|---|---|---|
| T64.4-04 Tampering | asset/runtime/config -> count | Exact bytes, runtime, canonical fingerprint, repeated counts, typed failure before side effects. |
| T64.4-07 Disclosure | tokenizer failure -> logs | Allowlisted codes only; no text, path, exception, secret, or raw payload. |
</threat_model>
<validation_mapping>
| Task | Gate |
|---|---|
| 64.4-01-01 | `make lint` plus tokenizer asset/config tests |
| 64.4-01-02 | `make lint` plus offline determinism/failure tests |
</validation_mapping>
<verification>Run both task gates through the MOCA uv environment; never use bare pytest/Python.</verification>
<success_criteria>SC-64.4-1 and D-01, D-02, D-04, D-05 are covered without provider or chunking scope.</success_criteria>
<output>Create `64.4-01-SUMMARY.md`.</output>
