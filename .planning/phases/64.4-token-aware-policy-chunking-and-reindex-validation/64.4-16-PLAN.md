---
phase: 64.4-token-aware-policy-chunking-and-reindex-validation
plan: "16"
name: Deterministic Claim-State Publication And Exact Candidate Recovery
type: execute
wave: 16
depends_on: [64.4-15]
files_modified:
  - scripts/reindex_policies.py
  - src/rag/policy_reindex_artifacts.py
  - evaluation/reports/rag_token_chunk_ab/v1/candidates/README.md
  - evaluation/reports/rag_token_chunk_ab/v1/candidates/tenants/64300000-0000-4000-8000-000000000001/runs/f8e190f1-ad0e-4476-9f77-704735e3dc46/states/
  - tests/rag/test_policy_reindex.py
  - tests/rag/test_policy_reindex_artifacts.py
  - .planning/ARCHITECTURE-DEBT.md
  - .planning/LOCAL-VALIDATION-ISSUES.md
autonomous: true
requirements: [SC-64.4-5]
must_haves:
  truths:
    - "Every future reviewed claim publishes the committed claimed/v1/index0 state before the separate resume transaction may commit and publish building/v2/index0; replay and every injected crash converge to the same contiguous immutable sequence."
    - "The sole Plan15 candidate may acquire a claimed/v1 predecessor only when DB truth is exactly building/v2/index0 for the same descriptor/run/corpus and every other identity field is byte-equivalent; no other missing predecessor is inferred."
    - "The live repair reuses descriptor f8e190f1-ad0e-4476-9f77-704735e3dc46 and candidate 64932871-4488-4b8b-b438-a02791a1151f only after lease, parity, source, evidence, DB, state and zero-budget facts still match, and it causes no provider, reservation, A-B, selection, activation, pointer/history or persistent DB mutation."
  artifacts:
    - {path: scripts/reindex_policies.py, provides: "Ordered reviewed claim publication and exact missing-predecessor recovery", contains: "_claim_reviewed"}
    - {path: src/rag/policy_reindex_artifacts.py, provides: "Durable byte-identical replay of immutable state publication", contains: "_publish_canonical_bytes"}
    - {path: evaluation/reports/rag_token_chunk_ab/v1/candidates/tenants/64300000-0000-4000-8000-000000000001/runs/f8e190f1-ad0e-4476-9f77-704735e3dc46/states/00000001.json, provides: "Exact claimed/v1 predecessor for the retained Plan15 building/v2 identity", contains: "\"state\":\"claimed\""}
  key_links:
    - {from: scripts/reindex_policies.py, to: src/rag/policy_reindex_artifacts.py, via: "commit claimed/v1 -> publish v1 -> separate recover/resume commit -> publish v2", pattern: "write_policy_reindex_state_create_only"}
    - {from: scripts/reindex_policies.py, to: src/rag/policy_reindex.py, via: "read-only descriptor-bound recover_identity before any exact predecessor reconciliation", pattern: "recover_identity"}
---

<objective>Repair the claim/state composition deterministically, then reconcile only the retained Plan15 candidate without broadening live authority.</objective>
<execution_context>@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @64.4-13-SUMMARY.md @64.4-14-SUMMARY.md @64.4-15-SUMMARY.md @64.4-RECOVERY-PLAN-DECISIONS.md @scripts/reindex_policies.py @src/rag/policy_reindex_artifacts.py</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Publish future claim states in order and permit only the exact v2/index0 predecessor repair</name>
  <read_first>@AGENTS.md @64.4-15-SUMMARY.md @scripts/reindex_policies.py @src/rag/policy_reindex.py @src/rag/policy_reindex_artifacts.py @src/repositories/policy_corpus_repo.py @tests/rag/test_policy_reindex.py @tests/rag/test_policy_reindex_artifacts.py @evaluation/reports/rag_token_chunk_ab/v1/candidates/README.md</read_first>
  <files>scripts/reindex_policies.py, src/rag/policy_reindex_artifacts.py, evaluation/reports/rag_token_chunk_ab/v1/candidates/README.md, tests/rag/test_policy_reindex.py, tests/rag/test_policy_reindex_artifacts.py, .planning/ARCHITECTURE-DEBT.md, .planning/LOCAL-VALIDATION-ISSUES.md</files>
  <behavior>
    - "A new descriptor-bound claim commits claimed/state_version=1/next_document_index=0, publishes 00000001.json, then uses a new transaction to recover and resume the same row before committing and publishing building/state_version=2/next_document_index=0."
    - "Crash or injected failure after either DB commit or either file publication replays to the same descriptor/run/corpus with contiguous v1,v2 bytes; it never creates a second row, renews the lease, or skips an ordinal."
    - "A missing v1 may be reconstructed only from exact current building/v2/index0 plus the same descriptor-bound fields; claimed/v1 is derived by changing only state and state_version. Missing history for v3+, nonzero index, built/validating/complete, or any mismatched identity refuses."
    - "The reviewed recover-state entry rechecks its own current timestamp against both the descriptor's absolute lease and parity expiry before any artifact write; the separate operator preflight cannot be the sole freshness enforcement."
    - "An already-existing byte-identical state file is successful only after fsync of its parent directory in both the pre-existence and FileExists race paths; conflict bytes remain fail-closed."
  </behavior>
  <action>Write RED composition and fault tests first. Refactor `_claim_reviewed` so it cannot call `claim_from_descriptor` and `resume` in one transaction: transaction A commits the exact `claimed`, version `1`, index `0` identity; `write_policy_reindex_state_create_only` durably publishes `00000001.json`; transaction B opens a new session, calls descriptor-bound `recover_identity`, compares it to the published v1, resumes the same row, commits exact `building`, version `2`, index `0`, then publishes `00000002.json` before reconciling the already descriptor-bound build-budget manifest. On replay, accept `claimed/v1/index0` to finish v1->resume->v2, or accept `building/v2/index0` only when strict v1 already exists to replay v2; `claim-reviewed` itself must refuse a missing v1 on the historical building/v2 shape. Modify the existing `_recover_state` command handler and add a distinct `_derive_exact_initial_claim_predecessor` helper for that Plan15 shape: capture one UTC `checked_at`, require it is strictly earlier than both `descriptor.lease_expires_at` and `descriptor.parity_expires_at` before any artifact write, then call exact DB recovery. Only when DB returns the same descriptor/run/corpus as an existing canonical v2 whose state is `building`, state_version is `2`, next_document_index is `0`, and v1 is absent may it derive v1 by changing only `state=claimed` and `state_version=1`; publish v1 first and byte-identically replay v2. Later current versions may publish only when every earlier state file is already contiguous; never infer a missing predecessor for v3+, nonzero index or any other state. Reject every other gap or divergent field. In `_publish_canonical_bytes`, fsync `path.parent` before returning from either byte-identical replay branch and fault-test that durability boundary; do not weaken no-replace/conflict behavior. Add end-to-end composition tests around `_claim_reviewed`, `_recover_state`, in-command expiry, state sequencing, DB-commit/file-publication faults and re-entry. Update the candidate recovery README and append only verified Chinese ledger entries. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/rag/test_policy_reindex_artifacts.py tests/rag/test_policy_reindex.py</automated></verify>
  <acceptance_criteria>
    - `scripts/reindex_policies.py` contains two separate transaction scopes for new reviewed claim publication, with `00000001` logically published before the resume transaction can produce `00000002`.
    - Focused tests prove recovery at every claim/v1/resume/v2 boundary yields one DB row and the exact contiguous `[00000001.json, 00000002.json]` sequence.
    - Tests prove the missing-predecessor helper accepts only building/v2/index0 and rejects state_version 3, next_document_index 1, terminal states and any descriptor/run/corpus/config/parity/source/evidence mismatch.
    - Tests prove lease or parity expiry makes `_recover_state` refuse before creating/replaying any state or budget artifact, even when an external preflight was previously green.
    - Tests prove byte-identical replay fsyncs the state parent before return and divergent bytes still raise the existing safe conflict code.
    - The automated gate exits 0 after `make format` has normalized all Python edits.
  </acceptance_criteria>
  <done>Future claims publish v1 then v2 durably, while the one historical v2/index0 gap has a deterministic, non-generalized recovery rule backed by composition and fault tests.</done>
</task>
<task type="auto">
  <name>Task 2: Reconcile the retained Plan15 candidate with zero live-budget or DB side effects</name>
  <read_first>@64.4-15-SUMMARY.md @scripts/reindex_policies.py @evaluation/reports/rag_token_chunk_ab/v1/candidates/tenants/64300000-0000-4000-8000-000000000001/runs/f8e190f1-ad0e-4476-9f77-704735e3dc46/descriptor.json @evaluation/reports/rag_token_chunk_ab/v1/candidates/tenants/64300000-0000-4000-8000-000000000001/runs/f8e190f1-ad0e-4476-9f77-704735e3dc46/states/00000002.json @evaluation/reports/rag_token_chunk_ab/v1/candidates/tenants/64300000-0000-4000-8000-000000000001/runs/f8e190f1-ad0e-4476-9f77-704735e3dc46/build-budget/manifest.json @evaluation/reports/rag_token_chunk_ab/v1/recovery-budgets/ @evaluation/reports/rag_token_chunk_ab/v1/selections/ @evaluation/reports/rag_token_chunk_ab/v1/recovery-authorizations/ @evaluation/reports/rag_token_chunk_ab/v1/activations/</read_first>
  <files>evaluation/reports/rag_token_chunk_ab/v1/candidates/tenants/64300000-0000-4000-8000-000000000001/runs/f8e190f1-ad0e-4476-9f77-704735e3dc46/states/, .planning/ARCHITECTURE-DEBT.md, .planning/LOCAL-VALIDATION-ISSUES.md</files>
  <action>Before invoking any recovery command, capture a read-only before snapshot of descriptor/state/build-budget bytes; candidate attempts/results; canonical A-B manifest/attempts/runs/diagnostics/selections/authorizations/activations; and the evaluation tenant's candidate row, active pointer, rollout epoch, history, current-view counts, source manifest and evidence rollout. Require exactly the retained descriptor run `f8e190f1-ad0e-4476-9f77-704735e3dc46`, corpus `64932871-4488-4b8b-b438-a02791a1151f`, DB and canonical state `building/v2/index0`, only `00000002.json`, matching descriptor/config/parity/source/evidence fields, an unexpired absolute lease and parity report, the byte-identical existing cap=2 build manifest, zero candidate attempts/results, absent canonical A-B manifest, zero A-B attempts and zero selection/authorization/activation evidence. If any fact differs, append the verified Chinese issue and stop without recovery, provider construction, reservation or mutation. If all facts match, invoke only `UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/reindex_policies.py recover-state --artifact-root evaluation/reports/rag_token_chunk_ab/v1/candidates --tenant-id 64300000-0000-4000-8000-000000000001 --run-token f8e190f1-ad0e-4476-9f77-704735e3dc46`. Require it to create canonical `00000001.json` then byte-identically replay `00000002.json` and the existing build manifest. Re-run the snapshot and prove no provider request, build reservation/result, A-B artifact, pointer/history/current-view/source/evidence or persistent DB field changed. Do not call `claim-reviewed`, `build-next-reviewed`, the A-B CLI, activation, lease renewal, parity regeneration or any compatibility writer.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/rag/test_policy_reindex_artifacts.py tests/rag/test_policy_reindex.py tests/knowledge/test_document_source_identity.py tests/replay/test_production_evidence_binding.py</automated></verify>
  <acceptance_criteria>
    - The retained run directory contains exactly canonical contiguous `states/00000001.json` and `states/00000002.json`; strict loaders bind both to descriptor SHA `sha256:60940d6b1c202bcf4137bbfa79cfae1ffccb75fbdc9543891ae021ee4ecffa5d` and the same run/corpus.
    - v1 is `claimed/state_version=1/next_document_index=0`; v2 remains byte-identical `building/state_version=2/next_document_index=0` for corpus `64932871-4488-4b8b-b438-a02791a1151f`.
    - Candidate attempt/result counts remain `0/0`; canonical A-B remains manifest absent and `0/2`; runs, selections, recovery authorizations and activations gain zero files.
    - A before/after read-only DB comparison proves the candidate row, active corpus, rollout epoch/history, current view, source manifest and evidence rollout are identical.
    - If lease, parity or any exact authority check is no longer valid, no recovery artifact is written and Plan17 remains blocked with the reason recorded in both Chinese ledgers.
  </acceptance_criteria>
  <done>The exact retained candidate has a canonical contiguous v1/v2 state sequence with zero provider, reservation, A-B, activation, pointer/history or persistent DB side effect; otherwise the plan stops truthfully.</done>
</task>
</tasks>

<threat_model>
| Threat | Boundary | Disposition | Mitigation |
|---|---|---|---|
| T64.4-02 Tampering | inferred predecessor -> foreign/advanced candidate | mitigate | Accept only the one descriptor-bound building/v2/index0 identity and field-for-field derived claimed/v1; all other gaps refuse. |
| T64.4-03 Tampering | DB commit -> state publication/replay | mitigate | Separate transactions, ordered canonical publication, byte comparison and parent-directory fsync on identical replay. |
| T64.4-05 Tampering | stale lease/parity/source/evidence -> live recovery | mitigate | Exact preflight with unexpired authority and same DB truth before writing v1. |
| T64.4-18 Elevation | repair path -> provider/A-B/activation authority | mitigate | Plan16 forbids provider construction, reservations, A-B, selection, activation and persistent DB mutation; before/after counters prove zero. |
| T64.4-07 Disclosure | recovery evidence -> logs/ledgers | mitigate | Record only allowlisted IDs, hashes, counts and safe codes; no key, DSN, raw content, payload or absolute secret-bearing path. |
</threat_model>
<validation_mapping>
| Task | Gate |
|---|---|
| 64.4-16-01 | ordered claim composition, exact predecessor refusal, identical-replay fsync and fault-injection tests |
| 64.4-16-02 | exact retained-candidate recovery plus zero provider/reservation/A-B/DB-mutation before/after proof |
</validation_mapping>
<verification>Deterministic code/fault/PostgreSQL tests must pass first. Live Task2 is limited to exact state-artifact reconciliation and is complete only with the stated no-side-effect proof; any expired or drifted authority is a truthful blocking stop.</verification>
<success_criteria>SC-64.4-5 regains a contiguous crash-safe state authority for the same Plan15 candidate without claiming or authorizing any new live work.</success_criteria>
<output>Create `64.4-16-SUMMARY.md`; preserve `64.4-15-SUMMARY.md`, all earlier plans/summaries and every prior live artifact byte except the newly justified `states/00000001.json`.</output>
