---
phase: 64.4-token-aware-policy-chunking-and-reindex-validation
plan: "13"
name: Reversible Activation Drill And Closeout Guard
type: execute
wave: 13
depends_on: [64.4-12]
files_modified:
  - scripts/check_phase64_4_closeout.py
  - evaluation/reports/rag_token_chunk_ab/v1/activations/
  - tests/rag/test_phase64_4_closeout.py
  - .planning/ARCHITECTURE-DEBT.md
  - .planning/LOCAL-VALIDATION-ISSUES.md
autonomous: true
requirements: [SC-64.4-5, SC-64.4-6]
must_haves:
  truths:
    - "Activation is impossible without strict-loadable fresh parity, machine-budgeted selected_pass report and immutable selection SHA; deterministic fixtures or diagnostics never authorize pointer mutation."
    - "The evaluation tenant completes selected cutover -> source-compatible prior rollback -> selected restoration with exactly three new history events and three create-only hash-chained receipts; final authority is the selected token corpus."
    - "A machine closeout guard refuses live-complete/Nyquist claims unless the real selection, all three receipts, pointer/history proof and artifact hashes resolve; missing evidence blocks Plan14."
  artifacts:
    - {path: evaluation/reports/rag_token_chunk_ab/v1/activations/, provides: "Three selected activation receipts chained to DB history", contains: "rag_token_chunk_activation.v1"}
    - {path: scripts/check_phase64_4_closeout.py, provides: "Fail-closed live selection/receipt/pointer closeout guard", contains: "require-live-complete"}
  key_links:
    - {from: evaluation/reports/rag_token_chunk_ab/v1/selections/, to: evaluation/reports/rag_token_chunk_ab/v1/activations/, via: "exact selection SHA readiness then three committed CAS events", pattern: "selection_decision_sha256"}
    - {from: scripts/check_phase64_4_closeout.py, to: .planning/phases/64.4-token-aware-policy-chunking-and-reindex-validation/64.4-VALIDATION.md, via: "nyquist/live-complete artifact guard", pattern: "nyquist_compliant"}
---

<objective>Prove the selected corpus can be activated, rolled back and restored without ambiguity, then make a false phase closeout machine-detectable.</objective>
<execution_context>@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @64.4-10-SUMMARY.md @64.4-11-SUMMARY.md @64.4-12-SUMMARY.md @src/rag/policy_reindex.py @src/rag/activation_receipt.py</context>

<tasks>
<task type="auto">
  <name>Task 1: Add a fail-closed live closeout evidence guard</name>
  <read_first>@src/rag/activation_receipt.py @src/rag/evaluation/token_chunk_ab.py @evaluation/reports/rag_token_chunk_ab/v1/selections/ @.planning/phases/64.4-token-aware-policy-chunking-and-reindex-validation/64.4-VALIDATION.md</read_first>
  <files>scripts/check_phase64_4_closeout.py, tests/rag/test_phase64_4_closeout.py, .planning/ARCHITECTURE-DEBT.md, .planning/LOCAL-VALIDATION-ISSUES.md</files>
  <action>Wave0 tests first. Add a read-only closeout checker that strict-loads fresh parity, Plan10 retained failures, the Plan12 budget manifest/reservations, selected_pass run and immutable selection, then validates exactly three ordered activation receipts and their selection/prior-receipt hashes. `--require-live-complete` always requires the real selection/receipt chain and final selected pointer/history proof; default mode still fails whenever VALIDATION says `nyquist_compliant: true` but those artifacts are absent or mismatched. Deterministic fixtures, diagnostics, Markdown-only evidence, missing DB proof and receipt gaps cannot satisfy it. Tests use temporary artifact trees plus PostgreSQL fixtures, include false-green VALIDATION cases, hash tampering, receipt reordering, stale pointer and correct chain. The checker never writes pointer/history/artifacts. Record verified defects/incidents in the required Chinese ledgers. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/rag/test_phase64_4_closeout.py tests/rag/test_activation_receipt.py tests/rag/test_policy_reindex.py</automated></verify>
  <done>A test-backed read-only guard fails before any false live-complete or Nyquist claim and accepts only the real selected pointer plus exact three-receipt chain.</done>
</task>
<task type="auto">
  <name>Task 2: Execute cutover rollback restoration and receipt chain</name>
  <read_first>@64.4-12-SUMMARY.md @src/rag/policy_reindex.py @src/rag/activation_receipt.py @scripts/reindex_policies.py @scripts/check_phase64_4_closeout.py @tests/knowledge/test_evidence_cutover.py @tests/replay/test_production_evidence_binding.py</read_first>
  <files>evaluation/reports/rag_token_chunk_ab/v1/activations/, .planning/ARCHITECTURE-DEBT.md, .planning/LOCAL-VALIDATION-ISSUES.md</files>
  <action>Re-hash fresh parity, selected_pass report, budget reservation and immutable selection; verify candidate completeness/source/config and exact pointer/evidence epochs. On migrated evaluation PostgreSQL perform selected cutover, query the exact token view, source-compatible prior rollback, query the exact prior view, and selected restoration. After each committed CAS, use a new session to prove exactly-one pointer/history row and write or reconcile the create-only receipt; require three ordered new events and previous-receipt chaining. Prove stale CAS no-op, ordinary ingestion/source-stale guard, legacy replay and selection/parity byte immutability. Run `scripts/check_phase64_4_closeout.py --require-live-complete`; final pointer/view must be the selected token corpus. Any failure updates the Chinese ledgers immediately and stops for a separately reviewed repair—no unplanned product edit or additional provider call.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/rag/test_phase64_4_closeout.py tests/rag/test_activation_receipt.py tests/rag/test_policy_reindex.py tests/knowledge/test_evidence_cutover.py tests/replay/test_production_evidence_binding.py</automated></verify>
  <done>Real selected token authority is final after the reversible drill, with three committed history events, three valid hash-chained receipts and a passing live closeout guard.</done>
</task>
</tasks>

<threat_model>
| Threat | Boundary | Mitigation |
|---|---|---|
| T64.4-18 Elevation | nonpassing/diagnostic evidence -> cutover | Strict selected_pass + budget + selection SHA readiness before any CAS. |
| T64.4-19 Repudiation | pointer event -> activation evidence | New-session DB recheck plus three create-only receipts chained to selection and prior receipt. |
| T64.4-20 Tampering | validation flag -> false completion | Read-only closeout guard rejects missing/hash-invalid artifacts and stale pointer/history. |
</threat_model>
<validation_mapping>
| Task | Gate |
|---|---|
| 64.4-13-01 | false-closeout, hash, receipt order and live pointer guard tests |
| 64.4-13-02 | real cutover/rollback/restore, receipt chain, replay/current-view and closeout proof |
</validation_mapping>
<verification>Real PostgreSQL selection/pointer/history/receipt evidence is mandatory; deterministic fixtures cannot satisfy Task2.</verification>
<success_criteria>SC-64.4-5/6 and D-19/D-24 pass with final selected token authority, reversible history and machine-enforced closeout honesty.</success_criteria>
<output>Create `64.4-13-SUMMARY.md`; never modify Plan10 failure evidence.</output>
