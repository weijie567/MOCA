---
phase: 64.4-token-aware-policy-chunking-and-reindex-validation
plan: "13"
name: Crash-Safe Candidate Recovery And Build Budget
type: execute
wave: 13
depends_on: [64.4-12]
files_modified:
  - src/rag/policy_reindex_artifacts.py
  - src/rag/policy_reindex.py
  - src/rag/embedder.py
  - scripts/reindex_policies.py
  - evaluation/reports/rag_token_chunk_ab/v1/candidates/README.md
  - tests/rag/test_policy_reindex_artifacts.py
  - tests/rag/test_policy_reindex.py
  - tests/test_embedder.py
  - .planning/ARCHITECTURE-DEBT.md
  - .planning/LOCAL-VALIDATION-ISSUES.md
autonomous: true
requirements: [SC-64.4-5]
must_haves:
  truths:
    - "A create-only pre-claim descriptor fixes tenant/run/generation/owner/absolute lease expiry/fresh parity/config/source/evidence identity before DB claim; recover-state resolves only that run from DB and can never mint or renew a second candidate."
    - "Every claim/build/validate state artifact is staged, fsynced and atomically published; a crash after DB commit but before file publication recovers the exact current DB identity from the descriptor with byte-conflict refusal."
    - "Candidate build has a create-only per-document provider-execution budget of two build invocations, reserved before provider construction; crashes consume an ordinal, internal provider retries are disabled, and exhaustion stops with both Chinese ledgers updated."
  artifacts:
    - {path: src/rag/policy_reindex_artifacts.py, provides: "Descriptor/state/recovery/build-budget immutable artifact contracts", contains: "PolicyReindexRecoveryDescriptorV1"}
    - {path: evaluation/reports/rag_token_chunk_ab/v1/candidates/README.md, provides: "Operator protocol for one crash-safe bounded candidate", contains: "recover-state"}
  key_links:
    - {from: scripts/reindex_policies.py, to: src/rag/policy_reindex.py, via: "descriptor-bound claim and strict recover_identity by tenant/run", pattern: "recover-state"}
    - {from: src/rag/policy_reindex_artifacts.py, to: src/rag/embedder.py, via: "reserve build execution before EmbeddingService(max_retries=1)", pattern: "reserve_candidate_build_attempt"}
---

<objective>Close the candidate rebuild crash/retry gaps before any new live candidate is claimed or provider call occurs.</objective>
<execution_context>@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @64.4-12-SUMMARY.md @scripts/reindex_policies.py @src/rag/policy_reindex.py @src/repositories/policy_corpus_repo.py</context>

<tasks>
<task type="auto">
  <name>Task 1: Seal one candidate descriptor and crash-safe state recovery</name>
  <read_first>@scripts/reindex_policies.py @src/rag/policy_reindex.py @src/repositories/policy_corpus_repo.py @src/db/models.py @tests/rag/test_policy_reindex.py</read_first>
  <files>src/rag/policy_reindex_artifacts.py, src/rag/policy_reindex.py, scripts/reindex_policies.py, evaluation/reports/rag_token_chunk_ab/v1/candidates/README.md, tests/rag/test_policy_reindex_artifacts.py, tests/rag/test_policy_reindex.py, .planning/ARCHITECTURE-DEBT.md, .planning/LOCAL-VALIDATION-ISSUES.md</files>
  <action>Wave0 tests first. Define frozen `policy_reindex_recovery_descriptor.v1` with descriptor payload hash, tenant, fixed run token/generation, lease owner, absolute lease expiry (maximum two hours), exact fresh parity file SHA/config/probe/content/capture/expiry, source manifest/id/revision/hash, source active corpus/epoch and evidence rollout. Publish it create-only via staging, file+directory fsync and atomic no-replace; conflicts fail closed. Add descriptor-consuming claim that never recomputes UUID/lease identity. Add `PolicyReindexService.recover_identity` plus CLI `recover-state`: lock exact tenant/run, require exactly one row and every descriptor field/source authority, return the current DB identity without renewal or mutation. Replace direct state writes with canonical JSON staging/fsync/atomic create-only publication; identical replay is idempotent, truncation/conflict fails. Fault-inject descriptor/state publication and every DB-commit-before-state-publish boundary for claim/build/validate; prove recover-state always returns the same corpus/run/version and a different run cannot be created through the descriptor. Existing unreviewed CLI modes remain backward-compatible but are not accepted by the reviewed rebuild path. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/rag/test_policy_reindex_artifacts.py tests/rag/test_policy_reindex.py</automated></verify>
  <done>One immutable descriptor owns exactly one recoverable candidate identity across commit/publication crashes, with atomic state artifacts and no lease renewal or duplicate claim.</done>
</task>
<task type="auto">
  <name>Task 2: Enforce bounded per-document candidate provider executions</name>
  <read_first>@src/rag/policy_reindex_artifacts.py @scripts/reindex_policies.py @src/rag/embedder.py @src/rag/policy_reindex.py @tests/rag/test_policy_reindex_artifacts.py</read_first>
  <files>src/rag/policy_reindex_artifacts.py, scripts/reindex_policies.py, evaluation/reports/rag_token_chunk_ab/v1/candidates/README.md, tests/rag/test_policy_reindex_artifacts.py, tests/rag/test_policy_reindex.py, .planning/ARCHITECTURE-DEBT.md, .planning/LOCAL-VALIDATION-ISSUES.md</files>
  <action>Add a create-only descriptor-bound `policy_candidate_build_budget.v1` and per-document ordinal reservations with `max_build_executions_per_document=2`. The reviewed build-next command requires the canonical descriptor/state/budget root, checks current DB state_version/next document, atomically reserves before constructing `EmbeddingService(max_retries=1)`, and records the fixed expected batch count from assembled inputs. Crash consumes the ordinal. A second ordinal is allowed only after recover-state proves the same document/state_version and the first execution produced an allowlisted provider-unavailable/transient safe result without DB advance; source/config/parity/response/projection errors stop. Success must advance exactly one document and makes remaining reservations for that document invalid. Concurrent executors have one winner; alternate root, ordinal overflow, descriptor/state mismatch and attempts after lease/parity expiry refuse before provider construction. Fault-inject reserve/provider/DB-commit/state-publish boundaries and prove at most two reviewed build executions per ordered document. Record only safe codes/counts/hashes, never raw exception/payload/content/credential/path. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/rag/test_policy_reindex_artifacts.py tests/rag/test_policy_reindex.py tests/test_embedder.py</automated></verify>
  <done>Each document has a machine-enforced two-execution rebuild budget, one-request retry behavior, crash consumption and fail-closed authority before any future live rebuild.</done>
</task>
</tasks>

<threat_model>
| Threat | Boundary | Mitigation |
|---|---|---|
| T64.4-02 Tampering | crash/retry -> duplicate candidate or unbounded calls | Fixed descriptor, exact DB recover-state and create-only per-document ordinals. |
| T64.4-03 Tampering | state file -> DB identity | Atomic canonical state bytes plus descriptor/DB cross-check; mismatch refuses. |
| T64.4-07 Disclosure | provider failure -> build artifact | Closed safe codes/counts/hashes only; raw exception/provider/content/secret/path forbidden. |
| T64.4-18 Elevation | alternate CLI/root -> reviewed rebuild | Canonical descriptor/budget root and reviewed subcommand required; unreviewed modes cannot satisfy later authority. |
</threat_model>
<validation_mapping>
| Task | Gate |
|---|---|
| 64.4-13-01 | descriptor, atomic state, recover-state and duplicate-refusal fault tests |
| 64.4-13-02 | per-document provider budget, retry matrix and refusal-before-provider tests |
</validation_mapping>
<verification>Only deterministic PostgreSQL/fault tests run; no live provider, candidate claim or A-B slot is permitted in this plan.</verification>
<success_criteria>SC-64.4-5 rebuild identity is crash-recoverable and provider executions are finitely machine-bounded before live work resumes.</success_criteria>
<output>Create `64.4-13-SUMMARY.md`.</output>
