---
phase: "64.4"
trigger: "Plan10 bounded live stop"
approved_by: user
approved_at: "2026-08-12"
status: reviewed_ready_for_execution
review_loops_completed: 4
latest_external_verdict: clean_pass
codex_independent_verdict: clean_authorized_plan13_execution
---

# Phase 64.4 Recovery Plan Decisions

## Trigger Evidence

- Fresh tokenizer parity passed and the inactive token candidate completed with 3 documents, 158 blocks and 60 chunks.
- Plan10 retained three create-only `execution_error` runs and exhausted its reviewed provider budget without a selection or pointer mutation.
- Attempts 1 and 2 exposed and fixed repository-backed implementation defects. Attempt 3 stopped as `provider_execution_failed`, while the exact active character baseline, rollout epoch/history and immutable state remained unchanged.
- The top-level broad catch did not retain a safe role/round/stage reason, so another uninstrumented retry would be unauditable and is prohibited.
- On 2026-08-12 the user approved the recommended recovery: add reviewed safe diagnostic provenance, grant a separate bounded provider budget, complete activation only after real selected_pass, then continue through PR.

## Locked Recovery Decisions

1. Plan10 is re-scoped transparently as the completed activation-receipt plus bounded-initial-evidence checkpoint. Its original stopped facts and artifacts remain immutable; they are not rewritten as success.
2. Plan11 adds `rag_token_chunk_execution_diagnostic.v1` as a separate create-only sidecar linked to the terminal run hash. A single create-only commit manifest is the semantic visibility point for the fsynced run/diagnostic four-file bundle; partial pre-manifest files are ignored/reconciled fail-closed. Existing `rag_token_chunk_ab.v1` and selection schemas stay byte-compatible.
3. Plan11 performs no live full-provider A/B attempt and cannot select, activate or mutate pointer/history state.
4. Plan12 receives a separate machine-enforced at-most-two-attempt provider budget. Create-only ordinal reservation happens before provider invocation and a crash consumes the slot. `candidate_failed`, safety failure, ambiguous/incomplete diagnostics, failed rollback proof and any implementation defect stop immediately. Attempt 2 is allowed only for a committed allowlisted transient execution diagnostic or an allowlisted unavailable terminal reason with a proved prerequisite state change. A code defect requires a separately reviewed repair plan and never authorizes an in-plan edit.
5. Locked 512/384/48 chunk parameters, exact Fraction/Decimal gates, sealed Phase64.3 hashes, provider/model identity and Phase64.2 replay identity do not change.
6. Plan12's fresh preflight must compare the exact report SHA in candidate DB/state to the retained fresh report before it creates a budget manifest or reserves a slot. The observed mismatch completes only a zero-attempt/no-drift handoff; it is not selection success.
7. Plan13 adds the missing candidate recovery contract before live work: a fixed create-only descriptor, exact DB recover-state, atomic state publication and a create-only maximum-two build execution budget per document. It performs no live claim/provider call.
8. Plan14 makes the A-B cap canonical before live work: production uses one resolved repository root, every reservation re-hashes the exact candidate state, and selected evidence gains a separate immutable recovery authorization that real activation must verify before CAS. Existing run/selection bytes remain unchanged.
9. Plan15 may claim exactly one new immutable inactive candidate against retained fresh parity using the reviewed descriptor/recover-state/build-budget path, then consume the canonical cap=2 A-B budget. Old candidate remains immutable; mutable rebinding/compatibility bridges are forbidden.
10. Only real fresh-parity `selected_pass` plus canonical recovery authorization from Plan15 may authorize Plan16's cutover -> prior rollback -> selected restore drill and receipt chain. Plan16 adds the full-lineage closeout guard.
11. Plan17 must account for all 34 tasks, both A-B budgets, candidate build budgets, zero-slot checkpoint, canonical recovery authority and final selected pointer/history. If Plan15 has no selection or Plan16 fails, Plan17 is blocked for separately reviewed recovery.

## Review Requirements

- Fresh GSD plan-checker over Plans10-17 and the 34-row validation map.
- External Claude plan review with every actionable item adjudicated against current code and immutable live artifacts.
- Independent Codex coverage/order/scope review after repairs.
- No execution resumes until both external and Codex reviews are clean.

## Review Findings

### Loop 1 — GSD Plan Checker

1. **Diagnostic four-file crash atomicity — 成立，已修复。** Existing helpers publish files sequentially, so exception cleanup alone cannot cover a process crash. Plan11 now stages/fsyncs all files and uses one atomic create-only commit manifest as the only reader-visible commit point; fault injection covers every publication boundary and partial recovery.
2. **`unavailable` retry evidence contradiction — 成立，已修复。** Plan12 now has a closed matrix: unavailable never requires the execution-error sidecar and instead needs an allowlisted immutable terminal reason plus a hashed prerequisite transition; execution_error needs a committed Plan11 bundle, allowlisted transient code and proved rollback.
3. **Unknown implementation-defect repair surface — 成立，已修复。** Plan12 no longer permits an in-plan code fix. A verified defect stops with its unused slot intact and requires a separately reviewed bounded repair plan before any remaining attempt.
4. **Mandatory Chinese ledgers missing from Plan12 — 成立，已修复。** Both ledgers are owned by Plan12, and every nonpassing live outcome or verified defect is recorded immediately rather than deferred to closeout.
5. **Plan12 rewrites Plan10 SUMMARY — 成立，已修复。** The output now creates only `64.4-12-SUMMARY.md`; Plan10's immutable failed handoff is referenced but never modified.
6. **Selection and activation in one high-risk plan — warning 成立，已修复。** Activation moved to Plan13; final documentation moved to Plan14.
7. **01–10 validation rows still pending — warning 成立，已修复。** Rows now cite their existing SUMMARY evidence; Plan10 Task2 is explicitly recorded only as the bounded no-drift checkpoint, not activation success.

### Loop 1 — External Claude Review

1. **Provider attempt limits procedural only — 成立，已修复。** Plan12 adds a fixed create-only budget manifest and reserve-before-call ordinal slots; concurrent/crashed execution cannot exceed cap=2 through the entry point.
2. **False closeout not test-enforced — 成立，已修复。** Plan13 adds a read-only closeout checker and tests that reject `nyquist_compliant: true` or `--require-live-complete` without a hash-valid selection, exactly three receipts and final selected pointer/history.
3. **No explicit branch if Plan12 fails — 成立，已修复。** Plan12 done/verification and Decision 7 now block Plans13-14 and require a separately reviewed recovery.
4. **Threat granularity — clarification accepted.** The repaired Plan12/13 task mappings name their exact threat sets.
5. **Diagnostic first live exercise risk — accepted as contained.** Plan11 cannot retroactively diagnose attempt3 and consumes no provider call; crash/fault/redaction tests precede the first Plan12 live exercise.

### Codex Independent Adjudication

- The GSD crash-publication, retry-matrix, unknown-fix, ledger and immutable-summary findings all match current plan/code boundaries and were accepted.
- Claude's machine-budget and false-closeout findings are also repository-backed: selection creation is code-gated, but neither attempt cap nor VALIDATION truth had an automated guard.
- The suggested generic ability to repair an unknown defect inside Plan12 was rejected as unplannable. The safer resolution is a hard stop plus a new reviewed bounded plan; this preserves the user's two-attempt maximum without guessing the future file surface.
- Recovery granularity is now 14 plans / 28 tasks. No locked threshold, provider/model identity, sealed hash, immutable identity rule or Plan10 artifact changed.

### Loop 2 — Clean Re-Review

- Fresh GSD plan-checker: `VERIFICATION PASSED`; all prior 5 blockers / 2 warnings closed, no new issue.
- External Claude: `PASS`; no actionable blocker or warning after repository-backed re-read.
- Codex final coverage/order/scope check: clean. Plans11-14 remain strictly sequential, each has two tasks and at most twelve files, VALIDATION has 28 one-to-one rows, and no recovery plan changes locked runtime values or Plan10 evidence.
- Execution is authorized dependency-first from Plan11. A nonpassing Plan12/13 outcome still stops for another reviewed recovery; this approval does not broaden the provider budget or repair scope.

## Fresh Candidate Rebuild Trigger And Adjudication

### Verified Plan12 Checkpoint

- Fresh parity run `c760c106-7e85-440e-a56d-ed7e00eb2fb7` passed exact-match with report SHA `sha256:166aba9633018ac529ab33c7dfe65126f2850ecf275987f46cfad9eb984ec1de`.
- The complete old candidate `b293e0b4-ada6-4165-8e2f-4f1739c88fdf` remains bound to passed report SHA `sha256:7ed994e05e52df4c93ef831669bcd120c731ab689f561c6c699d44ef239d33c5`.
- Config fingerprint, probe fixture and submitted-content identities match; exact create-only report SHA necessarily differs. `_validate_corpus_pair` correctly refused before provider construction. Budget manifest is absent, slots remain `0/2`, active pointer/history/baseline are unchanged.

### Options Checked Against Code

1. **Parity compatibility bridge — rejected.** A/B could prove stable parity identities, but `load_activation_authority` and `PolicyReindexService._validate_selection_proof` also require the selection/fresh parity hash to equal the target candidate DB parity hash. Supporting two hashes would expand the selection/activation authority surface and weaken the exact contract.
2. **Mutable candidate parity rebind — rejected.** The candidate row/state is immutable audit evidence and cannot be rewritten after validation.
3. **One new candidate under retained fresh parity — accepted.** Existing `scripts/reindex_policies.py claim/build-next/validate` already creates a new inactive corpus bound to the exact report SHA without modifying the active pointer. It preserves existing selection/activation schemas and exact equality checks.

### Bounded Rebuild Decisions

- One new run token/candidate claim only; transient recovery resumes the same descriptor/run/document and never creates a second candidate.
- Candidate build calls do not consume A-B slots but use create-only max-two execution ordinals per ordered document with crash consumption and `EmbeddingService(max_retries=1)`.
- New candidate must reproduce locked config/source/evidence identity, complete `3/158/60` projection and all validation proofs while active corpus, epoch/history and A-B slots remain unchanged.
- The retained fresh report must still satisfy the 24-hour freshness gate throughout claim and A-B readiness. Stale parity stops for another reviewed plan rather than minting another unbound report.
- After candidate completion, the canonical machine cap=2 ledger governs Plan15 live A-B without code changes or threshold tuning.

## Loop 3 — Fresh Candidate Draft Review And Codex Adjudication

### GSD Plan Checker Findings

1. **A-B cap reset via alternate output root — 成立，已修复于 Plan14。** Production CLI accepted arbitrary roots and reservation did not re-hash actual candidate state; activation had no budget lineage before CAS. Plan14 now requires one canonical resolved root, exact state binding, create-only recovery authorization and pre-CAS enforcement.
2. **DB commit before non-atomic state publication — 成立，已修复于 Plan13。** Existing CLI committed claim/build/validate before direct `open('x')`; crash could lose the only current identity. Plan13 adds pre-claim descriptor, exact recover-state and staged/fsynced atomic state publication with fault injection.
3. **Candidate provider retries unbounded — 成立，已修复于 Plan13。** Existing build-next could be repeatedly invoked and `EmbeddingService` internally retried. Plan13 adds per-document max-two create-only execution ordinals, crash consumption, closed transient authority and `max_retries=1`.

### External Claude Finding And Disagreement

- Claude returned PASS and correctly confirmed that a second inactive corpus can coexist and exact fresh parity binding works on the happy path. It raised only a historical 14/28 count note and the 24-hour freshness advisory.
- **Codex disagrees with the PASS.** Claude did not inspect the arbitrary `--output-root` reset, DB-commit/state-publication crash window, or unbounded build retries identified by GSD with exact code evidence. Those are pre-provider/pre-CAS integrity blockers, not optional hardening, so GSD's repository-backed findings take precedence.
- Historical Loop2 `14 plans / 28 tasks` text is retained as a dated statement; this section records the superseding structure (17/34) rather than rewriting past review evidence.

### Resulting Granularity

- Plan13 owns candidate crash recovery/build budget only.
- Plan14 owns canonical A-B/recovery activation authority only.
- Plan15 owns live candidate build/selection only.
- Plan16 owns activation/closeout guard only.
- Plan17 owns docs/full regression only.
- No locked thresholds, sealed hashes, provider/model identity, immutable evidence identity or prior live artifact changed.

## Loop 4 — Authority Repair Clean Re-Review

- Fresh GSD plan-checker: `VERIFICATION PASSED`; 17 plans / 34 tasks / 34 validation rows, with the crash-publication, per-document build budget and canonical pre-CAS authority blockers all closed and no new blocker or warning.
- External Claude: `PASS`; it independently reproduced the three original code gaps and confirmed Plans13–14 place deterministic integrity enforcement before Plan15 live work without changing existing v1 artifact bytes or locked runtime values.
- Codex final adjudication: clean. Repository evidence supports both verdicts; Plans13–17 are bounded, strictly sequential and fail closed if retained parity expires or live selection is absent.
- Execution is authorized from Plan13. This authorization does not create a candidate, consume a provider/A-B slot, extend the freshness window, or permit bypassing recovery authorization before pointer CAS.
