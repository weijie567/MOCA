---
phase: 64.4-token-aware-policy-chunking-and-reindex-validation
plan: "11"
name: Role Failure Provenance And Safe Diagnostic Artifacts
type: execute
wave: 11
depends_on: [64.4-10]
files_modified:
  - src/rag/evaluation/token_chunk_ab.py
  - src/rag/evaluation/retrieval_rounds.py
  - scripts/eval_rag_token_chunk_ab.py
  - evaluation/reports/rag_token_chunk_ab/v1/diagnostics/README.md
  - tests/eval/test_rag_token_chunk_ab.py
  - tests/eval/test_rag_retrieval_round_isolation.py
  - .planning/ARCHITECTURE-DEBT.md
  - .planning/LOCAL-VALIDATION-ISSUES.md
autonomous: true
requirements: [SC-64.4-6]
must_haves:
  truths:
    - "Every future execution_error retains create-only role, round, safe stage/reason and rollback-proof provenance without raw provider payload, policy content, credentials, paths or stack traces."
    - "Diagnostic evidence is a separate immutable artifact linked to the terminal run hash; a single create-only commit manifest is the only reader-visible bundle commit point, while the existing rag_token_chunk_ab.v1 run and selection schemas remain byte-compatible."
    - "Diagnostic code cannot create a selection, mutate the rollout pointer/history or consume the newly reviewed full-provider A-B budget."
  artifacts:
    - {path: evaluation/reports/rag_token_chunk_ab/v1/diagnostics/README.md, provides: "Strict diagnostic sidecar contract and operator interpretation", contains: "rag_token_chunk_execution_diagnostic.v1"}
  key_links:
    - {from: scripts/eval_rag_token_chunk_ab.py, to: src/rag/evaluation/retrieval_rounds.py, via: "typed safe role failure propagation into an immutable diagnostic bundle", pattern: "SafeRoleFailureV1"}
---

<objective>Close the verified provider_execution_failed observability gap before granting any new full-provider A-B attempt.</objective>
<execution_context>@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @64.4-10-SUMMARY.md @evaluation/reports/rag_token_chunk_ab/v1/runs/9aa10545-2350-4053-b4ef-03a57fda0535.json @src/rag/evaluation/retrieval_rounds.py @scripts/eval_rag_token_chunk_ab.py</context>

<tasks>
<task type="auto">
  <name>Task 1: Define strict create-only execution diagnostic evidence</name>
  <read_first>@src/rag/evaluation/token_chunk_ab.py @src/rag/evaluation/reporting.py @evaluation/reports/rag_token_chunk_ab/v1/README.md @64.4-10-SUMMARY.md</read_first>
  <files>src/rag/evaluation/token_chunk_ab.py, scripts/eval_rag_token_chunk_ab.py, evaluation/reports/rag_token_chunk_ab/v1/diagnostics/README.md, tests/eval/test_rag_token_chunk_ab.py</files>
  <action>Wave0 tests first. Define frozen `rag_token_chunk_execution_diagnostic.v1` with run ID, exact terminal-run SHA, failing role `character_incumbent|token_candidate|shared_preflight`, optional round format, allowlisted stage/reason code, provider availability/request classification, outer-rollback attempted/proved flags and safe timestamps/hashes/counts only. Forbid raw exception text, traceback, provider object/payload, prompt/policy content, credential/DSN, absolute path and activation/selection fields. For execution errors, stage run JSON/Markdown plus diagnostic JSON/Markdown under an unreferenced per-run staging directory, fsync every file and directory, publish create-only final files, then atomically publish one `rag_token_chunk_execution_bundle.v1` commit manifest binding all four exact hashes. Readers accept the bundle only when that manifest and all four hashes validate; pre-manifest partial files are invisible and are deterministically completed only for byte-identical content or quarantined/fail-closed on conflict. Fault-inject every stage-write/fsync/link/manifest-rename boundary and crash-resume path. Existing v1 run/selection bytes and non-execution publication stay unchanged. Non-execution outcomes never create diagnostics. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/eval/test_rag_token_chunk_ab.py tests/eval/test_rag_format_parity_report.py</automated></verify>
  <done>Execution errors have a crash-consistent, manifest-committed, immutable and run-hash-bound diagnostic artifact without changing selection semantics.</done>
</task>
<task type="auto">
  <name>Task 2: Preserve typed role and rollback provenance through the full runtime</name>
  <read_first>@src/rag/evaluation/retrieval_rounds.py @src/repositories/rag_evaluation_round_repo.py @scripts/eval_rag_token_chunk_ab.py @tests/eval/test_rag_retrieval_round_isolation.py</read_first>
  <files>src/rag/evaluation/retrieval_rounds.py, scripts/eval_rag_token_chunk_ab.py, tests/eval/test_rag_retrieval_round_isolation.py, tests/eval/test_rag_token_chunk_ab.py, .planning/ARCHITECTURE-DEBT.md, .planning/LOCAL-VALIDATION-ISSUES.md</files>
  <action>Add typed safe failures at shared preflight, role setup, per-format ingestion, retrieval/resource proof and post-rollback baseline verification boundaries. Preserve role/round/stage/reason and rollback status through the top-level catch without retaining raw exceptions. Tests inject failures for both roles and all safety-critical stages, prove outer rollback/new-session baseline verification still runs, prove only a valid commit manifest makes the four-file diagnostic bundle readable, prove partial/mismatched bundles fail closed, prove immutability/redaction, and statically reject pointer/activation/selection writes. Run no live full-provider A-B attempt in this plan; fresh live execution belongs only to Plan12's reviewed budget. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/eval/test_rag_token_chunk_ab.py tests/eval/test_rag_retrieval_round_isolation.py tests/eval/test_rag_format_parity_report.py</automated></verify>
  <done>Any future runtime failure identifies the safe role/round/stage and proves whether rollback restored the baseline, without pointer mutation or provider-budget consumption.</done>
</task>
</tasks>

<threat_model>
| Threat | Boundary | Mitigation |
|---|---|---|
| T64.4-07 Disclosure | provider/runtime failure -> diagnostic artifact | Strict allowlist, typed codes/hashes/counts only, adversarial secret/path/content/traceback tests. |
| T64.4-18 Elevation | diagnostic sidecar -> selection/retry authority | Diagnostic never selects or activates; Plan12 independently rechecks fresh parity, exact gates and budget. |
</threat_model>
<validation_mapping>
| Task | Gate |
|---|---|
| 64.4-11-01 | safe immutable diagnostic schema/commit-manifest/crash/redaction tests |
| 64.4-11-02 | typed role-stage propagation and rollback-proof isolation tests |
</validation_mapping>
<verification>Only deterministic contract/fault-injection tests run here; no provider attempt or pointer mutation is permitted.</verification>
<success_criteria>The attempt3 observability debt is closed with safe immutable evidence before any reviewed retry.</success_criteria>
<output>Create `64.4-11-SUMMARY.md`.</output>
