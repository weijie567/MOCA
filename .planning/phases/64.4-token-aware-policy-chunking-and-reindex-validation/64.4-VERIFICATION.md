---
phase: 64.4-token-aware-policy-chunking-and-reindex-validation
verified: 2026-08-12T12:54:22Z
status: gaps_found
delivery_scope: partial
score: 4/6 must-haves verified
plans_implemented: 17/20
overrides_applied: 0
gaps:
  - truth: "SC-64.4-5: Reindexing is isolated, resumable, and rollback-safe so failures preserve the prior usable index and no tenant observes a partially mixed old/new corpus."
    status: partial
    reason: "Isolation, immutable candidate state, active-pointer CAS, rollback primitives, receipts, and expiry gates exist, but the retained candidate is still building/v2/index0 under expired authority; no selected candidate, cutover/rollback/restore drill, receipt chain, or closeout guard exists."
    artifacts:
      - path: "evaluation/reports/rag_token_chunk_ab/v1/candidates/tenants/64300000-0000-4000-8000-000000000001/runs/f8e190f1-ad0e-4476-9f77-704735e3dc46/states/00000002.json"
        issue: "Truthfully records an incomplete building candidate at next_document_index 0; descriptor lease expired 2026-08-12T04:13:52.208631Z."
      - path: "scripts/reindex_policies.py"
        issue: "Production build-next-reviewed/build-next dispatch is intentionally disabled before DB, budget, or provider work."
      - path: "scripts/check_phase64_4_closeout.py"
        issue: "Missing because Plan 19 was not executed."
    missing:
      - "A separately reviewed post-PR rollout with a DB-backed globally unique provider/build budget authority."
      - "A fresh-authority complete candidate and proved selected cutover -> prior rollback -> selected restore sequence."
      - "Three hash-chained activation receipts and the fail-closed closeout guard from Plan 19."
  - truth: "SC-64.4-6: A versioned A/B report compares character- and token-aware candidates, passes explicit non-regression gates, and existing RAG tests pass."
    status: partial
    reason: "The exact metric/gate/reporting machinery exists, but retained terminal reports are execution_error only; there is no selected_pass decision, canonical recovery authorization, activation evidence, or Plan 20 regression closeout."
    artifacts:
      - path: "evaluation/reports/rag_token_chunk_ab/v1/runs/"
        issue: "Contains three rag_token_chunk_ab.v1 execution_error reports and no selected_pass report."
      - path: "evaluation/reports/rag_token_chunk_ab/v1/selections/"
        issue: "No selection artifacts exist."
      - path: "scripts/eval_rag_token_chunk_ab.py"
        issue: "Production run-ab dispatch is intentionally disabled before root, manifest, DB, reservation, artifact, or provider work."
      - path: ".planning/phases/64.4-token-aware-policy-chunking-and-reindex-validation/64.4-20-SUMMARY.md"
        issue: "Missing because Plan 20 was not executed."
    missing:
      - "DB-backed globally unique A/B provider budget authority before production provider dispatch can be re-enabled."
      - "A fresh canonical character-vs-token A/B run with selected_pass and immutable selection/authorization artifacts."
      - "Plan 20 full project-entry regression, lint/format, documentation, and 40-row validation closeout."
---

# Phase 64.4: Token-Aware Policy Chunking And Reindex Validation — Partial Delivery Verification

**Phase Goal:** Replace character-count policy chunk sizing with a versioned tokenizer-aware assembly path that measures the final `text-embedding-v4` input while preserving parser structure, provenance, evidence identity, deterministic rebuilds, and safe rollback, then prove the change against the Phase 64.3 format-parity baseline.

**Verified:** 2026-08-12T12:54:22Z
**Status:** `gaps_found` — accepted partial delivery, not phase completion
**Re-verification:** No — initial partial-delivery verification

## Verification Boundary

This report verifies the repository at HEAD `92e58771` read-only. Per the accepted partial-PR boundary, no pytest, database, provider, live-artifact, migration, lint, or formatting command was run. Evidence comes from the current implementation, retained immutable repository artifacts, PLAN/CONTEXT/ROADMAP contracts, and the final clean bounded code review. SUMMARY claims were used only to locate evidence and were checked against source/artifacts.

The phase remains frozen at 20 plans. Plans 17–20 are serial by decision R-28; Plan 17 reached the approved expired-authority checkpoint, so Plans 18–20 did not execute. The immutable lease is an honest stop: it does not authorize renewal, rebinding, a replacement candidate, a wider budget, or Plan 21.

## Goal Achievement

### Observable Truths

| # | Roadmap success criterion | Status | Repository evidence |
|---|---|---|---|
| 1 | Versioned deterministic offline tokenizer count plus provider-backed parity evidence | ✓ VERIFIED | `EmbeddingTokenizerConfigV1` and double-encode determinism are implemented in `src/rag/embedding_tokenizer.py:89-177`; `scripts/check_embedding_tokenizer_parity.py:53-215` submits assembler-owned safe probes and preserves request usage. Two retained `embedding_tokenizer_parity.v1` reports are `passed`, each with 10/10 exact probes and aggregate offline/provider prompt tokens `373/373`. |
| 2 | Exact final input, including envelope/table headers/overlap/source context, stays within 512 tokens with deterministic structural splitting | ✓ VERIFIED | `PolicyEmbeddingInputAssembler` is the sole token-aware owner (`src/rag/policy_embedding_input.py:80-620`); final rendered bytes are counted and rejected above the configured maximum at `:478-488`. Table/header, overlap, unpunctuated, multilingual/OCR-like/URL/number, no-truncation, deterministic rebuild, and tokenizer-failure contracts are present in `tests/rag/test_token_aware_chunker.py:138-420`. |
| 3 | Production ingestion, dry-run, golden validation, parity, and evaluation share the authoritative assembly contract | ✓ VERIFIED | `src/rag/ingestion.py:153-163,379-398` assembles once and submits exactly `dto.embedding_input`; dry-run/golden/parity/evaluation call the same boundary. Repository search finds no production `chunk_markdown()` caller outside its definition. Static ownership guards at `tests/architecture/test_rag_chunking_boundaries.py:130-230` enumerate the permitted assemblers and exact DTO consumers. |
| 4 | Config/model/token counts are auditable and incompatible rechunking cannot silently reuse evidence identity or break replay | ✓ VERIFIED | Current and immutable chunk rows persist config fingerprint, input hash, and token count (`src/db/models.py:757-788,964-997`; migration 030 mirrors them). `canonical_chunk_version_matches_projection()` checks replay material plus all audit fields while keeping corpus visibility external (`src/repositories/evidence_version_repo.py:115-145`); active bindings enforce the same compatibility at `src/repositories/policy_corpus_scope.py:360-388`. |
| 5 | Reindexing is isolated, resumable, and rollback-safe end to end | ✗ FAILED (foundation only) | Corpus pointer/CAS/history and candidate lifecycle are substantive, but the retained candidate artifact is still `building/v2/index0` with an expired lease. No Plan 18 completion/selection, Plan 19 activation drill, three receipts, or closeout guard exists. Production reindex provider dispatch is hard-disabled at `scripts/reindex_policies.py:857-863`. |
| 6 | Canonical A/B report selects a non-regressing token configuration and regression gates pass | ✗ FAILED (foundation only) | Exact metric and gate code covers Hit@1/3/5, MRR, anchor/locator/fallback, per-format spread, duplicate rate, chunk ratio, token ratio, and provider latency (`src/rag/evaluation/token_chunk_ab.py:133-223,1802-2068`). Actual retained runs are three `execution_error` outcomes; there is no selected-pass/selection/authorization. Production `run-ab` is hard-disabled at `scripts/eval_rag_token_chunk_ab.py:915-921`; Plan 20 did not run. |

**Score:** 4/6 roadmap truths verified.

The clean review is correctly scoped: `64.4-REVIEW.md` reports zero Critical/Warning/Info findings for the implemented partial production surface, while explicitly excluding Plans 18–20 and SC-64.4-5/6 from its verdict. It is not evidence that the full phase goal completed.

## Partial Delivery Breakdown

| Delivery slice | Result | Notes |
|---|---|---|
| Plans 01–04 | ✓ Implemented and verified | Tokenizer contract, exact final-input assembly, parity protocol, and authoritative path convergence support SC1–3. |
| Plans 05–09 | ✓ Foundation implemented | Auditable corpus/evidence identity, active pointer/CAS, resumable candidate service, and exact A/B contract exist. This is implementation foundation, not live SC5/6 completion. |
| Plans 10–14 | ✓ Safety/recovery foundation implemented | Immutable receipts, diagnostics, bounded recovery artifacts, candidate recovery, and pre-CAS authorization are substantive. Prior live A/B attempts ended nonpassing. |
| Plan 15 | ✓ Truthful blocked checkpoint | One descriptor/candidate was created, but build stopped before any provider/budget attempt; it completed no requirement. |
| Plan 16 | ✓ Deterministic repair implemented | Exact claimed/v1 predecessor and contiguous state publication were repaired without provider or persistent DB mutation. |
| Plan 17 | ✓ Truthful expired-authority checkpoint | Canonical issuance/current-time gate was implemented; live authority expired, so no manifest/provider/A-B side effect occurred and no requirement completed. |
| Plans 18–20 | ✗ Not executed | Same-candidate completion/A-B selection, activation drill/closeout guard, and final docs/regression closeout remain absent. |

## Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/rag/embedding_tokenizer.py` + pinned assets | Deterministic offline tokenizer contract | ✓ VERIFIED | Exists, substantive, wired to config/asset fingerprint and parity/assembler consumers. |
| `src/rag/policy_embedding_input.py` | Exact final-input DTO/assembler | ✓ VERIFIED | Exists, substantive, wired to tokenizer, ingestion, parity, golden, and evaluation consumers; dynamic input bytes flow unchanged. |
| `src/rag/tokenizer_parity.py` + retained parity reports | Immutable provider parity protocol/evidence | ✓ VERIFIED | Create-only/freshness gate is substantive; two retained passed reports have exact aggregate and probe matches. |
| `src/rag/ingestion.py` + architecture guard | One authoritative DTO consumption path | ✓ VERIFIED | Assembler DTO feeds embedder and persists the same audit fields; no independent production `chunk_markdown` caller exists. |
| Migration 030, corpus/evidence repositories | Auditable identity and isolated active visibility | ✓ VERIFIED | Active pointer, append-only projections, config-aware immutable reuse, and historical replay separation are present and wired. |
| `src/rag/policy_reindex.py` / `policy_reindex_artifacts.py` | Candidate claim/resume/validation and immutable recovery | ⚠ FOUNDATION | Substantive and wired, but provider-capable production continuation is intentionally unreachable. |
| `src/rag/evaluation/token_chunk_ab.py` | Exact A/B metrics, gates, immutable outcome schemas | ⚠ FOUNDATION | Substantive and test-covered in source, but no passing live outcome or selection exists. |
| Plan 18 canonical manifest/selection/authorization | Selected same-candidate A/B authority | ✗ MISSING | Canonical recovery-budget manifest is absent; selection and authorization directories contain no non-README artifacts. |
| Plan 19 closeout checker/tests and activation receipts | Reversible live drill and false-green guard | ✗ MISSING | `scripts/check_phase64_4_closeout.py` and `tests/rag/test_phase64_4_closeout.py` do not exist; no activation receipts exist. |
| Plan 20 runbook/SUMMARY/final regression evidence | Full documentation and regression closeout | ✗ MISSING | `docs/operations/rag-token-aware-reindex.md` and `64.4-20-SUMMARY.md` do not exist. |

## Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `embedding_tokenizer.py` | pinned contract/tokenizer asset | Strict config/runtime/size/SHA load | ✓ WIRED | Counter validates immutable configuration and tokenizes twice; no character fallback path exists. |
| `policy_embedding_input.py` | `embedding_tokenizer.py` | Count fully rendered provider bytes | ✓ WIRED | Envelope, overlap, table headers, and source IDs are included before the final count. |
| Ingestion/dry-run/golden/parity/evaluation | `PolicyEmbeddingInputAssembler` | Shared `assemble_policy_embedding_inputs`/assembler DTO | ✓ WIRED | Every token-aware consumer uses `embedding_input`; character incumbent is an explicitly named A/B compatibility owner. |
| Assembler DTO | current + immutable chunk rows | Ingestion persistence and compatibility predicate | ✓ WIRED | Fingerprint, input hash, count, source refs, citation and search material stay bound. |
| Current repository reads | active corpus rollout pointer | `ActivePolicyCorpusScope` joins | ✓ WIRED | One tenant pointer controls current visibility; historical immutable reads remain corpus-independent. |
| Candidate/A-B provider algorithms | production dispatch | Entry refusal `live_provider_execution_disabled` | ⚠ SAFE-BLOCKED | Both provider-capable paths stop before authority/reservation/provider work and have no CLI/environment override. This closes the reviewed vulnerability but also prevents SC5/6 completion. |
| A/B selected-pass | activation/rollback/restore | Recovery authorization + CAS + receipt chain | ✗ NOT WIRED IN DELIVERED OUTCOME | Code primitives exist, but no selected-pass/authorization/receipt artifacts were produced and Plan 19 did not run. |

## Data-Flow Trace (Level 4)

| Artifact | Data | Source | Produces real data | Status |
|---|---|---|---|---|
| `PolicyEmbeddingInputAssembler` | `embedding_input`, exact token count, provenance | Ordered `ParsedBlock` records + pinned tokenizer | Yes | ✓ FLOWING |
| `IngestionService` | embedding request + persisted chunk audit | Exact assembler DTO | Yes in implemented path | ✓ FLOWING |
| Tokenizer parity report | offline/provider prompt-token comparison | Safe assembler probes + request-level provider usage | Retained reports show exact 373/373 | ✓ FLOWING |
| Corpus/evidence projection | current rows and immutable replay binding | Active pointer + append-only bindings + config compatibility | Yes in repository design | ✓ FLOWING |
| Retained Plan 15/16 candidate | candidate progress | Descriptor + state v1/v2 | Real but incomplete (`building`, index 0) | ⚠ PARTIAL |
| Canonical A/B selection and activation | selected config and receipt chain | Complete candidate + fresh authority + provider A/B | No; production dispatch disabled and artifacts absent | ✗ DISCONNECTED BY SAFETY GATE |

## Behavioral Spot-Checks

Skipped by explicit verification boundary: no pytest, DB, provider, live-artifact, migration, lint, or formatting command was run. The final fixer report records `make format`, full `make lint`, and 134 focused reindex/A-B tests passing after the production-disable fix; this report treats that as prior evidence, not as a newly executed check. End-to-end SC5/6 behavior is failed, not inferred from deterministic tests.

After this read-only verification report was produced, the orchestrator ran the PR gate independently: `make lint` passed and the complete project-entry suite finished with `4893 passed, 4 skipped, 166 warnings`. That strengthens the partial delivery's regression evidence but does not change the 4/6 score or supply the missing live SC5/6 outcomes.

## Requirements Coverage

| Requirement | Source plans | Status | Evidence |
|---|---|---|---|
| SC-64.4-1 | 01, 03, 20 | ✓ SATISFIED in partial delivery | Pinned contract/counter plus retained exact provider parity reports. |
| SC-64.4-2 | 02, 20 | ✓ SATISFIED in partial delivery | Final-input assembler, hard token bound, deterministic structural/provenance behavior. |
| SC-64.4-3 | 04, 20 | ✓ SATISFIED in partial delivery | Shared assembler/DTO path and required text-shape regression source coverage. |
| SC-64.4-4 | 05, 06, 20 | ✓ SATISFIED in partial delivery | Auditable persistence, config-aware immutable reuse, corpus-free historical replay identity. |
| SC-64.4-5 | 05–08, 10, 13–20 | ✗ BLOCKED | Foundation exists; complete fresh candidate, live reversible drill, receipts, and closeout guard do not. |
| SC-64.4-6 | 09–12, 14–20 | ✗ BLOCKED | Metric/report foundation exists; only execution-error runs exist and no selected-pass/full regression closeout exists. |

All six Phase 64.4 requirements are claimed by at least one plan; no orphaned requirement was found. No later milestone phase explicitly owns the missing reindex/A/B completion, so these gaps are not deferred by the existing ROADMAP.

## Anti-Patterns Found

| File | Pattern | Severity | Impact |
|---|---|---|---|
| Reviewed production surface | TODO/FIXME/placeholder/stub scan | None | No goal-blocking placeholder was found. Empty collection returns inspected in repositories/artifact stores are legitimate empty-input or absent-directory guards. |
| `scripts/reindex_policies.py` | Provider-capable internal functions remain defined but production dispatch refuses them | ℹ Info | Intentional reviewed safety boundary; not a shipped production stub or bypass. |
| `scripts/eval_rag_token_chunk_ab.py` | Internal full-provider A/B algorithm remains test-callable but production dispatch refuses it | ℹ Info | Intentional reviewed safety boundary; no CLI/environment override found. |
| `64.4-REVIEW.md` | Final bounded deep review | None | Status `clean`, findings 0; verdict explicitly covers only the implemented partial scope. |

## Remaining Safety Boundary

Provider-capable reindex build and full A/B dispatch must remain disabled. File-backed ordinals/manifests are deterministic evidence, but they are not globally non-forgeable production authority. Before either dispatch is re-enabled, a new separately reviewed post-PR rollout must introduce a **database-backed, globally unique budget authority** that serializes reservation across worktrees/processes and binds tenant, run, candidate, parity, source/evidence epochs, absolute expiry, and provider execution.

The expired Plan 15 descriptor cannot be renewed, rebound, or replaced under the frozen Plans 18–20 authority. Any future live completion needs fresh explicitly reviewed authority; it must not reinterpret this partial PR as SC5/6 completion.

## Human Verification Required

None requested for this partial report. Visual/interactive behavior is not part of the phase. Live reindex, provider A/B, activation, rollback, and restoration are not marked “needs human”; they are objectively absent and therefore remain failed criteria.

## Gaps Summary

The partial PR safely delivers and verifies SC1–4 plus substantial reindex/A/B safety infrastructure. It does **not** achieve the full Phase 64.4 goal because SC5 and SC6 lack their required live outcomes and closeout evidence. The two gaps share one root concern: production provider execution was correctly disabled after review found file-backed authority was not globally non-forgeable. That safety fix is valid and clean, but it is not an override and cannot count as goal completion.

Plans 18–20 remain unexecuted exactly as the frozen serial/lease-expiry decisions require. The ROADMAP now transfers their outcome—not their expired authority—to Phase 64.5, which must introduce the DB-backed globally unique budget before any provider path is restored. Phase 64.4 remains partial; this report must not be used to mark it passed or complete.

---

_Verified: 2026-08-12T12:54:22Z_
_Verifier: Codex (gsd-verifier)_
