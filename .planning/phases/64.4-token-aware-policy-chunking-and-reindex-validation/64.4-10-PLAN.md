---
phase: 64.4-token-aware-policy-chunking-and-reindex-validation
plan: "10"
name: Activation Receipts And Bounded Initial Live Evidence
type: execute
wave: 10
depends_on: [64.4-09]
files_modified:
  - src/rag/activation_receipt.py
  - src/rag/policy_reindex.py
  - scripts/reindex_policies.py
  - evaluation/reports/rag_embedding_tokenizer/v1/runs/
  - evaluation/reports/rag_token_chunk_ab/v1/runs/
  - evaluation/reports/rag_token_chunk_ab/v1/selections/
  - evaluation/reports/rag_token_chunk_ab/v1/activations/
  - evaluation/reports/rag_token_chunk_ab/v1/activations/README.md
  - tests/rag/test_activation_receipt.py
  - tests/rag/test_policy_reindex.py
  - tests/knowledge/test_evidence_cutover.py
  - tests/replay/test_production_evidence_binding.py
autonomous: true
requirements: [SC-64.4-5, SC-64.4-6]
must_haves:
  truths:
    - "Activation receipt code consumes an immutable selection hash and never modifies parity or selection artifacts."
    - "Every committed cutover, rollback or selected restoration can write a separate append-only receipt bound to DB history and the previous receipt hash."
    - "The initial three-attempt live budget stops fail closed with immutable evidence and no pointer drift when no selected_pass exists; recovery requires a separately reviewed plan."
  artifacts:
    - {path: src/rag/activation_receipt.py, provides: "Strict append-only hash-chained activation receipt", contains: "rag_token_chunk_activation.v1"}
  key_links:
    - {from: src/rag/activation_receipt.py, to: src/repositories/policy_corpus_repo.py, via: "receipt binds committed history sequence and pointer epochs", pattern: "previous_receipt_sha256"}
---

<objective>Separate runtime activation evidence from selection, exercise the bounded initial live budget, and stop safely with a reviewed recovery handoff when no selection exists.</objective>
<execution_context>@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @64.4-09-SUMMARY.md @src/rag/policy_reindex.py @tests/knowledge/test_evidence_cutover.py</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement append-only activation receipt hash chain</name>
  <read_first>@src/rag/policy_reindex.py @src/repositories/policy_corpus_repo.py @evaluation/reports/rag_token_chunk_ab/v1/README.md</read_first>
  <files>src/rag/activation_receipt.py, src/rag/policy_reindex.py, scripts/reindex_policies.py, evaluation/reports/rag_token_chunk_ab/v1/activations/README.md, tests/rag/test_activation_receipt.py</files>
  <action>Define strict `rag_token_chunk_activation.v1` create-only receipt path by tenant/history sequence. Bind immutable selection SHA, event reason, from/to corpus, before/after epochs, DB history row hash/sequence, actor/time and previous receipt SHA (genesis marker for first). Cutover transaction commits first; receipt writer verifies live history/pointer then writes atomically. Missing receipt can be reconciled idempotently from history; mismatched or rewritten receipt fails. Selection/parity files are read-only and hashes rechecked. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/rag/test_activation_receipt.py tests/rag/test_policy_reindex.py</automated></verify>
  <done>Activation evidence is independently append-only, hash chained, reconcilable and selection-bound.</done>
</task>
<task type="auto">
  <name>Task 2: Execute bounded initial live evidence and preserve a fail-closed recovery handoff</name>
  <read_first>@scripts/check_embedding_tokenizer_parity.py @scripts/eval_rag_token_chunk_ab.py @scripts/reindex_policies.py @64.4-09-SUMMARY.md</read_first>
  <files>src/rag/policy_reindex.py, evaluation/reports/rag_embedding_tokenizer/v1/runs/, evaluation/reports/rag_token_chunk_ab/v1/runs/, evaluation/reports/rag_token_chunk_ab/v1/selections/, evaluation/reports/rag_token_chunk_ab/v1/activations/, tests/rag/test_policy_reindex.py, tests/knowledge/test_evidence_cutover.py, tests/replay/test_production_evidence_binding.py</files>
  <action>Run fresh live parity after Plan04 convergence, build and verify a complete inactive token candidate, then execute real same-provider A/B against the sealed Phase64.3 hashes without changing 512/384/48 or exact thresholds. Bound this initial full-provider budget at three: retries may address only a verified in-scope implementation defect or transient unavailable condition. If selected_pass exists, the activation drill may proceed; otherwise retain every create-only terminal report, prove the prior active pointer/view/history byte-for-byte unchanged, create no selection/activation artifact, and stop for an explicit user decision plus a separately reviewed recovery plan. Do not hide execution_error provenance, start a fourth attempt, or reinterpret a failed run as quality evidence. Finish any Python adjustments with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/rag/test_activation_receipt.py tests/rag/test_policy_reindex.py tests/knowledge/test_evidence_cutover.py tests/replay/test_production_evidence_binding.py</automated></verify>
  <done>Fresh parity, the inactive candidate, all three bounded terminal attempts and the exact no-drift checkpoint are immutable; either a selected drill completed or the approved recovery-plan handoff is explicit and fail closed.</done>
</task>
</tasks>

<threat_model>
| Threat | Boundary | Mitigation |
|---|---|---|
| T64.4-18 Elevation | red/unavailable selection -> cutover | Exact immutable selected_pass hash required in readiness and CAS. |
| T64.4-19 Repudiation | pointer event -> report | DB append-only history plus create-only hash-chained receipt and reconciliation. |
</threat_model>
<validation_mapping>
| Task | Gate |
|---|---|
| 64.4-10-01 | receipt immutability/hash-chain/reconcile tests |
| 64.4-10-02 | bounded live evidence, no-selection stop and zero-drift proof |
</validation_mapping>
<verification>Real provider and PostgreSQL evidence are mandatory; deterministic fakes alone cannot complete Phase64.4.</verification>
<success_criteria>Receipt mechanics and the bounded initial live/no-drift branch are proved; Plans11-12 own diagnostic recovery and the still-required selected activation drill.</success_criteria>
<output>Create `64.4-10-SUMMARY.md`.</output>
