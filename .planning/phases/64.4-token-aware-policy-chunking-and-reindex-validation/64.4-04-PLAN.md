---
phase: 64.4-token-aware-policy-chunking-and-reindex-validation
plan: "04"
name: Production Dry Run Golden And A-B Convergence
type: execute
wave: 4
depends_on: [64.4-03]
files_modified:
  - src/rag/ingestion.py
  - src/rag/chunker.py
  - scripts/ingest_policies.py
  - scripts/validate_golden_seeds.py
  - scripts/eval_rag_format_parity.py
  - tests/test_ingestion.py
  - tests/rag/test_ingestion_safety.py
  - tests/eval/test_rag_format_parity_contract.py
  - tests/architecture/test_rag_chunking_boundaries.py
autonomous: true
requirements: [ROADMAP-SC-3]
must_haves:
  truths:
    - "Production, dry-run, golden/fixture validation, safe parity probes and Phase64.3 candidate assembly all obtain final strings from PolicyEmbeddingInputAssembler."
    - "Legacy chunk_markdown is either a parser-compatible thin adapter or explicitly named read-only CharacterCompatibilityAssembler used only for bootstrap/A-B incumbent."
  artifacts:
    - {path: tests/architecture/test_rag_chunking_boundaries.py, provides: "Static single-assembler/no-local-split guard", contains: "PolicyEmbeddingInputAssembler"}
  key_links:
    - {from: src/rag/ingestion.py, to: src/rag/policy_embedding_input.py, via: "embed and persist returned DTO unchanged", pattern: "embedding_input"}
---

<objective>Converge every authoritative path on the assembler and retire duplicate production/golden splitting.</objective>
<execution_context>@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @64.4-03-SUMMARY.md @src/rag/ingestion.py @scripts/validate_golden_seeds.py @scripts/eval_rag_format_parity.py</context>

<tasks>
<task type="auto">
  <name>Task 1: Wire production ingestion and dry-run to exact DTO consumption</name>
  <read_first>@src/rag/ingestion.py @src/rag/embedder.py @scripts/ingest_policies.py @tests/test_ingestion.py</read_first>
  <files>src/rag/ingestion.py, src/rag/chunker.py, scripts/ingest_policies.py, tests/test_ingestion.py, tests/rag/test_ingestion_safety.py</files>
  <action>Inject `PolicyEmbeddingInputAssembler` and make the embedder consume the DTO's exact string without reconstruction or post-count decoration. Continue persisting only the existing citation/search/provenance projection fields in this pre-migration plan; keep exact count/hash/config in the in-memory typed DTO only, and do not write job/current/immutable audit fields before Plan05 migration 030. Dry-run calls the same parsed-block-to-DTO method and makes zero provider/DB writes. Replace implicit legacy behavior with named `CharacterCompatibilityAssembler`, allowed only when an explicit bootstrap/A-B incumbent mode is passed; token mode never falls back. Preserve parser and evidence projection semantics. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/test_ingestion.py tests/rag/test_ingestion_safety.py</automated></verify>
  <done>Production and dry-run consume the identical returned provider string and fail closed; pending audit values remain typed until Plan05 creates and wires their columns.</done>
</task>
<task type="auto">
  <name>Task 2: Migrate golden and Phase64.3 seams; forbid second algorithms</name>
  <read_first>@scripts/validate_golden_seeds.py @scripts/eval_rag_format_parity.py @scripts/check_embedding_tokenizer_parity.py @tests/eval/test_rag_format_parity_contract.py</read_first>
  <files>scripts/validate_golden_seeds.py, scripts/eval_rag_format_parity.py, tests/eval/test_rag_format_parity_contract.py, tests/architecture/test_rag_chunking_boundaries.py</files>
  <action>Migrate or retire `validate_golden_seeds.py` so fixture validation calls the production parsed-block assembly entry. Expose the same assembly seam to Phase64.3 evaluation; only an explicit `_character_baseline` owner may use `CharacterCompatibilityAssembler`. Add AST/rg guards over `src/` and `scripts/` against local token/character split loops, reconstructed embedding envelopes, direct production `chunk_markdown`, unnamed compatibility use, or parity submissions that are not the assembler DTO's exact final string. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/eval/test_rag_format_parity_contract.py tests/architecture/test_rag_chunking_boundaries.py tests/rag/test_tokenizer_parity.py</automated></verify>
  <done>D-11/D-12 have real runtime and static proof, including parity final strings from the sole assembler.</done>
</task>
</tasks>

<threat_model>
| Threat | Boundary | Mitigation |
|---|---|---|
| T64.4-04 Tampering | duplicate path -> misleading validation | Shared method plus AST guard and exact-input spy. |
| T64.4-09 Tampering | embedder reconstruction -> count drift | DTO string identity assertion at embed call. |
</threat_model>
<validation_mapping>
| Task | Gate |
|---|---|
| 64.4-04-01 | ingestion/dry-run exact DTO tests |
| 64.4-04-02 | golden/parity/eval convergence and static guards |
</validation_mapping>
<verification>Run both gates; a live parity run is required later, after this wiring exists.</verification>
<success_criteria>ROADMAP SC3 and D-11..D-13 are covered; Plan03 owns parity assembler wiring and this plan prevents all paths from drifting; excluded parser/retrieval work is untouched.</success_criteria>
<output>Create `64.4-04-SUMMARY.md`.</output>
