---
phase: 64.4-token-aware-policy-chunking-and-reindex-validation
plan: "08"
name: Atomic Pointer Activation And Ingestion Continuity
type: execute
wave: 8
depends_on: [64.4-07]
files_modified:
  - src/repositories/policy_corpus_repo.py
  - src/rag/policy_reindex.py
  - src/rag/ingestion.py
  - scripts/reindex_policies.py
  - tests/rag/test_policy_reindex.py
  - tests/knowledge/test_evidence_cutover.py
  - tests/knowledge/test_retrieval.py
  - tests/knowledge/test_hybrid_retrieval.py
  - tests/rag/test_ingestion_continuity.py
autonomous: true
requirements: [SC-64.4-5]
must_haves:
  truths:
    - "One tenant rollout pointer plus epoch is the sole active authority; append-only history records every CAS activation/rollback/restore, so a complete corpus can be reactivated without illegal state reversal."
    - "Ordinary ingestion selects the active named assembler and transactionally advances manifest/source epochs while invalidating stale direct rollback targets and in-flight candidates."
    - "Cutover requires a hash-bound immutable selection decision; no activation receipt is rewritten into that decision."
  artifacts:
    - {path: src/repositories/policy_corpus_repo.py, provides: "Exactly-one pointer CAS and append-only activation history", contains: "expected_rollout_epoch"}
  key_links:
    - {from: src/rag/ingestion.py, to: src/repositories/policy_corpus_repo.py, via: "active-config lock and manifest epoch refresh", pattern: "source_manifest"}
---

<objective>Implement safe cutover/rollback/reactivation and keep normal ingestion coherent across every pointer state.</objective>
<execution_context>@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @64.4-07-SUMMARY.md @src/rag/ingestion.py @src/repositories/evidence_version_repo.py @docs/contract-spec.md</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement pointer CAS, append-only history and reversible activation</name>
  <read_first>@src/repositories/policy_corpus_repo.py @src/repositories/evidence_version_repo.py @src/rag/policy_reindex.py @tests/knowledge/test_evidence_cutover.py</read_first>
  <files>src/repositories/policy_corpus_repo.py, src/rag/policy_reindex.py, scripts/reindex_policies.py, tests/rag/test_policy_reindex.py, tests/knowledge/test_evidence_cutover.py, tests/knowledge/test_retrieval.py, tests/knowledge/test_hybrid_retrieval.py</files>
  <action>Under evidence-rollout-first fixed locks, implement an activation API that validates complete candidate and exact immutable `selection_decision_sha256`, then CAS one pointer from expected corpus/epoch to target and appends history `{sequence, from, to, prior_epoch, new_epoch, reason, selection_hash?, actor, timestamp}` in the transaction. This plan tests the API with immutable contract fixtures; it does not claim or perform the real selected cutover before Plan09 creates that decision. Corpus build status remains complete and does not encode active/superseded authority. Thus cutover token, rollback prior, and restore selected are ordinary CAS events; stale CAS loses with no history/pointer mutation. Tests assert exactly one authority and no mixed results at each step. Cleanup is tenant/run allowlisted and never deletes immutable replay evidence. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/rag/test_policy_reindex.py tests/knowledge/test_evidence_cutover.py tests/knowledge/test_retrieval.py tests/knowledge/test_hybrid_retrieval.py</automated></verify>
  <done>Cutover, rollback and selected restoration are reversible, stale-safe and auditable without rewriting corpus state.</done>
</task>
<task type="auto">
  <name>Task 2: Make ordinary ingestion refresh manifests and stale alternatives transactionally</name>
  <read_first>@src/rag/ingestion.py @src/repositories/policy_corpus_repo.py @src/rag/policy_reindex.py @tests/test_ingestion.py</read_first>
  <files>src/rag/ingestion.py, src/repositories/policy_corpus_repo.py, src/rag/policy_reindex.py, tests/rag/test_ingestion_continuity.py, tests/rag/test_policy_reindex.py</files>
  <action>Within tenant rollout/source lock, resolve the active pointer/config and explicitly choose character compatibility or token assembler. After create/update/delete, append a source-manifest revision and CAS source/rollback epochs in the same transaction; mark in-flight candidates from the old manifest `source_stale` and make prior complete corpora ineligible for direct rollback after source drift (operator must rebuild current sources under prior config). Test pre-cutover character writes, post-token cutover writes, failed-selection/no-cutover, post-rollback writes, stale candidates, rollback refusal/rebuild and exactly-one active visibility. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/rag/test_ingestion_continuity.py tests/rag/test_policy_reindex.py tests/test_ingestion.py</automated></verify>
  <done>Normal writes remain available and coherent before/after cutover, failed selection, rollback and source drift.</done>
</task>
</tasks>

<threat_model>
| Threat | Boundary | Mitigation |
|---|---|---|
| T64.4-06 Tampering | concurrent ingestion/cutover -> pointer | Fixed locks, expected pointer/epochs, manifest compatibility, atomic history. |
| T64.4-10 Tampering | rollback after source drift -> obsolete policy | Mark stale, refuse direct flip, rebuild current source under prior config. |
| T64.4-02 Elevation | cross-tenant/run cleanup -> mutable or immutable data | Allowlisted tenant/run/owner cleanup; immutable replay evidence is never deleted. |
| T64.4-01 Disclosure | pointer transition -> mixed results | One-row atomic pointer, projection joins and transaction isolation tests. |
</threat_model>
<validation_mapping>
| Task | Gate |
|---|---|
| 64.4-08-01 | cutover/rollback/restore/stale CAS tests |
| 64.4-08-02 | ordinary ingestion lifecycle/source-drift tests |
</validation_mapping>
<verification>Use PostgreSQL interruption/concurrency tests; active corpus state labels are not authority.</verification>
<success_criteria>D-17..D-19 and SC-64.4-5 cover candidate build through ongoing production continuity.</success_criteria>
<output>Create `64.4-08-SUMMARY.md`.</output>
