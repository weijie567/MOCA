---
phase: 64.4-token-aware-policy-chunking-and-reindex-validation
fixed_at: 2026-08-12T09:18:21Z
review_path: .planning/phases/64.4-token-aware-policy-chunking-and-reindex-validation/64.4-REVIEW.md
iteration: 1
findings_in_scope: 5
fixed: 5
skipped: 0
status: all_fixed
---

# Phase 64.4: Code Review Fix Report

**Fixed at:** 2026-08-12T09:18:21Z  
**Source review:** `.planning/phases/64.4-token-aware-policy-chunking-and-reindex-validation/64.4-REVIEW.md`  
**Iteration:** 1

**Summary:**

- Findings in scope: 5
- Fixed: 5
- Skipped: 0
- Live provider/DB/artifact mutations: 0
- Plans 18–20 executed: no

## Fixed Issues

### CR-01: Copying the reviewed candidate artifact tree resets the provider-execution budget

**Files modified:** `scripts/reindex_policies.py`, `tests/rag/test_policy_reindex.py`, `.planning/LOCAL-VALIDATION-ISSUES.md`, `.planning/ARCHITECTURE-DEBT.md`  
**Commit:** `6f5b9178`  
**Status:** fixed  
**Applied fix:** Production reviewed commands now derive one canonical candidate root from the resolved repository root, reject alternate/copied/outside roots and every symlink component before artifact loading, reservation, or provider construction, and retain temporary roots only through an argparse-inaccessible test injection. Copied-tree and symlink adversarial tests prove zero reservation and zero provider-factory calls.

### WR-01: A clean install or newly created tenant cannot ingest its first policy document

**Files modified:** `src/db/migrations/versions/030_phase64_4_token_corpora.py`, `src/repositories/policy_corpus_repo.py`, `src/rag/ingestion.py`, `scripts/seed_demo.py`, `tests/knowledge/test_token_corpus_migration.py`, `tests/rag/test_ingestion_continuity.py`, `tests/test_seed_demo.py`, `.planning/ARCHITECTURE-DEBT.md`  
**Commit:** `bdfd1693`  
**Status:** fixed: requires human verification  
**Applied fix:** Migration 030 now bootstraps all existing tenants, including zero-document tenants. Runtime ingestion and demo seeding share an idempotent empty character-corpus initializer under the tenant advisory transaction lock. Existing rollout authority is preserved unchanged; partial authority fails closed. Clean seed, empty first-ingest, real migration, and concurrent first-ingest tests pass.

### WR-02: `claim-reviewed` backdates its second authorization transaction to descriptor creation time

**Files modified:** `scripts/reindex_policies.py`, `src/rag/policy_reindex.py`, `tests/rag/test_policy_reindex.py`, `.planning/ARCHITECTURE-DEBT.md`  
**Commit:** `3d117cc9`  
**Status:** fixed: requires human verification  
**Applied fix:** `claim-reviewed` captures fresh current UTC after transaction A/v1 publication and requires it to be strictly earlier than both descriptor expiries before transaction B. The same instant is passed through recovery and resume. `PolicyReindexService.recover_identity` now independently enforces lease/parity expiry. A between-transaction expiry fault leaves the DB row claimed/v1, publishes neither v2 nor budget, and makes zero resume calls.

### WR-03: Equality at parity expiry is treated as fresh

**Files modified:** `src/rag/policy_reindex.py`, `tests/rag/test_policy_reindex.py`, `.planning/ARCHITECTURE-DEBT.md`  
**Commit:** `9443eac1`  
**Status:** fixed: requires human verification  
**Applied fix:** The common lifecycle gate now rejects `checked_at >= parity_expires_at`. Equality coverage verifies rejection for resume, candidate preparation, provider-capable build, and validation; the denied build makes zero embedder calls.

### WR-04: Create-only authority files return before the directory entry is durable

**Files modified:** `src/rag/tokenizer_parity.py`, `src/rag/activation_receipt.py`, `tests/rag/test_tokenizer_parity.py`, `tests/rag/test_activation_receipt.py`, `.planning/ARCHITECTURE-DEBT.md`  
**Commit:** `51a613dd`  
**Status:** fixed  
**Applied fix:** Parity and activation receipt writers durably fsync each newly created directory level, remove the staging link, then fsync the destination parent after the successful hard-link commit. Activation byte-identical reconciliation also fsyncs the parent before return. Fault injection covers the `published` and `parent_fsynced` boundaries for both writers.

## Verification

- `make format` — passed.
- Full `make lint` — passed.
- Focused union: `UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/knowledge/test_token_corpus_migration.py tests/rag/test_ingestion_continuity.py tests/test_seed_demo.py tests/rag/test_policy_reindex.py tests/rag/test_tokenizer_parity.py tests/rag/test_activation_receipt.py` — `63 passed, 7 warnings` in `115.47s`.
- Documentation/validation ledger commit: `6d9641fa`.
- Full pytest suite was not run by this fixer to avoid duplicating the orchestrator's exclusive final gate. An earlier concurrently launched pre-fix suite reached `3 failed / 4 errors` during shared-schema DDL contention and was stopped; that result is invalid and is recorded in `.planning/LOCAL-VALIDATION-ISSUES.md`.

---

_Fixed: 2026-08-12T09:18:21Z_  
_Fixer: Codex (gsd-code-fixer)_  
_Iteration: 1_
