---
phase: 64.4-token-aware-policy-chunking-and-reindex-validation
plan: "06"
name: Active Corpus Paths And Immutable Bindings
type: execute
wave: 6
depends_on: [64.4-05]
files_modified:
  - src/repositories/policy_corpus_scope.py
  - src/repositories/policy_chunk_repo.py
  - src/repositories/document_block_repo.py
  - src/repositories/evidence_version_repo.py
  - src/repositories/rag_evaluation_round_repo.py
  - src/rag/ingestion.py
  - src/rag/search_text_backfill.py
  - scripts/seed_demo.py
  - tests/repositories/test_policy_chunk_repo.py
  - tests/knowledge/test_evidence_identity.py
  - tests/knowledge/test_evidence_projection.py
  - tests/architecture/test_rag_chunking_boundaries.py
autonomous: true
requirements: [ROADMAP-SC-4, ROADMAP-SC-5]
must_haves:
  truths:
    - "Every current read/write/delete/backfill/seed path resolves one tenant active pointer; historical reads resolve stored immutable IDs without the current pointer."
    - "Same source/content/provenance/config may reuse an immutable chunk version across corpus projections; incompatible fingerprint must mint a new version; corpus ID never changes evidence identity."
  artifacts:
    - {path: src/repositories/policy_corpus_scope.py, provides: "Single active/historical scope authority", contains: "ActivePolicyCorpusScope"}
  key_links:
    - {from: src/repositories/policy_chunk_repo.py, to: src/repositories/policy_corpus_scope.py, via: "mandatory active projection on current operations", pattern: "active_corpus"}
---

<objective>Close all active corpus paths and implement immutable reuse through explicit corpus projections.</objective>
<execution_context>@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @64.4-05-SUMMARY.md @src/repositories/policy_chunk_repo.py @src/rag/search_text_backfill.py @scripts/seed_demo.py</context>

<tasks>
<task type="auto">
  <name>Task 1: Centralize active-current and historical repository scopes</name>
  <read_first>@src/repositories/policy_chunk_repo.py @src/repositories/document_block_repo.py @src/repositories/evidence_version_repo.py @src/repositories/rag_evaluation_round_repo.py</read_first>
  <files>src/repositories/policy_corpus_scope.py, src/repositories/policy_chunk_repo.py, src/repositories/document_block_repo.py, src/repositories/evidence_version_repo.py, src/repositories/rag_evaluation_round_repo.py, tests/repositories/test_policy_chunk_repo.py, tests/knowledge/test_evidence_projection.py</files>
  <action>Add a mandatory scope DTO/helper. Current search/list/get/write/delete uses tenant pointer plus corpus projection; inactive/cross-tenant rows are invisible. Historical replay resolves explicit stored immutable document/chunk/evidence IDs without current pointer and remains byte exact. Evaluation paths require explicit corpus ownership rather than silently using active. Tests cover inactive isolation, exactly one active authority, corpus binding reuse, cross-tenant denial and legacy replay. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/repositories/test_policy_chunk_repo.py tests/knowledge/test_evidence_projection.py tests/replay/test_production_evidence_binding.py</automated></verify>
  <done>Current and historical authority are explicit and non-conflicting.</done>
</task>
<task type="auto">
  <name>Task 2: Route production ingestion, seed and backfill through active configuration</name>
  <read_first>@src/rag/ingestion.py @src/rag/search_text_backfill.py @scripts/seed_demo.py @tests/architecture/test_rag_chunking_boundaries.py</read_first>
  <files>src/rag/ingestion.py, src/rag/search_text_backfill.py, scripts/seed_demo.py, tests/knowledge/test_evidence_identity.py, tests/architecture/test_rag_chunking_boundaries.py</files>
  <action>Resolve the active corpus/config before ordinary ingestion: `character.v1` explicitly selects `CharacterCompatibilityAssembler`; token config selects `PolicyEmbeddingInputAssembler`; unknown/drifted config fails closed. Persist canonical document v2 independently from chunks and bind compatible immutable chunk versions through projection. Backfill/seed/delete require the same scope. Extend AST/rg audit to every PolicyChunk/DocumentBlock current path and reject direct corpus IDs in evidence identity/compatibility or `_document_citation_text(chunks)` as new identity authority. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/knowledge/test_evidence_identity.py tests/architecture/test_rag_chunking_boundaries.py tests/test_ingestion.py</automated></verify>
  <done>All ordinary production paths choose the active named config explicitly and preserve immutable/source identity rules.</done>
</task>
</tasks>

<threat_model>
| Threat | Boundary | Mitigation |
|---|---|---|
| T64.4-01 Disclosure | inactive/cross-tenant projection -> current read | Mandatory tenant pointer join and negative repository tests. |
| T64.4-03 Tampering | corpus ID -> evidence identity | Static and unit guards exclude corpus from compatibility/hash inputs. |
</threat_model>
<validation_mapping>
| Task | Gate |
|---|---|
| 64.4-06-01 | active/historical/projection repository tests |
| 64.4-06-02 | ingestion/backfill/seed/static boundary tests |
</validation_mapping>
<verification>Run both gates and AST audit; no unscoped production path may remain.</verification>
<success_criteria>SC4 current/historical behavior and D-12..D-16 are closed across all known paths.</success_criteria>
<output>Create `64.4-06-SUMMARY.md`.</output>
