---
phase: 64.4-token-aware-policy-chunking-and-reindex-validation
plan: "02"
name: Exact Final Input Token Chunker
type: execute
wave: 2
depends_on: [64.4-01]
files_modified:
  - src/rag/chunker.py
  - src/rag/policy_embedding_input.py
  - tests/rag/test_token_aware_chunker.py
  - tests/rag/test_block_chunker.py
  - tests/test_chunker.py
autonomous: true
requirements: [SC-64.4-2]
must_haves:
  truths:
    - "PolicyEmbeddingInputAssembler is the sole authority returning citation content, search text, exact provider input, exact count, config fingerprint and provenance together."
    - "All final inputs including envelope, table headers, overlap and source context are <=512 tokens; deterministic structural splitting never truncates and always progresses."
  artifacts:
    - {path: src/rag/policy_embedding_input.py, provides: "Authoritative final-input DTO and assembler", contains: "PolicyEmbeddingInputAssembler"}
  key_links:
    - {from: src/rag/policy_embedding_input.py, to: src/rag/embedding_tokenizer.py, via: "count final byte string after all decoration", pattern: "token_count"}
---

<objective>Implement the exact-final-string token-aware structural assembler in one bounded unit.</objective>
<execution_context>@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @64.4-01-SUMMARY.md @src/rag/chunker.py @src/rag/parsers/base.py @src/rag/parsers/registry.py @.planning/phases/64.4-token-aware-policy-chunking-and-reindex-validation/64.4-CONTEXT.md</context>

<tasks>
<task type="auto">
  <name>Task 1: Define the final-input DTO and deterministic structural splitter</name>
  <read_first>@src/rag/chunker.py @src/rag/parsers/base.py @src/rag/parsers/registry.py @tests/rag/test_block_chunker.py @tests/test_chunker.py</read_first>
  <files>src/rag/chunker.py, src/rag/policy_embedding_input.py, tests/rag/test_token_aware_chunker.py, tests/rag/test_block_chunker.py, tests/test_chunker.py</files>
  <action>Write property/contract tests first. Implement immutable assembly DTO and deterministic split order: parsed blocks, table rows, sentence/clause, then tokenizer windows for one oversized unit. Preserve row/header/protected-value/source provenance; charge repeated header, exact envelope, allowed source context and token overlap inside 512. Prove monotonic progress for empty, envelope-dominant, long unpunctuated and oversized cell inputs. Citation content, search text and embedding input remain distinct. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/rag/test_token_aware_chunker.py tests/rag/test_block_chunker.py tests/test_chunker.py</automated></verify>
  <done>Every structural path returns a bounded DTO without truncation or provenance loss.</done>
</task>
<task type="auto">
  <name>Task 2: Lock Unicode, overlap, table and rebuild invariants</name>
  <read_first>@src/rag/policy_embedding_input.py @tests/rag/test_token_aware_chunker.py @evaluation/golden/embedding_tokenizer_count_probes.v1.json</read_first>
  <files>src/rag/chunker.py, src/rag/policy_embedding_input.py, tests/rag/test_token_aware_chunker.py</files>
  <action>Add Chinese, English, mixed, OCR-like, combining Unicode, emoji, URLs, numbers, Markdown tables, zero-content and adversarial no-progress tests. Recount the returned exact final string and require equality; identical source/config must produce byte-identical DTO sequence. Reject envelope-only overflow and tokenizer failure before embedding/persistence. No independent legacy production algorithm or hidden character fallback is added. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/rag/test_token_aware_chunker.py tests/rag/test_block_chunker.py</automated></verify>
  <done>The assembler satisfies D-06..D-10 deterministically across all locked formats and failures.</done>
</task>
</tasks>

<threat_model>
| Threat | Boundary | Mitigation |
|---|---|---|
| T64.4-08 DoS | oversized/Unicode input -> splitter | Ordered bounded splitting, monotonic progress assertion, property tests. |
| T64.4-09 Tampering | appended envelope -> provider overflow | Exact final string is counted last and consumed unchanged. |
</threat_model>
<validation_mapping>
| Task | Gate |
|---|---|
| 64.4-02-01 | structural/table/progress tests |
| 64.4-02-02 | multilingual/determinism/recount/failure tests |
</validation_mapping>
<verification>Run both gates with pinned tokenizer and full lint.</verification>
<success_criteria>SC-64.4-2 and D-06..D-10 are fully covered.</success_criteria>
<output>Create `64.4-02-SUMMARY.md`.</output>
