---
phase: 64.4-token-aware-policy-chunking-and-reindex-validation
plan: "09"
name: Exact A-B Runtime And Immutable Selection
type: execute
wave: 9
depends_on: [64.4-08]
files_modified:
  - src/rag/evaluation/token_chunk_ab.py
  - src/rag/evaluation/retrieval_rounds.py
  - src/rag/evaluation/reporting.py
  - src/repositories/rag_evaluation_round_repo.py
  - scripts/eval_rag_token_chunk_ab.py
  - evaluation/reports/rag_token_chunk_ab/v1/README.md
  - evaluation/reports/rag_token_chunk_ab/v1/runs/
  - tests/eval/test_rag_token_chunk_ab.py
  - tests/eval/test_rag_retrieval_round_isolation.py
  - tests/eval/test_rag_format_parity_report.py
autonomous: true
requirements: [SC-64.4-6]
must_haves:
  truths:
    - "Same-run character incumbent and token candidate share sealed Phase64.3 inputs/provider/retrieval configuration in isolated corpus/round owners."
    - "All thresholds compare exact integers, Fraction or Decimal from raw numerator/denominator; rounding occurs only in report display."
    - "Every terminal A-B outcome writes one create-only canonical run report; selected_pass additionally writes a separate immutable selection decision, while failed/unavailable/error runs never do."
    - "The parity run, terminal A-B report, and selection decision are separate immutable artifacts; a selected_pass decision hash is final and never receives activation fields or rewrites."
  artifacts:
    - {path: src/rag/evaluation/token_chunk_ab.py, provides: "Exact metric/gate and immutable selection contract", contains: "rag_token_chunk_selection.v1"}
    - {path: evaluation/reports/rag_token_chunk_ab/v1/runs/, provides: "Create-only canonical JSON/Markdown for every terminal run outcome", contains: "candidate_failed"}
  key_links:
    - {from: scripts/eval_rag_token_chunk_ab.py, to: src/rag/policy_embedding_input.py, via: "token candidate sole assembly path", pattern: "PolicyEmbeddingInputAssembler"}
---

<objective>Produce an exact, reproducible and immutable token-aware selection decision against Phase64.3.</objective>
<execution_context>@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @64.4-08-SUMMARY.md @evaluation/reports/rag_format_parity/v1/baseline.json @evaluation/golden/rag_format_parity_gold.json @src/rag/evaluation/retrieval_rounds.py</context>

<tasks>
<task type="auto">
  <name>Task 1: Define exact A-B metrics, gates and immutable selection schema</name>
  <read_first>@src/rag/evaluation/reporting.py @src/rag/evaluation/retrieval_rounds.py @evaluation/reports/rag_format_parity/v1/baseline.json @.planning/phases/64.4-token-aware-policy-chunking-and-reindex-validation/64.4-RESEARCH.md</read_first>
  <files>src/rag/evaluation/token_chunk_ab.py, src/rag/evaluation/reporting.py, evaluation/reports/rag_token_chunk_ab/v1/README.md, evaluation/reports/rag_token_chunk_ab/v1/runs/, tests/eval/test_rag_token_chunk_ab.py, tests/eval/test_rag_format_parity_report.py</files>
  <action>Wave0 threshold boundary tests first. Store metric raw successes/denominators and compare `Fraction`: Hit5 at least max(9/10, incumbent); spread at most min(1/10, incumbent); Hit1/3/MRR decline at most 1/45; per-format Hit5 decline at most 1/15; anchor/locator/fallback decline at most 1/54; duplicate increase at most 1/50; chunks at most 3/2 incumbent; tokens at most 5/4 incumbent. Decimal cost uses explicit versioned basis. Never compare rounded `0.022223/0.066667/0.018519`; those are display-only. Define hard proofs and outcomes `selected_pass|candidate_failed|unavailable|execution_error`, with `candidate_failed.failure_class=quality_fail|safety_fail`. Every terminal run writes create-only canonical `runs/{run_id}.json/.md`; tests require strict safe schema, deterministic JSON/Markdown projection, byte immutability and truthful red/unavailable/error evidence. Only `selected_pass` additionally creates `selections/{selection_id}.json/.md`, a create-only canonical decision hash referencing the run and fresh parity hashes, sealed identities/configs and observations; it contains no activation/pointer fields. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/eval/test_rag_token_chunk_ab.py tests/eval/test_rag_format_parity_report.py</automated></verify>
  <done>Every exact safety/quality/resource decision has boundary proof without rounding drift or mutable selection evidence.</done>
</task>
<task type="auto">
  <name>Task 2: Run isolated incumbent/candidate and write immutable selection evidence</name>
  <read_first>@src/rag/evaluation/token_chunk_ab.py @src/rag/evaluation/retrieval_rounds.py @src/repositories/rag_evaluation_round_repo.py @scripts/eval_rag_format_parity.py</read_first>
  <files>src/rag/evaluation/token_chunk_ab.py, src/rag/evaluation/retrieval_rounds.py, src/repositories/rag_evaluation_round_repo.py, scripts/eval_rag_token_chunk_ab.py, evaluation/reports/rag_token_chunk_ab/v1/runs/, tests/eval/test_rag_token_chunk_ab.py, tests/eval/test_rag_retrieval_round_isolation.py</files>
  <action>Implement full-provider CLI under fixed evaluation tenant/owner and UUID run. Seal exact Phase64.3 `manifest_hash=e5544b20ecdf05c2eaf3325b4e5f89a4ef752c0b8c0d23b8bac224f006fdd53b`, `gold_hash=c6dc12536270fa9b9532ec4595e0a91d2b4ebddf83754a0f1ec107caabb64b8e`, `dataset_baseline_identity=3b1ddd8c19f8fce0a37ad113f3d1161039c200e39e60ce0f2e4d0917d870e110`, 45 answerable/54 total cases, ordered questions and provider/retrieval identities. Character incumbent uses only named baseline assembler; token candidate uses only production `PolicyEmbeddingInputAssembler`; both use identical provider/runtime and isolated inactive corpus/round ownership. Collect raw per-case metrics, duplicates, chunks, offline/provider token totals/status, latency and explicit cost. Always persist one truthful immutable terminal run JSON/Markdown pair, including quality/safety failure, unavailable and execution failure. Require a fresh immutable passed parity run for selected fingerprint; only an all-hard/all-exact-gate `selected_pass` run additionally writes a selection decision. No pointer mutation occurs in this plan. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/eval/test_rag_token_chunk_ab.py tests/eval/test_rag_retrieval_round_isolation.py tests/eval/test_rag_format_parity_report.py</automated></verify>
  <done>A real same-run evaluation always retains its terminal report and can create a separate immutable selected_pass decision without touching active state.</done>
</task>
</tasks>

<threat_model>
| Threat | Boundary | Mitigation |
|---|---|---|
| T64.4-17 Tampering | rounded metric -> selection | Raw numerator/denominator, Fraction/Decimal comparisons and boundary tests. |
| T64.4-07 Disclosure | evaluation -> artifact | Strict safe allowlist; no content, credentials, DSN, raw provider object or exceptions. |
</threat_model>
<validation_mapping>
| Task | Gate |
|---|---|
| 64.4-09-01 | exact Fraction/Decimal boundary and immutable schema tests |
| 64.4-09-02 | isolated same-run runtime/report tests |
</validation_mapping>
<verification>CI tests all contracts; final completion additionally requires real provider selected_pass with exact hashes.</verification>
<success_criteria>D-20..D-24 and SC-64.4-6 selection semantics are exact, immutable and provider-attributable.</success_criteria>
<output>Create `64.4-09-SUMMARY.md`.</output>
