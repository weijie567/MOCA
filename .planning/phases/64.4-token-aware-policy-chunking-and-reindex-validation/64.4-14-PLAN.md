---
phase: 64.4-token-aware-policy-chunking-and-reindex-validation
plan: "14"
name: Canonical Recovery Budget And Pre-CAS Authority
type: execute
wave: 14
depends_on: [64.4-13]
files_modified:
  - src/rag/evaluation/token_chunk_ab.py
  - scripts/eval_rag_token_chunk_ab.py
  - src/rag/activation_receipt.py
  - src/rag/policy_reindex.py
  - scripts/reindex_policies.py
  - evaluation/reports/rag_token_chunk_ab/v1/recovery-budgets/README.md
  - evaluation/reports/rag_token_chunk_ab/v1/recovery-authorizations/README.md
  - tests/eval/test_rag_token_chunk_ab.py
  - tests/rag/test_activation_receipt.py
  - tests/rag/test_policy_reindex.py
  - .planning/ARCHITECTURE-DEBT.md
  - .planning/LOCAL-VALIDATION-ISSUES.md
autonomous: true
requirements: [SC-64.4-5, SC-64.4-6]
must_haves:
  truths:
    - "Full-provider recovery accepts only the repository canonical resolved output root and canonical budget identity; copied/alternate roots, manifests and attempt directories refuse before provider construction."
    - "Every reservation re-hashes and strict-loads the exact candidate state, matching corpus/run/config/parity/source identity to the canonical manifest before consuming an ordinal."
    - "Selection produces a separate create-only recovery authorization binding canonical manifest, reservation, candidate state, terminal and selection hashes; activation validates it before CAS, so closeout is not the first enforcement point."
  artifacts:
    - {path: evaluation/reports/rag_token_chunk_ab/v1/recovery-authorizations/README.md, provides: "Immutable canonical budget-to-selection-to-activation authority", contains: "rag_token_chunk_recovery_authorization.v1"}
  key_links:
    - {from: scripts/eval_rag_token_chunk_ab.py, to: evaluation/reports/rag_token_chunk_ab/v1/recovery-budgets/, via: "canonical root and candidate-state verification before reserve/provider", pattern: "canonical_recovery_root"}
    - {from: evaluation/reports/rag_token_chunk_ab/v1/recovery-authorizations/, to: src/rag/policy_reindex.py, via: "strict activation loader projection required before pointer CAS", pattern: "recovery_authorization_sha256"}
---

<objective>Make the two-slot A-B cap and candidate lineage unbypassable, then carry that authority into activation before any pointer CAS.</objective>
<execution_context>@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @64.4-12-SUMMARY.md @64.4-13-SUMMARY.md @src/rag/evaluation/token_chunk_ab.py @src/rag/activation_receipt.py @scripts/reindex_policies.py</context>

<tasks>
<task type="auto">
  <name>Task 1: Canonicalize budget root and bind exact candidate state before reservation</name>
  <read_first>@src/rag/evaluation/token_chunk_ab.py @scripts/eval_rag_token_chunk_ab.py @evaluation/reports/rag_token_chunk_ab/v1/recovery-budgets/README.md @tests/eval/test_rag_token_chunk_ab.py</read_first>
  <files>src/rag/evaluation/token_chunk_ab.py, scripts/eval_rag_token_chunk_ab.py, evaluation/reports/rag_token_chunk_ab/v1/recovery-budgets/README.md, tests/eval/test_rag_token_chunk_ab.py, .planning/ARCHITECTURE-DEBT.md, .planning/LOCAL-VALIDATION-ISSUES.md</files>
  <action>Wave0 tests first. Define one repository-relative canonical recovery root resolved from the checked repository root; the production full-provider CLI rejects any alternate/symlink/outside `--output-root` before budget write or provider construction. The fixed Plan12 manifest has one canonical path and records exact candidate state file SHA plus candidate corpus/run/owner/config/parity/source/evidence identity. `reserve_recovery_attempt` must receive and re-hash the actual candidate-state path, strict-load it, match every field to the manifest and fresh parity, then atomically reserve under the canonical attempts directory. Copied manifests, cloned roots, alternate candidate state, stale state version, mismatched UUIDs/hashes and two concurrent roots refuse. Preserve injectable temporary roots only in unit-level store APIs, never through production CLI authority. Add adversarial tests proving no new run/selection UUID can gain extra slots from a copied manifest and refusal occurs before provider factory. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/eval/test_rag_token_chunk_ab.py tests/eval/test_rag_retrieval_round_isolation.py</automated></verify>
  <done>The production entry point has exactly one cap=2 namespace and every ordinal is bound to the exact live candidate state before provider construction.</done>
</task>
<task type="auto">
  <name>Task 2: Bind selection to recovery lineage and enforce it before activation CAS</name>
  <read_first>@src/rag/activation_receipt.py @src/rag/policy_reindex.py @scripts/reindex_policies.py @src/rag/evaluation/token_chunk_ab.py @tests/rag/test_activation_receipt.py @tests/rag/test_policy_reindex.py</read_first>
  <files>src/rag/evaluation/token_chunk_ab.py, scripts/eval_rag_token_chunk_ab.py, src/rag/activation_receipt.py, src/rag/policy_reindex.py, scripts/reindex_policies.py, evaluation/reports/rag_token_chunk_ab/v1/recovery-authorizations/README.md, tests/eval/test_rag_token_chunk_ab.py, tests/rag/test_activation_receipt.py, tests/rag/test_policy_reindex.py, .planning/ARCHITECTURE-DEBT.md, .planning/LOCAL-VALIDATION-ISSUES.md</files>
  <action>Define create-only `rag_token_chunk_recovery_authorization.v1` written or byte-identically reconciled only after selected_pass. It binds canonical budget manifest SHA/path identity, exact ordinal reservation SHA/number, candidate state SHA/corpus/run/config/parity/source, terminal run SHA/ID and selection SHA/ID; run/selection v1 bytes remain unchanged. Activation artifact paths must include authorization, manifest, reservation and candidate state. `load_activation_authority` strict-loads and cross-checks the complete lineage, rejects copied/noncanonical roots and projects `recovery_authorization_sha256` into the in-memory real selection proof. `PolicyReindexService._validate_selection_proof` requires that field for real selected cutover/restore before any CAS while keeping Plan08 fixture behavior explicit and isolated. Extend activation CLI arguments accordingly. Test missing/tampered/alternate manifest, wrong candidate state/reservation, over-budget selection, fabricated authorization and direct real activation without authority all fail before pointer/history mutation; valid lineage still supports receipt creation. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/eval/test_rag_token_chunk_ab.py tests/rag/test_activation_receipt.py tests/rag/test_policy_reindex.py tests/replay/test_production_evidence_binding.py</automated></verify>
  <done>Selection is immutably bound to one canonical budget ordinal and exact candidate, and real activation cannot CAS without that verified recovery authority.</done>
</task>
</tasks>

<threat_model>
| Threat | Boundary | Mitigation |
|---|---|---|
| T64.4-02 Tampering | alternate root/manifest -> extra attempts | Canonical resolved production root and single manifest/attempt namespace before provider. |
| T64.4-18 Elevation | unrelated candidate/reservation -> selection | Exact candidate-state and reservation hashes in create-only recovery authorization. |
| T64.4-19 Repudiation | selection lineage -> activation CAS | Strict loader plus required in-memory authorization SHA before pointer mutation. |
| T64.4-07 Disclosure | authority artifacts -> repository | IDs/hashes/codes only; no content, credentials, DSN or absolute machine paths. |
</threat_model>
<validation_mapping>
| Task | Gate |
|---|---|
| 64.4-14-01 | canonical-root, copied-manifest, candidate binding and over-budget refusal tests |
| 64.4-14-02 | recovery authorization lineage and pre-CAS activation refusal tests |
</validation_mapping>
<verification>Only deterministic contract/PostgreSQL tests run; no live provider, candidate build, A-B slot or pointer mutation is permitted.</verification>
<success_criteria>The reviewed A-B cap cannot be reset by filesystem roots and every later activation is machine-bound to one canonical selected ordinal/candidate lineage.</success_criteria>
<output>Create `64.4-14-SUMMARY.md`.</output>
