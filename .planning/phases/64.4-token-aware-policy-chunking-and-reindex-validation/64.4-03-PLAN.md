---
phase: 64.4-token-aware-policy-chunking-and-reindex-validation
plan: "03"
name: Provider Usage And Immutable Parity Protocol
type: execute
wave: 3
depends_on: [64.4-02]
files_modified:
  - src/rag/embedder.py
  - src/rag/tokenizer_parity.py
  - scripts/check_embedding_tokenizer_parity.py
  - evaluation/golden/embedding_tokenizer_parity_probes.v1.json
  - evaluation/reports/rag_embedding_tokenizer/v1/README.md
  - tests/rag/test_embedding_usage.py
  - tests/rag/test_tokenizer_parity.py
  - tests/test_embedder.py
autonomous: true
requirements: [ROADMAP-SC-1]
must_haves:
  truths:
    - "Provider request usage is retained truthfully per request; aggregate batch usage is never invented as per-input usage."
    - "Parity evidence is immutable per run and fingerprint, distinguishes passed/quarantined/unavailable, and is fresh for selection; final probe strings are supplied by the authoritative assembler added in Plan 02."
  artifacts:
    - {path: src/rag/tokenizer_parity.py, provides: "Immutable parity schema, freshness policy, and gate", contains: "embedding_tokenizer_parity.v1"}
    - {path: src/rag/embedder.py, provides: "Usage-aware embedding result", contains: "EmbeddingBatchResultV1"}
  key_links:
    - {from: scripts/check_embedding_tokenizer_parity.py, to: src/rag/tokenizer_parity.py, via: "strict immutable report writer", pattern: "provider_parity_status"}
---

<objective>Make provider usage and parity a truthful immutable protocol without yet creating a second assembly implementation.</objective>
<execution_context>@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md</execution_context>
<context>@AGENTS.md @64.4-02-SUMMARY.md @src/rag/embedder.py @tests/test_embedder.py @.planning/phases/64.4-token-aware-policy-chunking-and-reindex-validation/64.4-CONTEXT.md</context>

<tasks>
<task type="auto">
  <name>Task 1: Retain safe request-level provider usage</name>
  <read_first>@src/rag/embedder.py @tests/test_embedder.py @src/config.py</read_first>
  <files>src/rag/embedder.py, tests/rag/test_embedding_usage.py, tests/test_embedder.py</files>
  <action>Write tests, then add frozen request usage/batch result DTOs and `embed_documents_with_usage`. Preserve vector-only wrappers, response ordering/dimensions/retries, cap batches at ten, aggregate totals only when every request reports usage, and never divide totals among inputs. Missing usage is unavailable; failure/report objects exclude text, key, URL, raw response, path, and exception. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/rag/test_embedding_usage.py tests/test_embedder.py</automated></verify>
  <done>Embedding calls retain only truthful safe request-level accounting.</done>
</task>
<task type="auto">
  <name>Task 2: Define immutable parity and freshness contracts</name>
  <read_first>@src/rag/embedding_tokenizer.py @src/rag/embedder.py @evaluation/golden/embedding_tokenizer_count_probes.v1.json</read_first>
  <files>src/rag/tokenizer_parity.py, scripts/check_embedding_tokenizer_parity.py, evaluation/golden/embedding_tokenizer_parity_probes.v1.json, evaluation/reports/rag_embedding_tokenizer/v1/README.md, tests/rag/test_tokenizer_parity.py</files>
  <action>Define strict `embedding_tokenizer_parity.v1` with safe labels/counts, exact config/model, run UUID, captured timestamp/region class, content hash, passed|quarantined|unavailable and allowlisted reasons. Persist each run to fingerprint/run-id paths with create-only atomic semantics; never overwrite a prior run. `require_fresh_provider_parity` verifies schema, content hash, fingerprint, provider/model, maximum age, exact all-single plus ten-input aggregate equality. The CLI builds safe synthetic ParsedBlocks and obtains every submitted final string from the already-implemented `PolicyEmbeddingInputAssembler`; it must not implement any local envelope/splitting or accept caller-preassembled strings. Finish Python edits with `make format`.</action>
  <verify><automated>make lint &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run pytest -q tests/rag/test_tokenizer_parity.py tests/rag/test_embedding_usage.py</automated></verify>
  <done>Parity has immutable run identity, freshness, exact equality and safe unavailable/quarantine semantics, with no duplicate chunker.</done>
</task>
</tasks>

<threat_model>
| Threat | Boundary | Mitigation |
|---|---|---|
| T64.4-07 Disclosure | provider -> report | Strict allowlist; no probe text, keys, URLs, raw objects, or exceptions. |
| T64.4-11 Repudiation | stale/mutated parity -> selection | Create-only fingerprint/run artifact, content hash, maximum age and exact gate. |
</threat_model>
<validation_mapping>
| Task | Gate |
|---|---|
| 64.4-03-01 | usage/order/batch/unavailable tests |
| 64.4-03-02 | immutable parity/hash/freshness/safety tests |
</validation_mapping>
<verification>Run task gates after Plan02; live parity submissions must be exact assembler DTO strings.</verification>
<success_criteria>D-03, D-04 and D-24 are contractually covered without claiming provider success.</success_criteria>
<output>Create `64.4-03-SUMMARY.md`.</output>
