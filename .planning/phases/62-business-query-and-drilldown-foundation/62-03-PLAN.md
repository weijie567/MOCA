---
phase: 62-business-query-and-drilldown-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 62-02
files_modified:
  - src/auth/jwt.py
  - src/auth/permissions.py
  - src/platform/trusted_context.py
  - src/tools/catalog.py
  - src/tools/policy.py
  - src/tools/executors/business.py
  - tests/platform/test_trusted_context_factory.py
  - tests/tools/test_catalog.py
  - tests/tools/test_tool_platform.py
autonomous: true
requirements:
  - BQ-62-02
  - BQ-62-04
must_haves:
  truths:
    - "ToolPlatform exposes a read-only business_query descriptor only through trusted permissions."
    - "User text, LLM output, and frontend payloads cannot grant tenant, merchant scope, or business-query permission."
    - "Malformed business_query args are denied before execution, and business_metric_query remains a compatibility descriptor."
  artifacts:
    - path: "src/platform/trusted_context.py"
      provides: "trusted auth scope to tool:business_query permission projection"
      contains: "tool:business_query"
    - path: "src/tools/catalog.py"
      provides: "read-only business_query ToolCatalog descriptor and metric compatibility descriptor"
      contains: "business_query"
    - path: "tests/tools/test_tool_platform.py"
      provides: "ToolPolicy denial coverage for caller, permission, authority fields, raw SQL, and arbitrary filters"
      contains: "business_query"
  key_links:
    - from: "src/platform/trusted_context.py"
      to: "src/tools/catalog.py"
      via: "trusted scope maps to required_permission"
      pattern: "tool:business_query"
    - from: "src/tools/catalog.py"
      to: "src/business/query/schemas.py"
      via: "input/output schema derived from or mirrored to BusinessQuerySpec and BusinessQueryResultV1"
      pattern: "BusinessQuerySpec|business_query"
---

<objective>
Add the trusted ToolPlatform boundary for `business_query`.

Purpose: The strict schema from 62-02 must be visible and enforceable through the existing ToolPlatform policy layer before runtime execution is connected.
Output: trusted permission mapping, read-only ToolCatalog descriptor, policy denial tests, and safe executor boundary checks without database runtime work.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ARCHITECTURE-DEBT.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-CONTEXT.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-RESEARCH.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-PATTERNS.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-VALIDATION.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-01-SUMMARY.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-02-SUMMARY.md
@.planning/phases/61-product-experience-fixes/61-03-SUMMARY.md

<interfaces>
Contracts created by Plan 62-02:
```python
class BusinessQuerySpec(BaseModel): ...
class BusinessQueryResultV1(BaseModel): ...
def metric_input_to_business_query(metric: BusinessMetricQueryInput) -> BusinessQuerySpec: ...
```

Current trusted permission mapping:
```python
SCOPE_TO_TOOL_PERMISSION = {
    "metrics:read": "tool:query_business_metric",
}
```

Current catalog declaration shape:
```python
@dataclass(frozen=True)
class _ToolDeclaration:
    name: str
    kind: Literal["read", "retrieval", "write"]
    input_schema: dict[str, Any]
    output_schema: dict[str, Any]
    required_permission: str
    caller_allowlist: tuple[str, ...]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project trusted business-query permission</name>
  <files>src/auth/jwt.py, src/auth/permissions.py, src/platform/trusted_context.py, tests/platform/test_trusted_context_factory.py</files>
  <read_first>
    src/auth/jwt.py
    src/auth/permissions.py
    src/platform/trusted_context.py
    tests/platform/test_trusted_context_factory.py
    .planning/phases/62-business-query-and-drilldown-foundation/62-02-SUMMARY.md
  </read_first>
  <behavior>
    - Roles or JWT scopes that include the accepted business-query scope receive `tool:business_query`.
    - Missing trusted scopes fail closed and do not receive `tool:business_query`.
    - `metrics:read` to `tool:query_business_metric` compatibility remains intact.
    - User text, LLM slots, tool args, and frontend payloads have no path to inject `tool:business_query`.
  </behavior>
  <action>
    Add trusted OAuth/app scope `business:query` to role scope definitions in `src/auth/jwt.py` and `src/auth/permissions.py`, and map it to `tool:business_query` in `src/platform/trusted_context.py`.

    Preserve `metrics:read` to `tool:query_business_metric` compatibility unless the existing permission projection already supports one trusted scope mapping to both tool permissions. Any one-to-many mapping must be tested explicitly and must not widen existing metric permissions beyond the locked compatibility behavior per D-62-02.

    Tests must show that `tool:business_query` only comes from trusted auth context, not from request/body/tool args. This implements D-62-05 at the permission boundary while runtime scope separation remains owned by 62-04.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/platform/test_trusted_context_factory.py -q --tb=short</automated>
    <automated>rg -n "business:query|tool:business_query|tool:query_business_metric" src/auth/jwt.py src/auth/permissions.py src/platform/trusted_context.py tests/platform/test_trusted_context_factory.py</automated>
  </verify>
  <acceptance_criteria>
    - Trusted context tests prove authorized roles receive `tool:business_query`.
    - Missing token/role scope fails closed.
    - Existing metric compatibility permission remains green.
  </acceptance_criteria>
  <done>`business_query` permission is projected only from trusted context.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add read-only business_query descriptor and policy denials</name>
  <files>src/tools/catalog.py, src/tools/policy.py, src/tools/executors/business.py, tests/tools/test_catalog.py, tests/tools/test_tool_platform.py</files>
  <read_first>
    src/business/query/registry.py
    src/business/query/schemas.py
    src/tools/catalog.py
    src/tools/policy.py
    src/tools/executors/business.py
    tests/tools/test_catalog.py
    tests/tools/test_tool_platform.py
    .planning/phases/62-business-query-and-drilldown-foundation/62-02-SUMMARY.md
  </read_first>
  <behavior>
    - `business_query` is a read-only planner-visible ToolCatalog descriptor with `caller_allowlist=("investigate",)` and strict input/output schemas.
    - `business_query` requires trusted permission `tool:business_query`; wrong caller and missing permission are denied before runtime.
    - ToolPolicy denies tenant_id, merchant_scope, raw_sql, where, arbitrary filter objects, raw cursor strings, and other authority-bearing or free-form database fields before executor work.
    - `query_business_metric` remains visible for compatibility and is documented/tested as mapping into BusinessQuerySpec.
  </behavior>
  <action>
    Add a `business_query` `_ToolDeclaration` in `src/tools/catalog.py`:
    - `name="business_query"`
    - `kind="read"`
    - `side_effect="read_only"`
    - `required_permission="tool:business_query"`
    - `caller_allowlist=("investigate",)`
    - `resource_type="business_query"`
    - `executor="business"`
    - `input_schema` derived from `BusinessQuerySpec` and registry descriptors with `additionalProperties: false`
    - `output_schema` matching `BusinessQueryResultV1`

    Keep `query_business_metric` as a compatibility descriptor per D-62-02. Update `src/tools/policy.py` if required so policy validation denies authority fields, raw SQL keys, arbitrary filters, raw cursor strings, or caller nodes outside investigate before execution. Use existing `ToolPolicyDecision` reason codes where possible and add stable reason codes only when tests require them.

    Update `src/tools/executors/business.py` only for descriptor-safe tool-name recognition or explicit safe failure behavior required by ToolPlatform tests. Do not add database runtime, compiler calls, SQLAlchemy construction, repository access, or BusinessFactService query execution here; 62-04 owns runtime execution per D-62-06.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/tools/test_catalog.py tests/tools/test_tool_platform.py tests/business/test_business_query_schemas.py -q --tb=short</automated>
    <automated>rg -n "name=\"business_query\"|tool:business_query|additionalProperties|raw_sql|merchant_scope|tenant_id" src/tools/catalog.py src/tools/policy.py src/tools/executors/business.py tests/tools/test_catalog.py tests/tools/test_tool_platform.py</automated>
  </verify>
  <acceptance_criteria>
    - Tool catalog tests prove `business_query` is read-only, strict, and planner-visible only for investigate.
    - Tool platform tests prove wrong caller, missing permission, authority fields, raw SQL keys, arbitrary filters, and raw cursor strings are denied before runtime execution.
    - `business_query` descriptor does not expose tenant_id, merchant_scope, raw_sql, where, or arbitrary filter fields.
  </acceptance_criteria>
  <done>`business_query` has a strict ToolPlatform descriptor and policy boundary ready for 62-04 runtime execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JWT/trusted context to tool permission | Authenticated scopes become ToolCallContext permissions; user text must not grant permission. |
| ToolCatalog/ToolPolicy to executor | ToolPlatform must reject malformed or unauthorized specs before business runtime execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-08 | Elevation of Privilege | `TrustedContextFactory` and ToolPolicy | mitigate | Only trusted auth scopes map to `tool:business_query`; `merchant_id` remains a filter and cannot widen merchant scope. |
| T-62-09 | Information Disclosure | ToolCatalog schema | mitigate | `business_query` input schema excludes tenant_id, merchant_scope, raw SQL, where clauses, raw cursor strings, and arbitrary filters. |
| T-62-10 | Tampering | ToolPolicy | mitigate | Policy tests deny malformed args and wrong callers before executor work. |
| T-62-11 | Information Disclosure | `query_business_metric` compatibility path | mitigate | Compatibility descriptor remains read-only and prepares mapping into `BusinessQuerySpec` without bypassing policy. |
</threat_model>

<verification>
Run:

```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/platform/test_trusted_context_factory.py tests/tools/test_catalog.py tests/tools/test_tool_platform.py tests/business/test_business_query_schemas.py -q --tb=short
rg -n "business:query|tool:business_query|name=\"business_query\"|raw_sql|merchant_scope|tenant_id" src/auth/jwt.py src/auth/permissions.py src/platform/trusted_context.py src/tools/catalog.py src/tools/policy.py tests/tools/test_tool_platform.py
```
</verification>

<success_criteria>
- BQ-62-02 remains satisfied through ToolPlatform: `business_query` is the primary read descriptor and `business_metric_query` remains compatibility.
- BQ-62-04 is partially satisfied at policy boundary: malformed and authority-bearing inputs are denied before runtime.
- D-62-02, D-62-03, D-62-05, D-62-07, D-62-17, D-62-18, D-62-19, and D-62-20 are honored at the ToolPlatform boundary.
- Phase 63 risk/action taxonomy, Phase 64 RAG label unification, Phase 65 global label registry, Phase 66 config hygiene, and suggested Phase 67 state-machine registry remain explicit deferrals.
</success_criteria>

<output>
After completion, create `.planning/phases/62-business-query-and-drilldown-foundation/62-03-SUMMARY.md`.
</output>
