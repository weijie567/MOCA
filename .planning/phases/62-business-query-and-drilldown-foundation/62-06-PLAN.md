---
phase: 62-business-query-and-drilldown-foundation
plan: 06
type: execute
wave: 6
depends_on:
  - 62-05
files_modified:
  - src/business/query/projection.py
  - src/tools/projection.py
  - src/agent/nodes/final_response.py
  - src/api/routers/agent_runs.py
  - src/api/schemas/agent_runs.py
  - tests/tools/test_projection.py
  - tests/agent/test_nodes/test_final_response.py
  - tests/agent/test_graph.py
  - tests/test_agent_runs_api.py
  - scripts/eval_phase62_business_query.py
  - evaluation/golden/phase62_business_query_cases.jsonl
  - tests/eval/test_phase62_business_query_golden.py
autonomous: true
requirements:
  - BQ-62-06
  - BQ-62-08
  - BQ-62-04
must_haves:
  truths:
    - "Final responses and API/SSE payloads safely distinguish aggregate, list, detail, breakdown, and compare business-query answers."
    - "Backend projection emits bounded prompt-safe and UI-safe business_query payloads without raw rows, raw filters, hidden scope internals, or raw cursor tokens."
    - "Phase 62 golden/eval coverage includes drilldown, permission boundaries, list/detail no-existence-leak, breakdown, compare, projection bounds, clarification, and unsupported cases."
  artifacts:
    - path: "src/business/query/projection.py"
      provides: "business_query prompt-safe and UI-safe projection helpers"
      contains: "safe_business_query"
    - path: "src/agent/nodes/final_response.py"
      provides: "business_query_answer response text and metadata"
      contains: "business_query_answer"
    - path: "src/api/routers/agent_runs.py"
      provides: "allowlisted business_query SSE final payload"
      contains: "business_query"
    - path: "evaluation/golden/phase62_business_query_cases.jsonl"
      provides: "Phase 62 regression golden cases"
      contains: "drilldown"
  key_links:
    - from: "src/agent/nodes/final_response.py"
      to: "src/business/query/projection.py"
      via: "safe business_query response text and metadata"
      pattern: "safe_business_query|business_query_response_text"
    - from: "src/api/routers/agent_runs.py"
      to: "src/api/schemas/agent_runs.py"
      via: "business_query payload allowlist"
      pattern: "business_query"
---

<objective>
Finish backend projection, final response, API payloads, and deterministic Phase 62 eval/golden coverage for business-query answers.

Purpose: Runtime is not complete until backend answer text, API/SSE payloads, and regression fixtures prove no-existence-leak, drilldown, breakdown, compare, and raw-payload stripping.
Output: `business_query_answer` final/API payload, Phase 62 eval/golden suite, and backend validation.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ARCHITECTURE-DEBT.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-CONTEXT.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-RESEARCH.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-PATTERNS.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-VALIDATION.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-UI-SPEC.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-01-SUMMARY.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-02-SUMMARY.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-03-SUMMARY.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-04-SUMMARY.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-05-SUMMARY.md
@.planning/phases/61-product-experience-fixes/61-05-SUMMARY.md

<interfaces>
Current final/API metric anchors:
```python
def _metric_response_text(metric: dict[str, Any]) -> str: ...
def _metric_llm_output(response_text: str, metric: dict[str, Any]) -> dict[str, Any]: ...
def _final_response_payload(final_response: str, final_state: dict[str, Any]) -> dict[str, Any]: ...
```

UI target from `62-UI-SPEC.md`:
```text
response_kind: business_query_answer
business_query.operation: aggregate | list | detail | breakdown | compare
business_query rows are backend-projected UI-safe rows only.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project business_query answers into final response and API/SSE payloads</name>
  <files>src/business/query/projection.py, src/tools/projection.py, src/agent/nodes/final_response.py, src/api/routers/agent_runs.py, src/api/schemas/agent_runs.py, tests/tools/test_projection.py, tests/agent/test_nodes/test_final_response.py, tests/test_agent_runs_api.py</files>
  <read_first>
    src/business/query/schemas.py
    src/tools/projection.py
    src/agent/nodes/final_response.py
    src/api/routers/agent_runs.py
    tests/tools/test_projection.py
    tests/agent/test_nodes/test_final_response.py
    tests/test_agent_runs_api.py
    .planning/phases/62-business-query-and-drilldown-foundation/62-UI-SPEC.md
    .planning/phases/62-business-query-and-drilldown-foundation/62-05-SUMMARY.md
  </read_first>
  <behavior>
    - Final response for aggregate starts with the number/rate and includes safe scope, time, filters, and freshness.
    - Final response for list states bounded result count and includes up to five safe identifiers when useful, never raw rows.
    - Final response for detail, breakdown, and compare follows the UI spec and never claims RAG/policy evidence for business facts.
    - API/SSE final payload includes `response_kind="business_query_answer"` and allowlisted `business_query` fields only.
    - Denied and empty list/detail payloads use no-existence-leak safe copy and do not include sensitive ids or existence wording.
  </behavior>
  <action>
    Create `src/business/query/projection.py` with helpers that convert the normalized `BusinessQueryResultV1` payload from `BusinessFactResultV1.fact_data["business_query"]` into prompt-safe and UI-safe payloads. Adapters may accept projected tool result dictionaries only after validating that they contain the same normalized business-query shape. Required exported helpers:
    - `business_query_response_text`
    - `safe_business_query_metadata`
    - `safe_business_query_api_payload`

    Update `src/tools/projection.py` to add a business-query projection branch. The projection must produce normalized data with operation, resource, result_label, scope_label, time_label, filters_label, freshness_label, status, safe_reason, row_count, limit, cursor capability, allowed drilldowns, and allowlisted rows. Prompt summary must be bounded to safe labels and up to five safe row identifiers for list; it must not include raw row dictionaries.

    Update `src/agent/nodes/final_response.py` with `_business_query_fact`, `_business_query_response_text`, and `_business_query_llm_output`. Use `response_kind="business_query_answer"` for new Phase 62 query results. Preserve `metric_answer` compatibility where `query_business_metric` compatibility output still reaches existing tests.

    Update `src/api/routers/agent_runs.py` and `src/api/schemas/agent_runs.py` so final SSE/update payloads include only these fields under `business_query`: operation, resource_label, result_label, scope_label, time_label, filters_label, freshness_label, fields_label, safe_reason, rows, row_count, limit, cursor_label, allowed_drilldowns, group_by_label, compare_label. Reject or omit raw rows, raw filters, hidden scope internals, prompt payloads, tool args, routing hints, raw cursor tokens, stack traces, and denied sensitive ids.

    Tests must cover all five operations, denied/empty no-existence-leak, raw sentinel stripping, bounded row projection, no RAG evidence claims, and API payload allowlisting. Add an API schema snapshot or equivalent contract test for `business_query_answer.business_query` so frontend/backend drift is visible.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/tools/test_projection.py tests/agent/test_nodes/test_final_response.py tests/test_agent_runs_api.py -q --tb=short</automated>
    <automated>rg -n "business_query_answer|safe_business_query|business_query" src/business/query/projection.py src/tools/projection.py src/agent/nodes/final_response.py src/api/routers/agent_runs.py src/api/schemas/agent_runs.py</automated>
  </verify>
  <acceptance_criteria>
    - Backend tests prove operation-specific final response text for aggregate/list/detail/breakdown/compare.
    - API tests prove safe payload allowlist and no raw rows, raw args, routing hints, tenant_id, merchant_scope, raw cursor, or sensitive denied ids.
    - Projection tests prove raw `ToolResultV2.data` dictionaries are rejected or normalized before projection; projection consumes the stable `fact_data["business_query"]` handoff from 62-04.
    - API contract tests lock the `business_query_answer.business_query` allowlist consumed by frontend Phase 62 UI.
    - Final response tests prove business-fact-only answers do not claim RAG/policy evidence.
  </acceptance_criteria>
  <done>Backend final/API projection emits safe typed `business_query_answer` payloads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Phase 62 golden and deterministic eval coverage</name>
  <files>scripts/eval_phase62_business_query.py, evaluation/golden/phase62_business_query_cases.jsonl, tests/eval/test_phase62_business_query_golden.py</files>
  <read_first>
    scripts/eval_phase61_ux.py
    tests/eval/test_phase61_ux_golden.py
    evaluation/golden/phase61_ux_cases.jsonl
    .planning/phases/62-business-query-and-drilldown-foundation/62-VALIDATION.md
    .planning/phases/62-business-query-and-drilldown-foundation/62-CONTEXT.md
    .planning/phases/62-business-query-and-drilldown-foundation/62-05-SUMMARY.md
  </read_first>
  <behavior>
    - Golden cases include required drilldown `本周多少订单？` followed by `订单号是多少？`.
    - Golden cases include permission boundary, list no-existence-leak, detail no-existence-leak, breakdown runtime example, compare runtime example, projection bounds, clarification, and unsupported business-query boundaries.
    - Eval validator rejects missing required categories, invalid response kinds, raw payload expectations, and no-existence-leak cases without `expected_no_leak=true`.
  </behavior>
  <action>
    Create `scripts/eval_phase62_business_query.py` following the deterministic validator style of `scripts/eval_phase61_ux.py`. It must load JSONL, validate required categories, validate allowed response kinds including `business_query_answer`, validate expected operation/tool fields, validate multi-turn grouped cases, and write an optional JSON report under `evaluation/reports/`.

    Create `evaluation/golden/phase62_business_query_cases.jsonl` with required categories:
    - aggregate_order_this_week
    - aggregate_to_list_drilldown_order_no
    - unauthorized_merchant_list_no_leak
    - unauthorized_order_detail_no_leak
    - breakdown_order_by_status
    - compare_order_count_previous_period
    - projection_bounds_no_raw_rows
    - missing_business_query_time_range
    - unsupported_business_query_boundary

    Create `tests/eval/test_phase62_business_query_golden.py` to assert validator success, required categories, no-existence-leak flags, and the required drilldown sequence. Add graph/API tests or assertions that selected golden categories correspond to real agent/API behavior for aggregate, drilldown, unauthorized list/detail no-existence-leak, breakdown, and compare. Use `uv run python` only in docs/comments; all pytest verification commands must use MOCA-approved test entrypoints.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_phase62_business_query_golden.py tests/agent/test_graph.py tests/test_agent_runs_api.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/eval_phase62_business_query.py --golden-set evaluation/golden/phase62_business_query_cases.jsonl --output /tmp/phase62_business_query_eval.json</automated>
    <automated>rg -n "aggregate_to_list_drilldown_order_no|breakdown_order_by_status|compare_order_count_previous_period|expected_no_leak" evaluation/golden/phase62_business_query_cases.jsonl scripts/eval_phase62_business_query.py tests/eval/test_phase62_business_query_golden.py</automated>
  </verify>
  <acceptance_criteria>
    - Eval tests pass and required categories are present.
    - Golden data includes the exact required drilldown prompt sequence from D-62-15.
    - Golden data includes runtime-backed breakdown and compare examples per D-62-16.
    - At least one real graph/API test exercises each required high-risk golden family: aggregate-to-list drilldown, unauthorized list/detail no-existence-leak, breakdown, and compare.
    - Golden validator rejects raw payload leakage and missing no-existence-leak assertions.
  </acceptance_criteria>
  <done>Phase 62 regression eval/golden coverage is executable and covers required safety/product cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool result to final response | Backend projection turns business facts into user-visible text and metadata. |
| final state to API/SSE | API exposes only allowlisted fields to the browser. |
| golden/eval fixtures to regression gate | Eval data locks required safety and drilldown scenarios without live LLM dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-20 | Information Disclosure | `final_response.py` | mitigate | Business-query answer text uses safe projections and no-existence-leak copy; tests assert no RAG claims or sensitive denied ids. |
| T-62-21 | Information Disclosure | `agent_runs.py` API/SSE payload | mitigate | `_safe_business_query_payload` allowlists fields and strips raw rows, filters, prompt payloads, tool args, scope internals, and raw cursor tokens. |
| T-62-22 | Information Disclosure | `ToolResultProjector` | mitigate | Projection strips raw rows, args, payloads, tenant, scope internals, stack traces, and denied identifiers. |
| T-62-23 | Repudiation | eval/golden coverage | mitigate | Deterministic Phase 62 golden validator records required categories, no-leak flags, and drilldown sequence. |
</threat_model>

<verification>
Run backend/eval:

```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/tools/test_projection.py tests/agent/test_nodes/test_final_response.py tests/agent/test_graph.py tests/test_agent_runs_api.py tests/eval/test_phase62_business_query_golden.py -q --tb=short
UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/eval_phase62_business_query.py --golden-set evaluation/golden/phase62_business_query_cases.jsonl --output /tmp/phase62_business_query_eval.json
```
</verification>

<success_criteria>
- BQ-62-06 is satisfied: projection, final response, and API/SSE payloads are safe and typed.
- BQ-62-08 is satisfied: eval/golden coverage includes drilldown, permission boundary, list/detail no-existence-leak, breakdown, compare, projection bounds, clarification, and unsupported cases.
- BQ-62-04 is covered at service, API, final response, and eval layers.
- D-62-13, D-62-14, D-62-15, D-62-16, D-62-18, and D-62-19 are implemented without adding Phase 63/64/65/66 scopes.
- Phase 63 risk/action taxonomy, Phase 64 RAG label unification, Phase 65 global label registry, Phase 66 config hygiene, and suggested Phase 67 state-machine registry remain explicit deferrals.
</success_criteria>

<output>
After completion, create `.planning/phases/62-business-query-and-drilldown-foundation/62-06-SUMMARY.md`.
</output>
