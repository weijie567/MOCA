---
phase: 62-business-query-and-drilldown-foundation
plan: 07
type: execute
wave: 7
depends_on:
  - 62-06
files_modified:
  - frontend/src/types/events.ts
  - frontend/src/hooks/useAgentRun.ts
  - frontend/src/components/timeline/TimelineStep.tsx
  - frontend/src/components/details/DetailsPanel.tsx
  - frontend/src/components/details/BusinessQueryResultTab.tsx
  - frontend/src/hooks/useAgentRun.test.ts
  - frontend/e2e/agent-console.spec.ts
autonomous: true
requirements:
  - BQ-62-07
must_haves:
  truths:
    - "Frontend Timeline and Details render typed business_query payloads without parsing localized final-response text."
    - "Agent Console distinguishes aggregate, list, detail, breakdown, compare, RAG, clarification, and unsupported results."
    - "Frontend does not render raw rows, raw SQL, arbitrary filters, hidden scope internals, prompt payloads, tool args, routing hints, stack traces, or raw cursor tokens."
  artifacts:
    - path: "frontend/src/types/events.ts"
      provides: "BusinessQueryPayload type for aggregate/list/detail/breakdown/compare"
      contains: "BusinessQueryPayload"
    - path: "frontend/src/components/timeline/TimelineStep.tsx"
      provides: "operation-specific business_query Timeline labels and subtitles"
      contains: "业务汇总查询完成"
    - path: "frontend/src/components/details/BusinessQueryResultTab.tsx"
      provides: "typed Result tab for safe business-query display"
      contains: "BusinessQueryResultTab"
    - path: "frontend/e2e/agent-console.spec.ts"
      provides: "phase-gate E2E coverage for typed payload rendering and no-overlap checks"
      contains: "business_query"
  key_links:
    - from: "src/api/routers/agent_runs.py"
      to: "frontend/src/types/events.ts"
      via: "business_query payload shape from 62-06"
      pattern: "business_query"
    - from: "frontend/src/components/details/DetailsPanel.tsx"
      to: "frontend/src/components/details/BusinessQueryResultTab.tsx"
      via: "Result tab first in tab order"
      pattern: "BusinessQueryResultTab"
---

<objective>
Render typed business-query payloads in the existing Agent Console Timeline and Details surfaces.

Purpose: Users need safe, inspectable query results for aggregate/list/detail/breakdown/compare without frontend raw payload rendering or localized text parsing.
Output: typed frontend payload models, operation-specific Timeline labels, Result tab rendering, frontend unit/build checks, and E2E phase gate.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-CONTEXT.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-RESEARCH.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-PATTERNS.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-VALIDATION.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-UI-SPEC.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-06-SUMMARY.md
@.planning/phases/61-product-experience-fixes/61-05-SUMMARY.md

<interfaces>
Backend payload from Plan 62-06:
```text
response_kind: business_query_answer
business_query.operation: aggregate | list | detail | breakdown | compare
business_query fields: operation, resource_label, result_label, scope_label, time_label, filters_label, freshness_label, fields_label, safe_reason, rows, row_count, limit, cursor_label, allowed_drilldowns, group_by_label, compare_label
```

Current frontend payload shape:
```ts
export interface SseEventPayload {
  response_kind?: 'small_talk' | 'direct_response' | 'clarification' | 'unsupported' | 'metric_answer' | 'rag_answer' | string
  metric?: { metric_id?: string; metric_label?: string; scope_label?: string }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed business_query payload support to frontend state and Timeline</name>
  <files>frontend/src/types/events.ts, frontend/src/hooks/useAgentRun.ts, frontend/src/components/timeline/TimelineStep.tsx, frontend/src/hooks/useAgentRun.test.ts</files>
  <read_first>
    .planning/phases/62-business-query-and-drilldown-foundation/62-UI-SPEC.md
    .planning/phases/62-business-query-and-drilldown-foundation/62-06-SUMMARY.md
    frontend/src/types/events.ts
    frontend/src/hooks/useAgentRun.ts
    frontend/src/components/timeline/TimelineStep.tsx
    frontend/src/hooks/useAgentRun.test.ts
  </read_first>
  <behavior>
    - Timeline recognizes business_query aggregate/list/detail/breakdown/compare and renders labels/subtitles from typed payload fields.
    - Existing metric_answer, rag_answer, clarification, unsupported, and error labels remain supported.
    - Frontend state keeps the existing run-generation guard so late SSE callbacks from old runs cannot repopulate the active result.
    - Unit tests assert raw payload sentinels such as routing_hints, raw_args, raw_payload, raw_sql, merchant_scope, tenant_id, and raw cursor text are not rendered.
  </behavior>
  <action>
    Update `frontend/src/types/events.ts` with `BusinessQueryPayload`:
    - `operation`: aggregate, list, detail, breakdown, compare.
    - `resource_label`, `result_label`, `scope_label`, `time_label`, `filters_label`, `freshness_label`, `fields_label`, `safe_reason`.
    - `rows`: UI-safe projected rows only.
    - `row_count`, `limit`, `cursor_label`, `allowed_drilldowns`, `group_by_label`, `compare_label`.

    Update `TimelineStep.tsx` with operation-specific labels from `62-UI-SPEC.md`:
    - aggregate running/completed: `正在查询业务汇总` / `业务汇总查询完成`
    - list: `正在查询业务列表` / `业务列表查询完成`
    - detail: `正在查询业务详情` / `业务详情查询完成`
    - breakdown: `正在查询业务分组` / `业务分组查询完成`
    - compare: `正在查询业务对比` / `业务对比查询完成`

    Keep the existing stable timeline row grid and truncate/wrap behavior. Do not render row values in Timeline except safe result labels/counts/scope metadata. If `useAgentRun` changes are needed to retain latest safe payload, preserve the current stale-run generation guard.
  </action>
  <verify>
    <automated>npm --prefix frontend test</automated>
    <automated>npm --prefix frontend run build</automated>
    <automated>rg -n "BusinessQueryPayload|business_query|业务汇总查询完成|业务列表查询完成|业务分组查询完成|业务对比查询完成" frontend/src/types/events.ts frontend/src/hooks/useAgentRun.ts frontend/src/components/timeline/TimelineStep.tsx frontend/src/hooks/useAgentRun.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Frontend unit tests pass for typed payload parsing, operation-specific labels, raw sentinel rejection, and stale-run guard behavior.
    - Frontend build passes.
    - Timeline labels distinguish aggregate/list/detail/breakdown/compare from existing RAG, clarification, unsupported, and metric compatibility paths.
  </acceptance_criteria>
  <done>Timeline and frontend state understand typed business-query payloads safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Result tab for safe business-query Details rendering</name>
  <files>frontend/src/components/details/DetailsPanel.tsx, frontend/src/components/details/BusinessQueryResultTab.tsx, frontend/src/hooks/useAgentRun.test.ts, frontend/e2e/agent-console.spec.ts</files>
  <read_first>
    .planning/phases/62-business-query-and-drilldown-foundation/62-UI-SPEC.md
    frontend/src/components/details/DetailsPanel.tsx
    frontend/src/components/details/EvidenceTab.tsx
    frontend/src/hooks/useAgentRun.test.ts
    frontend/e2e/agent-console.spec.ts
  </read_first>
  <behavior>
    - Details panel tab order is Result, Evidence, Approval, Trace, Run Info.
    - BusinessQueryResultTab renders aggregate summary, list rows, detail definitions, breakdown table, compare table, denied/empty states, and cursor/drilldown controls only when backend provides safe capabilities.
    - BusinessQueryResultTab consumes only safe typed payload fields and never renders raw JSON, raw rows, raw filters, hidden scope internals, prompt payloads, tool args, routing hints, stack traces, or raw cursor tokens.
    - E2E coverage exists for the phase gate but is not part of this task-local verify command.
  </behavior>
  <action>
    Update `DetailsPanel.tsx` to add `Result` as the first tab and create `BusinessQueryResultTab.tsx`. Reuse existing shadcn-style primitives and lucide icons. Do not create a new page shell, chart dashboard, export surface, nested cards, decorative gradients, or raw JSON viewer. Keep dense operational layout, 8px-or-less radius conventions, stable timeline grid columns, and text truncation/wrapping per `62-UI-SPEC.md`.

    Add frontend unit cases for aggregate/list/detail/breakdown/compare, denied/empty states, safe cursor labels, disabled/omitted drilldown controls, and raw payload rejection. Add Playwright cases in `frontend/e2e/agent-console.spec.ts` for the phase gate: all five operations, raw payload rejection, no overlap, cursor label safety, denied/empty states, and the aggregate-to-list drilldown UI sequence.

    Do not put `npm --prefix frontend run e2e` in this task-local `<verify>` block. It belongs to the plan/phase gate below because it is a full frontend E2E run.
  </action>
  <verify>
    <automated>npm --prefix frontend test</automated>
    <automated>npm --prefix frontend run build</automated>
    <automated>rg -n "BusinessQueryResultTab|Result|business_query|raw_args|routing_hints|raw_cursor|查看更多" frontend/src/components/details/DetailsPanel.tsx frontend/src/components/details/BusinessQueryResultTab.tsx frontend/src/hooks/useAgentRun.test.ts frontend/e2e/agent-console.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Frontend unit tests pass for typed Details rendering and raw-payload rejection.
    - Build passes through repository npm entrypoints.
    - E2E cases are present for the phase gate and cover no-overlap plus aggregate-to-list drilldown UI sequence.
    - Frontend uses typed payload fields and does not parse localized final response text to build result UI.
  </acceptance_criteria>
  <done>Agent Console Details safely renders typed business-query results and has E2E phase-gate coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE payload to frontend state | Browser receives backend-projected business_query payloads and must preserve stale-run protection. |
| frontend state to Timeline/Details | UI renders typed safe fields and must not expose raw debug/business rows. |
| E2E fixture to phase gate | Playwright coverage verifies rendered safety and no-overlap behavior after unit/build checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-24 | Information Disclosure | frontend payload rendering | mitigate | UI consumes typed safe fields only; unit tests assert raw payload sentinels are not rendered. |
| T-62-25 | Information Disclosure | Result tab cursor/drilldown controls | mitigate | Render cursor/drilldown affordances only from backend safe labels/capabilities, never raw cursor tokens. |
| T-62-26 | Denial of Service | frontend list/detail rendering | mitigate | UI renders bounded rows and stable dimensions; Playwright phase gate checks desktop/mobile no-overlap behavior. |
| T-62-27 | Spoofing | stale SSE updates | mitigate | Preserve `useAgentRun` generation guard so old runs cannot repopulate current Result tab. |
</threat_model>

<verification>
Run task-local frontend checks:

```bash
npm --prefix frontend test
npm --prefix frontend run build
```

Plan/phase gate after task-local checks:

```bash
npm --prefix frontend run e2e
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/ -q --tb=short
UV_CACHE_DIR=/tmp/uv-cache uv run python scripts/eval_phase62_business_query.py --golden-set evaluation/golden/phase62_business_query_cases.jsonl --output /tmp/phase62_business_query_eval.json
```
</verification>

<success_criteria>
- BQ-62-07 is satisfied: frontend Timeline/Details render business-query operations from typed payloads.
- D-62-13, D-62-14, D-62-15, D-62-16, and D-62-19 are honored in the Agent Console without adding Phase 63/64/65/66 scopes.
- Full frontend E2E is present only as a plan/phase gate, not task-local verification.
- Phase 63 risk/action taxonomy, Phase 64 RAG label unification, Phase 65 global label registry, Phase 66 config hygiene, and suggested Phase 67 state-machine registry remain explicit deferrals.
</success_criteria>

<output>
After completion, create `.planning/phases/62-business-query-and-drilldown-foundation/62-07-SUMMARY.md`.
</output>
