---
phase: 62-business-query-and-drilldown-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 62-03
files_modified:
  - src/business/query/compiler.py
  - src/business/service.py
  - src/business/adapters.py
  - src/tools/executors/business.py
  - src/agent/nodes/investigate.py
  - tests/business/test_business_query_service.py
  - tests/business/test_service.py
  - tests/tools/test_tool_platform.py
  - tests/agent/test_nodes/test_investigate.py
  - tests/architecture/test_business_query_boundaries.py
autonomous: true
requirements:
  - BQ-62-03
  - BQ-62-04
  - BQ-62-08
must_haves:
  truths:
    - "BusinessFactService owns business_query compilation and execution for aggregate, list, detail, breakdown, and compare."
    - "List and detail queries apply trusted scope before existence disclosure and never reveal whether out-of-scope merchant/resource/id inputs exist."
    - "At least one breakdown and one compare query execute through controlled runtime paths and are ready for eval coverage."
  artifacts:
    - path: "src/business/query/compiler.py"
      provides: "controlled SQLAlchemy compiler helpers for registry-owned query shapes"
      contains: "BusinessQueryCompiler"
    - path: "src/business/service.py"
      provides: "BusinessFactService.query_business runtime entrypoint"
      contains: "query_business"
    - path: "tests/business/test_business_query_service.py"
      provides: "aggregate/list/detail/breakdown/compare and no-existence-leak service coverage"
      contains: "test_business_query"
  key_links:
    - from: "src/tools/executors/business.py"
      to: "src/business/service.py"
      via: "BusinessToolExecutor dispatch for business_query and query_business_metric compatibility"
      pattern: "query_business"
    - from: "src/business/service.py"
      to: "src/business/query/compiler.py"
      via: "controlled SQLAlchemy query construction"
      pattern: "BusinessQueryCompiler"
---

<objective>
Implement the BusinessFactService-owned runtime for controlled business reads.

Purpose: The safe contract and ToolPlatform boundary from 62-02/62-03 must execute behind the business-fact authority boundary, not through agent-node SQL, generic repository list helpers, or tool-side query builders.
Output: `BusinessFactService.query_business`, controlled compiler methods, business executor dispatch, service/tool tests for all five operations, and static boundary tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ARCHITECTURE-DEBT.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-CONTEXT.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-RESEARCH.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-PATTERNS.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-VALIDATION.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-01-SUMMARY.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-02-SUMMARY.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-03-SUMMARY.md
@.planning/phases/61-product-experience-fixes/61-03-SUMMARY.md
@.planning/phases/61-product-experience-fixes/61-04-SUMMARY.md

<interfaces>
Current BusinessFactService metric execution:
```python
async def query_business_metric(self, args: dict[str, Any], ctx: ToolCallContext) -> BusinessFactResultV1: ...
def _authorized_metric_merchant_ids(self, query: BusinessMetricQueryInput, ctx: ToolCallContext) -> list[str] | None: ...
def _permission_denied_result(self, resource_name: str, tenant_id: str, *, source_system: str = "business_fact_service"): ...
```

Plan 62-02 contracts:
```python
class BusinessQuerySpec(BaseModel): ...
class BusinessQueryResultV1(BaseModel): ...
def metric_input_to_business_query(metric: BusinessMetricQueryInput) -> BusinessQuerySpec: ...
```

Plan 62-03 ToolPlatform descriptor:
```python
name = "business_query"
required_permission = "tool:business_query"
caller_allowlist = ("investigate",)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement controlled BusinessFactService query runtime</name>
  <files>src/business/query/compiler.py, src/business/service.py, tests/business/test_business_query_service.py, tests/business/test_service.py</files>
  <read_first>
    src/business/query/registry.py
    src/business/query/schemas.py
    src/business/service.py
    src/db/models.py
    tests/business/test_service.py
    .planning/phases/62-business-query-and-drilldown-foundation/62-02-SUMMARY.md
    .planning/phases/62-business-query-and-drilldown-foundation/62-03-SUMMARY.md
  </read_first>
  <behavior>
    - Aggregate order_count this_week executes through `BusinessFactService.query_business` and returns the same trusted scope/time/freshness semantics as Phase 61 metric runtime.
    - List order queries return bounded safe rows with order_no and allowed fields only, apply tenant and authorized merchant scope first, and produce a structured cursor capability when more rows exist.
    - Detail queries for unauthorized resource ids return the same safe denied or empty-safe result without confirming existence and without fetching by id before scope checks.
    - Breakdown order by status and compare order_count current period versus previous period both execute through controlled SQLAlchemy statements per D-62-16.
    - Invalid fields, status filters, sort fields, cursor values, limits, and resource-operation incompatibilities fail closed with safe result codes.
  </behavior>
  <action>
    Create `src/business/query/compiler.py` with `BusinessQueryCompiler`. The compiler must accept validated `BusinessQuerySpec`, registry descriptors, tenant id, authorized merchant ids, and effective time. It must return SQLAlchemy `select()` statements or small execution descriptors only for known resources and operations. Do not use raw SQL strings, string-concatenated conditions, generic repository list-all helpers, or agent/tool-supplied tenant/scope.

    Extend `BusinessFactService` with:
    - `async def query_business(self, args: dict[str, Any] | BusinessQuerySpec, ctx: ToolCallContext) -> BusinessFactResultV1`
    - `_authorized_business_query_merchant_ids(spec, ctx)` separated from action target merchant per D-62-05.
    - `_business_query_result_to_fact_result(result, ctx)` that returns `BusinessFactResultV1` with resource `business_query`, safe fact data, safe missing facts, and `BusinessFactRefV1` refs.

    Runtime target coverage:
    - aggregate: metric-compatible order_count, refund_case_count, pending_ticket_count, coupon_record_count, merchant_refund_rate.
    - list: order list with safe order_no and at most registry limit rows.
    - detail: order detail by id or order_no with scope-before-existence semantics.
    - breakdown: order_count by status.
    - compare: order_count for current requested period versus previous equivalent period.

    Preserve `query_business_metric` but implement it as validation through `BusinessMetricQueryInput`, conversion through `metric_input_to_business_query`, and delegation to `query_business`. Keep the existing metric result shape available for compatibility tests if downstream code still consumes it.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/business/test_business_query_service.py tests/business/test_service.py -q --tb=short</automated>
    <automated>rg -n "async def query_business|BusinessQueryCompiler|metric_input_to_business_query" src/business/service.py src/business/query/compiler.py</automated>
  </verify>
  <acceptance_criteria>
    - `BusinessFactService.query_business` executes all five operations without exposing raw SQL or generic list helpers.
    - Service tests prove scope is resolved before detail/list existence disclosure and sensitive out-of-scope ids are absent from serialized results.
    - Breakdown and compare are runtime-backed, not schema-only promises.
    - Existing `query_business_metric` service tests still pass and show compatibility delegation into the business-query path.
  </acceptance_criteria>
  <done>BusinessFactService owns and executes safe business queries for aggregate/list/detail/breakdown/compare.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire business_query through business executor and investigate</name>
  <files>src/business/adapters.py, src/tools/executors/business.py, src/agent/nodes/investigate.py, tests/tools/test_tool_platform.py, tests/agent/test_nodes/test_investigate.py</files>
  <read_first>
    src/business/adapters.py
    src/tools/executors/business.py
    src/agent/nodes/investigate.py
    tests/tools/test_tool_platform.py
    tests/agent/test_nodes/test_investigate.py
    .planning/phases/62-business-query-and-drilldown-foundation/62-03-SUMMARY.md
  </read_first>
  <behavior>
    - `ToolPlatform.invoke("business_query", ...)` dispatches to `BusinessFactService.query_business` and returns a safe `ToolResultV2`.
    - `ToolPlatform.invoke("query_business_metric", ...)` still works and reaches the same BusinessFactService business-query path.
    - Investigate accumulates business_query facts under `business_context.facts["business_query"]` and preserves metric compatibility under `business_context.facts["business_metric"]` where existing final response tests require it.
    - Agent nodes do not compile SQL, import DB models, call repositories, or call BusinessFactService directly.
  </behavior>
  <action>
    Update `src/tools/executors/business.py` so `BusinessToolExecutor` dispatches `business_query` to `BusinessFactService.query_business`. Keep `query_business_metric` dispatch as a compatibility call that delegates through the new service path.

    Update `src/business/adapters.py` only if strict conversion is required to adapt `BusinessQueryResultV1` into existing `ToolResultV2` conventions. The adapter must not dump ORM rows or hidden scope internals into tool data.

    Update `src/agent/nodes/investigate.py` to accumulate `business_query` facts and refs from tool results. The deterministic fallback may build `business_query` args from active slots after 62-05 adds drilldown derivation, but this plan must not compile SQL or call service methods in the node.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/tools/test_tool_platform.py tests/agent/test_nodes/test_investigate.py -q --tb=short</automated>
    <automated>rg -n "business_query|query_business" src/tools/executors/business.py src/business/adapters.py src/agent/nodes/investigate.py</automated>
  </verify>
  <acceptance_criteria>
    - ToolPlatform tests prove `business_query` and `query_business_metric` execute through the business executor and enforce ToolPolicy decisions from 62-03.
    - Investigate tests prove graph nodes still use ToolPlatform and do not import repositories, DB models, or BusinessFactService for query compilation.
    - Compatibility metric behavior remains green.
  </acceptance_criteria>
  <done>Business-query runtime is reachable only through ToolPlatform and produces safe tool facts for later projection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add static runtime ownership backstops</name>
  <files>tests/architecture/test_business_query_boundaries.py</files>
  <read_first>
    tests/architecture/test_tool_contract_backstops.py
    src/agent/nodes/investigate.py
    src/tools/executors/business.py
    src/business/query/compiler.py
    src/business/service.py
  </read_first>
  <behavior>
    - Static tests fail if agent nodes import repositories, DB models, `BusinessFactService`, or `BusinessQueryCompiler` for business-query execution.
    - Static tests fail if `src/business/query/compiler.py` contains raw SQL execution strings or generic list-all helper names.
    - Static tests prove ToolPlatform/investigate allowed tool names include `business_query` consistently with ToolCatalog.
  </behavior>
  <action>
    Add `tests/architecture/test_business_query_boundaries.py` following the style of `tests/architecture/test_tool_contract_backstops.py`. Use AST or targeted source scans rather than broad fragile grep. Required guards:
    - `src/agent/nodes/investigate.py` must not import `src.repositories`, `src.db.models`, `BusinessFactService`, or `BusinessQueryCompiler`.
    - `src/tools/executors/business.py` may call `BusinessFactService` but must not construct SQLAlchemy filters or import repositories.
    - `src/business/query/compiler.py` must not call raw SQL text execution or expose generic unbounded list helpers.
    - planner-visible investigate tools must include `business_query` and `query_business_metric` consistently between ToolCatalog and `INVESTIGATE_ALLOWED_TOOL_NAMES`, or the code must derive the planner allowlist from ToolCatalog.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_business_query_boundaries.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - Boundary tests fail on direct agent-to-service or agent-to-DB business query coupling.
    - Boundary tests fail on raw SQL/generic list exposure in business query compiler.
    - Boundary tests lock ToolCatalog and investigate planner allowlist parity for `business_query`.
  </acceptance_criteria>
  <done>Static backstops protect BusinessFactService and ToolPlatform ownership boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ToolPlatform to BusinessFactService | Validated specs cross from tool runtime into business fact execution. |
| BusinessFactService to database | Trusted scope and allowlisted descriptors become SQLAlchemy statements. |
| runtime result to tool fact | Runtime data is normalized into safe facts for later projection and response work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-12 | Tampering | `BusinessQueryCompiler` | mitigate | Build only SQLAlchemy expressions from registry descriptors; static tests reject raw SQL and generic list helpers. |
| T-62-13 | Elevation of Privilege | `BusinessFactService.query_business` | mitigate | Derive merchant scope from trusted ToolCallContext before execution; reject untrusted tenant/scope fields. |
| T-62-14 | Information Disclosure | list/detail runtime | mitigate | Scope-before-existence tests assert denied/out-of-scope ids do not appear and detail lookup is not performed before authorization. |
| T-62-15 | Denial of Service | list/cursor runtime | mitigate | Enforce registry max limit, deterministic ordering, and limit-plus-one cursor detection. |
</threat_model>

<verification>
Run:

```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/business/test_business_query_service.py tests/business/test_service.py tests/tools/test_tool_platform.py tests/agent/test_nodes/test_investigate.py tests/architecture/test_business_query_boundaries.py -q --tb=short
```
</verification>

<success_criteria>
- BQ-62-03 is satisfied: BusinessFactService-owned runtime executes aggregate/list/detail/breakdown/compare.
- BQ-62-04 is satisfied at service/tool level: unauthorized list/detail inputs fail closed without existence leak.
- BQ-62-08 is partially satisfied: breakdown and compare runtime examples exist and are ready for golden/eval coverage in 62-06.
- D-62-01, D-62-02, D-62-05, D-62-06, D-62-07, D-62-08, D-62-11, and D-62-16 are honored at runtime.
- Phase 63 risk/action taxonomy, Phase 64 RAG label unification, Phase 65 global label registry, Phase 66 config hygiene, and suggested Phase 67 state-machine registry remain explicit deferrals.
</success_criteria>

<output>
After completion, create `.planning/phases/62-business-query-and-drilldown-foundation/62-04-SUMMARY.md`.
</output>
