---
phase: 62-business-query-and-drilldown-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 62-01
files_modified:
  - docs/contract-spec.md
  - src/business/query/schemas.py
  - src/business/schemas.py
  - tests/business/test_business_query_schemas.py
  - tests/business/test_schemas.py
autonomous: true
requirements:
  - BQ-62-02
  - BQ-62-04
must_haves:
  truths:
    - "Callers can construct only strict, allowlisted BusinessQuerySpec objects for aggregate, list, detail, breakdown, and compare."
    - "business_metric_query remains a compatibility entry and maps into BusinessQuerySpec instead of becoming a permanent runtime branch."
    - "The accepted contract spec records the Phase 62 business_query semantics before ToolPlatform, runtime, API, or UI work depends on them."
  artifacts:
    - path: "docs/contract-spec.md"
      provides: "accepted contract delta for business_query semantics, BusinessFactService ownership, no-existence-leak, and compatibility"
      contains: "business_query"
    - path: "src/business/query/schemas.py"
      provides: "strict BusinessQuerySpec, result, cursor, answer context, filter, sort, and compatibility schemas"
      contains: "BusinessQuerySpec"
    - path: "tests/business/test_business_query_schemas.py"
      provides: "schema strictness and metric compatibility coverage"
      contains: "metric_input_to_business_query"
  key_links:
    - from: "src/business/query/schemas.py"
      to: "src/business/query/registry.py"
      via: "operation/resource/time/status/field/limit descriptor validation"
      pattern: "BUSINESS_QUERY_REGISTRY|BusinessQueryRegistry"
    - from: "src/business/schemas.py"
      to: "src/business/query/schemas.py"
      via: "BusinessMetricQueryInput compatibility mapping"
      pattern: "metric_input_to_business_query|BusinessQuerySpec"
---

<objective>
Define the safe long-term `business_query` contract and strict schema layer.

Purpose: Business reads need a documented contract and strict Pydantic schema before auth, ToolPlatform, runtime, answer context, API, eval, or UI work can safely consume them.
Output: `docs/contract-spec.md` business-query delta, `BusinessQuerySpec` and related result/context/cursor models, and tests proving `business_metric_query` maps into the primary contract.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ARCHITECTURE-DEBT.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-CONTEXT.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-RESEARCH.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-PATTERNS.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-VALIDATION.md
@.planning/phases/62-business-query-and-drilldown-foundation/62-01-SUMMARY.md
@.planning/phases/61-product-experience-fixes/61-03-SUMMARY.md

<interfaces>
Contracts created by Plan 62-01:
```python
from src.business.query.registry import BUSINESS_QUERY_REGISTRY, BusinessQueryRegistry
```

Current Phase 61 compatibility contracts:
```python
class BusinessMetricQueryInput(BaseModel): ...
class BusinessMetricResultV1(BaseModel): ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record business_query contract semantics</name>
  <files>docs/contract-spec.md</files>
  <read_first>
    docs/contract-spec.md
    .planning/phases/62-business-query-and-drilldown-foundation/62-CONTEXT.md
    .planning/phases/62-business-query-and-drilldown-foundation/62-RESEARCH.md
    .planning/phases/62-business-query-and-drilldown-foundation/62-01-SUMMARY.md
  </read_first>
  <action>
    Update `docs/contract-spec.md` to define `business_query` as the long-term primary read contract per D-62-02. The contract delta must include: operation taxonomy aggregate/list/detail/breakdown/compare per D-62-03; resources order/refund_case/ticket/coupon_record/merchant_metric per D-62-04; BusinessFactService compiler/executor ownership per D-62-06; permission-before-existence semantics per D-62-07; descriptor compatibility gates including `current_snapshot` per D-62-08; answer context constraints per D-62-10; safe projection responsibilities per D-62-14; and business facts versus RAG authority separation per D-62-18.

    Explicitly preserve deferrals: Phase 63 owns risk/action taxonomy, Phase 64 owns RAG risk label unification, Phase 65 owns global event/response-kind/node/tool/console label registry, Phase 66 owns demo/config/test hygiene, and Phase 67 remains a registration-only recommendation per D-62-17 through D-62-20.

    Do not describe spec text as proof of implementation. Mark the section as Phase 62 target contract semantics that the subsequent plans implement and verify.
  </action>
  <verify>
    <automated>rg -n "business_query|BusinessQuerySpec|aggregate|breakdown|compare|BusinessFactService|no-existence|Phase 63|Phase 64|Phase 65|Phase 66|Phase 67" docs/contract-spec.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/contract-spec.md` contains the accepted `business_query` contract delta and names the ownership boundaries.
    - The contract notes that `business_metric_query` is compatibility into `BusinessQuerySpec`.
    - The contract does not silently implement or register Phase 63/64/65/66/67 scope.
  </acceptance_criteria>
  <done>Contract-spec semantics match locked Phase 62 decisions without expanding into deferred phases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define strict BusinessQuerySpec and compatibility schemas</name>
  <files>src/business/query/schemas.py, src/business/schemas.py, tests/business/test_business_query_schemas.py, tests/business/test_schemas.py</files>
  <read_first>
    src/business/query/registry.py
    src/business/schemas.py
    tests/business/test_schemas.py
    .planning/phases/62-business-query-and-drilldown-foundation/62-CONTEXT.md
    .planning/phases/62-business-query-and-drilldown-foundation/62-PATTERNS.md
    .planning/phases/62-business-query-and-drilldown-foundation/62-01-SUMMARY.md
  </read_first>
  <behavior>
    - `BusinessQuerySpec` accepts only aggregate, list, detail, breakdown, compare operations from the registry per D-62-03.
    - `BusinessQuerySpec` accepts only order, refund_case, ticket, coupon_record, merchant_metric resources per D-62-04.
    - Schema validation rejects tenant_id, merchant_scope, raw SQL, where clauses, arbitrary filters, wildcard merchant filters, raw cursor strings, unknown fields, disallowed fields, disallowed status values, invalid time ranges, and limits above the registry maximum.
    - `BusinessMetricQueryInput` maps into `BusinessQuerySpec` with operation aggregate and never bypasses strict validation per D-62-02.
  </behavior>
  <action>
    Create `src/business/query/schemas.py` with strict Pydantic models using `ConfigDict(extra="forbid")`. Required exported names:
    - `BusinessQuerySpec`
    - `BusinessQueryFilterSet`
    - `BusinessQuerySort`
    - `BusinessQueryCursor`
    - `BusinessQueryResultV1`
    - `BusinessQueryAnswerContext`
    - `BusinessQueryResultCursor`
    - `BusinessQueryScopeSummary`
    - `metric_input_to_business_query`

    `BusinessQuerySpec` target fields:
    - `operation`: aggregate, list, detail, breakdown, compare.
    - `resource`: order, refund_case, ticket, coupon_record, merchant_metric.
    - `metric_id`: optional, required for metric aggregate/compare paths.
    - `time_preset`, `start_at`, `end_at`: validated through registry time descriptors.
    - `merchant_id`: optional filter only; wildcard `*` is invalid.
    - `resource_id`: optional detail identifier; service owns no-existence-leak validation.
    - `filters`: typed `BusinessQueryFilterSet`, initially status_filter plus resource-compatible controlled filters from registry.
    - `fields`: requested field ids checked against resource allowlists.
    - `group_by`: required for breakdown where the descriptor requires it, checked against resource allowlists.
    - `compare_to`: controlled value such as previous_period for the Phase 62 compare example.
    - `sort`: optional `BusinessQuerySort`, field and direction checked against registry.
    - `limit`: default 20, maximum from registry, rejects zero and oversized values.
    - `cursor`: optional `BusinessQueryCursor`; reject raw free-form cursor strings.

    Extend `src/business/schemas.py` only for compatibility exports or helper imports if needed. Keep existing `BusinessMetricQueryInput` strict behavior. Do not add trusted tenant, merchant scope, permission, draft, or execute fields to tool args per D-62-05 and D-62-17.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/business/test_business_query_schemas.py tests/business/test_schemas.py -q --tb=short</automated>
    <automated>rg -n "class BusinessQuerySpec|class BusinessQueryResultV1|class BusinessQueryAnswerContext|def metric_input_to_business_query" src/business/query/schemas.py</automated>
  </verify>
  <acceptance_criteria>
    - Strict schema tests reject authority-bearing fields, raw SQL/filter shapes, raw cursor strings, unknown operations, unknown resources, disallowed fields, and oversized limits.
    - Compatibility tests prove `BusinessMetricQueryInput` maps to `BusinessQuerySpec(operation="aggregate")` for all five Phase 61 metrics.
    - `current_snapshot` validation is descriptor-owned and rejects event-count metrics while accepting pending_ticket_count.
    - No test or production code treats `business_metric_query` as the long-term primary read contract.
  </acceptance_criteria>
  <done>`BusinessQuerySpec` is the safe primary read contract and metric input is a compatibility shim into it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller to BusinessQuerySpec | Untrusted tool args cross into strict schema validation. |
| contract spec to implementation plans | Target contract semantics guide implementation but do not prove runtime behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-04 | Tampering | `BusinessQuerySpec` | mitigate | Strict Pydantic models with `extra="forbid"` reject authority fields, arbitrary filters, raw SQL, raw cursor strings, unknown operations/resources, and oversized limits. |
| T-62-05 | Information Disclosure | list/detail schema inputs | mitigate | Schema accepts only filters and identifiers; runtime no-existence-leak is planned in 62-04 and API/UI projection in 62-06/62-07. |
| T-62-06 | Tampering | business read operation taxonomy | mitigate | Contract and schema tests exclude draft and execute from business-query operations per D-62-17. |
| T-62-07 | Repudiation | `docs/contract-spec.md` | mitigate | Contract-spec delta records Phase 62 semantics and explicit deferrals so implementation/spec divergence is reviewable. |
</threat_model>

<verification>
Run:

```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/business/test_business_query_schemas.py tests/business/test_schemas.py -q --tb=short
rg -n "class BusinessQuerySpec|BusinessQuerySpec|business_query|no-existence" src/business/query/schemas.py docs/contract-spec.md
```
</verification>

<success_criteria>
- BQ-62-02 is satisfied at the contract/schema layer: safe `business_query` contract exists and `business_metric_query` maps into it.
- BQ-62-04 is partially satisfied at the schema layer: authority-bearing fields and no-leak-breaking input shapes are rejected before runtime.
- D-62-02, D-62-03, D-62-04, D-62-05, D-62-07, D-62-08, D-62-10, D-62-14, D-62-17, D-62-18, D-62-19, and D-62-20 are honored at the contract/schema layer.
</success_criteria>

<output>
After completion, create `.planning/phases/62-business-query-and-drilldown-foundation/62-02-SUMMARY.md`.
</output>
