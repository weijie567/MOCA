---
phase: 41-tool-platform-legacy-manager-cleanup
plan: 04
type: execute
wave: 4
depends_on:
  - "41-03"
files_modified:
  - .planning/phases/41-tool-platform-legacy-manager-cleanup/41-REVIEW.md
  - .planning/phases/41-tool-platform-legacy-manager-cleanup/41-VERIFICATION.md
  - .planning/phases/41-tool-platform-legacy-manager-cleanup/41-CLOSURE-REVIEW.md
  - .planning/LOCAL-VALIDATION-ISSUES.md
autonomous: true
requirements:
  - TPH-06
must_haves:
  truths:
    - "Implementation code review is completed before Phase 41 is marked complete."
    - "Claude light closure review is explicitly required after implementation review and final verification."
    - "Final verification proves ToolPlatform-only entrypoint and no protected model shape drift."
---

<objective>
Run Phase 41 code review and final verification before v2.1 archive.
</objective>

<context>
@.planning/phases/41-tool-platform-legacy-manager-cleanup/41-CONTEXT.md
@.planning/phases/41-tool-platform-legacy-manager-cleanup/41-01-SUMMARY.md
@.planning/phases/41-tool-platform-legacy-manager-cleanup/41-02-SUMMARY.md
@.planning/phases/41-tool-platform-legacy-manager-cleanup/41-03-SUMMARY.md
@src/tools
@src/agent/nodes
@tests/tools
@tests/agent
@tests/architecture
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run implementation code review</name>
  <files>.planning/phases/41-tool-platform-legacy-manager-cleanup/41-REVIEW.md</files>
  <action>
    Review the Phase 41 implementation from a code-review stance: bugs, behavior regressions, incomplete cleanup, missing tests, and spec/API mismatch.
    Prefer GSD code-review tooling if available; otherwise produce a source-based `41-REVIEW.md` with findings or an explicit clean-review statement.
    Fix any accepted findings before final verification. If fixes are needed, commit them separately and update the review record.
  </action>
  <verify>
    <automated>test -f .planning/phases/41-tool-platform-legacy-manager-cleanup/41-REVIEW.md</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Run final verification</name>
  <files>.planning/phases/41-tool-platform-legacy-manager-cleanup/41-VERIFICATION.md, .planning/LOCAL-VALIDATION-ISSUES.md</files>
  <action>
    Run final verification:
    `rg -n "UnifiedToolManager|src\\.tools\\.manager|tool_manager|action_tool_manager|\\._platform" src tests docs/contract-spec.md --glob '!**/.planning/**'`;
    `uv run pytest tests/tools/ tests/architecture/ -q`;
    `uv run pytest tests/agent/test_nodes/test_investigate.py tests/agent/test_graph.py tests/agent/test_policy_retrieval_ownership.py tests/knowledge/test_facade_integration.py tests/test_execute_action.py tests/agent/test_phase22_action_boundary.py -q`;
    `uv run ruff check src/tools src/agent/nodes tests/tools tests/architecture tests/agent/test_nodes/test_investigate.py tests/agent/test_graph.py tests/agent/test_policy_retrieval_ownership.py tests/knowledge/test_facade_integration.py tests/test_execute_action.py tests/agent/test_phase22_action_boundary.py`;
    `git diff -- src/tools/contracts.py`.
    If DB-backed tests fail only because local PostgreSQL is unavailable, append a Chinese entry to `.planning/LOCAL-VALIDATION-ISSUES.md` and keep the blocker separate from product-code failures.
    Write `41-VERIFICATION.md` with exact command results.
  </action>
  <verify>
    <automated>test -f .planning/phases/41-tool-platform-legacy-manager-cleanup/41-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - No `UnifiedToolManager` / `src.tools.manager` references remain outside historical `.planning/` docs.
    - Protected model contracts have no diff.
    - Final tests pass or environment blockers are recorded according to project rules.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Record Claude light closure review checkpoint</name>
  <files>.planning/phases/41-tool-platform-legacy-manager-cleanup/41-CLOSURE-REVIEW.md</files>
  <action>
    Create a closure-review record for the required Claude-side light review after implementation and verification.
    The record must require checking only these closeout questions: whether implementation deviated from plan must_haves, whether any TPH-06 requirement remains uncovered, and whether forbidden §8.0 identity fields or `ToolResultV2` / `ToolCallContext` envelope shapes changed.
    This checkpoint is not a Codex self-review substitute; it is the explicit handoff artifact for Claude's required phase-B light closeout.
  </action>
  <verify>
    <automated>test -f .planning/phases/41-tool-platform-legacy-manager-cleanup/41-CLOSURE-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - Closure review explicitly names plan must_haves, TPH-06 coverage, and §8.0/envelope no-diff checks.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Plan 41-04 is complete when code review is recorded, accepted findings are fixed, final verification is recorded, Claude light closure review is handed off, and Phase 41 can be marked complete.
</verification>

<output>
After completion, update Phase 41, TPH-06, ROADMAP, REQUIREMENTS, PROJECT, and STATE to complete.
</output>
