---
phase: 41-tool-platform-legacy-manager-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/contract-spec.md
  - src/tools/policy.py
  - tests/architecture/test_action_draft_boundaries.py
  - tests/architecture/test_tool_boundaries.py
  - src/tools/catalog.py
autonomous: true
requirements:
  - TPH-06
must_haves:
  truths:
    - "The spec/API decision is explicit: `UnifiedToolManager` compatibility is removed in Phase 41."
    - "`_side_effect_allowed` has a non-legacy home before `src/tools/manager.py` is deleted."
    - "No tool contract model shape changes are introduced."
  artifacts:
    - path: "docs/contract-spec.md"
      provides: "Normative removal of the legacy adapter contract"
      contains: "ToolPlatform"
    - path: "src/tools/policy.py"
      provides: "_side_effect_allowed helper home"
      contains: "def _side_effect_allowed"
    - path: "tests/architecture/test_action_draft_boundaries.py"
      provides: "updated helper import and action side-effect semantics"
      contains: "_side_effect_allowed"
---

<objective>
Make the breaking API/spec decision real and move live helper code out of the legacy manager module.

Purpose: before deleting `src/tools/manager.py`, remove the normative spec promise that it exists and relocate `_side_effect_allowed` to a non-legacy module.
</objective>

<context>
@.planning/phases/41-tool-platform-legacy-manager-cleanup/41-CONTEXT.md
@docs/contract-spec.md
@src/tools/manager.py
@src/tools/policy.py
@src/tools/catalog.py
@tests/architecture/test_action_draft_boundaries.py
@tests/architecture/test_tool_boundaries.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update spec and catalog wording away from UnifiedToolManager</name>
  <files>docs/contract-spec.md, src/tools/catalog.py</files>
  <action>
    Edit `docs/contract-spec.md` so §6/§10/§12.6 no longer describe `UnifiedToolManager` as a retained legacy compatibility adapter.
    Remove the `class UnifiedToolManager(Protocol)` block and any bullet allowing legacy `invoke(...) -> ToolResultV2` / `visible_tools(...)`.
    State that `ToolPlatform` is the single graph-facing dispatch and contract-validation entrypoint, backed by `ToolCatalog`.
    In `src/tools/catalog.py`, update the declaration-only error text from "use UnifiedToolManager" to "use ToolPlatform".
    Do not edit `src/tools/contracts.py` or change model fields.
  </action>
  <verify>
    <automated>rg -n "UnifiedToolManager" docs/contract-spec.md src/tools/catalog.py</automated>
    <automated>git diff -- src/tools/contracts.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "UnifiedToolManager" docs/contract-spec.md src/tools/catalog.py` has no matches.
    - `docs/contract-spec.md` still names `ToolPlatform` and `ToolCatalog` as the tool platform contract owners.
    - `git diff -- src/tools/contracts.py` is empty.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move _side_effect_allowed to policy.py</name>
  <files>src/tools/policy.py, tests/architecture/test_action_draft_boundaries.py, tests/architecture/test_tool_boundaries.py</files>
  <action>
    Move `_side_effect_allowed(caller_node, descriptor)` from `src/tools/manager.py` to `src/tools/policy.py`.
    Update `tests/architecture/test_action_draft_boundaries.py` to import it from `src.tools.policy` and to assert the `action_draft` side-effect rule in `policy.py`, not in manager source.
    Update `tests/architecture/test_tool_boundaries.py` so domain packages are forbidden from importing `src.tools.manager` and no longer require manager-specific import-boundary tests.
  </action>
  <verify>
    <automated>uv run pytest tests/architecture/test_action_draft_boundaries.py tests/architecture/test_tool_boundaries.py -q</automated>
    <automated>uv run ruff check src/tools/policy.py tests/architecture/test_action_draft_boundaries.py tests/architecture/test_tool_boundaries.py</automated>
  </verify>
  <acceptance_criteria>
    - `_side_effect_allowed` works from `src.tools.policy`.
    - No test imports `_side_effect_allowed` from `src.tools.manager`.
    - Architecture tests pass.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Plan 41-01 is complete when:
- `uv run pytest tests/architecture/test_action_draft_boundaries.py tests/architecture/test_tool_boundaries.py -q` passes.
- `uv run ruff check docs/contract-spec.md src/tools/policy.py tests/architecture/test_action_draft_boundaries.py tests/architecture/test_tool_boundaries.py` is adjusted if ruff cannot lint markdown; at minimum ruff passes for Python files.
- `git diff -- src/tools/contracts.py src/tools/manager.py src/tools/__init__.py` shows no deletion yet except no contracts diff.
</verification>

<output>
After completion, create `41-01-SUMMARY.md`.
</output>
