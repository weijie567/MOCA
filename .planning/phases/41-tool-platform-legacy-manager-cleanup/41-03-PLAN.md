---
phase: 41-tool-platform-legacy-manager-cleanup
plan: 03
type: execute
wave: 3
depends_on:
  - "41-02"
files_modified:
  - src/tools/manager.py
  - src/tools/__init__.py
  - tests/agent/test_tools/test_unified_tool_manager.py
  - tests/tools/test_tool_platform.py
  - tests/architecture/test_tool_boundaries.py
autonomous: true
requirements:
  - TPH-06
must_haves:
  truths:
    - "`src/tools/manager.py` is deleted after behavior coverage is migrated or proven redundant."
    - "`src.tools` no longer exports `UnifiedToolManager`."
    - "All remaining relevant behavior is covered by `ToolPlatform` tests."
---

<objective>
Delete the legacy adapter and clean the public API surface.
</objective>

<context>
@.planning/phases/41-tool-platform-legacy-manager-cleanup/41-CONTEXT.md
@src/tools/manager.py
@src/tools/__init__.py
@src/tools/platform.py
@tests/agent/test_tools/test_unified_tool_manager.py
@tests/tools/test_tool_platform.py
@tests/architecture/test_tool_boundaries.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate non-redundant manager tests to ToolPlatform tests</name>
  <files>tests/agent/test_tools/test_unified_tool_manager.py, tests/tools/test_tool_platform.py</files>
  <action>
    Audit `tests/agent/test_tools/test_unified_tool_manager.py`.
    For behavior already covered by `tests/tools/test_tool_platform.py`, delete redundant manager-specific tests.
    For still-useful behavior such as custom descriptors, visibility, action dispatch, invalid executor response, and output schema failure, migrate equivalent assertions to `tests/tools/test_tool_platform.py` using `ToolPlatform` directly.
    Do not silently drop descriptor filtering coverage: manager `descriptors("investigate")` behavior must be migrated to an equivalent `ToolCatalog` / `investigate_tool_names` or `ToolPlatform.visible_tools` assertion.
  </action>
  <verify>
    <automated>uv run pytest tests/tools/test_tool_platform.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Relevant behavior coverage exists under `ToolPlatform`.
    - No tests require `UnifiedToolManager`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete manager.py and public export</name>
  <files>src/tools/manager.py, src/tools/__init__.py, tests/architecture/test_tool_boundaries.py</files>
  <action>
    Delete `src/tools/manager.py`.
    Remove `UnifiedToolManager` from `src/tools/__init__.py` lazy `__getattr__` and `__all__`.
    Update architecture tests to assert no production code imports `src.tools.manager` and no `UnifiedToolManager` references remain in `src/`.
    Delete or replace `test_unified_manager_does_not_import_domain_services_directly`; it reads `src/tools/manager.py` directly and must not survive after that file is deleted.
    Remove redundant manager-specific boundary assertions once the global no-`src.tools.manager` reference guard exists.
    Delete `tests/agent/test_tools/test_unified_tool_manager.py` once migrated/redundant.
  </action>
  <verify>
    <automated>rg -n "UnifiedToolManager|src\\.tools\\.manager|from src.tools.manager|import src.tools.manager" src tests docs/contract-spec.md --glob '!**/.planning/**'</automated>
    <automated>uv run pytest tests/tools/test_tool_platform.py tests/architecture/test_tool_boundaries.py -q</automated>
    <automated>uv run ruff check src/tools/__init__.py tests/tools/test_tool_platform.py tests/architecture/test_tool_boundaries.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/tools/manager.py` no longer exists.
    - `UnifiedToolManager` has zero matches in `src`, `tests`, and `docs/contract-spec.md`.
    - `src.tools.__all__` does not include `UnifiedToolManager`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Plan 41-03 is complete when:
- The adapter file and public export are gone.
- `rg -n "UnifiedToolManager|src\\.tools\\.manager" src tests docs/contract-spec.md --glob '!**/.planning/**'` has no matches.
- `uv run pytest tests/tools/test_tool_platform.py tests/architecture/test_tool_boundaries.py -q` passes.
</verification>

<output>
After completion, create `41-03-SUMMARY.md`.
</output>
