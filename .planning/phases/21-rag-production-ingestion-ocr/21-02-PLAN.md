---
phase: "21-rag-production-ingestion-ocr"
plan: "02"
title: "Block Aware Chunking And Atomic Ingestion"
type: "execute"
wave: 4
depends_on: ["21-01a"]
files_modified:
  - "src/rag/chunker.py"
  - "src/rag/search_text.py"
  - "src/rag/versioning.py"
  - "src/rag/ingestion.py"
  - "src/repositories/policy_chunk_repo.py"
  - "src/repositories/document_block_repo.py"
  - "src/repositories/rag_ingestion_job_repo.py"
  - "tests/rag/phase21_xfail_inventory.py"
  - "tests/rag/test_block_chunker.py"
  - "tests/rag/test_search_text.py"
  - "tests/test_chunker.py"
  - "tests/test_ingestion.py"
  - "tests/rag/test_ingestion_jobs.py"
autonomous: true
requirements: [PROV-02, CHUNK-01, CHUNK-02, CHUNK-03, CHUNK-04, OCR-01, INGEST-02, INGEST-03, BOUNDARY-01, BOUNDARY-02, BOUNDARY-03]
requirements_addressed: [PROV-02, CHUNK-01, CHUNK-02, CHUNK-03, CHUNK-04, OCR-01, INGEST-02, INGEST-03, BOUNDARY-01, BOUNDARY-02, BOUNDARY-03]
must_haves:
  truths:
    - "Chunks are built from visible source-block text and keep ordered source-block refs."
    - "`PolicyChunk.search_text` is enriched for retrieval without changing `PolicyChunk.content` or `EvidenceRefV1.text_hash`."
    - "Parse, clean, chunk, and embed complete before the short DB write transaction mutates committed documents, blocks, or chunks."
  artifacts:
    - path: "src/rag/chunker.py"
      provides: "chunk_blocks next to existing chunk_markdown"
    - path: "src/rag/versioning.py"
      provides: "Canonical content/semantics version fingerprint"
    - path: "src/rag/ingestion.py"
      provides: "Parser-aware atomic ingestion flow"
  key_links:
    - from: "src/rag/parsers/base.py"
      to: "src/rag/chunker.py"
      via: "ParsedBlock input to chunk_blocks"
    - from: "src/rag/ingestion.py"
      to: "PolicyChunk.source_block_refs_json"
      via: "ordered refs persisted per chunk"
---

<objective>
Implement source-block aware chunking, retrieval-only enrichment, and atomic parser-aware ingestion.

Purpose: real document parsing is only useful if the block provenance survives chunking and failed ingestion cannot corrupt committed policy evidence.
Output: `chunk_blocks(...)`, table-aware chunk refs, version fingerprinting, and ingestion writes that persist documents, blocks, chunks, and jobs atomically.
</objective>

<scope>
In scope: chunking structured blocks, table header/row/cell preservation, search_text enrichment, canonical version bump policy using `PolicyDocument.policy_version_fingerprint`, parse/chunk/embed-before-write orchestration, rollback coverage, and durable safe job status updates.

Out of scope: PDF/DOCX/image/OCR adapter internals and provenance lookup API exposure.
</scope>

<context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@.planning/phases/21-rag-production-ingestion-ocr/21-RESEARCH.md
@.planning/phases/21-rag-production-ingestion-ocr/21-PATTERNS.md
@.planning/phases/20-rag-hybrid-retrieval/20-01-postgres-hybrid-retrieval-SUMMARY.md
@src/rag/parsers/base.py
@src/rag/chunker.py
@src/rag/search_text.py
@src/rag/ingestion.py
@src/repositories/policy_chunk_repo.py
@tests/test_chunker.py
@tests/test_ingestion.py
@tests/rag/test_search_text.py

<interfaces>
Current `ChunkResult` fields:
```python
@dataclass(frozen=True)
class ChunkResult:
    doc_key: str
    chunk_id: str
    section: str
    content: str
    chunk_index: int
    part_index: int | None = None
```

Current ingestion invariant:
```python
embeddings = await self.embedder.embed_documents(texts)
...
existing_doc = await self.doc_repo.get_by_doc_key_for_update(doc_key, self.tenant_id)
...
await self.chunk_repo.bulk_insert(db_chunks)
await self.session.commit()
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| ParsedBlock -> chunk text | Untrusted parser/OCR block text is transformed into canonical citation text and retrieval-only search text. |
| preflight -> DB transaction | Expensive parsing/embedding must finish before the short document write transaction mutates committed policy rows; separate short job-trace transactions may record sanitized status before/after preflight. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Tests | Residual Risk |
|---|---|---|---|---|---|---|
| T21-03 | D | `IngestionService` | mitigate | Complete parse/OCR/chunk/embed before locking/deleting/inserting; persist sanitized job failure rows for early timeout/failure without mutating documents/chunks. | `uv run pytest tests/test_ingestion.py tests/rag/test_ingestion_jobs.py -q` | Native OCR timeout behavior is completed in Plan 03. |
| T21-04 | S/E | `chunk_blocks`, `search_text` | mitigate | Keep hidden/raw parser text out of prompts and public evidence; store only faithful visible text in `content`, enrichment in `search_text`. | `uv run pytest tests/rag/test_block_chunker.py tests/rag/test_search_text.py tests/knowledge/test_text_hash.py -q` | Plan 04 verifies prompt/API surfaces. |
| T21-05 | I | chunk refs | mitigate | Persist ordered block refs scoped through tenant/doc/chunk, not standalone locators. | `uv run pytest tests/rag/test_block_chunker.py -q` | Plan 04 adds tenant/hash verified lookup. |
| T21-07 | E | `PolicyChunk.source_block_refs_json` | mitigate | Refs are subordinate metadata and cannot replace canonical evidence identity. | `uv run pytest tests/knowledge/test_phase21_boundaries.py -q` | Final full gate in Plan 05. |
| T21-08 | I | job errors | mitigate | Rollback failures and record sanitized stage/status/failure code without raw stack/path payloads. | `uv run pytest tests/rag/test_ingestion_jobs.py -q` | Report formatting finalized in Plan 04. |
</threat_model>

<tasks>

<task type="auto" id="21-02-01" tdd="true">
  <name>Add block-aware and table-aware chunking</name>
  <files>src/rag/chunker.py, src/rag/search_text.py, tests/rag/phase21_xfail_inventory.py, tests/rag/test_block_chunker.py, tests/test_chunker.py, tests/rag/test_search_text.py</files>
  <read_first>
    src/rag/parsers/base.py
    src/rag/chunker.py
    src/rag/search_text.py
    tests/rag/phase21_xfail_inventory.py
    tests/rag/test_block_chunker.py
    tests/test_chunker.py
    tests/rag/test_search_text.py
  </read_first>
  <action>
    Extend `src/rag/chunker.py` with `BlockChunkResult`, keeping existing `ChunkResult` and `chunk_markdown(...)` behavior intact.
    Implement `chunk_blocks(blocks: list[ParsedBlock] | tuple[ParsedBlock, ...], doc_key: str, max_chars: int = 1200, target_chars: int = 800, overlap_chars: int = 100) -> list[BlockChunkResult]`.
    `BlockChunkResult` must include `doc_key`, `chunk_id`, `section`, `content`, `chunk_index`, `part_index`, `source_block_refs: tuple[dict, ...]`, and `metadata: dict`.
    Derive `content` only from visible `ParsedBlock.text`, never normalized-only text or retrieval enrichment.
    Source-block refs must preserve order and include `source_block_id`, `block_index`, `block_type`, `text_hash`, `page_number`, `bbox`, `table`, and `ocr` keys when present.
    Table blocks must preserve header/row/cell context in visible citation text. For large tables, split by row groups and repeat headers in every emitted chunk while keeping refs ordered.
    Remove Wave 0 strict xfail markers for block chunker tests this task satisfies and remove their entries from `tests/rag/phase21_xfail_inventory.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/rag/test_block_chunker.py tests/test_chunker.py -q</automated>
  </verify>
  <acceptance_criteria>
    Existing `chunk_markdown` stable ID tests still pass.
    `test_block_chunker.py` asserts stable chunk IDs, faithful visible content, ordered source refs, table headers repeated across row-group splits, and merged-cell metadata retained in refs.
    `rg -n "def chunk_blocks|class BlockChunkResult|source_block_refs" src/rag/chunker.py tests/rag/test_block_chunker.py` finds implementation and tests.
  </acceptance_criteria>
  <done>`chunk_blocks(...)` returns stable chunk IDs, faithful visible citation text, ordered source refs, and table-aware content while `chunk_markdown(...)` remains compatible.</done>
</task>

<task type="auto" id="21-02-02" tdd="true">
  <name>Persist source blocks and chunks through an atomic ingestion flow</name>
  <files>src/rag/ingestion.py, src/repositories/policy_chunk_repo.py, src/repositories/document_block_repo.py, src/repositories/rag_ingestion_job_repo.py, tests/rag/phase21_xfail_inventory.py, tests/test_ingestion.py, tests/rag/test_ingestion_jobs.py, tests/rag/test_block_chunker.py</files>
  <read_first>
    src/rag/ingestion.py
    src/rag/parsers/registry.py
    src/rag/chunker.py
    src/rag/search_text.py
    src/repositories/policy_chunk_repo.py
    src/repositories/document_block_repo.py
    src/repositories/rag_ingestion_job_repo.py
    tests/rag/phase21_xfail_inventory.py
    tests/test_ingestion.py
    tests/rag/test_ingestion_jobs.py
  </read_first>
  <action>
    Refactor `IngestionService.ingest_document(...)` to use a parser registry. Preserve the existing Markdown path by registering Markdown/plain adapters and accepting current manifest metadata.
    Flow order must be: create/update a sanitized `RagIngestionJob` pending/running row in a short independent job-trace transaction, validate source/business-artifact guard, parse file to `ParseResult`, clean/filter accepted/review-needed blocks, call `chunk_blocks(...)`, build embedding texts from title/section/visible content/source context, call `embedder.embed_documents(...)`, validate embedding count, then enter the short locked document replacement transaction.
    Early parse/source validation/OCR preflight failures before the document replacement transaction must update the `RagIngestionJob` row in a separate short job-trace transaction with safe status/stage/error_code/safe_message unless the DB itself is unavailable. DB-unavailable early failures may return a safe in-memory `IngestionReport` only and must not claim a persisted job id.
    Inside the document replacement transaction: lock or create `PolicyDocument`, update source metadata, delete prior blocks/chunks for the document, insert `DocumentBlock` rows, insert `PolicyChunk` rows with `content`, `search_text`, `source_block_refs_json`, `ocr_metadata_json`, risk/effective metadata, and embeddings. Job success/failure status may be updated in the same transaction after document mutations or in a follow-up short job-trace transaction, but repository helpers must not perform independent commits.
    On any exception after document mutation starts, rollback the document replacement transaction, then update the job row in a separate short job-trace transaction with `status="failed"`, safe `error_code`, safe `safe_message`, zero new committed chunks, and no raw stack/path/parser dump.
    Keep `PolicyChunk.content` as faithful visible citation text and keep `search_text` retrieval-only via `build_policy_chunk_search_text(...)`.
    Remove Wave 0 strict xfail markers for ingestion/job tests this task satisfies and remove their entries from `tests/rag/phase21_xfail_inventory.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_ingestion.py tests/rag/test_ingestion_jobs.py tests/rag/test_block_chunker.py -q</automated>
  </verify>
  <acceptance_criteria>
    Tests prove parse/chunk/embed happen before `get_by_doc_key_for_update`.
    Tests prove malformed file, business artifact rejection, OCR runtime missing, and parser failure before document replacement persist sanitized failed `RagIngestionJob` rows unless the DB is unavailable.
    Tests prove DB insert failure rolls back document version, content, blocks, chunks, and source refs while the job row records only sanitized failed status/stage/error_code/safe_message.
    Tests prove DB-unavailable early failure returns a safe report without pretending a job row was persisted.
    `tests/test_ingestion.py` still asserts raw `chunk.content` is persisted while enriched text is used for embedding/search.
    `rg -n "source_block_refs_json|DocumentBlockRepository|RagIngestionJobRepository|chunk_blocks" src/rag/ingestion.py src/repositories tests/test_ingestion.py tests/rag/test_ingestion_jobs.py` finds the new path.
  </acceptance_criteria>
  <done>Parser-aware ingestion persists sanitized job trace rows, writes document/blocks/chunks/source refs/OCR metadata in a short rollback-safe document transaction after preflight work completes, and keeps early failures auditable without corrupting policy evidence.</done>
</task>

<task type="auto" id="21-02-03" tdd="true">
  <name>Implement content-based versioning and retrieval-only enrichment invariants</name>
  <files>src/rag/versioning.py, src/rag/ingestion.py, src/rag/search_text.py, tests/rag/phase21_xfail_inventory.py, tests/test_ingestion.py, tests/rag/test_search_text.py, tests/knowledge/test_text_hash.py, tests/knowledge/test_hybrid_retrieval.py</files>
  <read_first>
    src/rag/ingestion.py
    src/rag/search_text.py
    src/db/models.py
    src/knowledge/schemas.py
    src/knowledge/text_hash.py
    tests/rag/phase21_xfail_inventory.py
    tests/test_ingestion.py
    tests/rag/test_search_text.py
    tests/knowledge/test_text_hash.py
    tests/knowledge/test_hybrid_retrieval.py
  </read_first>
  <action>
    Create `src/rag/versioning.py` with `build_policy_version_fingerprint(*, citation_text: str, title: str, doc_type: str, risk_level: str, effective_date: date) -> str`.
    The fingerprint must use normalized canonical citation text plus semantic policy metadata `title`, `doc_type`, `risk_level`, and `effective_date`. It must exclude parser name/version, parser warnings, timings, file path, source URI, OCR engine version, OCR confidence, bbox, and source-block IDs.
    Update ingestion version logic so `PolicyDocument.version` increments only when this fingerprint changes. Store the current fingerprint only in the dedicated `PolicyDocument.policy_version_fingerprint` field created by Plan 01a. Do not store fingerprints inside `PolicyDocument.parser_metadata_json`; that field is trace/debug-only and must not be a version authority surface.
    Extend `build_policy_chunk_search_text(...)` to accept optional `heading_path`, `table_headers`, and `source_context` arguments and include them only in returned `search_text`; do not mutate or return altered content.
    Add tests proving parser/OCR trace-only changes do not bump `PolicyDocument.version`, canonical content or semantic metadata changes do bump version, and `EvidenceRefV1.text_hash` remains derived from `PolicyChunk.content`.
    Remove Wave 0 strict xfail markers for versioning/search-text tests this task satisfies and remove their entries from `tests/rag/phase21_xfail_inventory.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_ingestion.py tests/rag/test_search_text.py tests/knowledge/test_text_hash.py tests/knowledge/test_hybrid_retrieval.py -q</automated>
  </verify>
  <acceptance_criteria>
    Same citation text plus changed parser version/warnings/timings keeps document version unchanged.
    Same citation text plus changed `PolicyDocument.parser_metadata_json` keeps `build_policy_version_fingerprint(...)` output and document version unchanged.
    Changed citation text, `doc_type`, `risk_level`, `effective_date`, or `title` bumps document version.
    `PolicyDocument.policy_version_fingerprint` stores the current fingerprint and `parser_metadata_json` does not contain `policy_version_fingerprint`.
    `EvidenceRefV1.build(... text=chunk.content ...)` still produces the canonical text hash and never hashes `search_text`.
    Hybrid retrieval tests still prove dense/sparse/fuzzy filters and normalized confidence behavior.
  </acceptance_criteria>
  <done>Version bumps are driven by canonical citation text plus semantic metadata, while parser/OCR trace-only metadata and search enrichment do not alter evidence identity.</done>
</task>

</tasks>

<verification>
`uv run pytest tests/rag/test_block_chunker.py tests/test_chunker.py tests/test_ingestion.py tests/rag/test_search_text.py tests/rag/test_ingestion_jobs.py tests/knowledge/test_text_hash.py tests/knowledge/test_hybrid_retrieval.py -q`
</verification>

<must_haves>
- `chunk_blocks(...)` exists and preserves source-block provenance.
- Ingestion is all-or-nothing for documents, blocks, chunks, and source refs; job trace uses separate sanitized short transactions so early failures are durable without extending the document write transaction.
- Versioning ignores parser/OCR trace-only metadata and preserves v1.3 evidence identity semantics.
</must_haves>

<out_of_scope>
No native PDF/DOCX/image parsing internals, no maintainer provenance lookup API, no `MaterialClaim`, no semantic verifier, no query rewrite service, no reranker service/interface, and no external search backend.
</out_of_scope>

<output>
After completion, create `.planning/phases/21-rag-production-ingestion-ocr/21-02-SUMMARY.md`.
</output>
