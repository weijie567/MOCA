---
phase: "21-rag-production-ingestion-ocr"
plan: "05"
title: "Migration Rollback And Security Closure"
type: "execute"
wave: 8
depends_on: ["21-04a"]
files_modified:
  - "src/db/migrations/versions/015_rag_production_ingestion_ocr.py"
  - "src/rag/ingestion.py"
  - "src/rag/parsers/safety.py"
  - "src/rag/parsers/runtime.py"
  - "tests/rag/phase21_xfail_inventory.py"
  - "tests/test_rag_production_migration.py"
  - "tests/knowledge/test_hybrid_schema.py"
  - "tests/test_ingestion.py"
  - "tests/rag/test_ingestion_safety.py"
  - "tests/rag/test_ingestion_jobs.py"
  - "tests/rag/test_ocr_parser.py"
  - "tests/knowledge/test_provenance_lookup.py"
  - "tests/knowledge/test_phase21_boundaries.py"
autonomous: true
requirements: [SRC-03, SRC-04, SRC-05, PROV-03, PROV-04, OCR-02, SAFE-01, SAFE-02, SAFE-03, INGEST-03, INGEST-04, BOUNDARY-01, BOUNDARY-03, BOUNDARY-04]
requirements_addressed: [SRC-03, SRC-04, SRC-05, PROV-03, PROV-04, OCR-02, SAFE-01, SAFE-02, SAFE-03, INGEST-03, INGEST-04, BOUNDARY-01, BOUNDARY-03, BOUNDARY-04]
must_haves:
  truths:
    - "Migration upgrade, downgrade, and re-upgrade preserve rollback safety and existing Markdown/hybrid retrieval."
    - "Failed parsing, OCR timeout, embedding mismatch, DB insert failure, malformed files, unsafe files, and business artifacts leave previous committed policy evidence intact."
    - "No implementation-pending strict xfail remains for Phase 21-owned behavior before final acceptance."
  artifacts:
    - path: "tests/test_rag_production_migration.py"
      provides: "Migration downgrade/reupgrade coverage"
    - path: "tests/rag/test_ingestion_safety.py"
      provides: "Adversarial file and source boundary coverage"
    - path: "tests/test_ingestion.py"
      provides: "Rollback and prior-version preservation tests"
  key_links:
    - from: "src/db/migrations/versions/015_rag_production_ingestion_ocr.py"
      to: "tests/test_rag_production_migration.py"
      via: "static and optional live DB downgrade/reupgrade tests"
    - from: "src/rag/ingestion.py"
      to: "tests/test_ingestion.py"
      via: "rollback-safe failure coverage"
---

<objective>
Close migration, rollback, adversarial ingestion, and security gaps before the final acceptance record.

Purpose: parser/OCR ingestion changes schema, dependencies, and evidence boundaries; rollback and adversarial behavior must be proven before the phase can close.
Output: final migration/security/rollback tests with no implementation-pending xfails for Phase 21-owned behavior.
</objective>

<scope>
In scope: static and optional DB-backed migration round trip, rollback regression suite, adversarial file/source tests, OCR dependency skip/fail clarity, and xfail cleanup.

Out of scope: final acceptance documentation, Phase 22 context builder/hallucination control, Phase 23 reranker/query rewrite services, Phase RAG-5 external backend, source document UI, async workers, business data ingestion, and external action execution.
</scope>

<context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-rag-production-ingestion-ocr/21-VALIDATION.md
@.planning/phases/21-rag-production-ingestion-ocr/21-RESEARCH.md
@.planning/phases/21-rag-production-ingestion-ocr/21-PATTERNS.md
@.planning/phases/20-rag-hybrid-retrieval/20-01-postgres-hybrid-retrieval-SUMMARY.md
@tests/test_rag_production_migration.py
@tests/test_ingestion.py
@tests/rag/test_ingestion_safety.py
@tests/knowledge/test_phase21_boundaries.py
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| migration -> existing policy retrieval | New source-block/job/provenance structures must upgrade and downgrade without breaking existing Markdown/hybrid retrieval. |
| adversarial source -> ingestion pipeline | Malformed, spoofed, oversized, slow, hidden-prompt, and business-artifact inputs attempt to corrupt evidence or exhaust resources. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Tests | Residual Risk |
|---|---|---|---|---|---|---|
| T21-01 | T | safety/adapters | mitigate | Final spoofed extension/signature/source-type regression gate. | `uv run pytest tests/rag/test_ingestion_safety.py -q` | Additional real corpus signatures can be added after source operations begins. |
| T21-02 | D | migration/safety | mitigate | Final oversize, page, image, DOCX zip, and decompression hazard gate. | `uv run pytest tests/rag/test_ingestion_safety.py tests/test_rag_production_migration.py -q` | Optional live DB gate depends on disposable DB availability. |
| T21-03 | D | parser/OCR/ingestion | mitigate | Final parser timeout, OCR timeout, and rollback gate. | `uv run pytest tests/rag/test_ocr_parser.py tests/test_ingestion.py -q` | Native OCR runtime absence must produce explicit skip/fail message. |
| T21-04 | S/E | prompt/API/memory/action/replay | mitigate | Final hidden prompt injection and raw payload exclusion gate. | `uv run pytest tests/knowledge/test_phase21_boundaries.py -q` | Later UI/viewer surfaces need their own gate. |
| T21-05 | I | provenance lookup | mitigate | Final cross-tenant and hash mismatch negative tests. | `uv run pytest tests/knowledge/test_provenance_lookup.py -q` | Maintainer authorization UI remains future scope. |
| T21-06 | T/E | source guards | mitigate | Final business artifact and Tool System output rejection gate. | `uv run pytest tests/rag/test_ingestion_safety.py -q` | Future source upload workflows must reuse the same guard. |
| T21-07 | E | DocumentBlock authority | mitigate | Final static/runtime tests proving blocks cannot act as evidence/approval/memory/action/replay/business truth. | `uv run pytest tests/knowledge/test_phase21_boundaries.py -q` | Internal provenance remains debug/maintainer only. |
| T21-08 | I | job trace/reporting | mitigate | Final sanitized trace/report gate forbidding stack/path/raw parser leakage. | `uv run pytest tests/rag/test_ingestion_jobs.py -q` | Operational logs outside persisted rows are not broadened in Phase 21. |
</threat_model>

<tasks>

<task type="auto" id="21-05-01">
  <name>Prove migration upgrade, downgrade, and re-upgrade safety</name>
  <files>src/db/migrations/versions/015_rag_production_ingestion_ocr.py, tests/test_rag_production_migration.py, tests/knowledge/test_hybrid_schema.py</files>
  <read_first>
    src/db/migrations/versions/015_rag_production_ingestion_ocr.py
    src/db/migrations/versions/014_rag_hybrid_retrieval.py
    tests/test_rag_production_migration.py
    tests/knowledge/test_hybrid_schema.py
  </read_first>
  <action>
    Finish `tests/test_rag_production_migration.py` so static tests verify upgrade creation of `document_blocks`, `rag_ingestion_jobs`, additive `policy_documents` source metadata, `policy_chunks.source_block_refs_json`, and `policy_chunks.ocr_metadata_json`.
    Verify downgrade order with `source.index(...)`: dependent indexes/constraints first, chunk provenance columns before block/job tables, job table before block table only if FK dependency requires it, then additive document columns.
    Add an optional live round-trip test guarded by `MOCA_TEST_DATABASE_URL`. When set, it must run Alembic upgrade to head, downgrade to `014_rag_hybrid_retrieval`, and re-upgrade to head using the existing Alembic APIs or subprocess command style already used in the repository. When unset, it must skip with a clear message.
    Ensure downgrade tests assert existing Phase 20 `search_text`/`search_vector` structures are untouched by the Phase 21 downgrade except through normal revision movement.
  </action>
  <verify>
    <automated>uv run pytest tests/test_rag_production_migration.py tests/knowledge/test_hybrid_schema.py -q</automated>
  </verify>
  <acceptance_criteria>
    Static tests pass without a live DB.
    If `MOCA_TEST_DATABASE_URL` is set, the optional round-trip test exercises upgrade/downgrade/reupgrade.
    Migration source contains no strict out-of-scope implementation deliverable names: `MaterialClaim`, `semantic_verifier`, `SemanticVerifier`, `QueryRewriteService`, `query_rewriter`, `rewrite_query(`, `CrossEncoderReranker`, `ExternalRerankClient`, `Vespa`, `OpenSearch`, or full `SearchBackend`.
  </acceptance_criteria>
  <done>Migration tests prove dependency-safe downgrade/reupgrade behavior and preservation of existing Phase 20 hybrid structures.</done>
</task>

<task type="auto" id="21-05-02">
  <name>Run adversarial ingestion rollback and security closure</name>
  <files>src/rag/ingestion.py, src/rag/parsers/safety.py, src/rag/parsers/runtime.py, tests/rag/phase21_xfail_inventory.py, tests/test_ingestion.py, tests/rag/test_ingestion_safety.py, tests/rag/test_ingestion_jobs.py, tests/rag/test_ocr_parser.py, tests/knowledge/test_provenance_lookup.py, tests/knowledge/test_phase21_boundaries.py</files>
  <read_first>
    src/rag/ingestion.py
    src/rag/parsers/safety.py
    src/rag/parsers/runtime.py
    tests/rag/phase21_xfail_inventory.py
    tests/test_ingestion.py
    tests/rag/test_ingestion_safety.py
    tests/rag/test_ingestion_jobs.py
    tests/rag/test_ocr_parser.py
    tests/knowledge/test_provenance_lookup.py
    tests/knowledge/test_phase21_boundaries.py
  </read_first>
  <action>
    Remove all remaining target-code-absent strict xfail markers for behavior completed by Plans 01-04a and empty the corresponding entries from `tests/rag/phase21_xfail_inventory.py`. Any remaining xfail/skip must be dependency-only, such as absent native `chi_sim` data, and must include an explicit reason string.
    Add or fix rollback tests covering: parse failure, OCR timeout, embedding count mismatch, DB block insert failure, DB chunk insert failure, malformed PDF/DOCX/image, spoofed file type, oversize file, decompression hazard, hidden prompt injection text, and business artifact source rejection.
    Each failure case must assert prior committed `PolicyDocument.version`, `PolicyDocument.content`, chunks, blocks, chunk refs, and retrieval behavior remain unchanged.
    Each safe failure must record sanitized job trace fields only and must not include raw file bytes, parser dumps, local paths, stack traces, hidden instructions, business object payloads, or Tool System output.
  </action>
  <verify>
    <automated>uv run pytest tests/test_ingestion.py tests/rag/test_ingestion_safety.py tests/rag/test_ingestion_jobs.py tests/rag/test_ocr_parser.py tests/knowledge/test_provenance_lookup.py tests/knowledge/test_phase21_boundaries.py -q</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "target code absent|xfail" tests/rag tests/knowledge tests/test_ingestion.py tests/test_rag_production_migration.py` returns no implementation-pending xfails.
    `tests/rag/phase21_xfail_inventory.py` contains an empty `PHASE21_XFAIL_OWNERS` mapping or no implementation-pending owner entries after Plans 01-04a are complete.
    Dependency skips mention the missing executable or language data explicitly.
    Adversarial rollback tests prove previous committed policy evidence survives all listed failure types.
  </acceptance_criteria>
  <done>Adversarial ingestion and rollback tests pass with no target-code-absent xfails remaining for Phase 21-owned behavior.</done>
</task>

</tasks>

<verification>
`uv run pytest tests/test_rag_production_migration.py tests/knowledge/test_hybrid_schema.py tests/test_ingestion.py tests/rag/test_ingestion_safety.py tests/rag/test_ingestion_jobs.py tests/rag/test_ocr_parser.py tests/knowledge/test_provenance_lookup.py tests/knowledge/test_phase21_boundaries.py -q`
</verification>

<must_haves>
- Migration downgrade/reupgrade is statically covered and optionally live-DB covered when configured.
- No implementation-pending xfail remains for Phase 21-owned behavior.
- Adversarial rollback tests prove prior committed policy evidence survives failure cases.
</must_haves>

<out_of_scope>
No `MaterialClaim`, semantic verifier, runtime hallucination control, conflict/freshness routing, refusal/manual-review answer policy, query rewrite service, reranker service/interface/API, cross-encoder, Vespa, OpenSearch, full external `SearchBackend`, source document UI/CMS, real external action execution, or business data ingestion into RAG.
</out_of_scope>

<output>
After completion, create `.planning/phases/21-rag-production-ingestion-ocr/21-05-SUMMARY.md`.
</output>
