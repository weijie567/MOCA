---
phase: "21-rag-production-ingestion-ocr"
plan: "00"
title: "Wave 0 Validation And Test Scaffolding"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "tests/rag/phase21_xfail_inventory.py"
  - "tests/rag/test_parser_contract.py"
  - "tests/rag/test_document_block_schema.py"
  - "tests/rag/test_block_chunker.py"
  - "tests/rag/test_pdf_parser.py"
  - "tests/rag/test_docx_parser.py"
  - "tests/rag/test_ocr_parser.py"
  - "tests/rag/test_ingestion_safety.py"
  - "tests/rag/test_ingestion_jobs.py"
  - "tests/knowledge/test_provenance_lookup.py"
  - "tests/knowledge/test_phase21_boundaries.py"
  - "tests/test_rag_production_migration.py"
autonomous: true
requirements: [SRC-01, SRC-02, SRC-03, SRC-04, SRC-05, PROV-01, PROV-02, PROV-03, PROV-04, CHUNK-01, CHUNK-02, CHUNK-03, CHUNK-04, OCR-01, OCR-02, SAFE-01, SAFE-02, SAFE-03, INGEST-01, INGEST-02, INGEST-03, INGEST-04, BOUNDARY-01, BOUNDARY-02, BOUNDARY-03, BOUNDARY-04]
requirements_addressed: [SRC-01, SRC-02, SRC-03, SRC-04, SRC-05, PROV-01, PROV-02, PROV-03, PROV-04, CHUNK-01, CHUNK-02, CHUNK-03, CHUNK-04, OCR-01, OCR-02, SAFE-01, SAFE-02, SAFE-03, INGEST-01, INGEST-02, INGEST-03, INGEST-04, BOUNDARY-01, BOUNDARY-02, BOUNDARY-03, BOUNDARY-04]
must_haves:
  truths:
    - "Every Phase 21 requirement has a named automated test file before implementation starts."
    - "Missing parser/OCR/provenance tests are created as skip-safe or strict-xfail scaffolds tied to concrete expected behavior."
    - "Existing v1.3 evidence and hybrid retrieval regression tests remain un-xfailed and executable."
  artifacts:
    - path: "tests/rag/phase21_xfail_inventory.py"
      provides: "Owner-task inventory for implementation-pending Phase 21 xfails"
    - path: "tests/rag/test_parser_contract.py"
      provides: "Parser DTO and registry contract tests"
    - path: "tests/rag/test_block_chunker.py"
      provides: "Block-aware and table-aware chunking tests"
    - path: "tests/knowledge/test_phase21_boundaries.py"
      provides: "Scope guard and authority-boundary regression tests"
  key_links:
    - from: "21-VALIDATION.md"
      to: "tests/rag and tests/knowledge"
      via: "Wave 0 test files"
---

<objective>
Create the missing Phase 21 validation scaffold before implementation plans run.

Purpose: later plans should execute against named pytest files from `21-VALIDATION.md`, not invent coverage after implementation.
Output: test files that encode parser, provenance, chunking, OCR, safety, migration, and boundary expectations.
</objective>

<scope>
In scope: create test modules listed in `21-VALIDATION.md` with concrete test names, owner-tasked strict xfail markers for implementation-dependent assertions, a machine-readable xfail inventory, and skip-safe native OCR preflight checks.

Out of scope: production parser/OCR/schema code, migrations, dependencies, or retrieval behavior changes.
</scope>

<context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-rag-production-ingestion-ocr/21-VALIDATION.md
@.planning/phases/21-rag-production-ingestion-ocr/21-RESEARCH.md
@.planning/phases/21-rag-production-ingestion-ocr/21-PATTERNS.md
@tests/test_ingestion.py
@tests/test_chunker.py
@tests/rag/test_search_text.py
@tests/knowledge/test_hybrid_retrieval.py
@tests/knowledge/test_service.py
@tests/knowledge/test_hybrid_schema.py
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| source file -> parser registry | Untrusted maintainer-supplied file metadata and bytes enter parser selection. |
| parser/OCR output -> database | Untrusted text, coordinates, warnings, and failure metadata become durable rows. |
| database provenance -> maintainer/debug output | Internal provenance can leak across tenants or become an authority surface if not verified. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Tests | Residual Risk |
|---|---|---|---|---|---|---|
| T21-01 | T | test_parser_contract.py, test_ingestion_safety.py | mitigate | Scaffold extension/signature/source-type tests before adapters exist. | `uv run pytest tests/rag/test_parser_contract.py tests/rag/test_ingestion_safety.py -q` | Native signature coverage becomes real in Plan 03. |
| T21-02 | D | test_ingestion_safety.py, test_rag_production_migration.py | mitigate | Scaffold 20MB file, 50-page PDF, 8000x8000 image, DOCX zip hazard, and downgrade checks. | `uv run pytest tests/rag/test_ingestion_safety.py tests/test_rag_production_migration.py -q` | Live DB round trip remains optional until Plan 05. |
| T21-03 | D | test_ocr_parser.py, test_ingestion_jobs.py | mitigate | Scaffold parser timeout 30s and OCR timeout 15s/page tests. | `uv run pytest tests/rag/test_ocr_parser.py tests/rag/test_ingestion_jobs.py -q` | Native Tesseract language data may be absent locally. |
| T21-04 | S/E | test_phase21_boundaries.py | mitigate | Scaffold hidden prompt injection and raw payload exclusion tests. | `uv run pytest tests/knowledge/test_phase21_boundaries.py -q` | Full prompt-surface checks land after provenance/reporting code exists. |
| T21-05 | I | test_provenance_lookup.py | mitigate | Scaffold tenant plus evidence hash verification tests. | `uv run pytest tests/knowledge/test_provenance_lookup.py -q` | Requires Plan 04 repository/service implementation. |
| T21-06 | T/E | test_ingestion_safety.py | mitigate | Scaffold business artifact and Tool System output rejection tests. | `uv run pytest tests/rag/test_ingestion_safety.py -q` | Exact rejection helpers land in Plans 01/03. |
| T21-07 | E | test_phase21_boundaries.py | mitigate | Scaffold negative tests proving DocumentBlock IDs are not evidence, memory, approval, action, replay, or business authority. | `uv run pytest tests/knowledge/test_phase21_boundaries.py -q` | Full static scan grows as files are created. |
| T21-08 | I | test_ingestion_jobs.py | mitigate | Scaffold sanitized failure-code/report tests that reject paths, stack traces, bytes, and raw parser dumps. | `uv run pytest tests/rag/test_ingestion_jobs.py -q` | Plan 04 owns final safe projection behavior. |
</threat_model>

<tasks>

<task type="auto" id="21-00-01">
  <name>Scaffold parser, schema, migration, and scope-guard tests</name>
  <files>tests/rag/phase21_xfail_inventory.py, tests/rag/test_parser_contract.py, tests/rag/test_document_block_schema.py, tests/test_rag_production_migration.py, tests/knowledge/test_phase21_boundaries.py</files>
  <read_first>
    .planning/phases/21-rag-production-ingestion-ocr/21-VALIDATION.md
    .planning/phases/21-rag-production-ingestion-ocr/21-PATTERNS.md
    tests/knowledge/test_hybrid_schema.py
    tests/knowledge/test_evidence_projection.py
    src/knowledge/schemas.py
    src/db/models.py
  </read_first>
  <action>
    Create `tests/rag/test_parser_contract.py`, `tests/rag/test_document_block_schema.py`, `tests/test_rag_production_migration.py`, and `tests/knowledge/test_phase21_boundaries.py`.
    Create `tests/rag/phase21_xfail_inventory.py` with `PHASE21_XFAIL_OWNERS: dict[str, str]` mapping each implementation-pending test node id or stable marker id to the owning task id, such as `21-01-01`, `21-02-01`, `21-03-02`, or `21-04-01`.
    Encode concrete expected names from Phase 21: `ParseResult`, `ParsedBlock`, `SourceBox`, `ParserWarning`, `ParserFailureCode`, `ParserRegistry`, `DocumentBlock`, `RagIngestionJob`, `source_block_refs_json`, and migration revision `015_rag_production_ingestion_ocr` with down revision `014_rag_hybrid_retrieval`.
    Add strict xfail markers only to tests whose implementation target does not exist yet, and every such marker must include an owner task in the reason string: `pytest.mark.xfail(strict=True, reason="phase21 owner_task=21-XX-YY target code absent")`.
    Do not xfail existing `EvidenceRefV1`, `canonical_evidence_projection`, `evidence_text_hash`, approval snapshot, replay, or hybrid retrieval regression tests.
    Add static scope guard tests that fail if Phase 21 introduces strict Phase 22/23/RAG-5 deliverables in implementation surfaces (`src/`, `tests/`, and migration files): `MaterialClaim`, `semantic_verifier`, `SemanticVerifier`, `QueryRewriteService`, `query_rewriter`, `rewrite_query(`, `CrossEncoderReranker`, `ExternalRerankClient`, `SearchBackend`, `Vespa`, or `OpenSearch`.
    The guard must not ban bare existing v1.3 compatibility names. Explicitly allow pre-existing `KnowledgeSearchResult.query_rewrite`, `RERANK_CONFIG_VERSION`, `rerank_config_version`, `rerank_candidates(...)`, and existing Phase 20 hybrid retrieval tests unless a new Phase 23-style service/interface/backend is added.
    Explicitly allow deferred target-state documentation strings in `docs/` and `.planning/` files so existing roadmap/spec deferrals do not fail the guard.
  </action>
  <verify>
    <automated>uv run pytest tests/rag/test_parser_contract.py tests/rag/test_document_block_schema.py tests/test_rag_production_migration.py tests/knowledge/test_phase21_boundaries.py -q</automated>
  </verify>
  <acceptance_criteria>
    `test_parser_contract.py` contains tests for project-owned DTOs and deterministic parser output fields.
    `test_document_block_schema.py` asserts tenant/document scoped source-block and ingestion-job schema names.
    `test_rag_production_migration.py` asserts revision/down_revision and downgrade ordering.
    `phase21_xfail_inventory.py` contains `PHASE21_XFAIL_OWNERS` and every strict xfail reason in Phase 21 tests contains `owner_task=21-`.
    `test_phase21_boundaries.py` includes forbidden-name checks for strict out-of-scope implementation deliverables while allowing explicit deferred target-state documentation strings in docs/planning files and allowlisting current v1.3 `query_rewrite`/rerank compatibility names.
    The verify command exits successfully through passing existing checks plus owner-tasked strict-xfail implementation-pending checks, and `pytest -rxX` output names each xfail owner task.
  </acceptance_criteria>
  <done>Named parser, schema, migration, and boundary scaffold tests exist and run with strict-xfail markers only for absent Phase 21 target code.</done>
</task>

<task type="auto" id="21-00-02">
  <name>Scaffold block chunking, ingestion safety, and job trace tests</name>
  <files>tests/rag/phase21_xfail_inventory.py, tests/rag/test_block_chunker.py, tests/rag/test_ingestion_safety.py, tests/rag/test_ingestion_jobs.py</files>
  <read_first>
    .planning/phases/21-rag-production-ingestion-ocr/21-VALIDATION.md
    tests/rag/phase21_xfail_inventory.py
    tests/test_chunker.py
    tests/test_ingestion.py
    tests/rag/test_search_text.py
    src/rag/chunker.py
    src/rag/ingestion.py
    src/rag/search_text.py
  </read_first>
  <action>
    Create `tests/rag/test_block_chunker.py`, `tests/rag/test_ingestion_safety.py`, and `tests/rag/test_ingestion_jobs.py`.
    Test names must cover: faithful `PolicyChunk.content`, ordered source-block refs, table header/row/cell context including repeated headers and merged-cell metadata, retrieval-only `search_text` enrichment, parser trace-only metadata not bumping document version, parse/OCR/chunk/embed-before-write transaction order, rollback on parse/OCR/embed/DB failure, safe job status/warnings/counts/timings, and sanitized failure reasons.
    Lock concrete limits in tests: max file 20MB, max PDF pages 50, max image dimension 8000x8000, parser timeout 30s, OCR timeout 15s/page.
    Lock OCR thresholds in tests: accepted average confidence >= 80, review-needed 55-79, rejected < 55 or empty text.
    Use strict xfail only for not-yet-implemented Phase 21 behavior; each strict xfail must include `owner_task=21-` and be listed in `tests/rag/phase21_xfail_inventory.py`. Existing Markdown chunker/search_text regression checks must pass normally.
  </action>
  <verify>
    <automated>uv run pytest tests/rag/test_block_chunker.py tests/rag/test_ingestion_safety.py tests/rag/test_ingestion_jobs.py tests/test_chunker.py tests/rag/test_search_text.py -q</automated>
  </verify>
  <acceptance_criteria>
    `rg -n "20MB|50|8000|30|15|accepted|review-needed|rejected" tests/rag/test_ingestion_safety.py tests/rag/test_ocr_parser.py tests/rag/test_ingestion_jobs.py` shows the concrete thresholds or their constants.
    `test_block_chunker.py` includes cases for source-block refs and table context.
    `test_ingestion_jobs.py` includes safe trace assertions forbidding raw paths, stack traces, bytes, and parser dumps.
    Verify command exits successfully with implementation-pending assertions represented as owner-tasked strict xfail.
  </acceptance_criteria>
  <done>Block chunking, safety, and job trace scaffold tests encode thresholds, rollback expectations, and safe trace behavior.</done>
</task>

<task type="auto" id="21-00-03">
  <name>Scaffold parser adapter and provenance lookup tests</name>
  <files>tests/rag/phase21_xfail_inventory.py, tests/rag/test_pdf_parser.py, tests/rag/test_docx_parser.py, tests/rag/test_ocr_parser.py, tests/knowledge/test_provenance_lookup.py</files>
  <read_first>
    .planning/phases/21-rag-production-ingestion-ocr/21-VALIDATION.md
    tests/rag/phase21_xfail_inventory.py
    tests/knowledge/test_service.py
    tests/knowledge/test_hybrid_retrieval.py
    tests/agent/test_memory_evidence_boundary.py
    src/knowledge/service.py
    src/knowledge/retrieval.py
  </read_first>
  <action>
    Create `tests/rag/test_pdf_parser.py`, `tests/rag/test_docx_parser.py`, `tests/rag/test_ocr_parser.py`, and `tests/knowledge/test_provenance_lookup.py`.
    PDF tests must describe digital text blocks, table/cell metadata, scanned-page OCR fallback, top-left page-local `SourceBox` semantics, `pdf_point` units, page width/height, and rotation when known.
    DOCX tests must describe paragraphs/headings/tables in document order and assert page/bbox fields are absent rather than fabricated.
    OCR tests must describe image text, `pixel` boxes, language `chi_sim+eng`, engine/version, timeout/error status, and confidence thresholds from Task 21-00-02.
    Provenance lookup tests must mirror `get_verified_evidence_contents`: malformed tenant returns `{}`, duplicate `(doc_key, chunk_id)` returns `{}`, wrong tenant/hash returns `{}`, repository errors return `{}`, and valid refs return page/bbox/table/OCR locators without changing `EvidenceRefV1`.
  </action>
  <verify>
    <automated>uv run pytest tests/rag/test_pdf_parser.py tests/rag/test_docx_parser.py tests/rag/test_ocr_parser.py tests/knowledge/test_provenance_lookup.py -q</automated>
  </verify>
  <acceptance_criteria>
    Adapter test files contain owner-tasked strict-xfail tests for PDF, DOCX, image, and scanned-PDF OCR behavior.
    OCR tests use `pytest.importorskip` or an explicit runtime preflight for native Tesseract dependency checks, not silent pass-through.
    Provenance tests assert `EvidenceRefV1.model_dump()` has no page, bbox, parser, source-block, or OCR fields.
    Verify command exits successfully with implementation-pending behavior as owner-tasked strict xfail or dependency-specific skip.
  </acceptance_criteria>
  <done>PDF, DOCX, OCR, and verified provenance lookup scaffold tests exist and run skip-safe where native dependencies are absent.</done>
</task>

</tasks>

<verification>
Run all Wave 0 scaffold checks:
`uv run pytest tests/rag/test_parser_contract.py tests/rag/test_document_block_schema.py tests/rag/test_block_chunker.py tests/rag/test_pdf_parser.py tests/rag/test_docx_parser.py tests/rag/test_ocr_parser.py tests/rag/test_ingestion_safety.py tests/rag/test_ingestion_jobs.py tests/knowledge/test_provenance_lookup.py tests/knowledge/test_phase21_boundaries.py tests/test_rag_production_migration.py -q`
</verification>

<must_haves>
- All eleven missing files from `21-VALIDATION.md` exist.
- Every implementation-pending behavior is represented by a named owner-tasked strict xfail in `tests/rag/phase21_xfail_inventory.py` or an explicit dependency skip.
- Existing v1.3 evidence/retrieval tests are referenced but not weakened or xfailed.
</must_haves>

<out_of_scope>
No parser implementation, OCR execution, schema migration, provenance endpoint, `MaterialClaim`, semantic verifier, query rewrite service, reranker service/interface, cross-encoder, Vespa, OpenSearch, full external `SearchBackend`, CMS/viewer UI, business data ingestion, or external action execution.
</out_of_scope>

<output>
After completion, create `.planning/phases/21-rag-production-ingestion-ocr/21-00-SUMMARY.md`.
</output>
