---
phase: "21-rag-production-ingestion-ocr"
plan: "04"
title: "Verified Provenance Lookup And Safe Trace Reporting"
type: "execute"
wave: 6
depends_on: ["21-02", "21-03"]
files_modified:
  - "src/knowledge/provenance.py"
  - "src/knowledge/service.py"
  - "src/repositories/policy_chunk_repo.py"
  - "src/repositories/document_block_repo.py"
  - "src/rag/ingestion.py"
  - "src/repositories/rag_ingestion_job_repo.py"
  - "tests/rag/phase21_xfail_inventory.py"
  - "tests/knowledge/test_provenance_lookup.py"
  - "tests/knowledge/test_service.py"
  - "tests/rag/test_ingestion_jobs.py"
  - "tests/rag/test_ingestion_safety.py"
autonomous: true
requirements: [PROV-03, SAFE-02, INGEST-01, BOUNDARY-03]
requirements_addressed: [PROV-03, SAFE-02, INGEST-01, BOUNDARY-03]
must_haves:
  truths:
    - "Maintainers can resolve verified evidence refs to source-block provenance only after tenant and text-hash validation."
    - "Parser/OCR reports expose durable safe status, warnings, counts, timings, and versions without raw payload leakage, including early parse/source/OCR failures."
  artifacts:
    - path: "src/knowledge/provenance.py"
      provides: "Internal provenance DTOs"
    - path: "src/knowledge/service.py"
      provides: "get_verified_evidence_provenance side path"
    - path: "tests/knowledge/test_provenance_lookup.py"
      provides: "Tenant/hash verified provenance tests"
  key_links:
    - from: "src/knowledge/service.py"
      to: "src/repositories/policy_chunk_repo.py"
      via: "verified evidence content lookup before provenance expansion"
    - from: "src/repositories/policy_chunk_repo.py"
      to: "src/repositories/document_block_repo.py"
      via: "ordered source_block_refs_json"
---

<objective>
Expose source-block provenance and ingestion trace only through verified, tenant-scoped, safe internal paths.

Purpose: Phase 21 adds richer source-location metadata without changing canonical evidence identity or leaking parser/OCR internals into prompts, public evidence serialization, memory, replay, approvals, actions, or business facts.
Output: verified provenance side path and safe ingestion report projection.
</objective>

<scope>
In scope: provenance DTOs, tenant/hash verified service method, repository expansion from chunk refs to blocks, safe ingestion report projection, and safe failure reporting.

Out of scope: user-facing source document viewer/highlight UI, public evidence schema changes, boundary regression across prompts/actions/memory/replay, `MaterialClaim`, semantic verifier, query rewrite service, reranker service/interface, or external search backend.
</scope>

<context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@docs/contract-spec.md
@docs/rag-architecture-spec.md
@.planning/phases/21-rag-production-ingestion-ocr/21-RESEARCH.md
@.planning/phases/21-rag-production-ingestion-ocr/21-PATTERNS.md
@src/knowledge/service.py
@src/knowledge/schemas.py
@src/knowledge/retrieval.py
@src/repositories/policy_chunk_repo.py
@src/repositories/document_block_repo.py
@tests/knowledge/test_service.py
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| evidence ref -> provenance lookup | Caller-supplied evidence refs request source locations and internal metadata. |
| job trace -> maintainer report | Parser/OCR failure metadata is durably recorded and projected into readable diagnostics without raw payloads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Tests | Residual Risk |
|---|---|---|---|---|---|---|
| T21-05 | I | provenance lookup | mitigate | Validate caller tenant, ref tenant, unique keys, and `evidence_text_hash(content)` before returning locators. | `uv run pytest tests/knowledge/test_provenance_lookup.py tests/knowledge/test_service.py -q` | Provenance output remains maintainer/debug-only. |
| T21-08 | I | safe report projection | mitigate | Persist and project only source checksum, parser/OCR versions, stage/status, warnings, counts, timings, and sanitized failure reasons, including failures before the document write transaction. | `uv run pytest tests/rag/test_ingestion_jobs.py -q` | Debug log hygiene outside persisted report is not expanded. |
</threat_model>

<tasks>

<task type="auto" id="21-04-01" tdd="true">
  <name>Add verified source provenance lookup side path</name>
  <files>src/knowledge/provenance.py, src/knowledge/service.py, src/repositories/policy_chunk_repo.py, src/repositories/document_block_repo.py, tests/rag/phase21_xfail_inventory.py, tests/knowledge/test_provenance_lookup.py, tests/knowledge/test_service.py</files>
  <read_first>
    src/knowledge/service.py
    src/knowledge/schemas.py
    src/knowledge/text_hash.py
    src/repositories/policy_chunk_repo.py
    src/repositories/document_block_repo.py
    tests/rag/phase21_xfail_inventory.py
    tests/knowledge/test_provenance_lookup.py
    tests/knowledge/test_service.py
  </read_first>
  <action>
    Create `src/knowledge/provenance.py` with internal Pydantic models or frozen dataclasses: `SourceLocator`, `EvidenceProvenance`, and `EvidenceProvenanceLookupResult`. These are internal/debug/maintainer DTOs, not public `EvidenceRefV1` fields.
    Extend `PolicyRetriever` protocol in `src/knowledge/service.py` with `get_provenance_by_evidence_keys(tenant_id: UUID, keys: list[tuple[str, str]]) -> dict[tuple[str, str], EvidenceProvenance]` or implement an equivalent optional method in `PolicyRetrievalEngine`.
    Add `PolicyKnowledgeService.get_verified_evidence_provenance(tenant_id: str, evidence_refs: list[EvidenceRefV1]) -> dict[str, EvidenceProvenance]`.
    The method must first perform the same checks as `get_verified_evidence_contents`: tenant UUID parse, unique non-empty `(doc_key, chunk_id)`, repository error handling, ref tenant match, and `evidence_text_hash(content) == ref.text_hash`.
    Only after content/hash validation may it expand `PolicyChunk.source_block_refs_json` through `DocumentBlockRepository` into page, bbox, table row/cell, parser/version/warning-code summary, and OCR confidence locators. Do not return arbitrary raw `DocumentBlock.normalized_text`, parser dumps, file bytes, hidden comments, or full parser metadata blobs.
    Return `{}` for malformed tenant, duplicate keys, hash mismatch, wrong tenant, missing chunk, missing/ambiguous block rows, and repository exceptions.
    Remove Wave 0 strict xfail markers for provenance lookup tests this task satisfies and remove their entries from `tests/rag/phase21_xfail_inventory.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/knowledge/test_provenance_lookup.py tests/knowledge/test_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    Valid provenance lookup returns keyed entries by `EvidenceRefV1.evidence_id`.
    Wrong tenant, wrong hash, duplicate key, malformed tenant, missing source blocks, and repository error all return empty results.
    `EvidenceRefV1.model_dump()` remains unchanged and has no locator/provenance fields.
  </acceptance_criteria>
  <done>Verified provenance lookup returns locators only for tenant-matching, unique, content-hash-valid evidence refs.</done>
</task>

<task type="auto" id="21-04-02" tdd="true">
  <name>Add safe ingestion report and trace projection</name>
  <files>src/rag/ingestion.py, src/repositories/rag_ingestion_job_repo.py, tests/rag/phase21_xfail_inventory.py, tests/rag/test_ingestion_jobs.py, tests/rag/test_ingestion_safety.py</files>
  <read_first>
    src/rag/ingestion.py
    src/repositories/rag_ingestion_job_repo.py
    tests/rag/phase21_xfail_inventory.py
    tests/rag/test_ingestion_jobs.py
    tests/rag/test_ingestion_safety.py
    src/conversation/schemas.py
  </read_first>
  <action>
    Add a safe report projection, either in `src/rag/ingestion.py` or a small `src/rag/jobs.py` helper, named `build_safe_ingestion_report(job: RagIngestionJob | Mapping[str, Any]) -> dict[str, Any]`.
    The report must include only: `job_id`, `doc_key`, `source_type`, `source_checksum`, `parser_name`, `parser_version`, `ocr_engine`, `stage`, `status`, `error_code`, `safe_message`, `warnings`, `counts`, `timings`, `started_at`, and `completed_at`.
    The projection must recursively reject or redact forbidden keys matching raw payload patterns from `src/conversation/schemas.py`: `raw`, `raw_prompt`, `raw_args`, `raw_payload`, `raw_tool_output`, `private_reasoning`, `chain_of_thought`, `approval_authority_body`, `action_authority_body`, plus parser-specific keys `file_bytes`, `parser_dump`, `stack_trace`, `local_path`, and `debug_image`.
    Ingestion failure handling must set stable failure codes and safe messages for parser failure, OCR timeout, embedding mismatch, DB insert failure, malformed file, and unsafe business artifact source.
    Early failures before the document replacement transaction, including malformed files, business artifact rejection, parser failure, and OCR runtime unavailable, must persist sanitized `RagIngestionJob` failure rows unless the DB itself is unavailable. DB-unavailable failures must return a safe in-memory report and must not claim a persisted job id.
    Remove Wave 0 strict xfail markers for ingestion job/report tests this task satisfies and remove their entries from `tests/rag/phase21_xfail_inventory.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/rag/test_ingestion_jobs.py tests/rag/test_ingestion_safety.py -q</automated>
  </verify>
  <acceptance_criteria>
    Report tests assert allowed fields exactly and reject forbidden raw/stack/path/parser-dump keys recursively.
    Failure reports contain stable `error_code` plus safe message and do not include absolute filesystem paths or exception reprs.
    Tests prove malformed file, business artifact rejection, parser failure, and OCR runtime unavailable create sanitized failed `RagIngestionJob` rows without creating or mutating `PolicyDocument`, `DocumentBlock`, or `PolicyChunk`.
    Tests prove DB-unavailable early failure returns a safe report with no persisted job id claim.
    `rg -n "build_safe_ingestion_report|file_bytes|parser_dump|stack_trace|local_path" src/rag tests/rag/test_ingestion_jobs.py` shows the projection and tests.
  </acceptance_criteria>
  <done>Safe ingestion reports expose only allowed status, checksum, version, warning, count, timing, and sanitized failure fields.</done>
</task>

</tasks>

<verification>
`uv run pytest tests/knowledge/test_provenance_lookup.py tests/knowledge/test_service.py tests/rag/test_ingestion_jobs.py tests/rag/test_ingestion_safety.py -q`
</verification>

<must_haves>
- Provenance lookup verifies tenant and canonical text hash before source locator expansion.
- Safe ingestion reports never expose raw payloads, file bytes, local paths, parser dumps, or stack traces, and early parser/source/OCR failures have durable sanitized job rows when DB is available.
</must_haves>

<out_of_scope>
No public document viewer/highlight UI, no semantic verifier, no refusal/manual-review answer policy, no query rewrite service, no reranker service/interface, no cross-encoder, no external backend, and no business fact ingestion into RAG.
</out_of_scope>

<output>
After completion, create `.planning/phases/21-rag-production-ingestion-ocr/21-04-SUMMARY.md`.
</output>
