---
phase: 22-rag-context-builder-hallucination-control
phase_number: 22
phase_name: RAG Context Builder + Hallucination Control
status: ready
plan: "02"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/knowledge/test_phase22_evidence_validation.py
  - tests/agent/rag_context/test_leakage.py
  - tests/agent/test_phase22_recommendation_integration.py
  - tests/agent/test_phase22_action_boundary.py
  - tests/agent/test_phase22_final_response.py
  - tests/knowledge/test_phase21_boundaries.py
  - evaluation/golden/phase22_hallucination_cases.jsonl
  - scripts/eval_phase22_hallucination.py
autonomous: true
requirements:
  - CTX-02
  - CTX-06
  - VER-06
  - RTE-03
  - RTE-04
  - RTE-05
  - BND-01
  - BND-02
  - BND-03
  - BND-04
  - BND-05
  - EVAL-01
  - EVAL-02
  - EVAL-03
  - EVAL-04
  - EVAL-05
must_haves:
  truths:
    - "Wave 0 integration, leakage, evidence-validation, boundary, and eval acceptance tests exist before implementation starts."
    - "Latest/current policy version validation is a visible automated acceptance point."
    - "The final eval command is deterministic and does not require live model calls."
  artifacts:
    - path: tests/knowledge/test_phase22_evidence_validation.py
      provides: "RED tests for canonical policy evidence re-fetch, tenant/scope/hash/freshness/latest-version exclusion, and visible latest/current validation."
    - path: tests/agent/rag_context/test_leakage.py
      provides: "RED leakage tests for prompt/final/memory/replay/action surfaces."
    - path: tests/agent/test_phase22_recommendation_integration.py
      provides: "RED integration tests that recommendation generation uses the shared RAG reasoning kernel."
    - path: tests/agent/test_phase22_action_boundary.py
      provides: "RED integration tests that non-allow verifier outcomes block proposed actions, approvals, action drafts, and ActionSafetySnapshot evidence."
    - path: evaluation/golden/phase22_hallucination_cases.jsonl
      provides: "Golden cases covering Phase 22 hallucination-control categories and metrics."
    - path: scripts/eval_phase22_hallucination.py
      provides: "Deterministic local eval CLI with threshold failure support."
  key_links:
    - from: tests/knowledge/test_phase22_evidence_validation.py
      to: src/knowledge/service.py
      via: "PolicyKnowledgeService latest/current validation expectations"
      pattern: "latest|current|policy_version"
    - from: tests/agent/test_phase22_recommendation_integration.py
      to: src/agent/nodes/generate_recommendation.py
      via: "shared builder/verifier spy or fake"
      pattern: "ContextBuilder|MaterialClaimVerifier"
    - from: scripts/eval_phase22_hallucination.py
      to: evaluation/golden/phase22_hallucination_cases.jsonl
      via: "--dataset JSONL loader"
      pattern: "phase22_hallucination_cases"
---

<objective>
Create the remaining Wave 0 validation scaffold required by `22-VALIDATION.md`: evidence validation, leakage, graph/action/final integration, Phase 21 boundary updates, and the Phase 22 hallucination-control eval gate.

Purpose: Phase 22 completion depends on visible safety tests for current policy validation, no-leakage surfaces, deterministic integration behavior, and blocking metrics.
Output: RED pytest files, a deterministic JSONL golden dataset, and a local eval script that later implementation must make pass.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-rag-context-builder-hallucination-control/22-CONTEXT.md
@.planning/phases/22-rag-context-builder-hallucination-control/22-RESEARCH.md
@.planning/phases/22-rag-context-builder-hallucination-control/22-PATTERNS.md
@.planning/phases/22-rag-context-builder-hallucination-control/22-VALIDATION.md
@tests/knowledge/test_service.py
@tests/knowledge/test_provenance_lookup.py
@tests/knowledge/test_phase21_boundaries.py
@tests/agent/test_nodes/test_generate_recommendation.py
@tests/agent/test_nodes/test_assess_risk_and_approval.py
@tests/agent/test_nodes/test_final_response.py
@scripts/eval_rag.py
@evaluation/golden/rag_cases.jsonl
@evaluation/golden/agent_cases.jsonl
</context>

<scope_constraints>
- No UI work and no UI-SPEC gate. The earlier UI keyword detection is a false positive for Phase 22.
- No schema-push task unless implementation later introduces a concrete migration need; planning detected none.
- No live model calls in default tests or the default eval script.
- No automatic regeneration implementation beyond route enum/contract coverage.
- Do not remove Phase 21 boundary guards; update only the forbidden patterns that Phase 22 intentionally owns, and keep guards for EvidenceRefV1 identity, provenance boundaries, rerank/query-rewrite/search-backend scope, and external execution scope.
</scope_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create evidence validation and leakage RED tests</name>
  <dependencies>None. This is Wave 0 scaffolding.</dependencies>
  <files>tests/knowledge/test_phase22_evidence_validation.py, tests/agent/rag_context/test_leakage.py, tests/knowledge/test_phase21_boundaries.py</files>
  <read_first>
  - `.planning/phases/22-rag-context-builder-hallucination-control/22-CONTEXT.md` decisions D-05, D-06, D-13, D-19, D-20
  - `.planning/phases/22-rag-context-builder-hallucination-control/22-VALIDATION.md` Wave 0 requirements
  - `.planning/phases/22-rag-context-builder-hallucination-control/22-PATTERNS.md` sections for `src/knowledge/service.py`, `src/repositories/policy_chunk_repo.py`, `src/knowledge/provenance.py`, and boundary tests
  - `tests/knowledge/test_service.py`
  - `tests/knowledge/test_provenance_lookup.py`
  - `tests/knowledge/test_phase21_boundaries.py`
  </read_first>
  <behavior>
  - Test CTX-02 and VER-01: canonical policy evidence re-fetch excludes malformed tenant IDs, wrong-tenant refs, wrong-scope refs, duplicate keys, content/text_hash mismatch, stale/freshness-invalid refs, and latest/current policy version mismatch.
  - The latest/current mismatch test must isolate policy version semantics: create a current `PolicyDocument.version = 2` row and an incoming `EvidenceRefV1.policy_version = "v1"` that otherwise has valid effective-date and matching content hash, then assert exclusion as `latest_version_invalid` or an equivalent typed reason.
  - The wrong-scope test must bind to existing scope surfaces rather than an abstract label: use `KnowledgeContext.merchant_scope` authorization and/or retrieval `doc_type` / `risk_level` filters so out-of-scope evidence is excluded as `scope_invalid` or an equivalent typed reason.
  - Test CTX-06 and BND-05: source-block/OCR/provenance data may produce prompt-safe labels but raw internals do not enter ordinary prompt, final response, memory, replay, business fact, or action snapshot surfaces.
  - Test BND-01 and BND-02: `EvidenceRefV1` fields remain unchanged and Phase 22 still forbids query rewrite, model/cross-encoder reranking, external rerank APIs, new `SearchBackend`, and Phase 17 execution code.
  </behavior>
  <action>Create RED tests for Phase 22 evidence validation and leakage. Update `tests/knowledge/test_phase21_boundaries.py` so intentional Phase 22 files may contain MaterialClaim/verifier names while the still-forbidden scope patterns remain blocked. Make latest/current policy version validation explicit in test names and assertions.</action>
  <verify>
    <automated>bash -lc 'set +e; uv run pytest tests/knowledge/test_phase22_evidence_validation.py tests/agent/rag_context/test_leakage.py tests/knowledge/test_phase21_boundaries.py -q; status=$?; test "$status" -eq 1'</automated>
  </verify>
  <acceptance_criteria>
  - Evidence-validation tests name and assert latest/current policy version failure separately from effective-date/freshness and text_hash failure.
  - Evidence-validation tests include at least one wrong-scope fixture tied to `merchant_scope`, `doc_type`, or `risk_level`.
  - Leakage tests include sentinel strings for raw tool payloads, retrieval debug fields, verifier prompts/traces, raw provenance, source-block IDs, OCR raw metadata, private reasoning, and unbounded policy text.
  - Phase 21 boundary tests still assert the exact `EvidenceRefV1` field set.
  </acceptance_criteria>
  <done>Evidence, leakage, and boundary RED tests fail only for missing/unimplemented Phase 22 behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create graph/action/final-response integration RED tests</name>
  <dependencies>None. This is Wave 0 scaffolding.</dependencies>
  <files>tests/agent/test_phase22_recommendation_integration.py, tests/agent/test_phase22_action_boundary.py, tests/agent/test_phase22_final_response.py</files>
  <read_first>
  - `.planning/phases/22-rag-context-builder-hallucination-control/22-CONTEXT.md` decisions D-17 and D-18
  - `.planning/phases/22-rag-context-builder-hallucination-control/22-PATTERNS.md` sections for `generate_recommendation.py`, `graph.py`, `assess_risk_and_approval.py`, and `final_response.py`
  - `tests/agent/test_nodes/test_generate_recommendation.py`
  - `tests/agent/test_nodes/test_assess_risk_and_approval.py`
  - `tests/agent/test_nodes/test_final_response.py`
  - `tests/test_graph_routing.py`
  </read_first>
  <behavior>
  - Test RTE-03: `generate_recommendation` uses shared ContextBuilder/verifier output instead of local evidence re-fetch and membership-only citation logic.
  - Test RTE-04: non-allow verifier outcomes prevent proposed actions, approval requests, action drafts, and `ActionSafetySnapshot` evidence.
  - Test RTE-05: final responses for insufficient/refusal/conflict/stale/unauthorized/manual-review outcomes use safe user-facing wording and do not dump verifier/debug/provenance traces.
  </behavior>
  <action>Create RED integration tests with deterministic fakes for ContextBuilder, verifier, and model output. Do not invoke live LLMs. The tests should exercise public node/route functions and assert state deltas, including absence of `proposed_action`, approval routing, action draft creation, and unsafe final-response leakage on non-allow outcomes.</action>
  <verify>
    <automated>bash -lc 'set +e; uv run pytest tests/agent/test_phase22_recommendation_integration.py tests/agent/test_phase22_action_boundary.py tests/agent/test_phase22_final_response.py -q; status=$?; test "$status" -eq 1'</automated>
  </verify>
  <acceptance_criteria>
  - Tests assert the model never selects the safety route.
  - Tests include explicit non-allow states from RTE-02.
  - Tests assert final user-facing text excludes raw reason codes that are internal-only while still communicating safe refusal/insufficient/manual-review meaning.
  </acceptance_criteria>
  <done>Graph/action/final RED integration tests fail only for missing/unimplemented Phase 22 behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create hallucination-control golden eval scaffold</name>
  <dependencies>None. This is Wave 0 scaffolding.</dependencies>
  <files>evaluation/golden/phase22_hallucination_cases.jsonl, scripts/eval_phase22_hallucination.py</files>
  <read_first>
  - `.planning/phases/22-rag-context-builder-hallucination-control/22-CONTEXT.md` decisions D-19 through D-22
  - `.planning/phases/22-rag-context-builder-hallucination-control/22-VALIDATION.md` EVAL Wave 0 row
  - `.planning/phases/22-rag-context-builder-hallucination-control/22-PATTERNS.md` sections for `metrics.py`, `scripts/eval_phase22_hallucination.py`, and golden fixtures
  - `scripts/eval_rag.py`
  - `evaluation/golden/rag_cases.jsonl`
  - `evaluation/golden/agent_cases.jsonl`
  </read_first>
  <behavior>
  - Test EVAL-01: golden cases include supported policy claims, cited-but-unsupported claims, missing citations, stale policy, conflicting policy, unauthorized/hash-mismatched evidence, OCR-low-confidence traps, and insufficient evidence.
  - Test EVAL-02: golden cases prove business facts cannot be inferred from policy evidence, policy claims cannot be supported by business facts or memory, and action recommendations require both policy and business support.
  - Test EVAL-03: golden cases prove unsupported, conflicting, stale, unauthorized, hash-mismatched, OCR-low-confidence, and business-fact-missing outcomes route to refusal, insufficient evidence, regenerate route, or manual review as specified.
  - Test EVAL-05: eval script reports claim support accuracy, citation support accuracy, refusal/manual-review routing accuracy, unsafe answer rate, business-data hallucination rate, leakage count, Level 3 trigger rate, timeout rate, and fail-closed rate.
  </behavior>
  <action>Create a deterministic JSONL dataset with explicit case IDs, categories, expected verifier status, expected route, expected metrics bucket, and `must_not_contain` sentinels. Create a local eval CLI following `scripts/eval_rag.py` patterns with `--dataset`, `--fail-thresholds`, and JSON report output. For Wave 0, the script may fail because implementation adapters are missing, but it must parse arguments, load JSONL, validate case schema, and not call live models.</action>
  <verify>
    <automated>bash -lc 'set +e; uv run python scripts/eval_phase22_hallucination.py --dataset evaluation/golden/phase22_hallucination_cases.jsonl --fail-thresholds; status=$?; test "$status" -eq 1'</automated>
  </verify>
  <acceptance_criteria>
  - Dataset covers EVAL-01 through EVAL-03 and EVAL-05 categories listed in REQUIREMENTS.md.
  - Eval script fails with exit code 1 in Wave 0 only because Phase 22 implementation is missing or metrics do not yet meet thresholds.
  - Eval script has no network/model-provider dependency and no secrets dependency.
  </acceptance_criteria>
  <done>Golden eval scaffold exists and is ready to become passing in Plan 22-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| policy store/repository -> evidence validation tests | Canonical rows are trusted only after tenant, scope, hash, freshness/effective-at, and latest/current version checks pass. |
| OCR/provenance/debug -> prompt/final/memory/replay/action tests | Internal metadata must be projected only as allowlisted labels or kept out of ordinary surfaces. |
| verifier route -> graph/action/final tests | Backend state transitions must consume deterministic route/status fields and block unsafe action boundaries. |
| eval dataset -> eval script | Golden cases are local test data and must not trigger live model calls by default. |

## STRIDE Threat Register

| Threat ID | Severity | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-22-01 | high | Information Disclosure / Elevation of Privilege | `test_phase22_evidence_validation.py` | mitigate/block | Add tests that cross-tenant, unauthorized, and scope-invalid evidence is excluded before bundle inclusion or support. |
| T-22-02 | high | Tampering | `test_phase22_evidence_validation.py` | mitigate/block | Add separate tests for hash mismatch, stale/freshness-invalid refs, duplicate keys, and latest/current policy version mismatch. |
| T-22-03 | high | Tampering / Information Disclosure | `test_leakage.py` and boundary tests | mitigate/block | Add sentinel tests proving prompt/final/memory/replay/action projections never include raw policy/OCR/provenance/debug/private verifier material. |
| T-22-04 | high | Spoofing / Tampering | eval golden cases | mitigate/block | Include cases where business data is hallucinated from policy, memory, or model knowledge and must fail. |
| T-22-05 | high | Elevation of Privilege | graph integration tests | mitigate/block | Add tests proving route is deterministic backend output and that `regenerate_route` is only an enum/contract path. |
| T-22-06 | high | Tampering / Elevation of Privilege | action boundary tests | mitigate/block | Add tests proving non-allow status leaves proposed action, approval request, action draft, and safety snapshot absent. |
| T-22-07 | high | Information Disclosure | final-response and eval tests | mitigate/block | Add safe wording and leakage tests that expose only user-safe outcome categories and redacted metrics. |
</threat_model>

<verification>
Run the Wave 0 integration/eval scaffold checks:

```bash
bash -lc 'set +e; uv run pytest tests/knowledge/test_phase22_evidence_validation.py tests/agent/rag_context/test_leakage.py tests/agent/test_phase22_recommendation_integration.py tests/agent/test_phase22_action_boundary.py tests/agent/test_phase22_final_response.py tests/knowledge/test_phase21_boundaries.py -q; status=$?; test "$status" -eq 1'
bash -lc 'set +e; uv run python scripts/eval_phase22_hallucination.py --dataset evaluation/golden/phase22_hallucination_cases.jsonl --fail-thresholds; status=$?; test "$status" -eq 1'
```

Both commands must exit 0 because the underlying pytest/eval commands produce expected RED failures. Collection, syntax, no-test, fixture, argument-parsing, or live-provider failures must be fixed in this plan.
</verification>

<success_criteria>
- Wave 0 evidence, leakage, integration, boundary, dataset, and eval script scaffolds exist.
- Latest/current policy version validation is visible in test names and assertions.
- The eval scaffold covers EVAL-01 through EVAL-05 categories and metrics.
- No default command requires live model calls or external services.
</success_criteria>

<output>
After completion, create `.planning/phases/22-rag-context-builder-hallucination-control/22-02-SUMMARY.md` with created tests, expected failing commands, and any deviations.
</output>
