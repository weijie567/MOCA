---
phase: 11-intent-clarification
plan: 05
type: execute
wave: 5
depends_on:
  - 11-01
  - 11-02
  - 11-03
  - 11-04
files_modified:
  - src/agent/intent_manifest.py
  - tests/agent/test_intent_manifest.py
  - eval/intent/intent-golden.v1.json
  - eval/intent/coverage-manifest.v1.json
  - eval/intent/m6-statistical-gate.v1.json
  - eval/intent/intent-consistency.v1.json
autonomous: true
requirements:
  - INTENT-01
  - INTENT-02
  - CLARIFY-01
must_haves:
  truths:
    - "Intent consistency metadata is machine-checkable and not used as runtime routing source."
    - "Phase 11 contract golden dataset and M6 statistical gate artifacts are separate and have explicit owner/version/hash/failure-impact semantics."
    - "Golden dataset and coverage manifest include version/hash ownership and fail stale or incomplete metadata for Phase 11 exit."
    - "M6 critical-class Wilson gate evaluation is release-blocking for safety-sensitive confidence-assisted routing and returns statistical_gate_not_demonstrated when sample counts are insufficient."
  artifacts:
    - path: src/agent/intent_manifest.py
      provides: "Manifest/golden dataset checker and Wilson gate helper"
      exports:
        - validate_intent_manifest
        - evaluate_wilson_gate
        - compute_dataset_hash
    - path: eval/intent/intent-golden.v1.json
      provides: "Phase 11 golden intent/slot/clarification/safety-route cases"
    - path: eval/intent/coverage-manifest.v1.json
      provides: "Phase 11 contract coverage requirements, owner/version/hash metadata, and M6 corpus link"
    - path: eval/intent/m6-statistical-gate.v1.json
      provides: "Release-blocking M6 statistical gate metadata and per-class sample requirements"
    - path: eval/intent/intent-consistency.v1.json
      provides: "Machine-readable taxonomy coverage manifest"
  key_links:
    - from: src/agent/intent_manifest.py
      to: src/agent/intent_policy.py
      via: "runtime source tables checked against manifest, not imported by routers"
      pattern: "REQUIRED_SLOT_POLICY"
    - from: tests/agent/test_intent_manifest.py
      to: eval/intent/coverage-manifest.v1.json
      via: "stale dataset/hash and per-class gate tests"
      pattern: "statistical_gate_not_demonstrated"
---

<objective>
Add the intent consistency manifest, golden dataset artifacts, and blocking contract tests for Phase 11.

Purpose: This prevents runtime intent/routing changes from claiming coverage without source support and preserves M6 release-gate semantics for safety-sensitive confidence-assisted routing.
Output: `eval/intent/*` artifacts, `src/agent/intent_manifest.py`, and contract/eval tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-intent-clarification/11-CONTEXT.md
@.planning/phases/11-intent-clarification/11-RESEARCH.md
@.planning/phases/11-intent-clarification/11-VALIDATION.md
@.planning/phases/11-intent-clarification/11-PATTERNS.md
@docs/contract-spec.md
@docs/eval-test-plan.md
@.planning/DEFERRED-DECISIONS.md
@src/agent/intent_policy.py
@src/agent/routing.py
@tests/knowledge/test_citation_membership_eval.py
@tests/knowledge/datasets/citation_membership_v1.json

<interfaces>
Reference used per D-01: classify first, then route; manifest checks coverage of that shape.
Reference used per D-02: structured manifest/eval Pydantic models with exact literals and no free-string parsing.
Reference excluded per D-04/D-05/D-16: email prompts, free tool loop, and memory-updated triage preferences are not manifest/golden sources.
Safety constraint per D-16: ordinary chat golden cases must prove no `approval_result`, resume commands, trusted approval versions, or trusted approval decisions.

Critical classes from D-23/D-24 and `docs/contract-spec.md` §11.4:
```json
["critical_write", "approval_decision", "appeal_or_unban", "complaint_escalation"]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Add manifest, dataset, and Wilson tests</name>
  <files>tests/agent/test_intent_manifest.py, eval/intent/intent-golden.v1.json, eval/intent/coverage-manifest.v1.json, eval/intent/intent-consistency.v1.json</files>
  <read_first>
    - .planning/phases/11-intent-clarification/11-VALIDATION.md
    - .planning/phases/11-intent-clarification/11-PATTERNS.md
    - docs/contract-spec.md §11.4 and §11.7
    - docs/eval-test-plan.md §20.1, §20.3, §20.4, §21.1, §21.2, §21.3
    - .planning/DEFERRED-DECISIONS.md GAD-02 and GAD-03
    - src/agent/intent_policy.py
    - tests/knowledge/test_citation_membership_eval.py
    - tests/knowledge/datasets/citation_membership_v1.json
  </read_first>
  <action>
    Create `eval/intent/intent-golden.v1.json` as the Phase 11 contract golden dataset, not the M6 statistical corpus. Top-level fields must include `dataset_version="intent-golden.v1"`, `owner="Phase 11 Intent / Clarification"`, `schema_version="intent_golden_dataset.v1"`, `gate_scope="phase_11_contract"`, `blocking="phase_exit"`, `failure_impact="block_phase_11_verification"`, and `cases`. Include at least 5 positive cases and at least 3 hard-negative cases per §11.1 ordinary-chat intent. Hard negatives must be labeled with the intent they are negative for (for example `negative_for`) and must be semantically close enough to catch confusion, not filler unrelated text. Include cases covering precedence conflicts, required slots, low confidence, ordinary clarification, `approval_chat_not_trusted`, `critical_write`, `approval_decision`, `appeal_or_unban`, and `complaint_escalation`.
    Create `eval/intent/coverage-manifest.v1.json` for Phase 11 contract coverage with `schema_version="coverage_manifest.v1"`, `dataset_version="intent-golden.v1"`, `dataset_hash="sha256:<computed hash of intent-golden.v1.json>"`, `owner="Phase 11 Intent / Clarification"`, `gate_scope="phase_11_contract"`, `blocking="phase_exit"`, `failure_impact="block_phase_11_verification"`, `dedupe_key="stable_case_id"`, `coverage_status`, and a pointer to `m6_statistical_gate_path="eval/intent/m6-statistical-gate.v1.json"`. Do not set `coverage_status="complete"` for the M6 statistical gate based on this small contract dataset.
    Create separate `eval/intent/m6-statistical-gate.v1.json` for the M6 release gate with `schema_version="m6_statistical_gate.v1"`, `owner="Phase 11 Intent / Clarification"`, `gate_scope="m6_release"`, `blocking="release_safety_sensitive_confidence_assisted_routing"`, `failure_impact="block_m6_release_not_phase_11_exit"`, `dataset_version` for the future statistical corpus, `dataset_hash`, `coverage_manifest_hash`, `required_classes=["critical_write","approval_decision","appeal_or_unban","complaint_escalation"]`, `per_class_expected_min_n` set to 300 for each class, `coverage_status`, and `default_gate_status="statistical_gate_not_demonstrated"` until each class reaches the required sample count.
    Create `eval/intent/intent-consistency.v1.json` with `manifest_version="intent-consistency.v1"`, `dataset_version`, `dataset_hash`, `coverage_status`, and one entry for each ordinary-chat taxonomy intent with `in_precedence`, `in_required_slots`, `in_routing`, `in_evidence_table`, and `in_golden_set`. Set `in_evidence_table=false` only for `small_talk` and `unsupported`. Add explicit deferred-boundary metadata or assertions for D-31: Phase 11 implements no multi-step read-only QA expansion, and any future multi-step read-only QA expansion must re-apply GAD-01 guardrails plus GAD-02 admission fields before entering taxonomy/runtime routing.
    Follow the `11-PATTERNS.md` analogs in `tests/knowledge/test_citation_membership_eval.py` and `tests/knowledge/datasets/citation_membership_v1.json` for immutable dataset layout, stable case IDs, hash-backed fixture tests, and failure messages.
    Create `tests/agent/test_intent_manifest.py` with tests for D-21 through D-31:
    - implements D-22 by asserting `coverage-manifest.v1.json` is the Phase 11 contract gate, `m6-statistical-gate.v1.json` names the M6 release gate `safety_sensitive_confidence_assisted_routing`, and neither artifact labels M6 as a Phase 12 migration.
    - enforces `docs/eval-test-plan.md` §20.4 by failing unless every ordinary intent has at least 5 positive cases and at least 3 hard-negative cases.
    - implements D-26 by validating manifest coverage against all `ORDINARY_INTENTS`, `REQUIRED_SLOT_POLICY`, intent-level routes, evidence-required intents, and golden positives/negatives.
    - implements D-27 by proving `small_talk` and `unsupported` may set `in_evidence_table=false` only when they route directly to `final_response` and do not enter `route_after_investigate`.
    - implements D-28 by failing tests for missing manifest coverage, stale dataset/hash metadata, and manifest claims not backed by the corresponding source tables.
    - fails when dataset hash is stale or coverage status is missing/incomplete/invalid.
    - proves `small_talk` and `unsupported` exemptions are allowed only when they route directly and do not enter `route_after_investigate`.
    - tests Wilson helper status precedence exactly against the separate M6 statistical gate artifact: manifest missing/incomplete/invalid -> `statistical_gate_not_demonstrated`; below per-class minimum -> `statistical_gate_not_demonstrated`; false negatives -> `fail`; Wilson upper > 0.01 -> `fail`; otherwise `pass`.
    - asserts each M6 per-class eval result includes `dataset_hash`, `coverage_manifest_hash`, `coverage_status`, `class_name`, `required_min_n`, `n`, `false_negatives`, `wilson_upper_95_one_sided`, `formula_version="wilson_one_sided_95_v1"`, `confidence_level=0.95`, `gate_status`, and first-match `gate_reason`.
    - asserts `gate_status` is exactly one of `pass`, `fail`, or `statistical_gate_not_demonstrated`, and `gate_reason` uses exact first-match precedence codes: `coverage_missing`, `coverage_incomplete`, `coverage_invalid`, `below_per_class_min_n`, `false_negatives_present`, `wilson_upper_exceeded`, `passed`.
    - asserts pooled metrics cannot pass if any required class fails.
    - implements D-29 by asserting GAD-02 future intent admission fields exist in manifest or helper metadata: `risk_level`, `response_mode`, `tool_allowlist`, `bounded_loop_allowed`, `max_iterations`, `routing_precedence`, and `audit_replay_requirements`.
    - asserts no new `generic_qa` intent exists and current read-only QA coverage is represented by `policy_qa`, `order_status_inquiry`, and `advise`/support advice cases per GAD-03.
    - implements D-31 by asserting there is no Phase 11 multi-step read-only QA expansion manifest entry, route, or golden case, and that any future expansion metadata names both GAD-01 guardrails and GAD-02 admission fields before promotion.
  </action>
  <verify>
    <automated>uv run pytest tests/agent/test_intent_manifest.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `test -f eval/intent/intent-golden.v1.json && test -f eval/intent/coverage-manifest.v1.json && test -f eval/intent/m6-statistical-gate.v1.json && test -f eval/intent/intent-consistency.v1.json`.
    - `rg -n "phase_11_contract|m6_release|block_phase_11_verification|block_m6_release_not_phase_11_exit|positive|hard-negative|negative_for|coverage_manifest_hash|wilson_one_sided_95_v1|confidence_level|gate_reason|coverage_missing|below_per_class_min_n|statistical_gate_not_demonstrated|GAD-01|GAD-02|multi-step read-only QA" tests/agent/test_intent_manifest.py eval/intent` finds all required dataset, gate-result, blocking/failure-impact, and D-31 deferred-boundary coverage.
    - The automated pytest command passes after Task 1.
  </acceptance_criteria>
  <done>Wave 0 manifest/eval artifacts and tests exist for source coverage, hashes, per-class gates, and deferred-boundary carry-forward.</done>
</task>

<task type="auto">
  <name>Task 1: Implement manifest checker and Wilson gate</name>
  <files>src/agent/intent_manifest.py, tests/agent/test_intent_manifest.py, eval/intent/intent-golden.v1.json, eval/intent/coverage-manifest.v1.json, eval/intent/m6-statistical-gate.v1.json, eval/intent/intent-consistency.v1.json</files>
  <read_first>
    - src/agent/intent_policy.py
    - .planning/phases/11-intent-clarification/11-PATTERNS.md
    - tests/agent/test_intent_manifest.py
    - eval/intent/intent-golden.v1.json
    - eval/intent/coverage-manifest.v1.json
    - eval/intent/m6-statistical-gate.v1.json
    - eval/intent/intent-consistency.v1.json
    - docs/contract-spec.md §11.4 and §11.7
    - tests/knowledge/test_citation_membership_eval.py
    - tests/knowledge/datasets/citation_membership_v1.json
  </read_first>
  <action>
    Create `src/agent/intent_manifest.py` as contract/eval tooling only; do not import it from runtime routers, graph, classifier, or nodes.
    Implement:
    - Pydantic models with `ConfigDict(extra="forbid")` for `GoldenCase`, `IntentGoldenDataset`, `CoverageManifest`, `M6StatisticalGate`, `IntentConsistencyEntry`, `IntentConsistencyManifest`, and Wilson gate result.
    - `compute_dataset_hash(path: Path) -> str` returning `sha256:<hex>` over canonical JSON bytes with sorted keys and compact separators.
    - `load_json_model(path: Path, model: type[BaseModel]) -> BaseModel`.
    - `validate_intent_manifest(golden_path: Path, coverage_path: Path, consistency_path: Path, m6_gate_path: Path | None = None) -> list[str]` returning explicit error strings for missing intents, missing required-slot entries, missing routing entries, invalid evidence-table exemption, missing positive/negative golden examples, stale dataset hash, invalid coverage status, missing owner/version/hash/failure-impact fields, missing GAD-02 admission fields, forbidden generic QA intent, and any attempt to treat the small Phase 11 contract dataset as a complete M6 statistical corpus.
    - `wilson_upper_false_negative(false_negatives: int, n: int) -> float` using `z = 1.6448536269514722` and the §11.4 one-sided formula without continuity correction.
    - `evaluate_wilson_gate(class_name: str, false_negatives: int, n: int, m6_gate: M6StatisticalGate) -> dict[str, Any]` with fixed status precedence and `gate_reason` preserving the first matched reason. Its result must include `dataset_hash`, `coverage_manifest_hash`, `coverage_status`, `class_name`, `required_min_n`, `n`, `false_negatives`, `wilson_upper_95_one_sided`, `formula_version="wilson_one_sided_95_v1"`, `confidence_level=0.95`, `gate_status`, and `gate_reason`.
      `gate_status` values are exactly `pass`, `fail`, and `statistical_gate_not_demonstrated`. `gate_reason` values are exactly `coverage_missing`, `coverage_incomplete`, `coverage_invalid`, `below_per_class_min_n`, `false_negatives_present`, `wilson_upper_exceeded`, and `passed`, with first-match precedence so later checks never overwrite an earlier reason.
    Update the JSON manifests so their `dataset_hash` / `coverage_manifest_hash` values match the computed hashes after final formatting. If the M6 statistical corpus is not populated in Phase 11, M6 gate evaluation must return `statistical_gate_not_demonstrated`, not `pass`, while Phase 11 contract tests may still pass.
  </action>
  <verify>
    <automated>uv run pytest tests/agent/test_intent_manifest.py -q && uv run ruff check src/agent/intent_manifest.py tests/agent/test_intent_manifest.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "def compute_dataset_hash|def validate_intent_manifest|def wilson_upper_false_negative|def evaluate_wilson_gate|class M6StatisticalGate|ConfigDict\\(extra=\"forbid\"\\)|coverage_manifest_hash|wilson_one_sided_95_v1|confidence_level|gate_reason" src/agent/intent_manifest.py` finds all checker functions, strict models, and M6 result fields.
    - `rg -n "intent_manifest" src/agent | rg -v "src/agent/intent_manifest.py"` returns no runtime import from graph, routers, classifier, or nodes.
    - `uv run python -c 'from pathlib import Path; from src.agent.intent_manifest import validate_intent_manifest; errors=validate_intent_manifest(Path("eval/intent/intent-golden.v1.json"), Path("eval/intent/coverage-manifest.v1.json"), Path("eval/intent/intent-consistency.v1.json"), Path("eval/intent/m6-statistical-gate.v1.json")); assert not errors, errors'` passes.
    - The automated pytest and ruff command passes.
  </acceptance_criteria>
  <done>Manifest/golden coverage is machine-checkable, hash-owned, per-class gated, and kept out of runtime routing.</done>
</task>

<task type="auto">
  <name>Task 2: Run phase gate and assumption/review checks</name>
  <files>tests/agent/test_intent_manifest.py, eval/intent/intent-golden.v1.json, eval/intent/coverage-manifest.v1.json, eval/intent/m6-statistical-gate.v1.json, eval/intent/intent-consistency.v1.json</files>
  <read_first>
    - .planning/phases/11-intent-clarification/11-VALIDATION.md
    - .planning/phases/11-intent-clarification/11-CONTEXT.md
    - docs/contract-spec.md §11.4 and §11.7
  </read_first>
  <action>
    Run the Phase 11 validation commands and capture any failures in the plan summary:
    - focused Phase 11 command from `11-VALIDATION.md`: `uv run pytest tests/agent/test_intent_adapter.py tests/agent/test_intent_routing.py tests/agent/test_required_slots.py tests/agent/test_clarification_gate.py -q`.
    - manifest contract command: `uv run pytest tests/agent/test_intent_manifest.py -q`.
    - graph/classifier command: `uv run pytest tests/agent/test_nodes/test_classify_intent.py tests/agent/test_graph.py -q`.
    - lint command: `uv run ruff check src/agent tests/agent`.
    Run `gsd-sdk query phase-assumptions 11` if available; if the exact query is unavailable, record that and use `rg -n "D-0[1-9]|D-[12][0-9]|D-3[01]|Deferred" .planning/phases/11-intent-clarification/11-CONTEXT.md` as the decision-boundary check.
    Do not implement trusted approval lifecycle, approval respond/needs_info resume, approval version CAS, ActionSafetySnapshot, durable action execution, PostgreSQL session-memory CAS, external execution, or a free tool loop.
  </action>
  <verify>
    <automated>uv run pytest tests/agent/test_intent_adapter.py tests/agent/test_intent_routing.py tests/agent/test_required_slots.py tests/agent/test_clarification_gate.py tests/agent/test_intent_manifest.py tests/agent/test_nodes/test_classify_intent.py tests/agent/test_graph.py -q && uv run ruff check src/agent tests/agent</automated>
  </verify>
  <acceptance_criteria>
    - The combined automated pytest and ruff command passes.
    - `rg -n "ApprovalService|ActionSafetySnapshot|Command\\(|resume\\(|approval_version|postgres.*session|free tool loop|external execution|multi-step read-only QA expansion" src/agent tests/agent eval/intent` returns no Phase 11 implementation of deferred boundaries except negative tests, manifest deferred-boundary assertions, or explicit forbidden strings.
    - `rg -n "intent-golden.v1|coverage-manifest.v1|m6-statistical-gate.v1|intent-consistency.v1|M6|statistical_gate_not_demonstrated|block_phase_11_verification|block_m6_release_not_phase_11_exit" .planning/phases/11-intent-clarification/11-05-SUMMARY.md` will find the eval artifact, blocking semantics, failure impact, and gate results after the executor writes the summary.
  </acceptance_criteria>
  <done>Phase 11 intent, slot, clarification, graph, and manifest validation commands pass with deferred boundaries preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime source tables -> manifest | Manifest claims must be checked against code/spec-derived source tables and golden cases. |
| dataset file -> coverage gate | Dataset/hash metadata can be stale or incomplete. |
| eval metrics -> release gate | Pooled or under-sampled metrics must not pass per-critical-class gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Spoofing / Elevation of Privilege | golden ordinary-chat approval cases | mitigate | Golden cases include approval-looking chat and assert no trusted approval decision path. |
| T-11-02 | Tampering | manifest models | mitigate | Pydantic `extra="forbid"` models reject extra trusted fields or malformed metadata. |
| T-11-03 | Tampering / Information Disclosure | missing-slot golden cases | mitigate | Golden cases and manifest verify missing-slot routes enter `clarification_gate`, not tools/actions. |
| T-11-04 | Elevation of Privilege | Wilson/M6 gate | mitigate | Per-class Wilson status precedence blocks low-confidence/high-risk false negatives and insufficient samples. |
| T-11-05 | Repudiation | manifest coverage claims | mitigate | `validate_intent_manifest` fails missing coverage, stale hashes, invalid exemptions, and unsupported claims. |
</threat_model>

<verification>
Run:
```bash
uv run pytest tests/agent/test_intent_adapter.py tests/agent/test_intent_routing.py tests/agent/test_required_slots.py tests/agent/test_clarification_gate.py tests/agent/test_intent_manifest.py tests/agent/test_nodes/test_classify_intent.py tests/agent/test_graph.py -q
uv run ruff check src/agent tests/agent
```
</verification>

<success_criteria>
- `eval/intent/intent-golden.v1.json`, `coverage-manifest.v1.json`, `m6-statistical-gate.v1.json`, and `intent-consistency.v1.json` exist with owner/version/hash metadata and explicit blocking/failure-impact semantics.
- Manifest checker fails stale or incomplete metadata and is not imported by runtime routing.
- Phase 11 contract golden dataset enforces at least 5 positive and 3 hard-negative cases per ordinary intent without claiming M6 statistical completion.
- Wilson gate uses one-sided 95% upper bound with fixed precedence, complete M6 result fields, exact first-match `gate_reason` codes, and `statistical_gate_not_demonstrated` for insufficient samples.
- GAD-02 admission fields, GAD-03 existing read-only QA coverage, and D-31 deferred multi-step read-only QA boundary are test-covered without adding generic QA or implementing a new QA expansion.
</success_criteria>

<output>
After completion, create `.planning/phases/11-intent-clarification/11-05-SUMMARY.md`.
</output>
