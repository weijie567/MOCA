---
phase: 11-intent-clarification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/schemas.py
  - src/agent/state.py
  - src/agent/prompts.py
  - src/agent/nodes/classify_intent.py
  - tests/agent/test_intent_adapter.py
  - tests/agent/test_nodes/test_classify_intent.py
autonomous: true
requirements:
  - INTENT-01
must_haves:
  truths:
    - "LLM intent output is accepted only through a strict IntentResultV3 schema."
    - "IntentResultV3 maps into AgentState through a field-by-field adapter."
    - "Ordinary chat classifier output cannot write approval_result, trusted approval versions, resume commands, extracted_slots, active_slots, risk_signals, final_response, or tool/action outputs."
  artifacts:
    - path: src/agent/schemas.py
      provides: "IntentResultV3, RequiredSlotExpression, ClarificationRequest, and literal contracts"
      contains: "schema_version: Literal[\"intent_result.v3\"]"
    - path: src/agent/nodes/classify_intent.py
      provides: "IntentResultV3 structured-output classifier and adapter"
      exports:
        - classify_intent
        - intent_result_to_state
    - path: tests/agent/test_intent_adapter.py
      provides: "Adapter and forbidden-write tests"
  key_links:
    - from: src/agent/nodes/classify_intent.py
      to: src/agent/schemas.py
      via: "ChatOpenAI.with_structured_output(IntentResultV3)"
      pattern: "with_structured_output\\(IntentResultV3\\)"
    - from: src/agent/nodes/classify_intent.py
      to: src/agent/state.py
      via: "intent_result_to_state field-by-field update"
      pattern: "intent_result_to_state"
---

<objective>
Create the Phase 11 intent schema, prompt contract, AgentState fields, and explicit classifier adapter.

Purpose: This establishes the untrusted LLM boundary for all later intent routing work.
Output: Strict `IntentResultV3` models, canonical state fields, a classifier prompt that excludes ordinary-chat approval decisions, and adapter tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-intent-clarification/11-CONTEXT.md
@.planning/phases/11-intent-clarification/11-RESEARCH.md
@.planning/phases/11-intent-clarification/11-VALIDATION.md
@.planning/phases/11-intent-clarification/11-PATTERNS.md
@docs/contract-spec.md
@docs/agent-architecture-phase-decomposition.md
@docs/migration-plan.md
@docs/eval-test-plan.md
@.planning/DEFERRED-DECISIONS.md
@src/agent/schemas.py
@src/agent/state.py
@src/agent/prompts.py
@src/agent/nodes/classify_intent.py
@tests/agent/test_nodes/test_classify_intent.py
@tests/agent/conftest.py

<interfaces>
Reference used per D-01: `agents-from-scratch-ts` triage routing pattern only as classify-first-then-route structure.
Reference used per D-02: LangGraph adaptive RAG structured output routing with Pydantic `BaseModel` + `Literal[...]`; no free-string parsing.
Reference excluded per D-04/D-05/D-16: email prompts, customer-support notebook prompts, free tool loop, and memory-updated triage preferences.
Safety constraint per D-03/D-11/D-16: ordinary chat cannot create `approval_result`, `Command(resume=...)`, trusted approval versions, trusted approval decisions, or approval lifecycle state.

Existing classifier seam:
```python
structured_llm = _get_llm().with_structured_output(IntentResult)
result = await structured_llm.ainvoke(messages)
return {"current_intent": result.intent, "last_intent": result.intent, ...}
```

Target ordinary-chat taxonomy from `docs/contract-spec.md` §11.1:
```python
Intent = Literal[
    "policy_qa", "order_status_inquiry", "refund_troubleshooting",
    "compensation_suggestion", "ticket_reply_draft", "appeal_or_unban",
    "complaint_escalation", "action_request", "small_talk", "unsupported",
]
RequestedOperation = Literal["read_status", "advise", "draft_reply", "draft_action", "execute_action", "escalate"]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Add Wave 0 adapter and classifier tests</name>
  <files>tests/agent/test_intent_adapter.py, tests/agent/test_nodes/test_classify_intent.py</files>
  <read_first>
    - tests/agent/test_nodes/test_classify_intent.py
    - tests/agent/conftest.py
    - .planning/phases/11-intent-clarification/11-VALIDATION.md
    - docs/contract-spec.md §10.4 and §11.6
  </read_first>
  <action>
    Create `tests/agent/test_intent_adapter.py` with tests for D-06, D-08, D-17, D-18, D-19, and D-20:
    - validating a full `IntentResultV3` fixture with `schema_version="intent_result.v3"`, `primary_intent="refund_troubleshooting"`, `requested_operation="advise"`, `confidence=0.86`, `calibrated_confidence=0.81`, `secondary_intents=["compensation_suggestion"]`, `required_slots={"all_of":[],"any_of":[["refund_case_id","order_id"]],"optional":["ticket_id","merchant_id"]}`, `candidate_slots={"order_id":"ORD-1001"}`, `routing_hints={"needs_business_context": true}`, `classifier_version="intent_classifier.v2"`, `calibration_version="calibration.unverified"`, and `reason_codes=["refund_keywords"]`.
    - asserting `intent_result_to_state(result, prior_llm_outputs={...})` writes only `primary_intent`, `requested_operation`, `intent_confidence`, `secondary_intents`, `required_slots`, `candidate_slots`, `routing_hints`, compatibility `current_intent`, compatibility `last_intent`, and `llm_outputs.intent_classification.eval_metadata`.
    - asserting `confidence` maps to `intent_confidence` and `calibrated_confidence` maps only to `llm_outputs.intent_classification.eval_metadata.calibrated_confidence`.
    - asserting no returned state update contains `approval_result`, `approval_revision_refs`, `trusted_approval_result`, `resume`, `command`, `extracted_slots`, `active_slots`, `risk_signals`, `final_response`, `tool_results`, `action_result`, or `proposed_action`.
    Extend `tests/agent/test_nodes/test_classify_intent.py` so fake LLM success returns `primary_intent`, `requested_operation`, `intent_confidence`, `required_slots`, and compatibility `current_intent`, while invalid extra fields such as `approval_result` cause validation failure and safe `unsupported` or node error behavior.
  </action>
  <verify>
    <automated>uv run pytest tests/agent/test_intent_adapter.py tests/agent/test_nodes/test_classify_intent.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "IntentResultV3|intent_result_to_state|calibrated_confidence|approval_result|active_slots" tests/agent/test_intent_adapter.py` finds the adapter contract tests.
    - `rg -n "approval_result|trusted_approval|Command\\(|resume" tests/agent/test_nodes/test_classify_intent.py` finds negative ordinary-chat classifier coverage.
    - The automated pytest command passes after Tasks 1-2 in this plan.
  </acceptance_criteria>
  <done>Wave 0 tests exist for the classifier adapter, confidence separation, schema strictness, and forbidden state writes.</done>
</task>

<task type="auto">
  <name>Task 1: Define strict IntentResultV3 and AgentState fields</name>
  <files>src/agent/schemas.py, src/agent/state.py, src/agent/prompts.py</files>
  <read_first>
    - src/agent/schemas.py
    - src/agent/state.py
    - src/agent/prompts.py
    - docs/contract-spec.md §10.1, §10.4, §11.1, §11.3, §11.5, §11.6
    - .planning/phases/11-intent-clarification/11-CONTEXT.md
  </read_first>
  <action>
    In `src/agent/schemas.py`, add strict Pydantic contracts with `model_config = ConfigDict(extra="forbid")`:
    - `IntentLiteral = Literal["policy_qa","order_status_inquiry","refund_troubleshooting","compensation_suggestion","ticket_reply_draft","appeal_or_unban","complaint_escalation","action_request","small_talk","unsupported"]`.
    - `RequestedOperationLiteral = Literal["read_status","advise","draft_reply","draft_action","execute_action","escalate"]`.
    - `RequiredSlotExpression` with `all_of: list[str] = Field(default_factory=list)`, `any_of: list[list[str]] = Field(default_factory=list)`, and `optional: list[str] = Field(default_factory=list)`.
    - `ClarificationRequest` with `reason: Literal["missing_required_slots","low_confidence","unsupported_or_ambiguous","multi_target_request","approval_chat_not_trusted"]`, `clarification_request_id: str`, `questions: list[str]`, `blocked_nodes: list[str]`, and `resume_policy: Literal["same_thread_only"] = "same_thread_only"`.
    - `IntentResultV3` with the exact D-06 fields: `schema_version: Literal["intent_result.v3"] = "intent_result.v3"`, `primary_intent`, `requested_operation`, `confidence`, `calibrated_confidence`, `secondary_intents`, `required_slots`, `candidate_slots`, `routing_hints`, `classifier_version`, `calibration_version`, and `reason_codes`.
    - Preserve D-07 by documenting and enforcing that `primary_intent` stores domain semantics while `requested_operation` stores the user-requested operation; write/escalation operations must not overwrite the most specific domain intent.
    Keep the old `IntentResult` importable only if needed for backward-compatible tests, but switch new classifier code to `IntentResultV3`.
    In `src/agent/state.py`, add canonical Phase 11 fields to `AgentState`: `intent_confidence: float | None`, `secondary_intents: list[str] | None`, `required_slots: dict[str, Any] | None`, `candidate_slots: dict[str, Any] | None`, `routing_hints: dict[str, Any] | None`, and `clarification_request: dict[str, Any] | None`.
    In `src/agent/prompts.py`, replace the `CLASSIFY_INTENT_SYSTEM` contract so allowed ordinary-chat intents are only the §11.1 taxonomy above, `requested_operation` is one of the six §11.2 values, response JSON matches `IntentResultV3`, `approval_decision` is explicitly forbidden, and examples do not include email, mailbox, free tool-loop, memory preference, or `approval_request` content per D-04/D-05.
  </action>
  <verify>
    <automated>uv run pytest tests/agent/test_intent_adapter.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "class IntentResultV3|schema_version: Literal\\[\"intent_result.v3\"\\]|ConfigDict\\(extra=\"forbid\"\\)|class RequiredSlotExpression|class ClarificationRequest" src/agent/schemas.py` finds the new strict models.
    - `rg -n "intent_confidence|secondary_intents|required_slots|candidate_slots|routing_hints|clarification_request" src/agent/state.py` finds every Phase 11 AgentState field.
    - `rg -n "approval_request|approval_decision" src/agent/prompts.py` shows no `approval_request` allowed intent and only a forbidden-boundary mention for `approval_decision`.
    - `rg -n "email|mailbox|memory-updated|free tool" src/agent/prompts.py` returns no copied reference-domain prompt content.
  </acceptance_criteria>
  <done>`IntentResultV3` and related state/prompt contracts exist and reject extra trusted fields.</done>
</task>

<task type="auto">
  <name>Task 2: Replace classifier mapping with explicit adapter</name>
  <files>src/agent/nodes/classify_intent.py, tests/agent/test_intent_adapter.py, tests/agent/test_nodes/test_classify_intent.py</files>
  <read_first>
    - src/agent/nodes/classify_intent.py
    - src/agent/schemas.py
    - src/agent/state.py
    - tests/agent/test_intent_adapter.py
    - tests/agent/test_nodes/test_classify_intent.py
    - docs/contract-spec.md §10.4 and §11.6
  </read_first>
  <action>
    Update `src/agent/nodes/classify_intent.py` to import `IntentResultV3` and call `_get_llm().with_structured_output(IntentResultV3)`. Add `intent_result_to_state(result: IntentResultV3, prior_llm_outputs: dict[str, Any] | None = None) -> dict[str, Any]` that:
    - returns replace writes for `primary_intent`, `requested_operation`, `intent_confidence`, `secondary_intents`, `required_slots`, `candidate_slots`, `routing_hints`, compatibility `current_intent`, and compatibility `last_intent`.
    - writes `llm_outputs.intent_classification.raw` with the validated `IntentResultV3.model_dump()` and `llm_outputs.intent_classification.eval_metadata` with `calibrated_confidence`, `classifier_version`, and `calibration_version`.
    - never copies unknown keys and never returns forbidden keys named in D-20.
    Update `classify_intent` success path to merge the adapter output with the existing trace step. Update error fallback to return `primary_intent="unsupported"`, `requested_operation="advise"`, `intent_confidence=0.0`, compatibility `current_intent="unsupported"`, `last_intent="unsupported"`, a zero-slot `required_slots`, and a node error; do not return `approval_result`, resume commands, or tool/action outputs.
  </action>
  <verify>
    <automated>uv run pytest tests/agent/test_intent_adapter.py tests/agent/test_nodes/test_classify_intent.py -q && uv run ruff check src/agent/schemas.py src/agent/state.py src/agent/prompts.py src/agent/nodes/classify_intent.py tests/agent/test_intent_adapter.py tests/agent/test_nodes/test_classify_intent.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "with_structured_output\\(IntentResultV3\\)|def intent_result_to_state|intent_confidence|eval_metadata|classifier_version|calibration_version" src/agent/nodes/classify_intent.py` finds the new adapter wiring.
    - `rg -n "\"approval_result\"|Command\\(|resume|trusted_approval|risk_signals|action_result|proposed_action" src/agent/nodes/classify_intent.py` returns no classifier writes for those trusted/action fields.
    - The automated pytest and ruff command passes.
  </acceptance_criteria>
  <done>The intent classifier emits only validated V3 ordinary-chat state through an explicit adapter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user text -> intent classifier | Untrusted ordinary chat may include approval-looking text or action language. |
| LLM structured output -> AgentState | Model output is untrusted and must be schema-validated plus adapter-mapped. |
| classifier state -> downstream routers | Later routers consume only allowed, typed fields from this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Spoofing / Elevation of Privilege | `classify_intent` | mitigate | Exclude `approval_decision` from `IntentResultV3`; tests assert ordinary chat cannot write `approval_result`, trusted versions, or resume commands. |
| T-11-02 | Tampering | `IntentResultV3` | mitigate | Use `ConfigDict(extra="forbid")` and `intent_result_to_state` field-by-field adapter. |
| T-11-03 | Tampering / Information Disclosure | slot fields | transfer | This plan marks `candidate_slots` as hints only; Plan 11-03 implements deterministic slot completeness. |
| T-11-04 | Elevation of Privilege | confidence fields | mitigate | Store model confidence in `intent_confidence`; calibrated confidence only in eval metadata; no confidence-only action routing is implemented here. |
| T-11-05 | Repudiation | manifest/eval metadata | transfer | This plan writes classifier/calibration metadata; Plan 11-05 implements manifest/hash stale-coverage failure. |
</threat_model>

<verification>
Run:
```bash
uv run pytest tests/agent/test_intent_adapter.py tests/agent/test_nodes/test_classify_intent.py -q
uv run ruff check src/agent/schemas.py src/agent/state.py src/agent/prompts.py src/agent/nodes/classify_intent.py tests/agent/test_intent_adapter.py tests/agent/test_nodes/test_classify_intent.py
```
</verification>

<success_criteria>
- `IntentResultV3` is the classifier structured output schema.
- `intent_result_to_state` is the only classifier-to-state mapper.
- `approval_request` is no longer an ordinary-chat classifier intent.
- Candidate slots do not write `extracted_slots` or `active_slots`.
- Tests prove classifier output cannot set approval or resume state.
</success_criteria>

<output>
After completion, create `.planning/phases/11-intent-clarification/11-01-SUMMARY.md`.
</output>
