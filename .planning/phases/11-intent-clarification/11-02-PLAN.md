---
phase: 11-intent-clarification
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/agent/intent_policy.py
  - src/agent/nodes/classify_intent.py
  - tests/agent/test_intent_routing.py
  - tests/agent/test_nodes/test_classify_intent.py
autonomous: true
requirements:
  - INTENT-01
  - CLARIFY-01
must_haves:
  truths:
    - "Safety-sensitive ordinary text is classified by deterministic policy before router decisions consume LLM output."
    - "Intent precedence preserves the most specific domain intent while requested_operation carries write/escalation safety implications."
    - "Approval-looking ordinary chat is untrusted and maps to a safe ordinary-chat disposition, never a trusted approval decision."
  artifacts:
    - path: src/agent/intent_policy.py
      provides: "Taxonomy, requested-operation literals, required-slot table, deterministic pre-router, precedence helpers, and confidence defaults"
      exports:
        - ORDINARY_INTENTS
        - REQUESTED_OPERATIONS
        - REQUIRED_SLOT_POLICY
        - PreRouteDecision
        - detect_pre_route
        - resolve_intent_precedence
    - path: tests/agent/test_intent_routing.py
      provides: "Pre-router and precedence golden unit tests"
    - path: src/agent/nodes/classify_intent.py
      provides: "Runtime pre-route detection call and safe merge into classifier state"
  key_links:
    - from: src/agent/intent_policy.py
      to: docs/contract-spec.md
      via: "§11.1 taxonomy, §11.2 precedence, §11.3 required slots, §11.4 confidence defaults"
      pattern: "REQUIRED_SLOT_POLICY"
    - from: src/agent/nodes/classify_intent.py
      to: src/agent/intent_policy.py
      via: "`classify_intent` calls `detect_pre_route` before or alongside LLM classification"
      pattern: "detect_pre_route"
    - from: src/agent/nodes/classify_intent.py
      to: src/agent/routing.py
      via: "`PreRouteDecision` disposition and reason codes are persisted into `routing_hints` / `reason_codes` for later `route_after_intent` consumption"
      pattern: "pre_route_disposition|approval_chat_not_trusted"
---

<objective>
Implement the deterministic policy layer used before intent routing: ordinary-chat taxonomy constants, pre-router safety detection, precedence resolution, and confidence defaults.

Purpose: This makes high-risk action/write/escalation/approval-looking text deterministic before downstream graph routers run.
Output: `src/agent/intent_policy.py` and tests for precedence conflicts, safety-sensitive requests, and approval-chat rejection.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-intent-clarification/11-CONTEXT.md
@.planning/phases/11-intent-clarification/11-RESEARCH.md
@.planning/phases/11-intent-clarification/11-VALIDATION.md
@.planning/phases/11-intent-clarification/11-PATTERNS.md
@docs/contract-spec.md
@docs/eval-test-plan.md
@.planning/DEFERRED-DECISIONS.md
@src/agent/schemas.py
@src/agent/nodes/classify_intent.py
@src/agent/routing.py
@tests/agent/test_nodes/test_classify_intent.py

<interfaces>
Reference used per D-01: `agents-from-scratch-ts` triage routing pattern only as classify-first-then-route.
Reference used per D-02: LangGraph adaptive RAG structured output routing with `BaseModel` + `Literal[...]`; `PreRouteDecision` must be Pydantic and must not parse free-form model strings.
Reference excluded per D-04/D-05/D-16: email prompts, free tool loop, and memory-updated triage preferences.
Safety constraint per D-11/D-16: ordinary chat approval-looking text cannot create `approval_result`, trusted approval versions, resume commands, or trusted approval decisions.

From Plan 11-01:
```python
IntentLiteral
RequestedOperationLiteral
RequiredSlotExpression
IntentResultV3
```

Pattern analog from `11-PATTERNS.md`: use `src/agent/routing.py` as the closest pure-helper analog for `src/agent/intent_policy.py`; keep helpers deterministic, side-effect-free, and independent from LLM/tool/database calls.

Runtime wiring contract for D-09/D-11: `classify_intent` must call `detect_pre_route` before or alongside structured LLM classification, merge `PreRouteDecision.disposition`, `requires_clarification`, and `reason_codes` into ordinary classifier state, and make approval-looking text such as `approve APR-1` safe even if the LLM would classify it as a normal read-only request.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Add pre-router and precedence tests</name>
  <files>tests/agent/test_intent_routing.py</files>
  <read_first>
    - tests/test_graph_routing.py
    - tests/agent/test_nodes/test_classify_intent.py
    - .planning/phases/11-intent-clarification/11-VALIDATION.md
    - docs/contract-spec.md §11.1, §11.2, §11.4
    - docs/eval-test-plan.md §20.1 and §21.1
  </read_first>
  <action>
    Create `tests/agent/test_intent_routing.py` with pure unit tests for D-09, D-10, D-11, D-23, and D-30:
    - `detect_pre_route("approve APR-1")` returns `PreRouteDecision(disposition="approval_chat_not_trusted", requested_operation="advise", reason_codes` containing `approval_chat_not_trusted`) and never returns `approval_decision`.
    - `classify_intent` with user text `approve APR-1` and a fake LLM that would otherwise return a confident read-only classification still produces safe ordinary state: `routing_hints.pre_route_disposition="approval_chat_not_trusted"`, `routing_hints.clarification_reason="approval_chat_not_trusted"`, `reason_codes` containing `approval_chat_not_trusted`, `requested_operation="advise"`, no `approval_result`, and no trusted resume/approval fields. This test must not depend on LLM output to pass.
    - text asking to execute or approve a refund override produces requested operation `execute_action` or a safe clarification disposition, not a read-only route.
    - precedence conflicts preserve `primary_intent="appeal_or_unban"` for unban/appeal text with action language, `primary_intent="complaint_escalation"` for escalation text, `primary_intent="compensation_suggestion"` for compensation action text, and `requested_operation` carries `draft_action`, `execute_action`, or `escalate`.
    - `policy_qa`, `order_status_inquiry`, and `advise`/support advice terminal paths remain represented by existing intents; no `generic_qa`, `qa`, or `support_qa` intent is admitted per D-30.
    - low-confidence safety defaults route action-related, approval-related, appeal/unban, and complaint escalation classes toward safe clarification/risk paths rather than read-only final response.
  </action>
  <verify>
    <automated>uv run pytest tests/agent/test_intent_routing.py tests/agent/test_nodes/test_classify_intent.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "approval_chat_not_trusted|appeal_or_unban|complaint_escalation|compensation_suggestion|generic_qa|low_confidence" tests/agent/test_intent_routing.py` finds coverage for each listed case.
    - `rg -n "approve APR-1|pre_route_disposition|clarification_reason|approval_chat_not_trusted" tests/agent/test_nodes/test_classify_intent.py` finds classifier runtime wiring coverage.
    - The automated pytest command passes after Task 2.
  </acceptance_criteria>
  <done>Wave 0 routing tests encode deterministic safety precedence and the ordinary/trusted approval boundary.</done>
</task>

<task type="auto">
  <name>Task 1: Implement deterministic intent policy helpers</name>
  <files>src/agent/intent_policy.py, tests/agent/test_intent_routing.py</files>
  <read_first>
    - src/agent/schemas.py
    - src/agent/routing.py
    - docs/contract-spec.md §9.3, §9.5, §11.1, §11.2, §11.3, §11.4
    - .planning/DEFERRED-DECISIONS.md GAD-02 and GAD-03
    - tests/agent/test_intent_routing.py
  </read_first>
  <action>
    Create `src/agent/intent_policy.py` as deterministic constants/helpers, not a runtime `IntentRegistry` per D-25:
    - `ORDINARY_INTENTS` exactly matching §11.1 and no `approval_request`, `approval_review`, `approval_decision`, `manual_review`, `generic_qa`, `qa`, or `support_qa`.
    - `REQUESTED_OPERATIONS` exactly `read_status`, `advise`, `draft_reply`, `draft_action`, `execute_action`, `escalate`; do not include `approval_decision`.
    - `REQUIRED_SLOT_POLICY` with the exact §11.3 expressions for all ten ordinary-chat intents.
    - Explicit source-of-truth tables used by runtime routers and manifest validation, not copied ad hoc into tests: `PRECEDENCE_INTENTS` ordered by §11.2 precedence, `INTENT_ROUTE_POLICY` mapping each ordinary intent to its intended route family, `DIRECT_RESPONSE_INTENTS = {"small_talk", "unsupported"}`, and `EVIDENCE_REQUIRED_INTENTS` for every intent that enters `investigate`, with `small_talk` and `unsupported` excluded.
    - `HIGH_RISK_INTENTS = {"appeal_or_unban", "complaint_escalation", "action_request"}` and `CRITICAL_ROUTE_CLASSES = {"critical_write","approval_decision","appeal_or_unban","complaint_escalation"}` for eval-gate alignment.
    - `PreRouteDecision(BaseModel)` with `model_config = ConfigDict(extra="forbid")`, `disposition: Literal["none","approval_chat_not_trusted","safety_sensitive","multi_target_request"]`, `requested_operation: RequestedOperationLiteral | None`, `reason_codes: list[str]`, and `requires_clarification: bool = False`.
    - `detect_pre_route(query: str) -> PreRouteDecision` using deterministic keyword/regex checks for approval-looking chat (`approve`, `reject`, `accept`, `APR-`, `审批`, `通过`, `拒绝`), execute/write action language (`execute`, `refund now`, `发券`, `直接退款`, `执行`), escalation language (`escalate`, `主管`, `升级`, `投诉`), and multi-target phrases; no LLM calls, DB calls, tools, network, or repository access.
    - `resolve_intent_precedence(primary_intent: str, requested_operation: str, query: str, secondary_intents: list[str] | None = None) -> tuple[str, str, list[str]]` applying §11.2 precedence in order: appeal/unban, complaint escalation, compensation, ticket reply draft, refund/order facts, policy QA, generic action fallback, small_talk/unsupported.
    - `confidence_requires_clarification(primary_intent: str, requested_operation: str, confidence: float | None, pre_route: PreRouteDecision | None = None) -> bool` using §11.4 defaults: `<0.65` clarifies; action/approval/appeal/unban/refund write classes require `>=0.85` or deterministic trigger confirmation before drafting; confidence alone never authorizes action.
    Follow the `11-PATTERNS.md` analog in `src/agent/routing.py`: pure helper functions, fixed literal sets, fail-closed defaults, and no LLM/tool/database access.
    Keep this module pure and side-effect free. This plan is the D-15 `11-02` slice: deterministic pre-router plus intent precedence; the classifier-side call to this module is implemented in Task 2, while the D-15 `11-03` slice owns `route_after_intent`/slot graph routing.
  </action>
  <verify>
    <automated>uv run pytest tests/agent/test_intent_routing.py -q && uv run ruff check src/agent/intent_policy.py tests/agent/test_intent_routing.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "ORDINARY_INTENTS|REQUESTED_OPERATIONS|REQUIRED_SLOT_POLICY|PRECEDENCE_INTENTS|INTENT_ROUTE_POLICY|DIRECT_RESPONSE_INTENTS|EVIDENCE_REQUIRED_INTENTS|PreRouteDecision|detect_pre_route|resolve_intent_precedence|confidence_requires_clarification|CRITICAL_ROUTE_CLASSES" src/agent/intent_policy.py` finds all exported helpers and source tables.
    - `rg -n "approval_request|approval_review|approval_decision|generic_qa|support_qa|manual_review" src/agent/intent_policy.py` returns no ordinary intent or requested-operation admission; `approval_decision` may appear only inside `CRITICAL_ROUTE_CLASSES` for eval metadata.
    - `rg -n "requests|httpx|sqlalchemy|ChatOpenAI|invoke|ainvoke" src/agent/intent_policy.py` returns no side-effecting dependencies.
    - The automated pytest and ruff command passes.
  </acceptance_criteria>
  <done>Deterministic pre-router and precedence helpers exist and pass golden routing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Wire pre-route detection into classifier state</name>
  <files>src/agent/nodes/classify_intent.py, tests/agent/test_nodes/test_classify_intent.py</files>
  <read_first>
    - src/agent/nodes/classify_intent.py
    - src/agent/intent_policy.py
    - src/agent/schemas.py
    - src/agent/state.py
    - src/agent/routing.py
    - tests/agent/test_nodes/test_classify_intent.py
    - docs/contract-spec.md §10.4, §11.1, §11.2, §11.4, §11.6
  </read_first>
  <action>
    Update `src/agent/nodes/classify_intent.py` so `classify_intent` calls `detect_pre_route(user_text)` before or alongside the structured LLM call. Merge the resulting `PreRouteDecision` into the `IntentResultV3 -> AgentState` adapter output without whole-object merging:
    - add `routing_hints.pre_route_disposition`, `routing_hints.requires_clarification`, and `routing_hints.clarification_reason` when disposition is not `"none"`.
    - append pre-route `reason_codes` to the classifier `reason_codes` / eval metadata reason-code list without dropping LLM reason codes.
    - for `disposition="approval_chat_not_trusted"`, force safe ordinary-chat routing fields regardless of the LLM response: `requested_operation="advise"`, `routing_hints.clarification_reason="approval_chat_not_trusted"`, no `approval_decision`, no `approval_result`, no trusted approval versions, and no resume/Command output.
    - preserve `current_intent` / `last_intent` compatibility writes from Plan 11-01 while keeping canonical `primary_intent` as the source for later routers.
    Extend `tests/agent/test_nodes/test_classify_intent.py` with a fake LLM returning a confident read-only result for input `approve APR-1`; the expected state must still carry `approval_chat_not_trusted` pre-route metadata and be consumable by Plan 11-03 `route_after_intent` as a clarification path without relying on LLM output.
  </action>
  <verify>
    <automated>uv run pytest tests/agent/test_nodes/test_classify_intent.py tests/agent/test_intent_routing.py -q && uv run ruff check src/agent/nodes/classify_intent.py tests/agent/test_nodes/test_classify_intent.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "detect_pre_route|pre_route_disposition|clarification_reason|approval_chat_not_trusted" src/agent/nodes/classify_intent.py tests/agent/test_nodes/test_classify_intent.py` finds runtime wiring and classifier tests.
    - `rg -n "approval_result|trusted_approval|Command\\(|resume\\(" src/agent/nodes/classify_intent.py` returns no classifier writes for trusted approval lifecycle fields.
    - The automated pytest and ruff command passes.
  </acceptance_criteria>
  <done>`classify_intent` persists deterministic pre-route decisions into ordinary routing hints/reason codes before downstream routers consume classifier state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user text -> deterministic pre-router | Untrusted text may impersonate approval commands or high-risk action requests. |
| policy helper -> graph router | Pure helpers must not perform side effects or trust runtime manifest data. |
| deferred intent admission -> runtime taxonomy | GAD-02 admission rules prevent silent default enablement of new intents. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Spoofing / Elevation of Privilege | `detect_pre_route` | mitigate | Approval-looking text maps to `approval_chat_not_trusted` and cannot output `approval_decision`, `approval_result`, or resume. |
| T-11-02 | Tampering | `PreRouteDecision` | mitigate | Use Pydantic `BaseModel` with `extra="forbid"` and Literal-constrained dispositions/operations. |
| T-11-03 | Tampering / Information Disclosure | required-slot table | transfer | `REQUIRED_SLOT_POLICY` is defined here; Plan 11-03 evaluates completeness and blocks missing slots. |
| T-11-04 | Elevation of Privilege | confidence gate helper | mitigate | `confidence_requires_clarification` prevents low-confidence/high-risk requests from read-only routing and forbids confidence-only action authorization. |
| T-11-05 | Repudiation | coverage metadata | transfer | Critical classes are declared here; Plan 11-05 enforces per-class manifest/golden coverage and stale hash failure. |
</threat_model>

<verification>
Run:
```bash
uv run pytest tests/agent/test_intent_routing.py -q
uv run pytest tests/agent/test_nodes/test_classify_intent.py -q
uv run ruff check src/agent/intent_policy.py src/agent/nodes/classify_intent.py tests/agent/test_intent_routing.py tests/agent/test_nodes/test_classify_intent.py
```
</verification>

<success_criteria>
- Pre-routing is deterministic and side-effect free.
- `classify_intent` calls `detect_pre_route` and persists `PreRouteDecision` metadata into ordinary routing hints/reason codes.
- §11.2 precedence conflicts preserve the specific domain intent.
- Approval-looking ordinary chat is never a trusted approval command.
- Existing read-only QA endpoints stay represented by `policy_qa`, `order_status_inquiry`, and advisory response modes; no generic QA intent is added.
</success_criteria>

<output>
After completion, create `.planning/phases/11-intent-clarification/11-02-SUMMARY.md`.
</output>
