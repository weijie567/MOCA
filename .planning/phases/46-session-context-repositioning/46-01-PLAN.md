---
phase: 46-session-context-repositioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/contract-spec.md
  - docs/current-implementation-map.md
  - docs/architecture-overview.md
  - .planning/MEMORY-REDESIGN-DECISIONS.md
autonomous: true
requirements: [MEM-03]
must_haves:
  truths:
    - "Session memory is documented as thread-scoped temporary conversational context after Case Working Context exists"
    - "Session memory is documented as distinct from case_working_contexts, case_memories, and long_term_memories"
    - "Prompt hints from session context are documented as contextual pointers, not evidence, business facts, approval/action authority, or replay truth"
    - "Stale non-normative docs no longer describe production search_case_memory as session-derived"
    - "DEFER-2 and DEFER-3 remain named as Phase 47 and Phase 48 scope"
  artifacts:
    - path: "docs/contract-spec.md"
      provides: "normative MEM-03 session context boundary"
      contains: "same-thread temporary conversational context"
    - path: "docs/current-implementation-map.md"
      provides: "current implementation map for session and reviewed case memory"
      contains: "LegacySessionPrecedentSearchService"
    - path: "docs/architecture-overview.md"
      provides: "architecture overview memory layer wording"
      contains: "reviewed case memory"
    - path: ".planning/MEMORY-REDESIGN-DECISIONS.md"
      provides: "named remaining memory defers"
      contains: "DEFER-2 -> Phase 47"
  key_links:
    - from: "docs/contract-spec.md"
      to: "src/db/models.py"
      via: "session_memories storage identity wording"
      pattern: "session_memories.*tenant_id.*user_id.*thread_id"
    - from: "docs/contract-spec.md"
      to: "src/memory/session_bundle.py"
      via: "session hint semantics"
      pattern: "policy_topic_hints|prior_policy_mention_refs"
    - from: "docs/current-implementation-map.md"
      to: "src/tools/executors/memory.py"
      via: "production reviewed case memory executor"
      pattern: "CaseMemoryService|retrieve_reviewed"
---

<objective>
Reconcile the Phase 46 contract and architecture documentation so `session_memories` has one explicit post-CWC role: same-thread, short-lived conversational context.

Purpose: MEM-03 is mainly a semantic drift prevention phase. The docs must stop implying that session memory can act as cross-case working state, reviewed precedent, preference memory, policy evidence, business fact authority, approval/action authority, or replay truth.
Output: contract-spec wording, implementation-map corrections, architecture-overview corrections, and named defer carry-forward for Phase 47 and Phase 48.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/MEMORY-REDESIGN-DECISIONS.md
@.planning/phases/46-session-context-repositioning/46-CONTEXT.md
@.planning/phases/46-session-context-repositioning/46-RESEARCH.md
@.planning/phases/46-session-context-repositioning/46-VALIDATION.md
@.planning/phases/46-session-context-repositioning/46-PATTERNS.md
@.planning/phases/45-memory-lifecycle-wiring-for-case-working-context/45-04-SUMMARY.md
@.planning/phases/45-memory-lifecycle-wiring-for-case-working-context/45-VERIFICATION.md
@docs/contract-spec.md
@docs/current-implementation-map.md
@docs/architecture-overview.md

<interfaces>
Contract sections to update:
- `docs/contract-spec.md` Section 13.2: session memory post-CWC boundary.
- `docs/contract-spec.md` Section 13.4 / 13.4a: reviewed precedent and CWC separation. Do not move Phase 47 behavior into Phase 46.
- `docs/contract-spec.md` storage rows near `session_memories`: preserve tenant/user/thread unique scope and CAS wording.

Non-normative docs to reconcile:
- `docs/current-implementation-map.md` currently contains stale wording that says production `search_case_memory` wraps session precedent search.
- `docs/architecture-overview.md` currently contains stale wording that says `search_case_memory` can retrieve session-derived precedent.

Current production facts from research:
- `src/tools/executors/memory.py` uses `CaseMemoryService(CaseMemoryRepository(session))` and calls `retrieve_reviewed(...)`.
- `src/memory/search.py` contains `LegacySessionPrecedentSearchService`, documented as debug-only and not planner-facing.
- `session_memories` remains keyed by tenant/user/thread and has no case_id column.
</interfaces>

<source_audit>
SOURCE | ID | Feature / Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | Phase 46 | Reposition session_memories as thread-scoped short-lived conversational memory only | 46-01, 46-02, 46-03 | COVERED | Docs establish semantics, tests lock red lines, behavioral validation checks runtime surfaces.
REQ | MEM-03 | Prevent session context from becoming cross-case durable state, reviewed precedent, long-term preference memory, evidence/business/approval/action/replay authority | 46-01, 46-02, 46-03 | COVERED | All plans include MEM-03 and test/docs coverage.
RESEARCH | no migration | Preserve session_memories table identity unless a concrete defect is proven | 46-01, 46-02 | COVERED | Docs and static tests forbid destructive schema change.
RESEARCH | stale docs | Correct non-normative search_case_memory session-derived wording | 46-01, 46-02 | COVERED | Implementation map and architecture overview are reconciled, then static-tested.
RESEARCH | prompt hints | Keep policy/business/session hints contextual only | 46-01, 46-02, 46-03 | COVERED | Contract text, static scans, and behavioral tests cover hints.
RESEARCH | validation architecture | Create tests/memory/test_phase46_session_context_alignment.py | 46-02 | COVERED | Dedicated static test plan.
CONTEXT | D-46-01 | Preserve session_memories table identity; no migration by default | 46-01, 46-02 | COVERED | Contract text and schema/static tests.
CONTEXT | D-46-02 | Session contents stay narrow; disallowed authority contents named | 46-01, 46-02, 46-03 | COVERED | Exact allowed/disallowed terms in docs and tests.
CONTEXT | D-46-03 | Prompt hints are not authority | 46-01, 46-02, 46-03 | COVERED | Contract wording and hint boundary tests.
CONTEXT | D-46-04 | Session memory is not CWC fallback | 46-01, 46-02, 46-03 | COVERED | CWC fallback wording and tests.
CONTEXT | D-46-05 | Session memory is not reviewed precedent | 46-01, 46-02, 46-03 | COVERED | Docs and executor/static tests.
CONTEXT | D-46-06 | No generic long-term automatic sedimentation | 46-01, 46-03 | COVERED | Docs and write-service behavioral tests.
CONTEXT | D-46-07 | Keep Phase 45 CWC and ReAct boundaries intact | 46-01, 46-02, 46-03 | COVERED | Static tests preserve no CWC fallback and no ReAct/global slot writer reopening.
CONTEXT | D-46-08 | Approved pytest entrypoint only | 46-01, 46-02, 46-03 | COVERED | Every automated command starts with `UV_CACHE_DIR=/tmp/uv-cache uv run pytest`; Plan 46-02 adds a guard.
DEFERRED | Phase 47 | DEFER-2 case precedent generation | none | EXCLUDED | Named defer only; not implemented.
DEFERRED | Phase 48 | DEFER-3 explicit tenant preference memory | none | EXCLUDED | Named defer only; not implemented.
</source_audit>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add normative post-CWC session context boundary</name>
  <read_first>
    - docs/contract-spec.md Section 13.2 Session memory
    - docs/contract-spec.md Section 13.4 and 13.4a reviewed case memory / Case Working Context
    - docs/contract-spec.md storage rows for `session_memories`, `case_memories`, `long_term_memories`, `thread_case_links`
    - .planning/phases/46-session-context-repositioning/46-CONTEXT.md decisions D-46-01 through D-46-08
    - .planning/phases/45-memory-lifecycle-wiring-for-case-working-context/45-VERIFICATION.md
  </read_first>
  <files>docs/contract-spec.md</files>
  <action>
    Update `docs/contract-spec.md` Section 13.2 to state the Phase 46 target semantics using these exact concepts: after Case Working Context exists, `session_memories` remains same-thread temporary conversational context only; allowed contents are slot continuity, last intent, lightweight same-thread summary, unresolved questions, same-thread recent-message or rolling-summary prompt context, prompt-safe tool summaries, and prompt-safe refs/hints; disallowed contents are cross-case durable working state, CWC fallback, reviewed precedent, durable tenant/user/merchant preference memory, policy body text, policy evidence authority, business fact authority, risk decisions, approval decisions, action authorization, action outcome truth, replay truth, and sensitive raw PII. Add explicit wording that `policy_topic_hints`, `prior_policy_mention_refs`, `last_business_context_refs`, and tool summary refs are contextual hints only: they must not create `EvidenceRefV1`, must not satisfy policy or approval evidence requirements, must not replace fresh business tool reads, and must not be cited as current business facts. Preserve storage wording that `session_memories` is tenant/user/thread scoped with no `case_id`; do not add a migration note implying rename/drop/retype.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_phase45_contract_alignment.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "same-thread temporary conversational context" docs/contract-spec.md` matches.
    - `grep -n "policy_topic_hints" docs/contract-spec.md` matches in the session-memory section.
    - `grep -n "must not create.*EvidenceRefV1" docs/contract-spec.md` or equivalent wording matches.
    - `grep -n "session_memories.*tenant.*user.*thread" docs/contract-spec.md` still matches storage or session-memory scope wording.
    - No new migration file under `src/db/migrations/versions/` is created by this task.
  </acceptance_criteria>
  <done>`docs/contract-spec.md` normatively implements D-46-01, D-46-02, D-46-03, D-46-04, D-46-05, D-46-06, and D-46-07 without destructive schema wording.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile stale implementation docs and defer ledger wording</name>
  <read_first>
    - docs/current-implementation-map.md memory table and risk sections
    - docs/architecture-overview.md memory architecture section
    - .planning/MEMORY-REDESIGN-DECISIONS.md DEFER-1, DEFER-2, DEFER-3 block
    - src/tools/executors/memory.py reviewed case memory executor wiring
    - src/memory/search.py LegacySessionPrecedentSearchService docstring
  </read_first>
  <files>docs/current-implementation-map.md, docs/architecture-overview.md, .planning/MEMORY-REDESIGN-DECISIONS.md</files>
  <action>
    In `docs/current-implementation-map.md`, replace stale production wording that says `search_case_memory` wraps session precedent search with the current implementation fact: planner-facing `search_case_memory` is served by `MemoryToolExecutor -> CaseMemoryService.retrieve_reviewed(...)` over reviewed case memory, while `LegacySessionPrecedentSearchService` remains a legacy/debug-only read-only projection over `session_memories` and must not back the planner-facing tool. In `docs/architecture-overview.md`, update the memory bullets so session memory is same-thread continuity only and `search_case_memory` is reviewed case memory, not session-derived precedent. In `.planning/MEMORY-REDESIGN-DECISIONS.md`, add a Phase 46 planning trace that DEFER-1 is being locked as session context repositioning, while `DEFER-2 -> Phase 47` and `DEFER-3 -> Phase 48` remain named and unimplemented by Phase 46. Do not mark closed-case precedent generation, metadata-first precedent retrieval, or explicit preference memory as delivered.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_phase45_contract_alignment.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "CaseMemoryService" docs/current-implementation-map.md` matches.
    - `grep -n "LegacySessionPrecedentSearchService" docs/current-implementation-map.md docs/architecture-overview.md` matches with debug-only or legacy-only wording.
    - `grep -n "session-derived precedent" docs/current-implementation-map.md docs/architecture-overview.md` has no production-tool claim; any remaining occurrence is explicitly historical, legacy, or debug-only.
    - `grep -n "DEFER-2 -> Phase 47" .planning/MEMORY-REDESIGN-DECISIONS.md` matches.
    - `grep -n "DEFER-3 -> Phase 48" .planning/MEMORY-REDESIGN-DECISIONS.md` matches.
  </acceptance_criteria>
  <done>Non-normative docs no longer conflict with current code, and Phase 47 / Phase 48 defers remain visible by name.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> implementers | Normative and non-normative docs guide later implementation and must not grant session context authority. |
| session context -> authority services | Session hints may cross into prompts, but policy evidence, business facts, approval/action, CWC, and replay remain separate authorities. |
| planning ledger -> later phases | DEFER-2 and DEFER-3 must stay named without being implemented in Phase 46. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | I | `session_memories` scope docs | mitigate | State tenant/user/thread scope and no cross-case durable state; Plan 46-02 adds schema/static tests. |
| T-46-02 | T/E | session hints as policy evidence | mitigate | State hints must not create `EvidenceRefV1` or satisfy policy/approval evidence; Plan 46-02/03 test this. |
| T-46-03 | T | session hints as business facts | mitigate | State `last_business_context_refs` and tool summary refs are contextual pointers only and cannot replace fresh tool reads. |
| T-46-04 | R/T | session memory as reviewed precedent | mitigate | Correct docs so production `search_case_memory` is reviewed case memory; legacy session search remains debug-only. |
| T-46-05 | T/R | session memory as CWC fallback | mitigate | State CWC identity/read/write remains Phase 45 lifecycle-owned and cannot be backfilled from raw session context. |
| T-46-06 | E/R | session memory as approval/action/replay authority | mitigate | Contract text names approval decisions, action authorization/outcome truth, and replay truth as disallowed session contents. |
</threat_model>

<verification>
Task-level verification uses the existing Phase 45 red-line suite until Plan 46-02 creates the Phase 46 alignment suite:
```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_phase45_contract_alignment.py -x -q
```
</verification>

<success_criteria>
- `docs/contract-spec.md` gives `session_memories` a post-CWC same-thread temporary context role and names disallowed authority categories.
- `docs/current-implementation-map.md` and `docs/architecture-overview.md` no longer describe the planner-facing `search_case_memory` as session-derived.
- `.planning/MEMORY-REDESIGN-DECISIONS.md` carries `DEFER-2 -> Phase 47` and `DEFER-3 -> Phase 48` by name.
- No schema migration, table rename, column drop/retype, Phase 47 precedent generation, Phase 48 preference memory, CWC fallback, or ReAct/global slot writer is planned or implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/46-session-context-repositioning/46-01-SUMMARY.md`.
</output>
