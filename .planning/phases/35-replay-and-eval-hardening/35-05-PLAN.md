---
phase: 35-replay-and-eval-hardening
plan: 35-05
type: execute
wave: 3
depends_on:
  - 35-01
files_modified:
  - eval/replay/release-gate.v1.json
  - eval/replay/release-smoke-cases.v1.json
  - eval/replay/monitoring-gate.v1.json
  - tests/eval/test_phase35_release_monitoring_manifests.py
  - docs/evaluation.md
autonomous: true
requirements:
  - APF-18
user_setup: []
must_haves:
  truths:
    - "Release and monitoring gates exist as Phase 35 artifacts and are discoverable by future agents."
    - "Release gate status can be `statistical_gate_not_demonstrated` without blocking Phase 35 on release-scale samples."
    - "Monitoring gate status can be `pending`, `not_applicable`, or `sample_only` without requiring production telemetry."
  artifacts:
    - path: "eval/replay/release-gate.v1.json"
      provides: "Non-blocking release readiness manifest"
      contains: "statistical_gate_not_demonstrated"
    - path: "eval/replay/release-smoke-cases.v1.json"
      provides: "Limited release smoke cases for Phase 35 coverage gaps"
      contains: "phase35_replay_release_smoke_cases.v1"
    - path: "eval/replay/monitoring-gate.v1.json"
      provides: "Monitoring schema/status manifest"
      contains: "replay_completeness"
    - path: "tests/eval/test_phase35_release_monitoring_manifests.py"
      provides: "Manifest format and non-blocking status tests"
      contains: "sample_only"
    - path: "docs/evaluation.md"
      provides: "Discoverability for Phase 35 replay/eval artifacts"
      contains: "eval/replay/release-gate.v1.json"
  key_links:
    - from: "eval/replay/release-gate.v1.json"
      to: "eval/replay/phase35-coverage-matrix.v1.json"
      via: "coverage manifest path and hash"
      pattern: "coverage_manifest_hash"
    - from: "docs/evaluation.md"
      to: "eval/replay/monitoring-gate.v1.json"
      via: "documented artifact path"
      pattern: "monitoring-gate.v1.json"
---

<objective>
Artifactize release and monitoring gates without making missing release-scale data or production telemetry a Phase 35 blocker.

Purpose: implement D-09, D-11, D-12, and D-19. This plan depends only on 35-01 because release/monitoring manifests reference the coverage matrix; final cross-check with the dev-contract manifest happens in 35-06 after 35-04.
Output: release gate manifest, limited release smoke case artifact, monitoring gate manifest, tests for status semantics, and evaluation docs update.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-replay-and-eval-hardening/35-CONTEXT.md
@.planning/phases/35-replay-and-eval-hardening/35-RESEARCH.md
@.planning/phases/35-replay-and-eval-hardening/35-PATTERNS.md
@docs/eval-test-plan.md
@docs/evaluation.md
@eval/replay/phase35-coverage-matrix.v1.json
@eval/intent/m6-statistical-gate.v1.json
@scripts/eval_all.py
@scripts/eval_agent.py

<interfaces>
Existing release gate status pattern:
```json
{
  "gate_scope": "m6_release",
  "coverage_status": "incomplete",
  "default_gate_status": "statistical_gate_not_demonstrated"
}
```

Phase 35 monitoring status values:
```python
ALLOWED_MONITORING_STATUSES = {"pending", "not_applicable", "sample_only"}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add release and monitoring manifests</name>
  <files>eval/replay/release-gate.v1.json, eval/replay/release-smoke-cases.v1.json, eval/replay/monitoring-gate.v1.json, tests/eval/test_phase35_release_monitoring_manifests.py</files>
  <read_first>
    - AGENTS.md
    - .planning/phases/35-replay-and-eval-hardening/35-CONTEXT.md
    - .planning/phases/35-replay-and-eval-hardening/35-PATTERNS.md
    - docs/eval-test-plan.md sections 20.0, 20.3, 20.4
    - eval/replay/phase35-coverage-matrix.v1.json
    - eval/intent/m6-statistical-gate.v1.json
    - tests/agent/test_intent_manifest.py
  </read_first>
  <behavior>
    - Test release manifest includes dataset version/hash, coverage manifest hash, command entrypoint, metrics, gate status, and coverage/sample gaps.
    - Test release smoke cases include one limited smoke case each for `intent_hard_negatives`, `rag_claim_support`, and `approval_action_safety`.
    - Test release manifest uses `default_gate_status="statistical_gate_not_demonstrated"` when sample size is insufficient.
    - Test monitoring manifest defines replay completeness, drift, false-negative trend, tool deny reasons, RAG no-evidence trend, and memory write quality metrics.
    - Test monitoring statuses are limited to `pending`, `not_applicable`, and `sample_only`.
  </behavior>
  <action>
    Create `eval/replay/release-smoke-cases.v1.json` with `schema_version="phase35_replay_release_smoke_cases.v1"`, `dataset_version="phase35-replay-release-smoke.v1"`, `case_count=3`, and exactly one smoke case for each release area: `intent_hard_negatives`, `rag_claim_support`, and `approval_action_safety`. Each case must include `case_id`, `release_area`, `fixture_kind`, `expected_gate_level="release"`, `expected_status="smoke_only"`, `source_boundary`, and `coverage_gap`. These are limited smoke references only; they must not be presented as release-scale statistical evidence.

    Create `eval/replay/release-gate.v1.json` with `schema_version="phase35_replay_release_gate.v1"`, `gate_level="release"`, `blocking="release_not_phase35_exit"`, `failure_impact="block_replay_eval_release_not_phase_35_exit"`, `dataset_version="phase35-replay-release-smoke.v1"`, `dataset_path="eval/replay/release-smoke-cases.v1.json"`, `dataset_hash` computed from the smoke case artifact, `dataset_size=3`, `coverage_manifest_path="eval/replay/phase35-coverage-matrix.v1.json"`, `coverage_manifest_hash` computed from the matrix file, `command_entrypoint="UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_phase35_release_monitoring_manifests.py -q --tb=short"`, `default_gate_status="statistical_gate_not_demonstrated"`, and `coverage_status="incomplete"`.

    Include release `metrics` entries for `intent_hard_negatives`, `rag_claim_support`, and `approval_action_safety` with `gate_status="statistical_gate_not_demonstrated"`, `required_min_n` values from the manifest or eval plan, `smoke_n=1`, `statistical_n=0`, `smoke_case_ids` pointing to the matching smoke case, and gap reasons `release_scale_sample_size_not_demonstrated` or `coverage_gap_recorded`. This records D-19 manifests and limited smoke cases without expanding broad statistical datasets or pretending the smoke cases satisfy release thresholds.

    Create `eval/replay/monitoring-gate.v1.json` with `schema_version="phase35_replay_monitoring_gate.v1"`, `gate_level="monitoring"`, `blocking="monitoring_not_phase35_exit"`, `failure_impact="trigger_review_or_degrade_not_phase_35_exit"`, `command_entrypoint="UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_phase35_release_monitoring_manifests.py -q --tb=short"`, and metric definitions for exact metric ids: `replay_completeness`, `drift`, `false_negative_trend`, `tool_deny_reasons`, `rag_no_evidence_trend`, and `memory_write_quality`.

    For monitoring metrics use only these status values: `pending`, `not_applicable`, and `sample_only`. Missing production telemetry must be represented as `pending` or `not_applicable`; deterministic sample-only checks must use `sample_only` per D-12.

    Create `tests/eval/test_phase35_release_monitoring_manifests.py` with JSON schema/status tests. The tests must validate the smoke case artifact, verify `release-gate.v1.json.dataset_hash` against `release-smoke-cases.v1.json`, assert exactly the three release smoke areas are present, and assert every release metric has `smoke_n=1`, `statistical_n=0`, and `gate_status="statistical_gate_not_demonstrated"`. The tests may compute and compare matrix hash values directly; no production telemetry, external provider, or live sample corpus is required.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_phase35_release_monitoring_manifests.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check tests/eval/test_phase35_release_monitoring_manifests.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "phase35_replay_release_gate.v1|statistical_gate_not_demonstrated|release_scale_sample_size_not_demonstrated" eval/replay/release-gate.v1.json tests/eval/test_phase35_release_monitoring_manifests.py` returns matches.
    - `rg -n "phase35_replay_release_smoke_cases.v1|intent_hard_negatives|rag_claim_support|approval_action_safety" eval/replay/release-smoke-cases.v1.json tests/eval/test_phase35_release_monitoring_manifests.py` returns matches.
    - `rg -n "phase35_replay_monitoring_gate.v1|replay_completeness|false_negative_trend|tool_deny_reasons|sample_only" eval/replay/monitoring-gate.v1.json tests/eval/test_phase35_release_monitoring_manifests.py` returns matches.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_phase35_release_monitoring_manifests.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>Release and monitoring manifests exist with limited smoke cases, non-blocking status semantics, and test-backed format validation.</done>
</task>

<task type="auto">
  <name>Task 2: Document Phase 35 replay/eval artifact discovery</name>
  <files>docs/evaluation.md</files>
  <read_first>
    - AGENTS.md
    - docs/evaluation.md
    - docs/eval-test-plan.md sections 20.0, 20.3, 20.4
    - eval/replay/phase35-coverage-matrix.v1.json
    - eval/replay/release-gate.v1.json
    - eval/replay/monitoring-gate.v1.json
  </read_first>
  <action>
    Update `docs/evaluation.md` with a concise "Phase 35 Replay/Eval Gates" section.

    The section must list these exact artifact paths: `eval/replay/phase35-coverage-matrix.v1.json`, `eval/replay/dev-contract-manifest.v1.json`, `eval/replay/release-gate.v1.json`, `eval/replay/release-smoke-cases.v1.json`, and `eval/replay/monitoring-gate.v1.json`.

    Document these exact command entrypoints:
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay/test_phase35_coverage_matrix.py -q --tb=short`
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_phase35_release_monitoring_manifests.py -q --tb=short`
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_phase35_replay_eval_gates.py tests/architecture/test_phase35_replay_eval_boundaries.py -q --tb=short`

    State explicitly that `dev-contract` gates block Phase 35, while release `statistical_gate_not_demonstrated` and monitoring `pending`, `not_applicable`, or `sample_only` statuses do not block Phase 35 unless a deterministic forbidden-behavior regression is exposed. Document that Phase 35 includes limited release smoke cases for `intent_hard_negatives`, `rag_claim_support`, and `approval_action_safety`, but does not claim production-level sample size or production telemetry has been demonstrated.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_phase35_release_monitoring_manifests.py -q --tb=short</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Phase 35 Replay/Eval Gates|eval/replay/release-gate.v1.json|release-smoke-cases.v1.json|statistical_gate_not_demonstrated|sample_only" docs/evaluation.md` returns matches.
    - `docs/evaluation.md` does not document unscoped pytest entrypoints.
    - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_phase35_release_monitoring_manifests.py -q --tb=short` exits 0.
  </acceptance_criteria>
  <done>Evaluation docs make Phase 35 replay/eval artifacts and non-blocking release/monitoring semantics discoverable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release manifest -> release claims | Release artifacts must not imply production readiness when sample size is absent. |
| monitoring manifest -> operational claims | Monitoring schema exists without claiming production telemetry has been observed. |
| documentation -> future agents | Docs must point to concrete artifacts and approved commands without widening Phase 35 gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-05-01 | Repudiation | `eval/replay/release-gate.v1.json` | mitigate | Record dataset hash, coverage hash, command entrypoint, metric statuses, and sample gaps. |
| T-35-05-02 | Tampering | `eval/replay/monitoring-gate.v1.json` | mitigate | Validate exact metric ids and allowed status values through pytest. |
| T-35-05-03 | Information Disclosure | release/monitoring artifacts | accept | Artifacts contain only aggregate metric definitions, hashes, paths, and status codes; no raw prompt/tool/PII/action payloads. |
</threat_model>

<verification>
Run after both tasks:

```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_phase35_release_monitoring_manifests.py -q --tb=short
UV_CACHE_DIR=/tmp/uv-cache uv run ruff check tests/eval/test_phase35_release_monitoring_manifests.py
git diff --check
```
</verification>

<success_criteria>
- Release and monitoring manifests exist under `eval/replay/`.
- Release gates report sample/coverage gaps without blocking Phase 35 on missing statistical sample size.
- Monitoring gates define metric/report schemas without requiring production telemetry.
- `docs/evaluation.md` documents artifact paths and approved command entrypoints.
</success_criteria>

<output>
After completion, create `.planning/phases/35-replay-and-eval-hardening/35-05-SUMMARY.md`.
</output>
