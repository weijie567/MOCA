---
phase: 49-investigate-bounded-react-loop-migration
plan: "02"
type: execute
wave: 2
depends_on: [49-01]
files_modified:
  - src/agent/nodes/investigate.py
  - src/agent/nodes/investigate_planner.py
  - src/tools/projection.py
  - tests/agent/test_nodes/test_investigate.py
autonomous: true
requirements: [GAD-01-IMPL]
requirements_addressed: [GAD-01-IMPL]
must_haves:
  truths:
    - "The main investigate loop is planner-driven: one validated planner-selected read/retrieval tool per iteration or stop."
    - "Loop-local discovered slots are local scratchpad state only and never graph state writers."
    - "Discovered identifiers are extracted only from projected structured observations, not raw text regex."
    - "max_iterations, deadline_at, max_attempts, duplicate tool+args, and platform errors terminate safely."
  artifacts:
    - path: "src/agent/nodes/investigate.py"
      provides: "bounded runtime loop, loop context, local slot merge, termination semantics"
      contains: "termination_reason"
    - path: "src/tools/projection.py"
      provides: "optional prompt-safe structured identifier hints for loop-local discovery"
      contains: "relation_hints"
    - path: "tests/agent/test_nodes/test_investigate.py"
      provides: "order-to-ticket chain, no active_slots mutation, termination tests"
      contains: "discovered"
  key_links:
    - from: "src/tools/projection.py"
      to: "src/agent/nodes/investigate.py"
      via: "projected observation identifiers feed loop-local scratchpad"
      pattern: "prompt_projection"
    - from: "src/agent/nodes/investigate.py"
      to: "src/agent/nodes/investigate.py"
      via: "local `_case_slots` merge includes discovered slots without state writes"
      pattern: "_case_slots"
---

<objective>
Convert the validated single-step planner shell into the actual bounded investigate loop and implement loop-local discovered slots.

Purpose: make chained investigation possible, for example order lookup discovers a ticket identifier that the next iteration can use, without creating new memory/session/slot writers.
Output: local loop context, discovered-slot extraction from projected structured observations, bounded termination behavior, and focused chain/no-mutation tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-investigate-bounded-react-loop-migration/49-CONTEXT.md
@.planning/phases/49-investigate-bounded-react-loop-migration/49-01-PLAN.md
@docs/contract-spec.md
@src/agent/nodes/investigate.py
@src/tools/projection.py
@src/tools/contracts.py
@tests/agent/test_nodes/test_investigate.py

<source_audit>
SOURCE | ID | Requirement / Decision | Plan | Status
--- | --- | --- | --- | ---
CONTRACT | 9.4 | Bounded loop uses max_iterations/deadline_at/max_attempts | 49-02 | COVERED
DEFERRED | GAD-01 | observation->slot option A is loop-local | 49-02 | COVERED
USER | Tests 7-11 | order->ticket chain, no active_slots write, max/deadline/max_attempts | 49-02 | COVERED
CONTEXT | No raw regex | Discovery from projection/structured refs only | 49-02 | COVERED
</source_audit>

<file_scope>
Expected files:
- `src/agent/nodes/investigate.py`
- Optional `src/agent/nodes/investigate_planner.py`
- `src/tools/projection.py` only if the current projection does not expose enough safe structured identifiers for discovery
- `tests/agent/test_nodes/test_investigate.py`

Do not modify:
- `docs/contract-spec.md`
- `src/agent/state.py` field ownership or `active_slots` writer semantics
- `src/memory/**`
- intent classifier / contextual intent resolver / TaskPlan code
- risk, approval, action nodes/executors
</file_scope>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce loop-local context and slot merge helper</name>
  <read_first>
    - src/agent/nodes/investigate.py `_case_slots`, current loop variables, max/deadline/attempt handling
    - tests/agent/test_nodes/test_investigate.py current state fixtures and active_slots assertions
    - docs/contract-spec.md AgentState field registry entry for `active_slots`
  </read_first>
  <files>src/agent/nodes/investigate.py, src/agent/nodes/investigate_planner.py, tests/agent/test_nodes/test_investigate.py</files>
  <behavior>
    - Test 1: base slots still read from existing `extracted_slots` and `active_slots`.
    - Test 2: a discovered loop-local slot is visible to the next planner input and fallback slot resolver.
    - Test 3: discovered slots do not mutate `state["active_slots"]`, `state["extracted_slots"]`, or `state["candidate_slots"]`.
  </behavior>
  <action>
    Add an explicit local loop context. The implementation can use a dataclass or dict, but it must keep these concepts separate:
    ```python
    InvestigateLoopContext(
        base_slots=_case_slots(state),
        discovered_slots={},
        observations=[],
        attempted_keys=set(),
        attempt_count_by_key={},
        iteration=1,
        deadline_at=deadline_at,
    )
    ```

    Add a helper such as `_case_slots_for_loop(state, discovered_slots)` or `_merge_loop_slots(base_slots, discovered_slots)` that returns a local merged view. It may read `state["active_slots"]` and `state["extracted_slots"]`, but it must never write either field.

    Planner input and deterministic fallback should use the merged local slot view after discovery. The graph state returned from `investigate` must not include discovered slots except as optional trace/debug metadata that is not a writer surface and cannot be consumed by memory/session writers.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_investigate.py -k "slot or discovered or active_slots" -q</automated>
  </verify>
  <acceptance_criteria>
    - A test deep-copies `state["active_slots"]` before `investigate(...)` and asserts equality after the run.
    - `rg -n "active_slots.*=" src/agent/nodes/investigate.py` returns no investigate writer.
    - Local discovered slots are present in the next planner input captured by a fake planner.
  </acceptance_criteria>
  <done>Loop-local discovered slots exist as scratchpad only and feed later iterations without graph state mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Discover identifiers only from projected structured observations</name>
  <read_first>
    - src/tools/projection.py projection allowlists and forbidden raw key stripping
    - src/agent/nodes/investigate.py `_accumulate_tool_result`, `_accumulate_result`
    - tests/agent/test_nodes/test_investigate.py projection raw-stripping tests
  </read_first>
  <files>src/agent/nodes/investigate.py, src/tools/projection.py, tests/agent/test_nodes/test_investigate.py</files>
  <behavior>
    - Test 1: `get_order` projected output with a safe ticket identifier hint adds `ticket_id` to loop-local discovered slots.
    - Test 2: discovered `ticket_id` enables next iteration `get_ticket`.
    - Test 3: prompt-injection text inside policy/user/tool raw content does not become a slot.
    - Test 4: logistics tracking numbers, refund case, ticket, order, and merchant identifiers are discovered only when present in prompt-safe structured refs.
  </behavior>
  <action>
    Add identifier discovery from the projected result object that is already safe for planner use. Accept only structured fields from:
    - normalized `ToolResultV2.data` fields that the descriptor output schema and projection already expose safely;
    - `prompt_projection` fields;
    - explicit safe refs or `relation_hints` if added by `ToolResultProjector`.

    Supported loop-local slot keys:
    - `order_id`
    - `order_no`
    - `refund_case_id`
    - `refund_case_no`
    - `ticket_id`
    - `tracking_no`
    - `logistics_ref` only if it is explicitly mapped to `tracking_no` before calling `get_logistics`
    - `merchant_id` or `merchant_risk_ref` only when already available in safe projection

    Do not scrape identifiers from `text_for_prompt`, policy body text, user text, raw adapter payload, debug text, or arbitrary strings. If projection lacks a safe way to expose a relation, extend `ToolResultProjector` with a narrow structured safe field such as `relation_hints` and add raw-stripping tests for it.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_investigate.py -k "order or ticket or projection or prompt_injection" -q</automated>
  </verify>
  <acceptance_criteria>
    - `order_no -> get_order -> discovered ticket_id -> get_ticket` is covered by a failing-then-passing test.
    - The test proves `ticket_id` came from projection/structured refs, not regex over raw text.
    - Existing projection forbidden-key tests remain green.
  </acceptance_criteria>
  <done>Observation-to-slot回流 supports chained investigation while preserving the projection boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce bounded-loop termination and duplicate/attempt guards</name>
  <read_first>
    - src/agent/nodes/investigate.py current termination, deadline, attempts, duplicate key handling
    - docs/contract-spec.md section 9.4 termination reasons
    - tests/agent/test_nodes/test_investigate.py max iteration and status tests
  </read_first>
  <files>src/agent/nodes/investigate.py, tests/agent/test_nodes/test_investigate.py</files>
  <behavior>
    - Test 1: planner stop with `enough_evidence` terminates cleanly.
    - Test 2: no useful tools / duplicate same tool+args terminates as `no_more_useful_tools`.
    - Test 3: max iterations terminates with `termination_reason="max_iterations_reached"`.
    - Test 4: deadline exceeded terminates safely without throwing.
    - Test 5: max attempts for the same tool+args prevents unbounded retry.
    - Test 6: unrecoverable platform error terminates with `unrecoverable_error` only when no fallback/skip can safely continue.
  </behavior>
  <action>
    Ensure the runtime loop uses exactly one planner action per iteration. Keep these guards active on both planner and fallback paths:
    - `max_iterations`;
    - `deadline_at`;
    - `max_attempts` per normalized tool+args key;
    - duplicate same tool+same args attempt detection.

    Termination reason values must remain within the §9.4 enum. Reaching `max_iterations` may leave lifecycle/status completed, but `termination_reason` must be exactly `max_iterations_reached`. Do not downgrade `retrieval_status` to insufficient solely because the loop was truncated; calculate retrieval status from actual accumulated evidence/observations.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_investigate.py -k "max_iterations or deadline or max_attempts or no_more_useful_tools or retrieval_status" -q</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_investigate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Existing `max_iterations_reached_does_not_degrade_retrieval_status` style coverage remains true.
    - Deadline tests assert safe termination rather than exception.
    - Duplicate attempt tests assert no infinite loop and no repeated platform dispatch beyond the configured max.
  </acceptance_criteria>
  <done>The planner-driven loop is bounded, auditable, and cannot spin indefinitely.</done>
</task>

</tasks>

<rollback_fallback_behavior>
- If loop-local discovery creates regressions, disable discovery extraction while retaining the 49-01 planner validation/fallback shell.
- If the planner cannot produce a useful next step, use deterministic fallback only after the planner failure path; do not keep deterministic fallback as a parallel primary controller.
- If deadline/max/attempt guards trigger, return the safest current accumulated investigation result and explicit `termination_reason`.
</rollback_fallback_behavior>

<no_regression_checks>
- `active_slots`, `extracted_slots`, and `candidate_slots` are not mutated by `investigate`.
- Memory Phase 44-48 CWC/reviewed/preference writers are not imported or modified.
- Intent TaskPlan / contextual intent resolver contracts are untouched.
- Risk/approval/action nodes are not modified and cannot be reached from inside the investigate loop.
</no_regression_checks>

<minimal_test_commands>
```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_investigate.py -q
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_receive_request.py tests/agent/test_nodes/test_classify_intent.py tests/agent/test_intent_task_plan.py -q
```
</minimal_test_commands>
