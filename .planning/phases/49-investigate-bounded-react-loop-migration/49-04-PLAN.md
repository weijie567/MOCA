---
phase: 49-investigate-bounded-react-loop-migration
plan: "04"
type: execute
wave: 4
depends_on: [49-03]
files_modified:
  - tests/agent/test_graph.py
  - tests/agent/test_nodes/test_investigate.py
  - tests/agent/test_nodes/test_classify_intent.py
  - tests/agent/test_intent_task_plan.py
  - tests/agent/test_reviewed_memory_context_retrieve.py
  - tests/agent/test_memory_evidence_boundary.py
  - tests/memory/test_phase48_1_memory_compat_alignment.py
  - tests/memory/test_phase48_long_term_preference_alignment.py
  - .planning/DEFERRED-DECISIONS.md
  - .planning/ARCHITECTURE-DEBT.md
  - .planning/phases/49-investigate-bounded-react-loop-migration/49-04-SUMMARY.md
autonomous: true
requirements: [GAD-01-IMPL]
requirements_addressed: [GAD-01-IMPL]
must_haves:
  truths:
    - "Graph-level behavior proves ReAct stays inside investigate only; outer routers remain deterministic."
    - "Policy-only path does not require business context."
    - "Investigate may propose facts/context but cannot authorize action."
    - "Intent Phase 43, memory Phase 44-48.1, active_slots, and risk/approval/action fail-closed boundaries remain unchanged."
    - "GAD-01 status and architecture debt are updated only after implementation verification."
  artifacts:
    - path: "tests/agent/test_graph.py"
      provides: "graph-level route and downstream gate regressions"
      contains: "route_after_investigate"
    - path: "tests/agent/test_nodes/test_investigate.py"
      provides: "final investigate safety and 8-tool checks"
      contains: "bounded"
    - path: ".planning/DEFERRED-DECISIONS.md"
      provides: "GAD-01 implementation status update"
      contains: "GAD-01"
    - path: ".planning/ARCHITECTURE-DEBT.md"
      provides: "architecture debt closure record for legacy deterministic investigate"
      contains: "investigate"
  key_links:
    - from: "src/agent/graph.py"
      to: "tests/agent/test_graph.py"
      via: "outer graph routers remain deterministic"
      pattern: "route_after_investigate"
    - from: "src/agent/nodes/investigate.py"
      to: ".planning/DEFERRED-DECISIONS.md"
      via: "GAD-01 implementation status"
      pattern: "IMPLEMENTED"
---

<objective>
Run end-to-end behavior, safety, memory/intent/risk regressions, and planning-ledger closeout for Phase 49.

Purpose: prove that bounded ReAct changed only investigate internals. The graph around investigate, intent contracts, memory writers, active slot ownership, and downstream fail-closed gates must remain intact.
Output: graph/E2E safety tests, regression commands, GAD-01 status update, architecture debt record, and Phase 49 completion summary.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-investigate-bounded-react-loop-migration/49-CONTEXT.md
@.planning/phases/49-investigate-bounded-react-loop-migration/49-01-PLAN.md
@.planning/phases/49-investigate-bounded-react-loop-migration/49-02-PLAN.md
@.planning/phases/49-investigate-bounded-react-loop-migration/49-03-PLAN.md
@docs/contract-spec.md
@src/agent/graph.py
@src/agent/nodes/investigate.py
@src/agent/nodes/classify_intent.py
@src/agent/nodes/assess_risk_and_approval.py
@src/agent/nodes/approval_gate.py
@src/agent/nodes/action_draft.py
@tests/agent/test_graph.py
@tests/agent/test_nodes/test_investigate.py
@tests/agent/test_nodes/test_classify_intent.py
@tests/agent/test_intent_task_plan.py
@tests/agent/test_memory_evidence_boundary.py
@tests/agent/test_reviewed_memory_context_retrieve.py

<source_audit>
SOURCE | ID | Requirement / Decision | Plan | Status
--- | --- | --- | --- | ---
CONTRACT | 9.5 | Routers remain deterministic and side-effect-free | 49-04 | COVERED
CONTEXT | Three trust boundaries | ReAct only inside investigate, downstream gates fail closed | 49-04 | COVERED
USER | Tests 16-18 | memory, intent, risk/approval/action regressions | 49-04 | COVERED
AGENTS | Architecture debt rule | Tool/RAG subsystem debt fix recorded in architecture debt ledger | 49-04 | COVERED
</source_audit>

<file_scope>
Expected files:
- Targeted graph/regression tests under `tests/agent/`, `tests/memory/`, `tests/replay/`, and `tests/tools/` as needed
- `.planning/DEFERRED-DECISIONS.md` GAD-01 status after verification
- `.planning/ARCHITECTURE-DEBT.md` architecture-debt closure after verification
- `.planning/LOCAL-VALIDATION-ISSUES.md` only if local validation/debugging exposes a real issue
- `49-04-SUMMARY.md` and final validation notes

Do not modify:
- `docs/contract-spec.md`, unless a true spec blocker is found; stop and report instead
- Intent contract/schema code
- Memory Phase 44-48.1 schema/service/repository/writer code
- `active_slots` writer/field registry
- Risk/approval/action executor behavior
- Graph router topology except tests may assert it remains unchanged
</file_scope>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add graph-level behavior tests for investigate ReAct scope</name>
  <read_first>
    - src/agent/graph.py route functions and node chain
    - tests/agent/test_graph.py graph route and compiled-edge patterns
    - tests/agent/test_nodes/test_investigate.py fake planner/platform helpers
    - docs/contract-spec.md section 9.5 router table
  </read_first>
  <files>tests/agent/test_graph.py, tests/agent/test_nodes/test_investigate.py</files>
  <behavior>
    - Test 1: order query can perform order lookup then ticket lookup through loop-local slot discovery.
    - Test 2: refund case + policy evidence path accumulates business and policy context without writing `evidence_refs`.
    - Test 3: no evidence / no useful tools routes to insufficient evidence or final fail-closed behavior through existing downstream gates.
    - Test 4: policy-only path does not require business context and does not force business tool calls.
    - Test 5: proposed action still goes through claim/risk/approval/action draft chain; investigate cannot authorize it.
  </behavior>
  <action>
    Add graph-level tests that use fake planner/platform seams and existing graph test patterns. These should prove outer graph behavior, not re-test every planner validation detail from 49-01 to 49-03.

    Explicitly assert:
    - ReAct loop is inside `investigate` only;
    - `route_after_investigate` and downstream routers still determine graph movement;
    - planner output requesting refund/action/write/approval bypass is ignored or rejected at investigate boundary;
    - `policy_qa` or policy-only query can search policy without requiring order/refund/ticket context.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_graph.py tests/agent/test_nodes/test_investigate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Graph route tests still assert deterministic router edge sets.
    - E2E-style fake graph tests prove chained investigation but no downstream gate bypass.
    - Policy-only path test proves business context is optional.
  </acceptance_criteria>
  <done>Graph behavior confirms ReAct was inserted into investigate internals only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run intent, memory, active slot, and fail-closed safety regressions</name>
  <read_first>
    - tests/agent/test_nodes/test_classify_intent.py
    - tests/agent/test_intent_task_plan.py
    - tests/agent/test_nodes/test_receive_request.py
    - tests/agent/test_memory_evidence_boundary.py
    - tests/agent/test_reviewed_memory_context_retrieve.py
    - tests/memory/test_phase48_1_memory_compat_alignment.py
    - tests/memory/test_phase48_long_term_preference_alignment.py
    - tests/test_approval_gate.py
    - tests/agent/test_phase22_action_boundary.py
  </read_first>
  <files>tests/agent/test_nodes/test_investigate.py, tests/agent/test_graph.py, existing regression tests only for additive Phase 49 assertions/fixtures; do not relax existing intent/memory/risk/approval/action assertions</files>
  <behavior>
    - Test 1: Phase 43 TaskPlan / deferred steps behavior remains unchanged.
    - Test 2: Phase 44-48.1 memory/CWC/reviewed/preference tests remain unchanged.
    - Test 3: `active_slots` writer behavior remains owned outside investigate.
    - Test 4: risk/approval/action fail-closed tests still pass.
    - Test 5: prompt injection in tool output cannot alter allowlist, router, risk, approval, or action state.
  </behavior>
  <action>
    Run the regression sets below using only approved MOCA test entrypoints. If failures reveal a real local validation issue, document it in `.planning/LOCAL-VALIDATION-ISSUES.md` after diagnosis and fix. If a failure is due to using a bare pytest/Python entrypoint, discard it and rerun with `UV_CACHE_DIR=/tmp/uv-cache uv run pytest ...`.

    Do not update memory/intent production code unless a Phase 49 change caused a direct regression and the smallest fix is inside the integration seam. If the fix would alter intent or memory contracts, stop and report a blocker.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_classify_intent.py tests/agent/test_intent_task_plan.py tests/agent/test_nodes/test_receive_request.py -q</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_memory_evidence_boundary.py tests/agent/test_reviewed_memory_context_retrieve.py tests/memory/test_phase48_1_memory_compat_alignment.py tests/memory/test_phase48_long_term_preference_alignment.py -q</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_approval_gate.py tests/agent/test_phase22_action_boundary.py tests/test_interception_rate.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Intent Phase 43 focused tests pass.
    - Memory Phase 44-48.1 focused tests pass.
    - Risk/approval/action fail-closed focused tests pass.
    - `rg -n "active_slots.*=" src/agent/nodes/investigate.py` returns no writer.
  </acceptance_criteria>
  <done>Cross-subsystem regressions prove Phase 49 did not reopen memory, intent, or downstream gate work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Final no-go sweep and planning ledger updates</name>
  <read_first>
    - .planning/DEFERRED-DECISIONS.md GAD-01
    - .planning/ARCHITECTURE-DEBT.md tool/RAG/graph sections
    - .planning/LOCAL-VALIDATION-ISSUES.md append-only local validation issue format
    - .planning/phases/49-investigate-bounded-react-loop-migration/49-CONTEXT.md no-go and review checklists
  </read_first>
  <files>.planning/DEFERRED-DECISIONS.md, .planning/ARCHITECTURE-DEBT.md, .planning/LOCAL-VALIDATION-ISSUES.md, .planning/phases/49-investigate-bounded-react-loop-migration/49-04-SUMMARY.md</files>
  <behavior>
    - Test 1: forbidden diff areas remain clean.
    - Test 2: no direct service/repository dispatch bypass appears in investigate.
    - Test 3: no write tool is reachable from investigate.
    - Test 4: GAD-01 status updates only after tests pass.
    - Test 5: architecture debt records legacy deterministic investigate debt as fixed or fixed-with-limits based on verification evidence.
  </behavior>
  <action>
    Run the no-go grep checklist from `49-CONTEXT.md`. Absence checks must be inverted (`! rg ...`) or otherwise fail when forbidden production matches are found. Keep positive greps for rejection-test coverage separate from negative production greps. Update:
    - `.planning/DEFERRED-DECISIONS.md`: GAD-01 from `IMPLEMENTATION_PENDING` to `IMPLEMENTED` or `IMPLEMENTED_WITH_LIMITATIONS`, with concrete verification commands and known limits.
    - `.planning/ARCHITECTURE-DEBT.md`: record the graph/RAG/tool-call architecture debt closure for legacy deterministic investigate. Include status, impact, evidence files/tests, and residual risks.
    - `.planning/LOCAL-VALIDATION-ISSUES.md`: append only if local validation exposed an actual issue/failure.
    - `49-04-SUMMARY.md`: summarize changed files, verification, no-go results, and residual limitations.

    Do not edit `docs/contract-spec.md` in this task. If implementation discovered a real spec/code conflict, record a blocker and stop instead of silently changing the spec.
  </action>
  <verify>
    <automated>! rg -n "active_slots.*=" src/agent/nodes/investigate.py</automated>
    <automated>! rg -n "BusinessFactService|PolicyKnowledgeService|CaseMemoryService|RefundRepository|OrderRepository" src/agent/nodes/investigate.py</automated>
    <automated>! rg -n "create_coupon_grant_draft|issue_coupon|partial_refund|full_refund|close_ticket|escalate_ticket|manual_review" src/agent/nodes/investigate.py</automated>
    <automated>rg -n "create_coupon_grant_draft|write tool|write-tool" tests/agent/test_nodes/test_investigate.py</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_investigate.py tests/agent/test_graph.py tests/agent/test_nodes/test_classify_intent.py tests/agent/test_intent_task_plan.py tests/agent/test_memory_evidence_boundary.py tests/agent/test_reviewed_memory_context_retrieve.py tests/test_approval_gate.py tests/agent/test_phase22_action_boundary.py -q</automated>
  </verify>
  <acceptance_criteria>
    - GAD-01 ledger status matches implementation reality and cites verification.
    - Architecture debt entry distinguishes implemented fact from target contract and lists residual risk if any.
    - No forbidden diff is present in `docs/contract-spec.md`, intent contracts, memory writer contracts, active slot ownership, or risk/approval/action logic.
    - Final summary records all commands run with approved `uv` entrypoints.
  </acceptance_criteria>
  <done>Phase 49 implementation is closed with evidence, no-go checks, and planning ledgers updated.</done>
</task>

</tasks>

<rollback_fallback_behavior>
- If final graph regressions fail because the planner path is unstable, disable the planner main path behind a config fallback and leave deterministic `plan_next_step` as the active path while recording GAD-01 as not implemented.
- If the planner main path is disabled and deterministic `plan_next_step` remains the active production path, Phase 49 verdict is `BLOCKED` / `NOT_IMPLEMENTED` for GAD-01. Do not mark GAD-01 as `IMPLEMENTED` or `IMPLEMENTED_WITH_LIMITATIONS`.
- If `search_sop` or any of the eight tools is missing from planner visibility or cannot be dispatched through `ToolPlatform.invoke(...)`, this is a blocker, not an implemented limitation. It is acceptable for a declared read-only tool to dispatch through ToolPlatform and return a safe `unavailable`/no-data result when the backend has no data yet.
- If a true spec/code contradiction is found, stop implementation and open a separate spec review; do not edit `docs/contract-spec.md` inside Phase 49 closeout.
</rollback_fallback_behavior>

<no_regression_checks>
- Intent: `TaskPlan`, `deferred_steps`, `primary_intent`, `requested_operation`, `routing_hints`, and `contextual_intent_resolve` unchanged.
- Memory: CWC lifecycle, reviewed case memory, explicit long-term preference memory, `MemoryService` writer ownership, and `active_slots` ownership unchanged.
- Risk/approval/action: fail-closed gates and action draft/execution are still downstream only.
- Evidence: `investigate` still does not write authoritative `evidence_refs`; claim/evidence validation remains downstream.
- Routing: outer graph routers remain deterministic and side-effect-free.
</no_regression_checks>

<minimal_test_commands>
```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_investigate.py tests/agent/test_graph.py -q
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_classify_intent.py tests/agent/test_intent_task_plan.py tests/agent/test_nodes/test_receive_request.py -q
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_memory_evidence_boundary.py tests/agent/test_reviewed_memory_context_retrieve.py tests/memory/test_phase48_1_memory_compat_alignment.py tests/memory/test_phase48_long_term_preference_alignment.py -q
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_approval_gate.py tests/agent/test_phase22_action_boundary.py tests/test_interception_rate.py -q
```
</minimal_test_commands>
