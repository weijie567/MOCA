---
phase: 49-investigate-bounded-react-loop-migration
plan: "03"
type: execute
wave: 3
depends_on: [49-02]
files_modified:
  - src/agent/nodes/investigate.py
  - src/tools/executors/knowledge.py
  - src/tools/projection.py
  - src/agent/events.py
  - tests/agent/test_nodes/test_investigate.py
  - tests/tools/test_tool_platform.py
  - tests/replay/test_operation_pairing.py
autonomous: true
requirements: [GAD-01-IMPL]
requirements_addressed: [GAD-01-IMPL]
must_haves:
  truths:
    - "Planner-visible investigate tool descriptors cover all eight section 12.4 read/retrieval tools."
    - "Every tool observation shown to the planner is a projected prompt-safe observation, never raw payload."
    - "ToolPlatform remains the only graph-facing dispatch path."
    - "Every loop tool/RAG call emits an independent operation event with iteration metadata and replay-distinguishable identity."
  artifacts:
    - path: "src/agent/nodes/investigate.py"
      provides: "tool descriptor exposure, planner prompt observation selection, event parent/iteration emission"
      contains: "ToolPlatform.invoke"
    - path: "src/tools/executors/knowledge.py"
      provides: "real executor visibility for declared read-only retrieval tools when needed"
      contains: "search_sop"
    - path: "src/tools/projection.py"
      provides: "projection boundary and optional safe relation hints"
      contains: "ToolResultProjector"
    - path: "tests/replay/test_operation_pairing.py"
      provides: "same node operation with multiple loop tool operations can be replay-distinguished"
      contains: "iteration"
  key_links:
    - from: "src/tools/catalog.py"
      to: "src/tools/platform.py"
      via: "visible tool descriptor set for investigate"
      pattern: "search_case_memory"
    - from: "src/agent/events.py"
      to: "src/replay/service.py"
      via: "emit event operation/parent/iteration fields"
      pattern: "parent_operation_id"
---

<objective>
Complete the eight-tool read/retrieval surface, enforce the projection boundary for planner observations, and make per-iteration trace/replay behavior explicit.

Purpose: after the planner-driven loop works, make sure it is not a black box: all allowed tools are declared and visible as read/retrieval choices, raw results cannot enter planner context, and each loop tool operation remains replay-auditable.
Output: 8-tool coverage, projection-boundary hardening, trace/replay metadata tests, and static no-bypass guards.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/49-investigate-bounded-react-loop-migration/49-CONTEXT.md
@.planning/phases/49-investigate-bounded-react-loop-migration/49-01-PLAN.md
@.planning/phases/49-investigate-bounded-react-loop-migration/49-02-PLAN.md
@docs/contract-spec.md
@src/agent/nodes/investigate.py
@src/tools/catalog.py
@src/tools/platform.py
@src/tools/runtime.py
@src/tools/projection.py
@src/tools/executors/business.py
@src/tools/executors/knowledge.py
@src/tools/executors/memory.py
@src/agent/events.py
@src/replay/service.py
@src/replay/pairing.py
@tests/agent/test_nodes/test_investigate.py
@tests/tools/test_tool_platform.py
@tests/replay/test_operation_pairing.py

<source_audit>
SOURCE | ID | Requirement / Decision | Plan | Status
--- | --- | --- | --- | ---
CONTRACT | 12.4 | Eight investigate read/retrieval tools | 49-03 | COVERED
CONTRACT | 17.2 | Loop tool/RAG events carry iteration/replay identity | 49-03 | COVERED
CONTEXT | Projection boundary | Planner sees prompt-safe projection only | 49-03 | COVERED
USER | Tests 12-15 | trace iteration, replay distinction, prompt injection boundary, 8-tool coverage | 49-03 | COVERED
</source_audit>

<file_scope>
Expected files:
- `src/agent/nodes/investigate.py`
- `src/tools/executors/knowledge.py` only if real `search_sop` visibility is still missing
- `src/tools/projection.py` only for prompt-safe structured relation hints / raw-boundary tests
- `src/agent/events.py` or replay helpers only if existing event APIs cannot carry parent operation identity from investigate
- `tests/agent/test_nodes/test_investigate.py`
- `tests/tools/test_tool_platform.py`
- `tests/replay/test_operation_pairing.py` or another focused replay test file

Do not modify:
- `docs/contract-spec.md`
- Tool descriptor envelope shape or event schema fields; use existing `operation_id`, `parent_operation_id`, `attempt`, `iteration`, and redacted payload surfaces
- Tool write/action executors or action policies
- Business/knowledge/memory service internals except the minimal executor visibility stub noted above
- Graph routers, intent, memory writers, risk, approval, action logic
</file_scope>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove and complete eight-tool investigate visibility</name>
  <read_first>
    - docs/contract-spec.md section 12.4
    - src/tools/catalog.py declarations for `get_order`, `get_refund_case`, `get_ticket`, `get_logistics`, `get_merchant_risk`, `search_policy`, `search_sop`, `search_case_memory`
    - src/tools/platform.py `visible_tools`
    - src/tools/executors/knowledge.py `has_tool` and `execute`
    - src/tools/executors/memory.py `search_case_memory`
    - src/business/service.py `get_logistics`, `get_merchant_risk`
  </read_first>
  <files>src/tools/executors/knowledge.py, tests/agent/test_nodes/test_investigate.py, tests/tools/test_tool_platform.py</files>
  <behavior>
    - Test 1: planner-visible allowlist contains exactly the eight §12.4 tools.
    - Test 2: each of the eight tools has a descriptor and input schema.
    - Test 3: `search_sop` is not silently hidden by the real knowledge executor if it is declared planner-visible.
    - Test 4: no write/action tool is visible to the investigate planner.
    - Test 5: each of the eight tools can be invoked through `ToolPlatform.invoke(...)` with valid minimal args and returns a safe `ToolResultV2` status (`success`, `not_found`, `unavailable`, `permission_denied`, or another declared terminal status), never a direct service bypass.
  </behavior>
  <action>
    Add a static or runtime test that resolves the planner-visible investigate tool names from the actual `ToolPlatform.visible_tools(...)` / descriptor surface and asserts exact equality with:
    ```python
    [
        "get_order",
        "get_refund_case",
        "get_ticket",
        "get_logistics",
        "get_merchant_risk",
        "search_policy",
        "search_sop",
        "search_case_memory",
    ]
    ```

    Add an explicit smoke test that calls `ToolPlatform.invoke(...)` once for each of the eight tools using descriptor-valid minimal arguments. This is not an end-to-end business data assertion; it proves every allowlisted tool has a ToolPlatform-dispatchable runtime path. Accept safe terminal no-data results for tools whose backend is intentionally stubbed (`get_logistics`, `get_merchant_risk`, `search_sop`), but fail if a tool is invisible, cannot be invoked through ToolPlatform, or requires a direct service call from `investigate`.

    If `search_sop` is declared in `ToolCatalog` but still unavailable from `KnowledgeToolExecutor.has_tool(...)`, update `src/tools/executors/knowledge.py` minimally so `has_tool("search_sop")` returns true and execution remains read-only. If no SOP backend exists, return the same no-data/unavailable result shape already declared by the descriptor rather than inventing new SOP semantics.

    Do not add new tools. Do not change descriptor envelope fields. Do not expose write tools under investigate.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_investigate.py -k "allowlist or search_sop or visible" -q</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/tools/test_catalog.py tests/tools/test_tool_platform.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The eight-tool exact-set test fails if any tool is missing or any extra tool appears.
    - The eight-tool ToolPlatform smoke test fails if any allowlisted tool is non-dispatchable.
    - `search_sop` is either executor-visible as a read-only unavailable/no-data retrieval or the plan records a blocker before implementation proceeds.
    - `create_coupon_grant_draft` remains absent from investigate visibility.
  </acceptance_criteria>
  <done>The planner-visible investigate surface matches the normative eight-tool read/retrieval allowlist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep planner observations behind the projection boundary</name>
  <read_first>
    - src/tools/projection.py forbidden raw key handling
    - src/agent/nodes/investigate.py `_accumulate_tool_result` and planner input construction
    - tests/agent/test_nodes/test_investigate.py existing raw payload stripping tests
    - docs/target-agent-platform-architecture-plan.md section 6 trust boundary guide
  </read_first>
  <files>src/agent/nodes/investigate.py, src/tools/projection.py, tests/agent/test_nodes/test_investigate.py</files>
  <behavior>
    - Test 1: raw payload sentinel strings do not appear in captured planner input.
    - Test 2: prompt injection text in a tool result cannot modify allowlist, routing, risk, approval, or action behavior.
    - Test 3: planner input contains only projected summaries/refs and allowed structured identifier hints.
    - Test 4: raw tool payload may remain in existing safe trace/storage locations only if current contracts already permit it, but not in planner prompt.
  </behavior>
  <action>
    Build planner observation context only from `ToolResultProjectionV1` / projector output and locally accumulated safe summaries. Exclude:
    - raw tool adapter payloads;
    - policy body text beyond existing prompt-safe summaries/refs;
    - `raw_payload`, `private_reasoning`, `debug_trace`, secrets, PII sentinels, approval authority bodies, and similar forbidden keys;
    - arbitrary `text_for_prompt` regex-derived slots if text contains user/tool content.

    Treat `ToolResultProjector` as the prompt-injection boundary. If new structured identifier hints are needed for 49-02 slot discovery, add them as explicit prompt-safe projection fields with tests proving forbidden raw fields are stripped.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_investigate.py -k "projection or raw or prompt_injection or planner_input" -q</automated>
  </verify>
  <acceptance_criteria>
    - A captured fake-planner input assertion proves forbidden raw sentinel text is absent.
    - A prompt-injection fixture asking to change tools/route/approval/action is inert.
    - Planner allowlist/routing/risk behavior remains deterministic despite injected observation content.
  </acceptance_criteria>
  <done>Planner context consumes projected observations only and cannot be steered by raw tool payload text.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Emit replay-distinguishable per-iteration tool events</name>
  <read_first>
    - src/agent/nodes/investigate.py `_emit_tool_event`, `_append_tool_call_record`, current `operation_id` generation
    - src/agent/events.py `emit_event`
    - src/replay/service.py `emit_decision_event`
    - src/replay/pairing.py operation pairing rules
    - tests/replay/test_operation_pairing.py iteration/parent-operation coverage
    - tests/agent/test_nodes/test_investigate.py event iteration tests
  </read_first>
  <files>src/agent/nodes/investigate.py, src/agent/events.py, tests/agent/test_nodes/test_investigate.py, tests/replay/test_operation_pairing.py</files>
  <behavior>
    - Test 1: each tool call has a distinct `operation_id`.
    - Test 2: every tool event carries `iteration`.
    - Test 3: multiple tool operations can be associated with one investigate node operation through existing parent-operation semantics, without changing event schema.
    - Test 4: retry/max-attempt behavior emits new operation identity only when the replay contract requires it.
  </behavior>
  <action>
    Ensure every loop iteration emits started/completed/failed events with:
    - distinct tool `operation_id`;
    - `iteration`;
    - `tool_call_id`;
    - attempt count;
    - parent node operation if available.

    If current `emit_event(...)` cannot pass `parent_operation_id`, extend only the local event-emission helper/API surface needed to pass the already-supported replay field. Do not alter database event schema or decision event schema.

    Add tests that two tool calls under one investigate run produce two operation pairs and replay pairing can distinguish them by operation ID and iteration.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_investigate.py -k "event or iteration or replay" -q</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay/test_operation_pairing.py tests/replay/test_replay_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Existing iteration event tests remain green.
    - Replay tests prove multiple loop operations do not collapse into one opaque node result.
    - Event redaction still excludes raw args/payload where current event policy excludes them.
    - If parent node operation identity cannot be emitted without schema churn, Phase 49 closeout must be `IMPLEMENTED_WITH_LIMITATIONS`, not `IMPLEMENTED`, and 49-04 must record the replay parent-operation limitation explicitly in GAD-01 and `.planning/ARCHITECTURE-DEBT.md`.
  </acceptance_criteria>
  <done>Trace/replay can audit each planner-selected tool call independently.</done>
</task>

</tasks>

<rollback_fallback_behavior>
- If executor visibility for `search_sop` cannot be reconciled with current catalog/runtime contracts, stop and record it as a blocker rather than deleting `search_sop` from the allowlist or editing `contract-spec.md`.
- If parent node operation identity cannot be added without schema churn, preserve distinct tool operation IDs and iteration fields, then record the parent-operation gap as an implementation limitation for review. This limitation prevents a fully `IMPLEMENTED` GAD-01 closeout.
- Projection hardening regressions should fall back to less observation detail, not raw payload exposure.
</rollback_fallback_behavior>

<no_regression_checks>
- `ToolPlatform.invoke(...)` remains the only dispatch call from `investigate`.
- No direct `BusinessFactService`, knowledge service, memory service, repository, risk, approval, or action executor dispatch from `investigate`.
- Write tools remain unavailable and uninvoked.
- Existing replay schema/database fields are not changed.
</no_regression_checks>

<minimal_test_commands>
```bash
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_investigate.py -q
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/tools/test_catalog.py tests/tools/test_tool_platform.py -q
UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/replay/test_operation_pairing.py tests/replay/test_replay_service.py -q
```
</minimal_test_commands>
