---
phase: 24-agent-runs-short-term-memory-parity
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/conversation/test_service.py
  - tests/memory/test_thread_summary.py
  - tests/agent/context/test_assembler.py
  - tests/agent/test_session_memory_integration.py
  - tests/agent/test_required_slots.py
  - tests/agent/test_memory_evidence_boundary.py
autonomous: true
requirements:
  - STM-05
  - STM-06
  - STM-07
  - STM-08
  - STM-12
  - STM-13
must_haves:
  truths:
    - "Same-thread follow-ups can load recent messages, latest prior thread summary, prompt-safe tool summaries, and PostgreSQL session slots."
    - "Current-turn explicit slots override inherited trusted session slots."
    - "Stale, incompatible, cross-tenant, cross-user, or cross-thread memory fails closed."
    - "Memory context cannot satisfy policy evidence, current business facts, approval/action authority, or replay/audit truth."
  artifacts:
    - path: "tests/conversation/test_service.py"
      provides: "RED prompt-context and run-role idempotency service tests"
      contains: "test_agent_runs_prompt_context_loads_prior_summary_recent_messages_and_tool_summaries"
    - path: "tests/memory/test_thread_summary.py"
      provides: "RED rolling-summary idempotency and prompt-safe tool-summary tests"
      contains: "test_thread_rolling_summary_is_idempotent_for_same_source_end"
    - path: "tests/agent/test_session_memory_integration.py"
      provides: "RED run/SSE slot continuity and override integration tests"
      contains: "test_agent_runs_session_slots_explicit_current_turn_overrides_inherited"
  key_links:
    - from: "ConversationService.load_prompt_context"
      to: "ContextAssembler"
      via: "prompt-safe projected recent messages, summaries, and tool summaries"
      pattern: "load_prompt_context.*ContextAssembler"
    - from: "session_memory_load"
      to: "extract_slots"
      via: "trusted_session_memory active_slots with current-turn override"
      pattern: "trusted_session_memory"
---

<objective>
Create Wave 0 RED validation scaffolding for prompt context parity, session slot continuity, prompt-safety, legacy compatibility, and authority boundaries.

Purpose: prevent Phase 24 from adding persistence while leaving follow-up prompt context or memory authority protections unverified.
Output: failing tests in service, memory, context, and session-memory test files.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-agent-runs-short-term-memory-parity/24-CONTEXT.md
@.planning/phases/24-agent-runs-short-term-memory-parity/24-RESEARCH.md
@.planning/phases/24-agent-runs-short-term-memory-parity/24-VALIDATION.md
@.planning/phases/24-agent-runs-short-term-memory-parity/24-PATTERNS.md
@AGENTS.md
@CLAUDE.md
@docs/contract-spec.md
@tests/conversation/test_service.py
@tests/memory/test_thread_summary.py
@tests/agent/context/test_assembler.py
@tests/agent/test_session_memory_integration.py
@tests/agent/test_required_slots.py
@tests/agent/test_memory_evidence_boundary.py
@src/conversation/service.py
@src/memory/thread_summary.py
@src/agent/context/assembler.py
@src/agent/context/projectors.py
@src/agent/nodes/extract_slots.py
@src/agent/nodes/session_memory_load.py
</context>

<tasks>

<task type="auto" tdd="true">
  <id>24-W0-04</id>
  <name>Task 1: Add RED conversation and summary service tests</name>
  <files>tests/conversation/test_service.py, tests/memory/test_thread_summary.py</files>
  <read_first>
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-CONTEXT.md` decisions D-04, D-05, D-07, D-16, D-17.
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-VALIDATION.md` rows 24-W0-02 and 24-01-01.
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-PATTERNS.md` sections for `tests/conversation/test_service.py`, `tests/memory/test_thread_summary.py`, `src/conversation/service.py`, and `src/memory/thread_summary.py`.
    - `tests/conversation/test_service.py`.
    - `tests/memory/test_thread_summary.py`.
    - `src/conversation/service.py`.
    - `src/conversation/repository.py`.
    - `src/memory/thread_summary.py`.
  </read_first>
  <behavior>
    - `test_agent_runs_prompt_context_loads_prior_summary_recent_messages_and_tool_summaries`: `ConversationService.load_prompt_context` returns latest prior `thread_rolling` summary, bounded recent messages, and prompt-safe tool summaries for an `/agent-runs` same-thread follow-up.
    - `test_append_or_get_run_role_messages_are_idempotent`: expected service helpers return the same user/assistant message IDs for repeated `(tenant_id, user_id, thread_id, run_id, role)` calls.
    - `test_thread_rolling_summary_is_idempotent_for_same_source_end`: repeated `persist_thread_summary(..., run_id=same_run)` for the same source end message returns the existing summary instead of inserting a duplicate.
  </behavior>
  <action>
    Add RED tests using exact test names above. Tests may reference not-yet-existing helper methods such as `ConversationService.append_or_get_user_message_for_run`, `ConversationService.append_or_get_assistant_message_for_run`, and repository summary idempotency behavior; keep imports lazy only where needed so collection succeeds. Include forbidden prompt-safety markers in tool summaries and assert only `ToolResultPromptSummary.prompt_summary`/safe refs enter context. Do not change production code.
  </action>
  <verify>
    <automated>bash -lc 'set +e; uv run pytest tests/conversation/test_service.py::test_agent_runs_prompt_context_loads_prior_summary_recent_messages_and_tool_summaries tests/conversation/test_service.py::test_append_or_get_run_role_messages_are_idempotent tests/memory/test_thread_summary.py::test_thread_rolling_summary_is_idempotent_for_same_source_end -q; status=$?; test "$status" -eq 1'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "test_agent_runs_prompt_context_loads_prior_summary_recent_messages_and_tool_summaries|test_append_or_get_run_role_messages_are_idempotent" tests/conversation/test_service.py` finds both names.
    - `rg -n "test_thread_rolling_summary_is_idempotent_for_same_source_end" tests/memory/test_thread_summary.py` finds the summary idempotency test.
    - The prompt-context test asserts `latest_thread_summary`, `recent_messages`, and `tool_prompt_summaries` are all non-empty and scoped to the same tenant/user/thread.
    - The idempotency tests assert repeated calls return identical IDs and row counts stay at one.
  </acceptance_criteria>
  <done>RED service tests cover STM-05, STM-07, STM-08, and STM-13 prompt-context/idempotency foundations.</done>
</task>

<task type="auto" tdd="true">
  <id>24-W0-05</id>
  <name>Task 2: Add RED session slot continuity and fail-closed tests</name>
  <files>tests/agent/test_session_memory_integration.py, tests/agent/test_required_slots.py</files>
  <read_first>
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-CONTEXT.md` decisions D-05, D-08, D-09, D-14.
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-RESEARCH.md` requirement rows STM-05 and STM-06.
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-PATTERNS.md` sections for `tests/agent/test_session_memory_integration.py`, `tests/agent/test_required_slots.py`, `src/agent/nodes/session_memory_load.py`, and `src/memory/service.py`.
    - `tests/agent/test_session_memory_integration.py`.
    - `tests/agent/test_required_slots.py`.
    - `src/agent/nodes/session_memory_load.py`.
    - `src/memory/service.py`.
    - `src/agent/routing.py`.
  </read_first>
  <behavior>
    - `test_agent_runs_session_slots_explicit_current_turn_overrides_inherited`: current-turn extracted slot value wins over inherited `trusted_session_memory` value in `/agent-runs` follow-up flow.
    - `test_agent_runs_session_memory_wrong_scope_fails_closed`: wrong tenant, user, thread, stale, or incompatible inherited memory results in no trusted active slot and routes to clarification/current-turn-only behavior.
  </behavior>
  <action>
    Add RED tests with exact names above. Reuse existing `trusted_session_memory` metadata patterns and current-turn override helpers; do not create new authority semantics. Assertions must use exact metadata source value `trusted_session_memory` for inherited slots and require `continuity_claimed is False` or empty `active_slots` for stale/wrong-scope memory. Do not modify production code.
  </action>
  <verify>
    <automated>bash -lc 'set +e; uv run pytest tests/agent/test_session_memory_integration.py::test_agent_runs_session_slots_explicit_current_turn_overrides_inherited tests/agent/test_session_memory_integration.py::test_agent_runs_session_memory_wrong_scope_fails_closed tests/agent/test_required_slots.py -q; status=$?; test "$status" -eq 1'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "test_agent_runs_session_slots_explicit_current_turn_overrides_inherited|test_agent_runs_session_memory_wrong_scope_fails_closed" tests/agent/test_session_memory_integration.py` finds both names.
    - Tests assert current-turn metadata has `explicit_current_turn` or equivalent current-turn source and inherited metadata has `source == "trusted_session_memory"`.
    - Wrong-scope/stale/incompatible cases assert no inherited slot satisfies required slots.
  </acceptance_criteria>
  <done>RED session-memory tests cover STM-06 and fail-closed behavior for D-08.</done>
</task>

<task type="auto" tdd="true">
  <id>24-W0-06</id>
  <name>Task 3: Add RED prompt-safety and authority-boundary tests</name>
  <files>tests/agent/context/test_assembler.py, tests/agent/test_memory_evidence_boundary.py</files>
  <read_first>
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-CONTEXT.md` decisions D-06, D-07, D-08.
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-RESEARCH.md` security domain T-24-04 and T-24-05.
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-PATTERNS.md` sections for `tests/agent/context/test_assembler.py`, `tests/agent/test_memory_evidence_boundary.py`, `src/agent/context/assembler.py`, and `src/agent/context/projectors.py`.
    - `docs/contract-spec.md` §§9.2-10.1, 13, 17-18.
    - `tests/agent/context/test_assembler.py`.
    - `tests/agent/test_memory_evidence_boundary.py`.
    - `src/agent/context/assembler.py`.
    - `src/agent/context/projectors.py`.
    - `src/agent/rag_context/verifier.py`.
  </read_first>
  <behavior>
    - `test_agent_runs_prompt_context_excludes_raw_tool_private_authority_and_debug_fields`: prompt context assembled for `/agent-runs` excludes raw payloads, private reasoning, authority bodies, debug traces, secrets, and excess PII.
    - `test_agent_runs_memory_context_is_not_policy_business_action_or_replay_authority`: memory/recent/summary/session context can help reference resolution but cannot satisfy policy evidence, current business fact, approval/action authority, or replay/audit truth.
  </behavior>
  <action>
    Add RED tests with exact names above. Use existing forbidden marker constants/patterns from assembler and memory evidence boundary tests. Assertions must look for absence of exact forbidden markers: `raw_payload`, `private_reasoning`, `approval_authority_body`, `action_authority_body`, `debug_trace`, `secret`, `EvidenceRefV1`, and `ReplayEventV3` in prompt/memory serialized surfaces. Do not change production code.
  </action>
  <verify>
    <automated>bash -lc 'set +e; uv run pytest tests/agent/context/test_assembler.py::test_agent_runs_prompt_context_excludes_raw_tool_private_authority_and_debug_fields tests/agent/test_memory_evidence_boundary.py::test_agent_runs_memory_context_is_not_policy_business_action_or_replay_authority -q; status=$?; test "$status" -eq 1'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "test_agent_runs_prompt_context_excludes_raw_tool_private_authority_and_debug_fields" tests/agent/context/test_assembler.py` finds the prompt-safety test.
    - `rg -n "test_agent_runs_memory_context_is_not_policy_business_action_or_replay_authority" tests/agent/test_memory_evidence_boundary.py` finds the authority-boundary test.
    - Tests assert exact forbidden marker strings are absent from prompt or memory surfaces.
    - Tests assert business/policy/action/replay authority still requires existing current tool/evidence/approval/replay records.
  </acceptance_criteria>
  <done>RED authority and prompt-safety tests cover STM-07, STM-12, and D-06/D-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conversation repository -> prompt context | Stored messages/tool summaries become prompt input only through `ConversationService.load_prompt_context`. |
| session memory -> slot routing | PostgreSQL slot memory may influence reference resolution but must remain scoped and contextual. |
| prompt assembly -> model | Context crosses into model prompt through `ContextAssembler` and projector allowlists only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering/Repudiation | run-role idempotency RED tests | mitigate | RED service tests require same `(tenant_id, user_id, thread_id, run_id, role)` to return existing rows. |
| T-24-02 | Repudiation | terminal memory continuity tests | mitigate | RED tests require explicit context/session outcomes; no silent success. |
| T-24-03 | Spoofing/Tampering | trusted conversation IDs | transfer | Covered by Plan 24-01 API/SSE tests; this plan verifies downstream context surfaces only. |
| T-24-04 | Information Disclosure | prompt context and session memory tests | mitigate | Tests require tenant/user/thread scoping and fail-closed wrong-scope behavior. |
| T-24-05 | Elevation of Privilege/Tampering | assembler and memory evidence boundary tests | mitigate | Tests assert memory cannot satisfy policy/business/action/replay authority and forbidden raw markers are absent. |
| T-24-06 | Repudiation | non-completed memory behavior | transfer | Covered by Plan 24-01 API/SSE RED tests; this plan validates prompt/context consequences. |
</threat_model>

<must_haves>
- Prompt context RED tests cover recent messages, latest prior rolling summary, prompt-safe tool summaries, and scope isolation.
- Session memory RED tests cover explicit current-turn override and stale/cross-scope fail-closed behavior.
- Authority-boundary RED tests prove memory remains contextual only.
- No production `src/` file changes occur in this Wave 0 plan.
</must_haves>

<verification>
Run all Wave 0 prompt/context/session RED tests:

`bash -lc 'set +e; uv run pytest tests/conversation/test_service.py::test_agent_runs_prompt_context_loads_prior_summary_recent_messages_and_tool_summaries tests/conversation/test_service.py::test_append_or_get_run_role_messages_are_idempotent tests/memory/test_thread_summary.py::test_thread_rolling_summary_is_idempotent_for_same_source_end tests/agent/test_session_memory_integration.py::test_agent_runs_session_slots_explicit_current_turn_overrides_inherited tests/agent/test_session_memory_integration.py::test_agent_runs_session_memory_wrong_scope_fails_closed tests/agent/context/test_assembler.py::test_agent_runs_prompt_context_excludes_raw_tool_private_authority_and_debug_fields tests/agent/test_memory_evidence_boundary.py::test_agent_runs_memory_context_is_not_policy_business_action_or_replay_authority -q; status=$?; test "$status" -eq 1'`
</verification>

<success_criteria>
- All named tests exist and collect.
- Focused RED command exits with expected failure status 1.
- No source files are modified.
- Tests use existing safe boundaries: `ConversationService.load_prompt_context`, `ContextAssembler`, projector helpers, and PostgreSQL `MemoryService`.
</success_criteria>

<output>
After completion, create `.planning/phases/24-agent-runs-short-term-memory-parity/24-02-SUMMARY.md`.
</output>
