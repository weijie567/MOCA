---
phase: 24-agent-runs-short-term-memory-parity
plan: 06
type: execute
wave: 4
depends_on:
  - 24-05
files_modified:
  - src/api/services/agent_run_memory.py
  - src/api/routers/agent_runs.py
  - tests/test_agent_runs_api.py
  - tests/memory/test_session_memory_service.py
autonomous: true
requirements:
  - STM-03
  - STM-04
  - STM-06
  - STM-07
  - STM-10
  - STM-11
  - STM-13
must_haves:
  truths:
    - "Completed runs persist exactly one assistant message with final status metadata."
    - "Completed runs update the rolling thread summary idempotently from committed messages and prompt-safe tool summaries."
    - "Required terminal session-memory persistence completes/skips/errors before SSE final_response."
    - "Terminal memory failures are explicit and do not silently claim continuity success."
  artifacts:
    - path: "src/api/services/agent_run_memory.py"
      provides: "completed-only finalizer for assistant message, rolling summary, and bounded session memory"
      contains: "finalize_completed_agent_run_memory"
    - path: "src/api/routers/agent_runs.py"
      provides: "SSE final_response ordering after memory finalizer"
      contains: "finalize_completed_agent_run_memory"
  key_links:
    - from: "src/api/routers/agent_runs.py"
      to: "src/api/services/agent_run_memory.py"
      via: "call finalizer before _complete_run and final_response SSE"
      pattern: "finalize_completed_agent_run_memory"
    - from: "src/api/services/agent_run_memory.py"
      to: "src/agent/nodes/memory_write.py"
      via: "bounded memory_write invocation with explicit result trace"
      pattern: "memory_write"
---

<objective>
Implement the completed-run terminal memory finalizer for `/agent-runs + SSE`.

Purpose: completed Agent Console runs must persist assistant messages, rolling summaries, and required session-memory continuity before exposing `final_response`, while recording explicit skipped/error outcomes on bounded memory failure.
Output: new service `src/api/services/agent_run_memory.py`, SSE ordering changes, and green completed-memory/order tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-agent-runs-short-term-memory-parity/24-CONTEXT.md
@.planning/phases/24-agent-runs-short-term-memory-parity/24-RESEARCH.md
@.planning/phases/24-agent-runs-short-term-memory-parity/24-VALIDATION.md
@.planning/phases/24-agent-runs-short-term-memory-parity/24-PATTERNS.md
@AGENTS.md
@CLAUDE.md
@src/api/routers/agent_runs.py
@src/api/routers/agent.py
@src/conversation/service.py
@src/conversation/repository.py
@src/memory/thread_summary.py
@src/agent/nodes/memory_write.py
@src/agent/trace.py
@tests/test_agent_runs_api.py
@tests/memory/test_session_memory_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <id>24-06-01</id>
  <name>Task 1: Create completed-run memory finalizer service</name>
  <files>src/api/services/agent_run_memory.py, tests/test_agent_runs_api.py, tests/memory/test_session_memory_service.py</files>
  <read_first>
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-CONTEXT.md` decisions D-03, D-04, D-09, D-13, D-14, D-15.
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-PATTERNS.md` sections for `src/api/services/agent_run_memory.py`, `src/agent/nodes/memory_write.py`, and `src/agent/trace.py`.
    - `src/api/routers/agent.py`.
    - `src/api/routers/agent_runs.py`.
    - `src/conversation/service.py`.
    - `src/conversation/repository.py`.
    - `src/memory/thread_summary.py`.
    - `src/agent/nodes/memory_write.py`.
    - `tests/test_agent_runs_api.py`.
    - `tests/memory/test_session_memory_service.py`.
  </read_first>
  <action>
    Create `src/api/services/agent_run_memory.py` with a small service boundary and no FastAPI response imports. Provide a callable named `finalize_completed_agent_run_memory(...)` that accepts the current async `session`, trusted `run`, authenticated `user`, `final_state`, `final_response`, `trace_steps`, `trace_id`, and optional `conversation_service`.

    The function must:
    - execute only when `final_status == "completed"` and `final_response` is a non-empty string;
    - call `ConversationService.append_or_get_assistant_message_for_run(...)` with `metadata_json={"status": "completed", "source": "agent_runs.finalizer"}`;
    - call `ThreadRollingSummaryService(conversation_repository).persist_thread_summary(...)` using the trusted `run.id`, `run.tenant_id`, `run.user_id`, and `run.thread_id`;
    - call existing `memory_write({**final_state, "final_response": final_response}, {"configurable": {"session": session, "trace_id": trace_id or ""}})` for required terminal session-memory continuity;
    - capture `memory_write_result.status` or equivalent exact result status as one of `completed`, `skipped`, `error`, or `failed`;
    - append a trace step named exactly `agent_run_memory_finalize` with `metadata_json` or metrics containing `assistant_message_id`, `thread_summary_id`, and `memory_write_status`;
    - catch bounded memory-write timeout/error into explicit skipped/error result, not silent success;
    - never include private reasoning, raw tool payloads, authority bodies, debug traces, secrets, or PII beyond existing prompt-safe summaries in assistant metadata or summary content.
  </action>
  <verify>
    <automated>uv run pytest tests/test_agent_runs_api.py::test_completed_agent_run_persists_exactly_one_assistant_message tests/test_agent_runs_api.py::test_completed_agent_run_updates_thread_summary_idempotently tests/memory/test_session_memory_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "def finalize_completed_agent_run_memory|agent_runs.finalizer|agent_run_memory_finalize|memory_write_status" src/api/services/agent_run_memory.py` finds all required symbols/strings.
    - `rg -n "raw_payload|private_reasoning|approval_authority_body|action_authority_body|debug_trace" src/api/services/agent_run_memory.py` finds no allowlisted persistence of those markers.
    - Completed assistant-message and thread-summary idempotency tests pass.
    - Session-memory service tests remain green.
  </acceptance_criteria>
  <done>Completed-run memory finalizer persists required memory surfaces and records explicit memory-write outcome.</done>
</task>

<task type="auto" tdd="true">
  <id>24-06-02</id>
  <name>Task 2: Wire finalizer before SSE final_response</name>
  <files>src/api/routers/agent_runs.py, src/api/services/agent_run_memory.py, tests/test_agent_runs_api.py</files>
  <read_first>
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-CONTEXT.md` decisions D-09, D-13, D-14, D-15.
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-PATTERNS.md` sections "Old ordering to replace", "Terminal persistence currently too thin", and "Background task helper".
    - `src/api/routers/agent_runs.py`.
    - `src/api/services/agent_run_memory.py`.
    - `src/agent/nodes/memory_write.py`.
    - `src/agent/trace.py`.
    - `tests/test_agent_runs_api.py`.
  </read_first>
  <action>
    In `_event_generator` completed path, call `finalize_completed_agent_run_memory(...)` after final graph state is known and before `_complete_run(...)` and before yielding the SSE event whose `event_type` is `final_response`. Merge the finalizer-returned trace step(s) into the persisted `trace_steps` passed to `_complete_run(...)`. Keep `_complete_run(...)` as the run-status/agent-step persistence path. Remove `_schedule_memory_write_after_response(...)` from the `/agent-runs` completed final-response path; it may remain only for optional cleanup paths that are not required for next-turn short-term continuity and are not shown as completed timeline nodes.

    Exact status semantics:
    - only `completed` final status with final response calls the assistant/summary/session-memory finalizer;
    - finalizer result `memory_write_status in {"completed", "skipped", "error", "failed"}` must exist before `final_response`;
    - final SSE `final_response` payload must not claim memory success unless `memory_write_status == "completed"`;
    - if memory write is skipped/error/failed, still emit a safe final answer plus explicit trace/timeline result, not a silent success.
  </action>
  <verify>
    <automated>uv run pytest tests/test_agent_runs_api.py::test_sse_final_response_after_bounded_memory_persistence_result tests/test_agent_runs_api.py::test_completed_agent_run_persists_exactly_one_assistant_message tests/test_agent_runs_api.py::test_completed_agent_run_updates_thread_summary_idempotently -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "finalize_completed_agent_run_memory" src/api/routers/agent_runs.py` finds the router call.
    - `rg -n "_schedule_memory_write_after_response\\(" src/api/routers/agent_runs.py` finds no call in the completed `/agent-runs` `final_response` path.
    - `uv run pytest tests/test_agent_runs_api.py::test_sse_final_response_after_bounded_memory_persistence_result -q` passes and proves memory result precedes final_response.
    - Completed assistant and summary tests still pass after SSE wiring.
  </acceptance_criteria>
  <done>SSE final_response is ordered after bounded terminal memory persistence result.</done>
</task>

<task type="auto" tdd="true">
  <id>24-06-03</id>
  <name>Task 3: Verify completed-run idempotency and no raw leakage</name>
  <files>src/api/services/agent_run_memory.py, src/api/routers/agent_runs.py, tests/test_agent_runs_api.py</files>
  <read_first>
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-CONTEXT.md` decisions D-03, D-04, D-06, D-07, D-12.
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-PATTERNS.md` sections for prompt-safe projection boundary and completed-only terminal memory.
    - `src/api/services/agent_run_memory.py`.
    - `src/api/routers/agent_runs.py`.
    - `src/agent/context/projectors.py`.
    - `tests/test_agent_runs_api.py`.
  </read_first>
  <action>
    Tighten tests and implementation so duplicate invocation of the completed finalizer for the same run returns existing assistant message and summary IDs and does not create duplicate session-memory writes under the normal duplicate/reopened stream path. Add assertions that assistant message metadata and finalizer trace metadata do not contain exact forbidden keys: `raw_payload`, `private_reasoning`, `approval_authority_body`, `action_authority_body`, `debug_trace`, `snapshot`, `hash`, or `secret`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_agent_runs_api.py::test_duplicate_sse_stream_does_not_duplicate_memory_surfaces tests/test_agent_runs_api.py::test_completed_agent_run_persists_exactly_one_assistant_message tests/test_agent_runs_api.py::test_completed_agent_run_updates_thread_summary_idempotently -q</automated>
  </verify>
  <acceptance_criteria>
    - Duplicate stream test proves one user message, one assistant message, one summary, and no duplicate session-memory write for the same run.
    - `rg -n "raw_payload|private_reasoning|approval_authority_body|action_authority_body|debug_trace|secret" src/api/services/agent_run_memory.py tests/test_agent_runs_api.py` only finds forbidden-marker assertions or denylist references, not persisted success payload fields.
    - Focused duplicate/completed tests pass together.
  </acceptance_criteria>
  <done>Completed finalizer is idempotent under retry/reopen and prompt-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graph final state -> finalizer | LLM/tool-derived final state enters backend terminal persistence. |
| finalizer -> conversation/summaries/session memory | Durable short-term memory is written from safe committed inputs only. |
| finalizer -> SSE final_response | User-visible final response is emitted after required memory result exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering/Repudiation | completed finalizer | mitigate | Finalizer uses Plan 24-04 idempotent helpers and duplicate stream tests verify no duplicate memory surfaces. |
| T-24-02 | Repudiation | SSE final_response ordering | mitigate | Router waits for explicit `memory_write_status` before final_response; failures are recorded as skipped/error/failed. |
| T-24-03 | Spoofing/Tampering | conversation IDs | mitigate | Finalizer uses trusted run/user/session identity, not user payload IDs. |
| T-24-04 | Information Disclosure | finalizer scoped persistence | mitigate | Conversation and session writes are scoped by tenant/user/thread/run. |
| T-24-05 | Elevation of Privilege/Tampering | assistant metadata, summary, memory_write | mitigate | Finalizer persists prompt-safe metadata only and forbids raw/private/authority/debug fields. |
| T-24-06 | Repudiation | completed-only gate | mitigate | Finalizer executes only for exact `completed` status with final response; non-completed statuses handled in Plan 24-07. |
</threat_model>

<must_haves>
- New finalizer service exists and has no FastAPI response dependency.
- Completed runs write exactly one assistant message and one idempotent rolling summary.
- Required terminal session-memory result is explicit before `final_response`.
- Existing background post-response memory scheduling is not used for required `/agent-runs` continuity.
</must_haves>

<verification>
`uv run pytest tests/test_agent_runs_api.py::test_completed_agent_run_persists_exactly_one_assistant_message tests/test_agent_runs_api.py::test_completed_agent_run_updates_thread_summary_idempotently tests/test_agent_runs_api.py::test_sse_final_response_after_bounded_memory_persistence_result tests/test_agent_runs_api.py::test_duplicate_sse_stream_does_not_duplicate_memory_surfaces tests/memory/test_session_memory_service.py -q`

`uv run ruff check src/api/services/agent_run_memory.py src/api/routers/agent_runs.py tests/test_agent_runs_api.py tests/memory/test_session_memory_service.py`
</verification>

<success_criteria>
- STM-03, STM-04, STM-10, and STM-11 focused tests pass.
- Final SSE `final_response` is never emitted before terminal memory persistence result.
- Finalizer writes no raw/private/authority/debug material.
</success_criteria>

<output>
After completion, create `.planning/phases/24-agent-runs-short-term-memory-parity/24-06-SUMMARY.md`.
</output>
