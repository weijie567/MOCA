---
phase: 24-agent-runs-short-term-memory-parity
plan: 03
type: execute
wave: 1
depends_on:
  - 24-01
  - 24-02
files_modified:
  - src/db/models.py
  - src/db/migrations/versions/016_agent_run_memory_idempotency.py
autonomous: true
requirements:
  - STM-01
  - STM-03
  - STM-04
  - STM-10
  - STM-11
must_haves:
  truths:
    - "Database schema can enforce one active user message and one active assistant message per run."
    - "Database schema can enforce one active thread_rolling summary per source end message."
    - "Alembic upgrade succeeds before service-level idempotency implementation depends on the new indexes."
  artifacts:
    - path: "src/db/migrations/versions/016_agent_run_memory_idempotency.py"
      provides: "Alembic partial unique indexes for run-role messages and rolling summary source-end idempotency"
      contains: "uq_conversation_messages_active_tenant_run_role"
    - path: "src/db/models.py"
      provides: "ORM mirror for new partial unique indexes"
      contains: "uq_summaries_thread_rolling_source_end"
  key_links:
    - from: "src/db/models.py"
      to: "src/db/migrations/versions/016_agent_run_memory_idempotency.py"
      via: "matching SQLAlchemy Index names and Alembic op.create_index names"
      pattern: "uq_conversation_messages_active_tenant_run_role"
---

<objective>
Add DB-backed idempotency primitives for Phase 24 conversation messages and rolling summaries.

Purpose: exactly-once behavior for user messages, assistant messages, and rolling summaries must be enforceable across retries/processes, not only by in-process service checks.
Output: ORM indexes plus Alembic migration `016_agent_run_memory_idempotency.py`, verified with a blocking `uv run alembic upgrade head` gate before dependent service behavior.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-agent-runs-short-term-memory-parity/24-CONTEXT.md
@.planning/phases/24-agent-runs-short-term-memory-parity/24-RESEARCH.md
@.planning/phases/24-agent-runs-short-term-memory-parity/24-VALIDATION.md
@.planning/phases/24-agent-runs-short-term-memory-parity/24-PATTERNS.md
@AGENTS.md
@CLAUDE.md
@src/db/models.py
@src/db/migrations/versions/012_thread_user_scope.py
@src/db/migrations/versions/015_rag_production_ingestion_ocr.py
@alembic.ini
</context>

<tasks>

<task type="auto">
  <id>24-03-01</id>
  <name>Task 1: Add ORM and Alembic idempotency indexes</name>
  <files>src/db/models.py, src/db/migrations/versions/016_agent_run_memory_idempotency.py</files>
  <read_first>
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-CONTEXT.md` decisions D-01, D-03, D-04, D-12.
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-RESEARCH.md` open question "Should Phase 24 add DB partial unique indexes..." and recommendation.
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-PATTERNS.md` sections for `src/db/models.py` and `src/db/migrations/versions/016_agent_run_memory_idempotency.py`.
    - `src/db/models.py`.
    - `src/db/migrations/versions/012_thread_user_scope.py`.
    - `src/db/migrations/versions/015_rag_production_ingestion_ocr.py`.
    - `alembic.ini`.
  </read_first>
  <action>
    Add ORM `Index(...)` declarations that mirror the migration exactly:
    - `uq_conversation_messages_active_tenant_run_role` on `ConversationMessage.tenant_id`, `ConversationMessage.run_id`, `ConversationMessage.role`, `unique=True`, with partial predicate `deleted_at IS NULL AND run_id IS NOT NULL AND role IN ('user', 'assistant')`.
    - `uq_summaries_thread_rolling_source_end` on `ConversationSummary.tenant_id`, `ConversationSummary.conversation_thread_id`, `ConversationSummary.summary_type`, `ConversationSummary.source_end_message_id`, `unique=True`, with partial predicate `deleted_at IS NULL AND summary_type = 'thread_rolling' AND source_end_message_id IS NOT NULL`.
    Create `src/db/migrations/versions/016_agent_run_memory_idempotency.py` with `revision = "016_agent_run_memory_idempotency"` and `down_revision = "015_rag_production_ingestion_ocr"`. In `upgrade()`, run duplicate preflight checks before creating each unique index; raise `RuntimeError` naming the offending index if active duplicates exist. In `downgrade()`, drop both indexes by exact names. Use the migration style from `012_thread_user_scope.py` with `postgresql_where=sa.text(...)`.
  </action>
  <verify>
    <automated>uv run ruff check src/db/models.py src/db/migrations/versions/016_agent_run_memory_idempotency.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "uq_conversation_messages_active_tenant_run_role|uq_summaries_thread_rolling_source_end" src/db/models.py src/db/migrations/versions/016_agent_run_memory_idempotency.py` finds both index names in both files.
    - `rg -n "revision: str = \"016_agent_run_memory_idempotency\"|down_revision: str \\| None = \"015_rag_production_ingestion_ocr\"" src/db/migrations/versions/016_agent_run_memory_idempotency.py` finds both revision identifiers.
    - Migration `upgrade()` contains duplicate preflight logic for both run-role messages and `thread_rolling` source-end summaries before `op.create_index`.
    - Migration `downgrade()` contains `op.drop_index("uq_conversation_messages_active_tenant_run_role"` and `op.drop_index("uq_summaries_thread_rolling_source_end"`.
  </acceptance_criteria>
  <done>Schema-level idempotency primitives exist and mirror ORM/migration names exactly.</done>
</task>

<task type="auto">
  <id>24-03-02</id>
  <name>Task 2: [BLOCKING] Verify Alembic upgrade before dependent behavior</name>
  <files>src/db/migrations/versions/016_agent_run_memory_idempotency.py</files>
  <read_first>
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-VALIDATION.md` schema/DB migration requirement.
    - `src/db/migrations/versions/016_agent_run_memory_idempotency.py`.
    - `alembic.ini`.
    - `src/db/migrations/env.py`.
  </read_first>
  <action>
    Run the project's established non-interactive migration command `uv run alembic upgrade head`. This task is blocking: do not proceed to Plan 24-04 or later behavior verification until the migration applies successfully. If local DB state causes a migration failure, record the issue in `.planning/LOCAL-VALIDATION-ISSUES.md` in Chinese with symptom, reproduction command, evidence, current root-cause judgment, handling, remaining issue, and next entry point per `AGENTS.md`; then fix only migration/schema issues owned by this plan.
  </action>
  <verify>
    <automated>uv run alembic upgrade head</automated>
  </verify>
  <acceptance_criteria>
    - `uv run alembic upgrade head` exits 0.
    - Command output does not contain `FAILED`, `Traceback`, or `Duplicate`.
    - If `.planning/LOCAL-VALIDATION-ISSUES.md` is updated due to a real failure, it contains the failed command `uv run alembic upgrade head` and a concrete next entry point.
  </acceptance_criteria>
  <done>Blocking migration verification passes before any service/API plan relies on the new unique indexes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service logic -> PostgreSQL | Service idempotency relies on database uniqueness for duplicate process/retry safety. |
| Alembic migration -> existing data | Existing rows must not violate the new unique constraints silently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering/Repudiation | DB unique indexes | mitigate | Add partial unique indexes for run-role conversation messages and thread_rolling source-end summaries. |
| T-24-02 | Repudiation | migration preflight | mitigate | Duplicate preflight raises before enforcing indexes, preventing silent false exactly-once claims. |
| T-24-03 | Spoofing/Tampering | trusted conversation IDs | transfer | Later API plan resolves IDs; this plan only creates DB primitives. |
| T-24-04 | Information Disclosure | tenant-scoped indexes | mitigate | Indexes include `tenant_id`; service reads remain tenant/user/thread scoped in Plan 24-04. |
| T-24-05 | Elevation of Privilege/Tampering | memory authority | accept | Schema indexes do not add authority-bearing data; authority checks are handled in context plans. |
| T-24-06 | Repudiation | completed-only records | mitigate | Unique assistant-message and summary indexes prevent repeated false completed rows for a run/source end. |
</threat_model>

<must_haves>
- New migration revision follows the current Alembic chain after `015_rag_production_ingestion_ocr`.
- ORM and migration index names match exactly.
- Duplicate preflight exists before unique indexes are created.
- `[BLOCKING]` `uv run alembic upgrade head` passes before dependent plans execute.
</must_haves>

<verification>
`uv run ruff check src/db/models.py src/db/migrations/versions/016_agent_run_memory_idempotency.py`

`uv run alembic upgrade head`
</verification>

<success_criteria>
- Migration applies successfully.
- No service/API behavior is implemented in this plan.
- Later plans can rely on DB-backed duplicate-process/retry safety for run-role messages and rolling summaries.
</success_criteria>

<output>
After completion, create `.planning/phases/24-agent-runs-short-term-memory-parity/24-03-SUMMARY.md`.
</output>
