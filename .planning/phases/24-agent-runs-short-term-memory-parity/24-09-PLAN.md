---
phase: 24-agent-runs-short-term-memory-parity
plan: 09
type: execute
wave: 7
depends_on:
  - 24-08
files_modified:
  - src/api/routers/agent.py
  - tests/test_agent_runs_api.py
  - tests/conversation/test_service.py
  - tests/memory/test_thread_summary.py
  - tests/memory/test_session_memory_service.py
autonomous: true
requirements:
  - STM-08
  - STM-13
  - STM-14
must_haves:
  truths:
    - "Legacy `/api/v1/agent/chat` remains compatible with shared conversation/tool/summary/session-memory infrastructure."
    - "Focused regression covers conversation persistence, rolling summary, prompt context, session slots, idempotent retry, and authority boundaries."
    - "A three-turn `/agent-runs + SSE` smoke proves slot continuity and rolling-summary context."
  artifacts:
    - path: "tests/test_agent_runs_api.py"
      provides: "three-turn Agent Console smoke and legacy compatibility assertions"
      contains: "test_three_turn_agent_runs_smoke_uses_slots_and_summary_context"
    - path: "tests/conversation/test_service.py"
      provides: "shared conversation service regression coverage"
      contains: "load_prompt_context"
  key_links:
    - from: "tests/test_agent_runs_api.py"
      to: "/api/v1/agent-runs + SSE"
      via: "three-turn ASGI/SSE integration smoke"
      pattern: "test_three_turn_agent_runs_smoke_uses_slots_and_summary_context"
    - from: "tests/test_agent_runs_api.py"
      to: "/api/v1/agent/chat"
      via: "legacy route regression"
      pattern: "legacy|agent_chat|/agent/chat"
---

<objective>
Close Phase 24 with legacy compatibility, focused regression, and three-turn Agent Console smoke verification.

Purpose: prove that the current `/agent-runs + SSE` path reached short-term memory parity without breaking the legacy `/agent/chat` compatibility path or authority boundaries.
Output: green focused suite and integration smoke for STM-08, STM-13, and STM-14.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-agent-runs-short-term-memory-parity/24-CONTEXT.md
@.planning/phases/24-agent-runs-short-term-memory-parity/24-RESEARCH.md
@.planning/phases/24-agent-runs-short-term-memory-parity/24-VALIDATION.md
@.planning/phases/24-agent-runs-short-term-memory-parity/24-PATTERNS.md
@AGENTS.md
@CLAUDE.md
@src/api/routers/agent.py
@src/api/routers/agent_runs.py
@src/api/services/agent_run_memory.py
@src/conversation/service.py
@src/conversation/repository.py
@src/memory/thread_summary.py
@src/agent/nodes/extract_slots.py
@src/agent/nodes/session_memory_load.py
@src/agent/nodes/memory_write.py
@tests/test_agent_runs_api.py
@tests/conversation/test_service.py
@tests/memory/test_thread_summary.py
@tests/memory/test_session_memory_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <id>24-09-01</id>
  <name>Task 1: Verify legacy chat compatibility with shared helpers</name>
  <files>src/api/routers/agent.py, tests/test_agent_runs_api.py, tests/conversation/test_service.py</files>
  <read_first>
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-CONTEXT.md` decisions D-16 and D-17.
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-RESEARCH.md` open question "Should legacy chat interrupted/error assistant messages be changed now?"
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-PATTERNS.md` sections for `src/api/routers/agent.py` and legacy behavior warning.
    - `src/api/routers/agent.py`.
    - `src/api/routers/agent_runs.py`.
    - `src/conversation/service.py`.
    - `src/conversation/repository.py`.
    - `tests/test_agent_runs_api.py`.
    - `tests/conversation/test_service.py`.
  </read_first>
  <action>
    Keep legacy `/api/v1/agent/chat` compatible. If Plan 24-04 helpers are adopted in the legacy completed happy path, use `append_or_get_user_message_for_run` and `append_or_get_assistant_message_for_run` only where they preserve existing successful chat semantics. Do not silently change legacy interrupted/error assistant-message behavior unless a test explicitly documents the intended compatibility update. Add/adjust regression assertions so legacy chat still:
    - appends user/assistant messages for its existing successful path;
    - passes conversation IDs into graph config;
    - persists prompt-safe tool summaries through shared `ConversationService`;
    - updates rolling summary through `ThreadRollingSummaryService`;
    - keeps legacy tests such as `test_agent_chat_only_token_invokes_legacy_chat_with_no_tool_permissions` green.
  </action>
  <verify>
    <automated>uv run pytest tests/test_agent_runs_api.py::test_agent_chat_only_token_invokes_legacy_chat_with_no_tool_permissions tests/conversation/test_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Legacy chat focused test passes.
    - `uv run pytest tests/conversation/test_service.py -q` passes.
    - If `src/api/routers/agent.py` changes, `rg -n "append_or_get_user_message_for_run|append_or_get_assistant_message_for_run" src/api/routers/agent.py` finds helper usage only in successful completed path or documented compatibility path.
    - No Phase 24 change removes existing `/api/v1/agent/chat` route contract or auth scope.
  </acceptance_criteria>
  <done>Legacy chat remains compatible with shared conversation/tool/summary/session-memory infrastructure.</done>
</task>

<task type="auto" tdd="true">
  <id>24-09-02</id>
  <name>Task 2: Complete three-turn Agent Console smoke</name>
  <files>tests/test_agent_runs_api.py, tests/memory/test_session_memory_service.py</files>
  <read_first>
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-CONTEXT.md` decisions D-05, D-06, D-08, D-14.
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-VALIDATION.md` rows 24-W0-04 and 24-06-01.
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-PATTERNS.md` sections for three-turn smoke pattern and session memory tests.
    - `tests/test_agent_runs_api.py`.
    - `tests/memory/test_session_memory_service.py`.
    - `src/api/routers/agent_runs.py`.
    - `src/agent/nodes/extract_slots.py`.
    - `src/agent/nodes/session_memory_load.py`.
    - `src/memory/service.py`.
  </read_first>
  <action>
    Finish `test_three_turn_agent_runs_smoke_uses_slots_and_summary_context` as a provider-free integration smoke using the ASGI/SSE test client and fake graph. The three turns must prove:
    - turn 1 creates user/assistant messages and writes session slot memory for an explicit order/refund reference;
    - turn 2 same-thread follow-up can inherit trusted slot memory and still runs current tool/evidence behavior before asserting business/policy facts;
    - turn 3 same-thread follow-up can load latest prior rolling summary plus recent messages/tool summaries into prompt context;
    - final assertions verify memory context is used only for reference resolution and does not satisfy policy evidence, current business fact, approval/action authority, or replay/audit truth.
    Keep the smoke deterministic and local; do not require live model/provider credentials or browser automation.
  </action>
  <verify>
    <automated>uv run pytest tests/test_agent_runs_api.py::test_three_turn_agent_runs_smoke_uses_slots_and_summary_context -q</automated>
  </verify>
  <acceptance_criteria>
    - Three-turn smoke passes.
    - Test asserts at least three user messages and three completed assistant messages in the same conversation thread after three completed runs.
    - Test asserts at least one `thread_rolling` summary exists after completed turns and latest prior summary is loaded on a later turn.
    - Test asserts trusted session slot continuity and explicit current-turn override behavior.
    - Test includes a negative authority assertion for memory-not-evidence/business/action/replay truth.
  </acceptance_criteria>
  <done>STM-14 three-turn `/agent-runs + SSE` smoke is green and deterministic.</done>
</task>

<task type="auto">
  <id>24-09-03</id>
  <name>Task 3: Run focused Phase 24 regression gate</name>
  <files>tests/test_agent_runs_api.py, tests/conversation/test_service.py, tests/memory/test_thread_summary.py, tests/memory/test_session_memory_service.py</files>
  <read_first>
    - `.planning/phases/24-agent-runs-short-term-memory-parity/24-VALIDATION.md` full Phase 24 verification map.
    - `tests/test_agent_runs_api.py`.
    - `tests/conversation/test_service.py`.
    - `tests/memory/test_thread_summary.py`.
    - `tests/memory/test_session_memory_service.py`.
    - `.planning/LOCAL-VALIDATION-ISSUES.md` if it exists.
  </read_first>
  <action>
    Run the focused Phase 24 regression suite and lint. If any local validation failure occurs, append a Chinese entry to `.planning/LOCAL-VALIDATION-ISSUES.md` with symptom, reproduction command, evidence, current root-cause judgment, handling, remaining issue, and next entry point per `AGENTS.md`. Fix only Phase 24-owned failures. Do not broaden scope into frontend redesign, memory management UI, retention/archive controls, admin memory promotion, external action execution, policy source operations, or SSE replay UX.
  </action>
  <verify>
    <automated>uv run pytest tests/test_agent_runs_api.py tests/conversation/test_service.py tests/memory/test_thread_summary.py tests/memory/test_session_memory_service.py tests/agent/test_session_memory_integration.py tests/agent/test_required_slots.py tests/agent/context/test_assembler.py tests/agent/test_memory_evidence_boundary.py tests/agent/rag_context/test_authority_boundaries.py -q</automated>
    <automated>uv run ruff check src/ tests/</automated>
  </verify>
  <acceptance_criteria>
    - Focused Phase 24 pytest command exits 0.
    - `uv run ruff check src/ tests/` exits 0.
    - Every STM-01 through STM-14 named test from Plans 24-01 and 24-02 is green.
    - Any unresolved unrelated local failure is recorded in `.planning/LOCAL-VALIDATION-ISSUES.md` with a concrete next entry point.
  </acceptance_criteria>
  <done>STM-13 regression coverage is green for Phase 24-owned backend memory surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy `/agent/chat` -> shared memory helpers | Compatibility path may consume shared infrastructure without changing its public contract. |
| smoke fake graph -> API/SSE persistence | Deterministic test graph exercises backend memory surfaces without live provider trust. |
| regression gate -> local validation issues log | Failures become documented follow-up evidence instead of silent acceptance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering/Repudiation | full focused regression | mitigate | Duplicate/retry tests from earlier plans run in the final focused suite. |
| T-24-02 | Repudiation | final response ordering regression | mitigate | Final focused suite includes final_response-after-memory-result test. |
| T-24-03 | Spoofing/Tampering | legacy and run graph config tests | mitigate | Legacy and agent-runs config tests verify backend-trusted conversation IDs. |
| T-24-04 | Information Disclosure | three-turn smoke and service tests | mitigate | Tests assert tenant/user/thread scoping and wrong-scope fail-closed behavior. |
| T-24-05 | Elevation of Privilege/Tampering | authority-boundary regression | mitigate | Focused suite includes assembler and memory-authority tests. |
| T-24-06 | Repudiation | failure-state regression | mitigate | Focused suite includes error/cancel/interrupted no-completed-memory tests. |
</threat_model>

<must_haves>
- Legacy `/api/v1/agent/chat` focused tests remain green.
- Three-turn `/agent-runs + SSE` smoke proves slot continuity and rolling-summary context.
- Focused regression command covers STM-01 through STM-14.
- Any local validation issue is logged in `.planning/LOCAL-VALIDATION-ISSUES.md` in Chinese.
</must_haves>

<verification>
`uv run pytest tests/test_agent_runs_api.py tests/conversation/test_service.py tests/memory/test_thread_summary.py tests/memory/test_session_memory_service.py tests/agent/test_session_memory_integration.py tests/agent/test_required_slots.py tests/agent/context/test_assembler.py tests/agent/test_memory_evidence_boundary.py tests/agent/rag_context/test_authority_boundaries.py -q`

`uv run ruff check src/ tests/`
</verification>

<success_criteria>
- STM-08 legacy compatibility is verified.
- STM-13 focused regression coverage is green.
- STM-14 three-turn smoke is green and provider-free.
- No deferred/out-of-scope features are implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/24-agent-runs-short-term-memory-parity/24-09-SUMMARY.md`.
</output>
