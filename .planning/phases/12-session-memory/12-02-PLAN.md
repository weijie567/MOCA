---
phase: 12-session-memory
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/config.py
  - src/agent/state.py
  - src/agent/nodes/session_memory_load.py
  - src/agent/nodes/extract_slots.py
  - src/agent/routing.py
  - tests/agent/test_empty_session_adapter.py
  - tests/agent/test_required_slots.py
  - tests/agent/test_graph.py
  - tests/agent/test_session_memory_load.py
autonomous: true
requirements:
  - SESSION-02
  - SESSION-03
must_haves:
  truths:
    - "`session_memory_load` reads through MemoryService when enabled and an AsyncSession is available."
    - "All disabled, missing, expired, invalid, or unavailable cases preserve Phase 10 empty adapter behavior."
    - "Inherited slots can satisfy `route_after_slots` only with trusted metadata emitted by MemoryService."
    - "`slot_extraction`/slot resolution writes trusted inherited slots into resolved `AgentState.active_slots` for downstream investigation, while preserving source metadata."
    - "Current-turn explicit `extracted_slots` override inherited session slots."
    - "Inherited slots remain distinguishable from current-turn explicit slots and must not become memory-write explicit candidates."
    - "Router tests remain pure and do not call DB, Redis, LLM, or MemoryService."
  artifacts:
    - path: src/agent/nodes/session_memory_load.py
      provides: "MemoryService read integration with observable fallback"
      contains: "MemoryService"
    - path: src/agent/routing.py
      provides: "Trusted session slot metadata validation"
      exports:
        - resolve_slots_for_completeness
    - path: src/agent/nodes/extract_slots.py
      provides: "Resolved active slot state for downstream investigate/tool args"
      contains: "active_slot_metadata"
    - path: src/agent/state.py
      provides: "Run-scoped active slot source metadata field"
      contains: "active_slot_metadata"
    - path: tests/agent/test_session_memory_load.py
      provides: "Node-level enabled/disabled/fallback tests"
---

<objective>
Wire the authoritative MemoryService read path into `session_memory_load` and preserve deterministic slot inheritance safety.

Purpose: same-thread session memory becomes visible to the graph only through the existing Phase 10/11 safe adapter and router gate.
Output: feature-switched node integration, pure router inheritance tests, and graph-level same-thread continuity read coverage.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-session-memory/12-CONTEXT.md
@.planning/phases/12-session-memory/12-RESEARCH.md
@.planning/phases/12-session-memory/12-PATTERNS.md
@.planning/phases/12-session-memory/12-COVERAGE.md
@docs/contract-spec.md
@docs/eval-test-plan.md
@src/config.py
@src/agent/nodes/session_memory_load.py
@src/agent/routing.py
@src/agent/graph.py
@tests/agent/test_empty_session_adapter.py
@tests/agent/test_required_slots.py
@tests/agent/test_graph.py

<interfaces>
Existing empty adapter shape to preserve:
```python
{
    "session_memory": {
        "active_slots": {},
        "source": "empty_adapter",
        "continuity_claimed": False,
    },
    "trace_steps": [...],
}
```

  Target loaded view from MemoryService:
```python
{
    "source": "postgres_session_memory",
    "continuity_claimed": True,
    "active_slots": {"order_id": "ORD-1001"},
    "slot_metadata": {
        "order_id": {
            "source": "trusted_session_memory",
            "tenant_id": "...",
            "user_id": "...",
            "thread_id": "...",
            "fresh": True,
            "expires_at": "...",
            "source_run_id": "...",
            "compatible_intents": ["refund_troubleshooting"]
        }
    },
  }
  ```

  Resolved slot state required after `slot_extraction`:
  ```python
  {
      "extracted_slots": {"order_id": None, "refund_case_id": None},
      "active_slots": {"order_id": "ORD-1001"},
      "active_slot_metadata": {
          "order_id": {
              "source": "trusted_session_memory",
              "source_run_id": "...",
              "tenant_id": "...",
              "user_id": "...",
              "thread_id": "...",
              "explicit_current_turn": False,
          }
      },
  }
  ```

  `active_slots` is the resolved downstream working set. It may include trusted inherited slots, but `extracted_slots` remains the only current-turn explicit source for memory writes.

Config additions:
- `session_memory_enabled: bool = True`
- `session_memory_ttl_seconds: int = 1800`
- `session_memory_summary_max_chars: int = 500`

No Redis settings are added in this plan.
</interfaces>
</context>

<planning_gate>
## Spec sections covered

- `docs/contract-spec.md` §9.3-9.5 — `session_memory_load -> slot_extraction -> route_after_slots`.
- `docs/contract-spec.md` §10.2-10.4 — inherited slot rules and candidate-slot boundary.
- `docs/contract-spec.md` §13.2 — same-thread session memory inheritance and fallback.
- `docs/eval-test-plan.md` §20.1 and §21.3 — route safety and missing-slot cross-turn flow.

## Spec consistency findings

See `.planning/phases/12-session-memory/12-COVERAGE.md`. No additional 12-02-local findings after checking Phase 10 empty adapter summary, Phase 11 router summary, `12-CONTEXT.md`, and current `src/agent/routing.py`.

## Schema/migration owner

N/A: 12-02 does not add schema. It consumes the 12-01 `session_memories` service contract. Read-switch owner is 12-02 via `session_memory_enabled`, empty fallback behavior, and fallback telemetry in `session_memory_load`.

## Service/API owner

12-02 owns the graph node service seam: `src/agent/nodes/session_memory_load.py` composes `MemoryService(SessionMemoryRepository(session))`. No public API route changes in this plan.

## State/router impact

  12-02 owns loaded `state["session_memory"]` shape, router metadata validation, `active_slot_metadata`, and resolved `active_slots` handoff after `slot_extraction`. `resolve_slots_for_completeness` remains pure, deterministic, and DB/service-free.

## Required tests

- Contract tests: disabled/missing/unavailable fallback, trusted metadata validation, explicit override.
- Integration/golden tests: same-thread inherited slot can unblock a required-slot path; wrong scope/stale slots clarify.
- Eval gate: blocking route-safety gate; dataset owner is Phase 12 focused graph/router tests; failure blocks write-path execution.

## Acceptance criteria

Empty adapter fallback remains compatible with Phase 10 and all inherited-slot acceptance criteria are executable through pytest/grep.
</planning_gate>

<tasks>

<task type="auto">
  <name>Task 0: Add read-path fallback and inheritance tests</name>
    <files>tests/agent/test_session_memory_load.py, tests/agent/test_empty_session_adapter.py, tests/agent/test_required_slots.py, tests/agent/test_graph.py</files>
  <read_first>
    - tests/agent/test_empty_session_adapter.py
    - tests/agent/test_required_slots.py
    - tests/agent/test_graph.py
    - src/agent/nodes/session_memory_load.py
    - src/agent/routing.py
    - .planning/phases/12-session-memory/12-CONTEXT.md
  </read_first>
  <action>
    Add tests before implementation:
    - `test_session_memory_load_disabled_returns_empty_fallback` monkeypatches settings or config to disabled and asserts `continuity_claimed is False`, `active_slots == {}`, `source == "disabled"`, and trace step metrics include `fallback_reason="disabled"`.
    - `test_session_memory_load_without_async_session_returns_empty_fallback` passes config without `configurable.session` and asserts empty fallback with `source in {"empty_adapter","unavailable"}`.
    - `test_session_memory_load_uses_memory_service_view` injects/fakes MemoryService or repository and asserts returned state contains flattened `active_slots`, `slot_metadata`, `session_summary`, `unresolved_questions`, and `version`.
    - Extend `test_empty_session_adapter_routing` so the Phase 10 fallback shape remains accepted when MemoryService is disabled/unavailable.
      - Extend `tests/agent/test_required_slots.py` with wrong tenant, wrong user, wrong thread, expired/stale, incompatible intent, and current-turn explicit override cases. These tests must call `resolve_slots_for_completeness` and `route_after_slots` directly without DB/service.
      - Extend `tests/agent/test_graph.py` with a graph test where a same-thread inherited `order_id` from `session_memory_load` allows a refund path to investigate when the current turn says "what about this refund?" and no current-turn slot is extracted. The fake memory must include trusted metadata, the graph must expose resolved `active_slots.order_id`, and the fake business tool/investigate seam must receive/use `ORD-SESSION-001`.
      - Add an explicit distinction assertion: inherited `active_slots.order_id` is present for investigation, while `extracted_slots.order_id` remains absent/null and is not eligible as a current-turn explicit memory-write candidate.
  </action>
  <verify>
    <automated>uv run pytest tests/agent/test_session_memory_load.py tests/agent/test_empty_session_adapter.py tests/agent/test_required_slots.py tests/agent/test_graph.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "disabled|unavailable|fallback_reason|continuity_claimed" tests/agent/test_session_memory_load.py tests/agent/test_empty_session_adapter.py` finds fallback coverage.
    - `rg -n "wrong.*tenant|wrong.*user|wrong.*thread|expired|incompatible|explicit.*override" tests/agent/test_required_slots.py` finds inheritance safety negatives.
      - `rg -n "trusted_session_memory|session_memory_load|same-thread|same_thread|active_slots|extracted_slots|ORD-SESSION-001" tests/agent/test_graph.py` finds same-thread graph read coverage and explicit/inherited distinction.
  </acceptance_criteria>
  <done>Read-path tests define enabled, disabled, fallback, inheritance, and graph continuity behavior.</done>
</task>

<task type="auto">
  <name>Task 1: Add session memory config and node read integration</name>
  <files>src/config.py, src/agent/nodes/session_memory_load.py, tests/agent/test_session_memory_load.py, tests/agent/test_empty_session_adapter.py</files>
  <read_first>
    - src/config.py
    - src/agent/nodes/session_memory_load.py
    - src/memory/service.py
    - tests/agent/test_session_memory_load.py
    - tests/agent/test_empty_session_adapter.py
  </read_first>
  <action>
    In `src/config.py`, add Phase 12 settings:
    - `session_memory_enabled: bool = True`
    - `session_memory_ttl_seconds: int = 1800`
    - `session_memory_summary_max_chars: int = 500`

    Update `src/agent/nodes/session_memory_load.py`:
    - Read `configurable = config.get("configurable") or {}`.
    - Read `session = configurable.get("session")`.
    - If `settings.session_memory_enabled is False`, return the empty fallback view with `source="disabled"` and `fallback_reason="disabled"`.
    - If `session is None`, return the empty fallback view with `source="unavailable"` and `fallback_reason="missing_async_session"`.
    - Instantiate `MemoryService(SessionMemoryRepository(session), enabled=settings.session_memory_enabled)`.
    - Call `load_session_memory(tenant_id=state["tenant_id"], user_id=state["user_id"], thread_id=state["thread_id"], current_intent=state.get("primary_intent") or state.get("current_intent"))`.
    - Convert the typed view to JSON-safe dict and return it under `session_memory`.
    - Preserve trace step append with node `session_memory_load`; `metrics_json` must include `source`, `continuity_claimed`, `fallback_reason`, `slot_count`, and `version`.
    - Catch all service/repository exceptions and return empty fallback `source="unavailable"` with safe node error metadata. Do not expose DB exception text in user-facing state.
  </action>
  <verify>
    <automated>uv run pytest tests/agent/test_session_memory_load.py tests/agent/test_empty_session_adapter.py -q && uv run ruff check src/config.py src/agent/nodes/session_memory_load.py tests/agent/test_session_memory_load.py tests/agent/test_empty_session_adapter.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "session_memory_enabled|session_memory_ttl_seconds|session_memory_summary_max_chars" src/config.py` finds config keys.
    - `rg -n "MemoryService|SessionMemoryRepository|fallback_reason|missing_async_session|slot_count|continuity_claimed" src/agent/nodes/session_memory_load.py` finds integration and fallback telemetry.
    - The automated pytest and ruff command passes.
  </acceptance_criteria>
  <done>`session_memory_load` is feature-switched and reads through MemoryService while preserving empty fallback semantics.</done>
</task>

<task type="auto">
    <name>Task 2: Harden router trusted metadata checks and resolved slot handoff</name>
    <files>src/agent/state.py, src/agent/routing.py, src/agent/nodes/extract_slots.py, tests/agent/test_required_slots.py, tests/agent/test_graph.py</files>
  <read_first>
      - src/agent/routing.py
      - src/agent/state.py
      - src/agent/nodes/extract_slots.py
      - tests/agent/test_required_slots.py
      - src/memory/schemas.py
    - docs/contract-spec.md Section 10.2
    - docs/contract-spec.md Section 13.2
  </read_first>
  <action>
      Update `resolve_slots_for_completeness` and helper validation only as needed:
      - Keep current-turn `extracted_slots` as the first source and final override.
      - Accept inherited slot metadata only when `source == "trusted_session_memory"`, `tenant_id`, `user_id`, and `thread_id` all match state, slot is fresh or `expires_at` is in the future, and the current intent appears in `compatible_intents` or `intent_compatible is True`.
      - Missing metadata, wrong tenant, wrong user, wrong thread, stale/expired metadata, incompatible intent, empty values, malformed date values, or `continuity_claimed=True` without metadata must ignore the inherited slot and route missing slots to `clarification_gate`.
      - Do not read top-level `active_slots` for required-slot completeness.
      - Do not call MemoryService, SQLAlchemy, Redis, LLM, network, or settings from router code.
      Add `active_slot_metadata: dict[str, Any] | None` to `AgentState` as a run-scoped field reset each turn. Then update `src/agent/nodes/extract_slots.py` or a pure helper it calls so successful slot extraction writes:
      - `extracted_slots`: current-turn validated explicit slots only, including nulls/omissions as produced by the slot extractor.
      - `active_slots`: resolved slots from current-turn explicit slots plus trusted inherited `session_memory.active_slots` accepted by the same validation rules.
      - `active_slot_metadata`: per-slot metadata with `source="current_turn"` for current explicit values and `source="trusted_session_memory"` for inherited values.
      - Never merge stale top-level `state["active_slots"]` into the new run's resolved set.
      This handoff is required so downstream `investigate` and business tools receive the inherited identifier after `route_after_slots` passes, while `memory_write` can still tell explicit slots from inherited slots.
  </action>
  <verify>
      <automated>uv run pytest tests/agent/test_required_slots.py tests/agent/test_graph.py -q && uv run ruff check src/agent/state.py src/agent/routing.py src/agent/nodes/extract_slots.py tests/agent/test_required_slots.py tests/agent/test_graph.py</automated>
  </verify>
  <acceptance_criteria>
      - `rg -n "trusted_session_memory|expires_at|compatible_intents|intent_compatible|tenant_id|thread_id" src/agent/routing.py tests/agent/test_required_slots.py` finds trusted metadata checks and tests.
      - `rg -n "active_slot_metadata" src/agent/state.py src/agent/nodes/extract_slots.py tests/agent/test_graph.py` finds state contract and resolved slot source tracking.
      - `rg -n "SessionMemoryRepository|MemoryService|sqlalchemy|redis|ChatOpenAI|settings" src/agent/routing.py` returns no router dependency.
      - `rg -n "state\\.get\\(\"active_slots\"\\)|state\\.get\\('active_slots'\\)" src/agent/routing.py` returns no top-level active slot read inside completeness resolution.
      - `rg -n "active_slot_metadata|trusted_session_memory|current_turn|resolve_slots_for_completeness" src/agent/nodes/extract_slots.py tests/agent/test_graph.py` finds resolved handoff and source tracking.
      - The automated pytest and ruff command passes.
  </acceptance_criteria>
  <done>Router inheritance remains deterministic, fail-closed, and service-free.</done>
</task>

<task type="auto">
  <name>Task 3: Add graph read continuity coverage</name>
  <files>tests/agent/test_graph.py, tests/agent/test_session_memory_load.py</files>
  <read_first>
    - tests/agent/test_graph.py
    - tests/agent/test_session_memory_load.py
    - tests/conftest.py
    - src/agent/graph.py
    - src/agent/nodes/session_memory_load.py
  </read_first>
  <action>
    Add focused graph tests using monkeypatch seams:
    - Patch `session_memory_load` or MemoryService so the graph receives a trusted session memory view for the same tenant/user/thread.
    - Configure slot extraction to return no current `order_id`.
      - Assert `route_after_slots` reaches `investigate`, resolved `active_slots.order_id == "ORD-SESSION-001"`, `business_context.facts.order.order_no == "ORD-SESSION-001"`, and the final response is present.
      - Assert `extracted_slots` did not fabricate `order_id`, and source metadata distinguishes inherited memory from current-turn explicit input.
    - Add a companion wrong-thread or stale inherited slot case asserting no tool calls are made and the graph reaches `clarification_gate`.
    Keep these tests deterministic with fake LLM and fake tool manager, matching existing `tests/agent/test_graph.py` style.
  </action>
  <verify>
    <automated>uv run pytest tests/agent/test_graph.py tests/agent/test_required_slots.py tests/agent/test_session_memory_load.py -q</automated>
  </verify>
  <acceptance_criteria>
      - `rg -n "ORD-SESSION-001|trusted_session_memory|active_slot_metadata|extracted_slots|clarification_gate|wrong.*thread|stale" tests/agent/test_graph.py tests/agent/test_session_memory_load.py` finds graph continuity and negative cases.
    - The automated pytest command passes.
  </acceptance_criteria>
  <done>Full graph coverage proves safe same-thread read continuity and fail-closed negative routing.</done>
</task>

</tasks>

<coverage_matrix>
| Spec area | Covered by phase | Required tests | Migration owner | Gap / owner gate | Read-switch / rollback owner | Eval gate | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| SESSION-02 read inheritance | 12-02 loads MemoryService view and router accepts only trusted metadata; explicit current slots override inherited slots | `tests/agent/test_session_memory_load.py`, `tests/agent/test_required_slots.py`, graph continuity tests | N/A: schema owned by 12-01 | 12-04 owns cross-scope/stale full matrix | `session_memory_enabled=false` returns empty adapter | Blocking route-safety tests; failure blocks write path | COVERED |
| SESSION-03 read fallback/read-switch | 12-02 handles disabled, missing AsyncSession, missing/expired/unavailable memory with observable fallback | node fallback tests and empty adapter regression tests | N/A | None | 12-02 owns config switch, fallback telemetry, rollback to Phase 10 empty adapter | Blocking fallback tests; failure blocks execution | COVERED |
| State/router impact | 12-02 writes `state["session_memory"]` loaded view and hardens `resolve_slots_for_completeness` metadata checks | pure router tests with no DB/service/LLM/network | N/A | None | Empty adapter fallback; router stays service-free | Blocking deterministic router tests; failure blocks execution | COVERED |
| Integration golden flow | 12-02 proves same-thread trusted memory can unblock slot gate and wrong/stale memory clarifies | graph tests with fake LLM/tool manager | N/A | 12-04 owns API/persistence E2E continuity | Disable session memory to clarify instead | Blocking focused graph tests; failure blocks write path | COVERED |
</coverage_matrix>

<rollback_non_goals>
| Topic | Boundary |
|-------|----------|
| Rollback/fallback | Set `SESSION_MEMORY_ENABLED=false` or equivalent env override to return empty fallback behavior without graph rewiring. |
| Deferred/non-goals | No memory writes, no Redis cache, no long-term/case memory, no approval/action safety changes. |
</rollback_non_goals>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MemoryService view -> router | Only typed metadata may become inherited slot trust. |
| config/session -> graph node | Missing DB session or disabled config must fail closed to empty memory. |
| inherited slot -> business tool args | Wrong-scope, stale, or incompatible inherited values must not reach investigation tools. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Information Disclosure | slot inheritance | mitigate | Tenant/user/thread metadata checks gate every inherited slot. |
| T-12-06 | Tampering | inherited slot metadata | mitigate | Missing/malformed metadata fails closed; current explicit slots override inherited values. |
| T-12-07 | Denial of Service | memory unavailable | mitigate | Node catches service errors and continues with empty fallback view. |
| T-12-08 | Elevation of Privilege | router | mitigate | Router remains service-free and returns only fixed route keys. |
</threat_model>

<verification>
Run:
```bash
  uv run pytest tests/agent/test_session_memory_load.py tests/agent/test_empty_session_adapter.py tests/agent/test_required_slots.py tests/agent/test_graph.py -q
  uv run ruff check src/config.py src/agent/state.py src/agent/nodes/session_memory_load.py src/agent/nodes/extract_slots.py src/agent/routing.py tests/agent/test_session_memory_load.py tests/agent/test_required_slots.py tests/agent/test_graph.py
  ```
</verification>

<success_criteria>
- `session_memory_load` uses MemoryService only when enabled and session is present.
- Empty adapter fallback remains observable and compatible with Phase 10 behavior.
  - Inherited session slots pass completeness only with trusted scope/freshness/intent metadata.
  - Trusted inherited slots are written into resolved `active_slots` for downstream investigation/tool args.
  - Resolved slot metadata distinguishes current explicit slots from inherited session slots.
  - Current-turn explicit slots override inherited slots.
- Wrong-scope, stale, incompatible, or metadata-free slots route to clarification.
</success_criteria>

<output>
After completion, create `.planning/phases/12-session-memory/12-02-SUMMARY.md`.
</output>
