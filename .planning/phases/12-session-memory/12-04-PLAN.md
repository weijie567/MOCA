---
phase: 12-session-memory
plan: 04
type: execute
wave: 4
depends_on:
  - 12-01
  - 12-02
  - 12-03
files_modified:
  - tests/memory/test_session_memory_concurrency.py
  - tests/memory/test_session_memory_isolation.py
  - tests/agent/test_session_memory_integration.py
  - tests/agent/test_required_slots.py
  - tests/agent/test_memory_evidence_boundary.py
  - tests/agent/test_graph.py
  - src/memory/service.py
  - src/agent/routing.py
  - src/agent/nodes/memory_write.py
autonomous: true
requirements:
  - SESSION-01
  - SESSION-02
  - SESSION-03
must_haves:
  truths:
    - "Concurrent same-thread updates do not lose active slots, session summary, unresolved questions, last intent, or last business context refs."
    - "Cross-thread, cross-user, and cross-tenant session memory isolation is blocking."
    - "Stale, wrong-scope, incompatible, or unconfirmed inherited slots fail closed."
    - "Current-turn explicit slots override inherited slots."
    - "Session memory cannot satisfy policy evidence, citation, risk, approval, action authorization, or recommendation evidence requirements."
    - "PostgreSQL-only implementation passes; Redis fallback is not required unless Redis is deliberately introduced later."
  artifacts:
    - path: tests/memory/test_session_memory_concurrency.py
      provides: "CAS conflict and deterministic merge tests"
    - path: tests/memory/test_session_memory_isolation.py
      provides: "tenant/user/thread isolation tests"
    - path: tests/agent/test_memory_evidence_boundary.py
      provides: "negative tests proving memory is not policy evidence or action authority"
---

<objective>
Harden Phase 12 with the blocking safety and regression matrix.

Purpose: prove the PostgreSQL-only session memory path is correct before any optional cache work is considered.
Output: concurrency, isolation, stale/override, graph continuity, fallback, and evidence-boundary tests, plus implementation fixes if those tests expose gaps.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-session-memory/12-CONTEXT.md
@.planning/phases/12-session-memory/12-RESEARCH.md
@.planning/phases/12-session-memory/12-PATTERNS.md
@.planning/phases/12-session-memory/12-COVERAGE.md
@docs/contract-spec.md
@docs/eval-test-plan.md
@src/memory/service.py
@src/agent/routing.py
@src/agent/nodes/session_memory_load.py
@src/agent/nodes/memory_write.py
@src/agent/nodes/investigate.py
@src/agent/nodes/generate_recommendation.py
@tests/conftest.py
@tests/agent/test_required_slots.py
@tests/agent/test_graph.py
@tests/knowledge/test_citation_membership.py

<interfaces>
  Blocking Phase 12 test classes:
  - CAS/concurrency: no lost updates and no silent last-write-wins.
  - Field-level merge: slots, unresolved questions, summary, last intent, and business context refs use explicit deterministic merge/conflict rules.
  - Scope isolation: same tenant/user/thread only.
  - Freshness/compatibility: stale or incompatible inherited slots ignored.
  - Explicit override: current turn wins over inherited values.
  - Missing-slot cross-turn: unresolved question carryover works only in the same tenant/user/thread and still reruns slot/business/evidence checks.
  - Fallback: disabled/unavailable/missing memory keeps ordinary routing behavior.
- Evidence boundary: memory never becomes `EvidenceRefV1`, `policy_evidence`, recommendation citation, approval evidence, or action authorization.
- Redis boundary: since Redis is not implemented, tests assert no Redis correctness dependency. If Redis appears in code during this phase, fallback tests become mandatory before completion.
</interfaces>
</context>

<planning_gate>
## Spec sections covered

- `docs/contract-spec.md` §10.2 — slot inheritance scope/freshness/compatibility and explicit override.
- `docs/contract-spec.md` §13.2 — CAS miss behavior and memory-is-not-authority boundary.
- `docs/contract-spec.md` §18.1 — `session_memories` CAS constraints and no silent last-write-wins.
- `docs/contract-spec.md` memory lifecycle rules around PII classification before write.
- `docs/eval-test-plan.md` §20.1 session memory contract row and §21.3 missing-slot cross-turn golden flow.

## Spec consistency findings

See `.planning/phases/12-session-memory/12-COVERAGE.md`. 12-04 is the blocking verification plan for SCF-12-01 and SCF-12-02: it must prove post-response memory write is non-blocking and PII/prohibited content is blocked or skipped before persistence.

## Schema/migration owner

N/A: 12-04 verifies the 12-01 schema and 12-02/12-03 integration; it owns no new migrations.

## Service/API owner

12-04 owns test-driven hardening across `MemoryService`, router helpers, `session_memory_load`, and post-response `memory_write` hooks.

## State/router impact

Verifies loaded session metadata cannot leak across tenant/user/thread and cannot satisfy routes when stale, incompatible, malformed, or overridden by current explicit slots.

## Required tests

- Contract tests: CAS concurrency, explicit conflict returns conflict/fallback, PII/prohibited skip, memory-is-not-evidence.
- Integration/golden tests: same-thread continuity, cross-scope isolation, disabled/unavailable fallback, missing-slot cross-turn flow.
- Eval gate: blocking Phase 12 session memory route-safety gate; owner Phase 12; dataset/version/hash is the focused pytest matrix and current commit hash; failure blocks Phase 12 completion.

## Acceptance criteria

Every Phase 12 safety invariant must be executable through the focused verification commands; Redis absence remains acceptable.
</planning_gate>

<tasks>

<task type="auto">
  <name>Task 0: Add CAS and deterministic merge concurrency tests</name>
  <files>tests/memory/test_session_memory_concurrency.py, src/memory/service.py</files>
  <read_first>
    - tests/memory/test_session_memory_service.py
    - src/memory/service.py
    - src/memory/repository.py
    - docs/contract-spec.md Section 13.2
    - docs/contract-spec.md Section 18.1
  </read_first>
  <action>
      Create concurrency tests using the existing async database fixture:
      - Start from one active session memory row with `version=1`.
      - Run two concurrent writes for the same tenant/user/thread with different explicit slots, summaries, unresolved questions, last intents, and business refs.
      - Assert final row `version >= 2`, the final envelope preserves both non-conflicting slots, and every non-slot field follows the 12-01 field-level merge policy: unresolved questions are deduped union, business refs are key-merged or conflicted, session summary preserves both safe summaries or explicitly skips/conflicts when the cap would lose content, and last intent is updated or preserved only with an observable reason.
      - Add a conflicting explicit slot case where two runs write different values for the same slot from current-turn explicit input. Assert result is `status="conflict"` or an explicit fallback/skip result with `reason_code="explicit_slot_conflict"`; do not allow deterministic precedence to merge or overwrite conflicting current-turn explicit values from different runs.
      - Add scalar/JSON conflict cases for `session_summary`, `last_intent`, and `last_business_context_refs_json` proving the service never silently overwrites a concurrent value without a conflict/fallback/merge warning reason.
      - Add a stale expected version test that reloads latest state and returns `merged_after_conflict` when merge is safe.
      Fix `src/memory/service.py` if these tests expose shallow merge or stale overwrite behavior.
  </action>
  <verify>
    <automated>uv run pytest tests/memory/test_session_memory_concurrency.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "asyncio|gather|expected_version|merged_after_conflict|conflict|version" tests/memory/test_session_memory_concurrency.py` finds real concurrency/CAS tests.
      - `rg -n "last-write|last_write|overwrite|session_summary|unresolved_questions|last_intent|last_business_context_refs|merge_warning|reason_code" tests/memory/test_session_memory_concurrency.py src/memory/service.py` finds explicit field-level guards against silent last-write-wins.
    - The automated pytest command passes.
  </acceptance_criteria>
  <done>CAS conflict behavior is covered with real async tests and deterministic merge expectations.</done>
</task>

<task type="auto">
  <name>Task 1: Add scope isolation and stale slot matrix</name>
  <files>tests/memory/test_session_memory_isolation.py, tests/agent/test_required_slots.py, src/memory/service.py, src/agent/routing.py</files>
  <read_first>
    - tests/agent/test_required_slots.py
    - src/memory/service.py
    - src/agent/routing.py
    - .planning/phases/12-session-memory/12-CONTEXT.md
  </read_first>
  <action>
    Add isolation and freshness tests:
    - Same tenant/user/thread can load memory and produce `continuity_claimed=True`.
    - Same tenant/user but different thread cannot load active slots.
    - Same tenant/thread but different user cannot load active slots.
    - Same user/thread text but different tenant cannot load active slots.
    - Expired row returns fallback/no continuity.
    - Expired slot inside a non-expired row is omitted from loaded `active_slots`.
    - Slot with incompatible `compatible_intents` is omitted.
    - Router ignores inherited slot metadata with wrong tenant, wrong user, wrong thread, expired `expires_at`, missing `compatible_intents`, malformed `expires_at`, or metadata-free `continuity_claimed=True`.
    - Current explicit `extracted_slots.order_id="ORD-CURRENT"` overrides inherited `order_id="ORD-SESSION"`.
  </action>
  <verify>
    <automated>uv run pytest tests/memory/test_session_memory_isolation.py tests/agent/test_required_slots.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "different.*thread|different.*user|different.*tenant|expired|incompatible|continuity_claimed" tests/memory/test_session_memory_isolation.py tests/agent/test_required_slots.py` finds the isolation matrix.
    - `rg -n "ORD-CURRENT|ORD-SESSION|override" tests/agent/test_required_slots.py` finds explicit override coverage.
    - The automated pytest command passes.
  </acceptance_criteria>
  <done>Session memory inheritance is scoped, fresh, compatible, and override-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Add graph/API same-thread continuity and fallback regressions</name>
  <files>tests/agent/test_session_memory_integration.py, tests/agent/test_graph.py, tests/test_agent_runs_api.py, src/agent/nodes/session_memory_load.py, src/agent/nodes/memory_write.py</files>
  <read_first>
    - tests/agent/test_graph.py
    - tests/test_agent_runs_api.py
    - tests/conftest.py
    - src/agent/graph.py
    - src/agent/nodes/session_memory_load.py
    - src/agent/nodes/memory_write.py
    - docs/eval-test-plan.md Section 21.3
  </read_first>
  <action>
      Add integration coverage:
      - Turn 1 missing-slot golden: same tenant/user/thread user says "帮我看看这笔退款为什么没到账"; graph routes to `clarification_gate` and memory write stores unresolved question metadata for `["refund_case_id","order_id"]` without treating it as evidence or authority.
      - Turn 2 same tenant/user/thread user says "订单是 ORD-1001"; graph uses the unresolved question context plus current explicit slot, reruns slot/business/policy path, and reaches investigation. `active_slots.order_id` source is current turn, not inherited.
      - Turn 1 same tenant/user/thread: user provides `ORD-1001`; after final response/memory_write, row stores `order_id`.
      - Turn 2 same tenant/user/thread: user says "what about that refund?" with no explicit slot; graph inherits `order_id` through session memory and reaches investigation.
    - Turn 2 different thread/user/tenant: same vague text routes to clarification and does not call business tools with inherited slot.
    - Disabled session memory: same vague text routes to clarification with telemetry and no inherited slots.
    - Memory unavailable/service exception: graph continues with current-turn slots or clarification; final response path does not crash.
    Use fake LLM/tool-manager seams where possible; use DB fixture only where persistence behavior is under test.
  </action>
  <verify>
    <automated>uv run pytest tests/agent/test_session_memory_integration.py tests/agent/test_graph.py tests/test_agent_runs_api.py -q</automated>
  </verify>
  <acceptance_criteria>
      - `rg -n "missing.*slot|unresolved.*question|订单是 ORD-1001|same.*thread|different.*thread|different.*user|different.*tenant|disabled|unavailable|ORD-1001" tests/agent/test_session_memory_integration.py tests/agent/test_graph.py tests/test_agent_runs_api.py` finds integration paths.
    - `rg -n "session_memory_load|memory_write|continuity_claimed|fallback_reason" tests/agent/test_session_memory_integration.py` finds read/write and fallback assertions.
    - The automated pytest command passes.
  </acceptance_criteria>
  <done>End-to-end graph/API behavior proves same-thread continuity and cross-scope fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Add memory-is-not-evidence/action-authority negative tests</name>
  <files>tests/agent/test_memory_evidence_boundary.py, tests/agent/test_graph.py, src/agent/nodes/memory_write.py, src/memory/service.py</files>
  <read_first>
    - src/knowledge/schemas.py
    - src/agent/nodes/investigate.py
    - src/agent/nodes/generate_recommendation.py
    - src/agent/nodes/assess_risk_and_approval.py
    - src/agent/nodes/execute_action.py
    - src/agent/nodes/memory_write.py
    - docs/contract-spec.md Section 13
    - docs/eval-test-plan.md Session memory contract row
  </read_first>
  <action>
    Add negative tests proving:
    - `src/memory` cannot import or instantiate `EvidenceRefV1`.
    - Loaded `session_memory` never populates `policy_evidence`, `retrieved_evidence.evidence_refs`, `evidence_refs`, recommendation citations, approval result, action result, or proposed action.
    - A recommendation/citation path with only session memory and no KnowledgeService evidence returns insufficient/no evidence rather than a cited recommendation.
    - High-risk action target values inherited from session memory still require current business context and policy evidence before risk/approval/action path; memory alone cannot authorize execution or draft creation.
    - `memory_write` candidate builder excludes policy conclusions, risk decisions, approval decisions, action authorization, raw tool payload, and durable preferences.
    - PII/prohibited content tests prove raw PII-heavy summary, raw prompt/tool payload, and prohibited identifiers are not written to `session_memories`; result must expose `decision="skip"` or safe redaction with `reason_code in {"pii_blocked","sensitive_redacted","raw_payload_blocked"}`.
    If a code path currently promotes memory into evidence/authorization fields, fix it in the narrowest owner module.
  </action>
  <verify>
    <automated>uv run pytest tests/agent/test_memory_evidence_boundary.py tests/agent/test_graph.py -q && uv run ruff check tests/agent/test_memory_evidence_boundary.py src/memory src/agent/nodes/memory_write.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "EvidenceRefV1|policy_evidence|retrieved_evidence|evidence_refs|approval_result|action_result|proposed_action|authorize|authorization|pii_blocked|sensitive_redacted|raw_payload_blocked|prohibited" tests/agent/test_memory_evidence_boundary.py` finds boundary and PII blocked coverage.
    - `rg -n "from src\\.knowledge\\.schemas import EvidenceRefV1|EvidenceRefV1" src/memory src/agent/nodes/memory_write.py` returns no imports/usages.
    - The automated pytest and ruff command passes.
  </acceptance_criteria>
  <done>Tests prove session memory is contextual continuity only and cannot become evidence or action authority.</done>
</task>

<task type="auto">
  <name>Task 4: Run focused Phase 12 verification and fix narrow failures</name>
  <files>src/memory/service.py, src/agent/routing.py, src/agent/nodes/session_memory_load.py, src/agent/nodes/memory_write.py, tests/memory, tests/agent</files>
  <read_first>
    - .planning/phases/12-session-memory/12-01-SUMMARY.md
    - .planning/phases/12-session-memory/12-02-SUMMARY.md
    - .planning/phases/12-session-memory/12-03-SUMMARY.md
    - tests/memory/test_session_memory_concurrency.py
    - tests/memory/test_session_memory_isolation.py
    - tests/agent/test_session_memory_integration.py
    - tests/agent/test_memory_evidence_boundary.py
  </read_first>
  <action>
    Run the full focused Phase 12 verification command set. Fix only failures in Phase 12-owned files or tests:
    - `uv run pytest tests/memory tests/agent/test_session_memory_load.py tests/agent/test_memory_write_node.py tests/agent/test_required_slots.py tests/agent/test_session_memory_integration.py tests/agent/test_memory_evidence_boundary.py -q --tb=short`
    - `uv run pytest tests/agent/test_graph.py tests/test_agent_runs_api.py tests/agent/test_events.py -q --tb=short`
    - `uv run ruff check src/memory src/agent/nodes/session_memory_load.py src/agent/nodes/memory_write.py src/agent/routing.py tests/memory tests/agent/test_session_memory_load.py tests/agent/test_memory_write_node.py tests/agent/test_session_memory_integration.py tests/agent/test_memory_evidence_boundary.py`
    Do not add Redis to make tests pass. If Redis appears in code during prior tasks, stop and route to `12-05` decision gate before adding fallback tests.
  </action>
  <verify>
    <automated>uv run pytest tests/memory tests/agent/test_session_memory_load.py tests/agent/test_memory_write_node.py tests/agent/test_required_slots.py tests/agent/test_session_memory_integration.py tests/agent/test_memory_evidence_boundary.py -q --tb=short && uv run pytest tests/agent/test_graph.py tests/test_agent_runs_api.py tests/agent/test_events.py -q --tb=short && uv run ruff check src/memory src/agent/nodes/session_memory_load.py src/agent/nodes/memory_write.py src/agent/routing.py tests/memory tests/agent/test_session_memory_load.py tests/agent/test_memory_write_node.py tests/agent/test_session_memory_integration.py tests/agent/test_memory_evidence_boundary.py</automated>
  </verify>
  <acceptance_criteria>
    - All focused pytest commands pass.
    - Ruff passes for Phase 12-owned files.
    - `rg -n "redis|Redis" src/memory src/agent/nodes/session_memory_load.py src/agent/nodes/memory_write.py` returns no Redis usage unless `12-05` explicitly accepted optional cache work.
  </acceptance_criteria>
  <done>Phase 12 PostgreSQL-only safety matrix passes without introducing Redis.</done>
</task>

</tasks>

<coverage_matrix>
| Spec area | Covered by phase | Required tests | Migration owner | Gap / owner gate | Read-switch / rollback owner | Eval gate | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
  | SESSION-01 CAS/concurrency | 12-04 blocks completion on async same-thread concurrency, stale expected version, safe merge, and explicit conflict behavior | `tests/memory/test_session_memory_concurrency.py` | Schema owned by 12-01 | None | Disable writes/read fallback if needed | Blocking CAS gate; owner Phase 12; failure blocks completion | COVERED |
| SESSION-02 inheritance safety | 12-04 covers same-thread positive, cross-thread/user/tenant negative, stale/incompatible exclusion, explicit override | `tests/memory/test_session_memory_isolation.py`, `tests/agent/test_required_slots.py` | N/A | None | `session_memory_enabled=false` empty adapter fallback | Blocking route-safety gate; owner Phase 12; failure blocks completion | COVERED |
| SESSION-03 fallback/non-authority | 12-04 covers disabled/unavailable fallback and memory-is-not-evidence/action-authority negatives | `tests/agent/test_session_memory_integration.py`, `tests/agent/test_memory_evidence_boundary.py` | N/A | None | Disable read/write; KnowledgeService remains evidence owner | Blocking negative safety gate; owner Phase 12; failure blocks completion | COVERED |
| PII/prohibited session writes | 12-04 covers raw PII-heavy summary, raw prompt/tool payload, and prohibited identifiers not being persisted | `tests/agent/test_memory_evidence_boundary.py`, `tests/memory` service tests | N/A for Phase 12 schema beyond decision/result fields | Full memory identity/tombstones/review workflow owner Phase 16 | Disable writes or skip prohibited candidates | Blocking PII write safety gate; failure blocks write-path completion | COVERED |
| Redis boundary | 12-04 verifies PostgreSQL-only implementation passes and Redis absence is acceptable | grep/no Redis usage plus 12-05 decision gate | N/A | Optional cache owner 12-05, default skip | PostgreSQL remains authoritative | Non-blocking unless Redis code appears | DEFERRED_WITH_OWNER |
</coverage_matrix>

<rollback_non_goals>
| Topic | Boundary |
|-------|----------|
| Rollback/fallback | Disable session memory read/write and keep Phase 10 empty adapter behavior; migration can be downgraded if necessary. |
| Deferred/non-goals | No Redis implementation, no long-term/case memory, no memory_identity/tombstones, no approval/action/replay V3 expansion beyond memory write event additions. |
</rollback_non_goals>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent run -> session row | CAS protects row state from stale writers. |
| scope metadata -> inherited slot | Scope mismatch must prevent inheritance. |
| memory context -> evidence/action | Memory cannot satisfy policy evidence, approval, risk, or action authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-13 | Tampering | concurrent memory writes | mitigate | Async concurrency tests and CAS merge/conflict behavior are blocking. |
| T-12-14 | Information Disclosure | cross-scope continuity | mitigate | Isolation tests cover thread, user, and tenant boundaries. |
| T-12-15 | Elevation of Privilege | memory evidence boundary | mitigate | Negative tests assert memory cannot populate evidence, approval, action, or citation fields. |
| T-12-16 | Denial of Service | memory unavailable | mitigate | Graph/API fallback tests prove ordinary routing continues with current-turn slots or clarification. |
| T-12-17 | Information Disclosure | session write content | mitigate | PII/prohibited/raw payload tests prove unsafe content is skipped or redacted before persistence. |
</threat_model>

<verification>
Run:
```bash
uv run pytest tests/memory tests/agent/test_session_memory_load.py tests/agent/test_memory_write_node.py tests/agent/test_required_slots.py tests/agent/test_session_memory_integration.py tests/agent/test_memory_evidence_boundary.py -q --tb=short
uv run pytest tests/agent/test_graph.py tests/test_agent_runs_api.py tests/agent/test_events.py -q --tb=short
uv run ruff check src/memory src/agent/nodes/session_memory_load.py src/agent/nodes/memory_write.py src/agent/routing.py tests/memory tests/agent/test_session_memory_load.py tests/agent/test_memory_write_node.py tests/agent/test_session_memory_integration.py tests/agent/test_memory_evidence_boundary.py
```
</verification>

<success_criteria>
  - Concurrent same-thread writes do not silently lose any required session fields.
  - Field-level merge/conflict rules are enforced for active slots, summary, unresolved questions, last intent, and business refs.
  - Same-thread continuity works and cross-thread/user/tenant leaks are blocked.
  - Missing-slot unresolved question carryover works only same-thread and still reruns slot/business/evidence checks when the user supplies the missing value.
- Stale, wrong-scope, incompatible, metadata-free, or unconfirmed inherited slots fail closed.
- Current explicit slots override inherited values.
- Session memory cannot satisfy policy evidence, citation, risk, approval, action authorization, or recommendation evidence.
- PII/prohibited/raw prompt/tool payload content is not written to session memory; skipped/redacted writes expose decision and reason code.
- PostgreSQL-only implementation passes without Redis code.
</success_criteria>

<output>
After completion, create `.planning/phases/12-session-memory/12-04-SUMMARY.md`.
</output>
