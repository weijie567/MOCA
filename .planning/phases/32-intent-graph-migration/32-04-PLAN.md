---
phase: 32-intent-graph-migration
plan: 32-04
type: execute
wave: 3
depends_on:
  - 32-01
  - 32-02
  - 32-03
files_modified:
  - src/agent/merchant_context.py
  - src/agent/state.py
  - src/agent/nodes/receive_request.py
  - src/agent/trace.py
  - src/api/routers/agent_runs.py
  - src/api/routers/traces.py
  - src/repositories/trace_repo.py
  - src/api/schemas/agent_runs.py
  - src/api/schemas/approvals.py
  - tests/agent/test_trace.py
  - tests/test_agent_runs_api.py
  - tests/test_trace_api.py
  - tests/replay/test_replay_api.py
  - tests/architecture/test_trusted_context_boundaries.py
autonomous: true
requirements:
  - APF-11
requirements_addressed:
  - APF-11
user_setup: []
must_haves:
  truths:
    - "Trace, SSE, trace API, and eval-facing summaries expose target canonical graph names while preserving legacy implementation node names."
    - "Target merchant context status is one of `resolved`, `deferred`, `unavailable`, or `not_applicable` and is safe evidence/status metadata only."
    - "AgentRun, trace, and replay visibility remain owner/admin-only; manager/supervisor-style access is not broadened."
  artifacts:
    - path: "src/agent/trace.py"
      provides: "Target graph projection in trace summary and safe target merchant-context summary"
    - path: "src/agent/merchant_context.py"
      provides: "Safe target merchant-context status projection helper"
    - path: "src/api/routers/agent_runs.py"
      provides: "SSE/API projection fields without changing authorization guards"
    - path: "src/api/routers/traces.py"
      provides: "Trace/replay API target-name projection and unchanged owner/admin visibility"
    - path: "src/repositories/trace_repo.py"
      provides: "Timeline detail projection with implementation and target node names"
    - path: "tests/test_agent_runs_api.py"
      provides: "AgentRun/SSE projection and no-widening visibility tests"
    - path: "tests/test_trace_api.py"
      provides: "Trace API target projection and ghost-role 403 coverage"
    - path: "tests/replay/test_replay_api.py"
      provides: "Replay visibility no-widening regression"
  key_links:
    - from: "src/agent/trace.py"
      to: "src/agent/graph_vocabulary.py"
      via: "project_trace_step_for_contract"
      pattern: "target_nodes_executed|implementation_node|target_node"
    - from: "src/api/routers/agent_runs.py"
      to: "src/agent/trace.py"
      via: "build_trace_summary final/SSE payload"
      pattern: "target_node_name|target_merchant_context"
    - from: "src/api/routers/traces.py"
      to: "src/repositories/trace_repo.py"
      via: "steps/timeline projection"
      pattern: "target_node|implementation_node"
---

<objective>
把 Phase 32 的 target graph vocabulary 投影接到 trace/eval/API 表面，并记录安全的 target merchant-context evidence。

Purpose: 满足 APF-11、D-11/D-13/D-14/D-18，允许 contract/eval/API 断言 target canonical names 与 target merchant-context status，同时不改宽 AgentRun/trace/replay 可见性。
Output: trace/API projection fields、target merchant-context helper/state/status、API/security regression tests。
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-intent-graph-migration/32-CONTEXT.md
@.planning/phases/32-intent-graph-migration/32-RESEARCH.md
@.planning/phases/32-intent-graph-migration/32-PATTERNS.md
@.planning/phases/32-intent-graph-migration/32-VALIDATION.md
@.planning/phases/32-intent-graph-migration/32-01-SUMMARY.md
@.planning/phases/32-intent-graph-migration/32-02-SUMMARY.md
@.planning/phases/32-intent-graph-migration/32-03-SUMMARY.md
@.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-05-SUMMARY.md
@.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-06-SUMMARY.md
@docs/contract-spec.md
@docs/target-agent-platform-architecture-plan.md
@src/agent/graph_vocabulary.py
@src/agent/state.py
@src/agent/nodes/receive_request.py
@src/agent/trace.py
@src/api/routers/agent_runs.py
@src/api/routers/traces.py
@src/repositories/trace_repo.py
@src/api/schemas/agent_runs.py
@src/api/schemas/approvals.py
@tests/agent/test_trace.py
@tests/test_agent_runs_api.py
@tests/test_trace_api.py
@tests/replay/test_replay_api.py
@tests/architecture/test_trusted_context_boundaries.py

<interfaces>
Current trace summary:
```python
def build_trace_summary(run_id: str, final_state: dict[str, Any], total_latency_ms: int) -> dict[str, Any]:
    nodes_executed = [str(step.get("node") or "unknown") for step in trace_steps]
```

Current run/trace visibility guard to preserve:
```python
ADMIN_RUN_VISIBILITY_ROLES = {"admin"}
if run.user_id != user.id and user.role not in ADMIN_RUN_VISIBILITY_ROLES:
    raise HTTPException(status_code=403, ...)
```
</interfaces>

Scope guardrails:
- Do not mutate persisted `AgentStep.node_name`; projection is additive.
- Do not add manager/supervisor/approval_manager to run, trace, replay, or stream visibility allowlists.
- Do not expose raw merchant ids, order ids, refund case ids, ticket ids, prompt text, raw tool payloads, or business identifiers in target merchant-context evidence.
- Do not implement same-merchant AgentRun visibility; Phase 35/36+ ownership remains separate.
</context>

<source_audit>
| Source | ID | Requirement / Decision | Responsible Plan | Status |
| --- | --- | --- | --- | --- |
| GOAL | Phase 32 | Target graph migration while preserving legacy compatibility | 32-01..32-05 | COVERED |
| ROADMAP-SC | SC-01 | Legacy graph names map to target canonical projection in trace/eval surfaces | 32-01, 32-04, 32-05 | COVERED |
| ROADMAP-SC | SC-02 | Registries own effective route/slot decisions | 32-02, 32-03, 32-05 | COVERED |
| ROADMAP-SC | SC-03 | Router totality tests cover safety/slot/memory paths | 32-02, 32-03, 32-05 | COVERED |
| ROADMAP-SC | SC-04 | Target merchant context is recorded safely and manager/supervisor access is not tenant-wide | 32-04, 32-05 | COVERED |
| REQ | APF-11 | Target graph vocabulary mapping exposed to contract/eval/API surfaces | 32-01, 32-04, 32-05 | COVERED |
| REQ | APF-12 | Intent/slot registries drive effective decisions | 32-02, 32-03, 32-05 | COVERED |
| CONTEXT | D-01..D-04 | Compatibility vocabulary, real target names, Phase 33 non-runnable entries, split plans | 32-01, 32-05 | COVERED |
| CONTEXT | D-05..D-10 | Intent and slot policy ownership and route totality | 32-02, 32-03, 32-05 | COVERED |
| CONTEXT | D-11 | Trace/eval projections preserve debugging compatibility and expose target names | 32-04 | COVERED |
| CONTEXT | D-12 | Router totality tests cover target names and legacy edge keys | 32-03, 32-05 | COVERED |
| CONTEXT | D-13 | Target merchant context status is `resolved`, `deferred`, `unavailable`, or `not_applicable` | 32-04 | COVERED |
| CONTEXT | D-14 | Phase 32 does not broaden authorization semantics | 32-04, 32-05 | COVERED |
| CONTEXT | D-15..D-19 | RED tests, registry tests, route tests, merchant tests, approved test entrypoint | 32-01..32-05 | COVERED |
| RESEARCH | R-03 | Add target projection beside legacy trace/debug fields | 32-04 | COVERED |
| RESEARCH | R-05 | Target merchant context is evidence/status, not authorization grant | 32-04 | COVERED |
| VALIDATION | V-04 | API/trace/replay visibility tests | 32-04, 32-05 | COVERED |
| VALIDATION | V-06 | No fake runnable Phase 33 nodes | 32-01, 32-05 | COVERED |
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add target graph projection to trace, SSE, and trace API surfaces</name>
  <files>src/agent/trace.py, src/api/routers/agent_runs.py, src/api/routers/traces.py, src/repositories/trace_repo.py, src/api/schemas/agent_runs.py, src/api/schemas/approvals.py, tests/agent/test_trace.py, tests/test_agent_runs_api.py, tests/test_trace_api.py</files>
  <read_first>
- .planning/phases/32-intent-graph-migration/32-01-SUMMARY.md
- .planning/phases/32-intent-graph-migration/32-CONTEXT.md
- .planning/phases/32-intent-graph-migration/32-RESEARCH.md
- .planning/phases/32-intent-graph-migration/32-PATTERNS.md
- src/agent/graph_vocabulary.py
- src/agent/trace.py
- src/api/routers/agent_runs.py
- src/api/routers/traces.py
- src/repositories/trace_repo.py
- src/api/schemas/agent_runs.py
- src/api/schemas/approvals.py
- tests/agent/test_trace.py
- tests/test_agent_runs_api.py
- tests/test_trace_api.py
  </read_first>
  <action>
Implement D-02, D-11, D-18, APF-11, and research R-03.

Use `src.agent.graph_vocabulary.project_trace_step_for_contract(...)` and `target_graph_name(...)` as the only source for target node/router names.

Update `src/agent/trace.py`:
- keep `nodes_executed` unchanged as legacy implementation names;
- add `target_nodes_executed: list[str]`;
- add `graph_projection` with `schema_version: "target_graph_projection.v1"` and per-step entries containing `implementation_node`, `target_node`, `target_graph_status`, and `target_graph_runnable`;
- do not change `write_agent_steps(...)` persistence of `AgentStep.node_name`.

Update `src/api/routers/agent_runs.py`:
- `_sse_event(...)` output keeps `node_name` and adds `target_node_name` when `node_name` is present;
- `_extract_step_payload(...)` may include safe target projection fields but must not remove existing payload keys.

Update `src/api/routers/traces.py` and `src/repositories/trace_repo.py`:
- trace response step dicts keep `node` and add `implementation_node` plus `target_node`;
- timeline detail keeps `node_name` and adds `target_node`;
- `TraceResponse` schema in `src/api/schemas/approvals.py` may add optional `graph_projection: dict[str, Any] | None = None` if the route returns a top-level projection.

Update `src/api/schemas/agent_runs.py` only if the SSE payload model needs an optional `target_node_name` or `graph_projection` field for tests; keep extra output safe and bounded.
  </action>
  <acceptance_criteria>
- `rg -n "target_nodes_executed|graph_projection|target_graph_projection\\.v1|project_trace_step_for_contract" src/agent/trace.py tests/agent/test_trace.py` finds trace-summary projection.
- `rg -n "target_node_name|target_node" src/api/routers/agent_runs.py src/api/routers/traces.py src/repositories/trace_repo.py tests/test_agent_runs_api.py tests/test_trace_api.py` finds API projection fields.
- `rg -n "node_name|nodes_executed|AgentStep\\(" src/agent/trace.py src/api/routers/agent_runs.py src/api/routers/traces.py` still finds legacy/debug fields.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_trace.py tests/test_agent_runs_api.py tests/test_trace_api.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/trace.py src/api/routers/agent_runs.py src/api/routers/traces.py src/repositories/trace_repo.py src/api/schemas/agent_runs.py src/api/schemas/approvals.py tests/agent/test_trace.py tests/test_agent_runs_api.py tests/test_trace_api.py</automated>
  </verify>
  <done>Trace/eval/API projection exposes target canonical names while preserving legacy node/debug fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add safe target merchant-context status evidence</name>
  <files>src/agent/merchant_context.py, src/agent/state.py, src/agent/nodes/receive_request.py, src/agent/trace.py, src/api/routers/agent_runs.py, tests/agent/test_trace.py, tests/test_agent_runs_api.py, tests/architecture/test_trusted_context_boundaries.py</files>
  <read_first>
- .planning/phases/32-intent-graph-migration/32-CONTEXT.md
- .planning/phases/32-intent-graph-migration/32-RESEARCH.md
- .planning/phases/29.5-merchant-scope-role-model-alignment/29.5-05-SUMMARY.md
- .planning/phases/30-businessfactservice-boundary/30-03-SUMMARY.md
- docs/contract-spec.md section 8.0 and 8.0.1
- src/agent/state.py
- src/agent/nodes/receive_request.py
- src/agent/trace.py
- src/api/routers/agent_runs.py
- tests/agent/test_trace.py
- tests/test_agent_runs_api.py
- tests/architecture/test_trusted_context_boundaries.py
  </read_first>
  <action>
Implement D-13, D-14, D-18, APF-11, and research R-05.

Create `src/agent/merchant_context.py` with:
- `TargetMerchantContextStatus = Literal["resolved", "deferred", "unavailable", "not_applicable"]`
- `project_target_merchant_context(state: Mapping[str, Any]) -> dict[str, Any]`
- schema version `target_merchant_context.v1`

The projection must return only allowlisted safe fields:
- `schema_version`
- `status`
- `source`
- `reason_codes`
- `business_fact_ref_count` when derived from service-approved refs

Do not include raw merchant ids, order ids, refund case ids, ticket ids, user query text, prompt snippets, raw tool payloads, or repository rows.

Deterministic status rules:
- If `state["target_merchant_context"]` already contains an allowlisted valid status, normalize and return it.
- Return `resolved` only when service-approved business fact refs in `last_business_context_refs`, `business_context`, or equivalent graph state prove target merchant context; do not derive this from LLM text, memory, active slots, or prompt summaries.
- Return `deferred` for business-scoped intent/route state when no service-approved business fact ref proves the target merchant; use reason code `TARGET_MERCHANT_CONTEXT_DEFERRED_UNTIL_BUSINESS_FACT_REF`.
- Return `unavailable` for malformed trusted/business context evidence or explicit no-authority errors; use reason code `TARGET_MERCHANT_CONTEXT_UNAVAILABLE`.
- Return `not_applicable` for ordinary direct-response or tenant-public-policy-only paths with no business target.

Add optional `target_merchant_context: dict[str, Any] | None` to `AgentState` and reset it to `None` in `receive_request`.

Update `build_trace_summary(...)` and final/SSE run projection in `src/api/routers/agent_runs.py` to include `target_merchant_context` from `project_target_merchant_context(final_state)` or step update state. This status is evidence only; do not read it in `_ensure_can_view_run(...)`, `_ensure_can_execute_run(...)`, or trace/replay guards.
  </action>
  <acceptance_criteria>
- `test -f src/agent/merchant_context.py` succeeds.
- `rg -n "TargetMerchantContextStatus|target_merchant_context\\.v1|resolved|deferred|unavailable|not_applicable|TARGET_MERCHANT_CONTEXT_DEFERRED_UNTIL_BUSINESS_FACT_REF" src/agent/merchant_context.py` finds status implementation.
- `rg -n "target_merchant_context" src/agent/state.py src/agent/nodes/receive_request.py src/agent/trace.py src/api/routers/agent_runs.py tests/agent/test_trace.py tests/test_agent_runs_api.py` finds state reset and projection tests.
- `! rg -n "target_merchant_context.*merchant_id|target_merchant_context.*order_id|target_merchant_context.*refund|target_merchant_context.*ticket" tests/agent/test_trace.py tests/test_agent_runs_api.py src/agent/merchant_context.py` succeeds unless the match is an explicit negative assertion.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_trace.py tests/test_agent_runs_api.py tests/architecture/test_trusted_context_boundaries.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/merchant_context.py src/agent/state.py src/agent/nodes/receive_request.py src/agent/trace.py src/api/routers/agent_runs.py tests/agent/test_trace.py tests/test_agent_runs_api.py tests/architecture/test_trusted_context_boundaries.py</automated>
  </verify>
  <done>Target merchant-context status is recorded as safe evidence/status metadata and reset per turn without becoming an authorization source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve AgentRun, trace, and replay visibility boundaries</name>
  <files>src/api/routers/agent_runs.py, src/api/routers/traces.py, tests/test_agent_runs_api.py, tests/test_trace_api.py, tests/replay/test_replay_api.py</files>
  <read_first>
- .planning/phases/29.5-merchant-scope-role-model-alignment/29.5-05-SUMMARY.md
- .planning/phases/29.5-merchant-scope-role-model-alignment/29.5-06-SUMMARY.md
- .planning/phases/32-intent-graph-migration/32-CONTEXT.md
- src/api/routers/agent_runs.py
- src/api/routers/traces.py
- tests/test_agent_runs_api.py
- tests/test_trace_api.py
- tests/replay/test_replay_api.py
  </read_first>
  <action>
Implement D-13, D-14, D-18, and validation V-04.

Add or strengthen tests proving target graph projection and target merchant-context status do not widen access:
- `support`, `manager`, legacy `merchant`, `supervisor`, and `approval_manager` users who are not run owners still receive 403 for run status/evidence/stream execution, trace, and replay unless they are `admin`.
- `ADMIN_RUN_VISIBILITY_ROLES` remains exactly `{"admin"}` in both `src/api/routers/agent_runs.py` and `src/api/routers/traces.py`.
- No branch in `_ensure_can_view_run(...)`, `get_run_trace(...)`, or `get_run_replay(...)` checks `target_merchant_context` as an allow condition.

Do not implement same-merchant run visibility. If tests need new helper users, follow existing `_create_same_tenant_role_user(...)` patterns and keep ghost roles negative-only.
  </action>
  <acceptance_criteria>
- `rg -n "ADMIN_RUN_VISIBILITY_ROLES = \\{\"admin\"\\}" src/api/routers/agent_runs.py src/api/routers/traces.py` finds exact admin-only constants.
- `rg -n "supervisor|approval_manager|manager|merchant" tests/test_agent_runs_api.py tests/test_trace_api.py tests/replay/test_replay_api.py` finds negative visibility coverage.
- `! rg -n "_ensure_can_view_run|get_run_trace|get_run_replay" src/api/routers/agent_runs.py src/api/routers/traces.py | rg "target_merchant_context"` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_agent_runs_api.py tests/test_trace_api.py tests/replay/test_replay_api.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/api/routers/agent_runs.py src/api/routers/traces.py tests/test_agent_runs_api.py tests/test_trace_api.py tests/replay/test_replay_api.py</automated>
  </verify>
  <done>Run, trace, and replay visibility remain owner/admin-only and target merchant-context evidence is not an authorization input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| persisted AgentStep -> API/eval projection | Persisted implementation node names are projected to target names without rewriting audit truth. |
| graph/business evidence -> target merchant-context status | Only safe status metadata crosses into trace/API; raw business identifiers stay out. |
| target merchant-context status -> API authorization | Status evidence must not widen owner/admin-only run, trace, or replay visibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-32-04-01 | Repudiation / Tampering | `src/agent/trace.py` | mitigate | Keep legacy `nodes_executed` and add target projection fields from typed vocabulary helper. |
| T-32-04-02 | Information Disclosure | `src/agent/merchant_context.py` | mitigate | Allowlist status fields and forbid raw merchant/business identifiers in target merchant-context projection. |
| T-32-04-03 | Elevation of Privilege | AgentRun/trace/replay routers | mitigate | Tests assert `ADMIN_RUN_VISIBILITY_ROLES == {"admin"}` and no guard consumes `target_merchant_context`. |
</threat_model>

<verification>
Run focused trace/API/replay visibility suites with the MOCA-approved test entrypoint. If a validation failure is environmental or fixture-related and is handled, append the issue to `.planning/LOCAL-VALIDATION-ISSUES.md` in Chinese.
</verification>

<success_criteria>
- Trace summary, SSE events, trace API steps/timeline, and eval-facing projection expose target canonical graph names.
- Legacy `node_name`, `node`, and `nodes_executed` fields remain available.
- Target merchant-context status is deterministic, safe, and limited to `resolved`, `deferred`, `unavailable`, or `not_applicable`.
- Manager/supervisor-style roles do not gain AgentRun, trace, or replay access.
</success_criteria>

<output>
After completion, create `.planning/phases/32-intent-graph-migration/32-04-SUMMARY.md`.
</output>
