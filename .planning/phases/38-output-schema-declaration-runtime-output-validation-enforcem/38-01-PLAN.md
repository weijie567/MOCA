---
phase: 38-output-schema-declaration-runtime-output-validation-enforcem
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tools/validation.py
  - tests/tools/test_catalog.py
autonomous: true
requirements:
  - TPH-01
must_haves:
  truths:
    - "本地 schema validator 支持 Phase 38 output_schema 所需的 nullable/type-union 语义。"
    - "TPH-01 的八个 scoped read/retrieval tool 集合被测试锁定，action tool 不进入本阶段 real output_schema 断言。"
    - "所有新增验证命令使用 uv run 入口，不使用裸 pytest 或裸 python -m pytest。"
  artifacts:
    - path: "src/tools/validation.py"
      provides: "validate_json_value 支持 type list union 与 null"
      contains: "expected_type"
    - path: "tests/tools/test_catalog.py"
      provides: "validator nullable/type-union tests and TPH-01 scoped tool set contract"
      contains: "test_tph01_scoped_output_schema_tool_names_match_registered_tools"
  key_links:
    - from: "src/tools/validation.py"
      to: "src/tools/catalog.py"
      via: "descriptor input/output schemas consumed by validate_json_value"
      pattern: "validate_json_value"
    - from: "tests/tools/test_catalog.py"
      to: "src/tools.catalog.ToolCatalog"
      via: "_descriptor(...) and ToolCatalog().descriptors()"
      pattern: "ToolCatalog\\(\\)\\.descriptors"
---

<objective>
为 Phase 38 的 real `output_schema` 声明建立本地 validator 能力和 scoped tool 合同边界。

Purpose: 后续严格 output schemas 需要接受当前 producer 已经会发出的 `None` 字段；同时必须精确锁住 TPH-01 只覆盖八个 read/retrieval tools，不把 `create_coupon_grant_draft` action tool 混入本阶段。
Output: `validate_json_value` 支持 `type: ["string", "null"]` / `type: "null"`，并有 focused tests 证明 scoped tool 集合正确。
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-output-schema-declaration-runtime-output-validation-enforcem/38-RESEARCH.md
@.planning/phases/38-output-schema-declaration-runtime-output-validation-enforcem/38-VALIDATION.md
@.planning/phases/38-output-schema-declaration-runtime-output-validation-enforcem/38-PATTERNS.md
@.planning/phases/37-tool-declaration-runtime-policy-internal-consolidation/37-01-SUMMARY.md
@src/tools/validation.py
@src/tools/catalog.py
@tests/tools/test_catalog.py

<interfaces>
当前 executor 需要使用的接口形状：

```python
def validate_json_value(value: Any, schema: dict[str, Any]) -> None: ...
_validate_json_value = validate_json_value
```

```python
def _descriptor(name: str) -> ToolDescriptor:
    return next(descriptor for descriptor in ToolCatalog().descriptors() if descriptor.name == name)
```

TPH-01 scoped tool names are exactly:
`get_order`, `get_refund_case`, `get_ticket`, `get_logistics`, `get_merchant_risk`, `search_policy`, `search_sop`, `search_case_memory`.
`create_coupon_grant_draft` is preserved as the action tool outside TPH-01 output-schema hardening.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add nullable/type-union schema validation</name>
  <files>src/tools/validation.py, tests/tools/test_catalog.py</files>
  <read_first>
    - src/tools/validation.py
    - tests/tools/test_catalog.py
    - .planning/phases/38-output-schema-declaration-runtime-output-validation-enforcem/38-RESEARCH.md
    - .planning/phases/38-output-schema-declaration-runtime-output-validation-enforcem/38-PATTERNS.md
    - .planning/phases/38-output-schema-declaration-runtime-output-validation-enforcem/38-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `_validate_json_value(None, {"type": "null"})` passes.
    - Test 2: `_validate_json_value(None, {"type": ["string", "null"]})` and `_validate_json_value("x", {"type": ["string", "null"], "minLength": 1})` pass.
    - Test 3: `_validate_json_value(123, {"type": ["string", "null"]})` raises `TypeError` or `ValueError`.
    - Test 4: existing string/integer/number/boolean/object/array/enum behavior remains unchanged.
  </behavior>
  <action>
    Add tests in `tests/tools/test_catalog.py` named `test_json_schema_helper_accepts_nullable_union_and_null` and `test_json_schema_helper_rejects_nullable_union_mismatches`.
    Implement the narrow schema subset in `src/tools/validation.py`: if `schema["type"]` is a list, try each candidate by recursively calling `validate_json_value(value, {**schema, "type": candidate_type})` and accept on the first success; if none succeeds, raise a validation error without swallowing the underlying mismatch class. Add explicit `"null"` handling that accepts only `value is None` and returns before scalar/object branches.
    Do not add `jsonschema` or any new dependency. Do not change public function names or `_validate_json_value` alias.
  </action>
  <verify>
    <automated>uv run pytest tests/tools/test_catalog.py::test_json_schema_helper_accepts_nullable_union_and_null tests/tools/test_catalog.py::test_json_schema_helper_rejects_nullable_union_mismatches -q</automated>
    <automated>uv run ruff check src/tools/validation.py tests/tools/test_catalog.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "isinstance\\(expected_type, list\\)|expected_type == \"null\"" src/tools/validation.py` finds both implementation branches.
    - The two focused pytest node IDs above pass through `uv run pytest`.
    - `uv run ruff check src/tools/validation.py tests/tools/test_catalog.py` passes.
  </acceptance_criteria>
  <done>`validate_json_value` accepts current nullable output-field schema forms and rejects mismatched union values without changing existing input-schema behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the TPH-01 scoped output-schema tool set</name>
  <files>tests/tools/test_catalog.py</files>
  <read_first>
    - tests/tools/test_catalog.py
    - src/tools/catalog.py
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/38-output-schema-declaration-runtime-output-validation-enforcem/38-RESEARCH.md
    - .planning/phases/37-tool-declaration-runtime-policy-internal-consolidation/37-01-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: the eight TPH-01 tool names are all present in `ToolCatalog().descriptors()`.
    - Test 2: every TPH-01 scoped descriptor has `kind in {"read", "retrieval"}` and `exposure == "planner_visible"`.
    - Test 3: `create_coupon_grant_draft` is excluded from the TPH-01 scoped set and remains a `kind == "write"` / `exposure == "node_only"` action descriptor.
  </behavior>
  <action>
    In `tests/tools/test_catalog.py`, add a module-level `TPH01_OUTPUT_SCHEMA_TOOL_NAMES` constant as a `frozenset` with exactly the eight names from TPH-01.
    Add `test_tph01_scoped_output_schema_tool_names_match_registered_tools` using `ToolCatalog().descriptors()` and `_descriptor(...)` to assert the scoped set, descriptor kinds, planner visibility, and action exclusion.
    Keep the existing Phase 37 generic-output assertion in place in this plan; Plan 38-02 will replace it when catalog declarations become real. Do not edit `src/tools/catalog.py` in this task.
  </action>
  <verify>
    <automated>uv run pytest tests/tools/test_catalog.py::test_tph01_scoped_output_schema_tool_names_match_registered_tools tests/tools/test_catalog.py::test_catalog_registry_derives_identifier_schemas_without_drift -q</automated>
    <automated>uv run ruff check tests/tools/test_catalog.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "TPH01_OUTPUT_SCHEMA_TOOL_NAMES|test_tph01_scoped_output_schema_tool_names_match_registered_tools" tests/tools/test_catalog.py` finds the scoped constant and test.
    - The focused pytest node IDs above pass through `uv run pytest`.
    - `git diff -- src/tools/catalog.py` is empty after this task.
  </acceptance_criteria>
  <done>The Phase 38 scoped tool set is executable-test evidence before catalog schema declarations are changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor result -> ToolRuntime | Untrusted executor `ToolResultV2.data` later crosses into runtime validation. This plan prepares the validator subset used by that boundary. |
| Catalog declarations -> executor tests | Schema declarations become security policy for downstream data acceptance. The scoped set must not accidentally include action output behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering | `src/tools/validation.py::validate_json_value` | mitigate | Add explicit `type` union and `null` tests before strict output schemas rely on nullable fields. |
| T-38-02 | Elevation of Privilege | `tests/tools/test_catalog.py` scoped set | mitigate | Lock TPH-01 to read/retrieval planner-visible descriptors and exclude `create_coupon_grant_draft`. |
| T-38-03 | Information Disclosure | validation error handling | accept | This helper raises local exceptions only; runtime failure redaction is enforced in Plan 38-03. |
</threat_model>

<verification>
Plan 38-01 is complete when:
- `uv run pytest tests/tools/test_catalog.py -q` passes after the validator/scoped-set changes.
- `uv run ruff check src/tools/validation.py tests/tools/test_catalog.py` passes.
- No bare `pytest` / bare `python -m pytest` command appears in the plan summary.
</verification>

<success_criteria>
- The local schema validator supports the nullable/type-union forms documented in Phase 38 research.
- TPH-01 scoped output-schema tool names are locked in tests before catalog declaration edits.
- No source outside `src/tools/validation.py` and `tests/tools/test_catalog.py` changes in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/38-output-schema-declaration-runtime-output-validation-enforcem/38-01-SUMMARY.md`.
</output>
