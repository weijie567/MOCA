---
phase: 55-memory-context-load-cutover
plan: "03"
type: execute
wave: 3
depends_on:
  - "55-02"
files_modified:
  - src/agent/graph_vocabulary.py
  - src/api/routers/agent_runs.py
  - tests/agent/test_graph_vocabulary.py
  - tests/architecture/test_phase32_static_contract.py
  - tests/architecture/test_memory_contract_delta.py
  - tests/agent/test_trace.py
  - tests/test_trace_api.py
  - tests/test_agent_runs_api.py
  - docs/current-langgraph-architecture.md
  - .planning/ARCHITECTURE-DEBT.md
  - .planning/phases/55-memory-context-load-cutover/55-VALIDATION.md
autonomous: true
requirements:
  - CAGM-06
user_setup: []
must_haves:
  truths:
    - "`memory_context_load` is the runtime graph vocabulary entry."
    - "`long_term_memory_retrieve` is retained only as a compatibility alias with Phase 55 reason/delete metadata."
    - "`reviewed_memory_context_retrieve` is not a second runtime owner after Phase 55."
    - "Trace/API/SSE projection exposes canonical target names without rewriting historical implementation names."
    - "Current architecture docs describe the executed source graph, not target-state assumptions."
    - "Architecture debt ledger records the Phase 55 fix, retained compatibility, validation evidence, and Phase 58 delete phase."
    - "55-VALIDATION.md is closed only after focused tests, Ruff, active-legacy scans, docs/ledger checks, and artifact command scans pass."
  artifacts:
    - path: src/agent/graph_vocabulary.py
      provides: "runtime/compatibility graph vocabulary projection after Phase 55"
      contains: "PHASE_55_COMPATIBILITY_ALIAS"
    - path: src/api/routers/agent_runs.py
      provides: "SSE display label for memory_context_load"
      contains: "memory_context_load"
    - path: tests/agent/test_graph_vocabulary.py
      provides: "runtime and compatibility alias assertions"
      contains: "memory_context_load"
    - path: docs/current-langgraph-architecture.md
      provides: "current-source graph snapshot after Phase 55"
      contains: "memory_context_load"
    - path: .planning/ARCHITECTURE-DEBT.md
      provides: "canonical graph/memory compatibility debt ledger closeout"
      contains: "Phase 55"
    - path: .planning/phases/55-memory-context-load-cutover/55-VALIDATION.md
      provides: "Phase 55 final command evidence and Nyquist closeout"
      contains: "nyquist_compliant"
  key_links:
    - from: src/agent/graph_vocabulary.py
      to: tests/agent/test_graph_vocabulary.py
      via: "runtime and compatibility projection tests"
      pattern: "graph_vocabulary_entry"
    - from: src/api/routers/agent_runs.py
      to: tests/test_agent_runs_api.py
      via: "_sse_event target_node_name projection"
      pattern: "target_node_name"
    - from: docs/current-langgraph-architecture.md
      to: src/agent/graph.py
      via: "source-fact active graph path"
      pattern: "memory_context_load"
    - from: .planning/ARCHITECTURE-DEBT.md
      to: .planning/phases/55-memory-context-load-cutover/55-VALIDATION.md
      via: "shared validation evidence and compatibility ledger"
      pattern: "Phase 55"
---

<objective>
Close Phase 55 vocabulary, API projection, docs, architecture debt, and validation.

Purpose: After active graph cutover, trace/API/docs must treat `memory_context_load` as current runtime authority while preserving historical `long_term_memory_retrieve` readability as a documented compatibility surface slated for Phase 58 cleanup.
Output: runtime vocabulary projection, trace/API/SSE tests, current architecture docs, architecture debt ledger entry, and final validation evidence.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-memory-context-load-cutover/55-CONTEXT.md
@.planning/phases/55-memory-context-load-cutover/55-RESEARCH.md
@.planning/phases/55-memory-context-load-cutover/55-PATTERNS.md
@.planning/phases/55-memory-context-load-cutover/55-VALIDATION.md
@.planning/phases/55-memory-context-load-cutover/55-01-SUMMARY.md
@.planning/phases/55-memory-context-load-cutover/55-02-SUMMARY.md
@.planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
@docs/current-langgraph-architecture.md
@docs/contract-spec.md
@docs/target-agent-platform-architecture-plan.md
@src/agent/graph_vocabulary.py
@src/api/routers/agent_runs.py
@tests/agent/test_graph_vocabulary.py
@tests/agent/test_trace.py
@tests/test_trace_api.py
@tests/test_agent_runs_api.py
@.planning/ARCHITECTURE-DEBT.md
@AGENTS.md

<interfaces>
Vocabulary entries target:
```python
_PHASE55_MEMORY_ALIAS_REASON_CODES = (
    "PHASE_55_COMPATIBILITY_ALIAS",
    "HISTORICAL_TRACE_PROJECTION",
    "IMPORT_TEST_COMPATIBILITY",
    "DELETE_BY_PHASE_58",
)

_entry("long_term_memory_retrieve", "memory_context_load", "node", "compatibility_alias", True, _PHASE55_MEMORY_ALIAS_REASON_CODES)
_entry("reviewed_memory_context_retrieve", "memory_context_load", "node", "compatibility_alias", True, _PHASE55_MEMORY_ALIAS_REASON_CODES)
_entry("memory_context_load", "memory_context_load", "node", "runtime", True)
```

SSE label target:
```python
NODE_MESSAGES["memory_context_load"] = "正在加载记忆上下文"
```
</interfaces>
</context>

<source_coverage_audit>
| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Active runtime no longer uses `long_term_memory_retrieve` except recorded temporary reuse | Tasks 1-3 project historical alias, update docs/ledger, and scan active graph. |
| REQ | CAGM-06 | Covered in frontmatter and final focused suite. |
| RESEARCH | Vocabulary/API/docs/architecture-debt closeout | Tasks 1-3 implement projection, docs/ledger, and validation. |
| RESEARCH | Nyquist artifact command scan and final suite | Task 3 runs full focused suite, Ruff, active legacy scan, docs/ledger check, and artifact scan. |
| CONTEXT | D-55-13, D-55-14, D-55-15, D-55-16 | Tasks 1-3 implement compatibility alias metadata, canonical metrics projection, scoped baseline, and approved entrypoints. |
| CONTEXT | Deferred Phase 56/57/58 ideas | Task 3 scans that Phase 56/57 nodes remain legacy and Phase 58 cleanup is not implemented. |
</source_coverage_audit>

<compatibility_table>
| Legacy surface | Canonical owner | Reason | Trace projection | Validation | Delete phase |
|----------------|-----------------|--------|------------------|------------|--------------|
| Historical `long_term_memory_retrieve` trace/API node name | `memory_context_load` | Persisted historical rows and direct wrapper callers may still reference the old name | `long_term_memory_retrieve -> memory_context_load`, status `compatibility_alias`, reason codes include `PHASE_55_COMPATIBILITY_ALIAS`, `HISTORICAL_TRACE_PROJECTION`, `IMPORT_TEST_COMPATIBILITY`, `DELETE_BY_PHASE_58` | `tests/agent/test_graph_vocabulary.py`, `tests/agent/test_trace.py`, `tests/test_trace_api.py`, `tests/test_agent_runs_api.py` | Phase 58 |
| `reviewed_memory_context_retrieve` helper trace/API name | `memory_context_load` | Helper remains implementation/service test surface after canonical node becomes runtime owner | `reviewed_memory_context_retrieve -> memory_context_load`, status `compatibility_alias`; direct helper tests remain allowed | Vocabulary/static tests assert it is not runtime; helper tests cover direct behavior | Phase 58 or internal-only helper reclassification in Phase 58 |
| Direct `long_term_memory_retrieve` import/test wrapper | `memory_context_load` | Import compatibility retained without active graph registration | Wrapper may expose legacy `llm_outputs["long_term_memory_retrieve"]`; active graph uses canonical metrics | Plan 55-01 wrapper tests and Plan 55-02 active graph scan | Phase 58 |
</compatibility_table>

<must_haves>
- `memory_context_load` is runtime vocabulary.
- Retained legacy names are compatibility aliases with delete metadata.
- Current-source docs and architecture debt ledger reflect the implemented state.
- Final validation evidence uses approved MOCA entrypoints.
- Active scans prove Phase 55 did not implement Phase 56/57/58 scope.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote memory vocabulary and static contract tests</name>
  <files>src/agent/graph_vocabulary.py, tests/agent/test_graph_vocabulary.py, tests/architecture/test_phase32_static_contract.py, tests/architecture/test_memory_contract_delta.py</files>
  <read_first>
    - src/agent/graph_vocabulary.py
    - tests/agent/test_graph_vocabulary.py
    - tests/architecture/test_phase32_static_contract.py
    - tests/architecture/test_memory_contract_delta.py
    - .planning/phases/55-memory-context-load-cutover/55-CONTEXT.md
    - .planning/phases/55-memory-context-load-cutover/55-PATTERNS.md
    - .planning/phases/55-memory-context-load-cutover/55-02-SUMMARY.md
  </read_first>
  <behavior>
    - `graph_vocabulary_entry("memory_context_load", kind="node").status == "runtime"`.
    - `graph_vocabulary_entry("memory_context_load", kind="node").runnable is True`.
    - `graph_vocabulary_entry("long_term_memory_retrieve", kind="node").status == "compatibility_alias"`.
    - `graph_vocabulary_entry("long_term_memory_retrieve", kind="node").reason_codes` includes `PHASE_55_COMPATIBILITY_ALIAS`, `HISTORICAL_TRACE_PROJECTION`, `IMPORT_TEST_COMPATIBILITY`, and `DELETE_BY_PHASE_58`.
    - `graph_vocabulary_entry("reviewed_memory_context_retrieve", kind="node").status == "compatibility_alias"`, not runtime.
    - There is exactly one `(legacy_name, kind)` entry for `memory_context_load`, `long_term_memory_retrieve`, and `reviewed_memory_context_retrieve`.
    - Static contract tests that enumerate graph vocabulary agree with these three memory-node statuses.
  </behavior>
  <action>
    Update `src/agent/graph_vocabulary.py` per D-55-13 and D-55-14. Add `_PHASE55_MEMORY_ALIAS_REASON_CODES = ("PHASE_55_COMPATIBILITY_ALIAS", "HISTORICAL_TRACE_PROJECTION", "IMPORT_TEST_COMPATIBILITY", "DELETE_BY_PHASE_58")`. Modify existing memory entries rather than appending duplicates: `memory_context_load` becomes the `runtime` node entry; `long_term_memory_retrieve` becomes a `compatibility_alias` to `memory_context_load` with `_PHASE55_MEMORY_ALIAS_REASON_CODES`; `reviewed_memory_context_retrieve` becomes a `compatibility_alias` to `memory_context_load` with `_PHASE55_MEMORY_ALIAS_REASON_CODES` or a strictly equivalent Phase 55 helper reason tuple that includes `DELETE_BY_PHASE_58`. Do not keep `reviewed_memory_context_retrieve` as a second runtime owner. Update `tests/agent/test_graph_vocabulary.py`, `tests/architecture/test_phase32_static_contract.py`, and `tests/architecture/test_memory_contract_delta.py` to assert the exact statuses and reason-code requirements. Add duplicate-entry tests for the three memory node names. Do not update trace/API/SSE tests or `src/api/routers/agent_runs.py` in this task; Task 2 owns those consumers after the vocabulary contract is established.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_graph_vocabulary.py tests/architecture/test_phase32_static_contract.py tests/architecture/test_memory_contract_delta.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/graph_vocabulary.py tests/agent/test_graph_vocabulary.py tests/architecture/test_phase32_static_contract.py tests/architecture/test_memory_contract_delta.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/agent/graph_vocabulary.py` contains `_PHASE55_MEMORY_ALIAS_REASON_CODES`.
    - `memory_context_load` node vocabulary status is `runtime`.
    - `long_term_memory_retrieve` node vocabulary status is `compatibility_alias`.
    - `reviewed_memory_context_retrieve` node vocabulary status is not `runtime`.
    - Phase 55 retained aliases include `DELETE_BY_PHASE_58`.
    - Static vocabulary/contract tests agree on the three memory node statuses.
  </acceptance_criteria>
  <done>Graph vocabulary identifies `memory_context_load` as current runtime while retaining legacy memory names as documented compatibility only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update trace/API/SSE projection consumers</name>
  <files>src/api/routers/agent_runs.py, tests/agent/test_trace.py, tests/test_trace_api.py, tests/test_agent_runs_api.py</files>
  <read_first>
    - src/api/routers/agent_runs.py
    - tests/agent/test_trace.py
    - tests/test_trace_api.py
    - tests/test_agent_runs_api.py
    - src/agent/graph_vocabulary.py
    - .planning/phases/55-memory-context-load-cutover/55-CONTEXT.md
    - .planning/phases/55-memory-context-load-cutover/55-PATTERNS.md
    - .planning/phases/55-memory-context-load-cutover/55-02-SUMMARY.md
  </read_first>
  <behavior>
    - Trace projection preserves historical implementation node `long_term_memory_retrieve` while target node is `memory_context_load` and status is `compatibility_alias`.
    - Trace projection for direct helper node `reviewed_memory_context_retrieve` targets `memory_context_load` with compatibility status.
    - Trace projection for current runtime node `memory_context_load` has implementation node and target node both `memory_context_load` and status `runtime`.
    - SSE projection for `node_name="memory_context_load"` sets `target_node_name == "memory_context_load"`.
    - `NODE_MESSAGES` includes exact key `"memory_context_load"` with value `"正在加载记忆上下文"`.
  </behavior>
  <action>
    Update trace/API/SSE projection consumers after Task 1 vocabulary status changes. In `src/api/routers/agent_runs.py`, add `NODE_MESSAGES["memory_context_load"] = "正在加载记忆上下文"`. Keep historical `long_term_memory_retrieve` display compatibility only if it already exists or tests require it; do not add active graph behavior here. Update `tests/agent/test_trace.py`, `tests/test_trace_api.py`, and `tests/test_agent_runs_api.py` so historical `long_term_memory_retrieve` and direct-helper `reviewed_memory_context_retrieve` rows project to target `memory_context_load` with compatibility status, while current `memory_context_load` rows project as runtime. Add an SSE assertion that `_sse_event(..., node_name="memory_context_load")` emits `target_node_name == "memory_context_load"` and the new display message path remains bounded to the node label, not raw memory content.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_trace.py tests/test_trace_api.py tests/test_agent_runs_api.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/api/routers/agent_runs.py tests/agent/test_trace.py tests/test_trace_api.py tests/test_agent_runs_api.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/api/routers/agent_runs.py` contains `"memory_context_load": "正在加载记忆上下文"`.
    - Trace tests cover current runtime `memory_context_load`.
    - Trace/API tests cover historical `long_term_memory_retrieve -> memory_context_load` compatibility projection.
    - Trace/API tests cover direct-helper `reviewed_memory_context_retrieve -> memory_context_load` compatibility projection.
    - SSE tests cover `target_node_name == "memory_context_load"` for current node events.
  </acceptance_criteria>
  <done>Trace/API/SSE consumers project current and historical memory node names correctly without rewriting persisted implementation names.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs, ledger, and final validation closeout</name>
  <files>docs/current-langgraph-architecture.md, .planning/ARCHITECTURE-DEBT.md, .planning/phases/55-memory-context-load-cutover/55-VALIDATION.md</files>
  <read_first>
    - docs/current-langgraph-architecture.md
    - .planning/ARCHITECTURE-DEBT.md
    - .planning/phases/55-memory-context-load-cutover/55-VALIDATION.md
    - .planning/phases/55-memory-context-load-cutover/55-01-SUMMARY.md
    - .planning/phases/55-memory-context-load-cutover/55-02-SUMMARY.md
    - docs/contract-spec.md
    - docs/target-agent-platform-architecture-plan.md
    - .planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
    - src/agent/graph.py
    - src/agent/routing.py
    - src/agent/graph_vocabulary.py
    - tests/architecture/graph_baseline.py
    - AGENTS.md
  </read_first>
  <action>
    Update `docs/current-langgraph-architecture.md` as current-source fact after Phase 55. The graph diagram, node list, source-fact bullets, and compatibility table must show `slot_resolution_gate -> route_after_slot_resolution -> memory_context_load -> investigate`; active node set must include `memory_context_load` and exclude active `long_term_memory_retrieve`; Phase 56 `generate_recommendation` and Phase 57 `assess_risk_and_approval` remain active legacy rows. The compatibility table must include retained `long_term_memory_retrieve` wrapper/import/historical trace surface with canonical owner `memory_context_load`, reason, trace projection, validation evidence, and delete phase `Phase 58`. It must also describe `reviewed_memory_context_retrieve` as implementation/helper compatibility if retained in vocabulary. Do not describe `recommendation_generation`, `risk_gate`, or final no-debt cleanup as implemented. Update `.planning/ARCHITECTURE-DEBT.md` in Chinese under the canonical graph / memory compatibility area with Phase 55 closeout: problem/root cause, impact, status, files/phase evidence, exact validation commands, retained compatibility surfaces, remaining Phase 56/57/58 risks, and no destructive memory table/API/config rename. If `docs/contract-spec.md` or `docs/target-agent-platform-architecture-plan.md` already express the Phase 55 target semantics, do not edit them; instead record in the summary that they were checked and already aligned. If execution finds a real mismatch between implemented source and those target docs, record the mismatch in the summary and make the smallest necessary doc correction in this task without changing Phase 56/57 scope. Run final Phase 55 validation and update `55-VALIDATION.md`. Task 3 is the only task that may set `status`, `nyquist_compliant`, `wave_0_complete`, per-task status rows, final command evidence, and artifact scan conclusions green. Required checks: full focused suite from `55-VALIDATION.md`; Ruff on changed source/test surfaces; static scan proving active graph registers `memory_context_load` and not `long_term_memory_retrieve`; static scan proving `route_after_slot_resolution` route map and router source use `memory_context_load`, not active `long_term_memory_retrieve`; static scan proving Phase 56 `generate_recommendation` and Phase 57 `assess_risk_and_approval` are still active legacy rows and Phase 58 cleanup is not implemented; vocabulary projection checks for Phase 55 reason codes; docs/ledger checks; artifact scan proving Phase 55 artifacts contain no bare `pytest` or bare `python -m pytest` commands. Use only `UV_CACHE_DIR=/tmp/uv-cache uv run ...` command forms for Python/test/lint verification. If any local validation/debug failure occurs, handle it, append `.planning/LOCAL-VALIDATION-ISSUES.md` in Chinese with evidence per AGENTS.md, and reference it in `55-03-SUMMARY.md`.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture/test_canonical_graph_baseline.py tests/architecture/test_memory_contract_delta.py tests/architecture/test_phase32_static_contract.py tests/agent/test_graph.py tests/test_graph_routing.py tests/agent/test_intent_routing.py tests/agent/test_graph_vocabulary.py tests/agent/test_trace.py tests/test_trace_api.py tests/test_agent_runs_api.py tests/agent/test_memory_context_load.py tests/agent/test_reviewed_memory_context_retrieve.py tests/agent/test_memory_evidence_boundary.py tests/memory/test_reviewed_memory_context_boundary.py tests/memory/test_phase46_session_context_alignment.py tests/memory/test_phase47_case_precedent_alignment.py tests/memory/test_phase48_long_term_preference_alignment.py tests/memory/test_phase48_1_memory_compat_alignment.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent src/memory tests/architecture tests/agent tests/memory</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run python -c "import ast,pathlib; graph=pathlib.Path('src/agent/graph.py').read_text(); routing=pathlib.Path('src/agent/routing.py').read_text(); vocab=pathlib.Path('src/agent/graph_vocabulary.py').read_text(); tree=ast.parse(graph); nodes=set(); edges=set(); cond={};\nfor n in ast.walk(tree):\n    if isinstance(n, ast.Call) and isinstance(n.func, ast.Attribute) and n.func.attr=='add_node': nodes.add(n.args[0].value)\n    if isinstance(n, ast.Call) and isinstance(n.func, ast.Attribute) and n.func.attr=='add_edge': edges.add((n.args[0].value, n.args[1].value))\n    if isinstance(n, ast.Call) and isinstance(n.func, ast.Attribute) and n.func.attr=='add_conditional_edges': cond[(n.args[0].value, n.args[1].id)]={k.value:v.value for k,v in zip(n.args[2].keys,n.args[2].values,strict=True)}\nassert 'memory_context_load' in nodes and 'long_term_memory_retrieve' not in nodes; assert ('memory_context_load','investigate') in edges; assert cond[('slot_resolution_gate','route_after_slot_resolution')]['memory_context_load']=='memory_context_load'; assert 'return [], \"long_term_memory_retrieve\", []' not in routing; assert {'generate_recommendation','assess_risk_and_approval'} <= nodes; assert 'PHASE_55_COMPATIBILITY_ALIAS' in vocab and 'DELETE_BY_PHASE_58' in vocab; print('55-03 active graph/vocabulary scan OK')"</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run python -c "from pathlib import Path; docs=Path('docs/current-langgraph-architecture.md').read_text(); debt=Path('.planning/ARCHITECTURE-DEBT.md').read_text(); assert 'memory_context_load' in docs; assert 'long_term_memory_retrieve' in docs; assert 'Phase 55' in debt; assert 'memory_context_load' in debt; assert 'long_term_memory_retrieve' in debt; print('55-03 docs/debt text checks OK')"</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run python -c "from pathlib import Path; phase=Path('.planning/phases/55-memory-context-load-cutover'); bad=[]\nfor p in phase.glob('55-*.md'):\n    for line in p.read_text(errors='ignore').splitlines():\n        stripped=line.strip().lstrip('$ ').strip()\n        if stripped == 'pytest' or stripped.startswith('pytest ') or stripped == 'python -m pytest' or stripped.startswith('python -m pytest '): bad.append(f'{p}: {stripped}')\nassert not bad, bad; print('55 artifact command scan OK')"</automated>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - Full focused Phase 55 suite passes with the approved uv entrypoint.
    - Ruff passes on changed source/test surfaces.
    - Static scan proves active graph registers `memory_context_load` and not `long_term_memory_retrieve`.
    - Static scan proves `route_after_slot_resolution` maps to `memory_context_load`.
    - Static scan proves Phase 56/57 active legacy nodes remain present and Phase 58 cleanup is not implemented.
    - Vocabulary scan proves Phase 55 reason codes and `DELETE_BY_PHASE_58` exist.
    - `docs/current-langgraph-architecture.md` describes active `memory_context_load`, not active `long_term_memory_retrieve`.
    - `.planning/ARCHITECTURE-DEBT.md` contains a Chinese Phase 55 closeout entry with status, evidence, retained surfaces, remaining risks, and no destructive memory rename/drop.
    - Artifact scan finds no bare `pytest` or bare `python -m pytest` commands in Phase 55 artifacts.
    - `55-VALIDATION.md` has `nyquist_compliant: true` and `wave_0_complete: true` only after the final evidence passes.
  </acceptance_criteria>
  <done>Current-source docs, architecture debt ledger, final validation evidence, and artifact scans are recorded with no open high-severity threats or Nyquist gaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime trace -> historical projection | Current runs use `memory_context_load`; historical rows may contain retained legacy names. |
| API/SSE label surface -> operator display | SSE labels expose node progress and target names to API clients. |
| source graph -> docs/ledger | Docs and debt ledger must describe implemented source facts, not target assumptions. |
| validation artifacts -> downstream verification | Validation commands and evidence must use approved MOCA entrypoints and avoid scope creep. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-55-03-01 | Repudiation | HIGH | `graph_vocabulary.py`, trace/API projection | mitigate | Mark `memory_context_load` runtime and retained legacy names compatibility-only with Phase 55 reason/delete metadata. |
| T-55-03-02 | Information Disclosure | MEDIUM | SSE / trace payload labels | mitigate | Add only concise node display label and target projection; do not expose raw memory content. |
| T-55-03-03 | Tampering | HIGH | docs / architecture debt ledger | mitigate | Update docs/ledger from source/test evidence and keep target contract vs current implementation separated. |
| T-55-03-04 | Denial of Service | MEDIUM | invalid validation commands | mitigate | Artifact scan rejects bare `pytest` and bare `python -m pytest`; verification uses `UV_CACHE_DIR=/tmp/uv-cache uv run ...`. |
| T-55-03-05 | Scope Creep / Tampering | HIGH | Phase 56/57/58 boundaries | mitigate | Static scan proves `generate_recommendation` and `assess_risk_and_approval` remain active legacy rows and final no-debt cleanup is not implemented. |
| T-55-03-06 | Integrity | MEDIUM | retained compatibility aliases | mitigate | Compatibility table requires owner, reason, trace projection, validation evidence, and delete phase `Phase 58`. |
</threat_model>

<verification>
Plan 55-03 is complete when vocabulary/API/trace tests, focused Phase 55 suite, Ruff, active graph/vocabulary scans, docs/debt checks, artifact entrypoint scan, and `git diff --check` pass and are recorded in `55-VALIDATION.md`.
</verification>

<success_criteria>
- `memory_context_load` is the runtime graph vocabulary entry.
- `long_term_memory_retrieve` and `reviewed_memory_context_retrieve` are compatibility aliases, not active runtime owners.
- Trace/API/SSE projection works for current canonical and historical compatibility names.
- Current architecture docs and architecture debt ledger record retained compatibility surfaces with Phase 58 deletion.
- Final validation evidence is recorded using approved MOCA entrypoints.
- No Phase 56/57/58 implementation scope is included.
</success_criteria>

<output>
After completion, create `.planning/phases/55-memory-context-load-cutover/55-03-SUMMARY.md`.
</output>
