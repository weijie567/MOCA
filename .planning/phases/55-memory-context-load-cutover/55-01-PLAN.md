---
phase: 55-memory-context-load-cutover
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/nodes/memory_context_load.py
  - src/agent/nodes/long_term_memory_retrieve.py
  - tests/agent/test_memory_context_load.py
  - tests/agent/test_reviewed_memory_context_retrieve.py
  - tests/agent/test_memory_evidence_boundary.py
autonomous: true
requirements:
  - CAGM-06
user_setup: []
must_haves:
  truths:
    - "A first-class `memory_context_load` node exists and delegates to the existing reviewed-memory/CWC loader."
    - "`memory_context_load` writes canonical metrics under `llm_outputs[\"memory_context_load\"]`."
    - "Loaded memory/CWC outputs carry finite usage labels and `authority_class == \"contextual_only\"`."
    - "Missing trusted context, denied scope, missing services, and service errors continue without long-term/case memory and expose skipped or unavailable status."
    - "`long_term_memory_retrieve` remains only a compatibility wrapper and is not the canonical owner."
    - "Memory outputs cannot create or satisfy policy evidence, current business facts, approval/action authority, or replay truth."
  artifacts:
    - path: src/agent/nodes/memory_context_load.py
      provides: "canonical memory context load graph node"
      contains: "async def memory_context_load"
    - path: src/agent/nodes/long_term_memory_retrieve.py
      provides: "non-active compatibility wrapper"
      contains: "async def long_term_memory_retrieve"
    - path: tests/agent/test_memory_context_load.py
      provides: "canonical node metrics, labels, fail-closed, and compatibility tests"
      contains: "test_memory_context_load"
    - path: tests/agent/test_memory_evidence_boundary.py
      provides: "authority boundary regression coverage"
      contains: "contextual_only"
  key_links:
    - from: src/agent/nodes/memory_context_load.py
      to: src/agent/nodes/reviewed_memory_context_retrieve.py
      via: "delegates to reviewed_memory_context_retrieve"
      pattern: "reviewed_memory_context_retrieve"
    - from: src/agent/nodes/memory_context_load.py
      to: llm_outputs
      via: "canonical metrics key"
      pattern: "\"memory_context_load\""
    - from: src/agent/nodes/long_term_memory_retrieve.py
      to: src/agent/nodes/memory_context_load.py
      via: "compatibility wrapper delegates to canonical node"
      pattern: "memory_context_load"
    - from: tests/agent/test_memory_evidence_boundary.py
      to: src/memory/context_refs.py
      via: "contextual-only memory refs stay non-authoritative"
      pattern: "contextual_only"
---

<objective>
Create the canonical `memory_context_load` node contract before active graph cutover.

Purpose: CAGM-06 requires a canonical graph node that loads reviewed memory/CWC as context only. This plan creates that node and locks its metrics, labels, fail-closed behavior, and authority boundary before Plan 55-02 routes live traffic through it.
Output: `src/agent/nodes/memory_context_load.py`, compatibility wrapper updates, and focused node/security tests.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/55-memory-context-load-cutover/55-CONTEXT.md
@.planning/phases/55-memory-context-load-cutover/55-RESEARCH.md
@.planning/phases/55-memory-context-load-cutover/55-PATTERNS.md
@.planning/phases/55-memory-context-load-cutover/55-VALIDATION.md
@.planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
@.planning/phases/54-slot-resolution-gate-cutover/54-VERIFICATION.md
@src/agent/nodes/long_term_memory_retrieve.py
@src/agent/nodes/reviewed_memory_context_retrieve.py
@src/memory/context_refs.py
@src/memory/context_service.py
@tests/agent/test_reviewed_memory_context_retrieve.py
@tests/agent/test_memory_evidence_boundary.py
@tests/memory/test_reviewed_memory_context_boundary.py

<interfaces>
Current reviewed-memory implementation to delegate to:
```python
async def reviewed_memory_context_retrieve(
    state: AgentState,
    config: RunnableConfig,
    *,
    memory_context_service_cls: Any | None = None,
    long_term_memory_repository_cls: Any | None = None,
    case_memory_repository_cls: Any | None = None,
    long_term_memory_service_cls: Any | None = None,
    case_memory_service_cls: Any | None = None,
    case_working_context_lifecycle_adapter_cls: Any | None = None,
) -> dict: ...
```

Canonical metrics target for Phase 55:
```python
{
    "source": "reviewed_memory" | "reviewed_memory_unavailable" | "reviewed_memory_skipped" | "no_reviewed_memory",
    "authority_class": "contextual_only",
    "usage_labels": list[str],
    "long_term_count": int,
    "case_count": int,
    "fallback_reason": str | None,
    "filter_reasons": list[str],
}
```

Finite `usage_labels` values:
```python
{
    "session_continuity",
    "explicit_preference_memory",
    "reviewed_case_precedent",
    "case_working_context_status",
    "reviewed_memory_skipped",
    "reviewed_memory_unavailable",
}
```
</interfaces>
</context>

<source_coverage_audit>
| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Replace active `long_term_memory_retrieve` naming with `memory_context_load` and contextual-only authority | Plan 55-01 creates canonical node and labels; Plan 55-02 makes it active; Plan 55-03 documents projection/ledger. |
| REQ | CAGM-06 | Covered by all three plans in `requirements`. |
| RESEARCH | Canonical active node delegates to existing reviewed-memory loader | Task 1 implements this directly. |
| RESEARCH | Wave 0 gap `tests/agent/test_memory_context_load.py` | Task 1 creates the file before implementation verification. |
| CONTEXT | D-55-04, D-55-05, D-55-06, D-55-07, D-55-08 | Tasks 1-2 implement and test node ownership, labels, fail-closed status, and authority boundary. |
| CONTEXT | Deferred Phase 56/57/58 ideas | Excluded; only Phase 58 delete phase is named for retained compatibility. |
</source_coverage_audit>

<compatibility_table>
| Legacy surface | Canonical owner | Reason | Trace projection | Validation | Delete phase |
|----------------|-----------------|--------|------------------|------------|--------------|
| `src/agent/nodes/long_term_memory_retrieve.py` import/test wrapper | `memory_context_load` | Historical tests/imports may still call the old wrapper while active graph cutover happens in 55-02 | Direct wrapper invocations may keep `llm_outputs["long_term_memory_retrieve"]`; active canonical node writes `llm_outputs["memory_context_load"]` | `tests/agent/test_memory_context_load.py` proves wrapper delegates and canonical node metrics exist | Phase 58 |
| `reviewed_memory_context_retrieve` implementation helper | `memory_context_load` | Existing reviewed-memory/CWC loader owns service semantics; Phase 55 should not duplicate storage/service logic | Active canonical node trace step is `memory_context_load`; direct helper tests may keep helper trace identity | `tests/agent/test_reviewed_memory_context_retrieve.py` plus new canonical-node tests | Phase 58 or internal-only helper reclassification in Phase 58 |
</compatibility_table>

<must_haves>
- Active node contract exists before graph cutover.
- Canonical metrics are observable under `llm_outputs["memory_context_load"]`.
- Memory/CWC authority remains `contextual_only`.
- Legacy wrapper remains non-active compatibility only.
- Existing reviewed-memory fail-closed behavior is preserved.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create canonical memory_context_load node and focused tests</name>
  <files>src/agent/nodes/memory_context_load.py, src/agent/nodes/long_term_memory_retrieve.py, tests/agent/test_memory_context_load.py, tests/agent/test_reviewed_memory_context_retrieve.py</files>
  <read_first>
    - src/agent/nodes/long_term_memory_retrieve.py
    - src/agent/nodes/reviewed_memory_context_retrieve.py
    - src/memory/context_refs.py
    - src/memory/context_service.py
    - tests/agent/test_reviewed_memory_context_retrieve.py
    - .planning/phases/55-memory-context-load-cutover/55-CONTEXT.md
    - .planning/phases/55-memory-context-load-cutover/55-RESEARCH.md
    - .planning/phases/55-memory-context-load-cutover/55-VALIDATION.md
    - .planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
  </read_first>
  <behavior>
    - `from src.agent.nodes.memory_context_load import memory_context_load` succeeds.
    - `memory_context_load(state, config)` calls `reviewed_memory_context_retrieve(...)` with the same injectable repository/service class arguments supported by the reviewed-memory helper.
    - Successful loads write `llm_outputs["memory_context_load"]`.
    - `llm_outputs["memory_context_load"]["authority_class"] == "contextual_only"`.
    - `llm_outputs["memory_context_load"]["usage_labels"]` is a subset of `{"session_continuity", "explicit_preference_memory", "reviewed_case_precedent", "case_working_context_status", "reviewed_memory_skipped", "reviewed_memory_unavailable"}`.
    - Non-empty `long_term_memory` adds `explicit_preference_memory`.
    - Non-empty `case_memory` adds `reviewed_case_precedent`.
    - Present `case_working_context_lifecycle_status` adds `case_working_context_status`.
    - Missing trusted context produces empty long-term/case memory and a skipped status, not an exception.
    - Service errors produce unavailable/skipped metrics and retain `fallback_reason == "service_error"`.
    - Canonical trace steps emitted by `memory_context_load` use exact `node == "memory_context_load"` and metrics equal to `llm_outputs["memory_context_load"]`.
    - `long_term_memory_retrieve(...)` delegates to `memory_context_load(...)`, keeps legacy metrics under `llm_outputs["long_term_memory_retrieve"]`, and does not become the canonical owner.
  </behavior>
  <action>
    Create `src/agent/nodes/memory_context_load.py` per D-55-04, D-55-05, D-55-06, D-55-08, and CAGM-06. The exported function must be `async def memory_context_load(state: AgentState, config: RunnableConfig, *, memory_context_service_cls: Any | None = None, long_term_memory_repository_cls: Any | None = None, case_memory_repository_cls: Any | None = None, long_term_memory_service_cls: Any | None = None, case_memory_service_cls: Any | None = None, case_working_context_lifecycle_adapter_cls: Any | None = None) -> dict`. It must delegate to `reviewed_memory_context_retrieve(...)` and pass through the injectable classes to preserve existing test seams. After delegation, compute canonical metrics under `llm_outputs["memory_context_load"]` with exact keys `source`, `authority_class`, `usage_labels`, `long_term_count`, `case_count`, `fallback_reason`, and `filter_reasons`. The `authority_class` value is exactly `"contextual_only"`. Use finite usage labels only from `session_continuity`, `explicit_preference_memory`, `reviewed_case_precedent`, `case_working_context_status`, `reviewed_memory_skipped`, and `reviewed_memory_unavailable`. Derive counts from `result["long_term_memory"]` and `result["case_memory"]` when they are lists. Derive `fallback_reason` and `filter_reasons` from `result["reviewed_memory_context_retrieve_status"]` when it is a mapping. Derive `source` using the existing reviewed-memory semantics: `reviewed_memory` when long-term or case items exist, `reviewed_memory_unavailable` when fallback reason is `service_error` or `missing_memory_context_services`, `reviewed_memory_skipped` when any other fallback reason exists, otherwise `no_reviewed_memory`. Convert the canonical node trace step so the active trace contains `node: "memory_context_load"` with the canonical metrics; do not leave a canonical active run with only `node: "reviewed_memory_context_retrieve"` in `trace_steps`. Map `node_errors` entries with `node == "reviewed_memory_context_retrieve"` to `node == "memory_context_load"` for the canonical node return. Update `src/agent/nodes/long_term_memory_retrieve.py` so the compatibility wrapper imports and calls `memory_context_load(...)`, then appends only the legacy `llm_outputs["long_term_memory_retrieve"]` metrics produced by `_legacy_metrics(...)`. The active canonical node should not write `llm_outputs["long_term_memory_retrieve"]`; legacy metrics live in the wrapper path only. Create `tests/agent/test_memory_context_load.py` before implementation assertions go green. Include direct tests for canonical metrics, finite labels, canonical trace node name, missing trusted context skip, service error/unavailable status, and direct wrapper compatibility. Add narrow `tests/agent/test_reviewed_memory_context_retrieve.py` coverage only if existing helper tests need to assert helper identity remains direct-helper identity.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_memory_context_load.py tests/agent/test_reviewed_memory_context_retrieve.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/nodes/memory_context_load.py src/agent/nodes/long_term_memory_retrieve.py tests/agent/test_memory_context_load.py tests/agent/test_reviewed_memory_context_retrieve.py</automated>
  </verify>
  <acceptance_criteria>
    - `src/agent/nodes/memory_context_load.py` exists and contains `async def memory_context_load`.
    - `src/agent/nodes/memory_context_load.py` imports `reviewed_memory_context_retrieve`.
    - `tests/agent/test_memory_context_load.py` exists and contains tests asserting `llm_outputs["memory_context_load"]["authority_class"] == "contextual_only"`.
    - `tests/agent/test_memory_context_load.py` asserts all `usage_labels` values are from the finite allowed set.
    - `tests/agent/test_memory_context_load.py` asserts the canonical trace step node is `memory_context_load`.
    - `src/agent/nodes/long_term_memory_retrieve.py` imports `memory_context_load` and still writes `llm_outputs["long_term_memory_retrieve"]` only inside the compatibility wrapper.
    - Focused pytest and Ruff commands pass using `UV_CACHE_DIR=/tmp/uv-cache uv run ...`.
  </acceptance_criteria>
  <done>`memory_context_load` is a tested canonical node owner with contextual-only metrics and a bounded compatibility wrapper for `long_term_memory_retrieve`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock memory authority boundary against evidence/action/replay use</name>
  <files>tests/agent/test_memory_evidence_boundary.py, tests/agent/test_memory_context_load.py</files>
  <read_first>
    - tests/agent/test_memory_evidence_boundary.py
    - tests/agent/test_memory_context_load.py
    - src/agent/nodes/memory_context_load.py
    - src/memory/context_refs.py
    - src/tools/contracts.py
    - src/knowledge/schemas.py
    - .planning/phases/55-memory-context-load-cutover/55-CONTEXT.md
    - .planning/phases/55-memory-context-load-cutover/55-VALIDATION.md
    - docs/contract-spec.md
  </read_first>
  <behavior>
    - Canonical `memory_context_load` metrics and usage labels are not accepted as `EvidenceRefV1`.
    - Canonical `memory_context_load` metrics and usage labels are not accepted as `BusinessFactRefV1`.
    - Reviewed memory refs and CWC status remain contextual-only and cannot create approval decisions, action drafts, action authorization, or replay truth.
    - Existing tests for session context, reviewed case memory, long-term preference memory, and CWC authority still pass.
  </behavior>
  <action>
    Extend the existing memory-evidence boundary tests per D-55-05, D-55-07, D-55-09, D-55-10, and D-55-11. Add assertions that the canonical metrics payload from `llm_outputs["memory_context_load"]`, including `usage_labels` and `authority_class`, is treated as contextual metadata only and cannot be parsed or projected as `EvidenceRefV1` or `BusinessFactRefV1`. Reuse the existing boundary-test style instead of adding a new authority model. Do not introduce policy evidence, current business fact refs, approval decisions, action drafts, action authorization, or replay truth from memory payloads. Keep Phase 46 `session_context`, Phase 47 reviewed case precedent/CWC separation, and Phase 48 explicit preference-only long-term memory semantics unchanged. If a local validation command or test selector fails during execution, append the incident to `.planning/LOCAL-VALIDATION-ISSUES.md` in Chinese per AGENTS.md before closing the plan summary.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_memory_context_load.py tests/agent/test_memory_evidence_boundary.py tests/memory/test_reviewed_memory_context_boundary.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/memory/test_phase46_session_context_alignment.py tests/memory/test_phase47_case_precedent_alignment.py tests/memory/test_phase48_long_term_preference_alignment.py tests/memory/test_phase48_1_memory_compat_alignment.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check tests/agent/test_memory_evidence_boundary.py tests/agent/test_memory_context_load.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/agent/test_memory_evidence_boundary.py` contains a canonical `memory_context_load` assertion.
    - Boundary tests prove memory metrics/labels do not satisfy `EvidenceRefV1`.
    - Boundary tests prove memory metrics/labels do not satisfy `BusinessFactRefV1`.
    - Existing Phase 46/47/48/48.1 memory-layer alignment tests pass with the approved uv entrypoint.
    - No test command uses bare `pytest` or bare `python -m pytest`.
  </acceptance_criteria>
  <done>Canonical memory labels are explicitly contextual-only and cannot be promoted into evidence, business facts, action authority, approval authority, or replay truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| graph node -> memory services | `memory_context_load` delegates into existing memory repositories/services and receives trusted-scope-filtered memory results. |
| memory payload -> agent state | Loaded memory/CWC crosses into `AgentState` and prompt/trace surfaces as contextual metadata. |
| memory payload -> authority DTOs | Boundary tests must prevent contextual memory from satisfying evidence, business fact, approval/action, or replay authority. |
| compatibility wrapper -> canonical node | Direct legacy wrapper calls must not become a second semantic owner. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-55-01-01 | Information Disclosure / Elevation of Privilege | HIGH | `memory_context_load` trusted-scope delegation | mitigate | Reuse `reviewed_memory_context_retrieve` / `MemoryContextService` trusted-scope filtering and run missing/denied scope tests. |
| T-55-01-02 | Tampering | HIGH | memory authority labels | mitigate | Canonical metrics must include `authority_class == "contextual_only"` and finite usage labels; tests assert this exact payload. |
| T-55-01-03 | Elevation of Privilege | HIGH | evidence/business/action/replay boundaries | mitigate | Extend `test_memory_evidence_boundary.py` so memory labels/refs cannot satisfy `EvidenceRefV1`, `BusinessFactRefV1`, approvals, action drafts, action authorization, or replay truth. |
| T-55-01-04 | Denial of Service | MEDIUM | unavailable memory services | mitigate | Preserve fail-closed `service_error` / skipped behavior and expose explicit unavailable/skipped metrics without raising. |
| T-55-01-05 | Repudiation | MEDIUM | trace node identity | mitigate | Canonical node returns trace step `node == "memory_context_load"`; helper/wrapper compatibility is separately documented. |
</threat_model>

<verification>
Plan 55-01 is complete when the new canonical node tests, reviewed-memory helper tests, memory authority boundary tests, Phase 46/47/48/48.1 alignment tests, and Ruff commands all pass through `UV_CACHE_DIR=/tmp/uv-cache uv run ...`.
</verification>

<success_criteria>
- `memory_context_load` exists as the canonical node owner and has focused tests.
- Canonical metrics are written to `llm_outputs["memory_context_load"]`.
- Usage labels are finite and contextual-only.
- Fail-closed missing/denied/unavailable memory behavior is preserved.
- `long_term_memory_retrieve` remains a compatibility wrapper only.
- CAGM-06 authority boundary has automated evidence/action/replay regression coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/55-memory-context-load-cutover/55-01-SUMMARY.md`.
</output>
