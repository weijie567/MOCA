---
phase: 61-product-experience-fixes
plan: 05
type: execute
wave: 5
depends_on:
  - 61-04
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/playwright.config.ts
  - frontend/e2e/agent-console.spec.ts
  - frontend/src/types/events.ts
  - frontend/src/hooks/useAgentRun.ts
  - frontend/src/hooks/useAgentRun.test.ts
  - frontend/src/components/timeline/TimelineStep.tsx
  - frontend/src/components/timeline/AgentTimeline.tsx
  - evaluation/golden/phase61_ux_cases.jsonl
  - tests/eval/test_phase61_ux_golden.py
  - scripts/eval_phase61_ux.py
  - .planning/LOCAL-VALIDATION-ISSUES.md
  - .planning/ARCHITECTURE-DEBT.md
autonomous: true
requirements:
  - CONSOLE-01
  - CONSOLE-02
  - CONSOLE-03
  - EVAL-01
  - EVAL-02
  - EVAL-03
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - MET-01
  - MET-02
  - MET-03
  - MET-04
  - SCOPE-01
  - SCOPE-02
  - SCOPE-03
  - SCOPE-04
requirements_addressed:
  - CONSOLE-01
  - CONSOLE-02
  - CONSOLE-03
  - EVAL-01
  - EVAL-02
  - EVAL-03
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - MET-01
  - MET-02
  - MET-03
  - MET-04
  - SCOPE-01
  - SCOPE-02
  - SCOPE-03
  - SCOPE-04
user_setup: []
must_haves:
  truths:
    - "Timeline labels distinguish direct response, clarification, unsupported, metric answer, RAG answer, and tool calls."
    - "Timeline stays compact; detailed metric scope/freshness remains mainly in the final chat answer."
    - "Unsupported/clarification/error outcomes do not leak stale state into the next run."
    - "Regression coverage includes a large Phase 61 golden set plus full Playwright E2E."
  artifacts:
    - path: "frontend/src/components/timeline/TimelineStep.tsx"
      provides: "Payload-aware safe timeline rendering"
    - path: "frontend/e2e/agent-console.spec.ts"
      provides: "Playwright demo E2E coverage"
    - path: "evaluation/golden/phase61_ux_cases.jsonl"
      provides: "Phase 61 UX/metric golden set"
    - path: ".planning/LOCAL-VALIDATION-ISSUES.md"
      provides: "Required local validation issue/result log"
---

<objective>
Polish Agent Console timeline UX and add regression validation for Phase 61.

Purpose: make the product-facing console clearly distinguish result types and lock the known bad prompts plus role/scope metric cases in repeatable tests.
Output: frontend timeline rendering, frontend tests, Playwright E2E, Phase 61 golden set, and local validation documentation.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/61-product-experience-fixes/61-CONTEXT.md
@.planning/phases/61-product-experience-fixes/61-RESEARCH.md
@.planning/phases/61-product-experience-fixes/61-PATTERNS.md
@.planning/phases/61-product-experience-fixes/61-VALIDATION.md
@.planning/phases/61-product-experience-fixes/61-UI-SPEC.md
@frontend/src/components/timeline/TimelineStep.tsx
@frontend/src/hooks/useAgentRun.ts
@src/api/routers/agent_runs.py
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render safe timeline labels from backend response kind payloads</name>
  <files>frontend/src/types/events.ts, frontend/src/components/timeline/TimelineStep.tsx, frontend/src/components/timeline/AgentTimeline.tsx, frontend/src/hooks/useAgentRun.test.ts</files>
  <read_first>
    frontend/src/types/events.ts
    frontend/src/components/timeline/TimelineStep.tsx
    frontend/src/components/timeline/AgentTimeline.tsx
    frontend/src/hooks/useAgentRun.test.ts
    src/api/routers/agent_runs.py
    .planning/phases/61-product-experience-fixes/61-UI-SPEC.md
  </read_first>
  <behavior>
    - Direct response displays as direct response or small talk, not as policy investigation.
    - Clarification displays safe reason such as missing time range or missing identifier.
    - Unsupported displays safe capability boundary reason.
    - Metric answer displays "正在查询业务指标" and compact subtitle such as `metric: refund_case_count · scope: 当前权限范围`.
    - Tool calls display tool labels without raw args or debug payload.
  </behavior>
  <action>
    Extend frontend event payload types with `response_kind`, `safe_reason`, `metric_id`, `metric_label`, `scope_label`, and optional `tool_label`. Update `TimelineStep` label/subtitle helpers to prefer safe payload labels over static node names. Keep layout compact and avoid putting detailed metric scope/freshness in the timeline; final chat answer carries details. Add Vitest coverage by rendering representative `SseEvent` values or testing helper functions.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run</automated>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Frontend tests cover at least direct response, clarification, unsupported, metric answer, RAG answer, and tool-call labels.
    - Timeline never renders raw `routing_hints`, merchant ids from unauthorized requests, or internal debug strings.
    - Text remains within timeline layout at desktop and mobile widths in Playwright screenshots from Task 3.
  </acceptance_criteria>
  <done>Timeline labels distinguish result types safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock stale-state reset for retry and new conversation flows</name>
  <files>frontend/src/hooks/useAgentRun.ts, frontend/src/hooks/useAgentRun.test.ts</files>
  <read_first>
    frontend/src/hooks/useAgentRun.ts
    frontend/src/hooks/useAgentRun.test.ts
  </read_first>
  <behavior>
    - Unsupported, clarification, and error final responses do not keep stale timeline/message state for the next run.
    - New conversation creates a new thread id and clears run state.
    - Consecutive queries in the same conversation preserve prior chat messages but do not merge old timeline steps into the active run.
  </behavior>
  <action>
    Extend hook tests around existing state reset/upsert behavior. Only change hook logic if tests show stale state leaks. Preserve current behavior where completed prior chat messages remain visible in the conversation.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run frontend/src/hooks/useAgentRun.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove a metric/clarification/unsupported run followed by a new query starts with fresh active steps.
    - Tests prove prior messages are not erased accidentally in same-thread conversation history.
  </acceptance_criteria>
  <done>Retry/new-query active state is regression-covered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Playwright E2E for live Agent Console demo flows</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/playwright.config.ts, frontend/e2e/agent-console.spec.ts</files>
  <read_first>
    frontend/package.json
    frontend/src/App.tsx
    frontend/src/lib/api.ts
    docker-compose.yml
    README.md
    .planning/phases/61-product-experience-fixes/61-UI-SPEC.md
  </read_first>
  <behavior>
    - Playwright can run against local Vite/frontend and backend API with real SSE/API behavior.
    - E2E covers `你好`, missing time metric clarification, completed metric answer, unsupported capability, and role/scope denial.
    - Screenshots or assertions verify timeline labels are visible and non-overlapping enough for demo use.
  </behavior>
  <action>
    Add Playwright as a frontend dev dependency and `npm run e2e` script. Create config with webServer entries or documented expectation for existing local services. Mocked Playwright tests may be added for fast label/layout checks, but they are not sufficient for Phase 61 completion. Add a live tier, either included in `npm run e2e` by default or exposed as `npm run e2e:live` and required by final validation, that targets real frontend plus backend API/SSE and deterministic seeded demo flows. Do not skip the live E2E file; user explicitly selected full Playwright E2E.
  </action>
  <verify>
    <automated>cd frontend && npm run e2e</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/playwright.config.ts` exists.
    - `frontend/e2e/agent-console.spec.ts` includes the Phase 61 known bad prompts and metric/timeline expectations.
    - E2E command is documented in frontend package scripts.
    - At least one live Playwright spec logs in through demo auth and exercises real `/api/v1/agent-runs` SSE for `你好`, `当前有多少订单`, a completed metric query, unsupported capability, and unauthorized merchant metric denial.
  </acceptance_criteria>
  <done>Playwright E2E infrastructure covers the console UX flows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add Phase 61 UX/metric golden regression set</name>
  <files>evaluation/golden/phase61_ux_cases.jsonl, tests/eval/test_phase61_ux_golden.py, scripts/eval_phase61_ux.py</files>
  <read_first>
    evaluation/golden/agent_cases.jsonl
    scripts/eval_agent.py
    tests/agent/test_intent_golden_contract.py
    .planning/phases/61-product-experience-fixes/61-CONTEXT.md
  </read_first>
  <behavior>
    - Golden set includes known bad prompts: `你好`, `当前有多少订单`, missing-ID status queries, today refund count, weekly coupon count, month/quarter/year ranges, merchant refund rate, and unauthorized merchant metric queries.
    - Golden set includes support, manager, and admin role/scope cases.
    - Tests assert coverage categories and must-not-contain phrases such as false evidence wording or external coupon success claims.
  </behavior>
  <action>
    Create a separate Phase 61 JSONL golden file rather than overloading Phase 11 intent admission data. Add a lightweight test that validates required categories, role coverage, expected response kind, expected route/tool where applicable, and forbidden phrases. Add or adapt an evaluation script if it can reuse existing `scripts/eval_agent.py` patterns without requiring live LLM for CI.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_phase61_ux_golden.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - Golden cases cover all Phase 61 required prompt families and roles.
    - Golden tests fail if no unauthorized merchant metric case exists.
    - Golden tests fail if coupon count cases omit the demo-record caveat expectation.
  </acceptance_criteria>
  <done>Phase 61 UX regression set is repeatable and category-covered.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Run local validation and record issues/debt</name>
  <files>.planning/LOCAL-VALIDATION-ISSUES.md, .planning/ARCHITECTURE-DEBT.md</files>
  <read_first>
    AGENTS.md
    .planning/LOCAL-VALIDATION-ISSUES.md
    .planning/ARCHITECTURE-DEBT.md
    .planning/phases/61-product-experience-fixes/61-VALIDATION.md
  </read_first>
  <behavior>
    - Local UI/API validation result is recorded.
    - Any issues discovered during local debug/UI/API/RAG/agent/tool validation are appended to `.planning/LOCAL-VALIDATION-ISSUES.md`.
    - Any intent/tool/RAG/memory architecture debt found or fixed is appended to `.planning/ARCHITECTURE-DEBT.md`.
  </behavior>
  <action>
    Run final focused commands, start local services if needed, validate key Agent Console prompts, and record results according to `AGENTS.md`. If no issue occurs, record the validation outcome in Phase 61 summary or validation artifact; if an issue occurs, append it to `.planning/LOCAL-VALIDATION-ISSUES.md` with symptom, reproduction, evidence, root-cause judgment, treatment, residual risk, and next entry point.
  </action>
  <verify>
    <automated>git diff --check</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_contextual_intent_resolve.py tests/agent/test_nodes/test_slot_resolution_gate.py tests/agent/test_nodes/test_investigate.py tests/agent/test_nodes/test_final_response.py tests/business/test_service.py tests/tools/test_tool_platform.py tests/test_agent_runs_api.py tests/eval/test_phase61_ux_golden.py -q --tb=short</automated>
    <automated>cd frontend && npm run test -- --run && npm run build && npm run e2e</automated>
    <automated>cd frontend && npm run e2e:live</automated>
  </verify>
  <acceptance_criteria>
    - Final validation commands and outcomes are recorded in `61-05-SUMMARY.md`.
    - `.planning/LOCAL-VALIDATION-ISSUES.md` is updated for any discovered local validation issue.
    - `.planning/ARCHITECTURE-DEBT.md` is updated if Phase 61 changes uncover/fix subsystem-level debt.
  </acceptance_criteria>
  <done>Phase 61 validation evidence is documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend SSE payload -> frontend timeline | Frontend must render only safe projected labels and reasons. |
| Prior run state -> next run state | Hook state must not leak prior run timeline/result into a new active run. |
| Golden/E2E tests -> release confidence | Regression set must cover actual known UX failures and role/scope boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-05-01 | Information disclosure | timeline payload rendering | mitigate | Render only safe payload fields and test absence of raw internals. |
| T-61-05-02 | Tampering | stale client state | mitigate | Hook tests for unsupported/clarification/error followed by new query. |
| T-61-05-03 | Repudiation | UX regression coverage | mitigate | Golden cases and Playwright flows provide repeatable evidence. |
| T-61-05-04 | Information disclosure | role/scope E2E | mitigate | Unauthorized merchant metric flow must show no-existence-leak wording. |
</threat_model>

<verification>
Plan-local commands:
- `cd frontend && npm run test -- --run`
- `cd frontend && npm run build`
- `cd frontend && npm run e2e`
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/eval/test_phase61_ux_golden.py -q --tb=short`
- `git diff --check`
</verification>

<source_audit>
GOAL covered: console timeline UX, retry/new-query state, regression and local validation evidence.
Requirements completed: CONSOLE-01, CONSOLE-02, CONSOLE-03, EVAL-01, EVAL-02, EVAL-03, with final cross-plan validation and requirement completion for UX/MET/SCOPE.
Deferred ideas excluded: dashboards, charts, exports, scheduled reports, external coupon success integration.
</source_audit>

<success_criteria>
Agent Console displays safe, distinct result labels and Phase 61's known bad prompts/role-scope cases are protected by Vitest, Playwright, and golden regression coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/61-product-experience-fixes/61-05-SUMMARY.md` and update Phase 61 verification artifacts.
</output>
