---
phase: 61-product-experience-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/nodes/contextual_intent_resolve.py
  - src/agent/nodes/clarification_gate.py
  - src/agent/nodes/final_response.py
  - tests/agent/test_nodes/test_contextual_intent_resolve.py
  - tests/agent/test_clarification_gate.py
  - tests/agent/test_nodes/test_final_response.py
autonomous: true
requirements: []
requirements_addressed:
  - UX-01
  - UX-02
  - UX-03
  - UX-04
user_setup: []
must_haves:
  truths:
    - "Standalone greetings remain direct deterministic responses and do not call RAG, tools, or LLM fallback templates."
    - "Unsupported and clarification responses explain the user-safe capability/input boundary without internal debug details."
    - "Final responses never claim policy/RAG evidence unless verified evidence or policy refs exist."
    - "Current aggregate-order wording is treated as a temporary baseline only; Plan 61-02 will reclassify supported metric questions."
  artifacts:
    - path: "src/agent/nodes/final_response.py"
      provides: "Direct, unsupported, clarification, and no-false-evidence response templates"
    - path: "src/agent/nodes/clarification_gate.py"
      provides: "Safe clarification reason and accepted-input wording"
    - path: "tests/agent/test_nodes/test_final_response.py"
      provides: "UX baseline regression tests"
---

<objective>
Lock the Agent Response UX baseline before adding metric capability.

Purpose: prevent the recent confusing behavior from coming back while later plans replace aggregate-order unsupported behavior with a scoped metric query.
Output: deterministic small-talk, unsupported, clarification, and no-false-evidence response tests plus focused wording fixes.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/61-product-experience-fixes/61-CONTEXT.md
@.planning/phases/61-product-experience-fixes/61-RESEARCH.md
@.planning/phases/61-product-experience-fixes/61-PATTERNS.md
@.planning/phases/61-product-experience-fixes/61-VALIDATION.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand direct-response UX regression coverage</name>
  <files>tests/agent/test_nodes/test_contextual_intent_resolve.py, tests/agent/test_nodes/test_final_response.py, src/agent/nodes/contextual_intent_resolve.py, src/agent/nodes/final_response.py</files>
  <read_first>
    AGENTS.md
    .planning/phases/61-product-experience-fixes/61-CONTEXT.md
    src/agent/nodes/contextual_intent_resolve.py
    src/agent/nodes/final_response.py
    tests/agent/test_nodes/test_contextual_intent_resolve.py
    tests/agent/test_nodes/test_final_response.py
  </read_first>
  <behavior>
    - `你好`, `您好`, `hi`, and `谢谢` resolve to `small_talk` through the deterministic guard.
    - Standalone small talk does not invoke the LLM in node tests.
    - Small-talk final response contains a useful entry point and does not mention evidence, retrieved policy, or default recommendation text.
    - Out-of-domain unsupported requests get a capability-boundary response with supported alternatives.
  </behavior>
  <action>
    Add focused tests first, then adjust deterministic guards/templates only as needed. Keep the current direct-response architecture: `small_talk` and `unsupported` remain `direct_response=True`, `evidence_required=False`, and route to `final_response`. Do not add metric intent or metric slots in this plan. Do not remove the temporary aggregate-order unsupported guard yet; mark tests or comments so Plan 61-02 intentionally changes that behavior.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_contextual_intent_resolve.py tests/agent/test_nodes/test_final_response.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove standalone greetings do not produce policy-evidence wording.
    - Tests prove unsupported direct responses list supported alternatives without asking for an irrelevant order id.
    - `llm_outputs["final_response"]` identifies direct-response intent for small talk and unsupported responses.
  </acceptance_criteria>
  <done>Direct-response baseline is test-locked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Improve clarification wording without leaking internals</name>
  <files>src/agent/nodes/clarification_gate.py, src/agent/nodes/final_response.py, tests/agent/test_clarification_gate.py, tests/agent/test_nodes/test_final_response.py</files>
  <read_first>
    src/agent/nodes/clarification_gate.py
    src/agent/nodes/final_response.py
    tests/agent/test_clarification_gate.py
    tests/agent/test_nodes/test_final_response.py
  </read_first>
  <behavior>
    - Missing identifier clarification explains accepted identifiers: order number, refund case number, or ticket number.
    - Clarification responses do not expose raw `routing_hints`, blocked node lists, permission errors, trace internals, or debug labels.
    - Final response preserves clarification text exactly except for approved deferred-step decoration.
  </behavior>
  <action>
    Extend safe clarification questions and tests for missing-ID cases. Keep `ClarificationRequest.reason` values unchanged in this plan. Do not add metric-specific clarification yet; Plan 61-02 owns metric missing-slot semantics.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_clarification_gate.py tests/agent/test_nodes/test_final_response.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - Missing identifier response explains why an identifier is needed and what inputs are accepted.
    - Clarification final response contains no internal node names except user-visible labels already accepted by existing tests.
  </acceptance_criteria>
  <done>Clarification baseline is clear and safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add no-false-evidence response guard tests</name>
  <files>src/agent/nodes/final_response.py, tests/agent/test_nodes/test_final_response.py</files>
  <read_first>
    src/agent/nodes/final_response.py
    tests/agent/test_nodes/test_final_response.py
    src/agent/rag_context/schemas.py
    src/agent/nodes/rag_context_build.py
  </read_first>
  <behavior>
    - Direct/unsupported/clarification/business-fact-only paths do not say "根据已检索到的政策依据" unless evidence refs exist.
    - Business fact-only order status answers remain possible without policy citations.
    - Insufficient evidence wording remains explicit when evidence is missing.
  </behavior>
  <action>
    Add regression assertions around existing final-response branches. If implementation currently shares a default "suggestion" template that claims evidence for non-evidence paths, split that branch into explicit deterministic helpers. Keep behavior scoped to final response; do not modify RAG retrieval or claim verification.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_final_response.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail if small talk, unsupported, clarification, or business fact-only responses claim RAG/policy evidence.
    - Existing evidence-backed policy/recommendation branches still cite evidence when valid evidence exists.
  </acceptance_criteria>
  <done>No-false-evidence UX guard is covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User text -> deterministic direct response | Short small-talk text can bypass LLM, but cannot create facts or evidence claims. |
| Routing hints -> user-visible wording | Internal hints must be projected to safe reasons only. |
| Evidence state -> final response | Final text must not claim evidence unless evidence refs/verified packages exist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01-01 | Spoofing | small-talk guard | mitigate | Tests prove business-keyword text does not accidentally route as standalone small talk. |
| T-61-01-02 | Information disclosure | clarification/unsupported wording | mitigate | Tests reject internal routing/debug strings in user-visible text. |
| T-61-01-03 | Tampering | final evidence wording | mitigate | Branch-specific final-response tests prevent false RAG/policy claims. |
</threat_model>

<verification>
Plan-local command:
`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_contextual_intent_resolve.py tests/agent/test_clarification_gate.py tests/agent/test_nodes/test_final_response.py -q --tb=short`
</verification>

<source_audit>
GOAL covered: misleading direct responses, unsupported wording, clarification wording, and false evidence language.
Requirements addressed: UX-01, UX-02, UX-03, UX-04. `requirements` is intentionally empty so execute-plan does not mark user-facing UX requirements complete before final cross-plan regression validation in 61-05.
Deferred to later plans: metric intent, metric runtime, timeline labels, golden/Playwright coverage.
</source_audit>

<success_criteria>
Small talk, unsupported, clarification, and non-evidence response branches are deterministic, clear, and covered by focused regression tests.
</success_criteria>

<output>
After completion, create `.planning/phases/61-product-experience-fixes/61-01-SUMMARY.md`.
</output>
