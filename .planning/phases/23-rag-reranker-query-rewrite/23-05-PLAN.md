---
phase: 23-rag-reranker-query-rewrite
plan: "05"
type: execute
wave: 5
depends_on:
  - "23-04"
files_modified:
  - src/knowledge/diagnostics.py
  - src/knowledge/service.py
  - src/knowledge/retrieval.py
  - scripts/eval_rag_ablation.py
  - evaluation/golden/rag_cases.jsonl
  - tests/knowledge/test_retrieval_diagnostics.py
  - tests/knowledge/test_retrieval_budgets.py
  - tests/test_rag_ablation_eval.py
autonomous: true
requirements:
  - EXP-01
  - EXP-02
  - EXP-03
  - EXP-04
  - EVAL-01
  - EVAL-02
  - EVAL-03
  - EVAL-04
  - EVAL-05
  - QRW-05
  - RRK-05
  - BND-01
  - BND-02
  - BND-03
user_setup: []
must_haves:
  truths:
    - "Maintainer/eval diagnostics expose selected channels, rewrite contribution, rerank contribution, rank changes, safe score components, provider/config versions, selected candidate IDs, and fallback reasons."
    - "Diagnostics are internal/report-only and do not appear in ordinary prompts, final responses, memory, replay payloads, approval snapshots, action drafts, business facts, or EvidenceRefV1."
    - "A deterministic no-live-provider ablation script compares dense-only, sparse-only, fuzzy-only, RRF baseline, rewrite-enabled, reranker-enabled, and rewrite-plus-reranker variants."
    - "Ablation reports blocking Hit@K, MRR, citation-support compatibility, no-evidence precision, unsafe retrieval rate, fallback rate, and latency percentiles."
    - "Latency budgets are explicit and fallback paths remain safe."
  artifacts:
    - path: src/knowledge/diagnostics.py
      provides: "Strict internal RetrievalDiagnostics and RankingExplanation DTOs"
    - path: scripts/eval_rag_ablation.py
      provides: "No-live-provider deterministic ablation runner"
    - path: evaluation/golden/rag_cases.jsonl
      provides: "Phase 23 golden categories appended without deleting existing cases"
    - path: tests/test_rag_ablation_eval.py
      provides: "Ablation variant, metric, and report redaction tests"
  key_links:
    - from: scripts/eval_rag_ablation.py
      to: src/knowledge/retrieval.py
      via: "variant config/fake adapters"
      pattern: "dense_only|rewrite_plus_reranker"
    - from: src/knowledge/service.py
      to: src/knowledge/diagnostics.py
      via: "internal diagnostic run object"
      pattern: "RetrievalDiagnostics|RankingExplanation"
---

<objective>
Add safe ranking diagnostics and deterministic retrieval ablation evaluation for Phase 23 acceptance.

Purpose: Give maintainers and evals enough bounded information to tune retrieval quality while keeping diagnostics out of ordinary user and authority surfaces.
Output: Internal diagnostics DTOs, no-live-provider ablation script, golden-case coverage, metric/report tests, and latency budget verification.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-rag-reranker-query-rewrite/23-CONTEXT.md
@.planning/phases/23-rag-reranker-query-rewrite/23-RESEARCH.md
@.planning/phases/23-rag-reranker-query-rewrite/23-VALIDATION.md
@.planning/phases/23-rag-reranker-query-rewrite/23-PATTERNS.md
@.planning/phases/23-rag-reranker-query-rewrite/23-04-SUMMARY.md
@docs/contract-spec.md
@docs/rag-architecture-spec.md
@src/api/schemas/search.py
@src/knowledge/diagnostics.py
@src/knowledge/retrieval.py
@src/knowledge/service.py
@scripts/eval_rag.py
@scripts/eval_rag_hit_at_5.py
@evaluation/golden/rag_cases.jsonl
@tests/knowledge/test_retrieval_diagnostics.py
@tests/knowledge/test_retrieval_budgets.py
@tests/test_rag_ablation_eval.py
@tests/agent/rag_context/test_leakage.py

<interfaces>
Use these stable labels:
```python
REQUIRED_ABLATION_VARIANTS = (
    "dense_only", "sparse_only", "fuzzy_only", "rrf_baseline",
    "rewrite_enabled", "reranker_enabled", "rewrite_plus_reranker",
)
REQUIRED_ABLATION_METRICS = (
    "hit_at_k", "mrr", "citation_support_compatibility", "no_evidence_precision",
    "unsafe_retrieval_rate", "fallback_rate", "latency_p50_ms", "latency_p95_ms",
)
```

Diagnostics DTOs stay internal:
```python
class RankingExplanation(BaseModel):
    selected_channels: tuple[str, ...]
    rewrite_contribution: float
    rerank_contribution: float
    rank_before: int | None
    rank_after: int
    safe_score_components: dict[str, float]
    fallback_reason: str | None = None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement strict internal retrieval diagnostics and safe ranking explanations</name>
  <files>src/knowledge/diagnostics.py, src/knowledge/retrieval.py, src/knowledge/service.py, tests/knowledge/test_retrieval_diagnostics.py</files>
  <read_first>
    - src/knowledge/diagnostics.py
    - src/knowledge/retrieval.py
    - src/knowledge/service.py
    - src/api/schemas/search.py
    - tests/knowledge/test_retrieval_diagnostics.py
    - tests/agent/rag_context/test_leakage.py
    - .planning/phases/23-rag-reranker-query-rewrite/23-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `RankingExplanation` includes selected channels, rewrite/rerank contribution, rank changes, safe score components, config versions, and fallback reason.
    - Test 2: diagnostics do not extend `EvidenceRefV1` or public `EvidenceItem.model_dump()`.
    - Test 3: diagnostics preserve tenant isolation by being built only from already-filtered candidate IDs and canonical evidence IDs.
  </behavior>
  <action>Extend `src/knowledge/diagnostics.py` with strict/frozen `RankingExplanation`, `RewriteDiagnosticRecord`, `RerankDiagnosticRecord`, and `RetrievalDiagnostics`. Fields may include `original_query`, `query_rewrite_summary`, `selected_channels`, `rewrite_contribution`, `rerank_contribution`, `rank_before`, `rank_after`, `safe_score_components`, `provider_config_version`, `retrieval_config_version`, `rerank_config_version`, `fallback_reason`, `selected_candidate_ids`, and `latency_ms`. Do not include raw rewrite prompts, raw provider payloads, private reasoning, source-block/OCR/parser internals, raw tool facts, business fact payloads, or unbounded policy text per D-17/D-18/D-20. Connect diagnostics to `PolicyRetrievalRun` as an internal field only; `KnowledgeSearchResult` may still expose only `query_rewrite` safe summary. Use candidate IDs from tenant-filtered retrieval only per EXP-03/BND-01.</action>
  <acceptance_criteria>
    - `rg -n "class RetrievalDiagnostics|class RankingExplanation|class RewriteDiagnosticRecord|class RerankDiagnosticRecord|ConfigDict\\(extra=\"forbid\", frozen=True\\)" src/knowledge/diagnostics.py` finds DTOs.
    - `rg -n "selected_channels|rewrite_contribution|rerank_contribution|rank_before|rank_after|safe_score_components|fallback_reason|selected_candidate_ids" src/knowledge/diagnostics.py tests/knowledge/test_retrieval_diagnostics.py` finds required diagnostic fields.
    - `rg -n "raw_prompt|raw_provider|private_reasoning|source_block|ocr_metadata|raw_tool|business_fact_payload|unbounded" src/knowledge/diagnostics.py tests/knowledge/test_retrieval_diagnostics.py` confirms these appear only in denial/leakage tests or comments, not DTO fields.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/knowledge/test_retrieval_diagnostics.py -q --tb=short</automated>
    <automated>uv run pytest tests/knowledge/test_phase21_boundaries.py::test_evidence_ref_v1_remains_the_only_policy_evidence_authority_shape tests/knowledge/test_phase21_boundaries.py::test_public_search_api_evidence_serialization_excludes_phase21_internal_fields -q --tb=short</automated>
    <automated>uv run ruff check src/knowledge/diagnostics.py src/knowledge/retrieval.py src/knowledge/service.py tests/knowledge/test_retrieval_diagnostics.py</automated>
  </verify>
  <done>Safe internal diagnostics exist and remain separate from evidence identity and public surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement no-live-provider retrieval ablation script and golden categories</name>
  <files>scripts/eval_rag_ablation.py, evaluation/golden/rag_cases.jsonl, tests/test_rag_ablation_eval.py</files>
  <read_first>
    - scripts/eval_rag.py
    - scripts/eval_rag_hit_at_5.py
    - evaluation/golden/rag_cases.jsonl
    - tests/test_rag_eval.py
    - tests/test_rag_ablation_eval.py
    - .planning/phases/23-rag-reranker-query-rewrite/23-CONTEXT.md
    - .planning/phases/23-rag-reranker-query-rewrite/23-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: `REQUIRED_ABLATION_VARIANTS` contains dense-only, sparse-only, fuzzy-only, RRF baseline, rewrite-enabled, reranker-enabled, and rewrite-plus-reranker.
    - Test 2: Phase 23 golden data contains required categories: `rewrite_win`, `synonym_alias`, `ambiguous_support_wording`, `underspecified_question`, `no_evidence_out_of_domain`, `stale_unauthorized_evidence`, `ranking_regression`, and `reranker_win`.
    - Test 3: report helper returns redacted JSON and never requires provider credentials.
  </behavior>
  <action>Create `scripts/eval_rag_ablation.py` as a new script rather than overloading `scripts/eval_rag_hit_at_5.py`. Reuse local JSONL loading/report patterns from `scripts/eval_rag.py`. Add `REQUIRED_ABLATION_VARIANTS` with exact labels from the interface block. Add pure helper functions `score_ablation_case(case, variant_result)`, `build_ablation_report(results, thresholds)`, and `run_rag_ablation(...)`. Default mode must not require live provider credentials, network, Redis, or a real model; provider behavior must be faked/config-disabled by default. Append Phase 23 golden cases to `evaluation/golden/rag_cases.jsonl` without deleting existing cases. Each new JSONL line must include `id`, `phase: "23"`, `category`, `query`, `expected_doc_ids`, `expected_chunk_ids`, `should_fallback`, and any safe `expected_rewrite_triggers`/`expected_variant_wins` metadata. Do not write `evaluation/reports/rag_ablation.json` by hand; it is generated by the script.</action>
  <acceptance_criteria>
    - `rg -n "REQUIRED_ABLATION_VARIANTS|dense_only|sparse_only|fuzzy_only|rrf_baseline|rewrite_enabled|reranker_enabled|rewrite_plus_reranker|def score_ablation_case|def build_ablation_report|def run_rag_ablation" scripts/eval_rag_ablation.py` confirms the script API.
    - `rg -n "\"phase\": \"23\"|\"category\": \"rewrite_win\"|\"category\": \"synonym_alias\"|\"category\": \"ambiguous_support_wording\"|\"category\": \"underspecified_question\"|\"category\": \"no_evidence_out_of_domain\"|\"category\": \"stale_unauthorized_evidence\"|\"category\": \"ranking_regression\"|\"category\": \"reranker_win\"" evaluation/golden/rag_cases.jsonl` confirms golden coverage.
    - `rg -n "OPENAI_API_KEY|ANTHROPIC_API_KEY|provider_required|live_provider" scripts/eval_rag_ablation.py tests/test_rag_ablation_eval.py` returns no credential-required default path.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_rag_ablation_eval.py::test_phase23_golden_cases_cover_required_categories tests/test_rag_ablation_eval.py::test_ablation_variants_include_required_modes -q --tb=short</automated>
    <automated>uv run ruff check scripts/eval_rag_ablation.py tests/test_rag_ablation_eval.py</automated>
  </verify>
  <done>A deterministic ablation script and golden-case categories exist with no live provider default dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add blocking metrics, latency budgets, and safe fallback reporting</name>
  <files>scripts/eval_rag_ablation.py, src/knowledge/config.py, src/knowledge/retrieval.py, tests/test_rag_ablation_eval.py, tests/knowledge/test_retrieval_budgets.py</files>
  <read_first>
    - scripts/eval_rag_ablation.py
    - src/knowledge/config.py
    - src/knowledge/retrieval.py
    - tests/test_rag_ablation_eval.py
    - tests/knowledge/test_retrieval_budgets.py
    - .planning/phases/23-rag-reranker-query-rewrite/23-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: ablation report metrics include Hit@K, MRR, citation-support compatibility, no-evidence precision, unsafe retrieval rate, fallback rate, and latency percentiles.
    - Test 2: budget constants include total retrieval, rewrite stage, rerank stage, candidate count, text size, retries, and config versions.
    - Test 3: timeout/provider error/malformed/budget/disabled cases appear as fallback-safe report counters.
  </behavior>
  <action>In `src/knowledge/config.py`, ensure explicit Phase 23 budgets exist: `RETRIEVAL_TOTAL_TIMEOUT_SECONDS`, `REWRITE_STAGE_TIMEOUT_SECONDS`, `RERANK_STAGE_TIMEOUT_SECONDS`, `MAX_REWRITE_QUERIES`, `ORIGINAL_QUERY_TOP_K`, `REWRITE_QUERY_TOP_K`, `MERGED_CANDIDATE_CAP`, `RERANK_MAX_CANDIDATES`, `RERANK_TEXT_MAX_CHARS`, `RERANK_PROVIDER_MAX_RETRIES`, `QUERY_REWRITE_CONFIG_VERSION`, and `RERANK_CONFIG_VERSION`. If `RETRIEVAL_TIMEOUT_SECONDS` remains in `retrieval.py`, alias it to `RETRIEVAL_TOTAL_TIMEOUT_SECONDS` instead of duplicating magic numbers. Extend `scripts/eval_rag_ablation.py` to compute `hit_at_k`, `mrr`, `citation_support_compatibility`, `no_evidence_precision`, `unsafe_retrieval_rate`, `fallback_rate`, `latency_p50_ms`, and `latency_p95_ms`; include `thresholds`, `failed_cases`, `fallback_reasons`, and `provider_config_version`/`retrieval_config_version`/`rerank_config_version` in report JSON. Reports must contain metrics, failed case IDs, safe candidate IDs, and bounded text snippets only. This implements D-21..D-25 and EVAL-01..EVAL-05.</action>
  <acceptance_criteria>
    - `rg -n "RETRIEVAL_TOTAL_TIMEOUT_SECONDS|REWRITE_STAGE_TIMEOUT_SECONDS|RERANK_STAGE_TIMEOUT_SECONDS|ORIGINAL_QUERY_TOP_K|REWRITE_QUERY_TOP_K|MERGED_CANDIDATE_CAP|RERANK_MAX_CANDIDATES|RERANK_TEXT_MAX_CHARS|RERANK_PROVIDER_MAX_RETRIES" src/knowledge/config.py` finds all budgets.
    - `rg -n "hit_at_k|mrr|citation_support_compatibility|no_evidence_precision|unsafe_retrieval_rate|fallback_rate|latency_p50_ms|latency_p95_ms|fallback_reasons" scripts/eval_rag_ablation.py tests/test_rag_ablation_eval.py` confirms metrics.
    - `rg -n "provider_disabled|provider_timeout|provider_error|provider_malformed_output|budget_overflow|rewrite_timeout|rewrite_error" scripts/eval_rag_ablation.py tests/knowledge/test_retrieval_budgets.py` confirms fallback reporting.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_rag_ablation_eval.py::test_ablation_report_contains_rank_safety_fallback_and_latency_metrics tests/knowledge/test_retrieval_budgets.py -q --tb=short</automated>
    <automated>uv run python scripts/eval_rag_ablation.py --golden-set evaluation/golden/rag_cases.jsonl --output evaluation/reports/rag_ablation.json --dry-run</automated>
    <automated>uv run ruff check scripts/eval_rag_ablation.py src/knowledge/config.py src/knowledge/retrieval.py tests/test_rag_ablation_eval.py tests/knowledge/test_retrieval_budgets.py</automated>
  </verify>
  <done>Blocking ablation metrics and explicit latency/budget/fallback reporting pass without live providers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retrieval internals -> diagnostics | Internal rank/rewrite/rerank details are summarized for maintainer/eval only. |
| golden cases -> eval report | Test data and query text are processed into redacted metrics and failed case IDs. |
| eval script -> local environment | Script must not require live provider/network credentials by default. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05-01 | Information disclosure | diagnostics DTOs and eval reports | mitigate | Task 1/3 exclude raw prompts, raw provider payloads, source-block/OCR/parser internals, raw tool facts, private reasoning, and unbounded policy text. |
| T-23-05-02 | Spoofing/Elevation of privilege | diagnostic candidate IDs | mitigate | Task 1 builds diagnostics only from already tenant-filtered candidates and canonical evidence IDs. |
| T-23-05-03 | Denial of service | ablation script/provider defaults | mitigate | Task 2 forbids live provider default dependency; Task 3 uses dry-run/no-live-provider eval and explicit latency budgets. |
| T-23-05-04 | Repudiation | eval acceptance | mitigate | Task 3 writes machine-readable metrics, thresholds, fallback reasons, config versions, and failed case IDs. |
</threat_model>

<verification>
Plan-level verification:
```bash
uv run pytest tests/knowledge/test_retrieval_diagnostics.py tests/knowledge/test_retrieval_budgets.py tests/test_rag_ablation_eval.py -q --tb=short
uv run python scripts/eval_rag_ablation.py --golden-set evaluation/golden/rag_cases.jsonl --output evaluation/reports/rag_ablation.json --dry-run
uv run ruff check src/knowledge/diagnostics.py src/knowledge/service.py src/knowledge/retrieval.py src/knowledge/config.py scripts/eval_rag_ablation.py tests/knowledge/test_retrieval_diagnostics.py tests/knowledge/test_retrieval_budgets.py tests/test_rag_ablation_eval.py
```
</verification>

<success_criteria>
- Safe diagnostics and ranking explanations exist and remain internal/eval-only.
- Phase 23 golden cases cover all required categories.
- Ablation variants and blocking metrics match `23-VALIDATION.md`.
- Latency/candidate/text/retry/config budgets are explicit and tested.
- Default eval path uses no live provider credentials.
</success_criteria>

<output>
After completion, create `.planning/phases/23-rag-reranker-query-rewrite/23-05-SUMMARY.md`.
</output>
