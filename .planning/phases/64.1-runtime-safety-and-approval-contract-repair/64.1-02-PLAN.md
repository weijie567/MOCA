---
phase: 64.1-runtime-safety-and-approval-contract-repair
plan: "02"
type: execute
wave: 2
depends_on: ["64.1-01"]
files_modified:
  - rules/risk_rules.yaml
  - src/agent/nodes/risk_gate.py
  - tests/agent/test_nodes/test_risk_gate.py
  - tests/test_interception_rate.py
  - tests/approvals/test_hash_binding.py
  - tests/approvals/test_snapshots.py
autonomous: true
requirements: [SC-64.1-2, SC-64.1-5, D-04, D-05, D-06, D-15]
requirements_addressed: [SC-64.1-2, SC-64.1-5, D-04, D-05, D-06, D-15]
must_haves:
  truths:
    - "One deterministic evaluator consumes configured high, medium, and low rules with explicit precedence."
    - "LLM output may explain or upgrade risk but never downgrade deterministic severity/disposition."
    - "Invalid/missing config and LLM failure yield stable manual review or stronger handling and never auto-allow."
  artifacts:
    - {path: src/agent/nodes/risk_gate.py, provides: "Deterministic full-rule evaluator and fail-closed fallback", contains: "deterministic"}
    - {path: tests/agent/test_nodes/test_risk_gate.py, provides: "Rule/failure/no-downgrade matrix", contains: "medium"}
  key_links:
    - {from: src/agent/nodes/risk_gate.py, to: rules/risk_rules.yaml, via: "validated high/medium/low registry", pattern: "risk_rules"}
    - {from: src/agent/nodes/risk_gate.py, to: src/approvals/schemas.py, via: "existing RiskDecisionV1/snapshot binding", pattern: "RiskDecisionV1"}
---
<objective>Make deterministic backend rules the complete risk authority, with LLM output bounded to non-downgrading assistance. This plan does not mint capabilities or alter approval/frontend contracts.</objective>
<execution_context>@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md @/Users/ming/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md @.planning/phases/64.1-runtime-safety-and-approval-contract-repair/64.1-CONTEXT.md @.planning/phases/64.1-runtime-safety-and-approval-contract-repair/64.1-RESEARCH.md @.planning/phases/64.1-runtime-safety-and-approval-contract-repair/64.1-PATTERNS.md @src/agent/safety/taxonomy.py @src/agent/nodes/risk_gate.py @rules/risk_rules.yaml</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify deterministic rule and failure parity</name>
  <read_first>@rules/risk_rules.yaml @src/agent/nodes/risk_gate.py @tests/agent/test_nodes/test_risk_gate.py @tests/test_interception_rate.py @tests/approvals/test_hash_binding.py @tests/approvals/test_snapshots.py</read_first>
  <files>tests/agent/test_nodes/test_risk_gate.py, tests/test_interception_rate.py, tests/approvals/test_hash_binding.py, tests/approvals/test_snapshots.py</files>
  <action>Add RED matrices for every configured high/medium/low group, precedence/conflicts/duplicates, unknown action/rule, missing/malformed config, and LLM timeout/unavailable/invalid schema. Assert LLM never lowers deterministic severity/disposition, fallback never emits low/allow for unproven cases, and `RiskDecisionV1` plus safety snapshot retain canonical rule/config/policy identity.</action>
  <verify><automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_risk_gate.py tests/test_interception_rate.py tests/approvals/test_hash_binding.py tests/approvals/test_snapshots.py -q --tb=short; test $? -ne 0</automated></verify>
  <acceptance_criteria>RED tests reproduce medium-rule disappearance and define stable fail-closed decisions for every evaluator/config/LLM failure mode.</acceptance_criteria>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement validated deterministic evaluator and bounded LLM merge</name>
  <read_first>@tests/agent/test_nodes/test_risk_gate.py @rules/risk_rules.yaml @src/agent/safety/taxonomy.py @src/agent/nodes/risk_gate.py @src/approvals/schemas.py @src/approvals/snapshot_service.py</read_first>
  <files>rules/risk_rules.yaml, src/agent/nodes/risk_gate.py</files>
  <action>Replace high-only matching and first-low fallback with one validated high/medium/low evaluator using Phase 63 vocabulary. Define precedence and duplicate/conflict rejection. Combine LLM output monotonically: it may preserve/raise severity or add controlled explanation but cannot lower deterministic handling. Missing/invalid rules, unknown candidates, timeout/unavailable/parse/schema failure return stable medium/manual_review or stronger policy handling and never request auto capability. Keep existing `RiskDecisionV1`, canonical hashes, config/policy versions, and snapshot path.</action>
  <verify><automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_risk_gate.py tests/test_interception_rate.py tests/approvals/test_hash_binding.py tests/approvals/test_snapshots.py -q --tb=short</automated><automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/nodes/risk_gate.py tests/agent/test_nodes/test_risk_gate.py tests/test_interception_rate.py tests/approvals/test_hash_binding.py tests/approvals/test_snapshots.py</automated></verify>
  <acceptance_criteria>All rule levels are evaluated deterministically; invalid inputs/config and LLM failures fail closed; snapshot/hash tests prove the decision identity remains canonical.</acceptance_criteria>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Boundary | Mitigation |
|---|---|---|---|
| T-64.1-02 | Tampering | YAML/LLM output -> risk decision | Validate config, explicit precedence, monotonic non-downgrade merge. |
| T-64.1-02A | Elevation of Privilege | evaluator failure -> auto allow | Stable medium/manual-review-or-stronger fallback; no capability request. |
</threat_model>
<verification>Run focused pytest/ruff commands above; log actual validation incidents per `AGENTS.md`.</verification>
<success_criteria>SC-64.1-2 is satisfied when deterministic high/medium/low parity and all fail-closed modes are green.</success_criteria>
<output>After completion, create `64.1-02-SUMMARY.md`.</output>
