---
phase: 64.1-runtime-safety-and-approval-contract-repair
reviewed: 2026-07-10T09:50:31Z
depth: deep
iteration: 3
files_reviewed: 51
files_reviewed_list:
  - contracts/fixtures/approval_decision_context_v1.json
  - frontend/e2e/phase64_1-approval-safety.spec.ts
  - frontend/src/App.tsx
  - frontend/src/components/details/ApprovalTab.test.tsx
  - frontend/src/components/details/ApprovalTab.tsx
  - frontend/src/components/details/DetailsPanel.tsx
  - frontend/src/hooks/useAgentRun.test.ts
  - frontend/src/hooks/useAgentRun.ts
  - frontend/src/lib/api.test.ts
  - frontend/src/lib/api.ts
  - frontend/src/types/events.ts
  - src/actions/capabilities.py
  - src/actions/service.py
  - src/agent/graph.py
  - src/agent/nodes/action_draft.py
  - src/agent/nodes/final_response.py
  - src/agent/nodes/receive_request.py
  - src/agent/nodes/recommendation_generation.py
  - src/agent/nodes/risk_gate.py
  - src/agent/routing.py
  - src/agent/safety/taxonomy.py
  - src/agent/schemas.py
  - src/agent/state.py
  - src/agent/trace.py
  - src/api/routers/agent_runs.py
  - src/api/routers/approvals.py
  - src/api/schemas/approvals.py
  - src/api/services/agent_run_memory.py
  - src/approvals/repository.py
  - src/approvals/schemas.py
  - src/approvals/service.py
  - src/db/migrations/versions/023_phase64_1_auto_action_capabilities.py
  - src/db/models.py
  - src/replay/lifecycle.py
  - src/tools/executors/action.py
  - src/tools/policy.py
  - tests/actions/test_auto_action_capabilities.py
  - tests/agent/test_nodes/test_receive_request.py
  - tests/agent/test_nodes/test_recommendation_generation.py
  - tests/agent/test_nodes/test_risk_gate.py
  - tests/approvals/test_service_transitions.py
  - tests/architecture/graph_baseline.py
  - tests/architecture/test_action_draft_boundaries.py
  - tests/architecture/test_runtime_safety_boundaries.py
  - tests/architecture/test_safety_taxonomy_boundaries.py
  - tests/db/test_phase64_1_capability_migration.py
  - tests/integration/test_phase64_1_runtime_safety_matrix.py
  - tests/replay/test_lifecycle_finalizer.py
  - tests/test_agent_runs_api.py
  - tests/test_approval_api.py
  - tests/test_interception_rate.py
findings:
  critical: 0
  warning: 3
  info: 0
  total: 3
status: issues_found
---

# Phase 64.1: Code Review Report — Iteration 3

**Reviewed:** 2026-07-10T09:50:31Z
**Depth:** deep
**Files Reviewed:** 51
**Status:** issues_found

## Summary

Iteration 2 closes the four reported surface defects in their focused paths: ordinary AgentRun API/memory projection now preserves an already-rendered authoritative non-success terminal; the Console keeps an exact frozen decision body and requires a second confirmation after explicit `APPROVAL_RESUME_FAILED`; exact-context freshness revalidation is separate from strictly-newer replacement; and manager cross-merchant list/get/decide responses match the generic absent-resource 404 envelope.

The phase is still not clean. Three cross-layer warnings remain. Approval resume still bypasses the shared run-terminal projector and reclassifies authoritative manual-review/refusal as an action-draft error. The frontend maintains separate hook and ApprovalTab decision-context owners, allowing a newer SSE context to be submitted while the user is still viewing an older detail. Finally, recoverable resume retries are not single-flight: `attempted` is considered retryable and the request lock is released before graph invocation, so concurrent identical retry requests can invoke the graph more than once even though only one decision row exists.

## Fix Verification Matrix

| Finding | Iteration-3 result | Evidence |
| --- | --- | --- |
| Original CR-01 | Fixed | Unknown/manual-review results render and persist as non-success; memory and lifecycle regressions pass. |
| Original WR-01 | Fixed | Polling/recovery derive assistant-message status from the backend terminal. |
| Original WR-02 | Fixed | Approval callbacks return discriminated outcomes and no longer announce unconditional success. |
| Original WR-03 | Fixed | Read projectors are non-locking and mutations use Request -> Level -> Assignment order; PostgreSQL race coverage passes. |
| Original WR-04 | Fixed for terminal GET/query-first reconciliation | Terminal records return `decision_context=null`; no automatic POST replay occurs after ordinary ambiguity. |
| Original WR-05 | Fixed | `canonical_action` and `risk_signals` reset at ingress and on recommendation exits. |
| Original WR-06 | Fixed for monotonic replacement | Older/cross-identity contexts cannot replace the cached identity. |
| Iteration-2 WR-01 | Partially fixed | AgentRun API and memory use `project_run_terminal`; approval resume still uses the action-only projector. See WR-01. |
| Iteration-2 WR-02 | Partially fixed | Explicit UI recovery is user-confirmed and byte-identical, and the sequential regression retains one decision/one graph call. Backend concurrent retries are not single-flight. See WR-03. |
| Iteration-2 WR-03 | Partially fixed | Exact freshness is correctly separated from replacement, but split UI ownership can submit a newer SSE binding not shown in ApprovalTab. See WR-02. |
| Iteration-2 WR-04 | Fixed | Cross-merchant pending and resume-failed get/decide paths are indistinguishable from absent resources. |

## Warnings

### WR-01: Approval resume still lets action-draft shape override an authoritative manual-review/refusal terminal

**Files:** `src/api/routers/approvals.py:14`, `src/api/routers/approvals.py:368-401`, `src/agent/routing.py:148-163`, `src/api/services/agent_run_memory.py:119-136`, `src/agent/trace.py:366-405`

The normal AgentRun paths now call `project_run_terminal()`, which correctly gives an already-rendered `manual_review`, `refused`, or `error` response precedence over stale action-shaped fields. The approval-resume path does not. `_resume_graph_after_decision()` still calls `project_action_draft_terminal(final_state)` directly, then persists only `completed` or generic `error` via `update_agent_run_status()`.

A project-environment probe using the same state reproduced the disagreement for both authoritative statuses:

```text
run terminal: manual_review / refused, error_code=CLAIM_TERMINAL
approval-resume action-only terminal: error,
  error_code=ACTION_DRAFT_TERMINAL_FAILED,
  reason_code=draft_identity_invalid
```

An approval-resumed graph state containing an authoritative claim/evidence non-allow terminal plus stale `approval_result`/`action_draft`/`draft_outcome` therefore gets different DB status, safe response, lifecycle reason, and replay status depending on whether it completed through AgentRun streaming or the approval router. Memory independently refuses the write, so this does not launder success, but terminal authority/parity remains broken.

**Coverage gap:** The new precedence tests drive `_project_run_terminal()` through AgentRun SSE/DB/memory only. No approval-resume test returns an explicit `manual_review` or `refused` final response with stale action-shaped state.

**Fix:** Make `_resume_graph_after_decision()` consume `project_run_terminal()` for final status/message/reason/error projection, while retaining `project_action_draft_terminal(..., require_action=True)` only where a valid draft is specifically required. Add approval-resume DB/lifecycle/replay tests for manual-review and refusal with stale draft fields.

### WR-02: A newer SSE context can be submitted even though ApprovalTab still shows the older reviewed detail

**Files:** `frontend/src/hooks/useAgentRun.ts:404-417`, `frontend/src/hooks/useAgentRun.ts:644-667`, `frontend/src/components/details/ApprovalTab.tsx:144-165`, `frontend/src/components/details/ApprovalTab.tsx:195-211`, `frontend/src/components/details/DetailsPanel.tsx:90-99`

The hook and ApprovalTab own separate decision-context snapshots. ApprovalTab loads and displays its own `activeApproval` detail. For the active approval it does not pass that frozen context to the callback; it calls `onApprove()`/`onReject()` without a binding. Meanwhile, a same-identity strictly-newer SSE event replaces the hook's independent `approvalContext` and marks it fresh. On click, the hook GETs and submits its newer context if that GET is exact, even when ApprovalTab is still rendering the older action/risk/version snapshot.

This is a review/TOCTOU failure rather than a backend authorization bypass: the server validates the submitted binding, but the user may authorize a changed revision or proposed-action display that they never re-reviewed. The iteration-2 test only covers “hook cached old context, GET returns newer”; it does not cover “ApprovalTab displays old context, hook already accepted newer SSE.”

**Fix:** Establish one decision-context owner. Prefer passing the exact context displayed/frozen by ApprovalTab into the active callback and require the hook's authoritative GET to be exact to that caller-supplied snapshot. Alternatively, propagate newer SSE replacement into ApprovalTab as an invalidation that forces a visible detail refresh before buttons re-enable. Add an integrated component/hook test: load detail V1, receive SSE V2, click approve, assert no POST until V2 is fetched and explicitly re-reviewed.

### WR-03: Recoverable approval resume is not single-flight, so concurrent identical retries can invoke the graph more than once

**Files:** `src/api/routers/approvals.py:57-58`, `src/api/routers/approvals.py:279-304`, `src/api/routers/approvals.py:538-572`, `src/api/routers/approvals.py:584-609`, `tests/test_approval_api.py:557-726`

The explicit Console path now freezes one byte-identical body, never auto-replays it, and requires a second confirmation. The sequential backend regression also proves one durable `ApprovalDecision` and, for its completed-event failure scenario, one graph invocation.

Backend recovery is still raceable. `attempted` is included in `RESUME_INCOMPLETE_STATUSES`, so `_recoverable_resume_retry_result()` accepts an in-flight attempt. `_record_resume_completed_event_once()` locks the request but returns `False` for `attempted`; `_run_resume_lifecycle()` then appends another attempted event and commits, releasing the lock before `graph.ainvoke()`. A second identical request can therefore observe the first attempted event, follow the same branch, commit its own attempted event, and invoke the graph concurrently.

The decision row remains singular, but graph-side persistence, interrupts, trace/memory finalization, or future authorized handlers can run more than once. Current exact-retry tests are sequential and do not exercise two simultaneous recovery requests.

**Fix:** Add a durable single-flight resume-attempt identity/lease under the ApprovalRequest lock. Treat a live `attempted` attempt as conflict/in-progress; only `failed` or an explicitly stale/abandoned attempt may acquire a new lease. Record completion/failure against that attempt identity. Add a PostgreSQL/HTTP concurrency regression that sends two identical recovery POSTs and proves one `ApprovalDecision`, one active graph invocation, and stable second-request reconciliation.

## Verification

- Terminal/recommendation/lifecycle/integration/AgentRun focused aggregate:
  - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_receive_request.py tests/agent/test_nodes/test_recommendation_generation.py tests/agent/test_nodes/test_final_response.py tests/agent/test_phase22_final_response.py tests/replay/test_lifecycle_finalizer.py tests/integration/test_phase64_1_runtime_safety_matrix.py tests/test_agent_runs_api.py -q --tb=short`
  - Result: `255 passed, 3 warnings in 227.77s`.
- Approval service/API aggregate:
  - `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/approvals/test_service_transitions.py tests/test_approval_api.py -q --tb=short`
  - Result: `62 passed, 1 warning in 169.69s`.
- Frontend approval/hook/API suite:
  - `npm test -- --run src/components/details/ApprovalTab.test.tsx src/hooks/useAgentRun.test.ts src/lib/api.test.ts`
  - Result: `4 files passed, 36 tests passed`.
- Frontend production build: passed (`1766 modules transformed`).
- Independent terminal-precedence probe: confirmed shared run terminal `manual_review/refused` versus approval-resume action-only `ACTION_DRAFT_TERMINAL_FAILED/error` on the same merged state.

## Scope Notes

The review re-read every Phase 64.1 planning/research/UI/validation/summary/review artifact, all 50 files from iteration 2, and `frontend/src/App.tsx` added by the second fix iteration. Supporting approval-resume and final-response call chains were inspected beyond the counted file list.

No source file was changed and no commit was created. This report is the only edited artifact.

---

_Reviewed: 2026-07-10T09:50:31Z_
_Reviewer: Codex (gsd-code-review)_
_Depth: deep_
_Iteration: 3_
