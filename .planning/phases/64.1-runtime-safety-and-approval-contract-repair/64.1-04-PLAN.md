---
phase: 64.1-runtime-safety-and-approval-contract-repair
plan: "04"
type: execute
wave: 3
depends_on: ["64.1-02"]
files_modified:
  - src/actions/capabilities.py
  - src/actions/service.py
  - src/repositories/action_draft_repo.py
  - src/db/models.py
  - src/db/migrations/versions/023_phase64_1_auto_action_capabilities.py
  - src/agent/nodes/risk_gate.py
  - src/agent/nodes/action_draft.py
  - src/tools/executors/action.py
  - tests/approvals/test_phase34_boundary_bindings.py
  - tests/actions/test_auto_action_capabilities.py
  - tests/db/test_phase64_1_capability_migration.py
  - tests/actions/test_action_draft_v2.py
  - tests/actions/test_phase34_action_draft_bindings.py
  - tests/test_execute_action.py
  - tests/architecture/test_action_draft_boundaries.py
autonomous: true
requirements: [SC-64.1-4, SC-64.1-5, D-10, D-11, D-12, D-15]
requirements_addressed: [SC-64.1-4, SC-64.1-5, D-10, D-11, D-12, D-15]
must_haves:
  truths:
    - "Only a trusted backend capability service can mint short-lived auto-allow authority after deterministic allow and snapshot persistence."
    - "Capability verification binds tenant, actor, run, merchant, canonical action, payload/snapshot/risk identity, expiry, nonce, and the sole demo draft handler."
    - "Capability replay/cross-binding misuse creates no new draft and cannot broaden ToolPlatform permissions or authorize an external side effect."
    - "Approval resume and auto allow share draft invariants but retain distinct authorization-source audit identity."
  artifacts:
    - {path: src/actions/capabilities.py, provides: "Mint/verify/consume service", contains: "AutoActionCapability"}
    - {path: tests/actions/test_auto_action_capabilities.py, provides: "Binding, expiry, replay matrix", contains: "handler"}
    - {path: tests/architecture/test_action_draft_boundaries.py, provides: "Single mint/consume/handler guard", contains: "capability"}
  key_links:
    - {from: src/agent/nodes/risk_gate.py, to: src/actions/capabilities.py, via: "trusted mint request after allow+snapshot", pattern: "mint"}
    - {from: src/tools/executors/action.py, to: src/actions/service.py, via: "handler-bound verify/consume", pattern: "capability"}
---
<objective>Replace graph-state self-asserted auto bindings with a durable, opaque, server-minted capability that authorizes exactly one demo draft handler. This plan does not create a general operation gateway or production executor.</objective>
<execution_context>@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md @/Users/ming/.codex/get-shit-done/templates/summary.md</execution_context>
<context>@AGENTS.md @.planning/phases/64.1-runtime-safety-and-approval-contract-repair/64.1-CONTEXT.md @.planning/phases/64.1-runtime-safety-and-approval-contract-repair/64.1-RESEARCH.md @.planning/phases/64.1-runtime-safety-and-approval-contract-repair/64.1-PATTERNS.md @src/approvals/schemas.py @src/agent/nodes/risk_gate.py @src/agent/nodes/action_draft.py @src/actions/service.py @src/repositories/action_draft_repo.py @src/tools/executors/action.py</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Specify durable capability lifecycle and misuse matrix</name>
  <read_first>@src/approvals/schemas.py @src/actions/service.py @src/repositories/action_draft_repo.py @src/db/migrations/versions/018_phase34_approval_action_bindings.py @tests/approvals/test_phase34_boundary_bindings.py @tests/actions/test_action_draft_v2.py @tests/actions/test_phase34_action_draft_bindings.py @tests/test_execute_action.py</read_first>
  <files>tests/approvals/test_phase34_boundary_bindings.py, tests/actions/test_auto_action_capabilities.py, tests/actions/test_action_draft_v2.py, tests/actions/test_phase34_action_draft_bindings.py, tests/db/test_phase64_1_capability_migration.py, tests/test_execute_action.py</files>
  <action>Add RED tests for trusted mint prerequisites, opaque capability refs, short TTL, durable nonce/use status, schema/key version, exact tenant/actor/run/merchant/action/payload/snapshot/risk/handler matching, expiry, wrong-binding attempts, and replay. In `tests/db/test_phase64_1_capability_migration.py`, inspect the Alembic head/down_revision and exercise upgrade -> downgrade to `022_case_working_context` -> upgrade, asserting table removal/recreation plus required constraints and indexes. Specify idempotent exact retry as returning the same draft only when the already-consumed binding and idempotency key match; otherwise create zero drafts. Assert neither capability path mutates general permissions nor reaches any handler except `create_coupon_grant_draft`, and audit source distinguishes `auto_allow_capability` from `approval`.</action>
  <verify><automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/approvals/test_phase34_boundary_bindings.py tests/actions/test_auto_action_capabilities.py tests/actions/test_action_draft_v2.py tests/actions/test_phase34_action_draft_bindings.py tests/db/test_phase64_1_capability_migration.py tests/test_execute_action.py -q --tb=short; test $? -ne 0</automated></verify>
  <acceptance_criteria>RED tests define all D-10/D-11 bindings, one-use/idempotent retry semantics, and zero-draft behavior for every confused-deputy/replay case.</acceptance_criteria>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement durable mint, verify, and atomic consume</name>
  <read_first>@tests/actions/test_auto_action_capabilities.py @src/db/migrations/versions/018_phase34_approval_action_bindings.py @src/repositories/action_draft_repo.py @src/actions/service.py @src/approvals/schemas.py</read_first>
  <files>src/actions/capabilities.py, src/repositories/action_draft_repo.py, src/db/models.py, src/db/migrations/versions/023_phase64_1_auto_action_capabilities.py, src/actions/service.py</files>
  <action>Implement a durable DB-backed opaque-ref capability (do not trust a plain graph dict) with an `AutoActionCapability` ORM model in `src/db/models.py`. Migration `023_phase64_1_auto_action_capabilities` must declare `revision="023_phase64_1_auto_action_capabilities"` and `down_revision="022_case_working_context"`, and create/drop `auto_action_capabilities` in upgrade/downgrade. Persist immutable tenant/actor/run/merchant/canonical-action/payload-hash/snapshot-ref+hash/risk-ref+hash/handler bindings, issued/expiry timestamps, unique nonce and opaque ref, schema/key version, status (`issued|consumed|expired|revoked`), consumed timestamp, resulting draft id and idempotency key. Add FK/tenant-scope constraints where existing composite keys permit, unique constraints on opaque ref and nonce, status/expiry checks, and lookup indexes for tenant+run and expiry/status.

Lock the concrete transaction design to the repository's current ownership: `ActionService` owns one caller-supplied `AsyncSession` unit of work; capability selection uses `SELECT ... FOR UPDATE`; capability transition, `ActionDraft` insert, and critical `action_draft_created` replay event are flushed inside the same `begin_nested()` savepoint and are committed only by the outer API/graph session owner. No separate `AuditRepository.record_tool_call()` commit or second session is allowed on this path. Any validation/event exception rolls the savepoint back, leaving capability `issued` and zero draft/event rows; return no success. Exact retries lock the consumed row and may return only its same bound draft/idempotency identity. Add tests that inject failure after each write and assert rollback plus concurrent consume cardinality. Verify schema round-trip with `UV_CACHE_DIR=/tmp/uv-cache uv run alembic upgrade head && UV_CACHE_DIR=/tmp/uv-cache uv run alembic downgrade 022_case_working_context && UV_CACHE_DIR=/tmp/uv-cache uv run alembic upgrade head`, then run `UV_CACHE_DIR=/tmp/uv-cache uv run python -c "from src.db.models import AutoActionCapability; print(AutoActionCapability.__tablename__)"`; the downgrade test must assert the table is absent and the re-upgrade test its constraints/indexes are present.</action>
  <verify><automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/approvals/test_phase34_boundary_bindings.py tests/actions/test_auto_action_capabilities.py tests/actions/test_action_draft_v2.py tests/actions/test_phase34_action_draft_bindings.py tests/db/test_phase64_1_capability_migration.py -q --tb=short</automated><automated>UV_CACHE_DIR=/tmp/uv-cache uv run alembic upgrade head &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run alembic downgrade 022_case_working_context &amp;&amp; UV_CACHE_DIR=/tmp/uv-cache uv run alembic upgrade head</automated><automated>UV_CACHE_DIR=/tmp/uv-cache uv run python -c "from src.db.models import AutoActionCapability; print(AutoActionCapability.__tablename__)"</automated><automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/actions/capabilities.py src/actions/service.py src/repositories/action_draft_repo.py src/db/models.py src/db/migrations/versions/023_phase64_1_auto_action_capabilities.py tests/actions/test_auto_action_capabilities.py tests/db/test_phase64_1_capability_migration.py</automated></verify>
  <acceptance_criteria>Only the service mints and atomically consumes bounded capabilities; exact retries are idempotent; expired/replayed/mismatched requests produce no new draft or success audit.</acceptance_criteria>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Integrate mint/consume into risk and the sole draft handler</name>
  <read_first>@src/agent/nodes/risk_gate.py @src/agent/nodes/action_draft.py @src/tools/executors/action.py @src/actions/capabilities.py @src/actions/service.py @tests/architecture/test_action_draft_boundaries.py</read_first>
  <files>src/agent/nodes/risk_gate.py, src/agent/nodes/action_draft.py, src/tools/executors/action.py, tests/test_execute_action.py, tests/architecture/test_action_draft_boundaries.py</files>
  <action>Remove node-side trusted binding construction. After deterministic allow and snapshot persistence, request an opaque capability through the trusted service; carry only the ref plus non-authoritative display data. Require verification/consume for auto allow at the fixed demo draft handler; keep trusted approval resume as a distinct authorization source while sharing payload/snapshot/merchant/draft invariants. Add AST/static guards: only capability service mints, only the action draft handler consumes, no general permission widening, and no production executor.</action>
  <verify><automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/actions tests/test_execute_action.py tests/architecture/test_action_draft_boundaries.py -q --tb=short</automated><automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/nodes/risk_gate.py src/agent/nodes/action_draft.py src/tools/executors/action.py tests/test_execute_action.py tests/architecture/test_action_draft_boundaries.py</automated></verify>
  <acceptance_criteria>Auto-allowed draft creation cannot occur without a valid consumed server capability; approval and auto authorization are non-interchangeable; architecture guards prevent permission/handler expansion.</acceptance_criteria>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Boundary | Mitigation |
|---|---|---|---|
| T-64.1-05 | Elevation of Privilege | graph state -> draft handler | Opaque server-minted record; verify all bindings at service boundary. |
| T-64.1-06 | Replay/Confused Deputy | capability -> handler | TTL, durable nonce consume, exact idempotency, fixed handler and no permission mutation. |
| T-64.1-06A | Repudiation | authorization source -> audit | Distinct approval vs auto-capability source and fail-closed critical audit. |
</threat_model>
<verification>Run the focused backend tests/ruff above. Any actual migration/test incident must be recorded in `.planning/LOCAL-VALIDATION-ISSUES.md`; verified tool-call architecture findings/fixes are recorded by 64.1-06.</verification>
<success_criteria>SC-64.1-4 is satisfied when only a bounded, one-use/idempotent, server-minted capability authorizes the single durable demo draft handler.</success_criteria>
<output>After completion, create `64.1-04-SUMMARY.md`.</output>
