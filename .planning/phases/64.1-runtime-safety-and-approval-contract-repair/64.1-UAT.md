---
status: complete
phase: 64.1-runtime-safety-and-approval-contract-repair
source:
  - 64.1-01-SUMMARY.md
  - 64.1-02-SUMMARY.md
  - 64.1-03-SUMMARY.md
  - 64.1-04-SUMMARY.md
  - 64.1-05-SUMMARY.md
  - 64.1-06-SUMMARY.md
started: 2026-08-04T16:02:00+08:00
updated: 2026-08-04T16:06:00+08:00
mode: automated-self-test
---

# Phase 64.1 UAT

用户已授权“你来自己检测”。本次 UAT 使用仓库真实集成测试、API 测试、前端组件链路、production build 和 mocked Playwright 桌面/移动端场景自行验证；没有把自动化结果冒充人工视觉确认。

## Current Test

[testing complete]

## Tests

### 1. 冷启动与审批恢复租约迁移

expected: 迁移可 upgrade/downgrade/re-upgrade，审批恢复 attempt/lease 字段与约束完整，不破坏既有审批数据模型。
result: pass
evidence: `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/db/test_phase64_1_resume_attempt_migration.py -q --tb=short` -> `2 passed`。

### 2. 可执行建议与风险路径 fail closed

expected: 中英文合法动作先规范化再进入风险链；unknown、ambiguous、schema-invalid、provider failure 不会被误报为普通完成，也不会产生未授权 draft。
result: pass
evidence: Phase 64.1 runtime safety matrix -> `27 passed`；Plan 01/02 focused gates -> `226 passed` / `69 passed`。

### 3. 低风险 demo 与中高风险人工路径分离

expected: 唯一允许的低风险 demo handler 只产生一个持久化、可审计 draft；中高风险和不确定结果进入人工审批，不因 LLM/config 失败降级为自动执行。
result: pass
evidence: runtime safety matrix positive/negative cardinality、capability expiry/replay/cross-scope 场景全部通过；Plan 04 exact gate -> `110 passed`。

### 4. 审批页面只提交用户实际复核的 exact context

expected: 页面显示 V1 时即使 SSE 已收到 V2，也不能提交未展示的 V2；必须显式刷新、展示并重新复核 V2 后才允许 approve/reject。
result: pass
evidence: integrated `useAgentRun -> DetailsPanel -> ApprovalTab` regression；frontend approval/hook suite -> `4 files / 37 tests passed`，production build passed。

### 5. stale、响应丢失和失败恢复不自动重放

expected: stale detail 只刷新一次且 POST 不重放；提交已落库但响应丢失时查询权威终态收敛；draft 失败显示 error，绝不显示 completed success。
result: pass
evidence: `npm run e2e -- --grep "Phase 64.1"` -> mocked desktop/mobile `10 passed`。

### 6. 相同审批恢复请求 single-flight

expected: 两个并发、byte-identical 的恢复请求只创建一个 decision、只激活一次 graph；live lease 返回稳定 in-progress，完成/失败只写入对应 attempt identity。
result: pass
evidence: complete approval API -> `44 passed`，其中 PostgreSQL/HTTP concurrency regression 断言 one decision / one graph invocation / attempt-bound history。

### 7. 权威非成功终态不能被 stale draft 字段覆盖

expected: manual_review、refused、claim/evidence failure 在 graph、DB、API、SSE、approval resume、replay 与 memory 路径保持同一非成功终态，不被改写成成功或错误的 action-draft 结论。
result: pass
evidence: approval-resume terminal precedence focused -> `2 passed`；runtime safety matrix -> `27 passed`；Phase 64.1 full execution gate此前为 `2862 passed, 1 skipped`。

## Summary

total: 7
passed: 7
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none]

## Verification Evidence

- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/integration/test_phase64_1_runtime_safety_matrix.py -q --tb=short` -> `27 passed, 3 warnings in 75.51s`。
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_approval_api.py -q --tb=short` -> `44 passed, 1 warning in 122.71s`。
- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/db/test_phase64_1_resume_attempt_migration.py -q --tb=short` -> `2 passed, 4 warnings in 1.76s`。
- `cd frontend && npm test -- --run src/components/details/ApprovalTab.test.tsx src/hooks/useAgentRun.test.ts` -> `4 files / 37 tests passed`。
- `cd frontend && npm run build` -> passed, `1766 modules transformed`。
- `cd frontend && npm run e2e -- --grep "Phase 64.1"` -> mocked desktop/mobile `10 passed in 2.2m`。

现存 warning 为既有 LangGraph/LangChain annotation/deprecation warning，没有隐藏测试失败。
