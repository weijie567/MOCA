---
phase: 64.1-runtime-safety-and-approval-contract-repair
verified: 2026-08-04T08:26:59Z
status: passed
score: 20/20 must-haves verified
overrides_applied: 0
requirements_satisfied: 5
requirements_total: 5
decisions_satisfied: 15
decisions_total: 15
plan_must_haves_satisfied: 20
plan_must_haves_total: 20
gaps: []
human_verification: []
---

# Phase 64.1: Runtime Safety And Approval Contract Repair Verification Report

**Phase Goal:** 修复 recommendation generation、确定性风险判定、审批 API/SSE/frontend、auto-allow draft 和 final response 之间的跨层安全断点，确保 actionable recommendation 在进入 draft 或成功终态前完成规范化、风险分类、授权和审计。
**Verified:** 2026-08-04T08:26:59Z
**Status:** passed
**Re-verification:** No — initial phase-level goal verification

## Verdict

Phase 64.1 的目标已达成。5/5 roadmap success criteria、D-01..D-15 和 6 个 PLAN 的 20/20 observable must-haves 均有当前源码、wiring、持久化模型、负向测试和 current-HEAD 绿色证据支持。没有使用 SUMMARY 声明作为单独通过依据；SUMMARY 只用于定位变更面，结论来自实际代码、测试、架构 guard 与提交历史。

## Goal Achievement

### Observable Must-Haves

| # | Plan | Observable truth | Status | Repository evidence |
|---:|:---:|---|---|---|
| 1 | 01 | 中英文 actionable recommendation 在 material claim 前被 canonicalize | ✓ VERIFIED | `ActionResolution`/`resolve_action_text` 位于 `src/agent/safety/taxonomy.py:17,240`；producer 在 `src/agent/nodes/recommendation_generation.py:243` 先 resolve、后构造 claims。 |
| 2 | 01 | unknown、ambiguous、schema-invalid 不能进入普通 completed advice | ✓ VERIFIED | resolver 返回 `manual_review`；`src/agent/routing.py:1127,1140` 将 unresolved candidate 送入 claim/risk safety chain；runtime matrix 覆盖三类负向输入。 |
| 3 | 01 | recommendation/routing 不再拥有 actionable keyword authority | ✓ VERIFIED | `tests/architecture/test_safety_taxonomy_boundaries.py` 和 `test_runtime_safety_boundaries.py` 以 AST/source guard 锁定单一 taxonomy owner；未发现 `_ACTIONABLE_RECOMMENDATIONS`。 |
| 4 | 02 | 一个确定性 evaluator 覆盖 high/medium/low 并有明确优先级 | ✓ VERIFIED | `rules/risk_rules.yaml` 三组 7 条规则；`src/agent/nodes/risk_gate.py:97,216` 单一 loader/evaluator，high→medium→low；architecture guard 断言唯一 owner。 |
| 5 | 02 | LLM 只能保持/提高风险，不能 downgrade | ✓ VERIFIED | `_merge_llm_assessment` (`risk_gate.py:269`) 比较 severity/disposition rank；`test_llm_merge_is_monotonic...` 与 medium no-downgrade 用例覆盖。 |
| 6 | 02 | config/LLM failure 稳定 fail closed，不能 auto-allow | ✓ VERIFIED | invalid registry、unknown/unmatched action 返回 medium/manual_review；auto eligibility 在 `risk_gate.py:847` 严格要求 low+allow+no approval；provider failure matrix 断言 capability 为 null。 |
| 7 | 03 | list/get/SSE 共用 backend-owned v1 decision context | ✓ VERIFIED | `ApprovalDecisionContextV1` (`src/approvals/schemas.py:42`) 仅由 `ApprovalDecisionContext.project()` (`src/approvals/service.py:58`) 构造；list/get 与 agent-run SSE 均调用 service projector。 |
| 8 | 03 | frontend 校验完整 latest detail，并 exact/frozen echo，无默认推断 | ✓ VERIFIED | parser/serializer 位于 `frontend/src/lib/api.ts:108,125`；approve/reject 在 `useAgentRun.ts:644,697` 先 authoritative GET exact compare；`ApprovalTab.tsx:135` 传递实际展示的 frozen context。 |
| 9 | 03 | stale/cross-scope/hash/type/ambiguous outcome fail closed，无 existence leak/自动 replay | ✓ VERIFIED | backend 在锁内校验 request/level/assignment/revision/hash；frontend stale/ambiguous/lost-response tests 断言无自动 POST replay；manager cross-merchant API tests 返回通用 not-found envelope。 |
| 10 | 04 | 只有可信 backend 在 deterministic allow + snapshot persistence 后 mint capability | ✓ VERIFIED | `risk_gate.py:961` 是唯一 runtime mint call；`AutoActionCapabilityService.mint` (`src/actions/capabilities.py:112`) 校验 run/actor、scope、low deterministic allow 和持久 snapshot。 |
| 11 | 04 | capability 绑定 tenant/actor/run/merchant/action/payload/snapshot/risk/expiry/nonce/fixed handler | ✓ VERIFIED | durable model `src/db/models.py:1214`、migration 023、`lock_and_verify_for_draft` (`capabilities.py:228`) 对 exact tuple 逐项核验。 |
| 12 | 04 | replay/cross-binding 不新建 draft，也不扩通用 permission/外部执行 | ✓ VERIFIED | row lock + one-use state + exact retry identity；misuse/rollback/concurrency tests；architecture guard 禁止 permission mutation、production executor 和 external path import。 |
| 13 | 04 | approval resume 与 auto allow 共享 draft invariant，但授权来源可区分 | ✓ VERIFIED | `ActionService` 共享 snapshot/payload/draft invariant；auto path 写 `authorization_source=auto_allow_capability`，approval path 保持 `approval` identity。 |
| 14 | 05 | 只有 durable `not_executed_demo` draft + critical audit 可 completed | ✓ VERIFIED | `project_action_draft_terminal` (`src/agent/routing.py:228`) 校验 draft identity/outcome/audit；graph 在 `src/agent/graph.py:331` 使用 conditional edge。 |
| 15 | 05 | auth/capability/store/tool/outcome/audit failure 在 graph/DB/API/SSE/resume 一致失败 | ✓ VERIFIED | `RunTerminalV1`/`project_run_terminal` (`routing.py:133,148`) 被 agent-runs、approval resume 和 SSE 共用；负向 API/integration matrix 覆盖。 |
| 16 | 05 | failed run 不写 completed memory，也不输出 success copy | ✓ VERIFIED | `finalize_completed_agent_run_memory` (`src/api/services/agent_run_memory.py:105`) 独立重算 terminal；final response 与 API 仅对 genuine completed 调 finalizer。 |
| 17 | 06 | safety matrix 对 route/terminal/API/SSE/final 和 draft cardinality 做完整正负验证 | ✓ VERIFIED | `tests/integration/test_phase64_1_runtime_safety_matrix.py` 当前为 27-case parameterized matrix；positive path exactly one draft，所有负向路径 zero/new-zero draft。 |
| 18 | 06 | static guards 锁定 canonical action/risk/context/capability/terminal single owners | ✓ VERIFIED | `tests/architecture/test_runtime_safety_boundaries.py`、`test_safety_taxonomy_boundaries.py`、`test_action_draft_boundaries.py` 与 graph baseline 均存在且 substantive。 |
| 19 | 06 | 已验证修复和 named deferrals 被准确记录，不冒领后续 phase | ✓ VERIFIED | `.planning/ARCHITECTURE-DEBT.md` 明确记录 Phase 64.2/66/69/external-effects 边界；代码 diff 与 guard 同样支持。 |
| 20 | 06 | backend/frontend/security/validation/UAT gates 绿色 | ✓ VERIFIED | UAT 7/7；SECURITY 15/15 threats closed；VALIDATION 20/20 requirement/decision IDs、16/16 tasks；current-HEAD security aggregate 230 passed。 |

**Score:** 20/20 observable must-haves verified

## Roadmap Success Criteria

| Requirement | Status | Evidence |
|---|---|---|
| SC-64.1-1 | ✓ SATISFIED | Shared canonical resolver precedes material claims; multilingual, unknown, ambiguous and schema-invalid tests plus single-owner guards pass. |
| SC-64.1-2 | ✓ SATISFIED | Single validated high/medium/low deterministic evaluator, monotonic LLM merge, fail-closed config/provider/schema handling and no-capability assertions exist. |
| SC-64.1-3 | ✓ SATISFIED | One versioned backend projection feeds list/get/SSE; frontend validates and echoes exact displayed context; stale/hash/type/scope/replay cases close safely. |
| SC-64.1-4 | ✓ SATISFIED | Durable server-minted one-use capability binds trusted identity/scope/action/hashes/risk/expiry/fixed handler and is consumed atomically with draft/audit. |
| SC-64.1-5 | ✓ SATISFIED | Runtime matrix and API/frontend tests prove denied/stale/malformed/unsupported/auth/draft/audit failures cannot create unauthorized drafts or report completed success. |

**Requirements:** 5/5 satisfied; no orphaned Phase 64.1 requirement was found in `.planning/REQUIREMENTS.md`.

## Decision Trace (D-01..D-15)

| Decision | Status | Verification evidence |
|---|---|---|
| D-01 | ✓ | Taxonomy/normalizer is the action routing authority; producer/router local actionable sets are guarded out. |
| D-02 | ✓ | unknown/ambiguous/schema-invalid resolve to manual review and cannot ordinary-complete. |
| D-03 | ✓ | English/Chinese aliases live in the shared taxonomy; caller migration and drift tests exist. |
| D-04 | ✓ | One deterministic evaluator owns all configured severities; LLM merge is non-downgrading. |
| D-05 | ✓ | config/provider/schema/unknown/unmatched failures are medium/manual-review-or-stronger and cannot mint. |
| D-06 | ✓ | Runtime uses Phase 63 safety taxonomy helpers/vocabulary; no second risk vocabulary was introduced. |
| D-07 | ✓ | `approval_decision_context.v1` carries decision types, lifecycle versions, revision and integrity hashes across list/get/SSE/decide. |
| D-08 | ✓ | frontend runtime validator and exact serializer are paired; no legacy `decision`, integrity defaults or inferred fields. |
| D-09 | ✓ | backend exact binding/version/hash/type/scope checks and stable 403/404/409 mappings fail closed. |
| D-10 | ✓ | server-minted durable capability binds every required trusted identity, hash, risk, scope and sole handler. |
| D-11 | ✓ | capability authorizes demo draft only; policy records pending verification, mutates no permissions and exposes no production executor. |
| D-12 | ✓ | approval and auto-capability converge on draft invariant while retaining distinct authorization/audit sources. |
| D-13 | ✓ | shared terminal projector suppresses completed response/DB/API/SSE/memory on auth/draft/audit failure. |
| D-14 | ✓ | Six dependency-ordered plans/summaries separately cover canonicalization, risk, approval, capability, terminal and closeout. |
| D-15 | ✓ | All recorded pytest evidence uses `UV_CACHE_DIR=/tmp/uv-cache uv run pytest ...`; frontend uses repository Vitest/build/Playwright commands. |

**Decisions:** 15/15 verified.

## Required Artifacts

| Artifact | L1 Exists | L2 Substantive | L3 Wired | L4 Data Flow | Status |
|---|:---:|:---:|:---:|:---:|---|
| `src/agent/safety/taxonomy.py` | ✓ | 525 lines; typed resolver | producer/router import/use | recommendation text → typed candidate | ✓ VERIFIED |
| `src/agent/nodes/risk_gate.py` | ✓ | 1478 lines; validated evaluator/merge/mint | graph + snapshot/capability services | canonical action/context/config → risk/snapshot/capability | ✓ VERIFIED |
| `src/approvals/schemas.py` + `service.py` | ✓ | strict v1 models/projector | HTTP list/get and agent-run SSE | DB lifecycle rows → one decision context | ✓ VERIFIED |
| `frontend/src/lib/api.ts` + `useAgentRun.ts` | ✓ | runtime validator/exact serializer | API/SSE + ApprovalTab callbacks | server context → displayed reviewed snapshot → exact POST | ✓ VERIFIED |
| `src/actions/capabilities.py` + ORM/migrations | ✓ | mint/lock/verify/consume and constraints | risk gate + ActionService | trusted run/snapshot/risk → durable opaque capability | ✓ VERIFIED |
| `src/actions/service.py` | ✓ | atomic savepoint/idempotency/audit | fixed action executor | verified capability/approval → durable demo draft/event | ✓ VERIFIED |
| `src/agent/routing.py` + `graph.py` | ✓ | typed terminal projectors | conditional action_draft edge | draft/outcome/audit → terminal route/status | ✓ VERIFIED |
| `tests/integration/test_phase64_1_runtime_safety_matrix.py` | ✓ | 622 lines; positive/negative matrix | invokes real services/API paths | asserts exact draft cardinality and terminal projections | ✓ VERIFIED |
| `tests/architecture/test_runtime_safety_boundaries.py` | ✓ | 222 lines; AST/source ownership guards | scans production owners/current graph | prevents reintroduction of bypasses | ✓ VERIFIED |
| `frontend/e2e/phase64_1-approval-safety.spec.ts` | ✓ | 269 lines; desktop/mobile flows | mocked API + real UI components | latest detail/stale/ambiguous/failure → UI outcome | ✓ VERIFIED |

The SDK artifact/key-link parser did not understand the PLAN files' inline-map YAML and returned empty paths; those false negatives were not used. The table above is a manual L1-L4 verification against actual repository paths and call sites.

## Key Link Verification

| From | To | Via | Status |
|---|---|---|---|
| recommendation generation | safety taxonomy | `resolve_action_text` before claim construction | ✓ WIRED |
| routing | typed canonical state | unresolved candidate and verified action checks | ✓ WIRED |
| risk gate | `rules/risk_rules.yaml` | one validated high/medium/low loader/evaluator | ✓ WIRED |
| risk gate | capability service | mint only after low allow + persisted snapshot | ✓ WIRED |
| approval service | list/get/SSE | one `ApprovalDecisionContext.project()` | ✓ WIRED |
| ApprovalTab | useAgentRun/API serializer | displayed `reviewedContext` + exact authoritative GET | ✓ WIRED |
| ActionService | capability + draft/event persistence | one caller-session savepoint and row lock | ✓ WIRED |
| graph/API/resume/memory | shared run terminal | conditional router and `project_run_terminal()` | ✓ WIRED |

## Behavioral Evidence

Fresh current-HEAD evidence was reused as authorized; no full suite was rerun during this verification. Commit `73a9125` is the last source/test change, and the only later commits are SECURITY/VALIDATION documentation (`dedfec3`, `537bd79`).

| Behavior | Fresh command/evidence | Result | Status |
|---|---|---|---|
| Runtime safety matrix | approved project pytest entry on `tests/integration/test_phase64_1_runtime_safety_matrix.py` | 27 passed | ✓ PASS |
| Approval API incl. terminal precedence and concurrent resume | approved project pytest entry on `tests/test_approval_api.py` | 44 passed | ✓ PASS |
| Post-fix security aggregate after architecture guard | focused canonical/risk/architecture/capability/approval command | 230 passed | ✓ PASS |
| Resume attempt migration | approved project pytest entry on migration test | 2 passed | ✓ PASS |
| Frontend reviewed-context chain | Vitest ApprovalTab/useAgentRun suite | 4 files / 37 tests passed | ✓ PASS |
| Frontend production build | `npm run build` | passed, 1766 modules | ✓ PASS |
| Mocked Phase 64.1 approval E2E | Playwright Phase 64.1 grep | 10 passed | ✓ PASS |
| Original phase full gate | exact Plan 06 backend command | 2862 passed, 1 skipped; Ruff passed; frontend gate passed | ✓ PASS |

## Final Fix And Closeout Commit Evidence

All requested commits exist and are ancestors of current `HEAD`:

| Commit | Verified role |
|---|---|
| `43cbcb6` | approval resume uses authoritative shared run terminal; regression covers manual_review/refused over stale draft state |
| `21fd121` | UI decisions bind to exact displayed/reviewed context; SSE-newer context forces refresh/re-review |
| `475f6bf` | durable resume attempt/lease and PostgreSQL single-flight concurrency contract |
| `73a9125` | architecture guard updated to require `reviewedContext` exact comparison/freeze |
| `f6de4a8` | automated UAT complete: 7 passed, 0 issues |
| `dedfec3` | SECURITY report: 15/15 threats closed, `threats_open: 0` |
| `537bd79` | post-review VALIDATION audit: 20/20 IDs, 16/16 tasks, 4/4 review-fix surfaces |

## Anti-Scope And Deferral Check

| Boundary | Verdict | Evidence |
|---|---|---|
| Evidence identity / immutable replay / memory provenance → Phase 64.2 | ✓ PRESERVED | No evidence-ID, immutable-source or memory-provenance redesign. Memory/replay changes only add non-success terminal gating/status projection needed by D-13. |
| General operation/tool gateway → Phase 66 | ✓ PRESERVED | No new general operation contract/gateway. Tool changes are limited to the single fixed demo draft handler and authoritative capability verification. |
| Central LLM provider/model gateway → Phase 69 | ✓ PRESERVED | Risk-node LLM invocation remains local and is only bounded against downgrade; no provider/model/retry/observability gateway is claimed or added. |
| Production external effects → separately authorized phase | ✓ PRESERVED | Draft outcome is `not_executed_demo`, audit sets `external_side_effect=false`, architecture guards forbid production/action-execution nodes/imports. |
| Permission/security widening | ✓ NOT FOUND | Capability dispatch has `matched_scope=None` pending authoritative verification; source/architecture guards find no permission append/extend/update. |

These are deliberate named deferrals, not Phase 64.1 gaps.

## Anti-Patterns Found

| File | Pattern | Severity | Assessment |
|---|---|---|---|
| `frontend/src/components/details/ApprovalTab.tsx` | `placeholder="请输入驳回原因"` | ℹ️ Info | Normal accessible input placeholder, not an implementation placeholder/stub. |
| Several routing/validator helpers | `return {}` / `return []` / `return null` | ℹ️ Info | Valid absent/invalid/fail-closed branches that are populated on real data paths; none flows as a hardcoded successful user result. |

No TODO/FIXME/HACK stub, console-log-only handler, hardcoded empty decision context, unconditional action-draft success edge, or production executor was found in the Phase 64.1 source surface.

## Human Verification Required

None. The user explicitly authorized automated self-testing, Phase 64.1 UAT is complete (7/7), mocked desktop/mobile UI flows are green, and no remaining visual/external-service requirement is part of this phase's success contract.

## Gaps Summary

No actionable gaps found. No override was required. Named Phase 64.2/66/69/external-effects work remains outside this phase and is not counted against the score.

---

_Verified: 2026-08-04T08:26:59Z_
_Verifier: Codex (gsd-verifier)_
