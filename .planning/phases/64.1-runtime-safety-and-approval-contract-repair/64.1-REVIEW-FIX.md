---
phase: 64.1-runtime-safety-and-approval-contract-repair
fixed_at: 2026-08-04T15:59:43+08:00
review_path: .planning/phases/64.1-runtime-safety-and-approval-contract-repair/64.1-REVIEW.md
iteration: 3
findings_in_scope: 3
fixed: 3
skipped: 0
status: all_fixed
---

# Phase 64.1: Code Review Fix Report

**Fixed at:** 2026-08-04T15:59:43+08:00
**Source review:** `.planning/phases/64.1-runtime-safety-and-approval-contract-repair/64.1-REVIEW.md`
**Iteration:** 3

**Summary:**

- Findings in scope: 3
- Fixed: 3
- Skipped: 0

## Fixed Issues

### WR-01: Approval resume preserves authoritative manual-review/refusal terminal

**Result:** fixed: requires human verification
**Files modified:** `src/api/routers/approvals.py`, `tests/test_approval_api.py`
**Commit:** `43cbcb6`
**Applied fix:** `_resume_graph_after_decision()` now projects the final run through shared `project_run_terminal()` and persists its authoritative status, safe message, reason, error code, and final state. Action-draft reconciliation still uses `project_action_draft_terminal(..., require_action=True)` only for draft validity. Parameterized DB/lifecycle tests prove `manual_review` and `refused` win over stale action-shaped fields and do not write success memory.

### WR-02: Approval decisions bind to the exact context displayed and reviewed

**Result:** fixed: requires human verification
**Files modified:** `frontend/src/App.tsx`, `frontend/src/components/details/ApprovalTab.tsx`, `frontend/src/components/details/ApprovalTab.test.tsx`, `frontend/src/components/details/DetailsPanel.tsx`, `frontend/src/hooks/useAgentRun.ts`, `frontend/src/hooks/useAgentRun.test.ts`
**Commit:** `21fd121`
**Applied fix:** `ApprovalTab` passes a frozen clone of its displayed decision context to approve/reject callbacks. The hook requires authoritative GET to be exact to that caller-supplied snapshot before POST. A strictly newer SSE context invalidates the displayed V1 and requires an explicit detail refresh/re-review. The integrated hook/component regression proves V1 display + SSE V2 leaves POST count at zero until V2 is fetched, displayed, and confirmed.

### WR-03: Approval resume uses a durable single-flight attempt lease

**Result:** fixed: requires human verification
**Files modified:** `src/api/routers/approvals.py`, `src/db/models.py`, `src/db/migrations/versions/024_phase64_1_resume_attempt_lease.py`, `tests/test_approval_api.py`, `tests/db/test_phase64_1_resume_attempt_migration.py`
**Commit:** `475f6bf`
**Applied fix:** resume attempt identity/status/timestamps/lease are persisted on `ApprovalRequest` and acquired under its PostgreSQL row lock. A live lease returns `APPROVAL_RESUME_IN_PROGRESS`; failed or expired/abandoned attempts may acquire a new identity; completion/failure must match both attempt and decision identity. Migration round-trip and concurrent HTTP/DB coverage prove one decision row, one active graph invocation, and attempt-bound event history.

## Verification

- Complete approval API: `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_approval_api.py -q --tb=short` → `44 passed, 1 warning in 122.71s`.
- WR-01 focused terminal precedence: `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_approval_api.py -k 'preserves_authoritative_non_success_terminal' -q --tb=short` → `2 passed, 42 deselected, 1 warning in 6.81s`.
- WR-03 migration contract/round-trip: `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/db/test_phase64_1_resume_attempt_migration.py -q --tb=short` → `2 passed, 4 warnings in 1.76s`.
- Frontend approval/hook suite: `npm test -- --run src/components/details/ApprovalTab.test.tsx src/hooks/useAgentRun.test.ts` → `4 files passed, 37 tests passed`.
- Frontend production build: `npm run build` → passed, `1766 modules transformed`.
- Ruff: `uv run ruff check src/api/routers/approvals.py src/db/models.py src/db/migrations/versions/024_phase64_1_resume_attempt_lease.py tests/test_approval_api.py tests/db/test_phase64_1_resume_attempt_migration.py` → `All checks passed!`.

## Remaining Risk

- The 15-minute resume lease is a bounded recovery policy: after expiry, a new attempt may intentionally re-invoke the graph if no durable completed-finalizer evidence exists. Downstream graph operations must therefore continue to preserve their own durable idempotency boundaries.
- Verification proves the required deterministic and concurrent paths but does not replace manual review of the three state-machine/precedence decisions; all findings are marked accordingly.
- Existing LangGraph/LangChain deprecation warnings remain unchanged and were not treated as failures.

---

_Fixed: 2026-08-04T15:59:43+08:00_
_Fixer: Codex (gsd-code-fixer)_
_Iteration: 3_
