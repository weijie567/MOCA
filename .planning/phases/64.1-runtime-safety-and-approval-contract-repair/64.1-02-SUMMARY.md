---
phase: 64.1-runtime-safety-and-approval-contract-repair
plan: "02"
subsystem: agent-safety
tags: [deterministic-risk, fail-closed, llm-merge, approval-binding]
requires:
  - phase: 64.1-runtime-safety-and-approval-contract-repair
    provides: canonical action candidate and fail-closed recommendation routing from Plan 01
provides:
  - validated high/medium/low deterministic risk evaluator with explicit precedence
  - monotonic LLM merge that cannot downgrade deterministic risk
  - fail-closed config/provider/schema handling without auto capability
affects: [64.1-03, 64.1-04, approval-contract, action-draft]
tech-stack:
  added: []
  patterns: [validated rule registry, monotonic risk merge, low-allow-only capability eligibility]
key-files:
  created: []
  modified:
    - src/agent/nodes/risk_gate.py
    - tests/agent/test_nodes/test_risk_gate.py
    - tests/test_interception_rate.py
key-decisions:
  - "Evaluate deterministic rules in high, medium, low order and reject incomplete, duplicate, malformed, or unsupported rule registries."
  - "Treat only low plus allow plus approval_required=false as eligible for auto-allowed binding."
patterns-established:
  - "Deterministic authority: LLM output may preserve or strengthen a decision and add explanation, but cannot replace a stronger deterministic rule identity."
  - "Failure is non-capability: config, action, rule, or provider uncertainty resolves to manual review without auto authorization."
requirements-completed: [SC-64.1-2, SC-64.1-5, D-04, D-05, D-06, D-15]
duration: 10min
completed: 2026-07-10
---

# Phase 64.1 Plan 02: Deterministic Risk Authority Summary

**All configured high, medium, and low rules now participate in one validated backend evaluator, while LLM failures and downgrade attempts preserve fail-closed deterministic handling.**

## Performance

- **Duration:** 10 min
- **Started:** 2026-07-10T03:38:00Z
- **Completed:** 2026-07-10T03:47:30Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- Replaced the high-only/first-low fallback with a complete high > medium > low deterministic evaluator covering all seven configured rules.
- Added registry validation for missing/empty groups, duplicate identifiers, malformed YAML/root shapes, and unsupported conditions.
- Made LLM merge monotonic and added timeout, unavailable, invalid-schema, conflict, precedence, and non-downgrade matrices.
- Preserved existing `RiskDecisionV1`, canonical payload/snapshot hashes, policy/risk config versions, and snapshot persistence path.
- Prevented `medium/manual_review` and other uncertain results from producing auto-allowed bindings.

## Task Commits

1. **Task 1: Specify deterministic rule and failure parity** - `6f3e881` (test)
2. **Task 2: Implement validated deterministic evaluator and bounded LLM merge** - `74e78d7` (fix)

## Files Created/Modified

- `src/agent/nodes/risk_gate.py` - Validated three-level evaluator, monotonic LLM merge, provider failure handling, and narrow auto-allow eligibility.
- `tests/agent/test_nodes/test_risk_gate.py` - Complete rule/config/failure/no-downgrade matrix.
- `tests/test_interception_rate.py` - Updated unresolved rejection semantics to the canonical manual-review boundary.
- `.planning/ARCHITECTURE-DEBT.md` - Verified deterministic risk authority repair entry.
- `.planning/LOCAL-VALIDATION-ISSUES.md` - zsh RED-gate wrapper incident (left with the pre-existing user-owned dirty file).

## Decisions Made

- A valid registry must contain non-empty high, medium, and low groups with globally unique rule IDs and supported deterministic conditions.
- Strong deterministic rules are evaluated before action-resolution uncertainty, so an explicit high-risk amount cannot be hidden inside an unresolved free-form action.
- Manual review is deliberately not represented as approval-required and cannot mint an auto-allowed binding; later plans own the richer approval/capability contract.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected stale free-form rejection expectation**
- **Found during:** Task 2 focused verification
- **Issue:** An existing interception test expected an unresolved rejection sentence to create a `manual_review` proposed action and approval request, conflicting with Plan 01 canonical fail-closed semantics.
- **Fix:** Asserted manual-review disposition, no executable proposed action, and no approval route.
- **Files modified:** `tests/test_interception_rate.py`
- **Verification:** Full focused suite passes with 69 tests.
- **Committed in:** `74e78d7`

**2. [Rule 2 - Missing Critical] Restricted auto binding to deterministic low allow**
- **Found during:** Task 2 provider-failure matrix
- **Issue:** Existing code treated every `approval_required=false` result as auto-allowed, including `medium/manual_review`.
- **Fix:** Added a single eligibility predicate requiring low severity, allow disposition, and no required approval.
- **Files modified:** `src/agent/nodes/risk_gate.py`
- **Verification:** Medium timeout/unavailable/schema and downgrade tests assert no auto binding.
- **Committed in:** `74e78d7`

---

**Total deviations:** 2 auto-fixed (1 correctness bug, 1 missing critical safety guard).
**Impact on plan:** Both fixes enforce the locked fail-closed contract without expanding approval, capability, or frontend scope.

## Issues Encountered

- The first RED wrapper used zsh's read-only `status` variable after pytest. The pytest evidence itself was valid; subsequent commands used `rc`, and the incident was appended to `.planning/LOCAL-VALIDATION-ISSUES.md`.

## Verification

- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_risk_gate.py tests/test_interception_rate.py tests/approvals/test_hash_binding.py tests/approvals/test_snapshots.py -q --tb=short` → `69 passed`.
- `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/nodes/risk_gate.py tests/agent/test_nodes/test_risk_gate.py tests/test_interception_rate.py tests/approvals/test_hash_binding.py tests/approvals/test_snapshots.py` → `All checks passed!`.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Plan 03 can consume stable deterministic severity/disposition, rule/config identity, hashes, and snapshot refs when repairing the versioned approval decision context.
- No Plan 02 blocker remains. Bounded server capability and terminal failure propagation remain intentionally owned by Plans 04 and 05.

## Self-Check: PASSED

---
*Phase: 64.1-runtime-safety-and-approval-contract-repair*
*Completed: 2026-07-10*
