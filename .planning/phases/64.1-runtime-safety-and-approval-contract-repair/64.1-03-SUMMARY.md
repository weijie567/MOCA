---
phase: 64.1-runtime-safety-and-approval-contract-repair
plan: "03"
subsystem: approval-contract
tags: [approval, concurrency, sse, frontend, fail-closed]
requires:
  - phase: 64.1-runtime-safety-and-approval-contract-repair
    provides: canonical action routing and deterministic risk authority from Plans 01-02
provides:
  - one backend-owned approval_decision_context.v1 projection for list, get, and SSE
  - shared Python/TypeScript JSON fixture and frontend runtime validator
  - exact frozen decide serialization with stale, disconnect, and ambiguity fail-closed handling
  - approval Console list-to-latest-detail decidability and durable-draft copy
affects: [64.1-04, approval-resume, console-approval]
tech-stack:
  added: []
  patterns: [backend-owned context projector, exact echo serializer, latest-detail decision gate]
key-files:
  created:
    - contracts/fixtures/approval_decision_context_v1.json
    - frontend/src/components/details/ApprovalTab.test.tsx
  modified:
    - src/approvals/schemas.py
    - src/approvals/service.py
    - src/api/routers/approvals.py
    - src/api/routers/agent_runs.py
    - frontend/src/lib/api.ts
    - frontend/src/hooks/useAgentRun.ts
    - frontend/src/components/details/ApprovalTab.tsx
key-decisions:
  - "Project one safe proposed-action summary inside the otherwise complete decision binding so SSE does not expose raw args or evidence payloads."
  - "Treat list as discovery only; every Console decision freezes a freshly validated get response."
  - "Never replay an ambiguous POST; invalidate decidability and query authoritative state."
patterns-established:
  - "Exact echo: four versions and two hashes are copied from one frozen v1 context without defaults or legacy decision fields."
  - "Fail closed UI: malformed, disconnected, expired, unsupported, stale, and ambiguous states disable native decision controls."
requirements-completed: [SC-64.1-3, SC-64.1-5, D-07, D-08, D-09, D-15]
duration: 22min
completed: 2026-07-10
---

# Phase 64.1 Plan 03: Versioned Approval Decision Contract Summary

**Approval list, detail, SSE, decide, and Console now share one versioned concurrency binding, with exact frontend echo and fail-closed recovery.**

## Accomplishments

- Added backend `ApprovalDecisionContextV1` and one service-owned projector reused by pending list, latest detail, and `approval_required` SSE.
- Added one language-neutral JSON fixture imported directly by Python and TypeScript tests.
- Added strict frontend runtime validation and discriminated exact serialization; the legacy `decision` field and guessed versions/hashes are absent.
- Added run/generation guards, disconnect invalidation, latest-detail refresh, frozen request snapshots, and ambiguous-result no-replay behavior.
- Reworked ApprovalTab without redesigning the Console: list discovery precedes detail validation, native buttons remain disabled until decidable, reject reason is labelled/required, live status copy is bounded to durable draft creation.

## Task Commits

1. **Task 1: Establish one backend decision-context contract and parity tests** - `89afe7e`
2. **Task 2: Implement frontend runtime validation and exact request serialization** - `fae3059`
3. **Task 3: Make ApprovalTab decidability, copy, and accessibility match the contract** - `08777d3`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Security bug] Safe-project proposed action inside decision context**
- **Found during:** Task 1 focused backend verification
- **Issue:** The first projector included raw proposed-action args in SSE and failed the existing no-sensitive-payload assertion.
- **Fix:** Centralized a safe action summary in the domain projector while retaining all decision-critical versions, ids, refs, and hashes.
- **Verification:** SSE sensitive-field test and shared fixture shape test pass.
- **Committed in:** `89afe7e`

**Total deviations:** 1 auto-fixed security projection issue. **Impact:** No scope expansion; decision integrity remains complete while display data stays bounded.

## Issues Encountered

- The exact backend aggregate suite retains four pre-existing `tests/test_approval_integration.py` fixture/query failures after Plan 01 canonical routing changes. They do not reach the approval boundary and are recorded in `.planning/LOCAL-VALIDATION-ISSUES.md`; Plan 03 did not weaken fail-closed routing to make stale fixtures pass.

## Verification

- `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_approval_api.py -q --tb=short` -> `37 passed, 1 warning`.
- Targeted backend decision-context and SSE security checks -> `2 passed, 1 warning`.
- Exact backend aggregate gate -> Plan 03 surfaces pass; four known integration fixture drift failures remain outside this plan.
- Plan-scoped `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check ...` -> `All checks passed!`.
- `cd frontend && npm test -- --run src/components/details/ApprovalTab.test.tsx src/lib/api.test.ts src/hooks/useAgentRun.test.ts` -> `4 files passed, 20 tests passed`.
- `cd frontend && npm run build` -> production build passed.

## User Setup Required

None.

## Next Phase Readiness

- Plan 04 can consume the exact approval binding when minting bounded draft capability.
- No Plan 03 implementation blocker remains; the named integration fixture drift is a validation-baseline follow-up, not an approval-contract gap.

## Self-Check: PASSED

---
*Phase: 64.1-runtime-safety-and-approval-contract-repair*
*Completed: 2026-07-10*
