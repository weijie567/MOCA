---
phase: 64.1
slug: runtime-safety-and-approval-contract-repair
status: complete
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-10
updated: 2026-08-04
---

# Phase 64.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest + pytest-asyncio; Vitest/React Testing Library for frontend |
| **Config file** | `pyproject.toml`; `frontend/package.json`; `frontend/vite.config.ts` |
| **Quick run command** | `UV_CACHE_DIR=/tmp/uv-cache uv run pytest <plan-focused test files> -q --tb=short` |
| **Full suite command** | `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent tests/approvals tests/actions tests/architecture tests/integration/test_phase64_1_runtime_safety_matrix.py tests/test_approval_api.py tests/test_approval_integration.py tests/test_graph_routing.py tests/test_agent_runs_api.py -q --tb=short` then `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` then `cd frontend && npm test -- --run && npm run build && npm run e2e` (the E2E suite is mocked Playwright; no external side effects) |
| **Observed runtime** | Plan 06 focused matrix/architecture runs stayed under 120 seconds; the earlier Plan 05 API aggregate took `257.41s` and exceeded the sampling target. The exact ordered phase gate took about 16 minutes (`837.35s` backend plus Ruff and `1.8m` frontend E2E) |

---

## Sampling Rate

- **After every task commit:** Run the task's focused `UV_CACHE_DIR=/tmp/uv-cache uv run pytest ... -q --tb=short` command; frontend tasks also run the named Vitest file.
- **After every plan wave:** Run all focused files owned by completed plans plus `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check` on changed Python paths.
- **Before `$gsd-verify-work`:** The complete Phase 64.1 safety matrix, frontend test run, and frontend build must be green.
- **Max feedback latency:** 120 seconds for per-task focused checks.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 64.1-01-* | 01 | 1 | Canonical action/routing | T-64.1-01 | Chinese/English aliases normalize before claims; unknown/ambiguous actions fail closed | unit + node | `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_recommendation_generation.py tests/test_graph_routing.py -q --tb=short` | ✅ | ✅ green |
| 64.1-02-* | 02 | 2 | Deterministic risk parity | T-64.1-02 | High/medium/low rules share one authority; invalid LLM/config cannot downgrade | unit + node | `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_risk_gate.py tests/test_graph_routing.py -q --tb=short` | ✅ | ✅ green |
| 64.1-03-* | 03 | 3 | Approval decision contract | T-64.1-03, T-64.1-04 | list/get/SSE/decide round-trip exact versions/hashes; stale and cross-scope close safely | service + API + frontend | `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/approvals tests/test_approval_api.py tests/test_approval_integration.py tests/test_agent_runs_api.py -q --tb=short`, then `cd frontend && npm test -- --run src/lib/api.test.ts src/hooks/useAgentRun.test.ts`, then `cd frontend && npm run build` (the focused frontend test directly imports the repository-level shared JSON fixture through the configured `@contracts` alias) | ✅ | ✅ green |
| 64.1-04-* | 04 | 3 | Bounded auto capability | T-64.1-05, T-64.1-06 | Server-minted capability is actor/action/scope/hash/handler bound and replay safe | unit + migration + integration + architecture | Task 1 RED includes `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/db/test_phase64_1_capability_migration.py ...; test $? -ne 0`; GREEN runs `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/actions tests/db/test_phase64_1_capability_migration.py tests/architecture/test_action_draft_boundaries.py -q --tb=short` plus the upgrade/downgrade/upgrade schema verification in Plan 04 | ✅ | ✅ green |
| 64.1-05-* | 05 | 4 | Draft failure/terminal integrity | T-64.1-07, T-64.1-08 | Draft/auth/audit failures create no successful final/completed terminal and no unauthorized draft | graph + API + DB | `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_graph.py tests/test_graph_routing.py tests/test_agent_runs_api.py tests/test_approval_integration.py -q --tb=short` | ✅ | ✅ green |
| 64.1-06-* | 06 | 5 | Full safety matrix and drift guards | T-64.1-01 through T-64.1-08 | End-to-end positive/negative paths and single-source boundaries remain enforced | integration + architecture + lint + frontend | Exact full gate in **Required Full Gate** below | ✅ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Existing backend/frontend infrastructure covers the phase. Each plan may add focused test modules before implementation, but no framework/bootstrap task is required.

---

## Manual-Only Verifications

All safety and contract behaviors have automated verification. A short Console smoke may supplement, but cannot replace, list/get/SSE/decide round-trip and stale/failure tests.

---

## Required Safety Matrix

| Case | Expected route/terminal | Draft invariant |
|------|-------------------------|-----------------|
| Canonical English/Chinese low action | deterministic allow -> valid capability -> draft -> completed demo | exactly one durable draft |
| Medium action | approval/manual review | none before trusted decision |
| High action | approval required | none before trusted decision |
| Unknown/ambiguous/schema-invalid action | manual review/error; never ordinary completed advice | none |
| LLM timeout/unavailable/invalid schema | deterministic severity or fail-closed fallback | none unless deterministic allow is independently proven |
| Stale revision/version or hash mismatch | stable conflict/closed; no resume | none |
| Cross tenant/run/actor/merchant/handler capability | authorization failure | none |
| Expired/replayed capability | stable closed or explicit idempotent same-use result | no new draft |
| Draft store/tool/audit failure | error/manual terminal; no successful SSE/final | none or rolled-back partial row |

## Required Full Gate

Plan 06 and phase closeout must run this exact ordered gate; focused subsets do not replace it:

1. `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent tests/approvals tests/actions tests/architecture tests/integration/test_phase64_1_runtime_safety_matrix.py tests/test_approval_api.py tests/test_approval_integration.py tests/test_graph_routing.py tests/test_agent_runs_api.py -q --tb=short`
2. `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests`
3. `cd frontend && npm test -- --run && npm run build && npm run e2e`

The final `npm run e2e` is the mocked Playwright Phase 64.1 approval-safety flow. It must not call a production backend or create an external side effect.

## Execution Evidence

- **Task 1 safety matrix:** `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/integration/test_phase64_1_runtime_safety_matrix.py -q --tb=short` → `26 passed, 3 warnings in 58.22s`; the exact Task 1 frontend chain passed Vitest `4 files / 20 tests` and Playwright `16/16` desktop/mobile cases.
- **Task 2 ownership guards:** the exact architecture gate → `42 passed, 1 warning in 8.63s`; supplemental canonical graph baseline → `21 passed, 1 warning in 1.41s`; touched architecture Ruff passed.
- **Required backend gate:** the first run exposed four real precedence regressions in `tests/agent/test_phase22_final_response.py`; commit `8ae3c4b` moved the action-draft terminal check after authoritative evidence/claim verification non-allow handling. The same exact command then passed: `2862 passed, 1 skipped, 109 warnings in 837.35s (0:13:57)`.
- **Required Ruff gate:** `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests` → `All checks passed!`.
- **Required frontend gate:** `npm test -- --run` → `4 passed` files / `20 passed` tests; `npm run build` → `1766 modules transformed`, production build passed; `npm run e2e` → mocked desktop/mobile `16 passed (1.8m)`.
- **External-effects boundary:** Playwright routes are fully mocked and the matrix only creates `not_executed_demo` durable drafts; no production backend or external side effect was called.

## Roadmap Success-Criteria Trace

| Criterion | Green evidence |
|-----------|----------------|
| SC-64.1-1 canonical action before claims | English/Chinese/unknown/ambiguous/schema-invalid integration matrix; resolver/producer AST ownership guards |
| SC-64.1-2 deterministic risk authority | High/medium/low, provider failure, invalid config/schema and no-downgrade unit/integration cases; single-evaluator guard |
| SC-64.1-3 versioned decision context | list/get/SSE projector parity, exact decide echo, stale/hash/unsupported/cross-scope failures, frontend validator/serializer/E2E |
| SC-64.1-4 bounded capability | tenant/actor/run/action/payload/risk/merchant/handler/expiry/replay matrix, durable mint/consume guards, no permission widening |
| SC-64.1-5 terminal integrity | exactly zero-or-one draft assertions; store/tool/audit failure injection; DB/API/SSE/final/memory no-completed checks; conditional graph baseline |

## Decision Trace

| Decisions | Executable evidence |
|-----------|---------------------|
| D-01..D-03 — canonical action authority and compatibility | Plan 01 canonical resolver/producer/router tests; Plan 06 multilingual/invalid matrix and single-owner guards |
| D-04..D-06 — deterministic risk authority and taxonomy parity | Plan 02 high/medium/low/config/provider no-downgrade tests; Plan 06 evaluator guard and failure matrix |
| D-07..D-09 — versioned decision context and stale handling | Plan 03 list/get/SSE/decide backend/frontend contract tests; Plan 06 shared fixture, stale/hash/cross-scope/E2E cases |
| D-10..D-12 — bounded capability and distinct authorization source | Plan 04 migration/mint/consume/merchant/handler/replay tests plus Plan 05 approval-resume/auto-allow terminal assertions |
| D-13 — failure cannot become successful completion | Plan 05 conditional terminal/API/SSE/DB/memory suite and Plan 06 store/tool/audit failure injection |
| D-14 — dependency-ordered plan granularity | Six numbered Plan 01-06 artifacts and summaries cover canonical action, risk, approval, capability, terminal integrity, and closeout separately |
| D-15 — project-native automated verification | Every backend command uses `UV_CACHE_DIR=/tmp/uv-cache uv run`; frontend evidence uses repository npm/Vitest/build/Playwright entries |

## Threat Mitigation Sign-Off

| Threat | Mitigation evidence | Status |
|--------|---------------------|--------|
| T-64.1-01 — action alias/bypass | Canonical resolver before material claims; English/Chinese/unknown/ambiguous/schema-invalid matrix and AST guard | ✅ mitigated |
| T-64.1-02 — risk downgrade | One validated deterministic high/medium/low evaluator; provider/config/schema failures and monotonic merge tests | ✅ mitigated |
| T-64.1-03 — decision-context drift | One backend model/projector feeds list/get/SSE; shared fixture and exact parity assertions | ✅ mitigated |
| T-64.1-04 — stale/inferred frontend decision | Exact serializer, no legacy/default fields, stale refresh and ambiguous-submit no-replay E2E | ✅ mitigated |
| T-64.1-05 — forged/cross-scope capability | Server-minted durable capability binds tenant/actor/run/action/payload/risk/merchant/handler | ✅ mitigated |
| T-64.1-06 — expiry/replay/permission widening | Atomic consume, expiry/replay matrix, fixed handler and no-general-permission architecture guards | ✅ mitigated |
| T-64.1-07 — partial draft/store/tool/audit failure | Transactional/failure-injection matrix proves zero draft or rollback and stable error terminal | ✅ mitigated |
| T-64.1-08 — completed-success laundering | Conditional post-draft route and shared terminal projector across graph/DB/API/SSE/final/resume/memory | ✅ mitigated |
| T-64.1-09 — unsupported closeout claims | Exact commits, paths and commands recorded here and in architecture debt; first failed full run retained transparently | ✅ mitigated |
| T-64.1-10 — deferred-scope elevation | Phase 64.2/66/69 and production external effects remain explicitly deferred; no production executor added | ✅ mitigated |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or existing infrastructure coverage.
- [x] Sampling continuity: no 3 consecutive tasks without automated verify.
- [x] Every plan uses the MOCA project entry (`uv run` or `.venv/bin/...`), never bare `pytest`.
- [x] No watch-mode flags.
- [ ] The 120-second feedback-latency target was not met by every plan task: Plan 05's exact API aggregate took `257.41s`. Plan 06 focused backend/architecture runs were under 120 seconds, and the exception is recorded rather than hidden.
- [x] Full backend/frontend safety matrix is green.
- [x] `nyquist_compliant: true` was set only after the exact ordered gates passed.

**Approval:** approved on 2026-07-10 — Phase 64.1 Plan 06 exact backend, Ruff, frontend test/build/E2E gates are green; T-64.1-01 through T-64.1-10 have no open mitigation gap within this phase boundary.

## Validation Audit 2026-08-04 — Post-Review-Fix Nyquist Audit

| Metric | Count |
|--------|------:|
| Requirement/decision identifiers audited | 20 |
| COVERED | 20 |
| PARTIAL | 0 |
| MISSING | 0 |
| Plan tasks covered | 16/16 |
| Threats covered | 15/15 |
| Review-fix surfaces covered | 4/4 |
| New test files required | 0 |

`nyquist_compliant: true`

The original 13-minute full gate was not repeated because no uncertainty remained in unchanged Plan 01-06 surfaces. Post-review changes were verified through focused current-HEAD commands:

- Runtime matrix, architecture guards, authoritative approval-resume terminal, and concurrent resume single-flight: `38 passed, 3 warnings in 86.03s`.
- Durable resume attempt migration/ORM upgrade-downgrade-reupgrade: `2 passed, 4 warnings in 1.31s`.
- ApprovalTab/useAgentRun exact reviewed-context flow: `4 files / 37 tests passed in 1.82s`.

### Post-Review-Fix Evidence

| Surface | Automated proof | Status |
|---------|-----------------|--------|
| `manual_review`/`refused` remains authoritative during approval resume | Parameterized API regression asserts DB/lifecycle status, safe reason/code, attempt history, and no completed memory write | green |
| Displayed V1 plus SSE V2 cannot POST before explicit V2 refresh/re-review | Integrated hook/component regression asserts POST count remains zero and the stale action is disabled | green |
| Resume attempt/lease is durable and concurrent retries are single-flight | Migration round-trip plus PostgreSQL/HTTP race test asserts one decision and one graph invocation | green |
| Reviewed-context ownership cannot regress | Architecture guard from `73a9125` requires exact authoritative GET comparison with `reviewedContext` and freezes that reviewed snapshot | green |

No automated coverage gap, implementation bug, environment failure, or additional local-validation incident was found during the Nyquist audit.
