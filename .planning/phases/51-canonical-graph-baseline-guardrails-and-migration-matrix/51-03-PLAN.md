---
phase: 51-canonical-graph-baseline-guardrails-and-migration-matrix
plan: "03"
type: execute
wave: 3
depends_on: ["51-02"]
files_modified:
  - .planning/ARCHITECTURE-DEBT.md
  - .planning/phases/51-canonical-graph-baseline-guardrails-and-migration-matrix/51-VALIDATION.md
autonomous: true
requirements: [CAGM-02]
requirements_addressed: [CAGM-02]
must_haves:
  truths:
    - "Architecture debt records Phase 51 as guardrail/matrix coverage, not runtime migration completion."
    - "Validation artifacts record exact approved commands and note that runtime graph remains legacy/canonical mixed."
    - "Final no-debt enforcement is explicitly Phase 58 scope."
    - "Protected source files have no runtime graph behavior diff."
  artifacts:
    - path: ".planning/ARCHITECTURE-DEBT.md"
      provides: "Agent Graph migration debt ledger update for Phase 51 guardrail coverage"
      contains: "Phase 51"
    - path: ".planning/phases/51-canonical-graph-baseline-guardrails-and-migration-matrix/51-VALIDATION.md"
      provides: "Phase 51 validation closeout record"
      contains: "uv run pytest tests/architecture/test_canonical_graph_baseline.py -q"
  key_links:
    - from: ".planning/ARCHITECTURE-DEBT.md"
      to: "tests/architecture/test_canonical_graph_baseline.py"
      via: "evidence pointer to static graph baseline guardrails"
      pattern: "test_canonical_graph_baseline"
    - from: ".planning/phases/51-canonical-graph-baseline-guardrails-and-migration-matrix/51-VALIDATION.md"
      to: "src/agent/graph.py"
      via: "protected no-runtime-diff check"
      pattern: "git diff --exit-code -- src/agent/graph.py"
---

<objective>
Close Phase 51 documentation and validation records without claiming runtime graph migration is complete.

Purpose: CAGM-02 is a guardrail phase. The closeout must leave a clear ledger and validation trail while preserving Phase 58 as the final no-debt enforcement point.
Output: updated architecture-debt ledger and Phase 51 validation artifact.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/ARCHITECTURE-DEBT.md
@.planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
@.planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SUMMARY.md
@.planning/phases/51-canonical-graph-baseline-guardrails-and-migration-matrix/51-CONTEXT.md
@.planning/phases/51-canonical-graph-baseline-guardrails-and-migration-matrix/51-RESEARCH.md
@.planning/phases/51-canonical-graph-baseline-guardrails-and-migration-matrix/51-PATTERNS.md
@.planning/phases/51-canonical-graph-baseline-guardrails-and-migration-matrix/51-VALIDATION.md
@tests/architecture/graph_baseline.py
@tests/architecture/test_canonical_graph_baseline.py
@src/agent/graph.py
@src/agent/routing.py
@src/agent/graph_vocabulary.py

<decision_traceability>
D-51-01: Phase 51 is guardrail/test/docs only; no runtime graph rewiring or runtime node creation.
D-51-02: The target graph constant is exactly the 15 nodes listed in Phase 51 CONTEXT and Phase 50 SPEC.
D-51-03: Migration mode permits the current mixed graph only when every active legacy node maps to a canonical target and named deletion phase.
D-51-04: `slot_extraction`, `normalize_input`, `memory_write`, `trace_close`, and `action_execution` are forbidden registered main-chain nodes.
D-51-05: Tests must use static/source inspection with MOCA-approved `uv run pytest ...` commands.
D-51-06: Documentation/ledger closeout must state runtime graph remains legacy/canonical mixed until later phases.
</decision_traceability>

<source_audit>
SOURCE | ID | Requirement / Decision | Plan | Status
--- | --- | --- | --- | ---
GOAL | Phase 51 | Baseline graph guardrails and migration matrix checks exist before runtime rewiring | 51-01, 51-02, 51-03 | COVERED
REQ | CAGM-02 | Source-verified current nodes, router routes, legacy mapping, and no-`slot_extraction` rule | 51-01, 51-02, 51-03 | COVERED
RESEARCH | R-01 | Static tests avoid live providers/DB and runtime graph imports | 51-01, 51-02 | COVERED
RESEARCH | R-02 | Explicit matrix covers vocabulary gap for `generate_recommendation` | 51-01, 51-02 | COVERED
RESEARCH | R-03 | Final exact no-debt gate remains Phase 58-scoped | 51-02, 51-03 | COVERED
CONTEXT | D-51-01 | Guardrail/test/docs only; no runtime behavior change | 51-01, 51-02, 51-03 | COVERED
CONTEXT | D-51-02 | Exact target graph represented in tests | 51-01, 51-02 | COVERED
CONTEXT | D-51-03 | Current mixed graph allowed only in migration mode with mappings | 51-01, 51-02, 51-03 | COVERED
CONTEXT | D-51-04 | Forbidden main-chain names tested | 51-01, 51-02 | COVERED
CONTEXT | D-51-05 | Approved test commands only | 51-01, 51-02, 51-03 | COVERED
CONTEXT | D-51-06 | Ledger/validation state runtime graph remains mixed and final enforcement is Phase 58 | 51-03 | COVERED
</source_audit>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Agent Graph architecture debt ledger for Phase 51 guardrails</name>
  <read_first>
    - AGENTS.md architecture-debt ledger rule
    - CLAUDE.md target-vs-implemented fact separation
    - .planning/ARCHITECTURE-DEBT.md Agent Graph section
    - .planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md final no-debt gate
    - tests/architecture/graph_baseline.py
    - tests/architecture/test_canonical_graph_baseline.py
  </read_first>
  <files>.planning/ARCHITECTURE-DEBT.md</files>
  <action>
    Append or update the Agent Graph section in `.planning/ARCHITECTURE-DEBT.md` using the existing Chinese ledger style, per D-51-06.

    Record that Phase 51 adds source-verified static guardrails and a migration-mode matrix for the current mixed graph. The entry must explicitly say:
    - `src/agent/graph.py` runtime remains legacy/canonical mixed after Phase 51.
    - Phase 51 does not create `safety_pre_route`, `contextual_intent_resolve`, `slot_resolution_gate`, `memory_context_load`, `recommendation_generation`, or `risk_gate` runtime nodes.
    - Phase 51 test-local matrix covers active legacy mappings, including `generate_recommendation -> recommendation_generation`, because `graph_vocabulary.py` is not complete enough to be the only source.
    - `slot_extraction`, `normalize_input`, `memory_write`, `trace_close`, and `action_execution` remain forbidden as registered main-chain graph nodes.
    - Final exact no-debt enforcement and deletion/internalization of active legacy graph names remain Phase 58 scope.

    Use evidence pointers to Phase 50 SPEC, `tests/architecture/graph_baseline.py`, `tests/architecture/test_canonical_graph_baseline.py`, and the Phase 51 validation artifact. Do not mark runtime migration fixed or complete.
  </action>
  <verify>
    <automated>git diff --check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Phase 51|51|CAGM-02" .planning/ARCHITECTURE-DEBT.md` matches the new or updated ledger entry.
    - `rg -n "legacy/canonical|canonical mixed|混合" .planning/ARCHITECTURE-DEBT.md` matches wording that runtime remains mixed.
    - `rg -n "generate_recommendation.*recommendation_generation|recommendation_generation.*generate_recommendation" .planning/ARCHITECTURE-DEBT.md` matches.
    - `rg -n "Phase 58" .planning/ARCHITECTURE-DEBT.md` matches final no-debt scope wording.
    - `git diff --check` exits 0.
  </acceptance_criteria>
  <done>Architecture debt ledger records Phase 51 guardrail coverage while preserving runtime migration debt for later phases.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize validation artifact and protected no-runtime-diff checks</name>
  <read_first>
    - .planning/phases/51-canonical-graph-baseline-guardrails-and-migration-matrix/51-VALIDATION.md
    - .planning/phases/51-canonical-graph-baseline-guardrails-and-migration-matrix/51-CONTEXT.md validation command constraints
    - tests/architecture/test_canonical_graph_baseline.py
    - src/agent/graph.py
    - src/agent/routing.py
    - src/agent/graph_vocabulary.py
  </read_first>
  <files>.planning/phases/51-canonical-graph-baseline-guardrails-and-migration-matrix/51-VALIDATION.md</files>
  <action>
    Update `51-VALIDATION.md` after Plan 51-02 tests pass.

    Set `wave_0_complete: true` in frontmatter only after `tests/architecture/graph_baseline.py` and `tests/architecture/test_canonical_graph_baseline.py` exist.

    Update the Per-Task Verification Map statuses from pending to green for completed Phase 51 rows. For the final exact no-debt gate row, record it as intentionally skipped/Phase-58-scoped, not failed.

    Add a Validation Sign-Off section recording exact command results for:
    - `uv run pytest tests/architecture/test_canonical_graph_baseline.py -q`
    - `uv run pytest tests/architecture -q`
    - `git diff --check`
    - `git diff --exit-code -- src/agent/graph.py src/agent/routing.py src/agent/graph_vocabulary.py`

    The sign-off text must state that Phase 51 made no runtime graph behavior change and that the active graph remains legacy/canonical mixed until Phases 52-58 migrate and clean it up. Phrase the protected diff evidence as "protected runtime graph files no diff"; do not claim the whole working tree is clean, because unrelated planning/docs diffs may exist.
  </action>
  <verify>
    <automated>uv run pytest tests/architecture/test_canonical_graph_baseline.py -q</automated>
    <automated>uv run pytest tests/architecture -q</automated>
    <automated>git diff --check</automated>
    <automated>git diff --exit-code -- src/agent/graph.py src/agent/routing.py src/agent/graph_vocabulary.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "wave_0_complete: true" .planning/phases/51-canonical-graph-baseline-guardrails-and-migration-matrix/51-VALIDATION.md` matches after helper/test files exist.
    - `rg -n "uv run pytest tests/architecture/test_canonical_graph_baseline.py -q" .planning/phases/51-canonical-graph-baseline-guardrails-and-migration-matrix/51-VALIDATION.md` matches.
    - `rg -n "uv run pytest tests/architecture -q" .planning/phases/51-canonical-graph-baseline-guardrails-and-migration-matrix/51-VALIDATION.md` matches.
    - `rg -n "git diff --exit-code -- src/agent/graph.py src/agent/routing.py src/agent/graph_vocabulary.py" .planning/phases/51-canonical-graph-baseline-guardrails-and-migration-matrix/51-VALIDATION.md` matches.
    - `rg -n "protected runtime graph files no diff|protected runtime graph files" .planning/phases/51-canonical-graph-baseline-guardrails-and-migration-matrix/51-VALIDATION.md` matches.
    - `rg -n "Phase 58|legacy/canonical|mixed|混合" .planning/phases/51-canonical-graph-baseline-guardrails-and-migration-matrix/51-VALIDATION.md` matches.
    - All automated commands exit 0.
  </acceptance_criteria>
  <done>Validation artifact records green guardrail checks, protected source no-diff, and Phase 58-scoped final no-debt enforcement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test evidence -> planning ledger | Architecture tests and source facts are summarized in `.planning/ARCHITECTURE-DEBT.md`. |
| validation commands -> phase closeout | Local command results are recorded as the evidence for CAGM-02 completion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-07 | Repudiation | `.planning/ARCHITECTURE-DEBT.md` | mitigate | Require evidence pointers and explicit wording that runtime migration remains incomplete after Phase 51. |
| T-51-08 | Tampering | `51-VALIDATION.md` closeout | mitigate | Record exact approved commands and protected no-runtime-diff command before marking validation rows green. |
| T-51-09 | Information Disclosure | validation and ledger docs | accept | Docs reference source/test file paths and command summaries only; no secrets or live user data are involved. |
</threat_model>

<verification>
Run after this plan:

```bash
uv run pytest tests/architecture/test_canonical_graph_baseline.py -q
uv run pytest tests/architecture -q
git diff --check
git diff --exit-code -- src/agent/graph.py src/agent/routing.py src/agent/graph_vocabulary.py
```
</verification>

<success_criteria>
- `.planning/ARCHITECTURE-DEBT.md` records Phase 51 guardrails and explicitly preserves final runtime migration/no-debt cleanup for Phase 58.
- `51-VALIDATION.md` records the focused and architecture test commands with approved `uv run pytest ...` entrypoints.
- `51-VALIDATION.md` states the current runtime graph remains legacy/canonical mixed.
- Protected runtime graph files have no diff.
- No task changes `src/agent/graph.py`, `src/agent/routing.py`, or `src/agent/graph_vocabulary.py`.
</success_criteria>

<output>
After completion, create `.planning/phases/51-canonical-graph-baseline-guardrails-and-migration-matrix/51-03-SUMMARY.md`.
</output>
