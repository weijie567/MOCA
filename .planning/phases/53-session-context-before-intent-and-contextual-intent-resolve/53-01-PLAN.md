---
phase: 53-session-context-before-intent-and-contextual-intent-resolve
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent/nodes/contextual_intent_resolve.py
  - src/agent/nodes/classify_intent.py
  - src/agent/routing.py
  - tests/agent/test_nodes/test_contextual_intent_resolve.py
  - tests/agent/test_nodes/test_classify_intent.py
  - tests/test_graph_routing.py
  - .planning/phases/53-session-context-before-intent-and-contextual-intent-resolve/53-01-SUMMARY.md
autonomous: true
requirements:
  - CAGM-04
user_setup: []
must_haves:
  truths:
    - "contextual_intent_resolve is the canonical active intent node contract and owns canonical trace and llm_outputs data."
    - "Structured LLM output is candidate-only and cannot write route, slot-completion, memory, evidence, approval, action, tool, or final-response authority fields."
    - "route_after_contextual_intent exists as a tested non-active helper that deterministically routes slot-required paths to extract_slots as the Phase 54 compatibility destination."
    - "classification_trace.pre_route_decision is absent from canonical contextual intent traces."
  artifacts:
    - path: src/agent/nodes/contextual_intent_resolve.py
      provides: "canonical intent node and explicit AgentState adapter"
      contains: "async def contextual_intent_resolve"
    - path: src/agent/routing.py
      provides: "non-active route_after_contextual_intent deterministic router helper"
      contains: "route_after_contextual_intent"
    - path: tests/agent/test_nodes/test_contextual_intent_resolve.py
      provides: "canonical intent node unit coverage"
      contains: "contextual_intent_resolve"
  key_links:
    - from: src/agent/nodes/contextual_intent_resolve.py
      to: src/agent/routing.py
      via: "deterministic route_after_contextual_intent call"
      pattern: "route_after_contextual_intent"
    - from: tests/agent/test_nodes/test_contextual_intent_resolve.py
      to: src/agent/nodes/contextual_intent_resolve.py
      via: "direct async node invocation with fake LLM"
      pattern: "contextual_intent_resolve"
---

<objective>
Create the canonical `contextual_intent_resolve` node contract and deterministic router boundary before graph cutover.

Purpose: Phase 53 needs canonical node behavior and a tested contextual router helper before `src/agent/graph.py` can safely stop registering `classify_intent` and `session_memory_load`.
Output: A canonical intent node module, candidate-only AgentState adapter behavior, a non-active `route_after_contextual_intent` helper, and focused unit/router tests. Active `route_after_safety`, `route_after_intent`, and intent-policy route-value cutover is deliberately deferred to Plan 53-02 so graph path maps change atomically with active route values.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/53-session-context-before-intent-and-contextual-intent-resolve/53-CONTEXT.md
@.planning/phases/53-session-context-before-intent-and-contextual-intent-resolve/53-RESEARCH.md
@.planning/phases/53-session-context-before-intent-and-contextual-intent-resolve/53-VALIDATION.md
@.planning/phases/53-session-context-before-intent-and-contextual-intent-resolve/53-PATTERNS.md
@.planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
@.planning/phases/52-safety-pre-route-node/52-VERIFICATION.md
@src/agent/nodes/classify_intent.py
@src/agent/routing.py
@tests/agent/test_nodes/test_classify_intent.py
@tests/test_graph_routing.py

<interfaces>
Existing router wrapper pattern from `src/agent/routing.py`:
```python
def route_after_safety(state: AgentState) -> str:
    try:
        route = _route_after_safety(state)
    except Exception:
        return "clarification_gate"
    return route if route in SAFETY_ROUTES else "clarification_gate"
```

Atomicity boundary for Plan 53-01:
```text
Do not change active `route_after_safety`, active `route_after_intent`, or
`IntentDefinition.initial_route` values in this plan. Those active route-value
changes occur in Plan 53-02 together with `src/agent/graph.py` path-map changes.
```

Current explicit adapter pattern in `src/agent/nodes/classify_intent.py`:
```python
update = {
    "primary_intent": primary_intent,
    "requested_operation": requested_operation,
    "intent_confidence": result.confidence,
    "risk_tier": risk_decision.tier,
    "task_plan": task_plan_state,
    "deferred_steps": deferred_step_payloads,
    "secondary_intents": [str(intent) for intent in result.secondary_intents],
    "required_slots": policy_required_slots,
    "candidate_slots": dict(result.candidate_slots),
    "routing_hints": routing_hints,
    "current_intent": primary_intent,
    "last_intent": primary_intent,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock canonical contextual intent and router behavior</name>
  <files>tests/agent/test_nodes/test_contextual_intent_resolve.py, tests/agent/test_nodes/test_classify_intent.py, tests/test_graph_routing.py</files>
  <read_first>
    - tests/agent/test_nodes/test_classify_intent.py
    - tests/test_graph_routing.py
    - .planning/phases/53-session-context-before-intent-and-contextual-intent-resolve/53-CONTEXT.md
  </read_first>
  <behavior>
    - Test canonical success: fake structured LLM result for `refund_troubleshooting` writes `primary_intent`, `requested_operation`, `required_slots`, `candidate_slots`, task-plan fields, and `llm_outputs["contextual_intent_resolve"]`; `trace_steps[-1]["node"] == "contextual_intent_resolve"`; `classification_trace["route_decision"] == "extract_slots"`; `classification_trace` has no `pre_route_decision` key.
    - Test candidate-only authority: canonical node output does not contain `extracted_slots`, `active_slots`, `tool_results`, `approval_result`, `trusted_approval_result`, `risk_signals`, `proposed_action`, `action_result`, `final_response`, `resume`, or `command`.
    - Test deterministic pending-slot short reply: with `active_flow_state.kind == "pending_required_slot"` and user text `OD-12345`, the node does not call the LLM, resolves `workflow_state_resolution == "answered_pending_required_slot"`, routes to `extract_slots`, and writes no long-term/case memory, business fact, RAG, approval, action, or tool fields per D-13.
    - Test failure path: invalid structured output fails closed to `unsupported`, routes to `clarification_gate`, writes node error with `node == "contextual_intent_resolve"`, and still writes canonical `llm_outputs["contextual_intent_resolve"]`.
    - Router tests: `route_after_contextual_intent` exists as a non-active helper and returns only `clarification_gate`, `final_response`, `investigate`, or `extract_slots`; slot-required intents return `extract_slots`; direct-response intents return `final_response`; low confidence or ordinary-chat `approval_decision` returns `clarification_gate` per D-15, D-16, D-17.
    - Router tests assert 53-01 does not change active `route_after_safety` safe continuation or `route_after_intent` active compatibility behavior; active route-value/path-map cutover belongs to 53-02.
    - Legacy classifier compatibility tests are either updated to assert delegation/import compatibility to the canonical implementation, or explicitly retired so they no longer require `route_decision == "session_memory_load"` or `classification_trace["pre_route_decision"]`.
  </behavior>
  <action>
    Write the RED tests first. In `tests/agent/test_nodes/test_contextual_intent_resolve.py`, copy only the useful fixture shape from `test_classify_intent.py` and change expected active names to `contextual_intent_resolve`; do not assert legacy `intent_classification` as the primary owner. In `tests/agent/test_nodes/test_classify_intent.py`, update or retire the legacy classifier expectations so the file no longer asserts `route_decision == "session_memory_load"` or `classification_trace["pre_route_decision"]`; if `classify_intent.py` remains importable, its tests must state that it is compatibility-only and must assert canonical ownership or delegation rather than active runtime ownership. In `tests/test_graph_routing.py`, add route-totality tests for the new non-active `route_after_contextual_intent` helper, but keep active `route_after_safety` and active `route_after_intent` expectations unchanged until 53-02. The combined tests must cover router helper behavior, same-thread short-reply behavior, no downstream memory/tool/action authority from intent, no legacy classifier regression risk, and no `classification_trace.pre_route_decision` duplication per D-21. Do not change `src/agent/intent_policy.py`, `IntentRouteLiteral`, `IntentDefinition.initial_route`, active `SAFETY_ROUTES`, or active `_route_after_safety` return values in this plan.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_contextual_intent_resolve.py tests/agent/test_nodes/test_classify_intent.py tests/test_graph_routing.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `tests/agent/test_nodes/test_contextual_intent_resolve.py` exists and imports `src.agent.nodes.contextual_intent_resolve`.
    - `tests/agent/test_nodes/test_classify_intent.py` does not assert `route_decision == "session_memory_load"` or `classification_trace["pre_route_decision"]` as active classifier behavior after Phase 53.
    - The RED tests fail before implementation because `contextual_intent_resolve` or `route_after_contextual_intent` does not exist.
    - Test assertions contain the exact canonical strings `contextual_intent_resolve`, `route_after_contextual_intent`, and `extract_slots`.
    - Plan 53-01 tests do not require `route_after_safety` to return `session_context_load`; that active route-value change is tested in Plan 53-02 with graph path-map changes.
  </acceptance_criteria>
  <done>Canonical node/router expected behavior is test-locked before production code changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement canonical node adapter and deterministic route contract</name>
  <files>src/agent/nodes/contextual_intent_resolve.py, src/agent/nodes/classify_intent.py, src/agent/routing.py, tests/agent/test_nodes/test_contextual_intent_resolve.py, tests/agent/test_nodes/test_classify_intent.py, tests/test_graph_routing.py</files>
  <read_first>
    - src/agent/nodes/classify_intent.py
    - src/agent/routing.py
    - .planning/phases/53-session-context-before-intent-and-contextual-intent-resolve/53-PATTERNS.md
  </read_first>
  <action>
    Create `src/agent/nodes/contextual_intent_resolve.py` with public `async def contextual_intent_resolve(state: AgentState) -> dict` and an explicit adapter that writes only validated intent fields, `required_slots`, `candidate_slots`, `routing_hints`, task-plan/deferred-step fields, `classification_trace`, canonical `llm_outputs["contextual_intent_resolve"]`, trace steps, eval metadata, and node errors per D-06, D-07, and D-08. The failure payload under `llm_outputs["contextual_intent_resolve"]` must be an allowlisted shape with `status`, `fallback_intent`, `reason_codes`, and `error_type`; do not store raw invalid model blobs unless explicitly redacted and test-covered. Reuse deterministic helpers from the current classifier by moving only stateless helper code needed for canonical ownership into the new canonical module, then keep `src/agent/nodes/classify_intent.py` as import/test compatibility only: it may delegate to the canonical implementation or re-export adapter helpers, but the active canonical implementation must not require `classify_intent` as a graph node. Update `tests/agent/test_nodes/test_classify_intent.py` in the same task so legacy compatibility tests match the retained wrapper/import contract and no old active-route/pre-route assertions remain. Remove `classification_trace["pre_route_decision"]` from all canonical contextual traces per D-09; keep any safety disposition only in `routing_hints` and reason codes. Add `route_after_contextual_intent(state)` with allowlist `{"clarification_gate", "final_response", "investigate", "extract_slots"}` and fail-closed wrapper behavior, but do not wire it into the active graph and do not change active `route_after_safety`, active `route_after_intent`, `SAFETY_ROUTES`, `INTENT_ROUTES`, `IntentRouteLiteral`, or `IntentDefinition.initial_route` values in this plan. If `route_after_intent` is touched for import compatibility, it must continue preserving current active graph behavior until Plan 53-02; it must not delegate to `route_after_contextual_intent` before the graph path-map cutover. Do not add `slot_resolution_gate`, `memory_context_load`, long-term/case memory loading, evidence verification, risk lowering, action drafting, tool calls, or slot satisfaction in this plan per D-07, D-16, and the out-of-scope list.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_nodes/test_contextual_intent_resolve.py tests/agent/test_nodes/test_classify_intent.py tests/test_graph_routing.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/nodes/contextual_intent_resolve.py src/agent/nodes/classify_intent.py src/agent/routing.py tests/agent/test_nodes/test_contextual_intent_resolve.py tests/agent/test_nodes/test_classify_intent.py tests/test_graph_routing.py</automated>
    <automated>bash -lc "! rg -n 'pre_route_decision' src/agent/nodes/contextual_intent_resolve.py"</automated>
    <automated>bash -lc "! rg -n 'route_decision.*session_memory_load|pre_route_decision' tests/agent/test_nodes/test_classify_intent.py"</automated>
    <automated>bash -lc "! rg -n 'return \"session_context_load\"|SAFETY_ROUTES = \\{\"session_context_load\"|IntentRouteLiteral = Literal\\[\"investigate\", \"extract_slots\"|initial_route=\"extract_slots\"' src/agent/routing.py src/agent/intent_policy.py"</automated>
  </verify>
  <acceptance_criteria>
    - `src/agent/nodes/contextual_intent_resolve.py` contains `async def contextual_intent_resolve` and no `pre_route_decision` string.
    - `src/agent/routing.py` contains `def route_after_contextual_intent` as a non-active helper; active `route_after_safety` safe continuation remains unchanged until Plan 53-02.
    - Plan 53-01 does not change active `route_after_safety`, active `route_after_intent`, `SAFETY_ROUTES`, `INTENT_ROUTES`, `IntentRouteLiteral`, or `IntentDefinition.initial_route` route values.
    - `tests/agent/test_nodes/test_classify_intent.py` is updated, retired, or narrowed to compatibility-only assertions and is included in the Phase 53 focused test command.
    - `llm_outputs["contextual_intent_resolve"]` is the canonical owner for success, deterministic short-reply, and failure paths, and failure payloads use only allowlisted diagnostic fields.
    - The focused tests and Ruff command pass through the approved `UV_CACHE_DIR=/tmp/uv-cache uv run ...` entrypoint.
  </acceptance_criteria>
  <done>Canonical intent node and non-active contextual router helper exist and are tested, while active route/policy values remain compatible with the current graph until Plan 53-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user text -> contextual_intent_resolve LLM adapter | Untrusted ordinary chat text and structured model output cross into AgentState. |
| contextual_intent_resolve -> route_after_contextual_intent | Candidate intent fields cross into deterministic router decision logic. |
| Plan 53-01 -> Plan 53-02 active cutover | Plan 53-01 prepares canonical helper surfaces without changing active graph route values; Plan 53-02 performs the atomic active graph/router/policy cutover. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01-01 | Tampering / Elevation of Privilege | `contextual_intent_resolve` adapter | mitigate | Keep explicit allowlisted AgentState update keys and test absence of `active_slots`, `extracted_slots`, `tool_results`, `approval_result`, `risk_signals`, `proposed_action`, and `final_response` per D-07/D-08. |
| T-53-01-02 | Spoofing / Elevation of Privilege | `route_after_contextual_intent` | mitigate | Treat ordinary-chat `requested_operation == "approval_decision"` as invalid and route to `clarification_gate`; do not let LLM output choose graph routes per D-17. |
| T-53-01-03 | Reliability / Route Drift | `src/agent/routing.py`, `src/agent/intent_policy.py` | mitigate | Do not change active safety/intent route values in Plan 53-01; defer active route/policy cutover to Plan 53-02 where graph path maps change in the same task. |
| T-53-01-04 | Repudiation | `classification_trace` | mitigate | Remove canonical `classification_trace.pre_route_decision`; Phase 52 `safety_pre_route` remains the runtime pre-route owner per D-09. |
| T-53-01-05 | Information Disclosure | pending-slot short replies | mitigate | Test deterministic short replies use same-thread state only and do not call long-term/case memory, business fact, RAG, approval, action, or tools per D-13. |
| T-53-01-06 | Scope Creep | `slot_resolution_gate` / `memory_context_load` | mitigate | Route slot-required paths to `extract_slots` only and do not create active `slot_resolution_gate` or `memory_context_load` in this plan per D-16 and Phase 54/55 boundaries. |
</threat_model>

<verification>
Plan 53-01 is complete when the canonical node/router focused suite passes, Ruff passes, `src/agent/nodes/contextual_intent_resolve.py` has no `pre_route_decision` string, legacy classifier tests no longer enforce old active classifier ownership, and active router/policy route values remain compatible with the current graph until Plan 53-02.
</verification>

<success_criteria>
- `contextual_intent_resolve` exists as a tested canonical node contract.
- `route_after_contextual_intent` exists as a non-active helper and deterministically returns only `clarification_gate`, `final_response`, `investigate`, or `extract_slots`.
- Canonical contextual intent traces and `llm_outputs` use `contextual_intent_resolve` as owner.
- LLM structured output remains candidate-only with no forbidden authority fields.
- Active router/policy route-value cutover remains deferred to Plan 53-02 for atomic graph path-map consistency.
</success_criteria>

<output>
After completion, create `.planning/phases/53-session-context-before-intent-and-contextual-intent-resolve/53-01-SUMMARY.md`. The summary must explicitly state that after 53-01 the active graph still uses legacy route values compatible with the pre-53-02 graph, and that `route_after_contextual_intent` remains non-active until the 53-02 atomic router/policy/graph cutover.
</output>
