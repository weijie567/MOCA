---
phase: 53-session-context-before-intent-and-contextual-intent-resolve
plan: "03"
type: execute
wave: 3
depends_on:
  - "53-01"
  - "53-02"
files_modified:
  - src/agent/graph_vocabulary.py
  - src/api/routers/agent_runs.py
  - tests/agent/test_graph_vocabulary.py
  - tests/agent/test_trace.py
  - docs/current-langgraph-architecture.md
  - .planning/ARCHITECTURE-DEBT.md
  - .planning/phases/53-session-context-before-intent-and-contextual-intent-resolve/53-VALIDATION.md
  - .planning/phases/53-session-context-before-intent-and-contextual-intent-resolve/53-03-SUMMARY.md
autonomous: true
requirements:
  - CAGM-04
user_setup: []
must_haves:
  truths:
    - "Graph vocabulary marks contextual_intent_resolve and route_after_contextual_intent as runtime."
    - "Retained classify_intent, intent_classification, session_memory_load, and route_after_intent surfaces are compatibility aliases only, with ledgered owner/reason/validation/delete phase."
    - "Current architecture docs describe the verified Phase 53 graph order and distinguish target contract from current source facts."
    - ".planning/ARCHITECTURE-DEBT.md closes Phase 52 active classify_intent compatibility and records any retained helper/module/output mirrors."
    - "Final validation proves no active classify_intent or session_memory_load graph node/route destination remains, while extract_slots remains explicitly Phase 54-owned."
  artifacts:
    - path: src/agent/graph_vocabulary.py
      provides: "runtime/compat graph vocabulary projection"
      contains: "route_after_contextual_intent"
    - path: src/api/routers/agent_runs.py
      provides: "SSE node label coverage for canonical runtime nodes"
      contains: "contextual_intent_resolve"
    - path: docs/current-langgraph-architecture.md
      provides: "current source architecture snapshot after Phase 53"
      contains: "session_context_load"
    - path: .planning/ARCHITECTURE-DEBT.md
      provides: "Chinese architecture debt ledger for Phase 53 facts and remaining compatibility"
      contains: "Phase 53"
    - path: .planning/phases/53-session-context-before-intent-and-contextual-intent-resolve/53-VALIDATION.md
      provides: "closed validation evidence"
      contains: "nyquist_compliant: true"
  key_links:
    - from: src/agent/graph_vocabulary.py
      to: tests/agent/test_graph_vocabulary.py
      via: "runtime/compat projection assertions"
      pattern: "contextual_intent_resolve"
    - from: docs/current-langgraph-architecture.md
      to: src/agent/graph.py
      via: "source-fact graph order and node set"
      pattern: "session_context_load.*contextual_intent_resolve"
    - from: .planning/ARCHITECTURE-DEBT.md
      to: .planning/phases/53-session-context-before-intent-and-contextual-intent-resolve/53-VALIDATION.md
      via: "shared compatibility rows and command evidence"
      pattern: "classify_intent"
---

<objective>
Synchronize graph vocabulary, streaming labels, documentation, debt ledger, and validation evidence with the Phase 53 runtime cutover.

Purpose: The code cutover is not complete unless trace/API vocabulary and planning ledgers clearly distinguish runtime canonical names from retained compatibility aliases.
Output: Runtime vocabulary projection, canonical node labels, current architecture snapshot, architecture-debt ledger update, and completed Phase 53 validation artifact.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/53-session-context-before-intent-and-contextual-intent-resolve/53-CONTEXT.md
@.planning/phases/53-session-context-before-intent-and-contextual-intent-resolve/53-RESEARCH.md
@.planning/phases/53-session-context-before-intent-and-contextual-intent-resolve/53-VALIDATION.md
@.planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
@.planning/phases/52-safety-pre-route-node/52-VERIFICATION.md
@src/agent/graph_vocabulary.py
@src/api/routers/agent_runs.py
@tests/agent/test_graph_vocabulary.py
@tests/agent/test_trace.py
@docs/current-langgraph-architecture.md
@.planning/ARCHITECTURE-DEBT.md

<interfaces>
Graph vocabulary entry shape from `src/agent/graph_vocabulary.py`:
```python
@dataclass(frozen=True)
class GraphVocabularyEntry:
    legacy_name: str
    target_name: str
    kind: TargetGraphKind
    status: TargetGraphStatus
    runnable: bool
    reason_codes: tuple[str, ...] = ()
```

SSE label map from `src/api/routers/agent_runs.py`:
```python
NODE_MESSAGES: dict[str, str] = {
    "receive_request": "正在接收请求",
    "classify_intent": "正在识别意图",
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote canonical vocabulary and runtime labels</name>
  <files>src/agent/graph_vocabulary.py, src/api/routers/agent_runs.py, tests/agent/test_graph_vocabulary.py, tests/agent/test_trace.py</files>
  <read_first>
    - src/agent/graph_vocabulary.py
    - tests/agent/test_graph_vocabulary.py
    - tests/agent/test_trace.py
    - src/api/routers/agent_runs.py
    - .planning/phases/53-session-context-before-intent-and-contextual-intent-resolve/53-CONTEXT.md
  </read_first>
  <behavior>
    - Vocabulary tests assert `contextual_intent_resolve` node and `route_after_contextual_intent` router are `status == "runtime"` and `runnable is True`.
    - Vocabulary tests assert `classify_intent`, `intent_classification`, `session_memory_load`, and `route_after_intent` are not runtime; they remain compatibility aliases only when retained with reason codes and ledgered delete phase.
    - Vocabulary tests assert `extract_slots` remains `compatibility_alias` to `slot_resolution_gate` with Phase 54 reason code and is not treated as a Phase 53 failure.
    - Trace projection tests assert current `contextual_intent_resolve` trace steps project to target node `contextual_intent_resolve` with runtime status.
    - API label map contains exact keys `session_context_load` and `contextual_intent_resolve`; legacy `classify_intent` label may remain only for historical trace display and must not be described as active runtime.
  </behavior>
  <action>
    Update `src/agent/graph_vocabulary.py` per D-06, D-10, D-18, D-19, and D-20. Change the identity entries for `contextual_intent_resolve` and `route_after_contextual_intent` from `compatibility_alias` to `runtime`. Keep `classify_intent`, `intent_classification`, `session_memory_load`, and `route_after_intent` as compatibility aliases only if still needed for import/test/historical trace compatibility; add reason codes that name `PHASE_53_COMPATIBILITY_ALIAS` or a more specific code, and ensure closeout docs ledger the retained surface with owner, reason, validation, trace projection, and delete phase. Keep `extract_slots` as a compatibility alias to `slot_resolution_gate` with its existing Phase 54-oriented reason; do not promote `slot_resolution_gate` to runtime in this plan. Update `src/api/routers/agent_runs.py` `NODE_MESSAGES` with `session_context_load` and `contextual_intent_resolve` labels. Update vocabulary/trace tests to prove canonical runtime projection and retained compatibility semantics.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/agent/test_graph_vocabulary.py tests/agent/test_trace.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent/graph_vocabulary.py src/api/routers/agent_runs.py tests/agent/test_graph_vocabulary.py tests/agent/test_trace.py</automated>
  </verify>
  <acceptance_criteria>
    - `graph_vocabulary_entry("contextual_intent_resolve", kind="node").status == "runtime"` is asserted in tests.
    - `graph_vocabulary_entry("route_after_contextual_intent", kind="router").status == "runtime"` is asserted in tests.
    - `graph_vocabulary_entry("classify_intent", kind="node").status != "runtime"` and `graph_vocabulary_entry("session_memory_load", kind="node").status != "runtime"` are asserted in tests.
    - `src/api/routers/agent_runs.py` contains exact keys `"session_context_load"` and `"contextual_intent_resolve"` in `NODE_MESSAGES`.
  </acceptance_criteria>
  <done>Trace/API vocabulary projects Phase 53 canonical runtime names and treats retained legacy names only as compatibility aliases.</done>
</task>

<task type="auto">
  <name>Task 2: Close docs, debt ledger, validation, and artifact scans</name>
  <files>docs/current-langgraph-architecture.md, .planning/ARCHITECTURE-DEBT.md, .planning/phases/53-session-context-before-intent-and-contextual-intent-resolve/53-VALIDATION.md</files>
  <read_first>
    - docs/current-langgraph-architecture.md
    - .planning/ARCHITECTURE-DEBT.md
    - .planning/phases/53-session-context-before-intent-and-contextual-intent-resolve/53-VALIDATION.md
    - .planning/phases/50-canonical-agent-graph-migration-spec-and-guardrails/50-SPEC.md
    - .planning/phases/52-safety-pre-route-node/52-VERIFICATION.md
  </read_first>
  <action>
    Update `docs/current-langgraph-architecture.md` as a current-source snapshot, not a target-contract document. Replace the Phase 52 path with the verified Phase 53 path `receive_request -> safety_pre_route -> session_context_load -> contextual_intent_resolve`; list the active registered graph nodes after Phase 53 with `session_context_load` and `contextual_intent_resolve`, without `classify_intent` or `session_memory_load`; keep `extract_slots`, `long_term_memory_retrieve`, `generate_recommendation`, and `assess_risk_and_approval` as later-phase migration names with their owner phases. State explicitly that historical traces containing `classify_intent` or `session_memory_load` remain readable through vocabulary projection, but those names are not active graph registration or active route authority after Phase 53. Replace the Phase 52 compatibility table with Phase 53 rows: closed rows for safe-route continuation to `classify_intent` and classifier-owned `classification_trace.pre_route_decision`; retained rows only for helper/module/import/output mirrors that remain after scans, each with legacy surface, canonical owner, reason, trace projection, validation, and delete phase per Phase 50 compatibility policy and D-10/D-19/D-20. Specifically scan `intent_classification` / `llm_outputs["intent_classification"]` readers; if a non-test or historical-reader compatibility mirror remains, ledger it with canonical owner `contextual_intent_resolve`, validation evidence, and delete phase no later than Phase 58. Append a Chinese Phase 53 entry to `.planning/ARCHITECTURE-DEBT.md` with verified facts, handling status, evidence files, and remaining risks; distinguish target contract from current source facts. Update `53-VALIDATION.md` according to its existing frontmatter/schema only after commands pass: set `nyquist_compliant: true`, `wave_0_complete: true`, and status fields to complete when those fields exist, or record equivalent completed status if the schema changes; record exact approved-entrypoint command evidence and artifact scan results for graph order, router maps, vocabulary projection, same-thread short replies, no downstream authority from intent, legacy classifier compatibility test coverage, and no pre-route trace duplication per D-21. Do not edit `docs/contract-spec.md` unless source/doc comparison finds a real contract error; if no edit is needed, record in the summary that §9 already contains the Phase 53 target semantics.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/architecture tests/agent/test_graph.py tests/test_graph_routing.py tests/agent/test_intent_routing.py tests/agent/test_graph_vocabulary.py tests/agent/test_session_memory_load.py tests/agent/test_session_memory_integration.py tests/agent/test_nodes/test_safety_pre_route.py tests/agent/test_nodes/test_contextual_intent_resolve.py tests/agent/test_nodes/test_classify_intent.py -q --tb=short</automated>
    <automated>UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/agent tests/agent tests/architecture</automated>
    <automated>bash -lc "! rg -n 'add_node\(\"classify_intent\"|add_node\(\"session_memory_load\"|\"classify_intent\": \"classify_intent\"|\"session_memory_load\": \"session_memory_load\"' src/agent/graph.py tests/architecture/graph_baseline.py"</automated>
    <automated>bash -lc "! rg -n 'classification_trace.*pre_route_decision|pre_route_decision\": pre_route|pre_route_decision\": pre_route\.model_dump' src/agent/nodes/contextual_intent_resolve.py"</automated>
    <automated>bash -lc "rg -n '\"session_memory_load\"|route_after_intent|classify_intent|intent_classification' src/agent/graph.py src/agent/routing.py src/agent/intent_policy.py src/agent/nodes src/api tests/architecture/graph_baseline.py tests/agent || true"</automated>
    <automated>bash -lc "! rg -n '(<''automated>[[:space:]]*(pytest|python -m pytest)([[:space:]]|$)|^[[:space:]]*(pytest|python -m pytest)([[:space:]]|$))' .planning/phases/53-session-context-before-intent-and-contextual-intent-resolve/*.md"</automated>
    <automated>git diff --check -- src/agent/graph_vocabulary.py src/api/routers/agent_runs.py tests/agent/test_graph_vocabulary.py tests/agent/test_trace.py docs/current-langgraph-architecture.md .planning/ARCHITECTURE-DEBT.md .planning/phases/53-session-context-before-intent-and-contextual-intent-resolve/53-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/current-langgraph-architecture.md` contains `receive_request -> safety_pre_route -> session_context_load -> contextual_intent_resolve` and no longer describes `safety_pre_route -> classify_intent` as the current active path.
    - `.planning/ARCHITECTURE-DEBT.md` has a Phase 53 ledger entry that marks active `classify_intent` and active `session_memory_load` graph compatibility closed, and names any retained helper/module/output mirror delete phase.
    - `53-VALIDATION.md` frontmatter has `nyquist_compliant: true` and `wave_0_complete: true` only after the listed tests, Ruff command, artifact scans, and diff check pass.
    - The first artifact scan returns no active graph/baseline hits for `classify_intent` or `session_memory_load`.
    - The second artifact scan returns no `pre_route_decision` duplicate ownership hits in `src/agent/nodes/contextual_intent_resolve.py`.
    - The third artifact scan is reviewed against the compatibility ledger; allowed hits are limited to non-active wrapper/import/test/historical compatibility and must not include active graph registration, active route destination, or active policy route values.
    - Any retained `intent_classification` or `llm_outputs["intent_classification"]` hit is either removed, test-only, or ledgered as a non-authoritative compatibility mirror with canonical owner `contextual_intent_resolve` and delete phase no later than Phase 58.
    - The bare-command scan returns no matches for bare `pytest` or bare `python -m pytest` commands in Phase 53 planning artifacts.
  </acceptance_criteria>
  <done>Phase 53 vocabulary, docs, architecture debt, validation, and artifact scans are synchronized with the verified runtime cutover.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime traces -> graph vocabulary/API labels | Current run node names are projected for API, replay, and operator-facing status. |
| source facts -> docs/debt ledger | Verified implementation facts become planning and maintenance records. |
| retained compatibility aliases -> future migration work | Legacy names remain readable without becoming active runtime authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-03-01 | Repudiation | `graph_vocabulary.py`, trace projection | mitigate | Mark canonical Phase 53 runtime names as runtime and legacy names as compatibility only; test trace projection for `contextual_intent_resolve`. |
| T-53-03-02 | Information Disclosure / Audit Confusion | docs and architecture debt | mitigate | Update current architecture docs from source facts and ledger retained aliases with owner/reason/validation/delete phase so historical names do not imply active authority. |
| T-53-03-03 | Reliability / Route Drift | artifact scans | mitigate | Run scans for active `classify_intent` / `session_memory_load` registrations and destinations; block if active hits remain. |
| T-53-03-04 | Repudiation | `classification_trace.pre_route_decision` | mitigate | Scan canonical contextual intent source for duplicate pre-route trace ownership and require no hits. |
| T-53-03-05 | Scope Creep | Phase 54/55/58 aliases | mitigate | Keep `extract_slots`, `long_term_memory_retrieve`, `generate_recommendation`, and `assess_risk_and_approval` as owner-phase compatibility rows; do not close Phase 54/55/56/57/58 debt here. |
| T-53-03-06 | Validation Integrity | Phase 53 artifacts | mitigate | Record only approved `UV_CACHE_DIR=/tmp/uv-cache uv run ...` commands and scan Phase 53 artifacts for bare command entries. |
</threat_model>

<verification>
Plan 53-03 is complete when graph vocabulary and trace tests pass, full focused Phase 53 tests pass, Ruff passes, artifact scans are reviewed, current architecture docs and architecture debt ledger reflect verified Phase 53 facts, and `53-VALIDATION.md` is closed with approved command evidence.
</verification>

<success_criteria>
- `contextual_intent_resolve` and `route_after_contextual_intent` are runtime vocabulary entries.
- API/SSE node labels cover `session_context_load` and `contextual_intent_resolve`.
- Current architecture docs and architecture debt ledger record Phase 53 active graph facts and retained compatibility surfaces.
- Validation artifact records green focused tests, Ruff, artifact scans, and no bare test commands.
- Phase 54 `extract_slots` compatibility remains explicitly named and not removed in Phase 53.
</success_criteria>

<output>
After completion, create `.planning/phases/53-session-context-before-intent-and-contextual-intent-resolve/53-03-SUMMARY.md`.
</output>
