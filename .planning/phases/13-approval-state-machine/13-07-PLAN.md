---
phase: 13-approval-state-machine
plan: 07
type: execute
wave: 7
depends_on:
  - 13-01
  - 13-02
  - 13-03
  - 13-04
  - 13-05
  - 13-06
files_modified:
  - src/repositories/approval_repo.py
  - tests/architecture/test_approval_boundaries.py
  - tests/test_approval_models.py
  - tests/test_approval_api.py
  - tests/test_approval_integration.py
  - tests/test_execute_action.py
  - tests/test_graph_routing.py
autonomous: true
requirements:
  - APPROVAL-01
  - APPROVAL-02
  - APPROVAL-03
  - SNAPSHOT-01
must_haves:
  truths:
    - "No API router or agent run router imports the legacy approval repository transition path."
    - "No graph node imports raw external/action/business adapters for approval decisions."
    - "Legacy ApprovalRepository.decide is deleted or made package-private compatibility only, with no new imports."
    - "Old v1 tests are rewritten around ApprovalService and exact hash/snapshot/revision semantics."
  artifacts:
    - path: tests/architecture/test_approval_boundaries.py
      provides: "Static import and boundary tests for approval ownership"
      contains: "src.repositories.approval_repo"
---

<objective>
Quarantine or delete obsolete v1 approval paths and rewrite legacy tests.

Purpose: ensure execution cannot silently preserve old transition truth after the new ApprovalService, schema, event, and resume paths are in place.
Output: static boundary tests, legacy repository disposition, and old test rewrites.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-approval-state-machine/13-CONTEXT.md
@.planning/phases/13-approval-state-machine/13-RESEARCH.md
@.planning/phases/13-approval-state-machine/13-VALIDATION.md
@.planning/phases/13-approval-state-machine/13-PATTERNS.md
@docs/phase-13-17-architecture-plan.md
@docs/agent-architecture-phase-decomposition.md
@tests/architecture/test_tool_boundaries.py
@src/repositories/approval_repo.py
@tests/test_approval_models.py
@tests/test_approval_api.py
@tests/test_approval_integration.py
@tests/test_execute_action.py
</context>

<planning_gate>
## Spec Consistency Findings

- Implemented fact before this plan: old tests and repository paths may still encode v1 approve/reject semantics even after target service exists.
- Target contract: ApprovalService and target tables are canonical; compatibility paths are forbidden for new references and named with deletion/quarantine gates.
- MVP scope: test rewrite and quarantine. Phase 14 owns full action draft v2 binding and demo wording; Phase 15 owns replay enrichment and scanner enablement; Phase 17 owns external execution.

## Architecture Alignment

Canonical owner package: `src/approvals`; schema owner: `src/db/models.py` and migration `008_approval_state_machine`.
Old-path strategy: delete `src/repositories/approval_repo.py` when possible, or leave only a display/read shim with no public `decide` or `mark_expired`.
Forbidden imports: routers, agent routers, graph nodes, and rewritten tests cannot import legacy approval transition methods or raw action/business adapters for approval decisions.
Boundary tests: this plan adds `tests/architecture/test_approval_boundaries.py` and rewrites legacy tests so the owner boundary is executable.
Non-overlap: Phase 13 finishes approval ownership and quarantine; Phase 14 owns action draft/executor binding; Phase 15 owns replay enrichment and scanner enablement; Phase 16 owns memory; Phase 17 owns external execution.
This plan enforces the owner boundary from `docs/phase-13-17-architecture-plan.md`: define owner, move code, delete/quarantine old paths, and add boundary tests before declaring Phase 13 ready.

## Migration / Read-Switch / Rollback

Plan 13-08 records final read-switch status after this quarantine is complete. Rollback requires downgrading schema to `007_session_memories` and restoring v1 router/repository behavior only if no v2 approvals were authorized; otherwise v2 rows remain non-executable until resolved.

## Compatibility-Layer Disposition

| Path | Owner | Forbidden New References | Protection Test | Deletion/Gate |
|------|-------|--------------------------|-----------------|---------------|
| `src/repositories/approval_repo.py` | Compatibility shim or deleted | Routers, agent routers, graph nodes, and tests cannot import transition methods | `tests/architecture/test_approval_boundaries.py` | Delete in Phase 13 or leave only read/list shim with explicit exception |
| `ApprovalRepository.decide` and `mark_expired` | Obsolete v1 transition API | No new references anywhere outside optional shim-internal calls | `tests/architecture/test_approval_boundaries.py` | Removed or private in this plan |
| `ApprovalStep` | Trace fallback only | No target transition/event writes | tests verify new approval_events path | Phase 15 replay migration |
| v1 approval tests | Plan 07 | Cannot assert v1 idempotent approve/reject as target truth | rewritten tests | Rewritten in this plan |

## Readiness Hand-Off

Plan 13-08 owns the coverage matrix, eval manifest, cross-table enforcement row, and final gate recording. This plan must leave compatibility disposition observable by tests before 13-08 can mark the phase ready.
</planning_gate>

<threat_model>
| Threat | Severity | Mitigation in this plan | Verification |
|--------|----------|-------------------------|--------------|
| Old repository transition bypasses service | high | AST import boundary test forbids router/agent/test imports of legacy transition path | `tests/architecture/test_approval_boundaries.py` |
| Legacy tests keep obsolete semantics alive | high | rewrite tests around ApprovalService, v2 rows, and exact hash/revision checks | focused approval suites |
| Action path ignores new snapshot/hash refs | high | execute-action tests assert missing/mismatched snapshot/hash fail or remain Phase 14-owned deferred with gate | `tests/test_execute_action.py` |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add approval architecture boundary tests</name>
  <files>tests/architecture/test_approval_boundaries.py</files>
  <read_first>
    - tests/architecture/test_tool_boundaries.py.
    - .planning/phases/13-approval-state-machine/13-PATTERNS.md static boundary section.
    - docs/phase-13-17-architecture-plan.md Non-Negotiable Architecture Tests.
  </read_first>
  <action>
    Create `tests/architecture/test_approval_boundaries.py` using the AST import scanner pattern from `tests/architecture/test_tool_boundaries.py`.
    Add tests:
    - `test_approval_routers_do_not_import_legacy_approval_repository`: scan `src/api/routers/approvals.py`, `src/api/routers/agent.py`, and `src/api/routers/agent_runs.py` for `src.repositories.approval_repo` and fail on any import.
    - `test_approval_transition_methods_are_not_imported_outside_approvals_package`: scan `src` and `tests` for imports of `ApprovalRepository` from `src.repositories.approval_repo`, allowing only the compatibility file itself if it remains.
    - `test_graph_nodes_do_not_import_raw_action_or_business_adapters_for_approval`: scan `src/agent/nodes` for `src.integrations`, `src.business`, raw action adapters, or `src.repositories.approval_repo` imports used by approval decision flow.
    - `test_approval_service_is_canonical_transition_owner`: assert `src/approvals/service.py` exists and contains `class ApprovalService`.
  </action>
  <verify>
    <automated>rg -n "src\\.repositories\\.approval_repo|ApprovalService|test_approval_routers_do_not_import_legacy" tests/architecture/test_approval_boundaries.py</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "src\\.repositories\\.approval_repo|ApprovalService|test_approval_routers_do_not_import_legacy" tests/architecture/test_approval_boundaries.py` returns matches.
    - The automated boundary-test scaffolding grep exits 0 before Task 2/3 quarantine rewrites.
    - Full architecture boundary pytest remains gated to Task 3 after legacy imports are rewritten.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Delete or quarantine legacy approval repository transition methods</name>
  <files>src/repositories/approval_repo.py</files>
  <read_first>
    - src/repositories/approval_repo.py.
    - src/approvals/repository.py.
    - tests/architecture/test_approval_boundaries.py.
    - .planning/phases/13-approval-state-machine/13-CONTEXT.md D-31.
  </read_first>
  <action>
    Prefer deleting `src/repositories/approval_repo.py` if all callers have moved to `src/approvals`.
    If deletion would break trace/list compatibility, leave a shim with:
    - module docstring naming `src/approvals/repository.py` and Phase 13 as owner/removal phase.
    - no public `decide` or `mark_expired` method.
    - no target transition behavior.
    - comments stating new imports are forbidden.
    Ensure all imports in routers/tests use `src.approvals.repository` or `src.approvals.service`, not `src.repositories.approval_repo`.
  </action>
  <verify>
    <automated>uv run python -c 'from pathlib import Path; import re; repo=Path("src/repositories/approval_repo.py"); text=repo.read_text() if repo.exists() else ""; assert "def decide" not in text and "def mark_expired" not in text; bad=[str(p) for p in Path("src").rglob("*.py") if p != repo and re.search(r"from src\\.repositories\\.approval_repo|import src\\.repositories\\.approval_repo", p.read_text())]; assert not bad, bad'</automated>
  </verify>
  <acceptance_criteria>
    - Deletion-aware check passes: `test ! -e src/repositories/approval_repo.py || ! rg -n "def decide|def mark_expired" src/repositories/approval_repo.py`.
    - `rg -n "from src\\.repositories\\.approval_repo|import src\\.repositories\\.approval_repo" src` returns no matches except an allowed shim-internal reference if the file remains.
    - Full architecture boundary pytest remains gated to Task 3 after test imports are rewritten.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Rewrite legacy approval and action tests around v2 service semantics</name>
  <files>tests/test_approval_models.py, tests/test_approval_api.py, tests/test_approval_integration.py, tests/test_execute_action.py, tests/test_graph_routing.py</files>
  <read_first>
    - tests/test_approval_models.py.
    - tests/test_approval_api.py.
    - tests/test_approval_integration.py.
    - tests/test_execute_action.py.
    - tests/test_graph_routing.py.
    - tests/approvals/test_service_transitions.py.
    - tests/approvals/test_hash_binding.py.
    - tests/approvals/test_needs_info_resume.py.
  </read_first>
  <action>
    Rewrite tests that import `src.repositories.approval_repo.ApprovalRepository` to use `ApprovalService`, `ApprovalDecisionCommand`, and v2 factory helpers from the new tests.
    Remove tests that assert v1 idempotent `ApprovalRepository.decide(...)` is target behavior.
    Replace them with tests that assert:
    - idempotent duplicate same decision is allowed only when expected version/revision and hashes match service rules.
    - changed decision after terminal status conflicts.
    - missing hashes or legacy v1 row cannot authorize action.
    - action path receives or requires `action_payload_hash`, `safety_snapshot_ref`, and `safety_snapshot_hash`; if full draft v2 binding remains Phase 14-owned, test documents it as blocked/deferred with owner rather than silently passing.
  </action>
  <verify>
    <automated>uv run pytest tests/architecture/test_approval_boundaries.py tests/test_approval_models.py tests/test_approval_api.py tests/test_approval_integration.py tests/test_execute_action.py tests/test_graph_routing.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "src\\.repositories\\.approval_repo|ApprovalRepository" tests/test_approval_models.py tests/test_approval_api.py tests/test_approval_integration.py tests/test_execute_action.py tests/test_graph_routing.py` returns no matches unless the line is a negative assertion string.
    - `rg -n "ApprovalService|ApprovalDecisionCommand|action_payload_hash|safety_snapshot_hash|legacy_v1" tests/test_approval_models.py tests/test_approval_api.py tests/test_approval_integration.py tests/test_execute_action.py tests/test_graph_routing.py` returns matches.
    - The automated pytest command, including architecture boundary tests, exits 0.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run:
1. `uv run pytest tests/architecture/test_approval_boundaries.py tests/test_approval_models.py tests/test_approval_api.py tests/test_approval_integration.py tests/test_execute_action.py tests/test_graph_routing.py -q --tb=short`
</verification>

<success_criteria>
- Legacy approval transition paths are deleted or quarantined.
- Boundary tests prevent routers/graph/tests from importing the old transition owner.
- Old tests no longer encode v1 approval truth as target behavior.
- Architecture boundary suite passes.
- Focused legacy approval/action rewrite suite passes.
</success_criteria>
