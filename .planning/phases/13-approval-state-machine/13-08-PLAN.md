---
phase: 13-approval-state-machine
plan: 08
type: execute
wave: 8
depends_on:
  - 13-07
files_modified:
  - .planning/phases/13-approval-state-machine/13-COVERAGE.md
  - tests/approvals/phase13_eval_manifest.json
autonomous: true
requirements:
  - APPROVAL-01
  - APPROVAL-02
  - APPROVAL-03
  - SNAPSHOT-01
must_haves:
  truths:
    - "Phase 13 coverage matrix has no relevant MISSING rows."
    - "Phase 13 approval-contract eval metadata names dataset owner, version, hash, blocking status, and failure impact."
    - "Every PARTIAL or DEFERRED_WITH_OWNER row names owner phase, non-blocking rationale, dependency, and acceptance gate."
    - "Spec reconciliation rows record any Phase 13 contract choices that differ from or clarify the current blueprint."
    - "Full focused Phase 13 suite, Alembic upgrade, ruff, and full pytest gate are documented."
  artifacts:
    - path: .planning/phases/13-approval-state-machine/13-COVERAGE.md
      provides: "Final requirement/follow-up/readiness coverage matrix"
      contains: "APPROVAL-01"
    - path: tests/approvals/phase13_eval_manifest.json
      provides: "Phase 13 approval contract eval metadata"
      contains: "approval-contract-phase13.v1"
---

<objective>
Create the final Phase 13 coverage matrix, eval manifest, and verification gate record.

Purpose: make readiness explicit after the schema, service, API, resume, event, SLA-disabled scanner, and legacy quarantine plans have completed.
Output: `13-COVERAGE.md`, `phase13_eval_manifest.json`, and recorded verification command statuses.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-approval-state-machine/13-CONTEXT.md
@.planning/phases/13-approval-state-machine/13-RESEARCH.md
@.planning/phases/13-approval-state-machine/13-VALIDATION.md
@.planning/phases/13-approval-state-machine/13-PATTERNS.md
@.planning/phases/13-approval-state-machine/13-MIGRATION-REPORT.md
@docs/phase-13-17-architecture-plan.md
@docs/agent-architecture-phase-decomposition.md
</context>

<planning_gate>
## Spec Consistency Findings

- Implemented fact before this plan: Phase 13 execution spans multiple files and compatibility boundaries, so readiness cannot be inferred from a single focused test.
- Target contract: the coverage artifact must tie APPROVAL-01, APPROVAL-02, APPROVAL-03, and SNAPSHOT-01 to concrete tests, migration status, compatibility disposition, and deferred owner gates.
- MVP scope: Phase 13 may defer active SLA scanning and replay enrichment only when the row names the owner phase, dependency, non-blocking rationale, and acceptance gate.
- Spec reconciliation required before ready: coverage must record the Section 15.3 vs 18.2 `decision_type` enum conflict, the approval event field reconciliation (`metadata_json` plus redacted resource refs), and the missing upstream `action_safety_snapshot.v1` golden digest.

## Architecture Alignment

Canonical owner package: `src/approvals`; schema owner: `src/db/models.py`, migration `008_approval_state_machine`, and Phase 13 coverage/eval artifacts for readiness evidence.
Old-path strategy: final coverage records whether `src/repositories/approval_repo.py` was deleted or quarantined and blocks readiness if legacy transition imports remain.
Forbidden imports: final coverage must cite boundary tests proving routers, agent routers, graph nodes, and tests do not import legacy transition paths.
Boundary tests: `tests/architecture/test_approval_boundaries.py` and focused approval suites are required evidence before readiness can be claimed.
Non-overlap: the matrix must name Phase 14/15/16/17 owners for every deferred item and cannot mark those as Phase 13 complete work.
This plan closes the architecture audit loop required by `docs/phase-13-17-architecture-plan.md`: after ownership moves to `src/approvals`, the final artifact must prove that router, graph, replay, and legacy compatibility boundaries are covered or explicitly deferred with owners.

## Migration / Read-Switch / Rollback

`13-COVERAGE.md` must record the final Alembic head, migration/report status, legacy backfill disposition, read-switch status, and rollback note. Rollback requires downgrading schema to `007_session_memories` and restoring v1 router/repository behavior only if no v2 approvals were authorized; otherwise v2 rows remain non-executable until resolved.

## Compatibility-Layer Disposition

| Path | Owner | Forbidden New References | Protection Test | Deletion/Gate |
|------|-------|--------------------------|-----------------|---------------|
| `src/repositories/approval_repo.py` | Compatibility shim or deleted | Routers, agent routers, graph nodes, and tests cannot import transition methods | `tests/architecture/test_approval_boundaries.py` | Delete in Phase 13 or leave only read/list shim with explicit exception |
| `ApprovalRepository.decide` and `mark_expired` | Obsolete v1 transition API | No new references anywhere outside optional shim-internal calls | `tests/architecture/test_approval_boundaries.py` | Removed or private in Phase 13 |
| `ApprovalStep` | Trace fallback only | No target transition/event writes | tests verify new `approval_events` path | Phase 15 replay migration |
| v1 approval tests | Plan 13-07 | Cannot assert v1 idempotent approve/reject as target truth | rewritten tests | Rewritten before this plan |

## Cross-Table Enforcement Row

Final coverage must state that mismatch tests for `approval_decisions -> approval_assignments -> approval_levels -> approval_requests` exist and include wrong assignment-level, wrong level-request, wrong tenant, wrong run, wrong revision, stale request/level/assignment versions, and rollback/no orphan assertions.

## Follow-Up Coverage Matrix Rules

Allowed statuses: `COVERED`, `PARTIAL`, `DEFERRED_WITH_OWNER`, `MISSING`. `N/A` may appear only in owner/impact/eval/read-switch fields with a reason. A relevant `MISSING` row blocks execution readiness.
</planning_gate>

<threat_model>
| Threat | Severity | Mitigation in this plan | Verification |
|--------|----------|-------------------------|--------------|
| Relevant requirement row is untested | high | requirement coverage table maps each required ID to tests and owning plan | read `13-COVERAGE.md` |
| Deferred work hides a blocking gap | high | each PARTIAL or DEFERRED_WITH_OWNER row names owner, dependency, rationale, and acceptance gate | grep/read coverage rows |
| Eval contract is non-blocking or unowned | medium | eval manifest includes dataset owner, version, hash, blocking status, failure impact, requirements, and commands | read JSON |
| Final gate is claimed without command evidence | high | coverage records PASS, FAIL, or NOT_RUN for every final command and blocking follow-ups for failures | read verification table |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 13 coverage and readiness matrix</name>
  <files>.planning/phases/13-approval-state-machine/13-COVERAGE.md, tests/approvals/phase13_eval_manifest.json</files>
  <read_first>
    - .planning/REQUIREMENTS.md.
    - .planning/phases/13-approval-state-machine/13-CONTEXT.md.
    - .planning/phases/13-approval-state-machine/13-VALIDATION.md.
    - .planning/phases/13-approval-state-machine/13-MIGRATION-REPORT.md.
    - docs/agent-architecture-phase-decomposition.md follow-up rows.
  </read_first>
  <action>
    Create `tests/approvals/phase13_eval_manifest.json` with keys:
    `manifest_version`, `dataset_version`, `dataset_hash`, `owner_phase`, `blocking_status`, `failure_impact`, `requirements`, and `test_commands`.
    Use `dataset_version="approval-contract-phase13.v1"`, `owner_phase="Phase 13"`, `blocking_status="blocking_for_phase_exit"`, `failure_impact="phase_13_not_ready_for_execution"`, and include APPROVAL-01, APPROVAL-02, APPROVAL-03, SNAPSHOT-01.
    Use `dataset_hash="sha256:placeholder_until_cases_frozen"` only if execution has not created frozen cases yet, and record that placeholder as `PARTIAL` in coverage with owner Phase 13 before marking final ready.
    Create `.planning/phases/13-approval-state-machine/13-COVERAGE.md` with tables:
    - Requirement coverage for APPROVAL-01, APPROVAL-02, APPROVAL-03, SNAPSHOT-01, each with status `COVERED` or `PARTIAL` plus test command and owning plan.
    - Follow-up register for Phase 13 internal slices 13a/13b/13c, SLA scanner, cross-table enforcement row mapping, eval gates, and Phase 14/15/16/17 deferred boundaries.
    - Spec reconciliation register with rows:
      - `P13-SCF-01`: `approval_decision_command.v1` enum omits `approve` in Section 15.3 while Section 18.2/route/event contracts include it; Phase 13 uses `accept|approve|edit|respond|reject|ignore`, with blueprint update owner.
      - `P13-SCF-02`: `approval_events` carries Section 18.2 `actor_id`/`metadata_json` and minimal-envelope `resource_refs_json`/`redacted_payload_json`; Phase 15 must consume the recorded shape.
      - `P13-SCF-03`: spec lacks authoritative `action_safety_snapshot.v1` golden digest; Phase 13 freezes canonical JSON/hash input and records blueprint update owner.
    - Compatibility disposition copied from this plan with actual final status.
    - Verification commands and pass/fail placeholders.
    Ensure no relevant row has status `MISSING`. If any item is not implemented because it belongs to Phase 14/15/16/17, use `DEFERRED_WITH_OWNER` with owner phase, non-blocking rationale, dependency, and acceptance gate.
  </action>
  <verify>
    <automated>rg -n "APPROVAL-01|APPROVAL-02|APPROVAL-03|SNAPSHOT-01|DEFERRED_WITH_OWNER|Phase 15|Phase 14|MISSING|approval-contract-phase13.v1|blocking_for_phase_exit" .planning/phases/13-approval-state-machine/13-COVERAGE.md tests/approvals/phase13_eval_manifest.json</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "APPROVAL-01|APPROVAL-02|APPROVAL-03|SNAPSHOT-01" .planning/phases/13-approval-state-machine/13-COVERAGE.md` returns matches.
    - `rg -n "\\| MISSING \\|" .planning/phases/13-approval-state-machine/13-COVERAGE.md` returns no matches for relevant rows.
    - `rg -n "SLA scanner|Phase 15|DEFERRED_WITH_OWNER|acceptance gate" .planning/phases/13-approval-state-machine/13-COVERAGE.md` returns matches.
    - `rg -n "P13-SCF-01|P13-SCF-02|P13-SCF-03|approval_decision_command|metadata_json|action_safety_snapshot" .planning/phases/13-approval-state-machine/13-COVERAGE.md` returns matches.
    - `rg -n "approval-contract-phase13.v1|blocking_for_phase_exit|phase_13_not_ready_for_execution" tests/approvals/phase13_eval_manifest.json` returns matches.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run final Phase 13 verification gate</name>
  <files>.planning/phases/13-approval-state-machine/13-COVERAGE.md</files>
  <read_first>
    - .planning/phases/13-approval-state-machine/13-COVERAGE.md.
    - .planning/phases/13-approval-state-machine/13-VALIDATION.md.
    - .planning/phases/13-approval-state-machine/13-MIGRATION-REPORT.md.
  </read_first>
  <action>
    Run and record results in `13-COVERAGE.md`:
    - `uv run alembic upgrade head`.
    - `uv run pytest tests/approvals tests/architecture/test_approval_boundaries.py tests/test_approval_api.py tests/test_approval_integration.py tests/test_approval_models.py tests/test_approval_gate.py tests/test_execute_action.py tests/test_graph_routing.py tests/agent/test_events.py -q --tb=short`.
    - `uv run pytest -q --tb=short`.
    - `uv run ruff check src tests`.
    If any command fails, record exact failing command and blocker in the coverage file instead of claiming ready.
  </action>
  <verify>
    <automated>uv run alembic upgrade head && uv run pytest tests/approvals tests/architecture/test_approval_boundaries.py tests/test_approval_api.py tests/test_approval_integration.py tests/test_approval_models.py tests/test_approval_gate.py tests/test_execute_action.py tests/test_graph_routing.py tests/agent/test_events.py -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `13-COVERAGE.md` records each final command with status `PASS`, `FAIL`, or `NOT_RUN`.
    - If any final command is `FAIL` or `NOT_RUN`, the file includes a `Blocking follow-up` row with owner and exact command.
    - The focused automated command exits 0 before phase is marked ready.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run:
1. `uv run alembic upgrade head`
2. `uv run pytest tests/approvals tests/architecture/test_approval_boundaries.py tests/test_approval_api.py tests/test_approval_integration.py tests/test_approval_models.py tests/test_approval_gate.py tests/test_execute_action.py tests/test_graph_routing.py tests/agent/test_events.py -q --tb=short`
3. `uv run pytest -q --tb=short`
4. `uv run ruff check src tests`
</verification>

<success_criteria>
- Coverage file shows no relevant MISSING rows.
- Phase 13 eval manifest is owned, versioned, requirement-scoped, and blocking for phase exit.
- Every partial or deferred item has an owner, dependency, rationale, and acceptance gate.
- Final verification commands are recorded as PASS, FAIL, or NOT_RUN with blocking follow-ups when needed.
</success_criteria>
