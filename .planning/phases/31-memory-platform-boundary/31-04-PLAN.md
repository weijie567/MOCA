---
phase: 31-memory-platform-boundary
plan: 31-04
type: execute
wave: 2
depends_on:
  - 31-03
files_modified:
  - src/agent/state.py
  - src/agent/nodes/receive_request.py
  - src/agent/nodes/session_context_load.py
  - src/agent/nodes/session_memory_load.py
autonomous: true
requirements:
  - APF-09
  - APF-10
requirements_addressed:
  - APF-09
  - APF-10
user_setup: []
must_haves:
  truths:
    - "`session_context_load` exposes early same-thread context for intent/slot continuity without overriding explicit current-turn input."
    - "`session_memory_load` remains a compatibility wrapper or alias while target `session_context_*` fields are populated."
    - "Per-turn state reset clears target session-context and reviewed memory-context fields alongside legacy session-memory and long-term/case fields."
  artifacts:
    - path: "src/agent/state.py"
      provides: "Target AgentState fields for session_context, session_context_bundle, session_context_load_status, memory_context, memory_context_bundle, and reviewed_memory_context_retrieve_status"
    - path: "src/agent/nodes/receive_request.py"
      provides: "Per-turn reset for target session-context and reviewed memory-context fields"
    - path: "src/agent/nodes/session_context_load.py"
      provides: "Target graph-facing same-thread context load node"
    - path: "src/agent/nodes/session_memory_load.py"
      provides: "Legacy compatibility wrapper around session_context_load"
  key_links:
    - from: "src/agent/nodes/session_context_load.py"
      to: "src/memory/context_service.py"
      via: "MemoryContextService.load_session_context_for_intent"
      pattern: "load_session_context_for_intent"
    - from: "src/agent/nodes/session_memory_load.py"
      to: "src/agent/nodes/session_context_load.py"
      via: "compatibility call/import"
      pattern: "session_context_load"
    - from: "src/agent/nodes/receive_request.py"
      to: "src/agent/state.py"
      via: "target ephemeral reset fields"
      pattern: "session_context|memory_context|reviewed_memory_context_retrieve_status"
---

<objective>
Implement the early session context graph-facing boundary for APF-09.

Purpose: expose `SessionContextMemory` to the agent while keeping `SessionContinuityStore` and legacy `session_memory` fields as compatibility/internal storage concerns.
Output: target node/state fields with compatibility aliases and reset behavior.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-memory-platform-boundary/31-CONTEXT.md
@.planning/phases/31-memory-platform-boundary/31-RESEARCH.md
@.planning/phases/31-memory-platform-boundary/31-PATTERNS.md
@.planning/phases/31-memory-platform-boundary/31-VALIDATION.md
@.planning/phases/24.2-unified-session-memory-bundle-read-path/24.2-01-SUMMARY.md
@src/agent/state.py
@src/agent/nodes/receive_request.py
@src/agent/nodes/session_memory_load.py
@src/memory/context_service.py
@src/memory/session_bundle.py
@tests/agent/test_session_memory_load.py
@tests/agent/test_nodes/test_receive_request.py

Scope guardrails:
- Do not perform the full Phase 32 graph migration.
- Do not remove `session_memory` or `session_memory_bundle` from state.
- Do not change routing literals in `src/agent/routing.py` or graph registration unless a focused test requires it; wrappers satisfy Phase 31 compatibility.
- Do not let session context provide business facts, evidence refs, approval/action refs, or replay truth.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add target session-context and memory-context state fields and per-turn reset</name>
  <files>
src/agent/state.py
src/agent/nodes/receive_request.py
  </files>
  <read_first>
src/agent/state.py
src/agent/nodes/receive_request.py
tests/agent/test_nodes/test_receive_request.py
.planning/phases/31-memory-platform-boundary/31-CONTEXT.md
.planning/phases/31-memory-platform-boundary/31-PATTERNS.md
docs/contract-spec.md section 10
  </read_first>
  <action>
Add these optional ephemeral fields to `AgentState` in `src/agent/state.py` near the existing legacy memory fields:
- `session_context: dict[str, Any] | None`
- `session_context_bundle: dict[str, Any] | None`
- `session_context_load_status: dict[str, Any] | None`
- `memory_context: dict[str, Any] | None`
- `memory_context_bundle: dict[str, Any] | None`
- `reviewed_memory_context_retrieve_status: dict[str, Any] | None`

Update `src/agent/nodes/receive_request.py` so each new field is reset to `None` at the start of every turn, in the same reset block that currently clears `session_memory`, `session_memory_bundle`, `long_term_memory`, `case_memory`, and `memory_write_result`.

Do not remove or rename legacy fields. Do not add persistence behavior.
  </action>
  <acceptance_criteria>
    - `rg -n "session_context: dict\\[str, Any\\] \\| None|session_context_bundle: dict\\[str, Any\\] \\| None|session_context_load_status: dict\\[str, Any\\] \\| None" src/agent/state.py` finds all target fields.
    - `rg -n "memory_context: dict\\[str, Any\\] \\| None|memory_context_bundle: dict\\[str, Any\\] \\| None|reviewed_memory_context_retrieve_status: dict\\[str, Any\\] \\| None" src/agent/state.py` finds all target reviewed memory-context fields.
    - `rg -n "\"session_context\": None|\"session_context_bundle\": None|\"session_context_load_status\": None|\"memory_context\": None|\"memory_context_bundle\": None|\"reviewed_memory_context_retrieve_status\": None" src/agent/nodes/receive_request.py` finds reset entries.
    - `rg -n "session_memory: dict\\[str, Any\\] \\| None|session_memory_bundle: dict\\[str, Any\\] \\| None" src/agent/state.py` still finds legacy fields.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/agent/test_nodes/test_receive_request.py -q</automated>
    <automated>uv run ruff check src/agent/state.py src/agent/nodes/receive_request.py tests/agent/test_nodes/test_receive_request.py</automated>
  </verify>
  <done>AgentState and receive_request reset include target session-context and reviewed memory-context fields while preserving legacy fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement session_context_load and legacy session_memory_load wrapper</name>
  <files>
src/agent/nodes/session_context_load.py
src/agent/nodes/session_memory_load.py
  </files>
  <read_first>
src/agent/nodes/session_context_load.py
src/agent/nodes/session_memory_load.py
tests/agent/test_session_memory_load.py
tests/memory/test_session_memory_isolation.py
src/memory/context_service.py
src/memory/session_bundle.py
src/memory/service.py
.planning/phases/31-memory-platform-boundary/31-CONTEXT.md
.planning/phases/31-memory-platform-boundary/31-PATTERNS.md
  </read_first>
  <action>
Create `src/agent/nodes/session_context_load.py` by moving the current `session_memory_load` implementation shape to a target function named `session_context_load(state: AgentState, config: RunnableConfig) -> dict`.

Concrete target result:
- On success, return `session_context`, `session_context_bundle`, `session_context_load_status`, legacy `session_memory`, legacy `session_memory_bundle`, and appended `trace_steps`.
- `session_context` must be the target same-thread projection derived from `bundle.slot_continuity`, not a raw repository row.
- `session_context_bundle["schema_version"] == "session_context_bundle.v1"`.
- `session_context_load_status["schema_version"] == "session_context_load_status.v1"`.
- `session_context_load_status["authority_class"] == "contextual_only"`.
- Legacy `session_memory` equals the slot-continuity view currently returned by `session_memory_load`.
- Legacy `session_memory_bundle` remains the existing `SessionMemoryBundle.model_dump(mode="json")`.
- Trace step node name for the target node is `"session_context_load"`; if the legacy wrapper is called, it may preserve existing `"session_memory_load"` trace naming only if existing tests require it.
- When current explicit or trusted merchant context is merchant B, filter out merchant A session continuity from `session_context` and prompt-facing session context: active slots, rolling summary text, recent messages, and tool summaries from merchant A must not appear. Record deterministic status/filter metadata such as `"cross_merchant_session_context_filtered"` without overriding explicit current-turn merchant B input.

Update `src/agent/nodes/session_memory_load.py` to become a compatibility wrapper that imports/calls `session_context_load` and returns legacy fields plus target fields. Preserve current fallback reasons exactly: `"disabled"`, `"missing_async_session"`, `"missing_session_memory_bundle"`, and `"unavailable"`.

Tests must prove the production-compatible wrapper path currently used by the graph also returns `session_context`, `session_context_bundle`, and `session_context_load_status`; do not only test direct calls to the new target function.
  </action>
  <acceptance_criteria>
    - `test -f src/agent/nodes/session_context_load.py` succeeds.
    - `rg -n "async def session_context_load|session_context_load_status|session_context_bundle|authority_class.*contextual_only" src/agent/nodes/session_context_load.py` finds target node implementation.
    - `rg -n "from src\\.agent\\.nodes\\.session_context_load import session_context_load|session_context_load\\(" src/agent/nodes/session_memory_load.py` finds wrapper delegation.
    - `rg -n "missing_async_session|missing_session_memory_bundle|unavailable|disabled" src/agent/nodes/session_context_load.py src/agent/nodes/session_memory_load.py` finds preserved fallback reasons.
    - `rg -n "cross_merchant_session_context_filtered|merchant_scope_denied|explicit.*merchant" src/agent/nodes/session_context_load.py src/agent/nodes/session_memory_load.py` finds cross-merchant session filtering logic.
    - `rg -n "\"session_memory\"|\"session_memory_bundle\"" src/agent/nodes/session_context_load.py src/agent/nodes/session_memory_load.py` finds legacy compatibility output.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/agent/test_session_memory_load.py tests/memory/test_session_memory_bundle.py tests/memory/test_session_memory_isolation.py tests/agent/test_empty_session_adapter.py -q</automated>
    <automated>uv run ruff check src/agent/nodes/session_context_load.py src/agent/nodes/session_memory_load.py tests/agent/test_session_memory_load.py tests/memory/test_session_memory_bundle.py tests/memory/test_session_memory_isolation.py tests/agent/test_empty_session_adapter.py</automated>
  </verify>
  <done>Target `session_context_load` exists, legacy wrapper still works, and target/legacy session-context fields are returned together.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session continuity store -> agent state | Storage-backed slot continuity becomes agent-facing session context but remains contextual-only. |
| receive_request -> checkpointed state | Per-turn reset prevents stale session context from leaking into new turns. |
| target node -> legacy wrapper | Compatibility wrapper must not drop target status metadata or widen authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-cross-merchant | I | session context load | mitigate | Session context remains tenant/user/thread scoped through existing `SessionMemoryBundleService` inputs and reset fields. |
| T-31-authority-forgery | S/T | session_context fields | mitigate | Session context status carries `authority_class="contextual_only"` and no evidence/business/action/replay refs. |
| T-31-pii-leakage | I | session context bundle | mitigate | Node uses existing prompt-safe bundle/service projections, not raw repository rows. |
| T-31-tombstone-revival | T/R | session context node | accept | Tombstone lifecycle is reviewed-memory scope; Plan 31-05/31-06 mitigate it. Session context plan does not write long-term/case records. |
| T-31-replay-confusion | R/T | session_context_load_status | mitigate | Status ref is contextual-only and not a replay event. |
| T-31-write-rollback | D/T | session context load | accept | This plan does not write memory; Plan 31-06 owns write rollback/status mitigation. |
</threat_model>

<verification>
Run focused session-context tests serially. If any command fails because of local DB fixture/environment behavior, record the handled issue in `.planning/LOCAL-VALIDATION-ISSUES.md` in Chinese.
</verification>

<success_criteria>
- `AgentState` and `receive_request` reset target session-context and reviewed memory-context fields.
- `session_context_load` returns target fields and legacy aliases.
- The graph-facing legacy wrapper path returns the same target session-context fields while Phase 32 still owns full graph vocabulary migration.
- Merchant A session slots, rolling summaries, recent messages, and tool summaries cannot enter merchant B prompt context.
- Existing session memory tests still pass.
- No storage contracts or migrations are renamed.
</success_criteria>

<output>
After completion, create `.planning/phases/31-memory-platform-boundary/31-04-SUMMARY.md`.
</output>
