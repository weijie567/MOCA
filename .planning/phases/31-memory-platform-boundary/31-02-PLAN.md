---
phase: 31-memory-platform-boundary
plan: 31-02
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/agent/test_reviewed_memory_context_retrieve.py
  - tests/memory/test_reviewed_memory_context_boundary.py
  - tests/memory/test_session_memory_isolation.py
  - tests/agent/test_memory_write_node.py
autonomous: true
requirements:
  - APF-10
requirements_addressed:
  - APF-10
user_setup: []
must_haves:
  truths:
    - "Wave 0 RED tests prove reviewed long-term/case memory retrieval fails closed without trusted tenant and merchant/resource scope."
    - "Wave 0 RED tests prove one merchant's session slots, rolling summaries, recent messages, tool summaries, long-term memory, or case memory cannot enter another merchant's prompt context."
    - "Wave 0 RED tests pin `memory_write_decision.v2` status metadata and lifecycle exclusion behavior."
  artifacts:
    - path: "tests/agent/test_reviewed_memory_context_retrieve.py"
      provides: "Target node tests for reviewed memory trusted-scope fail-closed behavior"
    - path: "tests/memory/test_reviewed_memory_context_boundary.py"
      provides: "Cross-merchant and lifecycle retrieval boundary tests"
    - path: "tests/memory/test_session_memory_isolation.py"
      provides: "Cross-merchant session slot, rolling summary, recent message, and tool summary prompt-contamination tests"
    - path: "tests/agent/test_memory_write_node.py"
      provides: "Memory write decision status RED assertions"
  key_links:
    - from: "tests/agent/test_reviewed_memory_context_retrieve.py"
      to: "src/agent/nodes/reviewed_memory_context_retrieve.py"
      via: "planned target node import"
      pattern: "reviewed_memory_context_retrieve"
    - from: "tests/memory/test_reviewed_memory_context_boundary.py"
      to: "src/platform/trusted_context.py"
      via: "TrustedContext/MerchantScopeV1 scoped fixtures"
      pattern: "TrustedContext|MerchantScopeV1"
    - from: "tests/memory/test_session_memory_isolation.py"
      to: "src/memory/session_bundle.py"
      via: "SessionMemoryBundleService prompt-context projection under explicit merchant switch"
      pattern: "merchant-a|merchant-b|rolling_summary|tool_summaries"
    - from: "tests/agent/test_memory_write_node.py"
      to: "src/agent/nodes/memory_write.py"
      via: "memory_write_decision output assertions"
      pattern: "memory_write_decision"
---

<objective>
Create the second Wave 0 RED test slice for reviewed memory retrieval, merchant isolation, lifecycle filters, and memory write decision status.

Purpose: APF-10 is safety-critical; the phase must fail closed before implementation can introduce prompt-context leakage or memory authority.
Output: failing tests that drive Plan 31-05 and Plan 31-06.
</objective>

<execution_context>
@/Users/ming/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ming/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-memory-platform-boundary/31-CONTEXT.md
@.planning/phases/31-memory-platform-boundary/31-RESEARCH.md
@.planning/phases/31-memory-platform-boundary/31-PATTERNS.md
@.planning/phases/31-memory-platform-boundary/31-VALIDATION.md
@.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-04-SUMMARY.md
@.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-06-SUMMARY.md
@.planning/phases/24.3-memory-write-isolation-policy-and-observability-mvp/24.3-01-SUMMARY.md
@docs/contract-spec.md
@src/platform/trusted_context.py
@src/agent/nodes/long_term_memory_retrieve.py
@src/agent/nodes/memory_write.py
@src/memory/long_term.py
@src/memory/case_memory.py
@src/memory/tombstones.py
@src/memory/write_isolation.py

Scope guardrails:
- Do not edit production `src/` files in this plan.
- Do not enable tenant/global memory retrieval.
- Do not implement review queue UI, DB/RLS redesign, or full replay event coverage.
- Use `uv run pytest ...`; do not use bare pytest.
- Run DB-backed tests serially.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED reviewed-memory trusted-scope and merchant-isolation tests</name>
  <files>
tests/agent/test_reviewed_memory_context_retrieve.py
tests/memory/test_reviewed_memory_context_boundary.py
tests/memory/test_session_memory_isolation.py
  </files>
  <read_first>
tests/agent/test_reviewed_memory_context_retrieve.py
tests/memory/test_reviewed_memory_context_boundary.py
.planning/phases/31-memory-platform-boundary/31-CONTEXT.md
.planning/phases/31-memory-platform-boundary/31-RESEARCH.md
.planning/phases/31-memory-platform-boundary/31-VALIDATION.md
.planning/phases/29.5-merchant-scope-role-model-alignment/29.5-04-SUMMARY.md
src/platform/trusted_context.py
src/agent/nodes/long_term_memory_retrieve.py
src/memory/long_term.py
src/memory/case_memory.py
tests/memory/test_session_memory_isolation.py
tests/memory/test_case_memory_retrieval.py
  </read_first>
  <action>
Create `tests/agent/test_reviewed_memory_context_retrieve.py` with RED tests for the planned `src.agent.nodes.reviewed_memory_context_retrieve.reviewed_memory_context_retrieve` node.

Required exact tests:
- `test_reviewed_memory_context_retrieve_fails_closed_without_trusted_context`: with `config={"configurable": {"session": object()}}`, result must include `memory_context == {"schema_version": "reviewed_memory_context_bundle.v1", "long_term_items": [], "case_items": []}` and `reviewed_memory_context_retrieve_status["fallback_reason"] == "missing_trusted_context"`.
- `test_reviewed_memory_context_retrieve_fails_closed_without_actor_merchant_scope`: with `TrustedContext(merchant_scope=MerchantScopeV1(merchant_ids=[]))`, result status must be `status == "skipped"`, `fallback_reason == "missing_actor_merchant_scope"`, and no service fake is called.
- `test_reviewed_memory_context_retrieve_denies_out_of_scope_merchant`: with explicit slot `merchant_id="merchant-b"` and trusted scope `["merchant-a"]`, result status must include `filter_reasons` containing `"merchant_scope_denied:merchant-b"`.
- `test_reviewed_memory_context_retrieve_does_not_use_session_memory_to_create_scope`: if `state["session_memory"]["active_slots"]["merchant_id"] == "merchant-b"` but current explicit slots do not include merchant and trusted business context is absent, no merchant scope is produced and status includes `"memory_scope_not_authority"`.
- `test_reviewed_memory_context_retrieve_keeps_legacy_aliases_empty_on_fail_closed`: fail-closed result must still return `long_term_memory == []` and `case_memory == []` for compatibility.

Create `tests/memory/test_reviewed_memory_context_boundary.py` with DB-backed serial tests using existing repositories/services:
- `test_reviewed_memory_context_excludes_cross_merchant_long_term_and_case_memory`: merchant A actor cannot retrieve merchant B long-term/case memory; status filter reasons include `"merchant_scope_denied"`.
- `test_reviewed_memory_context_rejects_global_or_tenant_scope_without_allowlist`: tenant/global scope request returns empty bundle and `fallback_reason == "tenant_global_memory_unsupported"`.
- `test_reviewed_memory_context_excludes_deleted_expired_rejected_superseded_needs_review_and_non_prompt_safe_pii`: deleted, expired, rejected, superseded, needs_review, and `pii_classification in {"sensitive", "prohibited"}` records do not appear in `memory_context.long_term_items` or `memory_context.case_items`.

Extend `tests/memory/test_session_memory_isolation.py` with RED tests proving session context is not a cross-merchant prompt-contamination channel:
- `test_session_context_excludes_cross_merchant_slots_summary_messages_and_tool_summaries`: seed a same-tenant/user/thread continuity bundle or conversation prompt context containing merchant A active slots, rolling summary, recent messages, and prompt-safe tool summaries; when the current explicit trusted merchant context is merchant B, the projected `session_context` / prompt context must not contain merchant A identifiers, merchant A order IDs, merchant A rolling summary text, merchant A recent message text, or merchant A tool summary text.
- `test_session_context_does_not_let_merchant_a_active_slot_override_merchant_b_current_turn`: with a stored active slot `merchant_id="merchant-a"` and current explicit slot `merchant_id="merchant-b"`, the target projection must keep merchant B and record a deterministic filter/fallback reason such as `"cross_merchant_session_context_filtered"` for the dropped merchant A continuity.
- Keep the existing user/thread isolation tests intact; these new tests must cover merchant contamination inside the same tenant/user/thread path, not only cross-user or cross-thread isolation.

Use `TrustedContextFactory` or `TrustedContext` and `MerchantScopeV1` directly in fixtures; do not fabricate wildcard merchant scope for support/manager users.
  </action>
  <acceptance_criteria>
    - `rg -n "missing_trusted_context|missing_actor_merchant_scope|merchant_scope_denied|memory_scope_not_authority|tenant_global_memory_unsupported" tests/agent/test_reviewed_memory_context_retrieve.py tests/memory/test_reviewed_memory_context_boundary.py` finds all required fail-closed reasons.
    - `rg -n "reviewed_memory_context_retrieve_status|reviewed_memory_context_bundle\\.v1|long_term_items|case_items" tests/agent/test_reviewed_memory_context_retrieve.py tests/memory/test_reviewed_memory_context_boundary.py` finds target structured bundle/status assertions.
    - `rg -n "merchant-b|merchant-a|MerchantScopeV1|TrustedContext" tests/agent/test_reviewed_memory_context_retrieve.py tests/memory/test_reviewed_memory_context_boundary.py` finds merchant isolation fixtures.
    - `rg -n "cross_merchant_session_context_filtered|rolling_summary|recent_messages|tool_summaries|active_slots" tests/memory/test_session_memory_isolation.py` finds session prompt-contamination assertions.
    - `rg -n "merchant-b|merchant-a|MerchantScopeV1|TrustedContext" tests/agent/test_reviewed_memory_context_retrieve.py tests/memory/test_reviewed_memory_context_boundary.py tests/memory/test_session_memory_isolation.py` finds merchant isolation fixtures.
    - `rg -n "xfail|skip\\(" tests/agent/test_reviewed_memory_context_retrieve.py tests/memory/test_reviewed_memory_context_boundary.py tests/memory/test_session_memory_isolation.py` returns no matches introduced by this task.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set +e; uv run pytest tests/agent/test_reviewed_memory_context_retrieve.py tests/memory/test_reviewed_memory_context_boundary.py tests/memory/test_session_memory_isolation.py -q; status=$?; test "$status" -ne 0'</automated>
    <automated>uv run ruff check tests/agent/test_reviewed_memory_context_retrieve.py tests/memory/test_reviewed_memory_context_boundary.py tests/memory/test_session_memory_isolation.py</automated>
  </verify>
  <done>Reviewed-memory trusted-scope, cross-merchant, tenant/global unsupported, lifecycle exclusion, and session prompt-contamination RED tests exist and fail for missing planned implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RED memory write decision status tests</name>
  <files>
tests/agent/test_memory_write_node.py
tests/memory/test_reviewed_memory_context_boundary.py
  </files>
  <read_first>
tests/agent/test_memory_write_node.py
tests/memory/test_reviewed_memory_context_boundary.py
.planning/phases/31-memory-platform-boundary/31-CONTEXT.md
.planning/phases/31-memory-platform-boundary/31-VALIDATION.md
.planning/phases/24.3-memory-write-isolation-policy-and-observability-mvp/24.3-01-SUMMARY.md
src/agent/nodes/memory_write.py
src/memory/service.py
src/memory/long_term.py
src/memory/case_memory.py
src/memory/write_isolation.py
  </read_first>
  <action>
Extend `tests/agent/test_memory_write_node.py` so existing session-memory write tests assert `memory_write_decision` next to legacy `memory_write_result`.

Required exact assertions:
- written path: `result["memory_write_decision"]["schema_version"] == "memory_write_decision.v2"`, `status == "written"`, `decision == "write"`, `memory_type == "session"`, `authority_class == "contextual_only"`, `candidate_hash` starts with `"sha256:"`, and `scope["thread_id"]` equals the test thread.
- skipped not-completed path: status is `"skipped"`, decision is `"skip"`, reason_code is `"not_completed_path"`, and legacy `memory_write_result["reason_code"] == "not_completed_path"` remains present.
- timeout path: status is `"skipped"`, reason_code is `"write_timeout"`, fallback_reason is `"write_timeout"`, final response is preserved, and main-path rollback is not required by memory failure.
- PII blocked path: status is `"skipped"`, decision is `"skip"`, pii_classification is `"prohibited"` or `"sensitive"` as existing test data dictates, reason_code is `"pii_blocked"`, and no session memory write is persisted.

In `tests/memory/test_reviewed_memory_context_boundary.py`, add RED lifecycle assertions for `MemoryWriteDecisionV2` projection over long-term/case service outcomes:
- needs_review maps to `status == "needs_review"`, `review_status == "needs_review"`, and prompt retrieval excludes the memory.
- tombstone/delete maps to `decision in {"tombstone", "delete"}` and a later candidate with the same content/source identity does not appear in reviewed prompt context.
- supersede maps to `decision == "supersede"` and only one current memory for the same tenant/scope/source identity remains prompt-facing.
  </action>
  <acceptance_criteria>
    - `rg -n "memory_write_decision|memory_write_decision\\.v2|authority_class.*contextual_only" tests/agent/test_memory_write_node.py tests/memory/test_reviewed_memory_context_boundary.py` finds write decision assertions.
    - `rg -n "write_timeout|pii_blocked|needs_review|tombstone|supersede" tests/agent/test_memory_write_node.py tests/memory/test_reviewed_memory_context_boundary.py` finds the lifecycle/failure cases.
    - `rg -n "memory_write_result" tests/agent/test_memory_write_node.py` confirms legacy compatibility assertions remain.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set +e; uv run pytest tests/agent/test_memory_write_node.py tests/memory/test_reviewed_memory_context_boundary.py -q; status=$?; test "$status" -ne 0'</automated>
    <automated>uv run ruff check tests/agent/test_memory_write_node.py tests/memory/test_reviewed_memory_context_boundary.py</automated>
  </verify>
  <done>Memory write decision, timeout/no-rollback, PII-block, needs-review, tombstone, and supersede RED tests exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TrustedContext -> reviewed memory retrieval | Trusted tenant/user/merchant scope controls whether long-term/case memory can enter prompt context. |
| memory stores -> prompt context | Long-term/case/session records must pass scope, review, PII, deletion, expiry, and tombstone filters before prompt exposure. |
| memory write side effect -> main response/action/approval path | Memory write failures must record status and not roll back the main path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-cross-merchant | I | reviewed retrieval tests | mitigate | RED tests require merchant A cannot retrieve merchant B long-term/case/session memory and no wildcard support/manager scope is fabricated. |
| T-31-authority-forgery | S/T | reviewed memory bundle/status | mitigate | RED tests require `authority_class="contextual_only"` and no reviewed memory output becomes evidence/business/action/replay authority. |
| T-31-pii-leakage | I | reviewed retrieval lifecycle tests | mitigate | RED tests require sensitive/prohibited PII and non-prompt-safe memory to be excluded from reviewed prompt context. |
| T-31-tombstone-revival | T/R | long-term/case lifecycle tests | mitigate | RED tests require tombstoned/deleted memory cannot be revived by later same-content/source candidates. |
| T-31-replay-confusion | R/T | reviewed status refs | mitigate | RED tests require status refs are contextual-only handoff metadata, not replay truth. |
| T-31-write-rollback | D/T | `memory_write` tests | mitigate | RED tests require timeout/error statuses preserve final response and expose `fallback_reason` without rolling back main path. |
</threat_model>

<verification>
Wave 0 RED verification is successful when new reviewed-memory and write-decision tests fail because planned production symbols/fields do not exist, ruff passes, and all pytest commands are serial.
</verification>

<success_criteria>
- Trusted-scope fail-closed reasons are pinned by RED tests.
- Cross-merchant reviewed memory contamination is pinned by RED tests.
- Memory write decision metadata and no-rollback behavior are pinned by RED tests.
- No production files are modified by this Wave 0 plan.
</success_criteria>

<output>
After completion, create `.planning/phases/31-memory-platform-boundary/31-02-SUMMARY.md`.
</output>
